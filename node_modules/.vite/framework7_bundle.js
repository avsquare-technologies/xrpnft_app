import {
  ConstructorMethods,
  ModalMethods,
  app_class_default,
  auroraPreloaderContent,
  bindMethods,
  class_default,
  clicks_default,
  colorHexToRgb,
  colorHsbToHsl,
  colorHslToHsb,
  colorHslToRgb,
  colorRgbToHex,
  colorRgbToHsl,
  component_class_default,
  component_default,
  component_loader_default,
  create_store_default,
  deleteProps,
  device_default,
  dom7_default,
  extend,
  getDevice,
  getSupport,
  getTranslate,
  history_default,
  id,
  iosPreloaderContent,
  jsx_default,
  mdPreloaderContent,
  modal_class_default,
  modal_default,
  navbar_default,
  nextFrame,
  nextTick,
  now,
  requestAnimationFrame,
  request_default,
  request_default2,
  resize_default,
  router_class_default,
  router_default,
  serializeObject,
  service_worker_default,
  statusbar_default,
  store_default,
  subnavbar_default,
  support_default,
  toolbar_default,
  touch_default,
  touch_ripple_default,
  utils_default,
  utils_exports,
  view_default
} from "./chunk-MM2RYTLR.js";
import {
  $,
  addClass,
  append,
  attr,
  children,
  closest,
  css,
  each,
  eq,
  filter,
  find,
  getDocument,
  getWindow,
  hasClass,
  html,
  index,
  is,
  next,
  nextAll,
  off,
  offset,
  on,
  outerHeight,
  outerWidth,
  parent,
  parents,
  prepend,
  prev,
  prevAll,
  remove,
  removeAttr,
  removeClass,
  styles,
  text,
  toggleClass,
  transform,
  transition,
  transitionEnd,
  trigger
} from "./chunk-WVCW7TPP.js";
import "./chunk-IGMYUX52.js";

// node_modules/framework7/esm/components/appbar/appbar.js
var appbar_default = {
  name: "appbar"
};

// node_modules/framework7/esm/shared/$jsx.js
var $jsx = function $jsx2(tag, props) {
  var attrs = props || {};
  for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }
  var children2 = args || [];
  var attrsString = Object.keys(attrs).map(function(attr2) {
    if (attr2[0] === "_") {
      if (attrs[attr2])
        return attr2.replace("_", "");
      return "";
    }
    return attr2 + '="' + attrs[attr2] + '"';
  }).filter(function(attr2) {
    return !!attr2;
  }).join(" ");
  if (["path", "img", "circle", "polygon", "line", "input"].indexOf(tag) >= 0) {
    return ("<" + tag + " " + attrsString + " />").trim();
  }
  var childrenContent = children2.filter(function(c) {
    return !!c;
  }).map(function(c) {
    return Array.isArray(c) ? c.join("") : c;
  }).join("");
  return ("<" + tag + " " + attrsString + ">" + childrenContent + "</" + tag + ">").trim();
};
var jsx_default2 = $jsx;

// node_modules/framework7/esm/components/dialog/dialog-class.js
function _assertThisInitialized(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf(subClass, superClass);
}
function _setPrototypeOf(o, p) {
  _setPrototypeOf = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf(o, p);
}
var Dialog = function(_Modal) {
  _inheritsLoose(Dialog2, _Modal);
  function Dialog2(app, params) {
    var _this;
    var extendedParams = extend({
      title: app.params.dialog.title,
      text: void 0,
      content: "",
      buttons: [],
      verticalButtons: false,
      onClick: void 0,
      cssClass: void 0,
      destroyOnClose: false,
      on: {}
    }, params);
    if (typeof extendedParams.closeByBackdropClick === "undefined") {
      extendedParams.closeByBackdropClick = app.params.dialog.closeByBackdropClick;
    }
    if (typeof extendedParams.backdrop === "undefined") {
      extendedParams.backdrop = app.params.dialog.backdrop;
    }
    _this = _Modal.call(this, app, extendedParams) || this;
    var dialog = _assertThisInitialized(_this);
    var device2 = getDevice();
    var document = getDocument();
    var title = extendedParams.title, text2 = extendedParams.text, content = extendedParams.content, buttons = extendedParams.buttons, verticalButtons = extendedParams.verticalButtons, cssClass = extendedParams.cssClass, backdrop = extendedParams.backdrop;
    dialog.params = extendedParams;
    var $el;
    if (!dialog.params.el) {
      var dialogClasses = ["dialog"];
      if (buttons.length === 0)
        dialogClasses.push("dialog-no-buttons");
      if (buttons.length > 0)
        dialogClasses.push("dialog-buttons-" + buttons.length);
      if (verticalButtons)
        dialogClasses.push("dialog-buttons-vertical");
      if (cssClass)
        dialogClasses.push(cssClass);
      var buttonsHTML = "";
      if (buttons.length > 0) {
        buttonsHTML = jsx_default2("div", {
          class: "dialog-buttons"
        }, buttons.map(function(button) {
          return jsx_default2("span", {
            class: "dialog-button" + (button.bold ? " dialog-button-bold" : "") + (button.color ? " color-" + button.color : "") + (button.cssClass ? " " + button.cssClass : "")
          }, button.text);
        }));
      }
      var dialogHtml = jsx_default2("div", {
        class: dialogClasses.join(" ")
      }, jsx_default2("div", {
        class: "dialog-inner"
      }, title && jsx_default2("div", {
        class: "dialog-title"
      }, title), text2 && jsx_default2("div", {
        class: "dialog-text"
      }, text2), content), buttonsHTML);
      $el = dom7_default(dialogHtml);
    } else {
      $el = dom7_default(dialog.params.el);
    }
    if ($el && $el.length > 0 && $el[0].f7Modal) {
      return $el[0].f7Modal || _assertThisInitialized(_this);
    }
    if ($el.length === 0) {
      return dialog.destroy() || _assertThisInitialized(_this);
    }
    var $backdropEl;
    if (backdrop) {
      $backdropEl = app.$el.children(".dialog-backdrop");
      if ($backdropEl.length === 0) {
        $backdropEl = dom7_default('<div class="dialog-backdrop"></div>');
        app.$el.append($backdropEl);
      }
    }
    function buttonOnClick(e) {
      var buttonEl = this;
      var index2 = dom7_default(buttonEl).index();
      var button = buttons[index2];
      if (button.onClick)
        button.onClick(dialog, e);
      if (dialog.params.onClick)
        dialog.params.onClick(dialog, index2);
      if (button.close !== false)
        dialog.close();
    }
    var addKeyboardHander;
    function onKeyDown(e) {
      var keyCode = e.keyCode;
      buttons.forEach(function(button, index2) {
        if (button.keyCodes && button.keyCodes.indexOf(keyCode) >= 0) {
          if (document.activeElement)
            document.activeElement.blur();
          if (button.onClick)
            button.onClick(dialog, e);
          if (dialog.params.onClick)
            dialog.params.onClick(dialog, index2);
          if (button.close !== false)
            dialog.close();
        }
      });
    }
    if (buttons && buttons.length > 0) {
      dialog.on("open", function() {
        $el.find(".dialog-button").each(function(buttonEl, index2) {
          var button = buttons[index2];
          if (button.keyCodes)
            addKeyboardHander = true;
          dom7_default(buttonEl).on("click", buttonOnClick);
        });
        if (addKeyboardHander && !device2.ios && !device2.android && !device2.cordova && !device2.capacitor) {
          dom7_default(document).on("keydown", onKeyDown);
        }
      });
      dialog.on("close", function() {
        $el.find(".dialog-button").each(function(buttonEl) {
          dom7_default(buttonEl).off("click", buttonOnClick);
        });
        if (addKeyboardHander && !device2.ios && !device2.android && !device2.cordova && !device2.capacitor) {
          dom7_default(document).off("keydown", onKeyDown);
        }
        addKeyboardHander = false;
      });
    }
    extend(dialog, {
      app,
      $el,
      el: $el[0],
      $backdropEl,
      backdropEl: $backdropEl && $backdropEl[0],
      type: "dialog",
      setProgress: function setProgress(progress, duration) {
        app.progressbar.set($el.find(".progressbar"), progress, duration);
        return dialog;
      },
      setText: function setText(newText) {
        var $textEl = $el.find(".dialog-text");
        if ($textEl.length === 0) {
          $textEl = dom7_default('<div class="dialog-text"></div>');
          if (typeof title !== "undefined") {
            $textEl.insertAfter($el.find(".dialog-title"));
          } else {
            $el.find(".dialog-inner").prepend($textEl);
          }
        }
        $textEl.html(newText);
        dialog.params.text = newText;
        return dialog;
      },
      setTitle: function setTitle(newTitle) {
        var $titleEl = $el.find(".dialog-title");
        if ($titleEl.length === 0) {
          $titleEl = dom7_default('<div class="dialog-title"></div>');
          $el.find(".dialog-inner").prepend($titleEl);
        }
        $titleEl.html(newTitle);
        dialog.params.title = newTitle;
        return dialog;
      }
    });
    function handleClick(e) {
      var target = e.target;
      var $target = dom7_default(target);
      if ($target.closest(dialog.el).length === 0) {
        if (dialog.params.closeByBackdropClick && dialog.backdropEl && dialog.backdropEl === target) {
          dialog.close();
        }
      }
    }
    dialog.on("opened", function() {
      if (dialog.params.closeByBackdropClick) {
        app.on("click", handleClick);
      }
    });
    dialog.on("close", function() {
      if (dialog.params.closeByBackdropClick) {
        app.off("click", handleClick);
      }
    });
    $el[0].f7Modal = dialog;
    if (dialog.params.destroyOnClose) {
      dialog.once("closed", function() {
        setTimeout(function() {
          dialog.destroy();
        }, 0);
      });
    }
    return dialog || _assertThisInitialized(_this);
  }
  return Dialog2;
}(modal_class_default);
var dialog_class_default = Dialog;

// node_modules/framework7/esm/components/dialog/dialog.js
function _extends() {
  _extends = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends.apply(this, arguments);
}
var dialog_default = {
  name: "dialog",
  params: {
    dialog: {
      title: void 0,
      buttonOk: "OK",
      buttonCancel: "Cancel",
      usernamePlaceholder: "Username",
      passwordPlaceholder: "Password",
      preloaderTitle: "Loading... ",
      progressTitle: "Loading... ",
      backdrop: true,
      closeByBackdropClick: false,
      destroyPredefinedDialogs: true,
      keyboardActions: true,
      autoFocus: true
    }
  },
  static: {
    Dialog: dialog_class_default
  },
  create: function create() {
    var app = this;
    function defaultDialogTitle() {
      return app.params.dialog.title || app.name;
    }
    var destroyOnClose = app.params.dialog.destroyPredefinedDialogs;
    var keyboardActions = app.params.dialog.keyboardActions;
    var autoFocus = app.params.dialog.autoFocus;
    var autoFocusHandler = autoFocus ? {
      on: {
        opened: function opened(dialog) {
          dialog.$el.find("input").eq(0).focus();
        }
      }
    } : {};
    app.dialog = extend(ModalMethods({
      app,
      constructor: dialog_class_default,
      defaultSelector: ".dialog.modal-in"
    }), {
      alert: function alert() {
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        var text2 = args[0], title = args[1], callbackOk = args[2];
        if (args.length === 2 && typeof args[1] === "function") {
          text2 = args[0];
          callbackOk = args[1];
          title = args[2];
        }
        return new dialog_class_default(app, {
          title: typeof title === "undefined" ? defaultDialogTitle() : title,
          text: text2,
          buttons: [{
            text: app.params.dialog.buttonOk,
            bold: true,
            onClick: callbackOk,
            keyCodes: keyboardActions ? [13, 27] : null
          }],
          destroyOnClose
        }).open();
      },
      prompt: function prompt() {
        for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
          args[_key2] = arguments[_key2];
        }
        var text2 = args[0], title = args[1], callbackOk = args[2], callbackCancel = args[3], defaultValue = args[4];
        if (typeof args[1] === "function") {
          text2 = args[0];
          callbackOk = args[1];
          callbackCancel = args[2];
          defaultValue = args[3];
          title = args[4];
        }
        defaultValue = typeof defaultValue === "undefined" || defaultValue === null ? "" : defaultValue;
        return new dialog_class_default(app, _extends({
          title: typeof title === "undefined" ? defaultDialogTitle() : title,
          text: text2,
          content: '<div class="dialog-input-field input"><input type="text" class="dialog-input" value="' + defaultValue + '"></div>',
          buttons: [{
            text: app.params.dialog.buttonCancel,
            keyCodes: keyboardActions ? [27] : null,
            color: app.theme === "aurora" ? "gray" : null
          }, {
            text: app.params.dialog.buttonOk,
            bold: true,
            keyCodes: keyboardActions ? [13] : null
          }],
          onClick: function onClick4(dialog, index2) {
            var inputValue = dialog.$el.find(".dialog-input").val();
            if (index2 === 0 && callbackCancel)
              callbackCancel(inputValue);
            if (index2 === 1 && callbackOk)
              callbackOk(inputValue);
          },
          destroyOnClose
        }, autoFocusHandler)).open();
      },
      confirm: function confirm() {
        for (var _len3 = arguments.length, args = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
          args[_key3] = arguments[_key3];
        }
        var text2 = args[0], title = args[1], callbackOk = args[2], callbackCancel = args[3];
        if (typeof args[1] === "function") {
          text2 = args[0];
          callbackOk = args[1];
          callbackCancel = args[2];
          title = args[3];
        }
        return new dialog_class_default(app, {
          title: typeof title === "undefined" ? defaultDialogTitle() : title,
          text: text2,
          buttons: [{
            text: app.params.dialog.buttonCancel,
            onClick: callbackCancel,
            keyCodes: keyboardActions ? [27] : null,
            color: app.theme === "aurora" ? "gray" : null
          }, {
            text: app.params.dialog.buttonOk,
            bold: true,
            onClick: callbackOk,
            keyCodes: keyboardActions ? [13] : null
          }],
          destroyOnClose
        }).open();
      },
      login: function login() {
        for (var _len4 = arguments.length, args = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
          args[_key4] = arguments[_key4];
        }
        var text2 = args[0], title = args[1], callbackOk = args[2], callbackCancel = args[3];
        if (typeof args[1] === "function") {
          text2 = args[0];
          callbackOk = args[1];
          callbackCancel = args[2];
          title = args[3];
        }
        return new dialog_class_default(app, _extends({
          title: typeof title === "undefined" ? defaultDialogTitle() : title,
          text: text2,
          content: '\n              <div class="dialog-input-field dialog-input-double input">\n                <input type="text" name="dialog-username" placeholder="' + app.params.dialog.usernamePlaceholder + '" class="dialog-input">\n              </div>\n              <div class="dialog-input-field dialog-input-double input">\n                <input type="password" name="dialog-password" placeholder="' + app.params.dialog.passwordPlaceholder + '" class="dialog-input">\n              </div>',
          buttons: [{
            text: app.params.dialog.buttonCancel,
            keyCodes: keyboardActions ? [27] : null,
            color: app.theme === "aurora" ? "gray" : null
          }, {
            text: app.params.dialog.buttonOk,
            bold: true,
            keyCodes: keyboardActions ? [13] : null
          }],
          onClick: function onClick4(dialog, index2) {
            var username = dialog.$el.find('[name="dialog-username"]').val();
            var password = dialog.$el.find('[name="dialog-password"]').val();
            if (index2 === 0 && callbackCancel)
              callbackCancel(username, password);
            if (index2 === 1 && callbackOk)
              callbackOk(username, password);
          },
          destroyOnClose
        }, autoFocusHandler)).open();
      },
      password: function password() {
        for (var _len5 = arguments.length, args = new Array(_len5), _key5 = 0; _key5 < _len5; _key5++) {
          args[_key5] = arguments[_key5];
        }
        var text2 = args[0], title = args[1], callbackOk = args[2], callbackCancel = args[3];
        if (typeof args[1] === "function") {
          text2 = args[0];
          callbackOk = args[1];
          callbackCancel = args[2];
          title = args[3];
        }
        return new dialog_class_default(app, _extends({
          title: typeof title === "undefined" ? defaultDialogTitle() : title,
          text: text2,
          content: '\n              <div class="dialog-input-field input">\n                <input type="password" name="dialog-password" placeholder="' + app.params.dialog.passwordPlaceholder + '" class="dialog-input">\n              </div>',
          buttons: [{
            text: app.params.dialog.buttonCancel,
            keyCodes: keyboardActions ? [27] : null,
            color: app.theme === "aurora" ? "gray" : null
          }, {
            text: app.params.dialog.buttonOk,
            bold: true,
            keyCodes: keyboardActions ? [13] : null
          }],
          onClick: function onClick4(dialog, index2) {
            var password2 = dialog.$el.find('[name="dialog-password"]').val();
            if (index2 === 0 && callbackCancel)
              callbackCancel(password2);
            if (index2 === 1 && callbackOk)
              callbackOk(password2);
          },
          destroyOnClose
        }, autoFocusHandler)).open();
      },
      preloader: function preloader(title, color) {
        var preloaders = {
          iosPreloaderContent,
          mdPreloaderContent,
          auroraPreloaderContent
        };
        var preloaderInner = preloaders[app.theme + "PreloaderContent"] || "";
        return new dialog_class_default(app, {
          title: typeof title === "undefined" || title === null ? app.params.dialog.preloaderTitle : title,
          content: '<div class="preloader' + (color ? " color-" + color : "") + '">' + preloaderInner + "</div>",
          cssClass: "dialog-preloader",
          destroyOnClose
        }).open();
      },
      progress: function progress() {
        for (var _len6 = arguments.length, args = new Array(_len6), _key6 = 0; _key6 < _len6; _key6++) {
          args[_key6] = arguments[_key6];
        }
        var title = args[0], progress2 = args[1], color = args[2];
        if (args.length === 2) {
          if (typeof args[0] === "number") {
            progress2 = args[0];
            color = args[1];
            title = args[2];
          } else if (typeof args[0] === "string" && typeof args[1] === "string") {
            title = args[0];
            color = args[1];
            progress2 = args[2];
          }
        } else if (args.length === 1) {
          if (typeof args[0] === "number") {
            progress2 = args[0];
            title = args[1];
            color = args[2];
          }
        }
        var infinite = typeof progress2 === "undefined";
        var dialog = new dialog_class_default(app, {
          title: typeof title === "undefined" ? app.params.dialog.progressTitle : title,
          cssClass: "dialog-progress",
          content: '\n              <div class="progressbar' + (infinite ? "-infinite" : "") + (color ? " color-" + color : "") + '">\n                ' + (!infinite ? "<span></span>" : "") + "\n              </div>\n            ",
          destroyOnClose
        });
        if (!infinite)
          dialog.setProgress(progress2);
        return dialog.open();
      }
    });
  }
};

// node_modules/framework7/esm/components/popup/popup-class.js
function _assertThisInitialized2(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose2(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf2(subClass, superClass);
}
function _setPrototypeOf2(o, p) {
  _setPrototypeOf2 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf2(o, p);
}
var Popup = function(_Modal) {
  _inheritsLoose2(Popup2, _Modal);
  function Popup2(app, params) {
    var _this;
    var extendedParams = extend({
      on: {}
    }, app.params.popup, params);
    _this = _Modal.call(this, app, extendedParams) || this;
    var popup = _assertThisInitialized2(_this);
    var window2 = getWindow();
    var document = getDocument();
    var support2 = getSupport();
    var device2 = getDevice();
    popup.params = extendedParams;
    var $el;
    if (!popup.params.el) {
      $el = dom7_default(popup.params.content).filter(function(node) {
        return node.nodeType === 1;
      }).eq(0);
    } else {
      $el = dom7_default(popup.params.el).eq(0);
    }
    if ($el && $el.length > 0 && $el[0].f7Modal) {
      return $el[0].f7Modal || _assertThisInitialized2(_this);
    }
    if ($el.length === 0) {
      return popup.destroy() || _assertThisInitialized2(_this);
    }
    var $backdropEl;
    if (popup.params.backdrop && popup.params.backdropEl) {
      $backdropEl = dom7_default(popup.params.backdropEl);
    } else if (popup.params.backdrop) {
      $backdropEl = popup.$containerEl.children(".popup-backdrop");
      if ($backdropEl.length === 0) {
        $backdropEl = dom7_default('<div class="popup-backdrop"></div>');
        popup.$containerEl.append($backdropEl);
      }
    }
    extend(popup, {
      app,
      push: $el.hasClass("popup-push") || popup.params.push,
      $el,
      el: $el[0],
      $backdropEl,
      backdropEl: $backdropEl && $backdropEl[0],
      type: "popup",
      $htmlEl: dom7_default("html")
    });
    if (popup.params.push) {
      $el.addClass("popup-push");
    }
    function handleClick(e) {
      var target = e.target;
      var $target = dom7_default(target);
      var keyboardOpened = !device2.desktop && device2.cordova && (window2.Keyboard && window2.Keyboard.isVisible || window2.cordova.plugins && window2.cordova.plugins.Keyboard && window2.cordova.plugins.Keyboard.isVisible);
      if (keyboardOpened)
        return;
      if ($target.closest(popup.el).length === 0) {
        if (popup.params && popup.params.closeByBackdropClick && popup.params.backdrop && popup.backdropEl && popup.backdropEl === target) {
          var needToClose = true;
          popup.$el.nextAll(".popup.modal-in").each(function(popupEl) {
            var popupInstance = popupEl.f7Modal;
            if (!popupInstance)
              return;
            if (popupInstance.params.closeByBackdropClick && popupInstance.params.backdrop && popupInstance.backdropEl === popup.backdropEl) {
              needToClose = false;
            }
          });
          if (needToClose) {
            popup.close();
          }
        }
      }
    }
    function onKeyDown(e) {
      var keyCode = e.keyCode;
      if (keyCode === 27 && popup.params.closeOnEscape) {
        popup.close();
      }
    }
    var pushOffset;
    var isPush;
    function pushViewScale(offset2) {
      return (app.height - offset2 * 2) / app.height;
    }
    var allowSwipeToClose = true;
    var isTouched = false;
    var startTouch;
    var currentTouch;
    var isScrolling;
    var touchStartTime;
    var touchesDiff;
    var isMoved = false;
    var pageContentEl;
    var pageContentScrollTop;
    var pageContentOffsetHeight;
    var pageContentScrollHeight;
    var popupHeight;
    var $pushEl;
    function handleTouchStart(e) {
      if (isTouched || !allowSwipeToClose || !popup.params.swipeToClose)
        return;
      if (popup.params.swipeHandler && dom7_default(e.target).closest(popup.params.swipeHandler).length === 0) {
        return;
      }
      isTouched = true;
      isMoved = false;
      startTouch = {
        x: e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX,
        y: e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY
      };
      touchStartTime = now();
      isScrolling = void 0;
      if (!popup.params.swipeHandler && e.type === "touchstart") {
        pageContentEl = dom7_default(e.target).closest(".page-content")[0];
      }
    }
    function handleTouchMove(e) {
      if (!isTouched)
        return;
      currentTouch = {
        x: e.type === "touchmove" ? e.targetTouches[0].pageX : e.pageX,
        y: e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY
      };
      if (typeof isScrolling === "undefined") {
        isScrolling = !!(isScrolling || Math.abs(currentTouch.x - startTouch.x) > Math.abs(currentTouch.y - startTouch.y));
      }
      if (isScrolling) {
        isTouched = false;
        isMoved = false;
        return;
      }
      touchesDiff = startTouch.y - currentTouch.y;
      if (isPush && pushOffset && touchesDiff > 0) {
        touchesDiff = 0;
      }
      var direction = touchesDiff < 0 ? "to-bottom" : "to-top";
      $el.transition(0);
      if (typeof popup.params.swipeToClose === "string" && direction !== popup.params.swipeToClose) {
        $el.transform("");
        $el.transition("");
        return;
      }
      if (!isMoved) {
        if (isPush && pushOffset) {
          popupHeight = $el[0].offsetHeight;
          $pushEl = $el.prevAll(".popup.modal-in").eq(0);
          if ($pushEl.length === 0) {
            $pushEl = app.$el.children(".view, .views");
          }
        }
        if (pageContentEl) {
          pageContentScrollTop = pageContentEl.scrollTop;
          pageContentScrollHeight = pageContentEl.scrollHeight;
          pageContentOffsetHeight = pageContentEl.offsetHeight;
          if (!(pageContentScrollHeight === pageContentOffsetHeight) && !(direction === "to-bottom" && pageContentScrollTop === 0) && !(direction === "to-top" && pageContentScrollTop === pageContentScrollHeight - pageContentOffsetHeight)) {
            $el.transform("");
            $el.transition("");
            isTouched = false;
            isMoved = false;
            return;
          }
        }
        isMoved = true;
        popup.emit("local::swipeStart popupSwipeStart", popup);
        popup.$el.trigger("popup:swipestart");
      } else {
        popup.emit("local::swipeMove popupSwipeMove", popup);
        popup.$el.trigger("popup:swipemove");
      }
      e.preventDefault();
      if (isPush && pushOffset) {
        var pushProgress = 1 - Math.abs(touchesDiff / popupHeight);
        var scale = 1 - (1 - pushViewScale(pushOffset)) * pushProgress;
        if ($pushEl.hasClass("popup")) {
          if ($pushEl.hasClass("popup-push")) {
            $pushEl.transition(0).forEach(function(el) {
              el.style.setProperty("transform", "translate3d(0, calc(-1 * " + pushProgress + " * (var(--f7-popup-push-offset) + 10px)) , 0px) scale(" + scale + ")", "important");
            });
          } else {
            $pushEl.transition(0).forEach(function(el) {
              el.style.setProperty("transform", "translate3d(0, 0px , 0px) scale(" + scale + ")", "important");
            });
          }
        } else {
          $pushEl.transition(0).forEach(function(el) {
            el.style.setProperty("transform", "translate3d(0,0,0) scale(" + scale + ")", "important");
          });
        }
      }
      $el.transition(0).transform("translate3d(0," + -touchesDiff + "px,0)");
    }
    function handleTouchEnd() {
      isTouched = false;
      if (!isMoved) {
        return;
      }
      popup.emit("local::swipeEnd popupSwipeEnd", popup);
      popup.$el.trigger("popup:swipeend");
      isMoved = false;
      allowSwipeToClose = false;
      $el.transition("");
      if (isPush && pushOffset) {
        $pushEl.transition("").transform("");
      }
      var direction = touchesDiff <= 0 ? "to-bottom" : "to-top";
      if (typeof popup.params.swipeToClose === "string" && direction !== popup.params.swipeToClose) {
        $el.transform("");
        allowSwipeToClose = true;
        return;
      }
      var diff = Math.abs(touchesDiff);
      var timeDiff = new Date().getTime() - touchStartTime;
      if (timeDiff < 300 && diff > 20 || timeDiff >= 300 && diff > 100) {
        nextTick(function() {
          if (direction === "to-bottom") {
            $el.addClass("swipe-close-to-bottom");
          } else {
            $el.addClass("swipe-close-to-top");
          }
          $el.transform("");
          popup.emit("local::swipeclose popupSwipeClose", popup);
          popup.$el.trigger("popup:swipeclose");
          popup.close();
          allowSwipeToClose = true;
        });
        return;
      }
      allowSwipeToClose = true;
      $el.transform("");
    }
    var passive = support2.passiveListener ? {
      passive: true
    } : false;
    if (popup.params.swipeToClose) {
      $el.on(app.touchEvents.start, handleTouchStart, passive);
      app.on("touchmove", handleTouchMove);
      app.on("touchend:passive", handleTouchEnd);
      popup.once("popupDestroy", function() {
        $el.off(app.touchEvents.start, handleTouchStart, passive);
        app.off("touchmove", handleTouchMove);
        app.off("touchend:passive", handleTouchEnd);
      });
    }
    var hasPreviousPushPopup;
    var updatePushOffset = function updatePushOffset2() {
      var wasPush = isPush;
      if (popup.push) {
        isPush = popup.push && (app.width < 630 || app.height < 630 || $el.hasClass("popup-tablet-fullscreen"));
      }
      if (isPush && !wasPush) {
        setPushOffset();
      } else if (isPush && wasPush) {
        popup.$htmlEl[0].style.setProperty("--f7-popup-push-scale", pushViewScale(pushOffset));
      } else if (!isPush && wasPush) {
        popup.$htmlEl.removeClass("with-modal-popup-push");
        popup.$htmlEl[0].style.removeProperty("--f7-popup-push-scale");
      }
    };
    var setPushOffset = function setPushOffset2() {
      app.off("resize", updatePushOffset);
      if (popup.push) {
        isPush = popup.push && (app.width < 630 || app.height < 630 || $el.hasClass("popup-tablet-fullscreen"));
      }
      if (isPush) {
        pushOffset = parseInt($el.css("--f7-popup-push-offset"), 10);
        if (Number.isNaN(pushOffset))
          pushOffset = 0;
        if (pushOffset) {
          $el.addClass("popup-push");
          popup.$htmlEl.addClass("with-modal-popup-push");
          popup.$htmlEl[0].style.setProperty("--f7-popup-push-scale", pushViewScale(pushOffset));
        }
      }
      app.on("resize", updatePushOffset);
    };
    popup.on("open", function() {
      hasPreviousPushPopup = false;
      if (popup.params.closeOnEscape) {
        dom7_default(document).on("keydown", onKeyDown);
      }
      $el.prevAll(".popup.modal-in").addClass("popup-behind");
      setPushOffset();
    });
    popup.on("opened", function() {
      $el.removeClass("swipe-close-to-bottom swipe-close-to-top");
      if (popup.params.closeByBackdropClick) {
        app.on("click", handleClick);
      }
    });
    popup.on("close", function() {
      hasPreviousPushPopup = popup.$el.prevAll(".popup-push.modal-in").length > 0;
      if (popup.params.closeOnEscape) {
        dom7_default(document).off("keydown", onKeyDown);
      }
      if (popup.params.closeByBackdropClick) {
        app.off("click", handleClick);
      }
      $el.prevAll(".popup.modal-in").eq(0).removeClass("popup-behind");
      if (isPush && pushOffset && !hasPreviousPushPopup) {
        popup.$htmlEl.removeClass("with-modal-popup-push");
        popup.$htmlEl.addClass("with-modal-popup-push-closing");
      }
      app.off("resize", updatePushOffset);
    });
    popup.on("closed", function() {
      $el.removeClass("popup-behind");
      if (isPush && pushOffset && !hasPreviousPushPopup) {
        popup.$htmlEl.removeClass("with-modal-popup-push-closing");
        popup.$htmlEl[0].style.removeProperty("--f7-popup-push-scale");
      }
    });
    $el[0].f7Modal = popup;
    return popup || _assertThisInitialized2(_this);
  }
  return Popup2;
}(modal_class_default);
var popup_class_default = Popup;

// node_modules/framework7/esm/components/popup/popup.js
var popup_default = {
  name: "popup",
  params: {
    popup: {
      backdrop: true,
      backdropEl: void 0,
      closeByBackdropClick: true,
      closeOnEscape: false,
      swipeToClose: false,
      swipeHandler: null,
      push: false,
      containerEl: null
    }
  },
  static: {
    Popup: popup_class_default
  },
  create: function create2() {
    var app = this;
    app.popup = ModalMethods({
      app,
      constructor: popup_class_default,
      defaultSelector: ".popup.modal-in",
      parentSelector: ".popup"
    });
  },
  clicks: {
    ".popup-open": function openPopup($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.popup.open(data.popup, data.animate, $clickedEl);
    },
    ".popup-close": function closePopup($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.popup.close(data.popup, data.animate, $clickedEl);
    }
  }
};

// node_modules/framework7/esm/components/login-screen/login-screen-class.js
function _assertThisInitialized3(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose3(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf3(subClass, superClass);
}
function _setPrototypeOf3(o, p) {
  _setPrototypeOf3 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf3(o, p);
}
var LoginScreen = function(_Modal) {
  _inheritsLoose3(LoginScreen2, _Modal);
  function LoginScreen2(app, params) {
    var _this;
    var extendedParams = extend({
      on: {}
    }, params);
    _this = _Modal.call(this, app, extendedParams) || this;
    var loginScreen = _assertThisInitialized3(_this);
    loginScreen.params = extendedParams;
    var $el;
    if (!loginScreen.params.el) {
      $el = dom7_default(loginScreen.params.content).filter(function(node) {
        return node.nodeType === 1;
      }).eq(0);
    } else {
      $el = dom7_default(loginScreen.params.el).eq(0);
    }
    if ($el && $el.length > 0 && $el[0].f7Modal) {
      return $el[0].f7Modal || _assertThisInitialized3(_this);
    }
    if ($el.length === 0) {
      return loginScreen.destroy() || _assertThisInitialized3(_this);
    }
    extend(loginScreen, {
      app,
      $el,
      el: $el[0],
      type: "loginScreen"
    });
    $el[0].f7Modal = loginScreen;
    return loginScreen || _assertThisInitialized3(_this);
  }
  return LoginScreen2;
}(modal_class_default);
var login_screen_class_default = LoginScreen;

// node_modules/framework7/esm/components/login-screen/login-screen.js
var login_screen_default = {
  name: "loginScreen",
  static: {
    LoginScreen: login_screen_class_default
  },
  create: function create3() {
    var app = this;
    app.loginScreen = ModalMethods({
      app,
      constructor: login_screen_class_default,
      defaultSelector: ".login-screen.modal-in"
    });
  },
  clicks: {
    ".login-screen-open": function openLoginScreen($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.loginScreen.open(data.loginScreen, data.animate, $clickedEl);
    },
    ".login-screen-close": function closeLoginScreen($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.loginScreen.close(data.loginScreen, data.animate, $clickedEl);
    }
  }
};

// node_modules/framework7/esm/components/popover/popover-class.js
function _assertThisInitialized4(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose4(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf4(subClass, superClass);
}
function _setPrototypeOf4(o, p) {
  _setPrototypeOf4 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf4(o, p);
}
var Popover = function(_Modal) {
  _inheritsLoose4(Popover2, _Modal);
  function Popover2(app, params) {
    var _this;
    var extendedParams = extend({
      on: {}
    }, app.params.popover, params);
    _this = _Modal.call(this, app, extendedParams) || this;
    var popover = _assertThisInitialized4(_this);
    var device2 = getDevice();
    var window2 = getWindow();
    var document = getDocument();
    popover.params = extendedParams;
    var $el;
    if (!popover.params.el) {
      $el = dom7_default(popover.params.content).filter(function(node) {
        return node.nodeType === 1;
      }).eq(0);
    } else {
      $el = dom7_default(popover.params.el).eq(0);
    }
    if ($el && $el.length > 0 && $el[0].f7Modal) {
      return $el[0].f7Modal || _assertThisInitialized4(_this);
    }
    var $targetEl = dom7_default(popover.params.targetEl).eq(0);
    if ($el.length === 0) {
      return popover.destroy() || _assertThisInitialized4(_this);
    }
    var $backdropEl;
    if (popover.params.backdrop && popover.params.backdropEl) {
      $backdropEl = dom7_default(popover.params.backdropEl);
    } else if (popover.params.backdrop) {
      $backdropEl = popover.$containerEl.children(".popover-backdrop");
      if ($backdropEl.length === 0) {
        $backdropEl = dom7_default('<div class="popover-backdrop"></div>');
        popover.$containerEl.append($backdropEl);
      }
    }
    var $angleEl;
    if ($el.find(".popover-angle").length === 0) {
      $angleEl = dom7_default('<div class="popover-angle"></div>');
      $el.prepend($angleEl);
    } else {
      $angleEl = $el.find(".popover-angle");
    }
    var originalOpen = popover.open;
    extend(popover, {
      app,
      $el,
      el: $el[0],
      $targetEl,
      targetEl: $targetEl[0],
      $angleEl,
      angleEl: $angleEl[0],
      $backdropEl,
      backdropEl: $backdropEl && $backdropEl[0],
      type: "popover",
      open: function open12() {
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        var targetEl = args[0], animate = args[1];
        if (typeof args[0] === "boolean") {
          animate = args[0];
          targetEl = args[1];
        }
        if (targetEl) {
          popover.$targetEl = dom7_default(targetEl);
          popover.targetEl = popover.$targetEl[0];
        }
        return originalOpen.call(popover, animate);
      }
    });
    function handleResize() {
      popover.resize();
    }
    popover.on("popoverOpen", function() {
      popover.resize();
      app.on("resize", handleResize);
      dom7_default(window2).on("keyboardDidShow keyboardDidHide", handleResize);
      popover.on("popoverClose popoverBeforeDestroy", function() {
        app.off("resize", handleResize);
        dom7_default(window2).off("keyboardDidShow keyboardDidHide", handleResize);
      });
    });
    var touchStartTarget = null;
    function handleTouchStart(e) {
      touchStartTarget = e.target;
    }
    function handleClick(e) {
      var target = e.target;
      var $target = dom7_default(target);
      var keyboardOpened = !device2.desktop && device2.cordova && (window2.Keyboard && window2.Keyboard.isVisible || window2.cordova.plugins && window2.cordova.plugins.Keyboard && window2.cordova.plugins.Keyboard.isVisible);
      if (keyboardOpened)
        return;
      if ($target.closest(popover.el).length === 0) {
        if (popover.params.closeByBackdropClick && popover.params.backdrop && popover.backdropEl && popover.backdropEl === target && touchStartTarget === target) {
          popover.close();
        } else if (popover.params.closeByOutsideClick && touchStartTarget === target) {
          popover.close();
        }
      }
    }
    function onKeyDown(e) {
      var keyCode = e.keyCode;
      if (keyCode === 27 && popover.params.closeOnEscape) {
        popover.close();
      }
    }
    if (popover.params.closeOnEscape) {
      popover.on("popoverOpen", function() {
        dom7_default(document).on("keydown", onKeyDown);
      });
      popover.on("popoverClose", function() {
        dom7_default(document).off("keydown", onKeyDown);
      });
    }
    popover.on("popoverOpened", function() {
      if (popover.params.closeByOutsideClick || popover.params.closeByBackdropClick) {
        app.on("touchstart", handleTouchStart);
        app.on("click", handleClick);
      }
    });
    popover.on("popoverClose", function() {
      if (popover.params.closeByOutsideClick || popover.params.closeByBackdropClick) {
        app.off("touchstart", handleTouchStart);
        app.off("click", handleClick);
      }
    });
    $el[0].f7Modal = popover;
    return popover || _assertThisInitialized4(_this);
  }
  var _proto = Popover2.prototype;
  _proto.resize = function resize4() {
    var popover = this;
    var app = popover.app, $el = popover.$el, $targetEl = popover.$targetEl, $angleEl = popover.$angleEl;
    var _popover$params = popover.params, targetX = _popover$params.targetX, targetY = _popover$params.targetY;
    $el.css({
      left: "",
      top: ""
    });
    var _ref = [$el.width(), $el.height()], width = _ref[0], height = _ref[1];
    var angleSize = 0;
    var angleLeft;
    var angleTop;
    if (app.theme === "ios" || app.theme === "aurora") {
      $angleEl.removeClass("on-left on-right on-top on-bottom").css({
        left: "",
        top: ""
      });
      angleSize = $angleEl.width() / 2;
    } else {
      $el.removeClass("popover-on-left popover-on-right popover-on-top popover-on-bottom popover-on-middle").css({
        left: "",
        top: ""
      });
    }
    var targetWidth;
    var targetHeight;
    var targetOffsetLeft;
    var targetOffsetTop;
    var safeAreaTop = parseInt(dom7_default("html").css("--f7-safe-area-top"), 10);
    var safeAreaLeft = parseInt(dom7_default("html").css("--f7-safe-area-left"), 10);
    var safeAreaRight = parseInt(dom7_default("html").css("--f7-safe-area-right"), 10);
    if (Number.isNaN(safeAreaTop))
      safeAreaTop = 0;
    if (Number.isNaN(safeAreaLeft))
      safeAreaLeft = 0;
    if (Number.isNaN(safeAreaRight))
      safeAreaRight = 0;
    if ($targetEl && $targetEl.length > 0) {
      targetWidth = $targetEl.outerWidth();
      targetHeight = $targetEl.outerHeight();
      var targetOffset = $targetEl.offset();
      targetOffsetLeft = targetOffset.left - app.left;
      targetOffsetTop = targetOffset.top - app.top;
      var targetParentPage = $targetEl.parents(".page");
      if (targetParentPage.length > 0) {
        targetOffsetTop -= targetParentPage[0].scrollTop;
      }
    } else if (typeof targetX !== "undefined" && targetY !== "undefined") {
      targetOffsetLeft = targetX;
      targetOffsetTop = targetY;
      targetWidth = popover.params.targetWidth || 0;
      targetHeight = popover.params.targetHeight || 0;
    }
    var left = 0, top = 0, diff = 0;
    var position = app.theme === "md" ? "bottom" : "top";
    if (app.theme === "md") {
      if (height < app.height - targetOffsetTop - targetHeight) {
        position = "bottom";
        top = targetOffsetTop + targetHeight;
      } else if (height < targetOffsetTop - safeAreaTop) {
        top = targetOffsetTop - height;
        position = "top";
      } else {
        position = "middle";
        top = targetHeight / 2 + targetOffsetTop - height / 2;
      }
      top = Math.max(8, Math.min(top, app.height - height - 8));
      var hPosition;
      if (targetOffsetLeft < app.width / 2) {
        hPosition = "right";
        left = position === "middle" ? targetOffsetLeft + targetWidth : targetOffsetLeft;
      } else {
        hPosition = "left";
        left = position === "middle" ? targetOffsetLeft - width : targetOffsetLeft + targetWidth - width;
      }
      left = Math.max(8, Math.min(left, app.width - width - 8 - safeAreaRight), safeAreaLeft);
      $el.addClass("popover-on-" + position + " popover-on-" + hPosition);
    } else {
      if (height + angleSize < targetOffsetTop - safeAreaTop) {
        top = targetOffsetTop - height - angleSize;
      } else if (height + angleSize < app.height - targetOffsetTop - targetHeight) {
        position = "bottom";
        top = targetOffsetTop + targetHeight + angleSize;
      } else {
        position = "middle";
        top = targetHeight / 2 + targetOffsetTop - height / 2;
        diff = top;
        top = Math.max(5, Math.min(top, app.height - height - 5));
        diff -= top;
      }
      if (position === "top" || position === "bottom") {
        left = targetWidth / 2 + targetOffsetLeft - width / 2;
        diff = left;
        left = Math.max(5, Math.min(left, app.width - width - 5));
        if (safeAreaLeft) {
          left = Math.max(left, safeAreaLeft);
        }
        if (safeAreaRight && left + width > app.width - 5 - safeAreaRight) {
          left = app.width - 5 - safeAreaRight - width;
        }
        if (position === "top") {
          $angleEl.addClass("on-bottom");
        }
        if (position === "bottom") {
          $angleEl.addClass("on-top");
        }
        diff -= left;
        angleLeft = width / 2 - angleSize + diff;
        angleLeft = Math.max(Math.min(angleLeft, width - angleSize * 2 - 13), 13);
        $angleEl.css({
          left: angleLeft + "px"
        });
      } else if (position === "middle") {
        left = targetOffsetLeft - width - angleSize;
        $angleEl.addClass("on-right");
        if (left < 5 || left + width + safeAreaRight > app.width || left < safeAreaLeft) {
          if (left < 5)
            left = targetOffsetLeft + targetWidth + angleSize;
          if (left + width + safeAreaRight > app.width)
            left = app.width - width - 5 - safeAreaRight;
          if (left < safeAreaLeft)
            left = safeAreaLeft;
          $angleEl.removeClass("on-right").addClass("on-left");
        }
        angleTop = height / 2 - angleSize + diff;
        angleTop = Math.max(Math.min(angleTop, height - angleSize * 2 - 13), 13);
        $angleEl.css({
          top: angleTop + "px"
        });
      }
    }
    $el.css({
      top: top + "px",
      left: left + "px"
    });
  };
  return Popover2;
}(modal_class_default);
var popover_class_default = Popover;

// node_modules/framework7/esm/components/popover/popover.js
var popover_default = {
  name: "popover",
  params: {
    popover: {
      backdrop: true,
      backdropEl: void 0,
      closeByBackdropClick: true,
      closeByOutsideClick: true,
      closeOnEscape: false,
      containerEl: null
    }
  },
  static: {
    Popover: popover_class_default
  },
  create: function create4() {
    var app = this;
    app.popover = extend(ModalMethods({
      app,
      constructor: popover_class_default,
      defaultSelector: ".popover.modal-in"
    }), {
      open: function open12(popoverEl, targetEl, animate) {
        var $popoverEl = dom7_default(popoverEl);
        if ($popoverEl.length > 1) {
          var $targetPage = dom7_default(targetEl).parents(".page");
          if ($targetPage.length) {
            $popoverEl.each(function(el) {
              var $el = dom7_default(el);
              if ($el.parents($targetPage)[0] === $targetPage[0]) {
                $popoverEl = $el;
              }
            });
          }
        }
        if ($popoverEl.length > 1) {
          $popoverEl = $popoverEl.eq($popoverEl.length - 1);
        }
        var popover = $popoverEl[0].f7Modal;
        var data = $popoverEl.dataset();
        if (!popover) {
          popover = new popover_class_default(app, Object.assign({
            el: $popoverEl,
            targetEl
          }, data));
        }
        return popover.open(targetEl, animate);
      }
    });
  },
  clicks: {
    ".popover-open": function openPopover($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.popover.open(data.popover, $clickedEl, data.animate);
    },
    ".popover-close": function closePopover($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.popover.close(data.popover, data.animate, $clickedEl);
    }
  }
};

// node_modules/framework7/esm/components/actions/actions-class.js
function _assertThisInitialized5(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose5(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf5(subClass, superClass);
}
function _setPrototypeOf5(o, p) {
  _setPrototypeOf5 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf5(o, p);
}
var Actions = function(_Modal) {
  _inheritsLoose5(Actions2, _Modal);
  function Actions2(app, params) {
    var _this;
    var extendedParams = extend({
      on: {}
    }, app.params.actions, params);
    _this = _Modal.call(this, app, extendedParams) || this;
    var actions = _assertThisInitialized5(_this);
    var device2 = getDevice();
    var window2 = getWindow();
    var document = getDocument();
    actions.params = extendedParams;
    var groups;
    if (actions.params.buttons) {
      groups = actions.params.buttons;
      if (!Array.isArray(groups[0]))
        groups = [groups];
    }
    actions.groups = groups;
    var $el;
    if (actions.params.el) {
      $el = dom7_default(actions.params.el).eq(0);
    } else if (actions.params.content) {
      $el = dom7_default(actions.params.content).filter(function(node) {
        return node.nodeType === 1;
      }).eq(0);
    } else if (actions.params.buttons) {
      if (actions.params.convertToPopover) {
        actions.popoverHtml = actions.renderPopover();
      }
      actions.actionsHtml = actions.render();
    }
    if ($el && $el.length > 0 && $el[0].f7Modal) {
      return $el[0].f7Modal || _assertThisInitialized5(_this);
    }
    if ($el && $el.length === 0 && !(actions.actionsHtml || actions.popoverHtml)) {
      return actions.destroy() || _assertThisInitialized5(_this);
    }
    var $backdropEl;
    if (actions.params.backdrop && actions.params.backdropEl) {
      $backdropEl = dom7_default(actions.params.backdropEl);
    } else if (actions.params.backdrop) {
      $backdropEl = actions.$containerEl.children(".actions-backdrop");
      if ($backdropEl.length === 0) {
        $backdropEl = dom7_default('<div class="actions-backdrop"></div>');
        actions.$containerEl.append($backdropEl);
      }
    }
    var originalOpen = actions.open;
    var originalClose = actions.close;
    var popover;
    function buttonOnClick(e) {
      var $buttonEl = dom7_default(this);
      var buttonIndex;
      var groupIndex;
      if ($buttonEl.hasClass("list-button") || $buttonEl.hasClass("item-link")) {
        buttonIndex = $buttonEl.parents("li").index();
        groupIndex = $buttonEl.parents(".list").index();
      } else {
        buttonIndex = $buttonEl.index();
        groupIndex = $buttonEl.parents(".actions-group").index();
      }
      if (typeof groups !== "undefined") {
        var button = groups[groupIndex][buttonIndex];
        if (button.onClick)
          button.onClick(actions, e);
        if (actions.params.onClick)
          actions.params.onClick(actions, e);
        if (button.close !== false)
          actions.close();
      }
    }
    actions.open = function open12(animate) {
      var convertToPopover = false;
      var _actions$params = actions.params, targetEl = _actions$params.targetEl, targetX = _actions$params.targetX, targetY = _actions$params.targetY, targetWidth = _actions$params.targetWidth, targetHeight = _actions$params.targetHeight;
      if (actions.params.convertToPopover && (targetEl || targetX !== void 0 && targetY !== void 0)) {
        if (actions.params.forceToPopover || device2.ios && device2.ipad || app.width >= 768 || device2.desktop && app.theme === "aurora") {
          convertToPopover = true;
        }
      }
      if (convertToPopover && actions.popoverHtml) {
        popover = app.popover.create({
          containerEl: actions.params.containerEl,
          content: actions.popoverHtml,
          backdrop: actions.params.backdrop,
          targetEl,
          targetX,
          targetY,
          targetWidth,
          targetHeight,
          on: {
            open: function open13() {
              if (!actions.$el) {
                actions.$el = popover.$el;
              }
              actions.$el.trigger("modal:open " + actions.type.toLowerCase() + ":open");
              actions.emit("local::open modalOpen " + actions.type + "Open", actions);
            },
            opened: function opened() {
              if (!actions.$el) {
                actions.$el = popover.$el;
              }
              actions.$el.trigger("modal:opened " + actions.type.toLowerCase() + ":opened");
              actions.emit("local::opened modalOpened " + actions.type + "Opened", actions);
            },
            close: function close12() {
              if (!actions.$el) {
                actions.$el = popover.$el;
              }
              actions.$el.trigger("modal:close " + actions.type.toLowerCase() + ":close");
              actions.emit("local::close modalClose " + actions.type + "Close", actions);
            },
            closed: function closed() {
              if (!actions.$el) {
                actions.$el = popover.$el;
              }
              actions.$el.trigger("modal:closed " + actions.type.toLowerCase() + ":closed");
              actions.emit("local::closed modalClosed " + actions.type + "Closed", actions);
            }
          }
        });
        popover.open(animate);
        popover.once("popoverOpened", function() {
          popover.$el.find(".list-button, .item-link").each(function(buttonEl) {
            dom7_default(buttonEl).on("click", buttonOnClick);
          });
        });
        popover.once("popoverClosed", function() {
          popover.$el.find(".list-button, .item-link").each(function(buttonEl) {
            dom7_default(buttonEl).off("click", buttonOnClick);
          });
          nextTick(function() {
            popover.destroy();
            popover = void 0;
          });
        });
      } else {
        actions.$el = actions.actionsHtml ? dom7_default(actions.actionsHtml) : actions.$el;
        actions.$el[0].f7Modal = actions;
        if (actions.groups) {
          actions.$el.find(".actions-button").each(function(buttonEl) {
            dom7_default(buttonEl).on("click", buttonOnClick);
          });
          actions.once("actionsClosed", function() {
            actions.$el.find(".actions-button").each(function(buttonEl) {
              dom7_default(buttonEl).off("click", buttonOnClick);
            });
          });
        }
        actions.el = actions.$el[0];
        originalOpen.call(actions, animate);
      }
      return actions;
    };
    actions.close = function close12(animate) {
      if (popover) {
        popover.close(animate);
      } else {
        originalClose.call(actions, animate);
      }
      return actions;
    };
    extend(actions, {
      app,
      $el,
      el: $el ? $el[0] : void 0,
      $backdropEl,
      backdropEl: $backdropEl && $backdropEl[0],
      type: "actions"
    });
    function handleClick(e) {
      var target = e.target;
      var $target = dom7_default(target);
      var keyboardOpened = !device2.desktop && device2.cordova && (window2.Keyboard && window2.Keyboard.isVisible || window2.cordova.plugins && window2.cordova.plugins.Keyboard && window2.cordova.plugins.Keyboard.isVisible);
      if (keyboardOpened)
        return;
      if ($target.closest(actions.el).length === 0) {
        if (actions.params.closeByBackdropClick && actions.params.backdrop && actions.backdropEl && actions.backdropEl === target) {
          actions.close();
        } else if (actions.params.closeByOutsideClick) {
          actions.close();
        }
      }
    }
    function onKeyDown(e) {
      var keyCode = e.keyCode;
      if (keyCode === 27 && actions.params.closeOnEscape) {
        actions.close();
      }
    }
    if (actions.params.closeOnEscape) {
      actions.on("open", function() {
        dom7_default(document).on("keydown", onKeyDown);
      });
      actions.on("close", function() {
        dom7_default(document).off("keydown", onKeyDown);
      });
    }
    actions.on("opened", function() {
      if (actions.params.closeByBackdropClick || actions.params.closeByOutsideClick) {
        app.on("click", handleClick);
      }
    });
    actions.on("close", function() {
      if (actions.params.closeByBackdropClick || actions.params.closeByOutsideClick) {
        app.off("click", handleClick);
      }
    });
    if ($el) {
      $el[0].f7Modal = actions;
    }
    return actions || _assertThisInitialized5(_this);
  }
  var _proto = Actions2.prototype;
  _proto.render = function render15() {
    var actions = this;
    if (actions.params.render)
      return actions.params.render.call(actions, actions);
    var groups = actions.groups;
    var cssClass = actions.params.cssClass;
    return jsx_default2("div", {
      class: "actions-modal" + (actions.params.grid ? " actions-grid" : "") + " " + (cssClass || "")
    }, groups.map(function(group) {
      return jsx_default2("div", {
        class: "actions-group"
      }, group.map(function(button) {
        var buttonClasses = ["actions-" + (button.label ? "label" : "button")];
        var color = button.color, bg = button.bg, bold = button.bold, disabled = button.disabled, label = button.label, text2 = button.text, icon = button.icon;
        if (color)
          buttonClasses.push("color-" + color);
        if (bg)
          buttonClasses.push("bg-color-" + bg);
        if (bold)
          buttonClasses.push("actions-button-bold");
        if (disabled)
          buttonClasses.push("disabled");
        if (label) {
          return jsx_default2("div", {
            class: buttonClasses.join(" ")
          }, text2);
        }
        return jsx_default2("div", {
          class: buttonClasses.join(" ")
        }, icon && jsx_default2("div", {
          class: "actions-button-media"
        }, icon), jsx_default2("div", {
          class: "actions-button-text"
        }, text2));
      }));
    }));
  };
  _proto.renderPopover = function renderPopover() {
    var actions = this;
    if (actions.params.renderPopover)
      return actions.params.renderPopover.call(actions, actions);
    var groups = actions.groups;
    var cssClass = actions.params.cssClass;
    return jsx_default2("div", {
      class: "popover popover-from-actions " + (cssClass || "")
    }, jsx_default2("div", {
      class: "popover-inner"
    }, groups.map(function(group) {
      return jsx_default2("div", {
        class: "list"
      }, jsx_default2("ul", null, group.map(function(button) {
        var itemClasses = [];
        var color = button.color, bg = button.bg, bold = button.bold, disabled = button.disabled, label = button.label, text2 = button.text, icon = button.icon;
        if (color)
          itemClasses.push("color-" + color);
        if (bg)
          itemClasses.push("bg-color-" + bg);
        if (bold)
          itemClasses.push("popover-from-actions-bold");
        if (disabled)
          itemClasses.push("disabled");
        if (label) {
          itemClasses.push("popover-from-actions-label");
          return '<li class="' + itemClasses.join(" ") + '">' + text2 + "</li>";
        }
        if (icon) {
          itemClasses.push("item-link item-content");
          return jsx_default2("li", null, jsx_default2("a", {
            class: itemClasses.join(" ")
          }, jsx_default2("div", {
            class: "item-media"
          }, icon), jsx_default2("div", {
            class: "item-inner"
          }, jsx_default2("div", {
            class: "item-title"
          }, text2))));
        }
        itemClasses.push("list-button");
        return jsx_default2("li", null, jsx_default2("a", {
          class: itemClasses.join(" ")
        }, text2));
      })));
    })));
  };
  return Actions2;
}(modal_class_default);
var actions_class_default = Actions;

// node_modules/framework7/esm/components/actions/actions.js
var actions_default = {
  name: "actions",
  params: {
    actions: {
      convertToPopover: true,
      forceToPopover: false,
      backdrop: true,
      backdropEl: void 0,
      cssClass: null,
      closeByBackdropClick: true,
      closeOnEscape: false,
      render: null,
      renderPopover: null,
      containerEl: null
    }
  },
  static: {
    Actions: actions_class_default
  },
  create: function create5() {
    var app = this;
    app.actions = ModalMethods({
      app,
      constructor: actions_class_default,
      defaultSelector: ".actions-modal.modal-in"
    });
  },
  clicks: {
    ".actions-open": function openActions($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.actions.open(data.actions, data.animate, $clickedEl);
    },
    ".actions-close": function closeActions($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.actions.close(data.actions, data.animate, $clickedEl);
    }
  }
};

// node_modules/framework7/esm/components/sheet/sheet-class.js
function _assertThisInitialized6(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose6(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf6(subClass, superClass);
}
function _setPrototypeOf6(o, p) {
  _setPrototypeOf6 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf6(o, p);
}
var Sheet = function(_Modal) {
  _inheritsLoose6(Sheet2, _Modal);
  function Sheet2(app, params) {
    var _this;
    var extendedParams = extend({
      on: {}
    }, app.params.sheet, params);
    _this = _Modal.call(this, app, extendedParams) || this;
    var sheet = _assertThisInitialized6(_this);
    var window2 = getWindow();
    var document = getDocument();
    var support2 = getSupport();
    var device2 = getDevice();
    sheet.params = extendedParams;
    if (typeof sheet.params.backdrop === "undefined") {
      sheet.params.backdrop = app.theme !== "ios";
    }
    var $el;
    if (!sheet.params.el) {
      $el = dom7_default(sheet.params.content).filter(function(node) {
        return node.nodeType === 1;
      }).eq(0);
    } else {
      $el = dom7_default(sheet.params.el).eq(0);
    }
    if ($el && $el.length > 0 && $el[0].f7Modal) {
      return $el[0].f7Modal || _assertThisInitialized6(_this);
    }
    if ($el.length === 0) {
      return sheet.destroy() || _assertThisInitialized6(_this);
    }
    var $backdropEl;
    if (sheet.params.backdrop && sheet.params.backdropEl) {
      $backdropEl = dom7_default(sheet.params.backdropEl);
    } else if (sheet.params.backdrop) {
      $backdropEl = sheet.$containerEl.children(".sheet-backdrop");
      if ($backdropEl.length === 0) {
        $backdropEl = dom7_default('<div class="sheet-backdrop"></div>');
        sheet.$containerEl.append($backdropEl);
      }
    }
    extend(sheet, {
      app,
      push: $el.hasClass("sheet-modal-push") || sheet.params.push,
      $el,
      el: $el[0],
      $backdropEl,
      backdropEl: $backdropEl && $backdropEl[0],
      type: "sheet",
      $htmlEl: dom7_default("html")
    });
    if (sheet.params.push) {
      $el.addClass("sheet-modal-push");
    }
    var $pageContentEl;
    function scrollToElementOnOpen() {
      var $scrollEl = dom7_default(sheet.params.scrollToEl).eq(0);
      if ($scrollEl.length === 0)
        return;
      $pageContentEl = $scrollEl.parents(".page-content");
      if ($pageContentEl.length === 0)
        return;
      var paddingTop = parseInt($pageContentEl.css("padding-top"), 10);
      var paddingBottom = parseInt($pageContentEl.css("padding-bottom"), 10);
      var pageHeight = $pageContentEl[0].offsetHeight - paddingTop - $el.height();
      var pageScrollHeight = $pageContentEl[0].scrollHeight - paddingTop - $el.height();
      var pageScroll = $pageContentEl.scrollTop();
      var newPaddingBottom;
      var scrollElTop = $scrollEl.offset().top - paddingTop + $scrollEl[0].offsetHeight;
      if (scrollElTop > pageHeight) {
        var scrollTop = pageScroll + scrollElTop - pageHeight;
        if (scrollTop + pageHeight > pageScrollHeight) {
          newPaddingBottom = scrollTop + pageHeight - pageScrollHeight + paddingBottom;
          if (pageHeight === pageScrollHeight) {
            newPaddingBottom = $el.height();
          }
          $pageContentEl.css({
            "padding-bottom": newPaddingBottom + "px"
          });
        }
        $pageContentEl.scrollTop(scrollTop, 300);
      }
    }
    function scrollToElementOnClose() {
      if ($pageContentEl && $pageContentEl.length > 0) {
        $pageContentEl.css({
          "padding-bottom": ""
        });
      }
    }
    function handleClick(e) {
      var target = e.target;
      var $target = dom7_default(target);
      var keyboardOpened = !device2.desktop && device2.cordova && (window2.Keyboard && window2.Keyboard.isVisible || window2.cordova.plugins && window2.cordova.plugins.Keyboard && window2.cordova.plugins.Keyboard.isVisible);
      if (keyboardOpened)
        return;
      if ($target.closest(sheet.el).length === 0) {
        if (sheet.params.closeByBackdropClick && sheet.params.backdrop && sheet.backdropEl && sheet.backdropEl === target) {
          sheet.close();
        } else if (sheet.params.closeByOutsideClick) {
          sheet.close();
        }
      }
    }
    function onKeyDown(e) {
      var keyCode = e.keyCode;
      if (keyCode === 27 && sheet.params.closeOnEscape) {
        sheet.close();
      }
    }
    var pushOffset;
    function pushViewScale(offset2) {
      return (app.height - offset2 * 2) / app.height;
    }
    var isTouched = false;
    var startTouch;
    var currentTouch;
    var isScrolling;
    var touchStartTime;
    var touchesDiff;
    var isMoved = false;
    var isTopSheetModal;
    var swipeStepTranslate;
    var startTranslate;
    var currentTranslate;
    var sheetElOffsetHeight;
    var minTranslate2;
    var maxTranslate2;
    var $pushViewEl;
    var pushBorderRadius;
    var sheetPageContentEl;
    var sheetPageContentScrollTop;
    var sheetPageContentScrollHeight;
    var sheetPageContentOffsetHeight;
    function handleTouchStart(e) {
      if (isTouched || !(sheet.params.swipeToClose || sheet.params.swipeToStep))
        return;
      if (sheet.params.swipeHandler && dom7_default(e.target).closest(sheet.params.swipeHandler).length === 0) {
        return;
      }
      isTouched = true;
      isMoved = false;
      startTouch = {
        x: e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX,
        y: e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY
      };
      touchStartTime = now();
      isScrolling = void 0;
      isTopSheetModal = $el.hasClass("sheet-modal-top");
      if (!sheet.params.swipeHandler && e.type === "touchstart") {
        sheetPageContentEl = dom7_default(e.target).closest(".page-content")[0];
      }
    }
    function handleTouchMove(e) {
      if (!isTouched)
        return;
      currentTouch = {
        x: e.type === "touchmove" ? e.targetTouches[0].pageX : e.pageX,
        y: e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY
      };
      if (typeof isScrolling === "undefined") {
        isScrolling = !!(isScrolling || Math.abs(currentTouch.x - startTouch.x) > Math.abs(currentTouch.y - startTouch.y));
      }
      if (isScrolling) {
        isTouched = false;
        isMoved = false;
        return;
      }
      touchesDiff = startTouch.y - currentTouch.y;
      var direction = touchesDiff < 0 ? "to-bottom" : "to-top";
      if (!isMoved) {
        if (sheetPageContentEl && !$el.hasClass("modal-in-swipe-step")) {
          sheetPageContentScrollTop = sheetPageContentEl.scrollTop;
          sheetPageContentScrollHeight = sheetPageContentEl.scrollHeight;
          sheetPageContentOffsetHeight = sheetPageContentEl.offsetHeight;
          if (!(sheetPageContentScrollHeight === sheetPageContentOffsetHeight) && !(direction === "to-bottom" && sheetPageContentScrollTop === 0) && !(direction === "to-top" && sheetPageContentScrollTop === sheetPageContentScrollHeight - sheetPageContentOffsetHeight)) {
            $el.transform("");
            isTouched = false;
            isMoved = false;
            return;
          }
        }
        if (sheet.push && pushOffset) {
          $pushViewEl = app.$el.children(".view, .views");
        }
        sheetElOffsetHeight = $el[0].offsetHeight;
        startTranslate = getTranslate($el[0], "y");
        if (isTopSheetModal) {
          minTranslate2 = sheet.params.swipeToClose ? -sheetElOffsetHeight : -swipeStepTranslate;
          maxTranslate2 = 0;
        } else {
          minTranslate2 = 0;
          maxTranslate2 = sheet.params.swipeToClose ? sheetElOffsetHeight : swipeStepTranslate;
        }
        isMoved = true;
      }
      currentTranslate = startTranslate - touchesDiff;
      currentTranslate = Math.min(Math.max(currentTranslate, minTranslate2), maxTranslate2);
      e.preventDefault();
      if (sheet.push && pushOffset) {
        var progress = (currentTranslate - startTranslate) / sheetElOffsetHeight;
        if (sheet.params.swipeToStep) {
          if (isTopSheetModal) {
            progress = currentTranslate / swipeStepTranslate;
          } else {
            progress = 1 - (swipeStepTranslate - currentTranslate) / swipeStepTranslate;
          }
        }
        progress = Math.abs(progress);
        progress = Math.min(Math.max(progress, 0), 1);
        var pushProgress = 1 - progress;
        var scale = 1 - (1 - pushViewScale(pushOffset)) * pushProgress;
        $pushViewEl.transition(0).forEach(function(el) {
          el.style.setProperty("transform", "translate3d(0,0,0) scale(" + scale + ")", "important");
        });
        if (sheet.params.swipeToStep) {
          $pushViewEl.css("border-radius", pushBorderRadius * pushProgress + "px");
        }
      }
      $el.transition(0).transform("translate3d(0," + currentTranslate + "px,0)");
      if (sheet.params.swipeToStep) {
        var _progress;
        if (isTopSheetModal) {
          _progress = 1 - currentTranslate / swipeStepTranslate;
        } else {
          _progress = (swipeStepTranslate - currentTranslate) / swipeStepTranslate;
        }
        _progress = Math.min(Math.max(_progress, 0), 1);
        $el.trigger("sheet:stepprogress", _progress);
        sheet.emit("local::stepProgress sheetStepProgress", sheet, _progress);
      }
    }
    function handleTouchEnd() {
      isTouched = false;
      if (!isMoved) {
        return;
      }
      isMoved = false;
      $el.transform("").transition("");
      if (sheet.push && pushOffset) {
        $pushViewEl.transition("").transform("");
        $pushViewEl.css("border-radius", "");
      }
      var direction = touchesDiff < 0 ? "to-bottom" : "to-top";
      var diff = Math.abs(touchesDiff);
      if (diff === 0 || currentTranslate === startTranslate)
        return;
      var timeDiff = new Date().getTime() - touchStartTime;
      if (!sheet.params.swipeToStep) {
        if (direction !== (isTopSheetModal ? "to-top" : "to-bottom")) {
          return;
        }
        if (timeDiff < 300 && diff > 20 || timeDiff >= 300 && diff > sheetElOffsetHeight / 2) {
          sheet.close();
        }
        return;
      }
      var openDirection = isTopSheetModal ? "to-bottom" : "to-top";
      var closeDirection = isTopSheetModal ? "to-top" : "to-bottom";
      var absCurrentTranslate = Math.abs(currentTranslate);
      var absSwipeStepTranslate = Math.abs(swipeStepTranslate);
      if (timeDiff < 300 && diff > 10) {
        if (direction === openDirection && absCurrentTranslate < absSwipeStepTranslate) {
          $el.removeClass("modal-in-swipe-step");
          $el.trigger("sheet:stepprogress", 1);
          sheet.emit("local::stepProgress sheetStepProgress", sheet, 1);
          sheet.emit("local::_swipeStep", false);
          $el.trigger("sheet:stepopen");
          sheet.emit("local::stepOpen sheetStepOpen", sheet);
          if (sheet.push && pushOffset) {
            sheet.$htmlEl[0].style.setProperty("--f7-sheet-push-scale", pushViewScale(pushOffset));
            $pushViewEl.css("border-radius", "");
          }
        }
        if (direction === closeDirection && absCurrentTranslate > absSwipeStepTranslate) {
          if (sheet.params.swipeToClose) {
            sheet.close();
          } else {
            $el.addClass("modal-in-swipe-step");
            $el.trigger("sheet:stepprogress", 0);
            sheet.emit("local::stepProgress sheetStepProgress", sheet, 0);
            sheet.emit("local::_swipeStep", true);
            $el.trigger("sheet:stepclose");
            sheet.emit("local::stepClose sheetStepClose", sheet);
            if (sheet.push && pushOffset) {
              sheet.$htmlEl[0].style.removeProperty("--f7-sheet-push-scale");
              $pushViewEl.css("border-radius", "0px");
            }
          }
        }
        if (direction === closeDirection && absCurrentTranslate <= absSwipeStepTranslate) {
          $el.addClass("modal-in-swipe-step");
          $el.trigger("sheet:stepprogress", 0);
          sheet.emit("local::stepProgress sheetStepProgress", sheet, 0);
          sheet.emit("local::_swipeStep", true);
          $el.trigger("sheet:stepclose");
          sheet.emit("local::stepClose sheetStepClose", sheet);
          if (sheet.push && pushOffset) {
            sheet.$htmlEl[0].style.removeProperty("--f7-sheet-push-scale");
            $pushViewEl.css("border-radius", "0px");
          }
        }
        return;
      }
      if (timeDiff >= 300) {
        var stepOpened = !$el.hasClass("modal-in-swipe-step");
        if (!stepOpened) {
          if (absCurrentTranslate < absSwipeStepTranslate / 2) {
            $el.removeClass("modal-in-swipe-step");
            $el.trigger("sheet:stepprogress", 1);
            sheet.emit("local::stepProgress sheetStepProgress", sheet, 1);
            sheet.emit("local::_swipeStep", false);
            $el.trigger("sheet:stepopen");
            sheet.emit("local::stepOpen sheetStepOpen", sheet);
            if (sheet.push && pushOffset) {
              sheet.$htmlEl[0].style.setProperty("--f7-sheet-push-scale", pushViewScale(pushOffset));
              $pushViewEl.css("border-radius", "");
            }
          } else if (absCurrentTranslate - absSwipeStepTranslate > (sheetElOffsetHeight - absSwipeStepTranslate) / 2) {
            if (sheet.params.swipeToClose)
              sheet.close();
          }
        } else if (stepOpened) {
          if (absCurrentTranslate > absSwipeStepTranslate + (sheetElOffsetHeight - absSwipeStepTranslate) / 2) {
            if (sheet.params.swipeToClose)
              sheet.close();
          } else if (absCurrentTranslate > absSwipeStepTranslate / 2) {
            $el.addClass("modal-in-swipe-step");
            $el.trigger("sheet:stepprogress", 0);
            sheet.emit("local::stepProgress sheetStepProgress", sheet, 0);
            sheet.emit("local::_swipeStep", true);
            $el.trigger("sheet:stepclose");
            sheet.emit("local::stepClose sheetStepClose", sheet);
            if (sheet.push && pushOffset) {
              sheet.$htmlEl[0].style.removeProperty("--f7-sheet-push-scale");
              $pushViewEl.css("border-radius", "0px");
            }
          }
        }
      }
    }
    sheet.setSwipeStep = function setSwipeStep(byResize) {
      if (byResize === void 0) {
        byResize = true;
      }
      var $swipeStepEl = $el.find(".sheet-modal-swipe-step").eq(0);
      if (!$swipeStepEl.length)
        return;
      if ($el.hasClass("sheet-modal-top")) {
        swipeStepTranslate = -($swipeStepEl.offset().top - $el.offset().top + $swipeStepEl[0].offsetHeight);
      } else {
        swipeStepTranslate = $el[0].offsetHeight - ($swipeStepEl.offset().top - $el.offset().top + $swipeStepEl[0].offsetHeight);
      }
      $el[0].style.setProperty("--f7-sheet-swipe-step", swipeStepTranslate + "px");
      if (!byResize) {
        $el.addClass("modal-in-swipe-step");
        sheet.emit("local::_swipeStep", true);
      }
    };
    function onResize2() {
      sheet.setSwipeStep(true);
    }
    var passive = support2.passiveListener ? {
      passive: true
    } : false;
    if (sheet.params.swipeToClose || sheet.params.swipeToStep) {
      $el.on(app.touchEvents.start, handleTouchStart, passive);
      app.on("touchmove", handleTouchMove);
      app.on("touchend:passive", handleTouchEnd);
      sheet.once("sheetDestroy", function() {
        $el.off(app.touchEvents.start, handleTouchStart, passive);
        app.off("touchmove", handleTouchMove);
        app.off("touchend:passive", handleTouchEnd);
      });
    }
    sheet.on("open", function() {
      if (sheet.params.closeOnEscape) {
        dom7_default(document).on("keydown", onKeyDown);
      }
      $el.prevAll(".popup.modal-in").addClass("popup-behind");
      if (sheet.params.swipeToStep) {
        sheet.setSwipeStep(false);
        app.on("resize", onResize2);
      }
      if (sheet.params.scrollToEl) {
        scrollToElementOnOpen();
      }
      if (sheet.push) {
        pushOffset = parseInt($el.css("--f7-sheet-push-offset"), 10);
        if (Number.isNaN(pushOffset))
          pushOffset = 0;
        if (pushOffset) {
          $el.addClass("sheet-modal-push");
          sheet.$htmlEl.addClass("with-modal-sheet-push");
          if (!sheet.params.swipeToStep) {
            sheet.$htmlEl[0].style.setProperty("--f7-sheet-push-scale", pushViewScale(pushOffset));
          } else {
            $pushViewEl = app.$el.children(".view, .views");
            pushBorderRadius = parseFloat($el.css("border-" + (isTopSheetModal ? "bottom" : "top") + "-left-radius"));
            $pushViewEl.css("border-radius", "0px");
          }
        }
      }
    });
    sheet.on("opened", function() {
      if (sheet.params.closeByOutsideClick || sheet.params.closeByBackdropClick) {
        app.on("click", handleClick);
      }
    });
    sheet.on("close", function() {
      if (sheet.params.swipeToStep) {
        $el.removeClass("modal-in-swipe-step");
        sheet.emit("local::_swipeStep", false);
        app.off("resize", onResize2);
      }
      if (sheet.params.closeOnEscape) {
        dom7_default(document).off("keydown", onKeyDown);
      }
      if (sheet.params.scrollToEl) {
        scrollToElementOnClose();
      }
      if (sheet.params.closeByOutsideClick || sheet.params.closeByBackdropClick) {
        app.off("click", handleClick);
      }
      $el.prevAll(".popup.modal-in").eq(0).removeClass("popup-behind");
      if (sheet.push && pushOffset) {
        sheet.$htmlEl.removeClass("with-modal-sheet-push");
        sheet.$htmlEl.addClass("with-modal-sheet-push-closing");
      }
    });
    sheet.on("closed", function() {
      if (sheet.push && pushOffset) {
        sheet.$htmlEl.removeClass("with-modal-sheet-push-closing");
        sheet.$htmlEl[0].style.removeProperty("--f7-sheet-push-scale");
      }
    });
    sheet.stepOpen = function stepOpen() {
      $el.removeClass("modal-in-swipe-step");
      sheet.emit("local::_swipeStep", false);
      if (sheet.push) {
        if (!pushOffset) {
          pushOffset = parseInt($el.css("--f7-sheet-push-offset"), 10);
          if (Number.isNaN(pushOffset))
            pushOffset = 0;
        }
        if (pushOffset) {
          sheet.$htmlEl[0].style.setProperty("--f7-sheet-push-scale", pushViewScale(pushOffset));
        }
      }
    };
    sheet.stepClose = function stepClose() {
      $el.addClass("modal-in-swipe-step");
      sheet.emit("local::_swipeStep", true);
      if (sheet.push) {
        sheet.$htmlEl[0].style.removeProperty("--f7-sheet-push-scale");
      }
    };
    sheet.stepToggle = function stepToggle() {
      $el.toggleClass("modal-in-swipe-step");
      sheet.emit("local::_swipeStep", $el.hasClass("modal-in-swipe-step"));
    };
    $el[0].f7Modal = sheet;
    return sheet || _assertThisInitialized6(_this);
  }
  return Sheet2;
}(modal_class_default);
var sheet_class_default = Sheet;

// node_modules/framework7/esm/components/sheet/sheet.js
var sheet_default = {
  name: "sheet",
  params: {
    sheet: {
      push: false,
      backdrop: void 0,
      backdropEl: void 0,
      closeByBackdropClick: true,
      closeByOutsideClick: false,
      closeOnEscape: false,
      swipeToClose: false,
      swipeToStep: false,
      swipeHandler: null,
      containerEl: null
    }
  },
  static: {
    Sheet: sheet_class_default
  },
  create: function create6() {
    var app = this;
    app.sheet = extend({}, ModalMethods({
      app,
      constructor: sheet_class_default,
      defaultSelector: ".sheet-modal.modal-in"
    }), {
      stepOpen: function stepOpen(sheet) {
        var sheetInstance = app.sheet.get(sheet);
        if (sheetInstance && sheetInstance.stepOpen)
          return sheetInstance.stepOpen();
        return void 0;
      },
      stepClose: function stepClose(sheet) {
        var sheetInstance = app.sheet.get(sheet);
        if (sheetInstance && sheetInstance.stepClose)
          return sheetInstance.stepClose();
        return void 0;
      },
      stepToggle: function stepToggle(sheet) {
        var sheetInstance = app.sheet.get(sheet);
        if (sheetInstance && sheetInstance.stepToggle)
          return sheetInstance.stepToggle();
        return void 0;
      }
    });
  },
  clicks: {
    ".sheet-open": function openSheet($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      if (dom7_default(".sheet-modal.modal-in").length > 0 && data.sheet && dom7_default(data.sheet)[0] !== dom7_default(".sheet-modal.modal-in")[0]) {
        app.sheet.close(".sheet-modal.modal-in");
      }
      app.sheet.open(data.sheet, data.animate, $clickedEl);
    },
    ".sheet-close": function closeSheet($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.sheet.close(data.sheet, data.animate, $clickedEl);
    }
  }
};

// node_modules/framework7/esm/components/toast/toast-class.js
function _assertThisInitialized7(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose7(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf7(subClass, superClass);
}
function _setPrototypeOf7(o, p) {
  _setPrototypeOf7 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf7(o, p);
}
var Toast = function(_Modal) {
  _inheritsLoose7(Toast2, _Modal);
  function Toast2(app, params) {
    var _this;
    var extendedParams = extend({
      on: {}
    }, app.params.toast, params);
    _this = _Modal.call(this, app, extendedParams) || this;
    var toast = _assertThisInitialized7(_this);
    var window2 = getWindow();
    toast.app = app;
    toast.params = extendedParams;
    var _toast$params = toast.params, closeButton = _toast$params.closeButton, closeTimeout = _toast$params.closeTimeout;
    var $el;
    if (!toast.params.el) {
      var toastHtml = toast.render();
      $el = dom7_default(toastHtml);
    } else {
      $el = dom7_default(toast.params.el);
    }
    if ($el && $el.length > 0 && $el[0].f7Modal) {
      return $el[0].f7Modal || _assertThisInitialized7(_this);
    }
    if ($el.length === 0) {
      return toast.destroy() || _assertThisInitialized7(_this);
    }
    extend(toast, {
      $el,
      el: $el[0],
      type: "toast"
    });
    $el[0].f7Modal = toast;
    if (closeButton) {
      $el.find(".toast-button").on("click", function() {
        toast.emit("local::closeButtonClick toastCloseButtonClick", toast);
        toast.close();
      });
      toast.on("beforeDestroy", function() {
        $el.find(".toast-button").off("click");
      });
    }
    var timeoutId;
    toast.on("open", function() {
      dom7_default(".toast.modal-in").each(function(openedEl) {
        var toastInstance = app.toast.get(openedEl);
        if (openedEl !== toast.el && toastInstance) {
          toastInstance.close();
        }
      });
      if (closeTimeout) {
        timeoutId = nextTick(function() {
          toast.close();
        }, closeTimeout);
      }
    });
    toast.on("close", function() {
      window2.clearTimeout(timeoutId);
    });
    if (toast.params.destroyOnClose) {
      toast.once("closed", function() {
        setTimeout(function() {
          toast.destroy();
        }, 0);
      });
    }
    return toast || _assertThisInitialized7(_this);
  }
  var _proto = Toast2.prototype;
  _proto.render = function render15() {
    var toast = this;
    if (toast.params.render)
      return toast.params.render.call(toast, toast);
    var _toast$params2 = toast.params, position = _toast$params2.position, horizontalPosition = _toast$params2.horizontalPosition, cssClass = _toast$params2.cssClass, icon = _toast$params2.icon, text2 = _toast$params2.text, closeButton = _toast$params2.closeButton, closeButtonColor = _toast$params2.closeButtonColor, closeButtonText = _toast$params2.closeButtonText;
    var horizontalClass = position === "top" || position === "bottom" ? "toast-horizontal-" + horizontalPosition : "";
    return jsx_default2("div", {
      class: "toast toast-" + position + " " + horizontalClass + " " + (cssClass || "") + " " + (icon ? "toast-with-icon" : "")
    }, jsx_default2("div", {
      class: "toast-content"
    }, icon && jsx_default2("div", {
      class: "toast-icon"
    }, icon), jsx_default2("div", {
      class: "toast-text"
    }, text2), closeButton && !icon && jsx_default2("a", {
      class: "toast-button button " + (closeButtonColor ? "color-" + closeButtonColor : "")
    }, closeButtonText)));
  };
  return Toast2;
}(modal_class_default);
var toast_class_default = Toast;

// node_modules/framework7/esm/components/toast/toast.js
var toast_default = {
  name: "toast",
  static: {
    Toast: toast_class_default
  },
  create: function create7() {
    var app = this;
    app.toast = extend({}, ModalMethods({
      app,
      constructor: toast_class_default,
      defaultSelector: ".toast.modal-in"
    }), {
      show: function show4(params) {
        extend(params, {
          destroyOnClose: true
        });
        return new toast_class_default(app, params).open();
      }
    });
  },
  params: {
    toast: {
      icon: null,
      text: null,
      position: "bottom",
      horizontalPosition: "left",
      closeButton: false,
      closeButtonColor: null,
      closeButtonText: "Ok",
      closeTimeout: null,
      cssClass: null,
      render: null,
      containerEl: null
    }
  }
};

// node_modules/framework7/esm/components/preloader/preloader.js
var Preloader = {
  init: function init(el) {
    var app = this;
    var preloaders = {
      iosPreloaderContent,
      mdPreloaderContent,
      auroraPreloaderContent
    };
    var $el = dom7_default(el);
    if ($el.length === 0 || $el.children(".preloader-inner").length > 0 || $el.children(".preloader-inner-line").length > 0)
      return;
    $el.append(preloaders[app.theme + "PreloaderContent"]);
  },
  visible: false,
  show: function show(color) {
    if (color === void 0) {
      color = "white";
    }
    var app = this;
    if (Preloader.visible)
      return;
    var preloaders = {
      iosPreloaderContent,
      mdPreloaderContent,
      auroraPreloaderContent
    };
    var preloaderInner = preloaders[app.theme + "PreloaderContent"] || "";
    dom7_default("html").addClass("with-modal-preloader");
    app.$el.append('\n      <div class="preloader-backdrop"></div>\n      <div class="preloader-modal">\n        <div class="preloader color-' + color + '">' + preloaderInner + "</div>\n      </div>\n    ");
    Preloader.visible = true;
  },
  showIn: function showIn(el, color) {
    if (color === void 0) {
      color = "white";
    }
    var app = this;
    var preloaders = {
      iosPreloaderContent,
      mdPreloaderContent,
      auroraPreloaderContent
    };
    var preloaderInner = preloaders[app.theme + "PreloaderContent"] || "";
    dom7_default(el || "html").addClass("with-modal-preloader");
    dom7_default(el || app.$el).append('\n      <div class="preloader-backdrop"></div>\n      <div class="preloader-modal">\n        <div class="preloader color-' + color + '">' + preloaderInner + "</div>\n      </div>\n    ");
  },
  hide: function hide() {
    var app = this;
    if (!Preloader.visible)
      return;
    dom7_default("html").removeClass("with-modal-preloader");
    app.$el.find(".preloader-backdrop, .preloader-modal").remove();
    Preloader.visible = false;
  },
  hideIn: function hideIn(el) {
    var app = this;
    dom7_default(el || "html").removeClass("with-modal-preloader");
    dom7_default(el || app.$el).find(".preloader-backdrop, .preloader-modal").remove();
  }
};
var preloader_default = {
  name: "preloader",
  create: function create8() {
    var app = this;
    bindMethods(app, {
      preloader: Preloader
    });
  },
  on: {
    photoBrowserOpen: function photoBrowserOpen(pb) {
      var app = this;
      pb.$el.find(".preloader").each(function(preloaderEl) {
        app.preloader.init(preloaderEl);
      });
    },
    tabMounted: function tabMounted(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".preloader").each(function(preloaderEl) {
        app.preloader.init(preloaderEl);
      });
    },
    pageInit: function pageInit(page) {
      var app = this;
      page.$el.find(".preloader").each(function(preloaderEl) {
        app.preloader.init(preloaderEl);
      });
    }
  },
  vnode: {
    preloader: {
      insert: function insert(vnode) {
        var app = this;
        var preloaderEl = vnode.elm;
        app.preloader.init(preloaderEl);
      }
    }
  }
};

// node_modules/framework7/esm/components/progressbar/progressbar.js
var Progressbar = {
  set: function set() {
    var app = this;
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    var el = args[0], progress = args[1], duration = args[2];
    if (typeof args[0] === "number") {
      progress = args[0];
      duration = args[1];
      el = app.$el;
    }
    if (typeof progress === "undefined" || progress === null)
      return el;
    if (!progress)
      progress = 0;
    var $el = dom7_default(el || app.$el);
    if ($el.length === 0) {
      return el;
    }
    var progressNormalized = Math.min(Math.max(progress, 0), 100);
    var $progressbarEl;
    if ($el.hasClass("progressbar"))
      $progressbarEl = $el.eq(0);
    else {
      $progressbarEl = $el.children(".progressbar");
    }
    if ($progressbarEl.length === 0 || $progressbarEl.hasClass("progressbar-infinite")) {
      return $progressbarEl;
    }
    var $progressbarLine = $progressbarEl.children("span");
    if ($progressbarLine.length === 0) {
      $progressbarLine = dom7_default("<span></span>");
      $progressbarEl.append($progressbarLine);
    }
    $progressbarLine.transition(typeof duration !== "undefined" ? duration : "").transform("translate3d(" + (-100 + progressNormalized) * (app.rtl ? -1 : 1) + "%,0,0)");
    return $progressbarEl[0];
  },
  show: function show2() {
    var app = this;
    for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      args[_key2] = arguments[_key2];
    }
    var el = args[0], progress = args[1], color = args[2];
    var type = "determined";
    if (args.length === 2) {
      if ((typeof args[0] === "string" || typeof args[0] === "object") && typeof args[1] === "string") {
        el = args[0];
        color = args[1];
        progress = args[2];
        type = "infinite";
      } else if (typeof args[0] === "number" && typeof args[1] === "string") {
        progress = args[0];
        color = args[1];
        el = app.$el;
      }
    } else if (args.length === 1) {
      if (typeof args[0] === "number") {
        el = app.$el;
        progress = args[0];
      } else if (typeof args[0] === "string") {
        type = "infinite";
        el = app.$el;
        color = args[0];
      }
    } else if (args.length === 0) {
      type = "infinite";
      el = app.$el;
    }
    var $el = dom7_default(el);
    if ($el.length === 0)
      return void 0;
    var $progressbarEl;
    if ($el.hasClass("progressbar") || $el.hasClass("progressbar-infinite")) {
      $progressbarEl = $el;
    } else {
      $progressbarEl = $el.children(".progressbar:not(.progressbar-out), .progressbar-infinite:not(.progressbar-out)");
      if ($progressbarEl.length === 0) {
        $progressbarEl = dom7_default('\n          <span class="progressbar' + (type === "infinite" ? "-infinite" : "") + (color ? " color-" + color : "") + ' progressbar-in">\n            ' + (type === "infinite" ? "" : "<span></span>") + "\n          </span>");
        $el.append($progressbarEl);
      }
    }
    if (typeof progress !== "undefined") {
      app.progressbar.set($progressbarEl, progress);
    }
    return $progressbarEl[0];
  },
  hide: function hide2(el, removeAfterHide) {
    if (removeAfterHide === void 0) {
      removeAfterHide = true;
    }
    var app = this;
    var $el = dom7_default(el || app.$el);
    if ($el.length === 0)
      return void 0;
    var $progressbarEl;
    if ($el.hasClass("progressbar") || $el.hasClass("progressbar-infinite")) {
      $progressbarEl = $el;
    } else {
      $progressbarEl = $el.children(".progressbar, .progressbar-infinite");
    }
    if ($progressbarEl.length === 0 || !$progressbarEl.hasClass("progressbar-in") || $progressbarEl.hasClass("progressbar-out")) {
      return $progressbarEl;
    }
    $progressbarEl.removeClass("progressbar-in").addClass("progressbar-out").animationEnd(function() {
      if (removeAfterHide) {
        $progressbarEl.remove();
      }
    });
    return $progressbarEl;
  }
};
var progressbar_default = {
  name: "progressbar",
  create: function create9() {
    var app = this;
    bindMethods(app, {
      progressbar: Progressbar
    });
  },
  on: {
    tabMounted: function tabMounted2(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".progressbar").each(function(progressbarEl) {
        var $progressbarEl = dom7_default(progressbarEl);
        app.progressbar.set($progressbarEl, $progressbarEl.attr("data-progress"));
      });
    },
    pageInit: function pageInit2(page) {
      var app = this;
      page.$el.find(".progressbar").each(function(progressbarEl) {
        var $progressbarEl = dom7_default(progressbarEl);
        app.progressbar.set($progressbarEl, $progressbarEl.attr("data-progress"));
      });
    }
  },
  vnode: {
    progressbar: {
      insert: function insert2(vnode) {
        var app = this;
        var el = vnode.elm;
        app.progressbar.set(el, el.getAttribute("data-progress"));
      },
      update: function update(vnode) {
        var app = this;
        var el = vnode.elm;
        app.progressbar.set(el, el.getAttribute("data-progress"));
      }
    }
  }
};

// node_modules/framework7/esm/components/sortable/sortable.js
var Sortable = {
  init: function init2() {
    var app = this;
    var document = getDocument();
    var isTouched;
    var isMoved;
    var touchStartY;
    var touchesDiff;
    var $sortingEl;
    var $sortingItems;
    var $sortableContainer;
    var sortingElHeight;
    var minTop;
    var maxTop;
    var $insertAfterEl;
    var $insertBeforeEl;
    var indexFrom;
    var $pageEl;
    var $pageContentEl;
    var pageHeight;
    var pageOffset;
    var sortingElOffsetLocal;
    var sortingElOffsetTop;
    var initialScrollTop;
    var wasTapHold;
    function handleTouchStart(e, isTapHold) {
      isMoved = false;
      isTouched = true;
      wasTapHold = false;
      touchStartY = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
      $sortingEl = dom7_default(e.target).closest("li").eq(0);
      indexFrom = $sortingEl.index();
      $sortableContainer = $sortingEl.parents(".sortable");
      var $listGroup = $sortingEl.parents(".list-group");
      if ($listGroup.length && $listGroup.parents($sortableContainer).length) {
        $sortableContainer = $listGroup;
      }
      $sortingItems = $sortableContainer.children("ul").children("li:not(.disallow-sorting):not(.no-sorting)");
      if (app.panel)
        app.panel.allowOpen = false;
      if (app.swipeout)
        app.swipeout.allow = false;
      if (isTapHold) {
        $sortingEl.addClass("sorting");
        $sortableContainer.addClass("sortable-sorting");
        wasTapHold = true;
      }
    }
    function handleTouchMove(e) {
      if (!isTouched || !$sortingEl)
        return;
      var pageY = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
      if (!isMoved) {
        $pageEl = $sortingEl.parents(".page");
        $pageContentEl = $sortingEl.parents(".page-content");
        var paddingTop = parseInt($pageContentEl.css("padding-top"), 10);
        var paddingBottom = parseInt($pageContentEl.css("padding-bottom"), 10);
        initialScrollTop = $pageContentEl[0].scrollTop;
        pageOffset = $pageEl.offset().top + paddingTop;
        pageHeight = $pageEl.height() - paddingTop - paddingBottom;
        $sortingEl.addClass("sorting");
        $sortableContainer.addClass("sortable-sorting");
        sortingElOffsetLocal = $sortingEl[0].offsetTop;
        minTop = $sortingEl[0].offsetTop;
        maxTop = $sortingEl.parent().height() - sortingElOffsetLocal - $sortingEl.height();
        sortingElHeight = $sortingEl[0].offsetHeight;
        sortingElOffsetTop = $sortingEl.offset().top;
      }
      isMoved = true;
      e.preventDefault();
      e.f7PreventSwipePanel = true;
      touchesDiff = pageY - touchStartY;
      var translateScrollOffset = $pageContentEl[0].scrollTop - initialScrollTop;
      var translate = Math.min(Math.max(touchesDiff + translateScrollOffset, -minTop), maxTop);
      $sortingEl.transform("translate3d(0," + translate + "px,0)");
      var scrollAddition = 44;
      var allowScroll = true;
      if (touchesDiff + translateScrollOffset + scrollAddition < -minTop) {
        allowScroll = false;
      }
      if (touchesDiff + translateScrollOffset - scrollAddition > maxTop) {
        allowScroll = false;
      }
      $insertBeforeEl = void 0;
      $insertAfterEl = void 0;
      var scrollDiff;
      if (allowScroll) {
        if (sortingElOffsetTop + touchesDiff + sortingElHeight + scrollAddition > pageOffset + pageHeight) {
          scrollDiff = sortingElOffsetTop + touchesDiff + sortingElHeight + scrollAddition - (pageOffset + pageHeight);
        }
        if (sortingElOffsetTop + touchesDiff < pageOffset + scrollAddition) {
          scrollDiff = sortingElOffsetTop + touchesDiff - pageOffset - scrollAddition;
        }
        if (scrollDiff) {
          $pageContentEl[0].scrollTop += scrollDiff;
        }
      }
      $sortingItems.each(function(el) {
        var $currentEl = dom7_default(el);
        if ($currentEl[0] === $sortingEl[0])
          return;
        var currentElOffset = $currentEl[0].offsetTop;
        var currentElHeight = $currentEl.height();
        var sortingElOffset = sortingElOffsetLocal + translate;
        var currentTranslate;
        var prevTranslate = $currentEl[0].f7Translate;
        if (sortingElOffset >= currentElOffset - currentElHeight / 2 && $sortingEl.index() < $currentEl.index()) {
          currentTranslate = -sortingElHeight;
          $currentEl.transform("translate3d(0, " + currentTranslate + "px,0)");
          $insertAfterEl = $currentEl;
          $insertBeforeEl = void 0;
        } else if (sortingElOffset <= currentElOffset + currentElHeight / 2 && $sortingEl.index() > $currentEl.index()) {
          currentTranslate = sortingElHeight;
          $currentEl[0].f7Translate = currentTranslate;
          $currentEl.transform("translate3d(0, " + currentTranslate + "px,0)");
          $insertAfterEl = void 0;
          if (!$insertBeforeEl)
            $insertBeforeEl = $currentEl;
        } else {
          currentTranslate = void 0;
          $currentEl.transform("translate3d(0, 0%,0)");
        }
        if (prevTranslate !== currentTranslate) {
          $currentEl.trigger("sortable:move");
          app.emit("sortableMove", $currentEl[0], $sortableContainer[0]);
        }
        $currentEl[0].f7Translate = currentTranslate;
      });
    }
    function handleTouchEnd() {
      if (!isTouched || !isMoved) {
        if (isTouched && !isMoved) {
          if (app.panel)
            app.panel.allowOpen = true;
          if (app.swipeout)
            app.swipeout.allow = true;
          if (wasTapHold) {
            $sortingEl.removeClass("sorting");
            $sortableContainer.removeClass("sortable-sorting");
          }
        }
        isTouched = false;
        isMoved = false;
        return;
      }
      if (app.panel)
        app.panel.allowOpen = true;
      if (app.swipeout)
        app.swipeout.allow = true;
      $sortingItems.transform("");
      $sortingEl.removeClass("sorting");
      $sortableContainer.removeClass("sortable-sorting");
      var indexTo;
      if ($insertAfterEl)
        indexTo = $insertAfterEl.index();
      else if ($insertBeforeEl)
        indexTo = $insertBeforeEl.index();
      var moveElements = $sortableContainer.dataset().sortableMoveElements;
      if (typeof moveElements === "undefined") {
        moveElements = app.params.sortable.moveElements;
      }
      if (moveElements) {
        if ($insertAfterEl) {
          $sortingEl.insertAfter($insertAfterEl);
        }
        if ($insertBeforeEl) {
          $sortingEl.insertBefore($insertBeforeEl);
        }
      }
      if (($insertAfterEl || $insertBeforeEl) && $sortableContainer.hasClass("virtual-list")) {
        indexFrom = $sortingEl[0].f7VirtualListIndex;
        if (typeof indexFrom === "undefined")
          indexFrom = $sortingEl.attr("data-virtual-list-index");
        if ($insertBeforeEl) {
          indexTo = $insertBeforeEl[0].f7VirtualListIndex;
          if (typeof indexTo === "undefined")
            indexTo = $insertBeforeEl.attr("data-virtual-list-index");
        } else {
          indexTo = $insertAfterEl[0].f7VirtualListIndex;
          if (typeof indexTo === "undefined")
            indexTo = $insertAfterEl.attr("data-virtual-list-index");
        }
        if (indexTo !== null)
          indexTo = parseInt(indexTo, 10);
        else
          indexTo = void 0;
        var virtualList = $sortableContainer[0].f7VirtualList;
        if (indexFrom)
          indexFrom = parseInt(indexFrom, 10);
        if (indexTo)
          indexTo = parseInt(indexTo, 10);
        if (virtualList)
          virtualList.moveItem(indexFrom, indexTo);
      }
      if (typeof indexTo !== "undefined" && !Number.isNaN(indexTo) && indexTo !== indexFrom) {
        $sortingEl.trigger("sortable:sort", {
          from: indexFrom,
          to: indexTo
        });
        app.emit("sortableSort", $sortingEl[0], {
          from: indexFrom,
          to: indexTo,
          el: $sortingEl[0]
        }, $sortableContainer[0]);
      }
      $insertBeforeEl = void 0;
      $insertAfterEl = void 0;
      isTouched = false;
      isMoved = false;
    }
    var activeListener = getSupport().passiveListener ? {
      passive: false,
      capture: false
    } : false;
    dom7_default(document).on(app.touchEvents.start, ".list.sortable .sortable-handler", handleTouchStart, activeListener);
    app.on("touchmove:active", handleTouchMove);
    app.on("touchend:passive", handleTouchEnd);
    dom7_default(document).on("taphold", ".sortable-tap-hold", function(e, pointerEvent) {
      handleTouchStart(pointerEvent, true);
    });
  },
  enable: function enable(el) {
    if (el === void 0) {
      el = ".list.sortable";
    }
    var app = this;
    var $el = dom7_default(el);
    if ($el.length === 0)
      return;
    $el.addClass("sortable-enabled");
    $el.trigger("sortable:enable");
    app.emit("sortableEnable", $el[0]);
  },
  disable: function disable(el) {
    if (el === void 0) {
      el = ".list.sortable";
    }
    var app = this;
    var $el = dom7_default(el);
    if ($el.length === 0)
      return;
    $el.removeClass("sortable-enabled");
    $el.trigger("sortable:disable");
    app.emit("sortableDisable", $el[0]);
  },
  toggle: function toggle(el) {
    if (el === void 0) {
      el = ".list.sortable";
    }
    var app = this;
    var $el = dom7_default(el);
    if ($el.length === 0)
      return;
    if ($el.hasClass("sortable-enabled")) {
      app.sortable.disable($el);
    } else {
      app.sortable.enable($el);
    }
  }
};
var sortable_default = {
  name: "sortable",
  params: {
    sortable: {
      moveElements: true
    }
  },
  create: function create10() {
    var app = this;
    bindMethods(app, {
      sortable: Sortable
    });
  },
  on: {
    init: function init3() {
      var app = this;
      if (!app.params.sortable)
        return;
      app.sortable.init();
    }
  },
  clicks: {
    ".sortable-enable": function enable2($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.sortable.enable(data.sortable);
    },
    ".sortable-disable": function disable2($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.sortable.disable(data.sortable);
    },
    ".sortable-toggle": function toggle2($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.sortable.toggle(data.sortable);
    }
  }
};

// node_modules/framework7/esm/components/swipeout/swipeout.js
var Swipeout = {
  init: function init4() {
    var app = this;
    var document = getDocument();
    var touchesStart = {};
    var isTouched;
    var isMoved;
    var isScrolling;
    var touchStartTime;
    var touchesDiff;
    var $swipeoutEl;
    var $swipeoutContent;
    var $actionsRight;
    var $actionsLeft;
    var actionsLeftWidth;
    var actionsRightWidth;
    var translate;
    var opened;
    var openedActionsSide;
    var $leftButtons;
    var $rightButtons;
    var direction;
    var $overswipeLeftButton;
    var $overswipeRightButton;
    var overswipeLeft;
    var overswipeRight;
    function handleTouchStart(e) {
      if (!app.swipeout.allow)
        return;
      isMoved = false;
      isTouched = true;
      isScrolling = void 0;
      touchesStart.x = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
      touchesStart.y = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
      touchStartTime = new Date().getTime();
      $swipeoutEl = dom7_default(this);
    }
    function handleTouchMove(e) {
      if (!isTouched)
        return;
      var pageX = e.type === "touchmove" ? e.targetTouches[0].pageX : e.pageX;
      var pageY = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
      if (typeof isScrolling === "undefined") {
        isScrolling = !!(isScrolling || Math.abs(pageY - touchesStart.y) > Math.abs(pageX - touchesStart.x));
      }
      if (isScrolling) {
        isTouched = false;
        return;
      }
      if (!isMoved) {
        if (dom7_default(".list.sortable-opened").length > 0)
          return;
        $swipeoutContent = $swipeoutEl.find(".swipeout-content");
        $actionsRight = $swipeoutEl.find(".swipeout-actions-right");
        $actionsLeft = $swipeoutEl.find(".swipeout-actions-left");
        actionsLeftWidth = null;
        actionsRightWidth = null;
        $leftButtons = null;
        $rightButtons = null;
        $overswipeRightButton = null;
        $overswipeLeftButton = null;
        if ($actionsLeft.length > 0) {
          actionsLeftWidth = $actionsLeft.outerWidth();
          $leftButtons = $actionsLeft.children("a");
          $overswipeLeftButton = $actionsLeft.find(".swipeout-overswipe");
        }
        if ($actionsRight.length > 0) {
          actionsRightWidth = $actionsRight.outerWidth();
          $rightButtons = $actionsRight.children("a");
          $overswipeRightButton = $actionsRight.find(".swipeout-overswipe");
        }
        opened = $swipeoutEl.hasClass("swipeout-opened");
        if (opened) {
          openedActionsSide = $swipeoutEl.find(".swipeout-actions-left.swipeout-actions-opened").length > 0 ? "left" : "right";
        }
        $swipeoutEl.removeClass("swipeout-transitioning");
        if (!app.params.swipeout.noFollow) {
          $swipeoutEl.find(".swipeout-actions-opened").removeClass("swipeout-actions-opened");
          $swipeoutEl.removeClass("swipeout-opened");
        }
      }
      isMoved = true;
      if (e.cancelable) {
        e.preventDefault();
      }
      touchesDiff = pageX - touchesStart.x;
      translate = touchesDiff;
      if (opened) {
        if (openedActionsSide === "right")
          translate -= actionsRightWidth;
        else
          translate += actionsLeftWidth;
      }
      if (translate > 0 && $actionsLeft.length === 0 || translate < 0 && $actionsRight.length === 0) {
        if (!opened) {
          isTouched = false;
          isMoved = false;
          $swipeoutContent.transform("");
          if ($rightButtons && $rightButtons.length > 0) {
            $rightButtons.transform("");
          }
          if ($leftButtons && $leftButtons.length > 0) {
            $leftButtons.transform("");
          }
          return;
        }
        translate = 0;
      }
      if (translate < 0)
        direction = "to-left";
      else if (translate > 0)
        direction = "to-right";
      else if (!direction)
        direction = "to-left";
      var buttonOffset;
      var progress;
      e.f7PreventSwipePanel = true;
      if (app.params.swipeout.noFollow) {
        if (opened) {
          if (openedActionsSide === "right" && touchesDiff > 0) {
            app.swipeout.close($swipeoutEl);
          }
          if (openedActionsSide === "left" && touchesDiff < 0) {
            app.swipeout.close($swipeoutEl);
          }
        } else {
          if (touchesDiff < 0 && $actionsRight.length > 0) {
            app.swipeout.open($swipeoutEl, "right");
          }
          if (touchesDiff > 0 && $actionsLeft.length > 0) {
            app.swipeout.open($swipeoutEl, "left");
          }
        }
        isTouched = false;
        isMoved = false;
        return;
      }
      overswipeLeft = false;
      overswipeRight = false;
      if ($actionsRight.length > 0) {
        var buttonTranslate = translate;
        progress = buttonTranslate / actionsRightWidth;
        if (buttonTranslate < -actionsRightWidth) {
          var ratio = buttonTranslate / -actionsRightWidth;
          buttonTranslate = -actionsRightWidth - Math.pow(-buttonTranslate - actionsRightWidth, 0.8);
          translate = buttonTranslate;
          if ($overswipeRightButton.length > 0 && ratio > app.params.swipeout.overswipeRatio) {
            overswipeRight = true;
          }
        }
        if (direction !== "to-left") {
          progress = 0;
          buttonTranslate = 0;
        }
        $rightButtons.each(function(buttonEl) {
          var $buttonEl = dom7_default(buttonEl);
          if (typeof buttonEl.f7SwipeoutButtonOffset === "undefined") {
            $buttonEl[0].f7SwipeoutButtonOffset = buttonEl.offsetLeft;
          }
          buttonOffset = buttonEl.f7SwipeoutButtonOffset;
          if ($overswipeRightButton.length > 0 && $buttonEl.hasClass("swipeout-overswipe") && direction === "to-left") {
            $buttonEl.css({
              left: (overswipeRight ? -buttonOffset : 0) + "px"
            });
            if (overswipeRight) {
              if (!$buttonEl.hasClass("swipeout-overswipe-active")) {
                $swipeoutEl.trigger("swipeout:overswipeenter");
                app.emit("swipeoutOverswipeEnter", $swipeoutEl[0]);
              }
              $buttonEl.addClass("swipeout-overswipe-active");
            } else {
              if ($buttonEl.hasClass("swipeout-overswipe-active")) {
                $swipeoutEl.trigger("swipeout:overswipeexit");
                app.emit("swipeoutOverswipeExit", $swipeoutEl[0]);
              }
              $buttonEl.removeClass("swipeout-overswipe-active");
            }
          }
          $buttonEl.transform("translate3d(" + (buttonTranslate - buttonOffset * (1 + Math.max(progress, -1))) + "px,0,0)");
        });
      }
      if ($actionsLeft.length > 0) {
        var _buttonTranslate = translate;
        progress = _buttonTranslate / actionsLeftWidth;
        if (_buttonTranslate > actionsLeftWidth) {
          var _ratio = _buttonTranslate / actionsRightWidth;
          _buttonTranslate = actionsLeftWidth + Math.pow(_buttonTranslate - actionsLeftWidth, 0.8);
          translate = _buttonTranslate;
          if ($overswipeLeftButton.length > 0 && _ratio > app.params.swipeout.overswipeRatio) {
            overswipeLeft = true;
          }
        }
        if (direction !== "to-right") {
          _buttonTranslate = 0;
          progress = 0;
        }
        $leftButtons.each(function(buttonEl, index2) {
          var $buttonEl = dom7_default(buttonEl);
          if (typeof buttonEl.f7SwipeoutButtonOffset === "undefined") {
            $buttonEl[0].f7SwipeoutButtonOffset = actionsLeftWidth - buttonEl.offsetLeft - buttonEl.offsetWidth;
          }
          buttonOffset = buttonEl.f7SwipeoutButtonOffset;
          if ($overswipeLeftButton.length > 0 && $buttonEl.hasClass("swipeout-overswipe") && direction === "to-right") {
            $buttonEl.css({
              left: (overswipeLeft ? buttonOffset : 0) + "px"
            });
            if (overswipeLeft) {
              if (!$buttonEl.hasClass("swipeout-overswipe-active")) {
                $swipeoutEl.trigger("swipeout:overswipeenter");
                app.emit("swipeoutOverswipeEnter", $swipeoutEl[0]);
              }
              $buttonEl.addClass("swipeout-overswipe-active");
            } else {
              if ($buttonEl.hasClass("swipeout-overswipe-active")) {
                $swipeoutEl.trigger("swipeout:overswipeexit");
                app.emit("swipeoutOverswipeExit", $swipeoutEl[0]);
              }
              $buttonEl.removeClass("swipeout-overswipe-active");
            }
          }
          if ($leftButtons.length > 1) {
            $buttonEl.css("z-index", $leftButtons.length - index2);
          }
          $buttonEl.transform("translate3d(" + (_buttonTranslate + buttonOffset * (1 - Math.min(progress, 1))) + "px,0,0)");
        });
      }
      $swipeoutEl.trigger("swipeout", progress);
      app.emit("swipeout", $swipeoutEl[0], progress);
      $swipeoutContent.transform("translate3d(" + translate + "px,0,0)");
    }
    function handleTouchEnd() {
      if (!isTouched || !isMoved) {
        isTouched = false;
        isMoved = false;
        return;
      }
      isTouched = false;
      isMoved = false;
      var timeDiff = new Date().getTime() - touchStartTime;
      var $actions = direction === "to-left" ? $actionsRight : $actionsLeft;
      var actionsWidth = direction === "to-left" ? actionsRightWidth : actionsLeftWidth;
      var action;
      var $buttons;
      var i;
      if (timeDiff < 300 && (touchesDiff < -10 && direction === "to-left" || touchesDiff > 10 && direction === "to-right") || timeDiff >= 300 && Math.abs(translate) > actionsWidth / 2) {
        action = "open";
      } else {
        action = "close";
      }
      if (timeDiff < 300) {
        if (Math.abs(translate) === 0)
          action = "close";
        if (Math.abs(translate) === actionsWidth)
          action = "open";
      }
      if (action === "open") {
        Swipeout.el = $swipeoutEl[0];
        $swipeoutEl.trigger("swipeout:open");
        app.emit("swipeoutOpen", $swipeoutEl[0]);
        $swipeoutEl.addClass("swipeout-opened swipeout-transitioning");
        var newTranslate = direction === "to-left" ? -actionsWidth : actionsWidth;
        $swipeoutContent.transform("translate3d(" + newTranslate + "px,0,0)");
        $actions.addClass("swipeout-actions-opened");
        $buttons = direction === "to-left" ? $rightButtons : $leftButtons;
        if ($buttons) {
          for (i = 0; i < $buttons.length; i += 1) {
            dom7_default($buttons[i]).transform("translate3d(" + newTranslate + "px,0,0)");
          }
        }
        if (overswipeRight) {
          $actionsRight.find(".swipeout-overswipe").trigger("click", "f7Overswipe");
        }
        if (overswipeLeft) {
          $actionsLeft.find(".swipeout-overswipe").trigger("click", "f7Overswipe");
        }
      } else {
        $swipeoutEl.trigger("swipeout:close");
        app.emit("swipeoutClose", $swipeoutEl[0]);
        Swipeout.el = void 0;
        $swipeoutEl.addClass("swipeout-transitioning").removeClass("swipeout-opened");
        $swipeoutContent.transform("");
        $actions.removeClass("swipeout-actions-opened");
      }
      var buttonOffset;
      if ($leftButtons && $leftButtons.length > 0 && $leftButtons !== $buttons) {
        $leftButtons.each(function(buttonEl) {
          var $buttonEl = dom7_default(buttonEl);
          buttonOffset = buttonEl.f7SwipeoutButtonOffset;
          if (typeof buttonOffset === "undefined") {
            $buttonEl[0].f7SwipeoutButtonOffset = actionsLeftWidth - buttonEl.offsetLeft - buttonEl.offsetWidth;
          }
          $buttonEl.transform("translate3d(" + buttonOffset + "px,0,0)");
        });
      }
      if ($rightButtons && $rightButtons.length > 0 && $rightButtons !== $buttons) {
        $rightButtons.each(function(buttonEl) {
          var $buttonEl = dom7_default(buttonEl);
          buttonOffset = buttonEl.f7SwipeoutButtonOffset;
          if (typeof buttonOffset === "undefined") {
            $buttonEl[0].f7SwipeoutButtonOffset = buttonEl.offsetLeft;
          }
          $buttonEl.transform("translate3d(" + -buttonOffset + "px,0,0)");
        });
      }
      $swipeoutContent.transitionEnd(function() {
        if (opened && action === "open" || !opened && action === "close")
          return;
        $swipeoutEl.trigger(action === "open" ? "swipeout:opened" : "swipeout:closed");
        app.emit(action === "open" ? "swipeoutOpened" : "swipeoutClosed", $swipeoutEl[0]);
        $swipeoutEl.removeClass("swipeout-transitioning");
        if (opened && action === "close") {
          if ($actionsRight.length > 0) {
            $rightButtons.transform("");
          }
          if ($actionsLeft.length > 0) {
            $leftButtons.transform("");
          }
        }
      });
    }
    var passiveListener = getSupport().passiveListener ? {
      passive: true
    } : false;
    app.on("touchstart", function(e) {
      if (Swipeout.el) {
        var $targetEl = dom7_default(e.target);
        if (!(dom7_default(Swipeout.el).is($targetEl[0]) || $targetEl.parents(".swipeout").is(Swipeout.el) || $targetEl.hasClass("modal-in") || ($targetEl.attr("class") || "").indexOf("-backdrop") > 0 || $targetEl.hasClass("actions-modal") || $targetEl.parents(".actions-modal.modal-in, .dialog.modal-in").length > 0)) {
          app.swipeout.close(Swipeout.el);
        }
      }
    });
    dom7_default(document).on(app.touchEvents.start, "li.swipeout", handleTouchStart, passiveListener);
    app.on("touchmove:active", handleTouchMove);
    app.on("touchend:passive", handleTouchEnd);
  },
  allow: true,
  el: void 0,
  open: function open() {
    var app = this;
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    var el = args[0], side = args[1], callback = args[2];
    if (typeof args[1] === "function") {
      el = args[0];
      callback = args[1];
      side = args[2];
    }
    var $el = dom7_default(el).eq(0);
    if ($el.length === 0)
      return;
    if (!$el.hasClass("swipeout") || $el.hasClass("swipeout-opened"))
      return;
    if (!side) {
      if ($el.find(".swipeout-actions-right").length > 0)
        side = "right";
      else
        side = "left";
    }
    var $swipeoutActions = $el.find(".swipeout-actions-" + side);
    var $swipeoutContent = $el.find(".swipeout-content");
    if ($swipeoutActions.length === 0)
      return;
    $el.trigger("swipeout:open").addClass("swipeout-opened").removeClass("swipeout-transitioning");
    app.emit("swipeoutOpen", $el[0]);
    $swipeoutActions.addClass("swipeout-actions-opened");
    var $buttons = $swipeoutActions.children("a");
    var swipeoutActionsWidth = $swipeoutActions.outerWidth();
    var translate = side === "right" ? -swipeoutActionsWidth : swipeoutActionsWidth;
    if ($buttons.length > 1) {
      $buttons.each(function(buttonEl, buttonIndex) {
        var $buttonEl = dom7_default(buttonEl);
        if (side === "right") {
          $buttonEl.transform("translate3d(" + -buttonEl.offsetLeft + "px,0,0)");
        } else {
          $buttonEl.css("z-index", $buttons.length - buttonIndex).transform("translate3d(" + (swipeoutActionsWidth - buttonEl.offsetWidth - buttonEl.offsetLeft) + "px,0,0)");
        }
      });
    }
    $el.addClass("swipeout-transitioning");
    $swipeoutContent.transitionEnd(function() {
      $el.trigger("swipeout:opened");
      app.emit("swipeoutOpened", $el[0]);
      if (callback)
        callback.call($el[0]);
    });
    nextFrame(function() {
      $buttons.transform("translate3d(" + translate + "px,0,0)");
      $swipeoutContent.transform("translate3d(" + translate + "px,0,0)");
    });
    Swipeout.el = $el[0];
  },
  close: function close(el, callback) {
    var app = this;
    var $el = dom7_default(el).eq(0);
    if ($el.length === 0)
      return;
    if (!$el.hasClass("swipeout-opened"))
      return;
    var side = $el.find(".swipeout-actions-opened").hasClass("swipeout-actions-right") ? "right" : "left";
    var $swipeoutActions = $el.find(".swipeout-actions-opened").removeClass("swipeout-actions-opened");
    var $buttons = $swipeoutActions.children("a");
    var swipeoutActionsWidth = $swipeoutActions.outerWidth();
    app.swipeout.allow = false;
    $el.trigger("swipeout:close");
    app.emit("swipeoutClose", $el[0]);
    $el.removeClass("swipeout-opened").addClass("swipeout-transitioning");
    var closeTimeout;
    function onSwipeoutClose() {
      app.swipeout.allow = true;
      if ($el.hasClass("swipeout-opened"))
        return;
      $el.removeClass("swipeout-transitioning");
      $buttons.transform("");
      $el.trigger("swipeout:closed");
      app.emit("swipeoutClosed", $el[0]);
      if (callback)
        callback.call($el[0]);
      if (closeTimeout)
        clearTimeout(closeTimeout);
    }
    $el.find(".swipeout-content").transform("").transitionEnd(onSwipeoutClose);
    closeTimeout = setTimeout(onSwipeoutClose, 500);
    $buttons.each(function(buttonEl) {
      var $buttonEl = dom7_default(buttonEl);
      if (side === "right") {
        $buttonEl.transform("translate3d(" + -buttonEl.offsetLeft + "px,0,0)");
      } else {
        $buttonEl.transform("translate3d(" + (swipeoutActionsWidth - buttonEl.offsetWidth - buttonEl.offsetLeft) + "px,0,0)");
      }
      $buttonEl.css({
        left: "0px"
      }).removeClass("swipeout-overswipe-active");
    });
    if (Swipeout.el && Swipeout.el === $el[0])
      Swipeout.el = void 0;
  },
  delete: function _delete(el, callback) {
    var app = this;
    var $el = dom7_default(el).eq(0);
    if ($el.length === 0)
      return;
    Swipeout.el = void 0;
    $el.trigger("swipeout:delete");
    app.emit("swipeoutDelete", $el[0]);
    $el.css({
      height: $el.outerHeight() + "px"
    });
    $el.transitionEnd(function() {
      $el.trigger("swipeout:deleted");
      app.emit("swipeoutDeleted", $el[0]);
      if (callback)
        callback.call($el[0]);
      if ($el.parents(".virtual-list").length > 0) {
        var virtualList = $el.parents(".virtual-list")[0].f7VirtualList;
        var virtualIndex = $el[0].f7VirtualListIndex;
        if (virtualList && typeof virtualIndex !== "undefined")
          virtualList.deleteItem(virtualIndex);
      } else if (app.params.swipeout.removeElements) {
        if (app.params.swipeout.removeElementsWithTimeout) {
          setTimeout(function() {
            $el.remove();
          }, app.params.swipeout.removeElementsTimeout);
        } else {
          $el.remove();
        }
      } else {
        $el.removeClass("swipeout-deleting swipeout-transitioning");
      }
    });
    nextFrame(function() {
      $el.addClass("swipeout-deleting swipeout-transitioning").css({
        height: "0px"
      }).find(".swipeout-content").transform("translate3d(-100%,0,0)");
    });
  }
};
var swipeout_default = {
  name: "swipeout",
  params: {
    swipeout: {
      actionsNoFold: false,
      noFollow: false,
      removeElements: true,
      removeElementsWithTimeout: false,
      removeElementsTimeout: 0,
      overswipeRatio: 1.2
    }
  },
  create: function create11() {
    var app = this;
    bindMethods(app, {
      swipeout: Swipeout
    });
  },
  clicks: {
    ".swipeout-open": function openSwipeout($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.swipeout.open(data.swipeout, data.side);
    },
    ".swipeout-close": function closeSwipeout($clickedEl) {
      var app = this;
      var $swipeoutEl = $clickedEl.closest(".swipeout");
      if ($swipeoutEl.length === 0)
        return;
      app.swipeout.close($swipeoutEl);
    },
    ".swipeout-delete": function deleteSwipeout($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      var $swipeoutEl = $clickedEl.closest(".swipeout");
      if ($swipeoutEl.length === 0)
        return;
      var _data = data, confirm = _data.confirm, confirmTitle = _data.confirmTitle;
      if (data.confirm) {
        app.dialog.confirm(confirm, confirmTitle, function() {
          app.swipeout.delete($swipeoutEl);
        });
      } else {
        app.swipeout.delete($swipeoutEl);
      }
    }
  },
  on: {
    init: function init5() {
      var app = this;
      if (!app.params.swipeout)
        return;
      app.swipeout.init();
    }
  }
};

// node_modules/framework7/esm/components/accordion/accordion.js
var Accordion = {
  toggleClicked: function toggleClicked($clickedEl) {
    var app = this;
    var $accordionItemEl = $clickedEl.closest(".accordion-item").eq(0);
    if (!$accordionItemEl.length)
      $accordionItemEl = $clickedEl.parents("li").eq(0);
    var $accordionContent = $clickedEl.parents(".accordion-item-content").eq(0);
    if ($accordionContent.length) {
      if ($accordionContent.parents($accordionItemEl).length)
        return;
    }
    if ($clickedEl.parents("li").length > 1 && $clickedEl.parents("li")[0] !== $accordionItemEl[0])
      return;
    app.accordion.toggle($accordionItemEl);
  },
  open: function open2(el) {
    var app = this;
    var $el = dom7_default(el);
    var prevented = false;
    function prevent() {
      prevented = true;
    }
    $el.trigger("accordion:beforeopen", {
      prevent
    }, prevent);
    app.emit("accordionBeforeOpen", $el[0], prevent);
    if (prevented)
      return;
    var $list = $el.parents(".accordion-list").eq(0);
    var $contentEl = $el.children(".accordion-item-content");
    $contentEl.removeAttr("aria-hidden");
    if ($contentEl.length === 0)
      $contentEl = $el.find(".accordion-item-content");
    if ($contentEl.length === 0)
      return;
    var $openedItem = $list.length > 0 && $el.parent().children(".accordion-item-opened");
    if ($openedItem.length > 0) {
      app.accordion.close($openedItem);
    }
    $contentEl.transitionEnd(function() {
      if ($el.hasClass("accordion-item-opened")) {
        $contentEl.transition(0);
        $contentEl.css("height", "auto");
        nextFrame(function() {
          $contentEl.transition("");
          $el.trigger("accordion:opened");
          app.emit("accordionOpened", $el[0]);
        });
      } else {
        $contentEl.css("height", "");
        $el.trigger("accordion:closed");
        app.emit("accordionClosed", $el[0]);
      }
    });
    $contentEl.css("height", $contentEl[0].scrollHeight + "px");
    $el.trigger("accordion:open");
    $el.addClass("accordion-item-opened");
    app.emit("accordionOpen", $el[0]);
  },
  close: function close2(el) {
    var app = this;
    var $el = dom7_default(el);
    var prevented = false;
    function prevent() {
      prevented = true;
    }
    $el.trigger("accordion:beforeclose", {
      prevent
    }, prevent);
    app.emit("accordionBeforeClose", $el[0], prevent);
    if (prevented)
      return;
    var $contentEl = $el.children(".accordion-item-content");
    if ($contentEl.length === 0)
      $contentEl = $el.find(".accordion-item-content");
    $el.removeClass("accordion-item-opened");
    $contentEl.attr("aria-hidden", true);
    $contentEl.transition(0);
    $contentEl.css("height", $contentEl[0].scrollHeight + "px");
    $contentEl.transitionEnd(function() {
      if ($el.hasClass("accordion-item-opened")) {
        $contentEl.transition(0);
        $contentEl.css("height", "auto");
        nextFrame(function() {
          $contentEl.transition("");
          $el.trigger("accordion:opened");
          app.emit("accordionOpened", $el[0]);
        });
      } else {
        $contentEl.css("height", "");
        $el.trigger("accordion:closed");
        app.emit("accordionClosed", $el[0]);
      }
    });
    nextFrame(function() {
      $contentEl.transition("");
      $contentEl.css("height", "");
      $el.trigger("accordion:close");
      app.emit("accordionClose", $el[0]);
    });
  },
  toggle: function toggle3(el) {
    var app = this;
    var $el = dom7_default(el);
    if ($el.length === 0)
      return;
    if ($el.hasClass("accordion-item-opened"))
      app.accordion.close(el);
    else
      app.accordion.open(el);
  }
};
var accordion_default = {
  name: "accordion",
  create: function create12() {
    var app = this;
    bindMethods(app, {
      accordion: Accordion
    });
  },
  clicks: {
    ".accordion-item .item-link, .accordion-item-toggle, .links-list.accordion-list > ul > li > a": function open3($clickedEl) {
      var app = this;
      Accordion.toggleClicked.call(app, $clickedEl);
    }
  }
};

// node_modules/framework7/esm/components/contacts-list/contacts-list.js
var contacts_list_default = {
  name: "contactsList"
};

// node_modules/framework7/esm/components/virtual-list/virtual-list-class.js
function _assertThisInitialized8(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose8(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf8(subClass, superClass);
}
function _setPrototypeOf8(o, p) {
  _setPrototypeOf8 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf8(o, p);
}
var VirtualList = function(_Framework7Class) {
  _inheritsLoose8(VirtualList2, _Framework7Class);
  function VirtualList2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var vl = _assertThisInitialized8(_this);
    var device2 = getDevice();
    var document = getDocument();
    var defaultHeight;
    if (app.theme === "md") {
      defaultHeight = 48;
    } else if (app.theme === "ios") {
      defaultHeight = 44;
    } else if (app.theme === "aurora") {
      defaultHeight = 38;
    }
    var defaults = {
      cols: 1,
      height: defaultHeight,
      cache: true,
      dynamicHeightBufferSize: 1,
      showFilteredItemsOnly: false,
      renderExternal: void 0,
      setListHeight: true,
      searchByItem: void 0,
      searchAll: void 0,
      ul: null,
      createUl: true,
      scrollableParentEl: void 0,
      renderItem: function renderItem(item) {
        return ('\n          <li>\n            <div class="item-content">\n              <div class="item-inner">\n                <div class="item-title">' + item + "</div>\n              </div>\n            </div>\n          </li>\n        ").trim();
      },
      on: {}
    };
    vl.useModulesParams(defaults);
    vl.params = extend(defaults, params);
    if (vl.params.height === void 0 || !vl.params.height) {
      vl.params.height = defaultHeight;
    }
    vl.$el = dom7_default(params.el);
    vl.el = vl.$el[0];
    if (vl.$el.length === 0)
      return _assertThisInitialized8(_this);
    vl.$el[0].f7VirtualList = vl;
    vl.items = vl.params.items;
    if (vl.params.showFilteredItemsOnly) {
      vl.filteredItems = [];
    }
    if (vl.params.renderItem) {
      vl.renderItem = vl.params.renderItem;
    }
    vl.$pageContentEl = vl.$el.parents(".page-content");
    vl.pageContentEl = vl.$pageContentEl[0];
    vl.$scrollableParentEl = vl.params.scrollableParentEl ? dom7_default(vl.params.scrollableParentEl).eq(0) : vl.$pageContentEl;
    if (!vl.$scrollableParentEl.length && vl.$pageContentEl.length) {
      vl.$scrollableParentEl = vl.$pageContentEl;
    }
    vl.scrollableParentEl = vl.$scrollableParentEl[0];
    if (typeof vl.params.updatableScroll !== "undefined") {
      vl.updatableScroll = vl.params.updatableScroll;
    } else {
      vl.updatableScroll = true;
      if (device2.ios && device2.osVersion.split(".")[0] < 8) {
        vl.updatableScroll = false;
      }
    }
    var ul = vl.params.ul;
    vl.$ul = ul ? dom7_default(vl.params.ul) : vl.$el.children("ul");
    if (vl.$ul.length === 0 && vl.params.createUl) {
      vl.$el.append("<ul></ul>");
      vl.$ul = vl.$el.children("ul");
    }
    vl.ul = vl.$ul[0];
    var $itemsWrapEl;
    if (!vl.ul && !vl.params.createUl)
      $itemsWrapEl = vl.$el;
    else
      $itemsWrapEl = vl.$ul;
    extend(vl, {
      $itemsWrapEl,
      itemsWrapEl: $itemsWrapEl[0],
      domCache: {},
      displayDomCache: {},
      tempDomElement: document.createElement("ul"),
      lastRepaintY: null,
      fragment: document.createDocumentFragment(),
      pageHeight: void 0,
      rowsPerScreen: void 0,
      rowsBefore: void 0,
      rowsAfter: void 0,
      rowsToRender: void 0,
      maxBufferHeight: 0,
      listHeight: void 0,
      dynamicHeight: typeof vl.params.height === "function"
    });
    vl.useModules();
    var handleScrollBound = vl.handleScroll.bind(vl);
    var handleResizeBound = vl.handleResize.bind(vl);
    var $pageEl;
    var $tabEl;
    var $panelEl;
    var $popupEl;
    vl.attachEvents = function attachEvents2() {
      $pageEl = vl.$el.parents(".page").eq(0);
      $tabEl = vl.$el.parents(".tab").filter(function(tabEl) {
        return dom7_default(tabEl).parent(".tabs").parent(".tabs-animated-wrap, .tabs-swipeable-wrap").length === 0;
      }).eq(0);
      $panelEl = vl.$el.parents(".panel").eq(0);
      $popupEl = vl.$el.parents(".popup").eq(0);
      vl.$scrollableParentEl.on("scroll", handleScrollBound);
      if ($pageEl.length)
        $pageEl.on("page:reinit", handleResizeBound);
      if ($tabEl.length)
        $tabEl.on("tab:show", handleResizeBound);
      if ($panelEl.length)
        $panelEl.on("panel:open", handleResizeBound);
      if ($popupEl.length)
        $popupEl.on("popup:open", handleResizeBound);
      app.on("resize", handleResizeBound);
    };
    vl.detachEvents = function attachEvents2() {
      vl.$scrollableParentEl.off("scroll", handleScrollBound);
      if ($pageEl.length)
        $pageEl.off("page:reinit", handleResizeBound);
      if ($tabEl.length)
        $tabEl.off("tab:show", handleResizeBound);
      if ($panelEl.length)
        $panelEl.off("panel:open", handleResizeBound);
      if ($popupEl.length)
        $popupEl.off("popup:open", handleResizeBound);
      app.off("resize", handleResizeBound);
    };
    vl.init();
    return vl || _assertThisInitialized8(_this);
  }
  var _proto = VirtualList2.prototype;
  _proto.setListSize = function setListSize() {
    var vl = this;
    var items = vl.filteredItems || vl.items;
    vl.pageHeight = vl.$scrollableParentEl[0].offsetHeight;
    if (vl.dynamicHeight) {
      vl.listHeight = 0;
      vl.heights = [];
      for (var i = 0; i < items.length; i += 1) {
        var itemHeight = vl.params.height(items[i]);
        vl.listHeight += itemHeight;
        vl.heights.push(itemHeight);
      }
    } else {
      vl.listHeight = Math.ceil(items.length / vl.params.cols) * vl.params.height;
      vl.rowsPerScreen = Math.ceil(vl.pageHeight / vl.params.height);
      vl.rowsBefore = vl.params.rowsBefore || vl.rowsPerScreen * 2;
      vl.rowsAfter = vl.params.rowsAfter || vl.rowsPerScreen;
      vl.rowsToRender = vl.rowsPerScreen + vl.rowsBefore + vl.rowsAfter;
      vl.maxBufferHeight = vl.rowsBefore / 2 * vl.params.height;
    }
    if (vl.updatableScroll || vl.params.setListHeight) {
      vl.$itemsWrapEl.css({
        height: vl.listHeight + "px"
      });
    }
  };
  _proto.render = function render15(force, forceScrollTop) {
    var vl = this;
    if (force)
      vl.lastRepaintY = null;
    var scrollTop = -(vl.$el[0].getBoundingClientRect().top - vl.$scrollableParentEl[0].getBoundingClientRect().top);
    if (typeof forceScrollTop !== "undefined")
      scrollTop = forceScrollTop;
    if (vl.lastRepaintY === null || Math.abs(scrollTop - vl.lastRepaintY) > vl.maxBufferHeight || !vl.updatableScroll && vl.$scrollableParentEl[0].scrollTop + vl.pageHeight >= vl.$scrollableParentEl[0].scrollHeight) {
      vl.lastRepaintY = scrollTop;
    } else {
      return;
    }
    var items = vl.filteredItems || vl.items;
    var fromIndex;
    var toIndex;
    var heightBeforeFirstItem = 0;
    var heightBeforeLastItem = 0;
    if (vl.dynamicHeight) {
      var itemTop = 0;
      var itemHeight;
      vl.maxBufferHeight = vl.pageHeight;
      for (var j = 0; j < vl.heights.length; j += 1) {
        itemHeight = vl.heights[j];
        if (typeof fromIndex === "undefined") {
          if (itemTop + itemHeight >= scrollTop - vl.pageHeight * 2 * vl.params.dynamicHeightBufferSize)
            fromIndex = j;
          else
            heightBeforeFirstItem += itemHeight;
        }
        if (typeof toIndex === "undefined") {
          if (itemTop + itemHeight >= scrollTop + vl.pageHeight * 2 * vl.params.dynamicHeightBufferSize || j === vl.heights.length - 1)
            toIndex = j + 1;
          heightBeforeLastItem += itemHeight;
        }
        itemTop += itemHeight;
      }
      toIndex = Math.min(toIndex, items.length);
    } else {
      fromIndex = (parseInt(scrollTop / vl.params.height, 10) - vl.rowsBefore) * vl.params.cols;
      if (fromIndex < 0) {
        fromIndex = 0;
      }
      toIndex = Math.min(fromIndex + vl.rowsToRender * vl.params.cols, items.length);
    }
    var topPosition;
    var renderExternalItems = [];
    vl.reachEnd = false;
    var i;
    for (i = fromIndex; i < toIndex; i += 1) {
      var itemEl = void 0;
      var index2 = vl.items.indexOf(items[i]);
      if (i === fromIndex)
        vl.currentFromIndex = index2;
      if (i === toIndex - 1)
        vl.currentToIndex = index2;
      if (vl.filteredItems) {
        if (vl.items[index2] === vl.filteredItems[vl.filteredItems.length - 1])
          vl.reachEnd = true;
      } else if (index2 === vl.items.length - 1)
        vl.reachEnd = true;
      if (vl.params.renderExternal) {
        renderExternalItems.push(items[i]);
      } else if (vl.domCache[index2]) {
        itemEl = vl.domCache[index2];
        itemEl.f7VirtualListIndex = index2;
      } else {
        if (vl.renderItem) {
          vl.tempDomElement.innerHTML = vl.renderItem(items[i], index2).trim();
        } else {
          vl.tempDomElement.innerHTML = items[i].toString().trim();
        }
        itemEl = vl.tempDomElement.childNodes[0];
        if (vl.params.cache)
          vl.domCache[index2] = itemEl;
        itemEl.f7VirtualListIndex = index2;
      }
      if (i === fromIndex) {
        if (vl.dynamicHeight) {
          topPosition = heightBeforeFirstItem;
        } else {
          topPosition = i * vl.params.height / vl.params.cols;
        }
      }
      if (!vl.params.renderExternal) {
        itemEl.style.top = topPosition + "px";
        vl.emit("local::itemBeforeInsert vlItemBeforeInsert", vl, itemEl, items[i]);
        vl.fragment.appendChild(itemEl);
      }
    }
    if (!vl.updatableScroll) {
      if (vl.dynamicHeight) {
        vl.itemsWrapEl.style.height = heightBeforeLastItem + "px";
      } else {
        vl.itemsWrapEl.style.height = i * vl.params.height / vl.params.cols + "px";
      }
    }
    if (vl.params.renderExternal) {
      if (items && items.length === 0) {
        vl.reachEnd = true;
      }
    } else {
      vl.emit("local::beforeClear vlBeforeClear", vl, vl.fragment);
      vl.itemsWrapEl.innerHTML = "";
      vl.emit("local::itemsBeforeInsert vlItemsBeforeInsert", vl, vl.fragment);
      if (items && items.length === 0) {
        vl.reachEnd = true;
        if (vl.params.emptyTemplate)
          vl.itemsWrapEl.innerHTML = vl.params.emptyTemplate;
      } else {
        vl.itemsWrapEl.appendChild(vl.fragment);
      }
      vl.emit("local::itemsAfterInsert vlItemsAfterInsert", vl, vl.fragment);
    }
    if (typeof forceScrollTop !== "undefined" && force) {
      vl.$scrollableParentEl.scrollTop(forceScrollTop, 0);
    }
    if (vl.params.renderExternal) {
      vl.params.renderExternal(vl, {
        fromIndex,
        toIndex,
        listHeight: vl.listHeight,
        topPosition,
        items: renderExternalItems
      });
    }
  };
  _proto.filterItems = function filterItems(indexes, resetScrollTop) {
    if (resetScrollTop === void 0) {
      resetScrollTop = true;
    }
    var vl = this;
    vl.filteredItems = [];
    for (var i = 0; i < indexes.length; i += 1) {
      vl.filteredItems.push(vl.items[indexes[i]]);
    }
    if (resetScrollTop) {
      vl.$scrollableParentEl[0].scrollTop = 0;
    }
    vl.update();
  };
  _proto.resetFilter = function resetFilter() {
    var vl = this;
    if (vl.params.showFilteredItemsOnly) {
      vl.filteredItems = [];
    } else {
      vl.filteredItems = null;
      delete vl.filteredItems;
    }
    vl.update();
  };
  _proto.scrollToItem = function scrollToItem(index2) {
    var vl = this;
    if (index2 > vl.items.length)
      return false;
    var itemTop = 0;
    if (vl.dynamicHeight) {
      for (var i = 0; i < index2; i += 1) {
        itemTop += vl.heights[i];
      }
    } else {
      itemTop = index2 * vl.params.height;
    }
    var listTop = vl.$el[0].offsetTop;
    vl.render(true, listTop + itemTop - parseInt(vl.$scrollableParentEl.css("padding-top"), 10));
    return true;
  };
  _proto.handleScroll = function handleScroll() {
    var vl = this;
    vl.render();
  };
  _proto.isVisible = function isVisible() {
    var vl = this;
    return !!(vl.el.offsetWidth || vl.el.offsetHeight || vl.el.getClientRects().length);
  };
  _proto.handleResize = function handleResize() {
    var vl = this;
    if (vl.isVisible()) {
      vl.setListSize();
      vl.render(true);
    }
  };
  _proto.appendItems = function appendItems(items) {
    var vl = this;
    for (var i = 0; i < items.length; i += 1) {
      vl.items.push(items[i]);
    }
    vl.update();
  };
  _proto.appendItem = function appendItem(item) {
    var vl = this;
    vl.appendItems([item]);
  };
  _proto.replaceAllItems = function replaceAllItems(items) {
    var vl = this;
    vl.items = items;
    delete vl.filteredItems;
    vl.domCache = {};
    vl.update();
  };
  _proto.replaceItem = function replaceItem(index2, item) {
    var vl = this;
    vl.items[index2] = item;
    if (vl.params.cache)
      delete vl.domCache[index2];
    vl.update();
  };
  _proto.prependItems = function prependItems(items) {
    var vl = this;
    for (var i = items.length - 1; i >= 0; i -= 1) {
      vl.items.unshift(items[i]);
    }
    if (vl.params.cache) {
      var newCache = {};
      Object.keys(vl.domCache).forEach(function(cached) {
        newCache[parseInt(cached, 10) + items.length] = vl.domCache[cached];
      });
      vl.domCache = newCache;
    }
    vl.update();
  };
  _proto.prependItem = function prependItem(item) {
    var vl = this;
    vl.prependItems([item]);
  };
  _proto.moveItem = function moveItem(from, to) {
    var vl = this;
    var fromIndex = from;
    var toIndex = to;
    if (fromIndex === toIndex)
      return;
    var item = vl.items.splice(fromIndex, 1)[0];
    if (toIndex >= vl.items.length) {
      vl.items.push(item);
      toIndex = vl.items.length - 1;
    } else {
      vl.items.splice(toIndex, 0, item);
    }
    if (vl.params.cache) {
      var newCache = {};
      Object.keys(vl.domCache).forEach(function(cached) {
        var cachedIndex = parseInt(cached, 10);
        var leftIndex = fromIndex < toIndex ? fromIndex : toIndex;
        var rightIndex = fromIndex < toIndex ? toIndex : fromIndex;
        var indexShift = fromIndex < toIndex ? -1 : 1;
        if (cachedIndex < leftIndex || cachedIndex > rightIndex)
          newCache[cachedIndex] = vl.domCache[cachedIndex];
        if (cachedIndex === leftIndex)
          newCache[rightIndex] = vl.domCache[cachedIndex];
        if (cachedIndex > leftIndex && cachedIndex <= rightIndex)
          newCache[cachedIndex + indexShift] = vl.domCache[cachedIndex];
      });
      vl.domCache = newCache;
    }
    vl.update();
  };
  _proto.insertItemBefore = function insertItemBefore(index2, item) {
    var vl = this;
    if (index2 === 0) {
      vl.prependItem(item);
      return;
    }
    if (index2 >= vl.items.length) {
      vl.appendItem(item);
      return;
    }
    vl.items.splice(index2, 0, item);
    if (vl.params.cache) {
      var newCache = {};
      Object.keys(vl.domCache).forEach(function(cached) {
        var cachedIndex = parseInt(cached, 10);
        if (cachedIndex >= index2) {
          newCache[cachedIndex + 1] = vl.domCache[cachedIndex];
        }
      });
      vl.domCache = newCache;
    }
    vl.update();
  };
  _proto.deleteItems = function deleteItems(indexes) {
    var vl = this;
    var prevIndex;
    var indexShift = 0;
    var _loop = function _loop2(i2) {
      var index2 = indexes[i2];
      if (typeof prevIndex !== "undefined") {
        if (index2 > prevIndex) {
          indexShift = -i2;
        }
      }
      index2 += indexShift;
      prevIndex = indexes[i2];
      var deletedItem = vl.items.splice(index2, 1)[0];
      if (vl.filteredItems && vl.filteredItems.indexOf(deletedItem) >= 0) {
        vl.filteredItems.splice(vl.filteredItems.indexOf(deletedItem), 1);
      }
      if (vl.params.cache) {
        var newCache = {};
        Object.keys(vl.domCache).forEach(function(cached) {
          var cachedIndex = parseInt(cached, 10);
          if (cachedIndex === index2) {
            delete vl.domCache[index2];
          } else if (parseInt(cached, 10) > index2) {
            newCache[cachedIndex - 1] = vl.domCache[cached];
          } else {
            newCache[cachedIndex] = vl.domCache[cached];
          }
        });
        vl.domCache = newCache;
      }
    };
    for (var i = 0; i < indexes.length; i += 1) {
      _loop(i);
    }
    vl.update();
  };
  _proto.deleteAllItems = function deleteAllItems() {
    var vl = this;
    vl.items = [];
    delete vl.filteredItems;
    if (vl.params.cache)
      vl.domCache = {};
    vl.update();
  };
  _proto.deleteItem = function deleteItem(index2) {
    var vl = this;
    vl.deleteItems([index2]);
  };
  _proto.clearCache = function clearCache() {
    var vl = this;
    vl.domCache = {};
  };
  _proto.update = function update22(deleteCache) {
    var vl = this;
    if (deleteCache && vl.params.cache) {
      vl.domCache = {};
    }
    vl.setListSize();
    vl.render(true);
  };
  _proto.init = function init46() {
    var vl = this;
    vl.attachEvents();
    vl.setListSize();
    vl.render();
  };
  _proto.destroy = function destroy50() {
    var vl = this;
    vl.detachEvents();
    vl.$el[0].f7VirtualList = null;
    delete vl.$el[0].f7VirtualList;
    deleteProps(vl);
    vl = null;
  };
  return VirtualList2;
}(class_default);
var virtual_list_class_default = VirtualList;

// node_modules/framework7/esm/components/virtual-list/virtual-list.js
var virtual_list_default = {
  name: "virtualList",
  static: {
    VirtualList: virtual_list_class_default
  },
  create: function create13() {
    var app = this;
    app.virtualList = ConstructorMethods({
      defaultSelector: ".virtual-list",
      constructor: virtual_list_class_default,
      app,
      domProp: "f7VirtualList"
    });
  }
};

// node_modules/framework7/esm/components/list-index/list-index-class.js
function _assertThisInitialized9(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose9(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf9(subClass, superClass);
}
function _setPrototypeOf9(o, p) {
  _setPrototypeOf9 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf9(o, p);
}
var ListIndex = function(_Framework7Class) {
  _inheritsLoose9(ListIndex2, _Framework7Class);
  function ListIndex2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var index2 = _assertThisInitialized9(_this);
    var defaults = {
      el: null,
      listEl: null,
      indexes: "auto",
      iosItemHeight: 14,
      mdItemHeight: 14,
      auroraItemHeight: 14,
      scrollList: true,
      label: false,
      renderItem: function renderItem(itemContent, itemIndex) {
        return ("\n          <li>" + itemContent + "</li>\n        ").trim();
      },
      renderSkipPlaceholder: function renderSkipPlaceholder() {
        return '<li class="list-index-skip-placeholder"></li>';
      },
      on: {}
    };
    index2.useModulesParams(defaults);
    index2.params = extend(defaults, params);
    var $el;
    var $listEl;
    var $pageContentEl;
    var $ul;
    if (index2.params.el) {
      $el = dom7_default(index2.params.el);
    } else {
      return index2 || _assertThisInitialized9(_this);
    }
    if ($el[0].f7ListIndex) {
      return $el[0].f7ListIndex || _assertThisInitialized9(_this);
    }
    $ul = $el.find("ul");
    if ($ul.length === 0) {
      $ul = dom7_default("<ul></ul>");
      $el.append($ul);
    }
    if (index2.params.listEl) {
      $listEl = dom7_default(index2.params.listEl);
    }
    if (index2.params.indexes === "auto" && !$listEl) {
      return index2 || _assertThisInitialized9(_this);
    }
    if ($listEl) {
      $pageContentEl = $listEl.parents(".page-content").eq(0);
    } else {
      $pageContentEl = $el.siblings(".page-content").eq(0);
      if ($pageContentEl.length === 0) {
        $pageContentEl = $el.parents(".page").eq(0).find(".page-content").eq(0);
      }
    }
    $el[0].f7ListIndex = index2;
    extend(index2, {
      app,
      $el,
      el: $el && $el[0],
      $ul,
      ul: $ul && $ul[0],
      $listEl,
      listEl: $listEl && $listEl[0],
      $pageContentEl,
      pageContentEl: $pageContentEl && $pageContentEl[0],
      indexes: params.indexes,
      height: 0,
      skipRate: 0
    });
    index2.useModules();
    function handleResize() {
      var height = {
        index: index2
      };
      index2.calcSize();
      if (height !== index2.height) {
        index2.render();
      }
    }
    function handleClick(e) {
      var $clickedLi = dom7_default(e.target).closest("li");
      if (!$clickedLi.length)
        return;
      var itemIndex = $clickedLi.index();
      if (index2.skipRate > 0) {
        var percentage = itemIndex / ($clickedLi.siblings("li").length - 1);
        itemIndex = Math.round((index2.indexes.length - 1) * percentage);
      }
      var itemContent = index2.indexes[itemIndex];
      index2.$el.trigger("listindex:click", {
        content: itemContent,
        index: itemIndex
      });
      index2.emit("local::click listIndexClick", index2, itemContent, itemIndex);
      index2.$el.trigger("listindex:select", {
        content: itemContent,
        index: itemIndex
      });
      index2.emit("local::select listIndexSelect", index2, itemContent, itemIndex);
      if (index2.$listEl && index2.params.scrollList) {
        index2.scrollListToIndex(itemContent, itemIndex);
      }
    }
    var touchesStart = {};
    var isTouched;
    var isMoved;
    var topPoint;
    var bottomPoint;
    var $labelEl;
    var previousIndex = null;
    function handleTouchStart(e) {
      var $children = $ul.children();
      if (!$children.length)
        return;
      topPoint = $children[0].getBoundingClientRect().top;
      bottomPoint = $children[$children.length - 1].getBoundingClientRect().top + $children[0].offsetHeight;
      touchesStart.x = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
      touchesStart.y = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
      isTouched = true;
      isMoved = false;
      previousIndex = null;
    }
    function handleTouchMove(e) {
      if (!isTouched)
        return;
      if (!isMoved && index2.params.label) {
        $labelEl = dom7_default('<span class="list-index-label"></span>');
        $el.append($labelEl);
      }
      isMoved = true;
      var pageY = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
      e.preventDefault();
      var percentage = (pageY - topPoint) / (bottomPoint - topPoint);
      percentage = Math.min(Math.max(percentage, 0), 1);
      var itemIndex = Math.round((index2.indexes.length - 1) * percentage);
      var itemContent = index2.indexes[itemIndex];
      var ulHeight = bottomPoint - topPoint;
      var bubbleBottom = (index2.height - ulHeight) / 2 + (1 - percentage) * ulHeight;
      if (itemIndex !== previousIndex) {
        if (index2.params.label) {
          $labelEl.html(itemContent).transform("translateY(-" + bubbleBottom + "px)");
        }
        if (index2.$listEl && index2.params.scrollList) {
          index2.scrollListToIndex(itemContent, itemIndex);
        }
      }
      previousIndex = itemIndex;
      index2.$el.trigger("listindex:select");
      index2.emit("local::select listIndexSelect", index2, itemContent, itemIndex);
    }
    function handleTouchEnd() {
      if (!isTouched)
        return;
      isTouched = false;
      isMoved = false;
      if (index2.params.label) {
        if ($labelEl)
          $labelEl.remove();
        $labelEl = void 0;
      }
    }
    var passiveListener = getSupport().passiveListener ? {
      passive: true
    } : false;
    index2.attachEvents = function attachEvents2() {
      $el.parents(".tab").on("tab:show", handleResize);
      $el.parents(".page").on("page:reinit", handleResize);
      $el.parents(".panel").on("panel:open", handleResize);
      $el.parents(".sheet-modal, .actions-modal, .popup, .popover, .login-screen, .dialog, .toast").on("modal:open", handleResize);
      app.on("resize", handleResize);
      $el.on("click", handleClick);
      $el.on(app.touchEvents.start, handleTouchStart, passiveListener);
      app.on("touchmove:active", handleTouchMove);
      app.on("touchend:passive", handleTouchEnd);
    };
    index2.detachEvents = function attachEvents2() {
      $el.parents(".tab").off("tab:show", handleResize);
      $el.parents(".page").off("page:reinit", handleResize);
      $el.parents(".panel").off("panel:open", handleResize);
      $el.parents(".sheet-modal, .actions-modal, .popup, .popover, .login-screen, .dialog, .toast").off("modal:open", handleResize);
      app.off("resize", handleResize);
      $el.off("click", handleClick);
      $el.off(app.touchEvents.start, handleTouchStart, passiveListener);
      app.off("touchmove:active", handleTouchMove);
      app.off("touchend:passive", handleTouchEnd);
    };
    index2.init();
    return index2 || _assertThisInitialized9(_this);
  }
  var _proto = ListIndex2.prototype;
  _proto.scrollListToIndex = function scrollListToIndex(itemContent, itemIndex) {
    var index2 = this;
    var $listEl = index2.$listEl, $pageContentEl = index2.$pageContentEl, app = index2.app;
    if (!$listEl || !$pageContentEl || $pageContentEl.length === 0)
      return index2;
    var $scrollToEl;
    $listEl.find(".list-group-title, .item-divider").each(function(el) {
      if ($scrollToEl)
        return;
      var $el = dom7_default(el);
      if ($el.text() === itemContent) {
        $scrollToEl = $el;
      }
    });
    if (!$scrollToEl || $scrollToEl.length === 0)
      return index2;
    var parentTop = $scrollToEl.parent().offset().top;
    var paddingTop = parseInt($pageContentEl.css("padding-top"), 10);
    var scrollTop = $pageContentEl[0].scrollTop;
    var scrollToElTop = $scrollToEl.offset().top;
    if ($pageContentEl.parents(".page-with-navbar-large").length) {
      var navbarInnerEl = app.navbar.getElByPage($pageContentEl.parents(".page-with-navbar-large").eq(0));
      var $titleLargeEl = dom7_default(navbarInnerEl).find(".title-large");
      if ($titleLargeEl.length) {
        paddingTop -= $titleLargeEl[0].offsetHeight || 0;
      }
    }
    if (parentTop <= paddingTop) {
      $pageContentEl.scrollTop(parentTop + scrollTop - paddingTop);
    } else {
      $pageContentEl.scrollTop(scrollToElTop + scrollTop - paddingTop);
    }
    return index2;
  };
  _proto.renderSkipPlaceholder = function renderSkipPlaceholder() {
    var index2 = this;
    return index2.params.renderSkipPlaceholder.call(index2);
  };
  _proto.renderItem = function renderItem(itemContent, itemIndex) {
    var index2 = this;
    return index2.params.renderItem.call(index2, itemContent, itemIndex);
  };
  _proto.render = function render15() {
    var index2 = this;
    var $ul = index2.$ul, indexes = index2.indexes, skipRate = index2.skipRate;
    var wasSkipped;
    var html2 = indexes.map(function(itemContent, itemIndex) {
      if (itemIndex % skipRate !== 0 && skipRate > 0) {
        wasSkipped = true;
        return "";
      }
      var itemHtml = index2.renderItem(itemContent, itemIndex);
      if (wasSkipped) {
        itemHtml = index2.renderSkipPlaceholder() + itemHtml;
      }
      wasSkipped = false;
      return itemHtml;
    }).join("");
    $ul.html(html2);
    return index2;
  };
  _proto.calcSize = function calcSize() {
    var index2 = this;
    var app = index2.app, params = index2.params, el = index2.el, indexes = index2.indexes;
    var height = el.offsetHeight;
    var itemHeight = params[app.theme + "ItemHeight"];
    var maxItems = Math.floor(height / itemHeight);
    var items = indexes.length;
    var skipRate = 0;
    if (items > maxItems) {
      skipRate = Math.ceil((items * 2 - 1) / maxItems);
    }
    index2.height = height;
    index2.skipRate = skipRate;
    return index2;
  };
  _proto.calcIndexes = function calcIndexes() {
    var index2 = this;
    if (index2.params.indexes === "auto") {
      index2.indexes = [];
      index2.$listEl.find(".list-group-title, .item-divider").each(function(el) {
        var elContent = dom7_default(el).text();
        if (index2.indexes.indexOf(elContent) < 0) {
          index2.indexes.push(elContent);
        }
      });
    } else {
      index2.indexes = index2.params.indexes;
    }
    return index2;
  };
  _proto.update = function update22() {
    var index2 = this;
    index2.calcIndexes();
    index2.calcSize();
    index2.render();
    return index2;
  };
  _proto.init = function init46() {
    var index2 = this;
    index2.calcIndexes();
    index2.calcSize();
    index2.render();
    index2.attachEvents();
  };
  _proto.destroy = function destroy50() {
    var index2 = this;
    index2.$el.trigger("listindex:beforedestroy", index2);
    index2.emit("local::beforeDestroy listIndexBeforeDestroy");
    index2.detachEvents();
    if (index2.$el[0]) {
      index2.$el[0].f7ListIndex = null;
      delete index2.$el[0].f7ListIndex;
    }
    deleteProps(index2);
    index2 = null;
  };
  return ListIndex2;
}(class_default);
var list_index_class_default = ListIndex;

// node_modules/framework7/esm/components/list-index/list-index.js
var list_index_default = {
  name: "listIndex",
  static: {
    ListIndex: list_index_class_default
  },
  create: function create14() {
    var app = this;
    app.listIndex = ConstructorMethods({
      defaultSelector: ".list-index",
      constructor: list_index_class_default,
      app,
      domProp: "f7ListIndex"
    });
  },
  on: {
    tabMounted: function tabMounted3(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".list-index-init").each(function(listIndexEl) {
        var params = extend(dom7_default(listIndexEl).dataset(), {
          el: listIndexEl
        });
        app.listIndex.create(params);
      });
    },
    tabBeforeRemove: function tabBeforeRemove(tabEl) {
      dom7_default(tabEl).find(".list-index-init").each(function(listIndexEl) {
        if (listIndexEl.f7ListIndex)
          listIndexEl.f7ListIndex.destroy();
      });
    },
    pageInit: function pageInit3(page) {
      var app = this;
      page.$el.find(".list-index-init").each(function(listIndexEl) {
        var params = extend(dom7_default(listIndexEl).dataset(), {
          el: listIndexEl
        });
        app.listIndex.create(params);
      });
    },
    pageBeforeRemove: function pageBeforeRemove(page) {
      page.$el.find(".list-index-init").each(function(listIndexEl) {
        if (listIndexEl.f7ListIndex)
          listIndexEl.f7ListIndex.destroy();
      });
    }
  },
  vnode: {
    "list-index-init": {
      insert: function insert3(vnode) {
        var app = this;
        var listIndexEl = vnode.elm;
        var params = extend(dom7_default(listIndexEl).dataset(), {
          el: listIndexEl
        });
        app.listIndex.create(params);
      },
      destroy: function destroy(vnode) {
        var listIndexEl = vnode.elm;
        if (listIndexEl.f7ListIndex)
          listIndexEl.f7ListIndex.destroy();
      }
    }
  }
};

// node_modules/framework7/esm/components/timeline/timeline.js
var timeline_default = {
  name: "timeline"
};

// node_modules/framework7/esm/components/tabs/tabs.js
var Tab = {
  show: function show3() {
    var app = this;
    var tabEl;
    var tabLinkEl;
    var animate;
    var tabRoute;
    var animatedInit;
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    if (args.length === 1 && args[0] && args[0].constructor === Object) {
      tabEl = args[0].tabEl;
      tabLinkEl = args[0].tabLinkEl;
      animate = args[0].animate;
      tabRoute = args[0].tabRoute;
      animatedInit = args[0].animatedInit;
    } else {
      tabEl = args[0];
      tabLinkEl = args[1];
      animate = args[2];
      tabRoute = args[3];
      if (typeof args[1] === "boolean") {
        tabEl = args[0];
        animate = args[1];
        tabLinkEl = args[2];
        tabRoute = args[3];
        if (args.length > 2 && tabLinkEl.constructor === Object) {
          tabEl = args[0];
          animate = args[1];
          tabRoute = args[2];
          tabLinkEl = args[3];
        }
      }
    }
    if (typeof animate === "undefined")
      animate = true;
    var $newTabEl = dom7_default(tabEl);
    if (tabRoute && $newTabEl[0]) {
      $newTabEl[0].f7TabRoute = tabRoute;
    }
    if (!animatedInit && ($newTabEl.length === 0 || $newTabEl.hasClass("tab-active"))) {
      return {
        $newTabEl,
        newTabEl: $newTabEl[0]
      };
    }
    var $tabLinkEl;
    if (tabLinkEl)
      $tabLinkEl = dom7_default(tabLinkEl);
    var $tabsEl = $newTabEl.parent(".tabs");
    if ($tabsEl.length === 0) {
      return {
        $newTabEl,
        newTabEl: $newTabEl[0]
      };
    }
    if (app.swipeout)
      app.swipeout.allowOpen = true;
    var tabsChangedCallbacks = [];
    function onTabsChanged(callback) {
      tabsChangedCallbacks.push(callback);
    }
    function tabsChanged() {
      tabsChangedCallbacks.forEach(function(callback) {
        callback();
      });
    }
    var animated = false;
    if ($tabsEl.parent().hasClass("tabs-animated-wrap")) {
      $tabsEl.parent()[animate ? "removeClass" : "addClass"]("not-animated");
      var transitionDuration = parseFloat($tabsEl.css("transition-duration").replace(",", "."));
      if (animate && transitionDuration) {
        $tabsEl.transitionEnd(tabsChanged);
        animated = true;
      }
      var tabsTranslate = (app.rtl ? $newTabEl.index() : -$newTabEl.index()) * 100;
      $tabsEl.transform("translate3d(" + tabsTranslate + "%,0,0)");
    }
    var swiper;
    if ($tabsEl.parent().hasClass("tabs-swipeable-wrap") && app.swiper) {
      swiper = $tabsEl.parent()[0].swiper;
      if (swiper && swiper.activeIndex !== $newTabEl.index()) {
        animated = true;
        swiper.once("slideChangeTransitionEnd", function() {
          tabsChanged();
        }).slideTo($newTabEl.index(), animate ? void 0 : 0);
      } else if (swiper && swiper.animating) {
        animated = true;
        swiper.once("slideChangeTransitionEnd", function() {
          tabsChanged();
        });
      }
    }
    var $oldTabEl = $tabsEl.children(".tab-active");
    $oldTabEl.removeClass("tab-active");
    if (!animatedInit && (!swiper || swiper && !swiper.animating || swiper && tabRoute)) {
      if ($oldTabEl.hasClass("view") && $oldTabEl.children(".page").length) {
        $oldTabEl.children(".page").each(function(pageEl) {
          dom7_default(pageEl).trigger("page:tabhide");
          app.emit("pageTabHide", pageEl);
        });
      }
      $oldTabEl.trigger("tab:hide");
      app.emit("tabHide", $oldTabEl[0]);
    }
    $newTabEl.addClass("tab-active");
    if (!animatedInit && (!swiper || swiper && !swiper.animating || swiper && tabRoute)) {
      if ($newTabEl.hasClass("view") && $newTabEl.children(".page").length) {
        $newTabEl.children(".page").each(function(pageEl) {
          dom7_default(pageEl).trigger("page:tabshow");
          app.emit("pageTabShow", pageEl);
        });
      }
      $newTabEl.trigger("tab:show");
      app.emit("tabShow", $newTabEl[0]);
    }
    if (!$tabLinkEl) {
      if (typeof tabEl === "string")
        $tabLinkEl = dom7_default('.tab-link[href="' + tabEl + '"]');
      else
        $tabLinkEl = dom7_default('.tab-link[href="#' + $newTabEl.attr("id") + '"]');
      if (!$tabLinkEl || $tabLinkEl && $tabLinkEl.length === 0) {
        dom7_default("[data-tab]").each(function(el) {
          if ($newTabEl.is(dom7_default(el).attr("data-tab")))
            $tabLinkEl = dom7_default(el);
        });
      }
      if (tabRoute && (!$tabLinkEl || $tabLinkEl && $tabLinkEl.length === 0)) {
        $tabLinkEl = dom7_default('[data-route-tab-id="' + tabRoute.route.tab.id + '"]');
        if ($tabLinkEl.length === 0) {
          $tabLinkEl = dom7_default('.tab-link[href="' + tabRoute.url + '"]');
        }
      }
      if ($tabLinkEl.length > 1 && $newTabEl.parents(".page").length) {
        $tabLinkEl = $tabLinkEl.filter(function(tabLinkElement) {
          return dom7_default(tabLinkElement).parents(".page")[0] === $newTabEl.parents(".page")[0];
        });
        if (app.theme === "ios" && $tabLinkEl.length === 0 && tabRoute) {
          var $pageEl = $newTabEl.parents(".page");
          var $navbarEl = dom7_default(app.navbar.getElByPage($pageEl));
          $tabLinkEl = $navbarEl.find('[data-route-tab-id="' + tabRoute.route.tab.id + '"]');
          if ($tabLinkEl.length === 0) {
            $tabLinkEl = $navbarEl.find('.tab-link[href="' + tabRoute.url + '"]');
          }
        }
      }
    }
    if ($tabLinkEl.length > 0) {
      var $oldTabLinkEl;
      if ($oldTabEl && $oldTabEl.length > 0) {
        var oldTabId = $oldTabEl.attr("id");
        if (oldTabId) {
          $oldTabLinkEl = dom7_default('.tab-link[href="#' + oldTabId + '"]');
          if (!$oldTabLinkEl || $oldTabLinkEl && $oldTabLinkEl.length === 0) {
            $oldTabLinkEl = dom7_default('.tab-link[data-route-tab-id="' + oldTabId + '"]');
          }
        }
        if (!$oldTabLinkEl || $oldTabLinkEl && $oldTabLinkEl.length === 0) {
          dom7_default("[data-tab]").each(function(tabLinkElement) {
            if ($oldTabEl.is(dom7_default(tabLinkElement).attr("data-tab")))
              $oldTabLinkEl = dom7_default(tabLinkElement);
          });
        }
        if (!$oldTabLinkEl || $oldTabLinkEl && $oldTabLinkEl.length === 0) {
          $oldTabLinkEl = $tabLinkEl.siblings(".tab-link-active");
        }
      } else if (tabRoute) {
        $oldTabLinkEl = $tabLinkEl.siblings(".tab-link-active");
      }
      if ($oldTabLinkEl && $oldTabLinkEl.length > 1 && $oldTabEl && $oldTabEl.parents(".page").length) {
        $oldTabLinkEl = $oldTabLinkEl.filter(function(tabLinkElement) {
          return dom7_default(tabLinkElement).parents(".page")[0] === $oldTabEl.parents(".page")[0];
        });
      }
      if ($oldTabLinkEl && $oldTabLinkEl.length > 0)
        $oldTabLinkEl.removeClass("tab-link-active");
      if ($tabLinkEl && $tabLinkEl.length > 0) {
        $tabLinkEl.addClass("tab-link-active");
        var $tabbarEl = $tabLinkEl.parents(".tabbar, .tabbar-labels");
        var hasHighlight = app.toolbar && $tabbarEl.length > 0 && ($tabbarEl.hasClass("tabbar-highlight") || app.theme !== "ios");
        if (hasHighlight) {
          app.toolbar.setHighlight($tabbarEl);
        }
      }
    }
    return {
      $newTabEl,
      newTabEl: $newTabEl[0],
      $oldTabEl,
      oldTabEl: $oldTabEl[0],
      onTabsChanged,
      animated
    };
  }
};
var tabs_default = {
  name: "tabs",
  create: function create15() {
    var app = this;
    extend(app, {
      tab: {
        show: Tab.show.bind(app)
      }
    });
  },
  on: {
    "pageInit tabMounted": function onInit(pageOrTabEl) {
      var $el = dom7_default(pageOrTabEl.el || pageOrTabEl);
      var animatedTabEl = $el.find(".tabs-animated-wrap > .tabs > .tab-active")[0];
      if (!animatedTabEl)
        return;
      var app = this;
      app.tab.show({
        tabEl: animatedTabEl,
        animatedInit: true,
        animate: false
      });
    }
  },
  clicks: {
    ".tab-link": function tabLinkClick($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      if ($clickedEl.attr("href") && $clickedEl.attr("href").indexOf("#") === 0 || $clickedEl.attr("data-tab")) {
        var app = this;
        app.tab.show({
          tabEl: data.tab || $clickedEl.attr("href"),
          tabLinkEl: $clickedEl,
          animate: data.animate
        });
      }
    }
  }
};

// node_modules/framework7/esm/components/panel/swipe-panel.js
function swipePanel(panel) {
  var app = panel.app;
  if (panel.swipeInitialized) {
    return;
  }
  extend(panel, {
    swipeable: true,
    swipeInitialized: true
  });
  var params = panel.params;
  var $el = panel.$el, $backdropEl = panel.$backdropEl, side = panel.side, effect = panel.effect;
  var otherPanel;
  var isTouched;
  var isGestureStarted;
  var isMoved;
  var isScrolling;
  var isInterrupted;
  var touchesStart = {};
  var touchStartTime;
  var touchesDiff;
  var translate;
  var backdropOpacity;
  var panelWidth;
  var direction;
  var $viewEl;
  var touchMoves = 0;
  function handleTouchStart(e) {
    if (!panel.swipeable || isGestureStarted)
      return;
    if (!app.panel.allowOpen || !params.swipe && !params.swipeOnlyClose || isTouched)
      return;
    if (dom7_default(".modal-in:not(.toast):not(.notification), .photo-browser-in").length > 0)
      return;
    otherPanel = app.panel.get(side === "left" ? "right" : "left") || {};
    var otherPanelOpened = otherPanel.opened && otherPanel.$el && !otherPanel.$el.hasClass("panel-in-breakpoint");
    if (!panel.opened && otherPanelOpened) {
      return;
    }
    if (!params.swipeOnlyClose) {
      if (otherPanelOpened)
        return;
    }
    if (e.target && e.target.nodeName.toLowerCase() === "input" && e.target.type === "range")
      return;
    if (dom7_default(e.target).closest(".range-slider, .tabs-swipeable-wrap, .calendar-months, .no-swipe-panel, .card-opened").length > 0)
      return;
    touchesStart.x = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
    touchesStart.y = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
    if (params.swipeOnlyClose && !panel.opened) {
      return;
    }
    if (params.swipeActiveArea && !panel.opened) {
      if (side === "left") {
        if (touchesStart.x > params.swipeActiveArea)
          return;
      }
      if (side === "right") {
        if (touchesStart.x < app.width - params.swipeActiveArea)
          return;
      }
    }
    touchMoves = 0;
    $viewEl = dom7_default(panel.getViewEl());
    isMoved = false;
    isTouched = true;
    isScrolling = void 0;
    isInterrupted = false;
    touchStartTime = now();
    direction = void 0;
  }
  function handleTouchMove(e) {
    if (!isTouched || isGestureStarted || isInterrupted)
      return;
    touchMoves += 1;
    if (touchMoves < 2)
      return;
    if (e.f7PreventSwipePanel || app.preventSwipePanelBySwipeBack || app.preventSwipePanel) {
      isTouched = false;
      return;
    }
    var pageX = e.type === "touchmove" ? e.targetTouches[0].pageX : e.pageX;
    var pageY = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
    if (typeof isScrolling === "undefined") {
      isScrolling = !!(isScrolling || Math.abs(pageY - touchesStart.y) > Math.abs(pageX - touchesStart.x));
    }
    if (isScrolling) {
      isTouched = false;
      return;
    }
    if (!direction) {
      if (pageX > touchesStart.x) {
        direction = "to-right";
      } else {
        direction = "to-left";
      }
      if (params.swipeActiveArea > 0 && !panel.opened) {
        if (side === "left" && touchesStart.x > params.swipeActiveArea) {
          isTouched = false;
          return;
        }
        if (side === "right" && touchesStart.x < app.width - params.swipeActiveArea) {
          isTouched = false;
          return;
        }
      }
      if ($el.hasClass("panel-in-breakpoint")) {
        isTouched = false;
        return;
      }
      if (side === "left" && direction === "to-left" && !$el.hasClass("panel-in") || side === "right" && direction === "to-right" && !$el.hasClass("panel-in")) {
        isTouched = false;
        return;
      }
    }
    var threshold = panel.opened ? 0 : -params.swipeThreshold;
    if (side === "right")
      threshold = -threshold;
    if (!isMoved) {
      if (!panel.opened) {
        panel.insertToRoot();
        $el.addClass("panel-in-swipe");
        $backdropEl.css("visibility", "visible");
        $el.trigger("panel:swipeopen");
        panel.emit("local::swipeOpen panelSwipeOpen", panel);
      }
      panelWidth = $el[0].offsetWidth;
      if (effect === "reveal" && $el.hasClass("panel-in-collapsed")) {
        panelWidth -= parseFloat($viewEl.css("margin-" + side));
      }
      $el.transition(0);
    }
    isMoved = true;
    if (e.cancelable) {
      e.preventDefault();
    }
    touchesDiff = pageX - touchesStart.x + threshold;
    if (side === "right") {
      if (effect === "cover" || effect === "push") {
        translate = touchesDiff + (panel.opened ? 0 : panelWidth);
        if (translate < 0)
          translate = 0;
        if (translate > panelWidth) {
          translate = panelWidth;
        }
      } else {
        translate = touchesDiff - (panel.opened ? panelWidth : 0);
        if (translate > 0)
          translate = 0;
        if (translate < -panelWidth) {
          translate = -panelWidth;
        }
      }
    } else {
      translate = touchesDiff + (panel.opened ? panelWidth : 0);
      if (translate < 0)
        translate = 0;
      if (translate > panelWidth) {
        translate = panelWidth;
      }
    }
    var noFollowProgress = Math.abs(translate / panelWidth);
    if (effect === "reveal") {
      if (!params.swipeNoFollow) {
        $viewEl.transform("translate3d(" + translate + "px,0,0)").transition(0);
        $backdropEl.transform("translate3d(" + translate + "px,0,0)").transition(0);
      }
      $el.trigger("panel:swipe", Math.abs(translate / panelWidth));
      panel.emit("local::swipe panelSwipe", panel, Math.abs(translate / panelWidth));
    } else {
      if (side === "left")
        translate -= panelWidth;
      if (!params.swipeNoFollow) {
        $backdropEl.transition(0);
        backdropOpacity = 1 - Math.abs(translate / panelWidth);
        $backdropEl.css({
          opacity: backdropOpacity
        });
        $el.transform("translate3d(" + translate + "px,0,0)").transition(0);
        if (effect === "push") {
          var viewTranslate = side === "left" ? translate + panelWidth : translate - panelWidth;
          $viewEl.transform("translate3d(" + viewTranslate + "px,0,0)").transition(0);
          $backdropEl.transform("translate3d(" + viewTranslate + "px,0,0)").transition(0);
        }
      }
      $el.trigger("panel:swipe", Math.abs(translate / panelWidth));
      panel.emit("local::swipe panelSwipe", panel, Math.abs(translate / panelWidth));
    }
    if (params.swipeNoFollow) {
      var stateChanged = panel.opened && noFollowProgress === 0 || !panel.opened && noFollowProgress === 1;
      if (stateChanged) {
        isInterrupted = true;
        handleTouchEnd(e);
      }
    }
  }
  function handleTouchEnd(e) {
    if (!isTouched || !isMoved) {
      isTouched = false;
      isMoved = false;
      return;
    }
    var isGesture = e.type === "gesturestart" || isGestureStarted;
    isTouched = false;
    isMoved = false;
    var timeDiff = new Date().getTime() - touchStartTime;
    var action;
    var edge = (translate === 0 || Math.abs(translate) === panelWidth) && !params.swipeNoFollow;
    var threshold = params.swipeThreshold || 0;
    if (isGesture) {
      action = "reset";
    } else if (!panel.opened) {
      if (Math.abs(touchesDiff) < threshold) {
        action = "reset";
      } else if (effect === "cover" || effect === "push") {
        if (translate === 0) {
          action = "swap";
        } else if (timeDiff < 300 && Math.abs(translate) > 0) {
          action = "swap";
        } else if (timeDiff >= 300 && Math.abs(translate) < panelWidth / 2) {
          action = "swap";
        } else {
          action = "reset";
        }
      } else if (translate === 0) {
        action = "reset";
      } else if (timeDiff < 300 && Math.abs(translate) > 0 || timeDiff >= 300 && Math.abs(translate) >= panelWidth / 2) {
        action = "swap";
      } else {
        action = "reset";
      }
    } else if (effect === "cover" || effect === "push") {
      if (translate === 0) {
        action = "reset";
      } else if (timeDiff < 300 && Math.abs(translate) > 0) {
        action = "swap";
      } else if (timeDiff >= 300 && Math.abs(translate) < panelWidth / 2) {
        action = "reset";
      } else {
        action = "swap";
      }
    } else if (translate === -panelWidth) {
      action = "reset";
    } else if (timeDiff < 300 && Math.abs(translate) >= 0 || timeDiff >= 300 && Math.abs(translate) <= panelWidth / 2) {
      if (side === "left" && translate === panelWidth)
        action = "reset";
      else
        action = "swap";
    } else {
      action = "reset";
    }
    if (action === "swap") {
      if (panel.opened) {
        panel.close(!edge);
      } else {
        panel.open(!edge);
      }
    }
    var removePanelInClass = true;
    if (action === "reset") {
      if (!panel.opened) {
        if (edge) {
          $el.removeClass("panel-in-swipe");
        } else {
          removePanelInClass = false;
          var target = effect === "reveal" ? $viewEl : $el;
          panel.setStateClasses("before-closing");
          target.transitionEnd(function() {
            if ($el.hasClass("panel-in"))
              return;
            $el.removeClass("panel-in-swipe");
            panel.setStateClasses("after-closing");
          });
        }
      }
    }
    if (effect === "reveal" || effect === "push") {
      nextFrame(function() {
        $viewEl.transition("");
        $viewEl.transform("");
      });
    }
    if (removePanelInClass) {
      $el.removeClass("panel-in-swipe");
    }
    $el.transition("").transform("");
    $backdropEl.transform("").transition("").css({
      opacity: "",
      visibility: ""
    });
  }
  function handleGestureStart(e) {
    isGestureStarted = true;
    handleTouchEnd(e);
  }
  function handleGestureEnd() {
    isGestureStarted = false;
  }
  app.on("touchstart:passive", handleTouchStart);
  app.on("touchmove:active", handleTouchMove);
  app.on("touchend:passive", handleTouchEnd);
  app.on("gesturestart", handleGestureStart);
  app.on("gestureend", handleGestureEnd);
  panel.on("panelDestroy", function() {
    app.off("touchstart:passive", handleTouchStart);
    app.off("touchmove:active", handleTouchMove);
    app.off("touchend:passive", handleTouchEnd);
    app.off("gesturestart", handleGestureStart);
    app.off("gestureend", handleGestureEnd);
  });
}
var swipe_panel_default = swipePanel;

// node_modules/framework7/esm/components/panel/resizable-panel.js
function resizablePanel(panel) {
  var app = panel.app;
  var support2 = getSupport();
  if (panel.resizableInitialized)
    return;
  extend(panel, {
    resizable: true,
    resizableWidth: null,
    resizableInitialized: true
  });
  var $htmlEl = dom7_default("html");
  var $el = panel.$el, $backdropEl = panel.$backdropEl, side = panel.side, effect = panel.effect;
  if (!$el)
    return;
  var isTouched;
  var isMoved;
  var touchesStart = {};
  var touchesDiff;
  var panelWidth;
  var $viewEl;
  var panelMinWidth;
  var panelMaxWidth;
  var visibleByBreakpoint;
  function transformCSSWidth(v) {
    if (!v)
      return null;
    if (v.indexOf("%") >= 0 || v.indexOf("vw") >= 0) {
      return parseInt(v, 10) / 100 * app.width;
    }
    var newV = parseInt(v, 10);
    if (Number.isNaN(newV))
      return null;
    return newV;
  }
  function isResizable() {
    return panel.resizable && $el.hasClass("panel-resizable");
  }
  function handleTouchStart(e) {
    if (!isResizable())
      return;
    touchesStart.x = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
    touchesStart.y = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
    isMoved = false;
    isTouched = true;
    panelMinWidth = transformCSSWidth($el.css("min-width"));
    panelMaxWidth = transformCSSWidth($el.css("max-width"));
    visibleByBreakpoint = $el.hasClass("panel-in-breakpoint");
  }
  function handleTouchMove(e) {
    if (!isTouched)
      return;
    var pageX = e.type === "touchmove" ? e.targetTouches[0].pageX : e.pageX;
    if (!isMoved) {
      panelWidth = $el[0].offsetWidth;
      $el.transition(0);
      $el.addClass("panel-resizing");
      $htmlEl.css("cursor", "col-resize");
      if (effect !== "cover" || visibleByBreakpoint) {
        $viewEl = dom7_default(panel.getViewEl());
        if (panel.$containerEl && panel.$containerEl.hasClass("page")) {
          $viewEl.add(panel.$containerEl.children(".page-content, .tabs, .fab"));
        }
      }
      if (effect !== "cover" && !visibleByBreakpoint) {
        $backdropEl.transition(0);
        $viewEl.transition(0);
      }
    }
    isMoved = true;
    e.preventDefault();
    touchesDiff = pageX - touchesStart.x;
    var newPanelWidth = side === "left" ? panelWidth + touchesDiff : panelWidth - touchesDiff;
    if (panelMinWidth && !Number.isNaN(panelMinWidth)) {
      newPanelWidth = Math.max(newPanelWidth, panelMinWidth);
    }
    if (panelMaxWidth && !Number.isNaN(panelMaxWidth)) {
      newPanelWidth = Math.min(newPanelWidth, panelMaxWidth);
    }
    newPanelWidth = Math.min(Math.max(newPanelWidth, 0), app.width);
    panel.resizableWidth = newPanelWidth;
    $el[0].style.width = newPanelWidth + "px";
    if (effect !== "cover" && !visibleByBreakpoint) {
      if ($viewEl) {
        $viewEl.transform("translate3d(" + (side === "left" ? newPanelWidth : -newPanelWidth) + "px, 0, 0)");
      }
      if ($backdropEl) {
        $backdropEl.transform("translate3d(" + (side === "left" ? newPanelWidth : -newPanelWidth) + "px, 0, 0)");
      }
    } else if (visibleByBreakpoint && $viewEl) {
      $viewEl.css("margin-" + side, newPanelWidth + "px");
    }
    $el.trigger("panel:resize", newPanelWidth);
    panel.emit("local::resize panelResize", panel, newPanelWidth);
  }
  function handleTouchEnd() {
    dom7_default("html").css("cursor", "");
    if (!isTouched || !isMoved) {
      isTouched = false;
      isMoved = false;
      return;
    }
    isTouched = false;
    isMoved = false;
    $htmlEl[0].style.setProperty("--f7-panel-" + side + "-width", panel.resizableWidth + "px");
    $el[0].style.width = "";
    if (effect !== "cover" && !visibleByBreakpoint) {
      $viewEl.transform("");
      $backdropEl.transform("");
    }
    $el.removeClass("panel-resizing");
    nextFrame(function() {
      $el.transition("");
      if (effect !== "cover") {
        $backdropEl.transition("");
        if ($viewEl)
          $viewEl.transition("");
      }
    });
  }
  function handleResize() {
    if (!panel.opened || !panel.resizableWidth)
      return;
    panelMinWidth = transformCSSWidth($el.css("min-width"));
    panelMaxWidth = transformCSSWidth($el.css("max-width"));
    if (panelMinWidth && !Number.isNaN(panelMinWidth) && panel.resizableWidth < panelMinWidth) {
      panel.resizableWidth = Math.max(panel.resizableWidth, panelMinWidth);
    }
    if (panelMaxWidth && !Number.isNaN(panelMaxWidth) && panel.resizableWidth > panelMaxWidth) {
      panel.resizableWidth = Math.min(panel.resizableWidth, panelMaxWidth);
    }
    panel.resizableWidth = Math.min(Math.max(panel.resizableWidth, 0), app.width);
    $htmlEl[0].style.setProperty("--f7-panel-" + side + "-width", panel.resizableWidth + "px");
  }
  if (panel.$el.find(".panel-resize-handler").length === 0) {
    panel.$el.append('<div class="panel-resize-handler"></div>');
  }
  panel.$resizeHandlerEl = panel.$el.children(".panel-resize-handler");
  $el.addClass("panel-resizable");
  var passive = support2.passiveListener ? {
    passive: true
  } : false;
  panel.$el.on(app.touchEvents.start, ".panel-resize-handler", handleTouchStart, passive);
  app.on("touchmove:active", handleTouchMove);
  app.on("touchend:passive", handleTouchEnd);
  app.on("resize", handleResize);
  panel.on("beforeOpen", handleResize);
  panel.once("panelDestroy", function() {
    $el.removeClass("panel-resizable");
    panel.$resizeHandlerEl.remove();
    panel.$el.off(app.touchEvents.start, ".panel-resize-handler", handleTouchStart, passive);
    app.off("touchmove:active", handleTouchMove);
    app.off("touchend:passive", handleTouchEnd);
    app.off("resize", handleResize);
    panel.off("beforeOpen", handleResize);
  });
}
var resizable_panel_default = resizablePanel;

// node_modules/framework7/esm/components/panel/panel-class.js
function _assertThisInitialized10(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose10(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf10(subClass, superClass);
}
function _setPrototypeOf10(o, p) {
  _setPrototypeOf10 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf10(o, p);
}
var Panel = function(_Framework7Class) {
  _inheritsLoose10(Panel2, _Framework7Class);
  function Panel2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    var extendedParams = extend({
      on: {}
    }, app.params.panel, params);
    _this = _Framework7Class.call(this, extendedParams, [app]) || this;
    var panel = _assertThisInitialized10(_this);
    panel.params = extendedParams;
    panel.$containerEl = panel.params.containerEl ? dom7_default(panel.params.containerEl).eq(0) : app.$el;
    panel.containerEl = panel.$containerEl[0];
    if (!panel.containerEl) {
      panel.$containerEl = app.$el;
      panel.containerEl = app.$el[0];
    }
    var $el;
    if (panel.params.el) {
      $el = dom7_default(panel.params.el).eq(0);
    } else if (panel.params.content) {
      $el = dom7_default(panel.params.content).filter(function(node) {
        return node.nodeType === 1;
      }).eq(0);
    }
    if ($el.length === 0)
      return panel || _assertThisInitialized10(_this);
    if ($el[0].f7Panel)
      return $el[0].f7Panel || _assertThisInitialized10(_this);
    $el[0].f7Panel = panel;
    var _panel$params = panel.params, side = _panel$params.side, effect = _panel$params.effect, resizable = _panel$params.resizable;
    if (typeof side === "undefined")
      side = $el.hasClass("panel-left") ? "left" : "right";
    if (typeof effect === "undefined")
      effect = $el.hasClass("panel-cover") ? "cover" : $el.hasClass("panel-push") ? "push" : "reveal";
    if (typeof resizable === "undefined")
      resizable = $el.hasClass("panel-resizable");
    var $backdropEl;
    if (panel.params.backdrop && panel.params.backdropEl) {
      $backdropEl = dom7_default(panel.params.backdropEl);
    } else if (panel.params.backdrop) {
      $backdropEl = panel.$containerEl.children(".panel-backdrop");
      if ($backdropEl.length === 0) {
        $backdropEl = dom7_default('<div class="panel-backdrop"></div>');
        panel.$containerEl.prepend($backdropEl);
      }
    }
    extend(panel, {
      app,
      side,
      effect,
      resizable,
      $el,
      el: $el[0],
      opened: false,
      $backdropEl,
      backdropEl: $backdropEl && $backdropEl[0]
    });
    panel.useModules();
    panel.init();
    return panel || _assertThisInitialized10(_this);
  }
  var _proto = Panel2.prototype;
  _proto.getViewEl = function getViewEl() {
    var panel = this;
    var viewEl;
    if (panel.$containerEl.children(".views").length > 0) {
      viewEl = panel.$containerEl.children(".views")[0];
    } else {
      viewEl = panel.$containerEl.children(".view")[0];
    }
    return viewEl;
  };
  _proto.setStateClasses = function setStateClasses(state) {
    var panel = this;
    var side = panel.side, el = panel.el;
    var viewEl = panel.getViewEl();
    var panelInView = viewEl && viewEl.contains(el);
    var $targetEl = !viewEl || panelInView ? panel.$containerEl : dom7_default("html");
    if (state === "open") {
      $targetEl.addClass("with-panel with-panel-" + panel.side + "-" + panel.effect);
    }
    if (state === "before-closing") {
      $targetEl.addClass("with-panel-closing");
    }
    if (state === "closing") {
      $targetEl.addClass("with-panel-closing");
      $targetEl.removeClass("with-panel with-panel-" + panel.side + "-" + panel.effect);
    }
    if (state === "after-closing") {
      $targetEl.removeClass("with-panel-closing");
    }
    if (state === "closed") {
      $targetEl.removeClass("with-panel-" + side + "-reveal with-panel-" + side + "-cover with-panel-" + side + "-push with-panel");
    }
  };
  _proto.enableVisibleBreakpoint = function enableVisibleBreakpoint() {
    var panel = this;
    panel.visibleBreakpointDisabled = false;
    panel.setVisibleBreakpoint();
    return panel;
  };
  _proto.disableVisibleBreakpoint = function disableVisibleBreakpoint() {
    var panel = this;
    panel.visibleBreakpointDisabled = true;
    panel.setVisibleBreakpoint();
    return panel;
  };
  _proto.toggleVisibleBreakpoint = function toggleVisibleBreakpoint() {
    var panel = this;
    panel.visibleBreakpointDisabled = !panel.visibleBreakpointDisabled;
    panel.setVisibleBreakpoint();
    return panel;
  };
  _proto.setVisibleBreakpoint = function setVisibleBreakpoint(emitEvents) {
    if (emitEvents === void 0) {
      emitEvents = true;
    }
    var panel = this;
    var app = panel.app;
    if (!panel.visibleBreakpointResizeHandler) {
      panel.visibleBreakpointResizeHandler = function visibleBreakpointResizeHandler() {
        panel.setVisibleBreakpoint();
      };
      app.on("resize", panel.visibleBreakpointResizeHandler);
    }
    var side = panel.side, $el = panel.$el, $containerEl = panel.$containerEl, params = panel.params, visibleBreakpointDisabled = panel.visibleBreakpointDisabled;
    var breakpoint = params.visibleBreakpoint;
    var $viewEl = dom7_default(panel.getViewEl());
    var wasVisible = $el.hasClass("panel-in-breakpoint");
    if ($containerEl && $containerEl.hasClass("page")) {
      $viewEl.add($containerEl.children(".page-content, .tabs, .fab"));
    }
    if (app.width >= breakpoint && typeof breakpoint !== "undefined" && breakpoint !== null && !visibleBreakpointDisabled) {
      if (!wasVisible) {
        var _$viewEl$css;
        panel.setStateClasses("closed");
        $el.addClass("panel-in-breakpoint").removeClass("panel-in panel-in-collapsed");
        panel.onOpen(false);
        panel.onOpened();
        $viewEl.css((_$viewEl$css = {}, _$viewEl$css["margin-" + side] = $el.width() + "px", _$viewEl$css));
        app.allowPanelOpen = true;
        if (emitEvents) {
          panel.emit("local::breakpoint panelBreakpoint", panel);
          panel.$el.trigger("panel:breakpoint");
        }
      } else {
        var _$viewEl$css2;
        $viewEl.css((_$viewEl$css2 = {}, _$viewEl$css2["margin-" + side] = $el.width() + "px", _$viewEl$css2));
      }
    } else if (wasVisible) {
      var _$viewEl$css3;
      $el.removeClass("panel-in-breakpoint panel-in");
      panel.onClose();
      panel.onClosed();
      $viewEl.css((_$viewEl$css3 = {}, _$viewEl$css3["margin-" + side] = "", _$viewEl$css3));
      if (emitEvents) {
        panel.emit("local::breakpoint panelBreakpoint", panel);
        panel.$el.trigger("panel:breakpoint");
      }
    }
  };
  _proto.enableCollapsedBreakpoint = function enableCollapsedBreakpoint() {
    var panel = this;
    panel.collapsedBreakpointDisabled = false;
    panel.setCollapsedBreakpoint();
    return panel;
  };
  _proto.disableCollapsedBreakpoint = function disableCollapsedBreakpoint() {
    var panel = this;
    panel.collapsedBreakpointDisabled = true;
    panel.setCollapsedBreakpoint();
    return panel;
  };
  _proto.toggleCollapsedBreakpoint = function toggleCollapsedBreakpoint() {
    var panel = this;
    panel.collapsedBreakpointDisabled = !panel.collapsedBreakpointDisabled;
    panel.setCollapsedBreakpoint();
    return panel;
  };
  _proto.setCollapsedBreakpoint = function setCollapsedBreakpoint(emitEvents) {
    if (emitEvents === void 0) {
      emitEvents = true;
    }
    var panel = this;
    var app = panel.app;
    if (!panel.collapsedBreakpointResizeHandler) {
      panel.collapsedBreakpointResizeHandler = function collapsedBreakpointResizeHandler() {
        panel.setCollapsedBreakpoint();
      };
      app.on("resize", panel.collapsedBreakpointResizeHandler);
    }
    var $el = panel.$el, params = panel.params, collapsedBreakpointDisabled = panel.collapsedBreakpointDisabled;
    if ($el.hasClass("panel-in-breakpoint"))
      return;
    var breakpoint = params.collapsedBreakpoint;
    var wasVisible = $el.hasClass("panel-in-collapsed");
    if (app.width >= breakpoint && typeof breakpoint !== "undefined" && breakpoint !== null && !collapsedBreakpointDisabled) {
      if (!wasVisible) {
        panel.setStateClasses("closed");
        $el.addClass("panel-in-collapsed").removeClass("panel-in");
        panel.collapsed = true;
        app.allowPanelOpen = true;
        if (emitEvents) {
          panel.emit("local::collapsedBreakpoint panelCollapsedBreakpoint", panel);
          panel.$el.trigger("panel:collapsedbreakpoint");
        }
      }
    } else if (wasVisible) {
      $el.removeClass("panel-in-collapsed panel-in");
      panel.collapsed = false;
      if (emitEvents) {
        panel.emit("local::collapsedBreakpoint panelCollapsedBreakpoint", panel);
        panel.$el.trigger("panel:collapsedbreakpoint");
      }
    }
  };
  _proto.enableResizable = function enableResizable() {
    var panel = this;
    if (panel.resizableInitialized) {
      panel.resizable = true;
      panel.$el.addClass("panel-resizable");
    } else {
      resizable_panel_default(panel);
    }
    return panel;
  };
  _proto.disableResizable = function disableResizable() {
    var panel = this;
    panel.resizable = false;
    panel.$el.removeClass("panel-resizable");
    return panel;
  };
  _proto.enableSwipe = function enableSwipe() {
    var panel = this;
    if (panel.swipeInitialized) {
      panel.swipeable = true;
    } else {
      swipe_panel_default(panel);
    }
    return panel;
  };
  _proto.disableSwipe = function disableSwipe() {
    var panel = this;
    panel.swipeable = false;
    return panel;
  };
  _proto.onOpen = function onOpen(modifyHtmlClasses) {
    if (modifyHtmlClasses === void 0) {
      modifyHtmlClasses = true;
    }
    var panel = this;
    var app = panel.app;
    panel.opened = true;
    app.panel.allowOpen = false;
    panel.$el.trigger("panel:beforeopen");
    panel.emit("local::beforeOpen panelBeforeOpen", panel);
    if (modifyHtmlClasses) {
      panel.setStateClasses("open");
    }
    panel.$el.trigger("panel:open");
    panel.emit("local::open panelOpen", panel);
  };
  _proto.onOpened = function onOpened() {
    var panel = this;
    var app = panel.app;
    app.panel.allowOpen = true;
    panel.$el.trigger("panel:opened");
    panel.emit("local::opened panelOpened", panel);
  };
  _proto.onClose = function onClose() {
    var panel = this;
    var app = panel.app;
    panel.opened = false;
    app.panel.allowOpen = false;
    panel.$el.trigger("panel:beforeclose");
    panel.emit("local::beforeClose panelBeforeClose", panel);
    panel.setStateClasses("closing");
    panel.$el.trigger("panel:close");
    panel.emit("local::close panelClose", panel);
  };
  _proto.onClosed = function onClosed() {
    var panel = this;
    var app = panel.app;
    app.panel.allowOpen = true;
    panel.setStateClasses("after-closing");
    panel.$el.removeClass("panel-out");
    if (panel.$backdropEl) {
      var otherPanel = app.panel.get(".panel-in");
      var shouldHideBackdrop = !otherPanel || otherPanel && !otherPanel.$backdropEl;
      if (shouldHideBackdrop) {
        panel.$backdropEl.removeClass("panel-backdrop-in");
      }
    }
    panel.$el.trigger("panel:closed");
    panel.emit("local::closed panelClosed", panel);
  };
  _proto.toggle = function toggle11(animate) {
    if (animate === void 0) {
      animate = true;
    }
    var panel = this;
    var breakpoint = panel.params.visibleBreakpoint;
    var app = panel.app;
    if (app.width >= breakpoint && typeof breakpoint !== "undefined" && breakpoint !== null) {
      return panel.toggleVisibleBreakpoint();
    }
    if (panel.opened)
      panel.close(animate);
    else
      panel.open(animate);
    return panel;
  };
  _proto.insertToRoot = function insertToRoot() {
    var panel = this;
    var document = getDocument();
    var $el = panel.$el, $backdropEl = panel.$backdropEl, $containerEl = panel.$containerEl;
    var $panelParentEl = $el.parent();
    var wasInDom = $el.parents(document).length > 0;
    if (!$panelParentEl.is($containerEl) || $el.prevAll(".views, .view").length) {
      var $insertBeforeEl = $containerEl.children(".panel, .views, .view").eq(0);
      var $insertAfterEl = $containerEl.children(".panel-backdrop").eq(0);
      if ($insertBeforeEl.length) {
        $el.insertBefore($insertBeforeEl);
      } else if ($insertAfterEl) {
        $el.insertBefore($insertAfterEl);
      } else {
        $containerEl.prepend($el);
      }
      if ($backdropEl && $backdropEl.length && (!$backdropEl.parent().is($containerEl) && $backdropEl.nextAll(".panel").length === 0 || $backdropEl.parent().is($containerEl) && $backdropEl.nextAll(".panel").length === 0)) {
        $backdropEl.insertBefore($el);
      }
      panel.once("panelClosed", function() {
        if (wasInDom) {
          $panelParentEl.append($el);
        } else {
          $el.remove();
        }
      });
    }
  };
  _proto.open = function open12(animate) {
    if (animate === void 0) {
      animate = true;
    }
    var panel = this;
    var app = panel.app;
    if (!app.panel.allowOpen)
      return false;
    var effect = panel.effect, $el = panel.$el, $backdropEl = panel.$backdropEl, opened = panel.opened, $containerEl = panel.$containerEl;
    if (!$el || $el.hasClass("panel-in")) {
      return panel;
    }
    panel.insertToRoot();
    if (opened || $el.hasClass("panel-in-breakpoint") || $el.hasClass("panel-in"))
      return false;
    var otherOpenedPanel = app.panel.get(".panel-in");
    if (otherOpenedPanel && otherOpenedPanel !== panel) {
      otherOpenedPanel.close(animate);
    }
    $el[animate ? "removeClass" : "addClass"]("not-animated");
    $el.addClass("panel-in");
    if ($backdropEl) {
      $backdropEl.addClass("panel-backdrop-in");
      $backdropEl[animate ? "removeClass" : "addClass"]("not-animated");
    }
    if (panel.effect === "cover" || panel.effect === "push") {
      panel._clientLeft = $el[0].clientLeft;
    }
    var $viewEl = dom7_default(panel.getViewEl());
    if ($containerEl && $containerEl.hasClass("page")) {
      $viewEl.add($containerEl.children(".page-content, .tabs"));
    }
    var transitionEndTarget = effect === "reveal" ? $viewEl : $el;
    function panelTransitionEnd() {
      transitionEndTarget.transitionEnd(function(e) {
        if (dom7_default(e.target).is(transitionEndTarget)) {
          if ($el.hasClass("panel-out")) {
            panel.onClosed();
          } else {
            panel.onOpened();
          }
        } else
          panelTransitionEnd();
      });
    }
    if (animate) {
      if ($backdropEl) {
        $backdropEl.removeClass("not-animated");
      }
      panelTransitionEnd();
      $el.removeClass("panel-out not-animated").addClass("panel-in");
      panel.onOpen();
    } else {
      if ($backdropEl) {
        $backdropEl.addClass("not-animated");
      }
      $el.removeClass("panel-out").addClass("panel-in not-animated");
      panel.onOpen();
      panel.onOpened();
    }
    return true;
  };
  _proto.close = function close12(animate) {
    if (animate === void 0) {
      animate = true;
    }
    var panel = this;
    var effect = panel.effect, $el = panel.$el, $backdropEl = panel.$backdropEl, opened = panel.opened, $containerEl = panel.$containerEl;
    if (!opened || $el.hasClass("panel-in-breakpoint") || !$el.hasClass("panel-in"))
      return panel;
    $el[animate ? "removeClass" : "addClass"]("not-animated");
    if ($backdropEl) {
      $backdropEl[animate ? "removeClass" : "addClass"]("not-animated");
    }
    var $viewEl = dom7_default(panel.getViewEl());
    if ($containerEl && $containerEl.hasClass("page")) {
      $viewEl.add($containerEl.children(".page-content, .tabs"));
    }
    var transitionEndTarget = effect === "reveal" ? $viewEl : $el;
    function transitionEnd5() {
      if ($el.hasClass("panel-out")) {
        panel.onClosed();
      } else if ($el.hasClass("panel-in")) {
        panel.onOpened();
      }
      panel.setStateClasses("after-closing");
    }
    if (animate) {
      transitionEndTarget.transitionEnd(function() {
        transitionEnd5();
      });
      $el.removeClass("panel-in").addClass("panel-out");
      panel.onClose();
    } else {
      $el.addClass("not-animated").removeClass("panel-in").addClass("panel-out");
      panel.onClose();
      panel.onClosed();
    }
    return panel;
  };
  _proto.init = function init46() {
    var panel = this;
    if (typeof panel.params.visibleBreakpoint !== "undefined") {
      panel.setVisibleBreakpoint();
    }
    if (typeof panel.params.collapsedBreakpoint !== "undefined") {
      panel.setCollapsedBreakpoint();
    }
    if (panel.params.swipe) {
      panel.enableSwipe();
    }
    if (panel.resizable) {
      panel.enableResizable();
    }
  };
  _proto.destroy = function destroy50() {
    var panel = this;
    var app = panel.app;
    var _panel = panel, $containerEl = _panel.$containerEl;
    if (!panel.$el) {
      return;
    }
    panel.emit("local::beforeDestroy panelBeforeDestroy", panel);
    panel.$el.trigger("panel:beforedestroy");
    if (panel.visibleBreakpointResizeHandler) {
      app.off("resize", panel.visibleBreakpointResizeHandler);
    }
    if (panel.collapsedBreakpointResizeHandler) {
      app.off("resize", panel.collapsedBreakpointResizeHandler);
    }
    if (panel.$el.hasClass("panel-in-breakpoint") || panel.$el.hasClass("panel-in-collapsed")) {
      var _$viewEl$css4;
      var $viewEl = dom7_default(panel.getViewEl());
      if ($containerEl && $containerEl.hasClass("page")) {
        $viewEl.add($containerEl.children(".page-content, .tabs"));
      }
      panel.$el.removeClass("panel-in-breakpoint panel-in-collapsed panel-in");
      $viewEl.css((_$viewEl$css4 = {}, _$viewEl$css4["margin-" + panel.side] = "", _$viewEl$css4));
      panel.emit("local::breakpoint panelBreakpoint", panel);
      panel.$el.trigger("panel:breakpoint");
    }
    panel.$el.trigger("panel:destroy");
    panel.emit("local::destroy panelDestroy", panel);
    if (panel.el) {
      panel.el.f7Panel = null;
      delete panel.el.f7Panel;
    }
    deleteProps(panel);
    panel = null;
  };
  return Panel2;
}(class_default);
var panel_class_default = Panel;

// node_modules/framework7/esm/components/panel/panel.js
var panel_default = {
  name: "panel",
  params: {
    panel: {
      opened: void 0,
      side: void 0,
      effect: void 0,
      resizable: void 0,
      backdrop: true,
      backdropEl: void 0,
      visibleBreakpoint: void 0,
      collapsedBreakpoint: void 0,
      swipe: false,
      swipeNoFollow: false,
      swipeOnlyClose: false,
      swipeActiveArea: 0,
      swipeThreshold: 0,
      closeByBackdropClick: true,
      containerEl: void 0
    }
  },
  static: {
    Panel: panel_class_default
  },
  create: function create16() {
    var app = this;
    extend(app, {
      panel: {
        allowOpen: true,
        create: function create70(params) {
          return new panel_class_default(app, params);
        },
        get: function get2(el) {
          if (el === void 0) {
            el = ".panel";
          }
          if (el instanceof panel_class_default)
            return el;
          if (el === "left" || el === "right")
            el = ".panel-" + el;
          var $el = dom7_default(el);
          if ($el.length === 0 || $el.length > 1)
            return void 0;
          return $el[0].f7Panel;
        },
        destroy: function destroy50(el) {
          if (el === void 0) {
            el = ".panel";
          }
          var panel = app.panel.get(el);
          if (panel && panel.destroy)
            return panel.destroy();
          return void 0;
        },
        open: function open12(el, animate) {
          if (el === void 0) {
            el = ".panel";
          }
          if (el === "left" || el === "right")
            el = ".panel-" + el;
          var panel = app.panel.get(el);
          if (panel && panel.open)
            return panel.open(animate);
          if (!panel) {
            panel = app.panel.create({
              el
            });
            return panel.open(animate);
          }
          return void 0;
        },
        close: function close12(el, animate) {
          if (el === void 0) {
            el = ".panel-in";
          }
          if (el === "left" || el === "right")
            el = ".panel-" + el;
          var panel = app.panel.get(el);
          if (panel && panel.open)
            return panel.close(animate);
          if (!panel) {
            panel = app.panel.create({
              el
            });
            return panel.close(animate);
          }
          return void 0;
        },
        toggle: function toggle11(el, animate) {
          if (el === void 0) {
            el = ".panel";
          }
          if (el === "left" || el === "right")
            el = ".panel-" + el;
          var panel = app.panel.get(el);
          if (panel && panel.toggle)
            return panel.toggle(animate);
          if (!panel) {
            panel = app.panel.create({
              el
            });
            return panel.toggle(animate);
          }
          return void 0;
        }
      }
    });
  },
  on: {
    init: function init6() {
      var app = this;
      dom7_default(".panel-init").each(function(panelEl) {
        var params = Object.assign({
          el: panelEl
        }, dom7_default(panelEl).dataset() || {});
        app.panel.create(params);
      });
    },
    pageInit: function pageInit4(page) {
      var app = this;
      page.$el.find(".panel-init").each(function(panelEl) {
        var params = Object.assign({
          el: panelEl
        }, dom7_default(panelEl).dataset() || {});
        app.panel.create(params);
      });
    },
    pageBeforeRemove: function pageBeforeRemove2(page) {
      var app = this;
      page.$el.find(".panel-init").each(function(panelEl) {
        var panel = app.panel.get(panelEl);
        if (panel && panel.destroy)
          panel.destroy();
      });
    }
  },
  vnode: {
    "panel-init": {
      insert: function insert4(vnode) {
        var app = this;
        var panelEl = vnode.elm;
        var params = Object.assign({
          el: panelEl
        }, dom7_default(panelEl).dataset() || {});
        app.panel.create(params);
      },
      destroy: function destroy2(vnode) {
        var app = this;
        var panelEl = vnode.elm;
        var panel = app.panel.get(panelEl);
        if (panel && panel.destroy)
          panel.destroy();
      }
    }
  },
  clicks: {
    ".panel-open": function open4(clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.panel.open(data.panel, data.animate);
    },
    ".panel-close": function close3(clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.panel.close(data.panel, data.animate);
    },
    ".panel-toggle": function close4(clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.panel.toggle(data.panel, data.animate);
    },
    ".panel-backdrop": function close5() {
      var app = this;
      var $panelEl = dom7_default(".panel-in:not(.panel-out)");
      if (!$panelEl.length)
        return;
      var instance = $panelEl[0] && $panelEl[0].f7Panel;
      $panelEl.trigger("panel:backdrop-click");
      if (instance) {
        instance.emit("backdropClick", instance);
      }
      app.emit("panelBackdropClick", instance || $panelEl[0]);
      if (app.params.panel.closeByBackdropClick)
        app.panel.close();
    }
  }
};

// node_modules/framework7/esm/components/card/card.js
var CardExpandable = {
  open: function open5(cardEl, animate) {
    if (cardEl === void 0) {
      cardEl = ".card-expandable";
    }
    if (animate === void 0) {
      animate = true;
    }
    var app = this;
    var device2 = getDevice();
    var document = getDocument();
    var support2 = getSupport();
    var $cardEl = dom7_default(cardEl).eq(0);
    if (!$cardEl || !$cardEl.length)
      return;
    if ($cardEl.hasClass("card-opened") || $cardEl.hasClass("card-opening") || $cardEl.hasClass("card-closing"))
      return;
    var $pageEl = $cardEl.parents(".page").eq(0);
    if (!$pageEl.length)
      return;
    if ($pageEl.find(".card-opened").length) {
      return;
    }
    var prevented;
    function prevent() {
      prevented = true;
    }
    $cardEl.trigger("card:beforeopen", {
      prevent
    });
    app.emit("cardBeforeOpen", $cardEl[0], prevent);
    if (prevented)
      return;
    var cardParams = Object.assign({
      animate
    }, app.params.card, $cardEl.dataset());
    var $pageContentEl = $cardEl.parents(".page-content");
    var $backdropEl;
    if ($cardEl.attr("data-backdrop-el")) {
      $backdropEl = dom7_default($cardEl.attr("data-backdrop-el"));
    }
    if (!$backdropEl && cardParams.backdrop) {
      $backdropEl = $pageContentEl.find(".card-backdrop");
      if (!$backdropEl.length) {
        $backdropEl = dom7_default('<div class="card-backdrop"></div>');
        $pageContentEl.append($backdropEl);
      }
    }
    var $navbarEl;
    var $toolbarEl;
    if (cardParams.hideNavbarOnOpen) {
      $navbarEl = $pageEl.children(".navbar");
      if (!$navbarEl.length) {
        if ($pageEl[0].f7Page)
          $navbarEl = $pageEl[0].f7Page.$navbarEl;
      }
    }
    if (cardParams.hideToolbarOnOpen) {
      $toolbarEl = $pageEl.children(".toolbar");
      if (!$toolbarEl.length) {
        $toolbarEl = $pageEl.parents(".view").children(".toolbar");
      }
      if (!$toolbarEl.length) {
        $toolbarEl = $pageEl.parents(".views").children(".toolbar");
      }
    }
    var currTransform = $cardEl.css("transform");
    var hasTransform;
    if (currTransform && currTransform.match(/[2-9]/)) {
      hasTransform = true;
    }
    var $cardContentEl = $cardEl.children(".card-content");
    var $cardSizeEl = dom7_default(document.createElement("div")).addClass("card-expandable-size");
    $cardEl.append($cardSizeEl);
    var cardWidth = $cardEl[0].offsetWidth;
    var cardHeight = $cardEl[0].offsetHeight;
    var pageWidth = $pageEl[0].offsetWidth;
    var pageHeight = $pageEl[0].offsetHeight;
    var maxWidth = $cardSizeEl[0].offsetWidth || pageWidth;
    var maxHeight = $cardSizeEl[0].offsetHeight || pageHeight;
    var statusbarHeight;
    if ($navbarEl && !cardParams.hideStatusbarOnOpen && maxHeight === pageHeight) {
      statusbarHeight = parseInt($navbarEl.css("--f7-safe-area-top"), 10);
      if (Number.isNaN(statusbarHeight))
        statusbarHeight = 0;
    }
    if (statusbarHeight) {
      maxHeight -= statusbarHeight;
    }
    var scaleX = maxWidth / cardWidth;
    var scaleY = maxHeight / cardHeight;
    var offset2 = $cardEl.offset();
    var pageOffset = $pageEl.offset();
    if (statusbarHeight) {
      pageOffset.top += statusbarHeight / 2;
    }
    offset2.left -= pageOffset.left;
    var cardLeftOffset;
    var cardTopOffset;
    if (hasTransform) {
      var transformValues = currTransform.replace(/matrix\(|\)/g, "").split(",").map(function(el) {
        return el.trim();
      });
      if (transformValues && transformValues.length > 1) {
        var scale = parseFloat(transformValues[0]);
        cardLeftOffset = offset2.left - cardWidth * (1 - scale) / 2;
        cardTopOffset = offset2.top - pageOffset.top - cardHeight * (1 - scale) / 2;
        if (app.rtl)
          cardLeftOffset -= $cardEl[0].scrollLeft;
      } else {
        cardLeftOffset = $cardEl[0].offsetLeft;
        cardTopOffset = $cardEl[0].offsetTop - ($pageContentEl.length ? $pageContentEl[0].scrollTop : 0);
      }
    } else {
      cardLeftOffset = offset2.left;
      cardTopOffset = offset2.top - pageOffset.top;
      if (app.rtl)
        cardLeftOffset -= $cardEl[0].scrollLeft;
    }
    cardLeftOffset -= (pageWidth - maxWidth) / 2;
    cardTopOffset -= (pageHeight - maxHeight) / 2;
    var cardRightOffset = maxWidth - cardWidth - cardLeftOffset;
    if (app.rtl) {
      var _ref = [cardRightOffset, cardLeftOffset];
      cardLeftOffset = _ref[0];
      cardRightOffset = _ref[1];
    }
    var cardBottomOffset = maxHeight - cardHeight - cardTopOffset;
    var translateX = (cardRightOffset - cardLeftOffset) / 2;
    var translateY = (cardBottomOffset - cardTopOffset) / 2;
    if (cardParams.hideNavbarOnOpen && $navbarEl && $navbarEl.length) {
      if ($navbarEl.closest(".navbar-hidden").length) {
        $cardEl[0].f7KeepNavbarOnClose = true;
      } else {
        delete $cardEl[0].f7KeepNavbarOnClose;
        app.navbar.hide($navbarEl, cardParams.animate, cardParams.hideStatusbarOnOpen, true);
      }
    }
    if (cardParams.hideToolbarOnOpen && $toolbarEl && $toolbarEl.length) {
      if ($toolbarEl.closest(".toolbar-hidden").length) {
        $cardEl[0].f7KeepToolbarOnClose = true;
      } else {
        delete $cardEl[0].f7KeepToolbarOnClose;
        app.toolbar.hide($toolbarEl, cardParams.animate);
      }
    }
    if ($backdropEl) {
      $backdropEl.removeClass("card-backdrop-out").addClass("card-backdrop-in");
    }
    $cardEl.removeClass("card-transitioning");
    if (cardParams.animate) {
      $cardEl.addClass("card-opening");
    }
    $cardEl.trigger("card:open");
    app.emit("cardOpen", $cardEl[0]);
    function transitionEnd5() {
      $pageEl.addClass("page-with-card-opened");
      if (device2.ios && $pageContentEl.length) {
        $pageContentEl.css("height", $pageContentEl[0].offsetHeight + 1 + "px");
        setTimeout(function() {
          $pageContentEl.css("height", "");
        });
      }
      $cardEl.addClass("card-opened");
      $cardEl.removeClass("card-opening");
      $cardEl.trigger("card:opened");
      app.emit("cardOpened", $cardEl[0], $pageEl[0]);
    }
    $cardContentEl.css({
      width: maxWidth + "px",
      height: maxHeight + "px"
    }).transform("translate3d(" + (app.rtl ? cardLeftOffset + translateX : -cardLeftOffset - translateX) + "px, 0px, 0) scale(" + 1 / scaleX + ", " + 1 / scaleY + ")");
    $cardEl.transform("translate3d(" + (app.rtl ? -translateX : translateX) + "px, " + translateY + "px, 0) scale(" + scaleX + ", " + scaleY + ")");
    if (cardParams.animate) {
      $cardEl.transitionEnd(function() {
        transitionEnd5();
      });
    } else {
      transitionEnd5();
    }
    function onResize2() {
      $cardEl.removeClass("card-transitioning");
      cardWidth = $cardEl[0].offsetWidth;
      cardHeight = $cardEl[0].offsetHeight;
      pageWidth = $pageEl[0].offsetWidth;
      pageHeight = $pageEl[0].offsetHeight;
      maxWidth = $cardSizeEl[0].offsetWidth || pageWidth;
      maxHeight = $cardSizeEl[0].offsetHeight || pageHeight;
      statusbarHeight = 0;
      if ($navbarEl && !cardParams.hideStatusbarOnOpen && maxHeight === pageHeight) {
        statusbarHeight = parseInt($navbarEl.css("--f7-safe-area-top"), 10);
        if (Number.isNaN(statusbarHeight))
          statusbarHeight = 0;
      }
      if (statusbarHeight) {
        maxHeight -= statusbarHeight;
      }
      scaleX = maxWidth / cardWidth;
      scaleY = maxHeight / cardHeight;
      $cardEl.transform("translate3d(0px, 0px, 0) scale(1)");
      offset2 = $cardEl.offset();
      pageOffset = $pageEl.offset();
      if (statusbarHeight) {
        pageOffset.top += statusbarHeight / 2;
      }
      offset2.left -= pageOffset.left;
      offset2.top -= pageOffset.top;
      cardLeftOffset = offset2.left - (pageWidth - maxWidth) / 2;
      if (app.rtl)
        cardLeftOffset -= $cardEl[0].scrollLeft;
      cardTopOffset = offset2.top - (pageHeight - maxHeight) / 2;
      cardRightOffset = maxWidth - cardWidth - cardLeftOffset;
      cardBottomOffset = maxHeight - cardHeight - cardTopOffset;
      if (app.rtl) {
        var _ref2 = [cardRightOffset, cardLeftOffset];
        cardLeftOffset = _ref2[0];
        cardRightOffset = _ref2[1];
      }
      translateX = (cardRightOffset - cardLeftOffset) / 2;
      translateY = (cardBottomOffset - cardTopOffset) / 2;
      $cardEl.transform("translate3d(" + (app.rtl ? -translateX : translateX) + "px, " + translateY + "px, 0) scale(" + scaleX + ", " + scaleY + ")");
      $cardContentEl.css({
        width: maxWidth + "px",
        height: maxHeight + "px"
      }).transform("translate3d(" + (app.rtl ? cardLeftOffset + translateX : -cardLeftOffset - translateX) + "px, 0px, 0) scale(" + 1 / scaleX + ", " + 1 / scaleY + ")");
    }
    var cardScrollTop;
    var isTouched;
    var isMoved;
    var touchStartX;
    var touchStartY;
    var touchEndX;
    var touchEndY;
    var isScrolling;
    var progress;
    var isV;
    var isH;
    var $cardScrollableEl;
    function onTouchStart3(e) {
      if (!dom7_default(e.target).closest($cardEl).length)
        return;
      if (!$cardEl.hasClass("card-opened"))
        return;
      $cardScrollableEl = $cardEl.find(cardParams.scrollableEl);
      if ($cardScrollableEl[0] && $cardScrollableEl[0] !== $cardContentEl[0] && !$cardScrollableEl[0].contains(e.target)) {
        cardScrollTop = 0;
      } else {
        cardScrollTop = $cardScrollableEl.scrollTop();
      }
      isTouched = true;
      touchStartX = e.targetTouches[0].pageX;
      touchStartY = e.targetTouches[0].pageY;
      isScrolling = void 0;
      isV = false;
      isH = false;
    }
    function onTouchMove3(e) {
      if (!isTouched)
        return;
      touchEndX = e.targetTouches[0].pageX;
      touchEndY = e.targetTouches[0].pageY;
      if (typeof isScrolling === "undefined") {
        isScrolling = !!(isScrolling || Math.abs(touchEndY - touchStartY) > Math.abs(touchEndX - touchStartX));
      }
      if (!isH && !isV) {
        if (!isScrolling && e.targetTouches[0].clientX <= 50) {
          isH = true;
        } else {
          isV = true;
        }
      }
      if (!(isH || isV) || isV && cardScrollTop !== 0) {
        isTouched = true;
        isMoved = true;
        return;
      }
      if (!isMoved) {
        $cardEl.removeClass("card-transitioning");
      }
      isMoved = true;
      progress = isV ? Math.max((touchEndY - touchStartY) / 150, 0) : Math.max((touchEndX - touchStartX) / (cardWidth / 2), 0);
      if (progress > 0 && isV || isH) {
        if (isV && device2.ios && $cardScrollableEl[0] === $cardContentEl[0]) {
          $cardScrollableEl.css("-webkit-overflow-scrolling", "auto");
          $cardScrollableEl.scrollTop(0);
        }
        e.preventDefault();
      }
      if (progress > 1)
        progress = Math.pow(progress, 0.3);
      if (progress > (isV ? 1.3 : 1.1)) {
        isTouched = false;
        isMoved = false;
        app.card.close($cardEl);
      } else {
        $cardEl.transform("translate3d(" + (app.rtl ? -translateX : translateX) + "px, " + translateY + "px, 0) scale(" + scaleX * (1 - progress * 0.2) + ", " + scaleY * (1 - progress * 0.2) + ")");
      }
    }
    function onTouchEnd3() {
      if (!isTouched || !isMoved)
        return;
      isTouched = false;
      isMoved = false;
      if (device2.ios) {
        $cardScrollableEl.css("-webkit-overflow-scrolling", "");
      }
      if (progress >= 0.8) {
        app.card.close($cardEl);
      } else {
        $cardEl.addClass("card-transitioning").transform("translate3d(" + (app.rtl ? -translateX : translateX) + "px, " + translateY + "px, 0) scale(" + scaleX + ", " + scaleY + ")");
      }
    }
    $cardEl[0].detachEventHandlers = function detachEventHandlers() {
      app.off("resize", onResize2);
      if (support2.touch && cardParams.swipeToClose) {
        app.off("touchstart:passive", onTouchStart3);
        app.off("touchmove:active", onTouchMove3);
        app.off("touchend:passive", onTouchEnd3);
      }
    };
    app.on("resize", onResize2);
    if (support2.touch && cardParams.swipeToClose) {
      app.on("touchstart:passive", onTouchStart3);
      app.on("touchmove:active", onTouchMove3);
      app.on("touchend:passive", onTouchEnd3);
    }
  },
  close: function close6(cardEl, animate) {
    if (cardEl === void 0) {
      cardEl = ".card-expandable.card-opened";
    }
    if (animate === void 0) {
      animate = true;
    }
    var app = this;
    var device2 = getDevice();
    var $cardEl = dom7_default(cardEl).eq(0);
    if (!$cardEl || !$cardEl.length)
      return;
    if (!$cardEl.hasClass("card-opened") || $cardEl.hasClass("card-opening") || $cardEl.hasClass("card-closing"))
      return;
    var $cardContentEl = $cardEl.children(".card-content");
    var $pageContentEl = $cardEl.parents(".page-content");
    var $pageEl = $cardEl.parents(".page").eq(0);
    if (!$pageEl.length)
      return;
    var cardParams = Object.assign({
      animate
    }, app.params.card, $cardEl.dataset());
    var $cardScrollableEl = $cardEl.find(cardParams.scrollableEl);
    var $navbarEl;
    var $toolbarEl;
    var $backdropEl;
    if ($cardEl.attr("data-backdrop-el")) {
      $backdropEl = dom7_default($cardEl.attr("data-backdrop-el"));
    }
    if (cardParams.backdrop) {
      $backdropEl = $cardEl.parents(".page-content").find(".card-backdrop");
    }
    if (cardParams.hideNavbarOnOpen) {
      $navbarEl = $pageEl.children(".navbar");
      if (!$navbarEl.length) {
        if ($pageEl[0].f7Page)
          $navbarEl = $pageEl[0].f7Page.$navbarEl;
      }
      if ($navbarEl && $navbarEl.length && !$cardEl[0].f7KeepNavbarOnClose) {
        app.navbar.show($navbarEl, cardParams.animate, true);
      }
    }
    if (cardParams.hideToolbarOnOpen) {
      $toolbarEl = $pageEl.children(".toolbar");
      if (!$toolbarEl.length) {
        $toolbarEl = $pageEl.parents(".view").children(".toolbar");
      }
      if (!$toolbarEl.length) {
        $toolbarEl = $pageEl.parents(".views").children(".toolbar");
      }
      if ($toolbarEl && $toolbarEl.length && !$cardEl[0].f7KeepToolbarOnClose) {
        app.toolbar.show($toolbarEl, cardParams.animate);
      }
    }
    $pageEl.removeClass("page-with-card-opened");
    if (device2.ios && $pageContentEl.length) {
      $pageContentEl.css("height", $pageContentEl[0].offsetHeight + 1 + "px");
      setTimeout(function() {
        $pageContentEl.css("height", "");
      });
    }
    if ($backdropEl && $backdropEl.length) {
      $backdropEl.removeClass("card-backdrop-in").addClass("card-backdrop-out");
    }
    $cardEl.removeClass("card-opened card-transitioning");
    if (cardParams.animate) {
      $cardEl.addClass("card-closing");
    } else {
      $cardEl.addClass("card-no-transition");
    }
    $cardEl.transform("");
    $cardEl.trigger("card:close");
    app.emit("cardClose", $cardEl[0], $pageEl[0]);
    var animateWidth = $cardEl.hasClass("card-expandable-animate-width");
    function transitionEnd5() {
      if (!animateWidth) {
        $cardContentEl.css({
          width: "",
          height: ""
        });
      }
      if ($backdropEl && $backdropEl.length) {
        $backdropEl.removeClass("card-backdrop-in card-backdrop-out");
      }
      $cardEl.removeClass("card-closing card-no-transition");
      $cardEl.trigger("card:closed");
      $cardEl.find(".card-expandable-size").remove();
      app.emit("cardClosed", $cardEl[0], $pageEl[0]);
    }
    if (animateWidth) {
      $cardContentEl.css({
        width: "",
        height: ""
      });
    }
    $cardContentEl.transform("").scrollTop(0, animate ? 300 : 0);
    if ($cardScrollableEl.length && $cardScrollableEl[0] !== $cardContentEl[0]) {
      $cardScrollableEl.scrollTop(0, animate ? 300 : 0);
    }
    if (animate) {
      $cardContentEl.transitionEnd(function() {
        transitionEnd5();
      });
    } else {
      transitionEnd5();
    }
    if ($cardEl[0].detachEventHandlers) {
      $cardEl[0].detachEventHandlers();
      delete $cardEl[0].detachEventHandlers;
    }
  },
  toggle: function toggle4(cardEl, animate) {
    if (cardEl === void 0) {
      cardEl = ".card-expandable";
    }
    var app = this;
    var $cardEl = dom7_default(cardEl).eq(0);
    if (!$cardEl.length)
      return;
    if ($cardEl.hasClass("card-opened")) {
      app.card.close($cardEl, animate);
    } else {
      app.card.open($cardEl, animate);
    }
  }
};
var card_default = {
  name: "card",
  params: {
    card: {
      hideNavbarOnOpen: true,
      hideStatusbarOnOpen: true,
      hideToolbarOnOpen: true,
      scrollableEl: ".card-content",
      swipeToClose: true,
      closeByBackdropClick: true,
      backdrop: true
    }
  },
  create: function create17() {
    var app = this;
    bindMethods(app, {
      card: CardExpandable
    });
  },
  on: {
    pageBeforeIn: function pageBeforeIn(page) {
      var app = this;
      if (app.params.card.hideNavbarOnOpen && page.navbarEl && page.$el.find(".card-opened.card-expandable").length) {
        app.navbar.hide(page.navbarEl, true, app.params.card.hideStatusbarOnOpen, true);
      }
      if (app.params.card.hideToolbarOnOpen && page.$el.find(".card-opened.card-expandable").length) {
        var $toolbarEl = page.$el.children(".toolbar");
        if (!$toolbarEl.length) {
          $toolbarEl = page.$el.parents(".view").children(".toolbar");
        }
        if (!$toolbarEl.length) {
          $toolbarEl = page.$el.parents(".views").children(".toolbar");
        }
        if ($toolbarEl && $toolbarEl.length) {
          app.toolbar.hide($toolbarEl);
        }
      }
    }
  },
  clicks: {
    ".card-close": function closeCard($clickedEl, data) {
      var app = this;
      app.card.close(data.card, data.animate);
    },
    ".card-open": function closeCard2($clickedEl, data) {
      var app = this;
      app.card.open(data.card, data.animate);
    },
    ".card-expandable": function toggleExpandableCard($clickedEl, data, e) {
      var app = this;
      if ($clickedEl.hasClass("card-opened") || $clickedEl.hasClass("card-opening") || $clickedEl.hasClass("card-closing"))
        return;
      if (dom7_default(e.target).closest(".card-prevent-open, .card-close").length)
        return;
      app.card.open($clickedEl);
    },
    ".card-backdrop-in": function onBackdropClick() {
      var app = this;
      var needToClose = false;
      if (app.params.card.closeByBackdropClick)
        needToClose = true;
      var $openedCardEl = dom7_default(".card-opened");
      if (!$openedCardEl.length)
        return;
      if ($openedCardEl.attr("data-close-by-backdrop-click") === "true") {
        needToClose = true;
      } else if ($openedCardEl.attr("data-close-by-backdrop-click") === "false") {
        needToClose = false;
      }
      if (needToClose)
        app.card.close($openedCardEl);
    }
  }
};

// node_modules/framework7/esm/components/chip/chip.js
var chip_default = {
  name: "chip"
};

// node_modules/framework7/esm/components/form/form.js
var FormData = {
  store: function store(form, data) {
    var app = this;
    var window2 = getWindow();
    var formId = form;
    var $formEl = dom7_default(form);
    if ($formEl.length && $formEl.is("form") && $formEl.attr("id")) {
      formId = $formEl.attr("id");
    }
    app.form.data["form-" + formId] = data;
    window2.localStorage["f7form-" + formId] = JSON.stringify(data);
  },
  get: function get(form) {
    var app = this;
    var window2 = getWindow();
    var formId = form;
    var $formEl = dom7_default(form);
    if ($formEl.length && $formEl.is("form") && $formEl.attr("id")) {
      formId = $formEl.attr("id");
    }
    if (window2.localStorage["f7form-" + formId]) {
      return JSON.parse(window2.localStorage["f7form-" + formId]);
    }
    if (app.form.data["form-" + formId]) {
      return app.form.data["form-" + formId];
    }
    return void 0;
  },
  remove: function remove2(form) {
    var app = this;
    var window2 = getWindow();
    var formId = form;
    var $formEl = dom7_default(form);
    if ($formEl.length && $formEl.is("form") && $formEl.attr("id")) {
      formId = $formEl.attr("id");
    }
    if (app.form.data["form-" + formId]) {
      app.form.data["form-" + formId] = "";
      delete app.form.data["form-" + formId];
    }
    if (window2.localStorage["f7form-" + formId]) {
      window2.localStorage["f7form-" + formId] = "";
      window2.localStorage.removeItem("f7form-" + formId);
    }
  }
};
var FormStorage = {
  init: function init7(formEl) {
    var app = this;
    var $formEl = dom7_default(formEl);
    var formId = $formEl.attr("id");
    if (!formId)
      return;
    var initialData = app.form.getFormData(formId);
    if (initialData) {
      app.form.fillFromData($formEl, initialData);
    }
    function store2() {
      var data = app.form.convertToData($formEl);
      if (!data)
        return;
      app.form.storeFormData(formId, data);
      $formEl.trigger("form:storedata", data);
      app.emit("formStoreData", $formEl[0], data);
    }
    $formEl.on("change submit", store2);
  },
  destroy: function destroy3(formEl) {
    var $formEl = dom7_default(formEl);
    $formEl.off("change submit");
  }
};
function formToData(formEl) {
  var app = this;
  var $formEl = dom7_default(formEl).eq(0);
  if ($formEl.length === 0)
    return void 0;
  var data = {};
  var skipTypes = ["submit", "image", "button", "file"];
  var skipNames = [];
  $formEl.find("input, select, textarea").each(function(inputEl) {
    var $inputEl = dom7_default(inputEl);
    if ($inputEl.hasClass("ignore-store-data") || $inputEl.hasClass("no-store-data")) {
      return;
    }
    var name = $inputEl.attr("name");
    var type = $inputEl.attr("type");
    var tag = inputEl.nodeName.toLowerCase();
    if (skipTypes.indexOf(type) >= 0)
      return;
    if (skipNames.indexOf(name) >= 0 || !name)
      return;
    if (tag === "select" && $inputEl.prop("multiple")) {
      skipNames.push(name);
      data[name] = [];
      $formEl.find('select[name="' + name + '"] option').each(function(el) {
        if (el.selected)
          data[name].push(el.value);
      });
    } else {
      switch (type) {
        case "checkbox":
          skipNames.push(name);
          data[name] = [];
          $formEl.find('input[name="' + name + '"]').each(function(el) {
            if (el.checked)
              data[name].push(el.value);
          });
          break;
        case "radio":
          skipNames.push(name);
          $formEl.find('input[name="' + name + '"]').each(function(el) {
            if (el.checked)
              data[name] = el.value;
          });
          break;
        default:
          data[name] = $inputEl.val();
          break;
      }
    }
  });
  $formEl.trigger("form:todata", data);
  app.emit("formToData", $formEl[0], data);
  return data;
}
function formFromData(formEl, formData) {
  var app = this;
  var $formEl = dom7_default(formEl).eq(0);
  if (!$formEl.length)
    return;
  var data = formData;
  var formId = $formEl.attr("id");
  if (!data && formId) {
    data = app.form.getFormData(formId);
  }
  if (!data)
    return;
  var skipTypes = ["submit", "image", "button", "file"];
  var skipNames = [];
  $formEl.find("input, select, textarea").each(function(inputEl) {
    var $inputEl = dom7_default(inputEl);
    if ($inputEl.hasClass("ignore-store-data") || $inputEl.hasClass("no-store-data")) {
      return;
    }
    var name = $inputEl.attr("name");
    var type = $inputEl.attr("type");
    var tag = inputEl.nodeName.toLowerCase();
    if (typeof data[name] === "undefined" || data[name] === null)
      return;
    if (skipTypes.indexOf(type) >= 0)
      return;
    if (skipNames.indexOf(name) >= 0 || !name)
      return;
    if (tag === "select" && $inputEl.prop("multiple")) {
      skipNames.push(name);
      $formEl.find('select[name="' + name + '"] option').each(function(el) {
        var selectEl = el;
        if (data[name].indexOf(el.value) >= 0)
          selectEl.selected = true;
        else
          selectEl.selected = false;
      });
    } else {
      switch (type) {
        case "checkbox":
          skipNames.push(name);
          $formEl.find('input[name="' + name + '"]').each(function(el) {
            var checkboxEl = el;
            if (data[name].indexOf(el.value) >= 0)
              checkboxEl.checked = true;
            else
              checkboxEl.checked = false;
          });
          break;
        case "radio":
          skipNames.push(name);
          $formEl.find('input[name="' + name + '"]').each(function(el) {
            var radioEl = el;
            if (data[name] === el.value)
              radioEl.checked = true;
            else
              radioEl.checked = false;
          });
          break;
        default:
          $inputEl.val(data[name]);
          break;
      }
    }
    if (tag === "select" || tag === "input" || tag === "textarea") {
      $inputEl.trigger("change", "fromdata");
    }
  });
  $formEl.trigger("form:fromdata", data);
  app.emit("formFromData", $formEl[0], data);
}
function initAjaxForm() {
  var app = this;
  var window2 = getWindow();
  var document = getDocument();
  function onSubmitChange(e, fromData) {
    var $formEl = dom7_default(this);
    if (e.type === "change" && !$formEl.hasClass("form-ajax-submit-onchange"))
      return;
    if (e.type === "submit")
      e.preventDefault();
    if (e.type === "change" && fromData === "fromdata")
      return;
    var method = ($formEl.attr("method") || "GET").toUpperCase();
    var contentType = $formEl.prop("enctype") || $formEl.attr("enctype");
    var url = $formEl.attr("action");
    if (!url)
      return;
    var data;
    if (method === "POST") {
      if (contentType === "application/x-www-form-urlencoded") {
        data = app.form.convertToData($formEl[0]);
      } else {
        data = new window2.FormData($formEl[0]);
      }
    } else {
      data = serializeObject(app.form.convertToData($formEl[0]));
    }
    app.request({
      method,
      url,
      contentType,
      data,
      beforeSend: function beforeSend(xhr) {
        $formEl.trigger("formajax:beforesend", {
          data,
          xhr
        });
        app.emit("formAjaxBeforeSend", $formEl[0], data, xhr);
      },
      error: function error(xhr) {
        $formEl.trigger("formajax:error", {
          data,
          xhr
        });
        app.emit("formAjaxError", $formEl[0], data, xhr);
      },
      complete: function complete(xhr) {
        $formEl.trigger("formajax:complete", {
          data,
          xhr
        });
        app.emit("formAjaxComplete", $formEl[0], data, xhr);
      },
      success: function success(response, status, xhr) {
        $formEl.trigger("formajax:success", {
          data,
          xhr
        });
        app.emit("formAjaxSuccess", $formEl[0], data, xhr);
      }
    });
  }
  dom7_default(document).on("submit change", "form.form-ajax-submit, form.form-ajax-submit-onchange", onSubmitChange);
}
var form_default = {
  name: "form",
  create: function create18() {
    var app = this;
    extend(app, {
      form: {
        data: {},
        storeFormData: FormData.store.bind(app),
        getFormData: FormData.get.bind(app),
        removeFormData: FormData.remove.bind(app),
        convertToData: formToData.bind(app),
        fillFromData: formFromData.bind(app),
        storage: {
          init: FormStorage.init.bind(app),
          destroy: FormStorage.destroy.bind(app)
        }
      }
    });
  },
  on: {
    init: function init8() {
      var app = this;
      initAjaxForm.call(app);
    },
    tabBeforeRemove: function tabBeforeRemove2(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".form-store-data").each(function(formEl) {
        app.form.storage.destroy(formEl);
      });
    },
    tabMounted: function tabMounted4(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".form-store-data").each(function(formEl) {
        app.form.storage.init(formEl);
      });
    },
    pageBeforeRemove: function pageBeforeRemove3(page) {
      var app = this;
      page.$el.find(".form-store-data").each(function(formEl) {
        app.form.storage.destroy(formEl);
      });
    },
    pageInit: function pageInit5(page) {
      var app = this;
      page.$el.find(".form-store-data").each(function(formEl) {
        app.form.storage.init(formEl);
      });
    }
  }
};

// node_modules/framework7/esm/components/input/input.js
var Input = {
  ignoreTypes: ["checkbox", "button", "submit", "range", "radio", "image"],
  createTextareaResizableShadow: function createTextareaResizableShadow() {
    var document = getDocument();
    var $shadowEl = dom7_default(document.createElement("textarea"));
    $shadowEl.addClass("textarea-resizable-shadow");
    $shadowEl.prop({
      disabled: true,
      readonly: true
    });
    Input.textareaResizableShadow = $shadowEl;
  },
  textareaResizableShadow: void 0,
  resizeTextarea: function resizeTextarea(textareaEl) {
    var app = this;
    var window2 = getWindow();
    var $textareaEl = dom7_default(textareaEl);
    if (!Input.textareaResizableShadow) {
      Input.createTextareaResizableShadow();
    }
    var $shadowEl = Input.textareaResizableShadow;
    if (!$textareaEl.length)
      return;
    if (!$textareaEl.hasClass("resizable"))
      return;
    if (Input.textareaResizableShadow.parents().length === 0) {
      app.$el.append($shadowEl);
    }
    var styles2 = window2.getComputedStyle($textareaEl[0]);
    "padding-top padding-bottom padding-left padding-right margin-left margin-right margin-top margin-bottom width font-size font-family font-style font-weight line-height font-variant text-transform letter-spacing border box-sizing display".split(" ").forEach(function(style) {
      var styleValue = styles2[style];
      if ("font-size line-height letter-spacing width".split(" ").indexOf(style) >= 0) {
        styleValue = styleValue.replace(",", ".");
      }
      $shadowEl.css(style, styleValue);
    });
    var currentHeight = $textareaEl[0].clientHeight;
    $shadowEl.val("");
    var initialHeight = $shadowEl[0].scrollHeight;
    $shadowEl.val($textareaEl.val());
    $shadowEl.css("height", 0);
    var scrollHeight = $shadowEl[0].scrollHeight;
    if (currentHeight !== scrollHeight) {
      if (scrollHeight > initialHeight) {
        $textareaEl.css("height", scrollHeight + "px");
      } else if (scrollHeight < currentHeight) {
        $textareaEl.css("height", "");
      }
      if (scrollHeight > initialHeight || scrollHeight < currentHeight) {
        $textareaEl.trigger("textarea:resize", {
          initialHeight,
          currentHeight,
          scrollHeight
        });
        app.emit("textareaResize", {
          initialHeight,
          currentHeight,
          scrollHeight
        });
      }
    }
  },
  validate: function validate(inputEl) {
    var $inputEl = dom7_default(inputEl);
    if (!$inputEl.length)
      return true;
    var $itemInputEl = $inputEl.parents(".item-input");
    var $inputWrapEl = $inputEl.parents(".input");
    function unsetReadonly() {
      if ($inputEl[0].f7ValidateReadonly) {
        $inputEl[0].readOnly = false;
      }
    }
    function setReadonly() {
      if ($inputEl[0].f7ValidateReadonly) {
        $inputEl[0].readOnly = true;
      }
    }
    unsetReadonly();
    var validity = $inputEl[0].validity;
    var validationMessage = $inputEl.dataset().errorMessage || $inputEl[0].validationMessage || "";
    if (!validity) {
      setReadonly();
      return true;
    }
    if (!validity.valid) {
      var $errorEl = $inputEl.nextAll(".item-input-error-message, .input-error-message");
      if (validationMessage) {
        if ($errorEl.length === 0) {
          $errorEl = dom7_default('<div class="' + ($inputWrapEl.length ? "input-error-message" : "item-input-error-message") + '"></div>');
          $errorEl.insertAfter($inputEl);
        }
        $errorEl.text(validationMessage);
      }
      if ($errorEl.length > 0) {
        $itemInputEl.addClass("item-input-with-error-message");
        $inputWrapEl.addClass("input-with-error-message");
      }
      $itemInputEl.addClass("item-input-invalid");
      $inputWrapEl.addClass("input-invalid");
      $inputEl.addClass("input-invalid");
      setReadonly();
      return false;
    }
    $itemInputEl.removeClass("item-input-invalid item-input-with-error-message");
    $inputWrapEl.removeClass("input-invalid input-with-error-message");
    $inputEl.removeClass("input-invalid");
    setReadonly();
    return true;
  },
  validateInputs: function validateInputs(el) {
    var app = this;
    var validates = dom7_default(el).find("input, textarea, select").map(function(inputEl) {
      return app.input.validate(inputEl);
    });
    return validates.indexOf(false) < 0;
  },
  focus: function focus(inputEl) {
    var $inputEl = dom7_default(inputEl);
    var type = $inputEl.attr("type");
    if (Input.ignoreTypes.indexOf(type) >= 0)
      return;
    $inputEl.parents(".item-input").addClass("item-input-focused");
    $inputEl.parents(".input").addClass("input-focused");
    $inputEl.addClass("input-focused");
  },
  blur: function blur(inputEl) {
    var $inputEl = dom7_default(inputEl);
    $inputEl.parents(".item-input").removeClass("item-input-focused");
    $inputEl.parents(".input").removeClass("input-focused");
    $inputEl.removeClass("input-focused");
  },
  checkEmptyState: function checkEmptyState(inputEl) {
    var app = this;
    var $inputEl = dom7_default(inputEl);
    if (!$inputEl.is("input, select, textarea, .item-input [contenteditable]")) {
      $inputEl = $inputEl.find("input, select, textarea, .item-input [contenteditable]").eq(0);
    }
    if (!$inputEl.length)
      return;
    var isContentEditable = $inputEl[0].hasAttribute("contenteditable");
    var value;
    if (isContentEditable) {
      if ($inputEl.find(".text-editor-placeholder").length)
        value = "";
      else
        value = $inputEl.html();
    } else {
      value = $inputEl.val();
    }
    var $itemInputEl = $inputEl.parents(".item-input");
    var $inputWrapEl = $inputEl.parents(".input");
    if (value && typeof value === "string" && value.trim() !== "" || Array.isArray(value) && value.length > 0) {
      $itemInputEl.addClass("item-input-with-value");
      $inputWrapEl.addClass("input-with-value");
      $inputEl.addClass("input-with-value");
      $inputEl.trigger("input:notempty");
      app.emit("inputNotEmpty", $inputEl[0]);
    } else {
      $itemInputEl.removeClass("item-input-with-value");
      $inputWrapEl.removeClass("input-with-value");
      $inputEl.removeClass("input-with-value");
      $inputEl.trigger("input:empty");
      app.emit("inputEmpty", $inputEl[0]);
    }
  },
  scrollIntoView: function scrollIntoView(inputEl, duration, centered, force) {
    if (duration === void 0) {
      duration = 0;
    }
    var $inputEl = dom7_default(inputEl);
    var $scrollableEl = $inputEl.parents(".page-content, .panel, .card-expandable .card-content").eq(0);
    if (!$scrollableEl.length) {
      return false;
    }
    var contentHeight = $scrollableEl[0].offsetHeight;
    var contentScrollTop = $scrollableEl[0].scrollTop;
    var contentPaddingTop = parseInt($scrollableEl.css("padding-top"), 10);
    var contentPaddingBottom = parseInt($scrollableEl.css("padding-bottom"), 10);
    var contentOffsetTop = $scrollableEl.offset().top - contentScrollTop;
    var inputOffsetTop = $inputEl.offset().top - contentOffsetTop;
    var inputHeight = $inputEl[0].offsetHeight;
    var min = inputOffsetTop + contentScrollTop - contentPaddingTop;
    var max = inputOffsetTop + contentScrollTop - contentHeight + contentPaddingBottom + inputHeight;
    var centeredPosition = min + (max - min) / 2;
    if (contentScrollTop > min) {
      $scrollableEl.scrollTop(centered ? centeredPosition : min, duration);
      return true;
    }
    if (contentScrollTop < max) {
      $scrollableEl.scrollTop(centered ? centeredPosition : max, duration);
      return true;
    }
    if (force) {
      $scrollableEl.scrollTop(centered ? centeredPosition : max, duration);
    }
    return false;
  },
  init: function init9() {
    var app = this;
    var device2 = getDevice();
    var window2 = getWindow();
    var document = getDocument();
    Input.createTextareaResizableShadow();
    function onFocus() {
      var inputEl = this;
      if (app.params.input.scrollIntoViewOnFocus) {
        if (device2.android) {
          dom7_default(window2).once("resize", function() {
            if (document && document.activeElement === inputEl) {
              app.input.scrollIntoView(inputEl, app.params.input.scrollIntoViewDuration, app.params.input.scrollIntoViewCentered, app.params.input.scrollIntoViewAlways);
            }
          });
        } else {
          app.input.scrollIntoView(inputEl, app.params.input.scrollIntoViewDuration, app.params.input.scrollIntoViewCentered, app.params.input.scrollIntoViewAlways);
        }
      }
      app.input.focus(inputEl);
    }
    function onBlur() {
      var $inputEl = dom7_default(this);
      var tag = $inputEl[0].nodeName.toLowerCase();
      app.input.blur($inputEl);
      if ($inputEl.dataset().validate || $inputEl.attr("validate") !== null || $inputEl.attr("data-validate-on-blur") !== null) {
        app.input.validate($inputEl);
      }
      if (tag === "textarea" && $inputEl.hasClass("resizable")) {
        if (Input.textareaResizableShadow)
          Input.textareaResizableShadow.remove();
      }
    }
    function onChange() {
      var $inputEl = dom7_default(this);
      var type = $inputEl.attr("type");
      var tag = $inputEl[0].nodeName.toLowerCase();
      var isContentEditable = $inputEl[0].hasAttribute("contenteditable");
      if (Input.ignoreTypes.indexOf(type) >= 0)
        return;
      app.input.checkEmptyState($inputEl);
      if (isContentEditable)
        return;
      if ($inputEl.attr("data-validate-on-blur") === null && ($inputEl.dataset().validate || $inputEl.attr("validate") !== null)) {
        app.input.validate($inputEl);
      }
      if (tag === "textarea" && $inputEl.hasClass("resizable")) {
        app.input.resizeTextarea($inputEl);
      }
    }
    function onInvalid(e) {
      var $inputEl = dom7_default(this);
      if ($inputEl.attr("data-validate-on-blur") === null && ($inputEl.dataset().validate || $inputEl.attr("validate") !== null)) {
        e.preventDefault();
        app.input.validate($inputEl);
      }
    }
    function clearInput() {
      var $clicked = dom7_default(this);
      var $inputEl = $clicked.siblings("input, textarea").eq(0);
      var previousValue = $inputEl.val();
      $inputEl.val("").trigger("input change").focus().trigger("input:clear", previousValue);
      app.emit("inputClear", previousValue);
    }
    function preventDefault(e) {
      e.preventDefault();
    }
    dom7_default(document).on("click", ".input-clear-button", clearInput);
    dom7_default(document).on("mousedown", ".input-clear-button", preventDefault);
    dom7_default(document).on("change input", "input, textarea, select, .item-input [contenteditable]", onChange, true);
    dom7_default(document).on("focus", "input, textarea, select, .item-input [contenteditable]", onFocus, true);
    dom7_default(document).on("blur", "input, textarea, select, .item-input [contenteditable]", onBlur, true);
    dom7_default(document).on("invalid", "input, textarea, select", onInvalid, true);
  }
};
var input_default = {
  name: "input",
  params: {
    input: {
      scrollIntoViewOnFocus: void 0,
      scrollIntoViewCentered: false,
      scrollIntoViewDuration: 0,
      scrollIntoViewAlways: false
    }
  },
  create: function create19() {
    var app = this;
    if (typeof app.params.input.scrollIntoViewOnFocus === "undefined") {
      app.params.input.scrollIntoViewOnFocus = getDevice().android;
    }
    bindMethods(app, {
      input: Input
    });
  },
  on: {
    init: function init10() {
      var app = this;
      app.input.init();
    },
    tabMounted: function tabMounted5(tabEl) {
      var app = this;
      var $tabEl = dom7_default(tabEl);
      $tabEl.find(".item-input, .input").each(function(itemInputEl) {
        var $itemInputEl = dom7_default(itemInputEl);
        $itemInputEl.find("input, select, textarea, [contenteditable]").each(function(inputEl) {
          var $inputEl = dom7_default(inputEl);
          if (Input.ignoreTypes.indexOf($inputEl.attr("type")) >= 0)
            return;
          app.input.checkEmptyState($inputEl);
        });
      });
      $tabEl.find("textarea.resizable").each(function(textareaEl) {
        app.input.resizeTextarea(textareaEl);
      });
    },
    pageInit: function pageInit6(page) {
      var app = this;
      var $pageEl = page.$el;
      $pageEl.find(".item-input, .input").each(function(itemInputEl) {
        var $itemInputEl = dom7_default(itemInputEl);
        $itemInputEl.find("input, select, textarea, [contenteditable]").each(function(inputEl) {
          var $inputEl = dom7_default(inputEl);
          if (Input.ignoreTypes.indexOf($inputEl.attr("type")) >= 0)
            return;
          app.input.checkEmptyState($inputEl);
        });
      });
      $pageEl.find("textarea.resizable").each(function(textareaEl) {
        app.input.resizeTextarea(textareaEl);
      });
    },
    "panelBreakpoint panelCollapsedBreakpoint panelResize panelOpen panelSwipeOpen resize viewMasterDetailBreakpoint": function onPanelOpen(instance) {
      var app = this;
      if (instance && instance.$el) {
        instance.$el.find("textarea.resizable").each(function(textareaEl) {
          app.input.resizeTextarea(textareaEl);
        });
      } else {
        dom7_default("textarea.resizable").each(function(textareaEl) {
          app.input.resizeTextarea(textareaEl);
        });
      }
    }
  }
};

// node_modules/framework7/esm/components/checkbox/checkbox.js
var checkbox_default = {
  name: "checkbox"
};

// node_modules/framework7/esm/components/radio/radio.js
var radio_default = {
  name: "radio"
};

// node_modules/framework7/esm/components/toggle/toggle-class.js
function _assertThisInitialized11(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose11(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf11(subClass, superClass);
}
function _setPrototypeOf11(o, p) {
  _setPrototypeOf11 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf11(o, p);
}
var Toggle = function(_Framework7Class) {
  _inheritsLoose11(Toggle2, _Framework7Class);
  function Toggle2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var toggle11 = _assertThisInitialized11(_this);
    var support2 = getSupport();
    var defaults = {};
    toggle11.useModulesParams(defaults);
    toggle11.params = extend(defaults, params);
    var el = toggle11.params.el;
    if (!el)
      return toggle11 || _assertThisInitialized11(_this);
    var $el = dom7_default(el);
    if ($el.length === 0)
      return toggle11 || _assertThisInitialized11(_this);
    if ($el[0].f7Toggle)
      return $el[0].f7Toggle || _assertThisInitialized11(_this);
    var $inputEl = $el.children('input[type="checkbox"]');
    extend(toggle11, {
      app,
      $el,
      el: $el[0],
      $inputEl,
      inputEl: $inputEl[0],
      disabled: $el.hasClass("disabled") || $inputEl.hasClass("disabled") || $inputEl.attr("disabled") || $inputEl[0].disabled
    });
    Object.defineProperty(toggle11, "checked", {
      enumerable: true,
      configurable: true,
      set: function set2(checked) {
        if (!toggle11 || typeof toggle11.$inputEl === "undefined")
          return;
        if (toggle11.checked === checked)
          return;
        $inputEl[0].checked = checked;
        toggle11.$inputEl.trigger("change");
      },
      get: function get2() {
        return $inputEl[0].checked;
      }
    });
    $el[0].f7Toggle = toggle11;
    var isTouched;
    var touchesStart = {};
    var isScrolling;
    var touchesDiff;
    var toggleWidth;
    var touchStartTime;
    var touchStartChecked;
    function handleTouchStart(e) {
      if (isTouched || toggle11.disabled)
        return;
      touchesStart.x = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
      touchesStart.y = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
      touchesDiff = 0;
      isTouched = true;
      isScrolling = void 0;
      touchStartTime = now();
      touchStartChecked = toggle11.checked;
      toggleWidth = $el[0].offsetWidth;
      nextTick(function() {
        if (isTouched) {
          $el.addClass("toggle-active-state");
        }
      });
    }
    function handleTouchMove(e) {
      if (!isTouched || toggle11.disabled)
        return;
      var pageX = e.type === "touchmove" ? e.targetTouches[0].pageX : e.pageX;
      var pageY = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
      var inverter = app.rtl ? -1 : 1;
      if (typeof isScrolling === "undefined") {
        isScrolling = !!(isScrolling || Math.abs(pageY - touchesStart.y) > Math.abs(pageX - touchesStart.x));
      }
      if (isScrolling) {
        isTouched = false;
        return;
      }
      e.preventDefault();
      touchesDiff = pageX - touchesStart.x;
      var changed;
      if (touchesDiff * inverter < 0 && Math.abs(touchesDiff) > toggleWidth / 3 && touchStartChecked) {
        changed = true;
      }
      if (touchesDiff * inverter > 0 && Math.abs(touchesDiff) > toggleWidth / 3 && !touchStartChecked) {
        changed = true;
      }
      if (changed) {
        touchesStart.x = pageX;
        toggle11.checked = !touchStartChecked;
        touchStartChecked = !touchStartChecked;
      }
    }
    function handleTouchEnd() {
      if (!isTouched || toggle11.disabled) {
        if (isScrolling)
          $el.removeClass("toggle-active-state");
        isTouched = false;
        return;
      }
      var inverter = app.rtl ? -1 : 1;
      isTouched = false;
      $el.removeClass("toggle-active-state");
      var changed;
      if (now() - touchStartTime < 300) {
        if (touchesDiff * inverter < 0 && touchStartChecked) {
          changed = true;
        }
        if (touchesDiff * inverter > 0 && !touchStartChecked) {
          changed = true;
        }
        if (changed) {
          toggle11.checked = !touchStartChecked;
        }
      }
    }
    function handleInputChange() {
      toggle11.$el.trigger("toggle:change");
      toggle11.emit("local::change toggleChange", toggle11);
    }
    toggle11.attachEvents = function attachEvents2() {
      var passive = support2.passiveListener ? {
        passive: true
      } : false;
      $el.on(app.touchEvents.start, handleTouchStart, passive);
      app.on("touchmove", handleTouchMove);
      app.on("touchend:passive", handleTouchEnd);
      toggle11.$inputEl.on("change", handleInputChange);
    };
    toggle11.detachEvents = function detachEvents2() {
      var passive = support2.passiveListener ? {
        passive: true
      } : false;
      $el.off(app.touchEvents.start, handleTouchStart, passive);
      app.off("touchmove", handleTouchMove);
      app.off("touchend:passive", handleTouchEnd);
      toggle11.$inputEl.off("change", handleInputChange);
    };
    toggle11.useModules();
    toggle11.init();
    return _this;
  }
  var _proto = Toggle2.prototype;
  _proto.toggle = function toggle11() {
    var toggle12 = this;
    toggle12.checked = !toggle12.checked;
  };
  _proto.init = function init46() {
    var toggle11 = this;
    toggle11.attachEvents();
  };
  _proto.destroy = function destroy50() {
    var toggle11 = this;
    toggle11.$el.trigger("toggle:beforedestroy");
    toggle11.emit("local::beforeDestroy toggleBeforeDestroy", toggle11);
    delete toggle11.$el[0].f7Toggle;
    toggle11.detachEvents();
    deleteProps(toggle11);
    toggle11 = null;
  };
  return Toggle2;
}(class_default);
var toggle_class_default = Toggle;

// node_modules/framework7/esm/components/toggle/toggle.js
var toggle_default = {
  name: "toggle",
  create: function create20() {
    var app = this;
    app.toggle = ConstructorMethods({
      defaultSelector: ".toggle",
      constructor: toggle_class_default,
      app,
      domProp: "f7Toggle"
    });
  },
  static: {
    Toggle: toggle_class_default
  },
  on: {
    tabMounted: function tabMounted6(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".toggle-init").each(function(toggleEl2) {
        return app.toggle.create({
          el: toggleEl2
        });
      });
    },
    tabBeforeRemove: function tabBeforeRemove3(tabEl) {
      dom7_default(tabEl).find(".toggle-init").each(function(toggleEl2) {
        if (toggleEl2.f7Toggle)
          toggleEl2.f7Toggle.destroy();
      });
    },
    pageInit: function pageInit7(page) {
      var app = this;
      page.$el.find(".toggle-init").each(function(toggleEl2) {
        return app.toggle.create({
          el: toggleEl2
        });
      });
    },
    pageBeforeRemove: function pageBeforeRemove4(page) {
      page.$el.find(".toggle-init").each(function(toggleEl2) {
        if (toggleEl2.f7Toggle)
          toggleEl2.f7Toggle.destroy();
      });
    }
  },
  vnode: {
    "toggle-init": {
      insert: function insert5(vnode) {
        var app = this;
        var toggleEl2 = vnode.elm;
        app.toggle.create({
          el: toggleEl2
        });
      },
      destroy: function destroy4(vnode) {
        var toggleEl2 = vnode.elm;
        if (toggleEl2.f7Toggle)
          toggleEl2.f7Toggle.destroy();
      }
    }
  }
};

// node_modules/framework7/esm/components/range/range-class.js
function _assertThisInitialized12(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose12(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf12(subClass, superClass);
}
function _setPrototypeOf12(o, p) {
  _setPrototypeOf12 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf12(o, p);
}
var Range = function(_Framework7Class) {
  _inheritsLoose12(Range2, _Framework7Class);
  function Range2(app, params) {
    var _this;
    _this = _Framework7Class.call(this, params, [app]) || this;
    var range = _assertThisInitialized12(_this);
    var support2 = getSupport();
    var defaults = {
      el: null,
      inputEl: null,
      dual: false,
      step: 1,
      label: false,
      min: 0,
      max: 100,
      value: 0,
      draggableBar: true,
      vertical: false,
      verticalReversed: false,
      formatLabel: null,
      scale: false,
      scaleSteps: 5,
      scaleSubSteps: 0,
      formatScaleLabel: null,
      limitKnobPosition: app.theme === "ios"
    };
    range.useModulesParams(defaults);
    range.params = extend(defaults, params);
    var el = range.params.el;
    if (!el)
      return range || _assertThisInitialized12(_this);
    var $el = dom7_default(el);
    if ($el.length === 0)
      return range || _assertThisInitialized12(_this);
    if ($el[0].f7Range)
      return $el[0].f7Range || _assertThisInitialized12(_this);
    var dataset = $el.dataset();
    "step min max value scaleSteps scaleSubSteps".split(" ").forEach(function(paramName) {
      if (typeof params[paramName] === "undefined" && typeof dataset[paramName] !== "undefined") {
        range.params[paramName] = parseFloat(dataset[paramName]);
      }
    });
    "dual label vertical verticalReversed scale".split(" ").forEach(function(paramName) {
      if (typeof params[paramName] === "undefined" && typeof dataset[paramName] !== "undefined") {
        range.params[paramName] = dataset[paramName];
      }
    });
    if (!range.params.value) {
      if (typeof dataset.value !== "undefined")
        range.params.value = dataset.value;
      if (typeof dataset.valueLeft !== "undefined" && typeof dataset.valueRight !== "undefined") {
        range.params.value = [parseFloat(dataset.valueLeft), parseFloat(dataset.valueRight)];
      }
    }
    var $inputEl;
    if (!range.params.dual) {
      if (range.params.inputEl) {
        $inputEl = dom7_default(range.params.inputEl);
      } else if ($el.find('input[type="range"]').length) {
        $inputEl = $el.find('input[type="range"]').eq(0);
      }
    }
    var _range$params = range.params, dual = _range$params.dual, step = _range$params.step, label = _range$params.label, min = _range$params.min, max = _range$params.max, value = _range$params.value, vertical = _range$params.vertical, verticalReversed = _range$params.verticalReversed, scale = _range$params.scale, scaleSteps = _range$params.scaleSteps, scaleSubSteps = _range$params.scaleSubSteps, limitKnobPosition = _range$params.limitKnobPosition;
    extend(range, {
      app,
      $el,
      el: $el[0],
      $inputEl,
      inputEl: $inputEl ? $inputEl[0] : void 0,
      dual,
      step,
      label,
      min,
      max,
      value,
      previousValue: value,
      vertical,
      verticalReversed,
      scale,
      scaleSteps,
      scaleSubSteps,
      limitKnobPosition
    });
    if ($inputEl) {
      "step min max".split(" ").forEach(function(paramName) {
        if (!params[paramName] && $inputEl.attr(paramName)) {
          range.params[paramName] = parseFloat($inputEl.attr(paramName));
          range[paramName] = parseFloat($inputEl.attr(paramName));
        }
      });
      if (typeof $inputEl.val() !== "undefined") {
        range.params.value = parseFloat($inputEl.val());
        range.value = parseFloat($inputEl.val());
      }
    }
    if (range.dual) {
      $el.addClass("range-slider-dual");
    }
    if (range.label) {
      $el.addClass("range-slider-label");
    }
    if (range.vertical) {
      $el.addClass("range-slider-vertical");
      if (range.verticalReversed) {
        $el.addClass("range-slider-vertical-reversed");
      }
    } else {
      $el.addClass("range-slider-horizontal");
    }
    var $barEl = dom7_default('<div class="range-bar"></div>');
    var $barActiveEl = dom7_default('<div class="range-bar-active"></div>');
    $barEl.append($barActiveEl);
    var knobHTML = '\n      <div class="range-knob-wrap">\n        <div class="range-knob"></div>\n        ' + (range.label ? '<div class="range-knob-label"></div>' : "") + "\n      </div>\n    ";
    var knobs = [dom7_default(knobHTML)];
    if (range.dual) {
      knobs.push(dom7_default(knobHTML));
    }
    $el.append($barEl);
    knobs.forEach(function($knobEl) {
      $el.append($knobEl);
    });
    var labels = [];
    if (range.label) {
      labels.push(knobs[0].find(".range-knob-label"));
      if (range.dual) {
        labels.push(knobs[1].find(".range-knob-label"));
      }
    }
    var $scaleEl;
    if (range.scale && range.scaleSteps >= 1) {
      $scaleEl = dom7_default('\n        <div class="range-scale">\n          ' + range.renderScale() + "\n        </div>\n      ");
      $el.append($scaleEl);
    }
    extend(range, {
      knobs,
      labels,
      $barEl,
      $barActiveEl,
      $scaleEl
    });
    $el[0].f7Range = range;
    var isTouched;
    var touchesStart = {};
    var isScrolling;
    var rangeOffset;
    var rangeOffsetLeft;
    var rangeOffsetTop;
    var $touchedKnobEl;
    var dualValueIndex;
    var valueChangedByTouch;
    var targetTouchIdentifier;
    function onTouchChange() {
      valueChangedByTouch = true;
    }
    function handleTouchStart(e) {
      if (isTouched)
        return;
      if (!range.params.draggableBar) {
        if (dom7_default(e.target).closest(".range-knob").length === 0) {
          return;
        }
      }
      valueChangedByTouch = false;
      touchesStart.x = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
      touchesStart.y = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
      if (e.type === "touchstart") {
        targetTouchIdentifier = e.targetTouches[0].identifier;
      }
      isTouched = true;
      isScrolling = void 0;
      rangeOffset = $el.offset();
      rangeOffsetLeft = rangeOffset.left;
      rangeOffsetTop = rangeOffset.top;
      var progress;
      if (range.vertical) {
        progress = (touchesStart.y - rangeOffsetTop) / range.rangeHeight;
        if (!range.verticalReversed)
          progress = 1 - progress;
      } else if (range.app.rtl) {
        progress = (rangeOffsetLeft + range.rangeWidth - touchesStart.x) / range.rangeWidth;
      } else {
        progress = (touchesStart.x - rangeOffsetLeft) / range.rangeWidth;
      }
      var newValue = progress * (range.max - range.min) + range.min;
      if (range.dual) {
        if (Math.abs(range.value[0] - newValue) < Math.abs(range.value[1] - newValue)) {
          dualValueIndex = 0;
          $touchedKnobEl = range.knobs[0];
          newValue = [newValue, range.value[1]];
        } else {
          dualValueIndex = 1;
          $touchedKnobEl = range.knobs[1];
          newValue = [range.value[0], newValue];
        }
      } else {
        $touchedKnobEl = range.knobs[0];
        newValue = progress * (range.max - range.min) + range.min;
      }
      nextTick(function() {
        if (isTouched)
          $touchedKnobEl.addClass("range-knob-active-state");
      }, 70);
      range.on("change", onTouchChange);
      range.setValue(newValue, true);
    }
    function handleTouchMove(e) {
      if (!isTouched)
        return;
      var pageX;
      var pageY;
      if (e.type === "touchmove") {
        for (var i = 0; i < e.targetTouches.length; i += 1) {
          if (e.targetTouches[i].identifier === targetTouchIdentifier) {
            pageX = e.targetTouches[i].pageX;
            pageY = e.targetTouches[i].pageY;
          }
        }
      } else {
        pageX = e.pageX;
        pageY = e.pageY;
      }
      if (typeof pageX === "undefined" && typeof pageY === "undefined")
        return;
      if (typeof isScrolling === "undefined" && !range.vertical) {
        isScrolling = !!(isScrolling || Math.abs(pageY - touchesStart.y) > Math.abs(pageX - touchesStart.x));
      }
      if (isScrolling) {
        isTouched = false;
        return;
      }
      e.preventDefault();
      var progress;
      if (range.vertical) {
        progress = (pageY - rangeOffsetTop) / range.rangeHeight;
        if (!range.verticalReversed)
          progress = 1 - progress;
      } else if (range.app.rtl) {
        progress = (rangeOffsetLeft + range.rangeWidth - pageX) / range.rangeWidth;
      } else {
        progress = (pageX - rangeOffsetLeft) / range.rangeWidth;
      }
      var newValue = progress * (range.max - range.min) + range.min;
      if (range.dual) {
        var leftValue;
        var rightValue;
        if (dualValueIndex === 0) {
          leftValue = newValue;
          rightValue = range.value[1];
          if (leftValue > rightValue) {
            rightValue = leftValue;
          }
        } else {
          leftValue = range.value[0];
          rightValue = newValue;
          if (rightValue < leftValue) {
            leftValue = rightValue;
          }
        }
        newValue = [leftValue, rightValue];
      }
      range.setValue(newValue, true);
    }
    function handleTouchEnd(e) {
      if (e.type === "touchend") {
        var touchEnded;
        for (var i = 0; i < e.changedTouches.length; i += 1) {
          if (e.changedTouches[i].identifier === targetTouchIdentifier)
            touchEnded = true;
        }
        if (!touchEnded)
          return;
      }
      if (!isTouched) {
        if (isScrolling)
          $touchedKnobEl.removeClass("range-knob-active-state");
        isTouched = false;
        return;
      }
      range.off("change", onTouchChange);
      isTouched = false;
      $touchedKnobEl.removeClass("range-knob-active-state");
      if (valueChangedByTouch && range.$inputEl && !range.dual) {
        range.$inputEl.trigger("change");
      }
      valueChangedByTouch = false;
      if (typeof range.previousValue !== "undefined") {
        if (range.dual && (range.previousValue[0] !== range.value[0] || range.previousValue[1] !== range.value[1]) || !range.dual && range.previousValue !== range.value) {
          range.$el.trigger("range:changed", range.value);
          range.emit("local::changed rangeChanged", range, range.value);
        }
      }
    }
    function handleResize() {
      range.calcSize();
      range.layout();
    }
    var parentModals;
    var parentPanel;
    var parentPage;
    range.attachEvents = function attachEvents2() {
      var passive = support2.passiveListener ? {
        passive: true
      } : false;
      range.$el.on(app.touchEvents.start, handleTouchStart, passive);
      app.on("touchmove", handleTouchMove);
      app.on("touchend:passive", handleTouchEnd);
      app.on("tabShow", handleResize);
      app.on("resize", handleResize);
      parentModals = range.$el.parents(".sheet-modal, .actions-modal, .popup, .popover, .login-screen, .dialog, .toast");
      parentModals.on("modal:open", handleResize);
      parentPanel = range.$el.parents(".panel");
      parentPanel.on("panel:open panel:resize", handleResize);
      parentPage = range.$el.parents(".page").eq(0);
      parentPage.on("page:reinit", handleResize);
    };
    range.detachEvents = function detachEvents2() {
      var passive = support2.passiveListener ? {
        passive: true
      } : false;
      range.$el.off(app.touchEvents.start, handleTouchStart, passive);
      app.off("touchmove", handleTouchMove);
      app.off("touchend:passive", handleTouchEnd);
      app.off("tabShow", handleResize);
      app.off("resize", handleResize);
      if (parentModals) {
        parentModals.off("modal:open", handleResize);
      }
      if (parentPanel) {
        parentPanel.off("panel:open panel:resize", handleResize);
      }
      if (parentPage) {
        parentPage.off("page:reinit", handleResize);
      }
      parentModals = null;
      parentPanel = null;
      parentPage = null;
    };
    range.useModules();
    range.init();
    return range || _assertThisInitialized12(_this);
  }
  var _proto = Range2.prototype;
  _proto.calcSize = function calcSize() {
    var range = this;
    if (range.vertical) {
      var height = range.$el.outerHeight();
      if (height === 0)
        return;
      range.rangeHeight = height;
      range.knobHeight = range.knobs[0].outerHeight();
    } else {
      var width = range.$el.outerWidth();
      if (width === 0)
        return;
      range.rangeWidth = width;
      range.knobWidth = range.knobs[0].outerWidth();
    }
  };
  _proto.layout = function layout() {
    var range = this;
    var app = range.app, knobWidth = range.knobWidth, knobHeight = range.knobHeight, rangeWidth = range.rangeWidth, rangeHeight = range.rangeHeight, min = range.min, max = range.max, knobs = range.knobs, $barActiveEl = range.$barActiveEl, value = range.value, label = range.label, labels = range.labels, vertical = range.vertical, verticalReversed = range.verticalReversed, limitKnobPosition = range.limitKnobPosition;
    var knobSize = vertical ? knobHeight : knobWidth;
    var rangeSize = vertical ? rangeHeight : rangeWidth;
    var positionProperty = vertical ? verticalReversed ? "top" : "bottom" : app.rtl ? "right" : "left";
    if (range.dual) {
      var _$barActiveEl$css;
      var progress = [(value[0] - min) / (max - min), (value[1] - min) / (max - min)];
      $barActiveEl.css((_$barActiveEl$css = {}, _$barActiveEl$css[positionProperty] = progress[0] * 100 + "%", _$barActiveEl$css[vertical ? "height" : "width"] = (progress[1] - progress[0]) * 100 + "%", _$barActiveEl$css));
      knobs.forEach(function($knobEl, knobIndex) {
        var startPos2 = rangeSize * progress[knobIndex];
        if (limitKnobPosition) {
          var realStartPos2 = rangeSize * progress[knobIndex] - knobSize / 2;
          if (realStartPos2 < 0)
            startPos2 = knobSize / 2;
          if (realStartPos2 + knobSize > rangeSize)
            startPos2 = rangeSize - knobSize / 2;
        }
        $knobEl.css(positionProperty, startPos2 + "px");
        if (label)
          labels[knobIndex].text(range.formatLabel(value[knobIndex], labels[knobIndex][0]));
      });
    } else {
      var _progress = (value - min) / (max - min);
      $barActiveEl.css(vertical ? "height" : "width", _progress * 100 + "%");
      var startPos = rangeSize * _progress;
      if (limitKnobPosition) {
        var realStartPos = rangeSize * _progress - knobSize / 2;
        if (realStartPos < 0)
          startPos = knobSize / 2;
        if (realStartPos + knobSize > rangeSize)
          startPos = rangeSize - knobSize / 2;
      }
      knobs[0].css(positionProperty, startPos + "px");
      if (label)
        labels[0].text(range.formatLabel(value, labels[0][0]));
    }
    if (range.dual && value.indexOf(min) >= 0 || !range.dual && value === min) {
      range.$el.addClass("range-slider-min");
    } else {
      range.$el.removeClass("range-slider-min");
    }
    if (range.dual && value.indexOf(max) >= 0 || !range.dual && value === max) {
      range.$el.addClass("range-slider-max");
    } else {
      range.$el.removeClass("range-slider-max");
    }
  };
  _proto.setValue = function setValue(newValue, byTouchMove) {
    var range = this;
    var step = range.step, min = range.min, max = range.max;
    var valueChanged;
    var oldValue;
    if (range.dual) {
      oldValue = [range.value[0], range.value[1]];
      var newValues = newValue;
      if (!Array.isArray(newValues))
        newValues = [newValue, newValue];
      if (newValue[0] > newValue[1]) {
        newValues = [newValues[0], newValues[0]];
      }
      newValues = newValues.map(function(value2) {
        return Math.max(Math.min(Math.round(value2 / step) * step, max), min);
      });
      if (newValues[0] === range.value[0] && newValues[1] === range.value[1]) {
        return range;
      }
      newValues.forEach(function(value2, valueIndex) {
        range.value[valueIndex] = value2;
      });
      valueChanged = oldValue[0] !== newValues[0] || oldValue[1] !== newValues[1];
      range.layout();
    } else {
      oldValue = range.value;
      var value = Math.max(Math.min(Math.round(newValue / step) * step, max), min);
      range.value = value;
      range.layout();
      valueChanged = oldValue !== value;
    }
    if (valueChanged) {
      range.previousValue = oldValue;
    }
    if (!valueChanged)
      return range;
    range.$el.trigger("range:change", range.value);
    if (range.$inputEl && !range.dual) {
      range.$inputEl.val(range.value);
      if (!byTouchMove) {
        range.$inputEl.trigger("input change");
      } else {
        range.$inputEl.trigger("input");
      }
    }
    if (!byTouchMove) {
      range.$el.trigger("range:changed", range.value);
      range.emit("local::changed rangeChanged", range, range.value);
    }
    range.emit("local::change rangeChange", range, range.value);
    return range;
  };
  _proto.getValue = function getValue() {
    return this.value;
  };
  _proto.formatLabel = function formatLabel(value, labelEl) {
    var range = this;
    if (range.params.formatLabel)
      return range.params.formatLabel.call(range, value, labelEl);
    return value;
  };
  _proto.formatScaleLabel = function formatScaleLabel(value) {
    var range = this;
    if (range.params.formatScaleLabel)
      return range.params.formatScaleLabel.call(range, value);
    return value;
  };
  _proto.renderScale = function renderScale() {
    var range = this;
    var app = range.app, verticalReversed = range.verticalReversed, vertical = range.vertical;
    var positionProperty = vertical ? verticalReversed ? "top" : "bottom" : app.rtl ? "right" : "left";
    var html2 = "";
    Array.from({
      length: range.scaleSteps + 1
    }).forEach(function(scaleEl, index2) {
      var scaleStepValue = (range.max - range.min) / range.scaleSteps;
      var scaleValue = range.min + scaleStepValue * index2;
      var progress = (scaleValue - range.min) / (range.max - range.min);
      html2 += '<div class="range-scale-step" style="' + positionProperty + ": " + progress * 100 + '%">' + range.formatScaleLabel(scaleValue) + "</div>";
      if (range.scaleSubSteps && range.scaleSubSteps > 1 && index2 < range.scaleSteps) {
        Array.from({
          length: range.scaleSubSteps - 1
        }).forEach(function(subStepEl, subIndex) {
          var subStep = scaleStepValue / range.scaleSubSteps;
          var scaleSubValue = scaleValue + subStep * (subIndex + 1);
          var subProgress = (scaleSubValue - range.min) / (range.max - range.min);
          html2 += '<div class="range-scale-step range-scale-substep" style="' + positionProperty + ": " + subProgress * 100 + '%"></div>';
        });
      }
    });
    return html2;
  };
  _proto.updateScale = function updateScale() {
    var range = this;
    if (!range.scale || range.scaleSteps < 1) {
      if (range.$scaleEl)
        range.$scaleEl.remove();
      delete range.$scaleEl;
      return;
    }
    if (!range.$scaleEl) {
      range.$scaleEl = dom7_default('<div class="range-scale"></div>');
      range.$el.append(range.$scaleEl);
    }
    range.$scaleEl.html(range.renderScale());
  };
  _proto.init = function init46() {
    var range = this;
    range.calcSize();
    range.layout();
    range.attachEvents();
    return range;
  };
  _proto.destroy = function destroy50() {
    var range = this;
    range.$el.trigger("range:beforedestroy");
    range.emit("local::beforeDestroy rangeBeforeDestroy", range);
    delete range.$el[0].f7Range;
    range.detachEvents();
    deleteProps(range);
    range = null;
  };
  return Range2;
}(class_default);
var range_class_default = Range;

// node_modules/framework7/esm/components/range/range.js
var range_default = {
  name: "range",
  create: function create21() {
    var app = this;
    app.range = extend(ConstructorMethods({
      defaultSelector: ".range-slider",
      constructor: range_class_default,
      app,
      domProp: "f7Range"
    }), {
      getValue: function getValue(el) {
        if (el === void 0) {
          el = ".range-slider";
        }
        var range = app.range.get(el);
        if (range)
          return range.getValue();
        return void 0;
      },
      setValue: function setValue(el, value) {
        if (el === void 0) {
          el = ".range-slider";
        }
        var range = app.range.get(el);
        if (range)
          return range.setValue(value);
        return void 0;
      }
    });
  },
  static: {
    Range: range_class_default
  },
  on: {
    tabMounted: function tabMounted7(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".range-slider-init").each(function(rangeEl) {
        return new range_class_default(app, {
          el: rangeEl
        });
      });
    },
    tabBeforeRemove: function tabBeforeRemove4(tabEl) {
      dom7_default(tabEl).find(".range-slider-init").each(function(rangeEl) {
        if (rangeEl.f7Range)
          rangeEl.f7Range.destroy();
      });
    },
    pageInit: function pageInit8(page) {
      var app = this;
      page.$el.find(".range-slider-init").each(function(rangeEl) {
        return new range_class_default(app, {
          el: rangeEl
        });
      });
    },
    pageBeforeRemove: function pageBeforeRemove5(page) {
      page.$el.find(".range-slider-init").each(function(rangeEl) {
        if (rangeEl.f7Range)
          rangeEl.f7Range.destroy();
      });
    }
  },
  vnode: {
    "range-slider-init": {
      insert: function insert6(vnode) {
        var rangeEl = vnode.elm;
        var app = this;
        app.range.create({
          el: rangeEl
        });
      },
      destroy: function destroy5(vnode) {
        var rangeEl = vnode.elm;
        if (rangeEl.f7Range)
          rangeEl.f7Range.destroy();
      }
    }
  }
};

// node_modules/framework7/esm/components/stepper/stepper-class.js
function _assertThisInitialized13(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose13(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf13(subClass, superClass);
}
function _setPrototypeOf13(o, p) {
  _setPrototypeOf13 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf13(o, p);
}
var Stepper = function(_Framework7Class) {
  _inheritsLoose13(Stepper2, _Framework7Class);
  function Stepper2(app, params) {
    var _this;
    _this = _Framework7Class.call(this, params, [app]) || this;
    var stepper = _assertThisInitialized13(_this);
    var defaults = {
      el: null,
      inputEl: null,
      valueEl: null,
      value: 0,
      formatValue: null,
      step: 1,
      min: 0,
      max: 100,
      watchInput: true,
      autorepeat: false,
      autorepeatDynamic: false,
      wraps: false,
      manualInputMode: false,
      decimalPoint: 4,
      buttonsEndInputMode: true
    };
    stepper.useModulesParams(defaults);
    stepper.params = extend(defaults, params);
    if (stepper.params.value < stepper.params.min) {
      stepper.params.value = stepper.params.min;
    }
    if (stepper.params.value > stepper.params.max) {
      stepper.params.value = stepper.params.max;
    }
    var el = stepper.params.el;
    if (!el)
      return stepper || _assertThisInitialized13(_this);
    var $el = dom7_default(el);
    if ($el.length === 0)
      return stepper || _assertThisInitialized13(_this);
    if ($el[0].f7Stepper)
      return $el[0].f7Stepper || _assertThisInitialized13(_this);
    var $inputEl;
    if (stepper.params.inputEl) {
      $inputEl = dom7_default(stepper.params.inputEl);
    } else if ($el.find(".stepper-input-wrap").find("input, textarea").length) {
      $inputEl = $el.find(".stepper-input-wrap").find("input, textarea").eq(0);
    }
    if ($inputEl && $inputEl.length) {
      "step min max".split(" ").forEach(function(paramName) {
        if (!params[paramName] && $inputEl.attr(paramName)) {
          stepper.params[paramName] = parseFloat($inputEl.attr(paramName));
        }
      });
      var _decimalPoint = parseInt(stepper.params.decimalPoint, 10);
      if (Number.isNaN(_decimalPoint)) {
        stepper.params.decimalPoint = 0;
      } else {
        stepper.params.decimalPoint = _decimalPoint;
      }
      var inputValue = parseFloat($inputEl.val());
      if (typeof params.value === "undefined" && !Number.isNaN(inputValue) && (inputValue || inputValue === 0)) {
        stepper.params.value = inputValue;
      }
    }
    var $valueEl;
    if (stepper.params.valueEl) {
      $valueEl = dom7_default(stepper.params.valueEl);
    } else if ($el.find(".stepper-value").length) {
      $valueEl = $el.find(".stepper-value").eq(0);
    }
    var $buttonPlusEl = $el.find(".stepper-button-plus");
    var $buttonMinusEl = $el.find(".stepper-button-minus");
    var _stepper$params = stepper.params, step = _stepper$params.step, min = _stepper$params.min, max = _stepper$params.max, value = _stepper$params.value, decimalPoint = _stepper$params.decimalPoint;
    extend(stepper, {
      app,
      $el,
      el: $el[0],
      $buttonPlusEl,
      buttonPlusEl: $buttonPlusEl[0],
      $buttonMinusEl,
      buttonMinusEl: $buttonMinusEl[0],
      $inputEl,
      inputEl: $inputEl ? $inputEl[0] : void 0,
      $valueEl,
      valueEl: $valueEl ? $valueEl[0] : void 0,
      step,
      min,
      max,
      value,
      decimalPoint,
      typeModeChanged: false
    });
    $el[0].f7Stepper = stepper;
    var touchesStart = {};
    var isTouched;
    var isScrolling;
    var preventButtonClick;
    var intervalId;
    var timeoutId;
    var autorepeatAction = null;
    var autorepeatInAction = false;
    var manualInput = false;
    function dynamicRepeat(current, progressions, startsIn, progressionStep, repeatEvery, action) {
      clearTimeout(timeoutId);
      timeoutId = setTimeout(function() {
        if (current === 1) {
          preventButtonClick = true;
          autorepeatInAction = true;
        }
        clearInterval(intervalId);
        action();
        intervalId = setInterval(function() {
          action();
        }, repeatEvery);
        if (current < progressions) {
          dynamicRepeat(current + 1, progressions, startsIn, progressionStep, repeatEvery / 2, action);
        }
      }, current === 1 ? startsIn : progressionStep);
    }
    function onTouchStart3(e) {
      if (isTouched)
        return;
      if (manualInput) {
        return;
      }
      if (dom7_default(e.target).closest($buttonPlusEl).length) {
        autorepeatAction = "increment";
      } else if (dom7_default(e.target).closest($buttonMinusEl).length) {
        autorepeatAction = "decrement";
      }
      if (!autorepeatAction)
        return;
      touchesStart.x = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
      touchesStart.y = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
      isTouched = true;
      isScrolling = void 0;
      var progressions = stepper.params.autorepeatDynamic ? 4 : 1;
      dynamicRepeat(1, progressions, 500, 1e3, 300, function() {
        stepper[autorepeatAction]();
      });
    }
    function onTouchMove3(e) {
      if (!isTouched)
        return;
      if (manualInput) {
        return;
      }
      var pageX = e.type === "touchmove" ? e.targetTouches[0].pageX : e.pageX;
      var pageY = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
      if (typeof isScrolling === "undefined" && !autorepeatInAction) {
        isScrolling = !!(isScrolling || Math.abs(pageY - touchesStart.y) > Math.abs(pageX - touchesStart.x));
      }
      var distance = Math.pow(Math.pow(pageX - touchesStart.x, 2) + Math.pow(pageY - touchesStart.y, 2), 0.5);
      if (isScrolling || distance > 20) {
        isTouched = false;
        clearTimeout(timeoutId);
        clearInterval(intervalId);
      }
    }
    function onTouchEnd3() {
      clearTimeout(timeoutId);
      clearInterval(intervalId);
      autorepeatAction = null;
      autorepeatInAction = false;
      isTouched = false;
    }
    function onMinusClick() {
      if (manualInput) {
        if (stepper.params.buttonsEndInputMode) {
          manualInput = false;
          stepper.endTypeMode(true);
        }
        return;
      }
      if (preventButtonClick) {
        preventButtonClick = false;
        return;
      }
      stepper.decrement(true);
    }
    function onPlusClick() {
      if (manualInput) {
        if (stepper.params.buttonsEndInputMode) {
          manualInput = false;
          stepper.endTypeMode(true);
        }
        return;
      }
      if (preventButtonClick) {
        preventButtonClick = false;
        return;
      }
      stepper.increment(true);
    }
    function onInputClick(e) {
      if (!e.target.readOnly && stepper.params.manualInputMode) {
        manualInput = true;
        if (typeof e.target.selectionStart === "number") {
          e.target.selectionStart = e.target.value.length;
          e.target.selectionEnd = e.target.value.length;
        }
      }
    }
    function onInputKey(e) {
      if (e.keyCode === 13 || e.which === 13) {
        e.preventDefault();
        manualInput = false;
        stepper.endTypeMode();
      }
    }
    function onInputBlur() {
      manualInput = false;
      stepper.endTypeMode(true);
    }
    function onInput(e) {
      if (manualInput) {
        stepper.typeValue(e.target.value);
        return;
      }
      if (e.detail && e.detail.sentByF7Stepper)
        return;
      stepper.setValue(e.target.value, true);
    }
    stepper.attachEvents = function attachEvents2() {
      $buttonMinusEl.on("click", onMinusClick);
      $buttonPlusEl.on("click", onPlusClick);
      if (stepper.params.watchInput && $inputEl && $inputEl.length) {
        $inputEl.on("input", onInput);
        $inputEl.on("click", onInputClick);
        $inputEl.on("blur", onInputBlur);
        $inputEl.on("keyup", onInputKey);
      }
      if (stepper.params.autorepeat) {
        app.on("touchstart:passive", onTouchStart3);
        app.on("touchmove:active", onTouchMove3);
        app.on("touchend:passive", onTouchEnd3);
      }
    };
    stepper.detachEvents = function detachEvents2() {
      $buttonMinusEl.off("click", onMinusClick);
      $buttonPlusEl.off("click", onPlusClick);
      if (stepper.params.watchInput && $inputEl && $inputEl.length) {
        $inputEl.off("input", onInput);
        $inputEl.off("click", onInputClick);
        $inputEl.off("blur", onInputBlur);
        $inputEl.off("keyup", onInputKey);
      }
    };
    stepper.useModules();
    stepper.init();
    return stepper || _assertThisInitialized13(_this);
  }
  var _proto = Stepper2.prototype;
  _proto.minus = function minus() {
    return this.decrement();
  };
  _proto.plus = function plus() {
    return this.increment();
  };
  _proto.decrement = function decrement() {
    var stepper = this;
    return stepper.setValue(stepper.value - stepper.step, false, true);
  };
  _proto.increment = function increment() {
    var stepper = this;
    return stepper.setValue(stepper.value + stepper.step, false, true);
  };
  _proto.setValue = function setValue(newValue, forceUpdate, withWraps) {
    var stepper = this;
    var step = stepper.step, min = stepper.min, max = stepper.max;
    var oldValue = stepper.value;
    var value = Math.round(newValue / step) * step;
    if (stepper.params.wraps && withWraps) {
      if (value > max)
        value = min;
      if (value < min)
        value = max;
    } else {
      value = Math.max(Math.min(value, max), min);
    }
    if (Number.isNaN(value)) {
      value = oldValue;
    }
    stepper.value = value;
    var valueChanged = oldValue !== value;
    if (!valueChanged && !forceUpdate)
      return stepper;
    stepper.$el.trigger("stepper:change", stepper.value);
    var formattedValue = stepper.formatValue(stepper.value);
    if (stepper.$inputEl && stepper.$inputEl.length) {
      stepper.$inputEl.val(formattedValue);
      stepper.$inputEl.trigger("input change", {
        sentByF7Stepper: true
      });
    }
    if (stepper.$valueEl && stepper.$valueEl.length) {
      stepper.$valueEl.html(formattedValue);
    }
    stepper.emit("local::change stepperChange", stepper, stepper.value);
    return stepper;
  };
  _proto.endTypeMode = function endTypeMode(noBlur) {
    var stepper = this;
    var min = stepper.min, max = stepper.max;
    var value = parseFloat(stepper.value);
    if (Number.isNaN(value))
      value = 0;
    value = Math.max(Math.min(value, max), min);
    stepper.value = value;
    if (!stepper.typeModeChanged) {
      if (stepper.$inputEl && stepper.$inputEl.length && !noBlur) {
        stepper.$inputEl.blur();
      }
      return stepper;
    }
    stepper.typeModeChanged = false;
    stepper.$el.trigger("stepper:change", stepper.value);
    var formattedValue = stepper.formatValue(stepper.value);
    if (stepper.$inputEl && stepper.$inputEl.length) {
      stepper.$inputEl.val(formattedValue);
      stepper.$inputEl.trigger("input change", {
        sentByF7Stepper: true
      });
      if (!noBlur)
        stepper.$inputEl.blur();
    }
    if (stepper.$valueEl && stepper.$valueEl.length) {
      stepper.$valueEl.html(formattedValue);
    }
    stepper.emit("local::change stepperChange", stepper, stepper.value);
    return stepper;
  };
  _proto.typeValue = function typeValue(value) {
    var stepper = this;
    stepper.typeModeChanged = true;
    var inputTxt = String(value);
    if (inputTxt.length === 1 && inputTxt === "-")
      return stepper;
    if (inputTxt.lastIndexOf(".") + 1 === inputTxt.length || inputTxt.lastIndexOf(",") + 1 === inputTxt.length) {
      if (inputTxt.lastIndexOf(".") !== inputTxt.indexOf(".") || inputTxt.lastIndexOf(",") !== inputTxt.indexOf(",")) {
        inputTxt = inputTxt.slice(0, -1);
        stepper.value = inputTxt;
        stepper.$inputEl.val(stepper.value);
        return stepper;
      }
    } else {
      var newValue = parseFloat(inputTxt.replace(",", "."));
      if (newValue === 0) {
        stepper.value = inputTxt.replace(",", ".");
        stepper.$inputEl.val(stepper.value);
        return stepper;
      }
      if (Number.isNaN(newValue)) {
        stepper.value = 0;
        stepper.$inputEl.val(stepper.value);
        return stepper;
      }
      var powVal = Math.pow(10, stepper.params.decimalPoint);
      newValue = Math.round(newValue * powVal).toFixed(stepper.params.decimalPoint + 1) / powVal;
      stepper.value = parseFloat(String(newValue).replace(",", "."));
      stepper.$inputEl.val(stepper.value);
      return stepper;
    }
    stepper.value = inputTxt;
    stepper.$inputEl.val(inputTxt);
    return stepper;
  };
  _proto.getValue = function getValue() {
    return this.value;
  };
  _proto.formatValue = function formatValue(value) {
    var stepper = this;
    if (!stepper.params.formatValue)
      return value;
    return stepper.params.formatValue.call(stepper, value);
  };
  _proto.init = function init46() {
    var stepper = this;
    stepper.attachEvents();
    if (stepper.$valueEl && stepper.$valueEl.length) {
      var formattedValue = stepper.formatValue(stepper.value);
      stepper.$valueEl.html(formattedValue);
    }
    return stepper;
  };
  _proto.destroy = function destroy50() {
    var stepper = this;
    stepper.$el.trigger("stepper:beforedestroy");
    stepper.emit("local::beforeDestroy stepperBeforeDestroy", stepper);
    delete stepper.$el[0].f7Stepper;
    stepper.detachEvents();
    deleteProps(stepper);
    stepper = null;
  };
  return Stepper2;
}(class_default);
var stepper_class_default = Stepper;

// node_modules/framework7/esm/components/stepper/stepper.js
var stepper_default = {
  name: "stepper",
  create: function create22() {
    var app = this;
    app.stepper = extend(ConstructorMethods({
      defaultSelector: ".stepper",
      constructor: stepper_class_default,
      app,
      domProp: "f7Stepper"
    }), {
      getValue: function getValue(el) {
        if (el === void 0) {
          el = ".stepper";
        }
        var stepper = app.stepper.get(el);
        if (stepper)
          return stepper.getValue();
        return void 0;
      },
      setValue: function setValue(el, value) {
        if (el === void 0) {
          el = ".stepper";
        }
        var stepper = app.stepper.get(el);
        if (stepper)
          return stepper.setValue(value);
        return void 0;
      }
    });
  },
  static: {
    Stepper: stepper_class_default
  },
  on: {
    tabMounted: function tabMounted8(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".stepper-init").each(function(stepperEl) {
        var dataset = dom7_default(stepperEl).dataset();
        app.stepper.create(extend({
          el: stepperEl
        }, dataset || {}));
      });
    },
    tabBeforeRemove: function tabBeforeRemove5(tabEl) {
      dom7_default(tabEl).find(".stepper-init").each(function(stepperEl) {
        if (stepperEl.f7Stepper)
          stepperEl.f7Stepper.destroy();
      });
    },
    pageInit: function pageInit9(page) {
      var app = this;
      page.$el.find(".stepper-init").each(function(stepperEl) {
        var dataset = dom7_default(stepperEl).dataset();
        app.stepper.create(extend({
          el: stepperEl
        }, dataset || {}));
      });
    },
    pageBeforeRemove: function pageBeforeRemove6(page) {
      page.$el.find(".stepper-init").each(function(stepperEl) {
        if (stepperEl.f7Stepper)
          stepperEl.f7Stepper.destroy();
      });
    }
  },
  vnode: {
    "stepper-init": {
      insert: function insert7(vnode) {
        var app = this;
        var stepperEl = vnode.elm;
        var dataset = dom7_default(stepperEl).dataset();
        app.stepper.create(extend({
          el: stepperEl
        }, dataset || {}));
      },
      destroy: function destroy6(vnode) {
        var stepperEl = vnode.elm;
        if (stepperEl.f7Stepper)
          stepperEl.f7Stepper.destroy();
      }
    }
  }
};

// node_modules/framework7/esm/components/smart-select/smart-select-class.js
function _defineProperties(target, props) {
  for (var i = 0; i < props.length; i++) {
    var descriptor = props[i];
    descriptor.enumerable = descriptor.enumerable || false;
    descriptor.configurable = true;
    if ("value" in descriptor)
      descriptor.writable = true;
    Object.defineProperty(target, descriptor.key, descriptor);
  }
}
function _createClass(Constructor, protoProps, staticProps) {
  if (protoProps)
    _defineProperties(Constructor.prototype, protoProps);
  if (staticProps)
    _defineProperties(Constructor, staticProps);
  return Constructor;
}
function _assertThisInitialized14(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose14(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf14(subClass, superClass);
}
function _setPrototypeOf14(o, p) {
  _setPrototypeOf14 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf14(o, p);
}
var SmartSelect = function(_Framework7Class) {
  _inheritsLoose14(SmartSelect2, _Framework7Class);
  function SmartSelect2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var ss = _assertThisInitialized14(_this);
    var defaults = extend({
      on: {}
    }, app.params.smartSelect);
    if (typeof defaults.searchbarDisableButton === "undefined") {
      defaults.searchbarDisableButton = app.theme !== "aurora";
    }
    ss.useModulesParams(defaults);
    ss.params = extend({}, defaults, params);
    ss.app = app;
    var $el = dom7_default(ss.params.el).eq(0);
    if ($el.length === 0)
      return ss || _assertThisInitialized14(_this);
    if ($el[0].f7SmartSelect)
      return $el[0].f7SmartSelect || _assertThisInitialized14(_this);
    var $selectEl = $el.find("select").eq(0);
    if ($selectEl.length === 0)
      return ss || _assertThisInitialized14(_this);
    var $valueEl;
    if (ss.params.setValueText) {
      $valueEl = dom7_default(ss.params.valueEl);
      if ($valueEl.length === 0) {
        $valueEl = $el.find(".item-after");
      }
      if ($valueEl.length === 0) {
        $valueEl = dom7_default('<div class="item-after"></div>');
        $valueEl.insertAfter($el.find(".item-title"));
      }
    }
    var url = params.url;
    if (!url) {
      if ($el.attr("href") && $el.attr("href") !== "#")
        url = $el.attr("href");
      else if ($selectEl.attr("name"))
        url = $selectEl.attr("name").toLowerCase() + "-select/";
    }
    if (!url)
      url = ss.params.url;
    var multiple = $selectEl[0].multiple;
    var inputType = multiple ? "checkbox" : "radio";
    var selectId = id();
    extend(ss, {
      $el,
      el: $el[0],
      $selectEl,
      selectEl: $selectEl[0],
      $valueEl,
      valueEl: $valueEl && $valueEl[0],
      url,
      multiple,
      inputType,
      id: selectId,
      inputName: inputType + "-" + selectId,
      selectName: $selectEl.attr("name"),
      maxLength: $selectEl.attr("maxlength") || params.maxLength
    });
    $el[0].f7SmartSelect = ss;
    function onClick4() {
      ss.open();
    }
    function onChange() {
      var value = ss.$selectEl.val();
      ss.$el.trigger("smartselect:change", value);
      ss.emit("local::change smartSelectChange", ss, value);
      if (ss.vl) {
        ss.vl.clearCache();
      }
      ss.setValueText();
    }
    ss.attachEvents = function attachEvents2() {
      $el.on("click", onClick4);
      $el.on("change", "select", onChange);
    };
    ss.detachEvents = function detachEvents2() {
      $el.off("click", onClick4);
      $el.off("change", "select", onChange);
    };
    function handleInputChange() {
      var optionEl;
      var text2;
      var inputEl = this;
      var value = inputEl.value;
      var optionText = [];
      var displayAs;
      if (inputEl.type === "checkbox") {
        for (var i = 0; i < ss.selectEl.options.length; i += 1) {
          optionEl = ss.selectEl.options[i];
          if (optionEl.value === value) {
            optionEl.selected = inputEl.checked;
          }
          if (optionEl.selected) {
            displayAs = optionEl.dataset ? optionEl.dataset.displayAs : dom7_default(optionEl).data("display-value-as");
            text2 = displayAs && typeof displayAs !== "undefined" ? displayAs : optionEl.textContent;
            optionText.push(text2.trim());
          }
        }
        if (ss.maxLength) {
          ss.checkMaxLength();
        }
      } else {
        optionEl = ss.$selectEl.find('option[value="' + value + '"]')[0];
        if (!optionEl) {
          optionEl = ss.$selectEl.find("option").filter(function(optEl) {
            return optEl.value === value;
          })[0];
        }
        displayAs = optionEl.dataset ? optionEl.dataset.displayAs : dom7_default(optionEl).data("display-as");
        text2 = displayAs && typeof displayAs !== "undefined" ? displayAs : optionEl.textContent;
        optionText = [text2];
        ss.selectEl.value = value;
      }
      ss.$selectEl.trigger("change");
      if (ss.params.setValueText) {
        ss.$valueEl.text(ss.formatValueText(optionText));
      }
      if (ss.params.closeOnSelect && ss.inputType === "radio") {
        ss.close();
      }
    }
    ss.attachInputsEvents = function attachInputsEvents() {
      ss.$containerEl.on("change", 'input[type="checkbox"], input[type="radio"]', handleInputChange);
    };
    ss.detachInputsEvents = function detachInputsEvents() {
      ss.$containerEl.off("change", 'input[type="checkbox"], input[type="radio"]', handleInputChange);
    };
    ss.useModules();
    ss.init();
    return ss || _assertThisInitialized14(_this);
  }
  var _proto = SmartSelect2.prototype;
  _proto.setValue = function setValue(value) {
    var ss = this;
    var newValue = value;
    var optionText = [];
    var optionEl;
    var displayAs;
    var text2;
    if (ss.multiple) {
      if (!Array.isArray(newValue))
        newValue = [newValue];
      for (var i = 0; i < ss.selectEl.options.length; i += 1) {
        optionEl = ss.selectEl.options[i];
        if (newValue.indexOf(optionEl.value) >= 0) {
          optionEl.selected = true;
        } else {
          optionEl.selected = false;
        }
        if (optionEl.selected) {
          displayAs = optionEl.dataset ? optionEl.dataset.displayAs : dom7_default(optionEl).data("display-value-as");
          text2 = displayAs && typeof displayAs !== "undefined" ? displayAs : optionEl.textContent;
          optionText.push(text2.trim());
        }
      }
    } else {
      optionEl = ss.$selectEl.find('option[value="' + newValue + '"]')[0];
      if (optionEl) {
        displayAs = optionEl.dataset ? optionEl.dataset.displayAs : dom7_default(optionEl).data("display-as");
        text2 = displayAs && typeof displayAs !== "undefined" ? displayAs : optionEl.textContent;
        optionText = [text2];
      }
      ss.selectEl.value = newValue;
    }
    if (ss.params.setValueText) {
      ss.$valueEl.text(ss.formatValueText(optionText));
    }
    ss.$selectEl.trigger("change");
    return ss;
  };
  _proto.unsetValue = function unsetValue() {
    var ss = this;
    if (ss.params.setValueText) {
      ss.$valueEl.text(ss.formatValueText([]));
    }
    ss.$selectEl.find("option").each(function(optionEl) {
      optionEl.selected = false;
      optionEl.checked = false;
    });
    ss.$selectEl[0].value = null;
    if (ss.$containerEl) {
      ss.$containerEl.find('input[name="' + ss.inputName + '"][type="checkbox"], input[name="' + ss.inputName + '"][type="radio"]').prop("checked", false);
    }
    ss.$selectEl.trigger("change");
  };
  _proto.getValue = function getValue() {
    var ss = this;
    return ss.$selectEl.val();
  };
  _proto.checkMaxLength = function checkMaxLength() {
    var ss = this;
    var $containerEl = ss.$containerEl;
    if (ss.selectEl.selectedOptions.length >= ss.maxLength) {
      $containerEl.find('input[type="checkbox"]').each(function(inputEl) {
        if (!inputEl.checked) {
          dom7_default(inputEl).parents("li").addClass("disabled");
        } else {
          dom7_default(inputEl).parents("li").removeClass("disabled");
        }
      });
    } else {
      $containerEl.find(".disabled").removeClass("disabled");
    }
  };
  _proto.formatValueText = function formatValueText(values) {
    var ss = this;
    var textValue;
    if (ss.params.formatValueText) {
      textValue = ss.params.formatValueText.call(ss, values, ss);
    } else {
      textValue = values.join(", ");
    }
    return textValue;
  };
  _proto.setValueText = function setValueText(value) {
    var ss = this;
    var valueArray = [];
    if (typeof value !== "undefined") {
      if (Array.isArray(value)) {
        valueArray = value;
      } else {
        valueArray = [value];
      }
    } else {
      ss.$selectEl.find("option").each(function(optionEl) {
        var $optionEl = dom7_default(optionEl);
        if (optionEl.selected) {
          var displayAs = optionEl.dataset ? optionEl.dataset.displayAs : $optionEl.data("display-value-as");
          if (displayAs && typeof displayAs !== "undefined") {
            valueArray.push(displayAs);
          } else {
            valueArray.push(optionEl.textContent.trim());
          }
        }
      });
    }
    if (ss.params.setValueText) {
      ss.$valueEl.text(ss.formatValueText(valueArray));
    }
  };
  _proto.getItemsData = function getItemsData() {
    var ss = this;
    var theme = ss.app.theme;
    var items = [];
    var previousGroupEl;
    ss.$selectEl.find("option").each(function(optionEl) {
      var $optionEl = dom7_default(optionEl);
      var optionData = $optionEl.dataset();
      var optionImage = optionData.optionImage || ss.params.optionImage;
      var optionIcon = optionData.optionIcon || ss.params.optionIcon;
      var optionIconIos = theme === "ios" && (optionData.optionIconIos || ss.params.optionIconIos);
      var optionIconMd = theme === "md" && (optionData.optionIconMd || ss.params.optionIconMd);
      var optionIconAurora = theme === "aurora" && (optionData.optionIconAurora || ss.params.optionIconAurora);
      var optionHasMedia = optionImage || optionIcon || optionIconIos || optionIconMd || optionIconAurora;
      var optionColor = optionData.optionColor;
      var optionClassName = optionData.optionClass || "";
      if ($optionEl[0].disabled)
        optionClassName += " disabled";
      var optionGroupEl = $optionEl.parent("optgroup")[0];
      var optionGroupLabel = optionGroupEl && optionGroupEl.label;
      var optionIsLabel = false;
      if (optionGroupEl && optionGroupEl !== previousGroupEl) {
        optionIsLabel = true;
        previousGroupEl = optionGroupEl;
        items.push({
          groupLabel: optionGroupLabel,
          isLabel: optionIsLabel
        });
      }
      items.push({
        value: $optionEl[0].value,
        text: $optionEl[0].textContent.trim(),
        selected: $optionEl[0].selected,
        groupEl: optionGroupEl,
        groupLabel: optionGroupLabel,
        image: optionImage,
        icon: optionIcon,
        iconIos: optionIconIos,
        iconMd: optionIconMd,
        iconAurora: optionIconAurora,
        color: optionColor,
        className: optionClassName,
        disabled: $optionEl[0].disabled,
        id: ss.id,
        hasMedia: optionHasMedia,
        checkbox: ss.inputType === "checkbox",
        radio: ss.inputType === "radio",
        inputName: ss.inputName,
        inputType: ss.inputType
      });
    });
    ss.items = items;
    return items;
  };
  _proto.renderSearchbar = function renderSearchbar() {
    var ss = this;
    if (ss.params.renderSearchbar)
      return ss.params.renderSearchbar.call(ss);
    return jsx_default2("form", {
      class: "searchbar"
    }, jsx_default2("div", {
      class: "searchbar-inner"
    }, jsx_default2("div", {
      class: "searchbar-input-wrap"
    }, jsx_default2("input", {
      type: "search",
      spellcheck: ss.params.searchbarSpellcheck || "false",
      placeholder: ss.params.searchbarPlaceholder
    }), jsx_default2("i", {
      class: "searchbar-icon"
    }), jsx_default2("span", {
      class: "input-clear-button"
    })), ss.params.searchbarDisableButton && jsx_default2("span", {
      class: "searchbar-disable-button"
    }, ss.params.searchbarDisableText)));
  };
  _proto.renderItem = function renderItem(item, index2) {
    var ss = this;
    if (ss.params.renderItem)
      return ss.params.renderItem.call(ss, item, index2);
    function getIconContent(iconValue) {
      if (iconValue === void 0) {
        iconValue = "";
      }
      if (iconValue.indexOf(":") >= 0) {
        return iconValue.split(":")[1];
      }
      return "";
    }
    function getIconClass(iconValue) {
      if (iconValue === void 0) {
        iconValue = "";
      }
      if (iconValue.indexOf(":") >= 0) {
        var className = iconValue.split(":")[0];
        if (className === "f7")
          className = "f7-icons";
        if (className === "material")
          className = "material-icons";
        return className;
      }
      return iconValue;
    }
    var itemHtml;
    if (item.isLabel) {
      itemHtml = '<li class="item-divider">' + item.groupLabel + "</li>";
    } else {
      var selected = item.selected;
      var disabled;
      if (ss.params.virtualList) {
        var ssValue = ss.getValue();
        selected = ss.multiple ? ssValue.indexOf(item.value) >= 0 : ssValue === item.value;
        if (ss.multiple) {
          disabled = ss.multiple && !selected && ssValue.length === parseInt(ss.maxLength, 10);
        }
      }
      var icon = item.icon, iconIos = item.iconIos, iconMd = item.iconMd, iconAurora = item.iconAurora;
      var hasIcon = icon || iconIos || iconMd || iconAurora;
      var iconContent = getIconContent(icon || iconIos || iconMd || iconAurora || "");
      var iconClass = getIconClass(icon || iconIos || iconMd || iconAurora || "");
      itemHtml = jsx_default2("li", {
        class: "" + (item.className || "") + (disabled ? " disabled" : "")
      }, jsx_default2("label", {
        class: "item-" + item.inputType + " item-content"
      }, jsx_default2("input", {
        type: item.inputType,
        name: item.inputName,
        value: item.value,
        _checked: selected
      }), jsx_default2("i", {
        class: "icon icon-" + item.inputType
      }), item.hasMedia && jsx_default2("div", {
        class: "item-media"
      }, hasIcon && jsx_default2("i", {
        class: "icon " + iconClass
      }, iconContent), item.image && jsx_default2("img", {
        src: item.image
      })), jsx_default2("div", {
        class: "item-inner"
      }, jsx_default2("div", {
        class: "item-title" + (item.color ? " text-color-" + item.color : "")
      }, item.text))));
    }
    return itemHtml;
  };
  _proto.renderItems = function renderItems() {
    var ss = this;
    if (ss.params.renderItems)
      return ss.params.renderItems.call(ss, ss.items);
    var itemsHtml = "\n      " + ss.items.map(function(item, index2) {
      return "" + ss.renderItem(item, index2);
    }).join("") + "\n    ";
    return itemsHtml;
  };
  _proto.renderPage = function renderPage() {
    var ss = this;
    if (ss.params.renderPage)
      return ss.params.renderPage.call(ss, ss.items);
    var pageTitle = ss.params.pageTitle;
    if (typeof pageTitle === "undefined") {
      var $itemTitleEl = ss.$el.find(".item-title");
      pageTitle = $itemTitleEl.length ? $itemTitleEl.text().trim() : "";
    }
    var cssClass = ss.params.cssClass;
    return jsx_default2("div", {
      class: "page smart-select-page " + cssClass,
      "data-name": "smart-select-page",
      "data-select-name": ss.selectName
    }, jsx_default2("div", {
      class: "navbar " + (ss.params.navbarColorTheme ? "color-" + ss.params.navbarColorTheme : "")
    }, jsx_default2("div", {
      class: "navbar-bg"
    }), jsx_default2("div", {
      class: "navbar-inner sliding " + (ss.params.navbarColorTheme ? "color-" + ss.params.navbarColorTheme : "")
    }, jsx_default2("div", {
      class: "left"
    }, jsx_default2("a", {
      class: "link back"
    }, jsx_default2("i", {
      class: "icon icon-back"
    }), jsx_default2("span", {
      class: "if-not-md"
    }, ss.params.pageBackLinkText))), pageTitle && jsx_default2("div", {
      class: "title"
    }, pageTitle), ss.params.searchbar && jsx_default2("div", {
      class: "subnavbar"
    }, ss.renderSearchbar()))), ss.params.searchbar && jsx_default2("div", {
      class: "searchbar-backdrop"
    }), jsx_default2("div", {
      class: "page-content"
    }, jsx_default2("div", {
      class: "list smart-select-list-" + ss.id + " " + (ss.params.virtualList ? " virtual-list" : "") + " " + (ss.params.formColorTheme ? "color-" + ss.params.formColorTheme : "")
    }, jsx_default2("ul", null, !ss.params.virtualList && ss.renderItems(ss.items)))));
  };
  _proto.renderPopup = function renderPopup() {
    var ss = this;
    if (ss.params.renderPopup)
      return ss.params.renderPopup.call(ss, ss.items);
    var pageTitle = ss.params.pageTitle;
    if (typeof pageTitle === "undefined") {
      var $itemTitleEl = ss.$el.find(".item-title");
      pageTitle = $itemTitleEl.length ? $itemTitleEl.text().trim() : "";
    }
    var cssClass = ss.params.cssClass || "";
    return jsx_default2("div", {
      class: "popup smart-select-popup " + cssClass + " " + (ss.params.popupTabletFullscreen ? "popup-tablet-fullscreen" : ""),
      "data-select-name": ss.selectName
    }, jsx_default2("div", {
      class: "view"
    }, jsx_default2("div", {
      class: "page smart-select-page " + (ss.params.searchbar ? "page-with-subnavbar" : ""),
      "data-name": "smart-select-page"
    }, jsx_default2("div", {
      class: "navbar " + (ss.params.navbarColorTheme ? "color-" + ss.params.navbarColorTheme : "")
    }, jsx_default2("div", {
      class: "navbar-bg"
    }), jsx_default2("div", {
      class: "navbar-inner sliding"
    }, pageTitle && jsx_default2("div", {
      class: "title"
    }, pageTitle), jsx_default2("div", {
      class: "right"
    }, jsx_default2("a", {
      class: "link popup-close",
      "data-popup": ".smart-select-popup[data-select-name='" + ss.selectName + "']"
    }, ss.params.popupCloseLinkText)), ss.params.searchbar && jsx_default2("div", {
      class: "subnavbar"
    }, ss.renderSearchbar()))), ss.params.searchbar && jsx_default2("div", {
      class: "searchbar-backdrop"
    }), jsx_default2("div", {
      class: "page-content"
    }, jsx_default2("div", {
      class: "list smart-select-list-" + ss.id + " " + (ss.params.virtualList ? " virtual-list" : "") + " " + (ss.params.formColorTheme ? "color-" + ss.params.formColorTheme : "")
    }, jsx_default2("ul", null, !ss.params.virtualList && ss.renderItems(ss.items)))))));
  };
  _proto.renderSheet = function renderSheet() {
    var ss = this;
    if (ss.params.renderSheet)
      return ss.params.renderSheet.call(ss, ss.items);
    var cssClass = ss.params.cssClass;
    return jsx_default2("div", {
      class: "sheet-modal smart-select-sheet " + cssClass,
      "data-select-name": ss.selectName
    }, jsx_default2("div", {
      class: "toolbar toolbar-top " + (ss.params.toolbarColorTheme ? "color-" + ss.params.toolbarColorTheme : "")
    }, jsx_default2("div", {
      class: "toolbar-inner"
    }, jsx_default2("div", {
      class: "left"
    }), jsx_default2("div", {
      class: "right"
    }, jsx_default2("a", {
      class: "link sheet-close"
    }, ss.params.sheetCloseLinkText)))), jsx_default2("div", {
      class: "sheet-modal-inner"
    }, jsx_default2("div", {
      class: "page-content"
    }, jsx_default2("div", {
      class: "list smart-select-list-" + ss.id + " " + (ss.params.virtualList ? " virtual-list" : "") + " " + (ss.params.formColorTheme ? "color-" + ss.params.formColorTheme : "")
    }, jsx_default2("ul", null, !ss.params.virtualList && ss.renderItems(ss.items))))));
  };
  _proto.renderPopover = function renderPopover() {
    var ss = this;
    if (ss.params.renderPopover)
      return ss.params.renderPopover.call(ss, ss.items);
    var cssClass = ss.params.cssClass;
    return jsx_default2("div", {
      class: "popover smart-select-popover " + cssClass,
      "data-select-name": ss.selectName
    }, jsx_default2("div", {
      class: "popover-inner"
    }, jsx_default2("div", {
      class: "list smart-select-list-" + ss.id + " " + (ss.params.virtualList ? " virtual-list" : "") + " " + (ss.params.formColorTheme ? "color-" + ss.params.formColorTheme : "")
    }, jsx_default2("ul", null, !ss.params.virtualList && ss.renderItems(ss.items)))));
  };
  _proto.scrollToSelectedItem = function scrollToSelectedItem() {
    var ss = this;
    var params = ss.params, $containerEl = ss.$containerEl;
    if (!ss.opened)
      return ss;
    if (params.virtualList) {
      var selectedIndex;
      ss.vl.items.forEach(function(item, index2) {
        if (typeof selectedIndex === "undefined" && item.selected) {
          selectedIndex = index2;
        }
      });
      if (typeof selectedIndex !== "undefined") {
        ss.vl.scrollToItem(selectedIndex);
      }
    } else {
      var $selectedItemEl = $containerEl.find("input:checked").parents("li");
      if (!$selectedItemEl.length)
        return ss;
      var $scrollableEl = $containerEl.find(".page-content, .popover-inner");
      if (!$scrollableEl.length)
        return ss;
      $scrollableEl.scrollTop($selectedItemEl.offset().top - $scrollableEl.offset().top - parseInt($scrollableEl.css("padding-top"), 10));
    }
    return ss;
  };
  _proto.onOpen = function onOpen(type, containerEl) {
    var ss = this;
    var app = ss.app;
    var $containerEl = dom7_default(containerEl);
    ss.$containerEl = $containerEl;
    ss.openedIn = type;
    ss.opened = true;
    if (ss.params.virtualList) {
      ss.vl = app.virtualList.create({
        el: $containerEl.find(".virtual-list"),
        items: ss.items,
        renderItem: ss.renderItem.bind(ss),
        height: ss.params.virtualListHeight,
        searchByItem: function searchByItem(query, item) {
          if (item.text && item.text.toLowerCase().indexOf(query.trim().toLowerCase()) >= 0)
            return true;
          return false;
        }
      });
    }
    if (ss.params.scrollToSelectedItem) {
      ss.scrollToSelectedItem();
    }
    if (ss.params.searchbar) {
      var $searchbarEl = $containerEl.find(".searchbar");
      if (type === "page" && app.theme === "ios") {
        $searchbarEl = dom7_default(app.navbar.getElByPage($containerEl)).find(".searchbar");
      }
      if (ss.params.appendSearchbarNotFound && (type === "page" || type === "popup")) {
        var $notFoundEl = null;
        if (typeof ss.params.appendSearchbarNotFound === "string") {
          $notFoundEl = dom7_default('<div class="block searchbar-not-found">' + ss.params.appendSearchbarNotFound + "</div>");
        } else if (typeof ss.params.appendSearchbarNotFound === "boolean") {
          $notFoundEl = dom7_default('<div class="block searchbar-not-found">Nothing found</div>');
        } else {
          $notFoundEl = ss.params.appendSearchbarNotFound;
        }
        if ($notFoundEl) {
          $containerEl.find(".page-content").append($notFoundEl[0]);
        }
      }
      var searchbarParams = extend({
        el: $searchbarEl,
        backdropEl: $containerEl.find(".searchbar-backdrop"),
        searchContainer: ".smart-select-list-" + ss.id,
        searchIn: ".item-title"
      }, typeof ss.params.searchbar === "object" ? ss.params.searchbar : {});
      ss.searchbar = app.searchbar.create(searchbarParams);
    }
    if (ss.maxLength) {
      ss.checkMaxLength();
    }
    if (ss.params.closeOnSelect) {
      ss.$containerEl.find('input[type="radio"][name="' + ss.inputName + '"]:checked').parents("label").once("click", function() {
        ss.close();
      });
    }
    ss.attachInputsEvents();
    ss.$el.trigger("smartselect:open");
    ss.emit("local::open smartSelectOpen", ss);
  };
  _proto.onOpened = function onOpened() {
    var ss = this;
    ss.$el.trigger("smartselect:opened");
    ss.emit("local::opened smartSelectOpened", ss);
  };
  _proto.onClose = function onClose() {
    var ss = this;
    if (ss.destroyed)
      return;
    if (ss.vl && ss.vl.destroy) {
      ss.vl.destroy();
      ss.vl = null;
      delete ss.vl;
    }
    if (ss.searchbar && ss.searchbar.destroy) {
      ss.searchbar.destroy();
      ss.searchbar = null;
      delete ss.searchbar;
    }
    ss.detachInputsEvents();
    ss.$el.trigger("smartselect:close");
    ss.emit("local::close smartSelectClose", ss);
  };
  _proto.onClosed = function onClosed() {
    var ss = this;
    if (ss.destroyed)
      return;
    ss.opened = false;
    ss.$containerEl = null;
    delete ss.$containerEl;
    ss.$el.trigger("smartselect:closed");
    ss.emit("local::closed smartSelectClosed", ss);
  };
  _proto.openPage = function openPage() {
    var ss = this;
    if (ss.opened)
      return ss;
    ss.getItemsData();
    var pageHtml = ss.renderPage(ss.items);
    ss.view.router.navigate({
      url: ss.url,
      route: {
        content: pageHtml,
        path: ss.url,
        on: {
          pageBeforeIn: function pageBeforeIn2(e, page) {
            ss.onOpen("page", page.el);
          },
          pageAfterIn: function pageAfterIn2(e, page) {
            ss.onOpened("page", page.el);
          },
          pageBeforeOut: function pageBeforeOut(e, page) {
            ss.onClose("page", page.el);
          },
          pageAfterOut: function pageAfterOut(e, page) {
            ss.onClosed("page", page.el);
          }
        }
      }
    });
    return ss;
  };
  _proto.openPopup = function openPopup2() {
    var ss = this;
    if (ss.opened)
      return ss;
    ss.getItemsData();
    var popupHtml = ss.renderPopup(ss.items);
    var popupParams = {
      content: popupHtml,
      push: ss.params.popupPush,
      swipeToClose: ss.params.popupSwipeToClose,
      on: {
        popupOpen: function popupOpen(popup) {
          ss.onOpen("popup", popup.el);
        },
        popupOpened: function popupOpened(popup) {
          ss.onOpened("popup", popup.el);
        },
        popupClose: function popupClose(popup) {
          ss.onClose("popup", popup.el);
        },
        popupClosed: function popupClosed(popup) {
          ss.onClosed("popup", popup.el);
        }
      }
    };
    if (ss.params.routableModals && ss.view) {
      ss.view.router.navigate({
        url: ss.url,
        route: {
          path: ss.url,
          popup: popupParams
        }
      });
    } else {
      ss.modal = ss.app.popup.create(popupParams).open();
    }
    return ss;
  };
  _proto.openSheet = function openSheet2() {
    var ss = this;
    if (ss.opened)
      return ss;
    ss.getItemsData();
    var sheetHtml = ss.renderSheet(ss.items);
    var sheetParams = {
      content: sheetHtml,
      backdrop: ss.params.sheetBackdrop,
      scrollToEl: ss.$el,
      closeByOutsideClick: true,
      push: ss.params.sheetPush,
      swipeToClose: ss.params.sheetSwipeToClose,
      on: {
        sheetOpen: function sheetOpen(sheet) {
          ss.onOpen("sheet", sheet.el);
        },
        sheetOpened: function sheetOpened(sheet) {
          ss.onOpened("sheet", sheet.el);
        },
        sheetClose: function sheetClose(sheet) {
          ss.onClose("sheet", sheet.el);
        },
        sheetClosed: function sheetClosed(sheet) {
          ss.onClosed("sheet", sheet.el);
        }
      }
    };
    if (ss.params.routableModals && ss.view) {
      ss.view.router.navigate({
        url: ss.url,
        route: {
          path: ss.url,
          sheet: sheetParams
        }
      });
    } else {
      ss.modal = ss.app.sheet.create(sheetParams).open();
    }
    return ss;
  };
  _proto.openPopover = function openPopover2() {
    var ss = this;
    if (ss.opened)
      return ss;
    ss.getItemsData();
    var popoverHtml = ss.renderPopover(ss.items);
    var popoverParams = {
      content: popoverHtml,
      targetEl: ss.$el,
      on: {
        popoverOpen: function popoverOpen(popover) {
          ss.onOpen("popover", popover.el);
        },
        popoverOpened: function popoverOpened(popover) {
          ss.onOpened("popover", popover.el);
        },
        popoverClose: function popoverClose(popover) {
          ss.onClose("popover", popover.el);
        },
        popoverClosed: function popoverClosed(popover) {
          ss.onClosed("popover", popover.el);
        }
      }
    };
    if (ss.params.routableModals && ss.view) {
      ss.view.router.navigate({
        url: ss.url,
        route: {
          path: ss.url,
          popover: popoverParams
        }
      });
    } else {
      ss.modal = ss.app.popover.create(popoverParams).open();
    }
    return ss;
  };
  _proto.open = function open12(type) {
    var ss = this;
    if (ss.opened)
      return ss;
    var prevented = false;
    function prevent() {
      prevented = true;
    }
    if (ss.$el) {
      ss.$el.trigger("smartselect:beforeopen", {
        prevent
      });
    }
    ss.emit("local::beforeOpen smartSelectBeforeOpen", ss, prevent);
    if (prevented)
      return ss;
    var openIn = type || ss.params.openIn;
    ss["open" + openIn.split("").map(function(el, index2) {
      if (index2 === 0)
        return el.toUpperCase();
      return el;
    }).join("")]();
    return ss;
  };
  _proto.close = function close12() {
    var ss = this;
    if (!ss.opened)
      return ss;
    if (ss.params.routableModals && ss.view || ss.openedIn === "page") {
      ss.view.router.back();
    } else {
      ss.modal.once("modalClosed", function() {
        nextTick(function() {
          if (ss.destroyed)
            return;
          ss.modal.destroy();
          delete ss.modal;
        });
      });
      ss.modal.close();
    }
    return ss;
  };
  _proto.init = function init46() {
    var ss = this;
    ss.attachEvents();
    ss.setValueText();
  };
  _proto.destroy = function destroy50() {
    var ss = this;
    ss.emit("local::beforeDestroy smartSelectBeforeDestroy", ss);
    ss.$el.trigger("smartselect:beforedestroy");
    ss.detachEvents();
    delete ss.$el[0].f7SmartSelect;
    deleteProps(ss);
    ss.destroyed = true;
  };
  _createClass(SmartSelect2, [{
    key: "view",
    get: function get2() {
      var params = this.params, $el = this.$el;
      var view;
      if (params.view) {
        view = params.view;
      }
      if (!view) {
        view = $el.parents(".view").length && $el.parents(".view")[0].f7View;
      }
      if (!view && params.openIn === "page") {
        throw Error("Smart Select requires initialized View");
      }
      return view;
    }
  }]);
  return SmartSelect2;
}(class_default);
var smart_select_class_default = SmartSelect;

// node_modules/framework7/esm/components/smart-select/smart-select.js
var smart_select_default = {
  name: "smartSelect",
  params: {
    smartSelect: {
      el: void 0,
      valueEl: void 0,
      setValueText: true,
      formatValueText: null,
      openIn: "page",
      popupPush: false,
      popupSwipeToClose: void 0,
      sheetPush: false,
      sheetSwipeToClose: void 0,
      sheetBackdrop: false,
      pageTitle: void 0,
      pageBackLinkText: "Back",
      popupCloseLinkText: "Close",
      popupTabletFullscreen: false,
      sheetCloseLinkText: "Done",
      searchbar: false,
      searchbarPlaceholder: "Search",
      searchbarDisableText: "Cancel",
      searchbarDisableButton: void 0,
      searchbarSpellcheck: false,
      closeOnSelect: false,
      virtualList: false,
      virtualListHeight: void 0,
      scrollToSelectedItem: false,
      formColorTheme: void 0,
      navbarColorTheme: void 0,
      routableModals: false,
      url: "select/",
      cssClass: "",
      renderPage: void 0,
      renderPopup: void 0,
      renderSheet: void 0,
      renderPopover: void 0,
      renderItems: void 0,
      renderItem: void 0,
      renderSearchbar: void 0
    }
  },
  static: {
    SmartSelect: smart_select_class_default
  },
  create: function create23() {
    var app = this;
    app.smartSelect = extend(ConstructorMethods({
      defaultSelector: ".smart-select",
      constructor: smart_select_class_default,
      app,
      domProp: "f7SmartSelect"
    }), {
      open: function open12(smartSelectEl) {
        var ss = app.smartSelect.get(smartSelectEl);
        if (ss && ss.open)
          return ss.open();
        return void 0;
      },
      close: function close12(smartSelectEl) {
        var ss = app.smartSelect.get(smartSelectEl);
        if (ss && ss.close)
          return ss.close();
        return void 0;
      }
    });
  },
  on: {
    tabMounted: function tabMounted9(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".smart-select-init").each(function(smartSelectEl) {
        app.smartSelect.create(extend({
          el: smartSelectEl
        }, dom7_default(smartSelectEl).dataset()));
      });
    },
    tabBeforeRemove: function tabBeforeRemove6(tabEl) {
      dom7_default(tabEl).find(".smart-select-init").each(function(smartSelectEl) {
        if (smartSelectEl.f7SmartSelect && smartSelectEl.f7SmartSelect.destroy) {
          smartSelectEl.f7SmartSelect.destroy();
        }
      });
    },
    pageInit: function pageInit10(page) {
      var app = this;
      page.$el.find(".smart-select-init").each(function(smartSelectEl) {
        app.smartSelect.create(extend({
          el: smartSelectEl
        }, dom7_default(smartSelectEl).dataset()));
      });
    },
    pageBeforeRemove: function pageBeforeRemove7(page) {
      page.$el.find(".smart-select-init").each(function(smartSelectEl) {
        if (smartSelectEl.f7SmartSelect && smartSelectEl.f7SmartSelect.destroy) {
          smartSelectEl.f7SmartSelect.destroy();
        }
      });
    }
  },
  clicks: {
    ".smart-select": function open6($clickedEl, data) {
      var app = this;
      if (!$clickedEl[0].f7SmartSelect) {
        var ss = app.smartSelect.create(extend({
          el: $clickedEl
        }, data));
        ss.open();
      }
    }
  },
  vnode: {
    "smart-select-init": {
      insert: function insert8(vnode) {
        var app = this;
        var smartSelectEl = vnode.elm;
        app.smartSelect.create(extend({
          el: smartSelectEl
        }, dom7_default(smartSelectEl).dataset()));
      },
      destroy: function destroy7(vnode) {
        var smartSelectEl = vnode.elm;
        if (smartSelectEl.f7SmartSelect && smartSelectEl.f7SmartSelect.destroy) {
          smartSelectEl.f7SmartSelect.destroy();
        }
      }
    }
  }
};

// node_modules/framework7/esm/components/grid/grid.js
function getElMinSize(dimension, $el) {
  var minSize = $el.css("min-" + dimension);
  if (minSize === "auto" || minSize === "none") {
    minSize = 0;
  } else if (minSize.indexOf("px") >= 0) {
    minSize = parseFloat(minSize);
  } else if (minSize.indexOf("%") >= 0) {
    minSize = $el.parent()[0][dimension === "height" ? "offsetHeight" : "offsetWidth"] * parseFloat(minSize) / 100;
  }
  return minSize;
}
function getElMaxSize(dimension, $el) {
  var maxSize = $el.css("max-" + dimension);
  if (maxSize === "auto" || maxSize === "none") {
    maxSize = null;
  } else if (maxSize.indexOf("px") >= 0) {
    maxSize = parseFloat(maxSize);
  } else if (maxSize.indexOf("%") >= 0) {
    maxSize = $el.parent()[0][dimension === "height" ? "offsetHeight" : "offsetWidth"] * parseFloat(maxSize) / 100;
  }
  return maxSize;
}
var Grid = {
  init: function init11() {
    var app = this;
    var isTouched;
    var isMoved;
    var touchStartX;
    var touchStartY;
    var $resizeHandlerEl;
    var $prevResizableEl;
    var $nextResizableEl;
    var prevElSize;
    var prevElMinSize;
    var prevElMaxSize;
    var nextElSize;
    var nextElMinSize;
    var nextElMaxSize;
    var parentSize;
    var itemsInFlow;
    var gapSize;
    var isScrolling;
    function handleTouchStart(e) {
      if (isTouched || isMoved)
        return;
      $resizeHandlerEl = dom7_default(e.target).closest(".resize-handler");
      touchStartX = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
      touchStartY = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
      isTouched = true;
      $prevResizableEl = void 0;
      $nextResizableEl = void 0;
      isScrolling = void 0;
    }
    function handleTouchMove(e) {
      if (!isTouched)
        return;
      var isRow = $resizeHandlerEl.parent(".row").length === 1;
      var sizeProp = isRow ? "height" : "width";
      var getSizeProp = isRow ? "offsetHeight" : "offsetWidth";
      if (!isMoved) {
        $prevResizableEl = $resizeHandlerEl.parent(isRow ? ".row" : ".col");
        if ($prevResizableEl.length && (!$prevResizableEl.hasClass("resizable") || $prevResizableEl.hasClass("resizable-fixed"))) {
          $prevResizableEl = $prevResizableEl.prevAll(".resizable:not(.resizable-fixed)").eq(0);
        }
        $nextResizableEl = $prevResizableEl.next(isRow ? ".row" : ".col");
        if ($nextResizableEl.length && (!$nextResizableEl.hasClass("resizable") || $nextResizableEl.hasClass("resizable-fixed"))) {
          $nextResizableEl = $nextResizableEl.nextAll(".resizable:not(.resizable-fixed)").eq(0);
        }
        if ($prevResizableEl.length) {
          prevElSize = $prevResizableEl[0][getSizeProp];
          prevElMinSize = getElMinSize(sizeProp, $prevResizableEl);
          prevElMaxSize = getElMaxSize(sizeProp, $prevResizableEl);
          parentSize = $prevResizableEl.parent()[0][getSizeProp];
          itemsInFlow = $prevResizableEl.parent().children(isRow ? ".row" : '[class*="col-"], .col').length;
          gapSize = parseFloat($prevResizableEl.css(isRow ? "--f7-grid-row-gap" : "--f7-grid-gap"));
        }
        if ($nextResizableEl.length) {
          nextElSize = $nextResizableEl[0][getSizeProp];
          nextElMinSize = getElMinSize(sizeProp, $nextResizableEl);
          nextElMaxSize = getElMaxSize(sizeProp, $nextResizableEl);
          if (!$prevResizableEl.length) {
            parentSize = $nextResizableEl.parent()[0][getSizeProp];
            itemsInFlow = $nextResizableEl.parent().children(isRow ? ".row" : '[class*="col-"], .col').length;
            gapSize = parseFloat($nextResizableEl.css(isRow ? "--f7-grid-row-gap" : "--f7-grid-gap"));
          }
        }
      }
      isMoved = true;
      var touchCurrentX = e.type === "touchmove" ? e.targetTouches[0].pageX : e.pageX;
      var touchCurrentY = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
      if (typeof isScrolling === "undefined" && !isRow) {
        isScrolling = !!(isScrolling || Math.abs(touchCurrentY - touchStartY) > Math.abs(touchCurrentX - touchStartX));
      }
      if (isScrolling) {
        isTouched = false;
        isMoved = false;
        return;
      }
      var isAbsolute = $prevResizableEl.hasClass("resizable-absolute") || $nextResizableEl.hasClass("resizable-absolute");
      var resizeNextEl = !isRow || isRow && !isAbsolute;
      if (resizeNextEl && !$nextResizableEl.length || !$prevResizableEl.length) {
        isTouched = false;
        isMoved = false;
        return;
      }
      e.preventDefault();
      var diff = isRow ? touchCurrentY - touchStartY : touchCurrentX - touchStartX;
      var prevElNewSize;
      var nextElNewSize;
      if ($prevResizableEl.length) {
        prevElNewSize = prevElSize + diff;
        if (prevElNewSize < prevElMinSize) {
          prevElNewSize = prevElMinSize;
          diff = prevElNewSize - prevElSize;
        }
        if (prevElMaxSize && prevElNewSize > prevElMaxSize) {
          prevElNewSize = prevElMaxSize;
          diff = prevElNewSize - prevElSize;
        }
      }
      if ($nextResizableEl.length && resizeNextEl) {
        nextElNewSize = nextElSize - diff;
        if (nextElNewSize < nextElMinSize) {
          nextElNewSize = nextElMinSize;
          diff = nextElSize - nextElNewSize;
          prevElNewSize = prevElSize + diff;
        }
        if (nextElMaxSize && nextElNewSize > nextElMaxSize) {
          nextElNewSize = nextElMaxSize;
          diff = nextElSize - nextElNewSize;
          prevElNewSize = prevElSize + diff;
        }
      }
      if (isAbsolute) {
        $prevResizableEl[0].style[sizeProp] = prevElNewSize + "px";
        if (resizeNextEl) {
          $nextResizableEl[0].style[sizeProp] = nextElNewSize + "px";
        }
        $prevResizableEl.trigger("grid:resize");
        $nextResizableEl.trigger("grid:resize");
        app.emit("gridResize", $prevResizableEl[0]);
        app.emit("gridResize", $nextResizableEl[0]);
        return;
      }
      var gapAddSize = (itemsInFlow - 1) * gapSize / itemsInFlow;
      var gapAddSizeCSS = isRow ? itemsInFlow - 1 + " * var(--f7-grid-row-gap) / " + itemsInFlow : "(var(--f7-cols-per-row) - 1) * var(--f7-grid-gap) / var(--f7-cols-per-row)";
      var prevElNewSizeNormalized = prevElNewSize + gapAddSize;
      var nextElNewSizeNormalized = nextElNewSize + gapAddSize;
      $prevResizableEl[0].style[sizeProp] = "calc(" + prevElNewSizeNormalized / parentSize * 100 + "% - " + gapAddSizeCSS + ")";
      $nextResizableEl[0].style[sizeProp] = "calc(" + nextElNewSizeNormalized / parentSize * 100 + "% - " + gapAddSizeCSS + ")";
      $prevResizableEl.trigger("grid:resize");
      $nextResizableEl.trigger("grid:resize");
      app.emit("gridResize", $prevResizableEl[0]);
      app.emit("gridResize", $nextResizableEl[0]);
    }
    function handleTouchEnd() {
      if (!isTouched)
        return;
      if (!isMoved) {
        isTouched = false;
      }
      isTouched = false;
      isMoved = false;
    }
    var document = getDocument();
    dom7_default(document).on(app.touchEvents.start, ".col > .resize-handler, .row > .resize-handler", handleTouchStart);
    app.on("touchmove", handleTouchMove);
    app.on("touchend", handleTouchEnd);
  }
};
var grid_default = {
  name: "grid",
  create: function create24() {
    var app = this;
    extend(app, {
      grid: {
        init: Grid.init.bind(app)
      }
    });
  },
  on: {
    init: function init12() {
      var app = this;
      app.grid.init();
    }
  }
};

// node_modules/framework7/esm/components/calendar/calendar-class.js
function _defineProperties2(target, props) {
  for (var i = 0; i < props.length; i++) {
    var descriptor = props[i];
    descriptor.enumerable = descriptor.enumerable || false;
    descriptor.configurable = true;
    if ("value" in descriptor)
      descriptor.writable = true;
    Object.defineProperty(target, descriptor.key, descriptor);
  }
}
function _createClass2(Constructor, protoProps, staticProps) {
  if (protoProps)
    _defineProperties2(Constructor.prototype, protoProps);
  if (staticProps)
    _defineProperties2(Constructor, staticProps);
  return Constructor;
}
function _assertThisInitialized15(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose15(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf15(subClass, superClass);
}
function _setPrototypeOf15(o, p) {
  _setPrototypeOf15 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf15(o, p);
}
var Calendar = function(_Framework7Class) {
  _inheritsLoose15(Calendar2, _Framework7Class);
  function Calendar2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var calendar = _assertThisInitialized15(_this);
    calendar.params = extend({}, app.params.calendar, params);
    var $containerEl;
    if (calendar.params.containerEl) {
      $containerEl = dom7_default(calendar.params.containerEl);
      if ($containerEl.length === 0)
        return calendar || _assertThisInitialized15(_this);
    }
    var $inputEl;
    if (calendar.params.inputEl) {
      $inputEl = dom7_default(calendar.params.inputEl);
    }
    var isHorizontal = calendar.params.direction === "horizontal";
    var inverter = 1;
    if (isHorizontal) {
      inverter = app.rtl ? -1 : 1;
    }
    extend(calendar, {
      app,
      $containerEl,
      containerEl: $containerEl && $containerEl[0],
      inline: $containerEl && $containerEl.length > 0,
      $inputEl,
      inputEl: $inputEl && $inputEl[0],
      initialized: false,
      opened: false,
      url: calendar.params.url,
      isHorizontal,
      inverter,
      animating: false,
      allowTouchMove: true,
      hasTimePicker: calendar.params.timePicker && !calendar.params.rangePicker && !calendar.params.multiple
    });
    calendar.dayFormatter = function(date) {
      var formatter = new Intl.DateTimeFormat(calendar.params.locale, {
        day: "numeric"
      });
      return formatter.format(date).replace(/日/, "");
    };
    calendar.monthFormatter = function(date) {
      var formatter = new Intl.DateTimeFormat(calendar.params.locale, {
        month: "long"
      });
      return formatter.format(date);
    };
    calendar.yearFormatter = function(date) {
      var formatter = new Intl.DateTimeFormat(calendar.params.locale, {
        year: "numeric"
      });
      return formatter.format(date);
    };
    calendar.timeSelectorFormatter = function(date) {
      var formatter = new Intl.DateTimeFormat(calendar.params.locale, calendar.params.timePickerFormat);
      return formatter.format(date);
    };
    var timeFormatCheckDate = calendar.timeSelectorFormatter(new Date()).toLowerCase();
    calendar.is12HoursFormat = timeFormatCheckDate.indexOf("pm") >= 0 || timeFormatCheckDate.indexOf("am") >= 0;
    var _calendar$params = calendar.params, monthNames = _calendar$params.monthNames, monthNamesShort = _calendar$params.monthNamesShort, dayNames = _calendar$params.dayNames, dayNamesShort = _calendar$params.dayNamesShort;
    var _calendar$getIntlName = calendar.getIntlNames(), monthNamesIntl = _calendar$getIntlName.monthNamesIntl, monthNamesShortIntl = _calendar$getIntlName.monthNamesShortIntl, dayNamesIntl = _calendar$getIntlName.dayNamesIntl, dayNamesShortIntl = _calendar$getIntlName.dayNamesShortIntl;
    if (monthNames === "auto")
      monthNames = monthNamesIntl;
    if (monthNamesShort === "auto")
      monthNamesShort = monthNamesShortIntl;
    if (dayNames === "auto")
      dayNames = dayNamesIntl;
    if (dayNamesShort === "auto")
      dayNamesShort = dayNamesShortIntl;
    extend(calendar, {
      monthNames,
      monthNamesShort,
      dayNames,
      dayNamesShort
    });
    function onInputClick() {
      calendar.open();
    }
    function onInputFocus(e) {
      e.preventDefault();
    }
    function onInputClear() {
      calendar.setValue([]);
      if (calendar.opened) {
        calendar.update();
      }
    }
    function onHtmlClick(e) {
      var $targetEl = dom7_default(e.target);
      if (calendar.destroyed || !calendar.params)
        return;
      if (calendar.isPopover())
        return;
      if (!calendar.opened || calendar.closing)
        return;
      if ($targetEl.closest('[class*="backdrop"]').length)
        return;
      if ($inputEl && $inputEl.length > 0) {
        if ($targetEl[0] !== $inputEl[0] && $targetEl.closest(".sheet-modal, .calendar-modal").length === 0) {
          calendar.close();
        }
      } else if (dom7_default(e.target).closest(".sheet-modal, .calendar-modal").length === 0) {
        calendar.close();
      }
    }
    extend(calendar, {
      attachInputEvents: function attachInputEvents() {
        calendar.$inputEl.on("click", onInputClick);
        calendar.$inputEl.on("input:clear", onInputClear);
        if (calendar.params.inputReadOnly) {
          calendar.$inputEl.on("focus mousedown", onInputFocus);
          if (calendar.$inputEl[0]) {
            calendar.$inputEl[0].f7ValidateReadonly = true;
          }
        }
      },
      detachInputEvents: function detachInputEvents() {
        calendar.$inputEl.off("click", onInputClick);
        calendar.$inputEl.off("input:clear", onInputClear);
        if (calendar.params.inputReadOnly) {
          calendar.$inputEl.off("focus mousedown", onInputFocus);
          if (calendar.$inputEl[0]) {
            delete calendar.$inputEl[0].f7ValidateReadonly;
          }
        }
      },
      attachHtmlEvents: function attachHtmlEvents() {
        app.on("click", onHtmlClick);
      },
      detachHtmlEvents: function detachHtmlEvents() {
        app.off("click", onHtmlClick);
      }
    });
    calendar.attachCalendarEvents = function attachCalendarEvents() {
      var allowItemClick = true;
      var isTouched;
      var isMoved;
      var touchStartX;
      var touchStartY;
      var touchCurrentX;
      var touchCurrentY;
      var touchStartTime;
      var touchEndTime;
      var currentTranslate;
      var wrapperWidth;
      var wrapperHeight;
      var percentage;
      var touchesDiff;
      var isScrolling;
      var $el = calendar.$el, $wrapperEl = calendar.$wrapperEl;
      function handleTouchStart(e) {
        if (isMoved || isTouched)
          return;
        isTouched = true;
        touchStartX = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
        touchCurrentX = touchStartX;
        touchStartY = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
        touchCurrentY = touchStartY;
        touchStartTime = new Date().getTime();
        percentage = 0;
        allowItemClick = true;
        isScrolling = void 0;
        currentTranslate = calendar.monthsTranslate;
      }
      function handleTouchMove(e) {
        if (!isTouched)
          return;
        var isH = calendar.isHorizontal;
        touchCurrentX = e.type === "touchmove" ? e.targetTouches[0].pageX : e.pageX;
        touchCurrentY = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
        if (typeof isScrolling === "undefined") {
          isScrolling = !!(isScrolling || Math.abs(touchCurrentY - touchStartY) > Math.abs(touchCurrentX - touchStartX));
        }
        if (isH && isScrolling || !calendar.allowTouchMove) {
          isTouched = false;
          return;
        }
        e.preventDefault();
        if (calendar.animating) {
          isTouched = false;
          return;
        }
        allowItemClick = false;
        if (!isMoved) {
          isMoved = true;
          wrapperWidth = $wrapperEl[0].offsetWidth;
          wrapperHeight = $wrapperEl[0].offsetHeight;
          $wrapperEl.transition(0);
        }
        touchesDiff = isH ? touchCurrentX - touchStartX : touchCurrentY - touchStartY;
        percentage = touchesDiff / (isH ? wrapperWidth : wrapperHeight);
        currentTranslate = (calendar.monthsTranslate * calendar.inverter + percentage) * 100;
        $wrapperEl.transform("translate3d(" + (isH ? currentTranslate : 0) + "%, " + (isH ? 0 : currentTranslate) + "%, 0)");
      }
      function handleTouchEnd() {
        if (!isTouched || !isMoved) {
          isTouched = false;
          isMoved = false;
          return;
        }
        isTouched = false;
        isMoved = false;
        touchEndTime = new Date().getTime();
        if (touchEndTime - touchStartTime < 300) {
          if (Math.abs(touchesDiff) < 10) {
            calendar.resetMonth();
          } else if (touchesDiff >= 10) {
            if (app.rtl)
              calendar.nextMonth();
            else
              calendar.prevMonth();
          } else if (app.rtl)
            calendar.prevMonth();
          else
            calendar.nextMonth();
        } else if (percentage <= -0.5) {
          if (app.rtl)
            calendar.prevMonth();
          else
            calendar.nextMonth();
        } else if (percentage >= 0.5) {
          if (app.rtl)
            calendar.nextMonth();
          else
            calendar.prevMonth();
        } else {
          calendar.resetMonth();
        }
        setTimeout(function() {
          allowItemClick = true;
        }, 100);
      }
      function handleDayClick(e) {
        if (!allowItemClick)
          return;
        var $dayEl = dom7_default(e.target).parents(".calendar-day");
        if ($dayEl.length === 0 && dom7_default(e.target).hasClass("calendar-day")) {
          $dayEl = dom7_default(e.target);
        }
        if ($dayEl.length === 0)
          return;
        if ($dayEl.hasClass("calendar-day-disabled"))
          return;
        if (!calendar.params.rangePicker) {
          if ($dayEl.hasClass("calendar-day-next"))
            calendar.nextMonth();
          if ($dayEl.hasClass("calendar-day-prev"))
            calendar.prevMonth();
        }
        var dateYear = parseInt($dayEl.attr("data-year"), 10);
        var dateMonth = parseInt($dayEl.attr("data-month"), 10);
        var dateDay = parseInt($dayEl.attr("data-day"), 10);
        calendar.emit("local::dayClick calendarDayClick", calendar, $dayEl[0], dateYear, dateMonth, dateDay);
        if (!$dayEl.hasClass("calendar-day-selected") || calendar.params.multiple || calendar.params.rangePicker) {
          var valueToAdd = new Date(dateYear, dateMonth, dateDay, 0, 0, 0);
          if (calendar.hasTimePicker) {
            if (calendar.value && calendar.value[0]) {
              valueToAdd.setHours(calendar.value[0].getHours(), calendar.value[0].getMinutes());
            } else {
              valueToAdd.setHours(new Date().getHours(), new Date().getMinutes());
            }
          }
          calendar.addValue(valueToAdd);
        }
        if (calendar.params.closeOnSelect) {
          if (calendar.params.rangePicker && calendar.value.length === 2 || !calendar.params.rangePicker) {
            calendar.close();
          }
        }
      }
      function onNextMonthClick() {
        calendar.nextMonth();
      }
      function onPrevMonthClick() {
        calendar.prevMonth();
      }
      function onNextYearClick() {
        calendar.nextYear();
      }
      function onPrevYearClick() {
        calendar.prevYear();
      }
      function onMonthSelectorClick() {
        $el.append(calendar.renderMonthPicker());
      }
      function onMonthSelectorItemClick() {
        var $clickedEl = dom7_default(this);
        if ($clickedEl.hasClass("calendar-month-picker-item-current")) {
          $el.find(".calendar-month-picker").remove();
          return;
        }
        $el.find(".calendar-month-picker-item-current").add($clickedEl).toggleClass("calendar-month-picker-item-current");
        var index2 = $clickedEl.index();
        var localeMonthIndex = parseInt(calendar.$el.find(".calendar-month-current").attr("data-locale-month"), 10);
        var monthIndex = calendar.currentMonth;
        var diff = localeMonthIndex - monthIndex;
        var diffIndex = index2 - diff;
        calendar.setYearMonth(calendar.currentYear, diffIndex, 0);
        setTimeout(function() {
          $el.find(".calendar-month-picker").remove();
        }, 200);
      }
      function onYearSelectorClick() {
        $el.append(calendar.renderYearPicker());
        var $currentEl = $el.find(".calendar-year-picker-item-current");
        var $yearPickerEl = $el.find(".calendar-year-picker");
        if (!$currentEl || !$currentEl.length)
          return;
        $yearPickerEl.scrollTop($currentEl[0].offsetTop - $yearPickerEl[0].offsetHeight / 2 + $currentEl[0].offsetHeight / 2);
      }
      function onYearSelectorItemClick() {
        var $clickedEl = dom7_default(this);
        if ($clickedEl.hasClass("calendar-year-picker-item-current")) {
          $el.find(".calendar-year-picker").remove();
          return;
        }
        $el.find(".calendar-year-picker-item-current").add($clickedEl).toggleClass("calendar-year-picker-item-current");
        var year = parseInt($clickedEl.attr("data-year"), 10);
        calendar.setYearMonth(year, void 0, 0);
        setTimeout(function() {
          $el.find(".calendar-year-picker").remove();
        }, 200);
      }
      function onTimeSelectorClick() {
        calendar.openTimePicker();
      }
      function onTimePickerCloseClick() {
        calendar.closeTimePicker();
      }
      var passiveListener = app.touchEvents.start === "touchstart" && getSupport().passiveListener ? {
        passive: true,
        capture: false
      } : false;
      $el.find(".calendar-prev-month-button").on("click", onPrevMonthClick);
      $el.find(".calendar-next-month-button").on("click", onNextMonthClick);
      $el.find(".calendar-prev-year-button").on("click", onPrevYearClick);
      $el.find(".calendar-next-year-button").on("click", onNextYearClick);
      if (calendar.params.monthPicker) {
        $el.find(".current-month-value").on("click", onMonthSelectorClick);
        $el.on("click", ".calendar-month-picker-item", onMonthSelectorItemClick);
      }
      if (calendar.params.yearPicker) {
        $el.find(".current-year-value").on("click", onYearSelectorClick);
        $el.on("click", ".calendar-year-picker-item", onYearSelectorItemClick);
      }
      if (calendar.hasTimePicker) {
        $el.find(".calendar-time-selector a").on("click", onTimeSelectorClick);
        $el.on("click", ".calendar-time-picker-close", onTimePickerCloseClick);
      }
      $wrapperEl.on("click", handleDayClick);
      if (calendar.params.touchMove) {
        $wrapperEl.on(app.touchEvents.start, handleTouchStart, passiveListener);
        app.on("touchmove:active", handleTouchMove);
        app.on("touchend:passive", handleTouchEnd);
      }
      calendar.detachCalendarEvents = function detachCalendarEvents() {
        $el.find(".calendar-prev-month-button").off("click", onPrevMonthClick);
        $el.find(".calendar-next-month-button").off("click", onNextMonthClick);
        $el.find(".calendar-prev-year-button").off("click", onPrevYearClick);
        $el.find(".calendar-next-year-button").off("click", onNextYearClick);
        if (calendar.params.monthPicker) {
          $el.find(".current-month-value").off("click", onMonthSelectorClick);
          $el.off("click", ".calendar-month-picker-item", onMonthSelectorItemClick);
        }
        if (calendar.params.yearPicker) {
          $el.find(".current-year-value").off("click", onYearSelectorClick);
          $el.off("click", ".calendar-year-picker-item", onYearSelectorItemClick);
        }
        if (calendar.hasTimePicker) {
          $el.find(".calendar-time-selector a").off("click", onTimeSelectorClick);
          $el.off("click", ".calendar-time-picker-close", onTimePickerCloseClick);
        }
        $wrapperEl.off("click", handleDayClick);
        if (calendar.params.touchMove) {
          $wrapperEl.off(app.touchEvents.start, handleTouchStart, passiveListener);
          app.off("touchmove:active", handleTouchMove);
          app.off("touchend:passive", handleTouchEnd);
        }
      };
    };
    calendar.init();
    return calendar || _assertThisInitialized15(_this);
  }
  var _proto = Calendar2.prototype;
  _proto.getIntlNames = function getIntlNames() {
    var calendar = this;
    var locale = calendar.params.locale;
    var monthNamesIntl = [];
    var monthNamesShortIntl = [];
    var dayNamesIntl = [];
    var dayNamesShortIntl = [];
    var formatterMonthNames = new Intl.DateTimeFormat(locale, {
      month: "long"
    });
    var formatterMonthNamesShort = new Intl.DateTimeFormat(locale, {
      month: "short"
    });
    var formatterDayNames = new Intl.DateTimeFormat(locale, {
      weekday: "long"
    });
    var formatterDayNamesShort = new Intl.DateTimeFormat(locale, {
      weekday: "short"
    });
    var year;
    var yearStarted;
    var yearEnded;
    for (var i = 0; i < 24; i += 1) {
      var date = new Date().setMonth(i, 1);
      var currentYear = calendar.yearFormatter(date);
      if (year && currentYear !== year) {
        if (yearStarted)
          yearEnded = true;
        yearStarted = true;
        year = currentYear;
      }
      if (!year) {
        year = currentYear;
      }
      if (yearStarted && year === currentYear && !yearEnded) {
        monthNamesIntl.push(formatterMonthNames.format(date));
        monthNamesShortIntl.push(formatterMonthNamesShort.format(date));
      }
    }
    var weekDay = new Date().getDay();
    for (var _i = 0; _i < 7; _i += 1) {
      var _date = new Date().getTime() + (_i - weekDay) * 24 * 60 * 60 * 1e3;
      dayNamesIntl.push(formatterDayNames.format(_date));
      dayNamesShortIntl.push(formatterDayNamesShort.format(_date));
    }
    return {
      monthNamesIntl,
      monthNamesShortIntl,
      dayNamesIntl,
      dayNamesShortIntl
    };
  };
  _proto.normalizeDate = function normalizeDate(date) {
    var calendar = this;
    var d = new Date(date);
    if (calendar.hasTimePicker) {
      return new Date(d.getFullYear(), d.getMonth(), d.getDate(), d.getHours(), d.getMinutes());
    }
    return new Date(d.getFullYear(), d.getMonth(), d.getDate());
  };
  _proto.normalizeValues = function normalizeValues(values) {
    var calendar = this;
    var newValues = [];
    if (values && Array.isArray(values)) {
      newValues = values.map(function(val) {
        return calendar.normalizeDate(val);
      });
    }
    return newValues;
  };
  _proto.initInput = function initInput() {
    var calendar = this;
    if (!calendar.$inputEl)
      return;
    if (calendar.params.inputReadOnly)
      calendar.$inputEl.prop("readOnly", true);
  };
  _proto.isPopover = function isPopover() {
    var calendar = this;
    var app = calendar.app, modal = calendar.modal, params = calendar.params;
    var device2 = getDevice();
    if (params.openIn === "sheet")
      return false;
    if (modal && modal.type !== "popover")
      return false;
    if (!calendar.inline && calendar.inputEl) {
      if (params.openIn === "popover")
        return true;
      if (device2.ios) {
        return !!device2.ipad;
      }
      if (app.width >= 768) {
        return true;
      }
      if (device2.desktop && app.theme === "aurora") {
        return true;
      }
    }
    return false;
  };
  _proto.formatDate = function formatDate(d) {
    var calendar = this;
    var date = new Date(d);
    var year = date.getFullYear();
    var month = date.getMonth();
    var month1 = month + 1;
    var day = date.getDate();
    var weekDay = date.getDay();
    var monthNames = calendar.monthNames, monthNamesShort = calendar.monthNamesShort, dayNames = calendar.dayNames, dayNamesShort = calendar.dayNamesShort;
    var _calendar$params2 = calendar.params, dateFormat = _calendar$params2.dateFormat, locale = _calendar$params2.locale;
    function twoDigits(number) {
      return number < 10 ? "0" + number : number;
    }
    if (typeof dateFormat === "string") {
      var tokens = {
        yyyy: year,
        yy: String(year).substring(2),
        mm: twoDigits(month1),
        m: month1,
        MM: monthNames[month],
        M: monthNamesShort[month],
        dd: twoDigits(day),
        d: day,
        DD: dayNames[weekDay],
        D: dayNamesShort[weekDay]
      };
      if (calendar.params.timePicker) {
        var hours = date.getHours();
        var minutes = date.getMinutes();
        var seconds = date.getSeconds();
        var hours12 = hours;
        if (hours > 12)
          hours12 = hours - 12;
        if (hours === 0)
          hours12 = 12;
        var a = hours >= 12 && hours !== 0 ? "pm" : "am";
        Object.assign(tokens, {
          HH: twoDigits(hours),
          H: hours,
          hh: twoDigits(hours12),
          h: hours12,
          ss: twoDigits(seconds),
          s: seconds,
          ":mm": twoDigits(minutes),
          ":m": minutes,
          a,
          A: a.toUpperCase()
        });
      }
      var regexp = new RegExp(Object.keys(tokens).map(function(t) {
        return "(" + t + ")";
      }).join("|"), "g");
      return dateFormat.replace(regexp, function(token) {
        if (token in tokens)
          return tokens[token];
        return token;
      });
    }
    if (typeof dateFormat === "function") {
      return dateFormat(date);
    }
    var formatter = new Intl.DateTimeFormat(locale, dateFormat);
    return formatter.format(date);
  };
  _proto.formatValue = function formatValue() {
    var calendar = this;
    var value = calendar.value;
    if (calendar.params.formatValue) {
      return calendar.params.formatValue.call(calendar, value);
    }
    return value.map(function(v) {
      return calendar.formatDate(v);
    }).join(calendar.params.rangePicker ? " - " : ", ");
  };
  _proto.addValue = function addValue(newValue) {
    var calendar = this;
    var _calendar$params3 = calendar.params, multiple = _calendar$params3.multiple, rangePicker = _calendar$params3.rangePicker, rangePickerMinDays = _calendar$params3.rangePickerMinDays, rangePickerMaxDays = _calendar$params3.rangePickerMaxDays;
    if (multiple) {
      if (!calendar.value)
        calendar.value = [];
      var inValuesIndex;
      for (var i = 0; i < calendar.value.length; i += 1) {
        if (new Date(newValue).getTime() === new Date(calendar.value[i]).getTime()) {
          inValuesIndex = i;
        }
      }
      if (typeof inValuesIndex === "undefined") {
        calendar.value.push(newValue);
      } else {
        calendar.value.splice(inValuesIndex, 1);
      }
      calendar.updateValue();
    } else if (rangePicker) {
      if (!calendar.value)
        calendar.value = [];
      if (calendar.value.length === 2 || calendar.value.length === 0) {
        calendar.value = [];
      }
      if (calendar.value.length === 0 || Math.abs(calendar.value[0].getTime() - newValue.getTime()) >= (rangePickerMinDays - 1) * 60 * 60 * 24 * 1e3 && (rangePickerMaxDays === 0 || Math.abs(calendar.value[0].getTime() - newValue.getTime()) <= (rangePickerMaxDays - 1) * 60 * 60 * 24 * 1e3))
        calendar.value.push(newValue);
      else
        calendar.value = [];
      calendar.value.sort(function(a, b) {
        return a - b;
      });
      calendar.updateValue();
    } else {
      calendar.value = [newValue];
      calendar.updateValue();
    }
  };
  _proto.setValue = function setValue(values) {
    var calendar = this;
    var currentValue = calendar.value;
    if (Array.isArray(currentValue) && Array.isArray(values) && currentValue.length === values.length) {
      var equal = true;
      currentValue.forEach(function(v, index2) {
        if (v !== values[index2])
          equal = false;
      });
      if (equal)
        return;
    }
    calendar.value = values;
    calendar.updateValue();
  };
  _proto.getValue = function getValue() {
    var calendar = this;
    return calendar.value;
  };
  _proto.updateValue = function updateValue(onlyHeader) {
    var calendar = this;
    var $el = calendar.$el, $wrapperEl = calendar.$wrapperEl, $inputEl = calendar.$inputEl, value = calendar.value, params = calendar.params;
    var i;
    if ($el && $el.length > 0) {
      $wrapperEl.find(".calendar-day-selected").removeClass("calendar-day-selected calendar-day-selected-range calendar-day-selected-left calendar-day-selected-right");
      var valueDate;
      if (params.rangePicker && value.length === 2) {
        var leftDate = new Date(value[0]).getTime();
        var rightDate = new Date(value[1]).getTime();
        for (i = leftDate; i <= rightDate; i += 24 * 60 * 60 * 1e3) {
          valueDate = new Date(i);
          var addClass2 = "calendar-day-selected";
          if (leftDate !== rightDate) {
            if (i !== leftDate && i !== rightDate) {
              addClass2 += " calendar-day-selected-range";
            }
            if (i === leftDate) {
              addClass2 += " calendar-day-selected-left";
            }
            if (i === rightDate) {
              addClass2 += " calendar-day-selected-right";
            }
          }
          $wrapperEl.find('.calendar-day[data-date="' + valueDate.getFullYear() + "-" + valueDate.getMonth() + "-" + valueDate.getDate() + '"]').addClass(addClass2);
        }
        valueDate = new Date(leftDate);
        $wrapperEl.find('.calendar-day[data-date="' + valueDate.getFullYear() + "-" + valueDate.getMonth() + "-" + valueDate.getDate() + '"]').removeClass("calendar-day-selected-range").addClass("calendar-day-selected calendar-day-selected-left");
        valueDate = new Date(rightDate);
        $wrapperEl.find('.calendar-day[data-date="' + valueDate.getFullYear() + "-" + valueDate.getMonth() + "-" + valueDate.getDate() + '"]').removeClass("calendar-day-selected-range").addClass("calendar-day-selected calendar-day-selected-right");
      } else {
        for (i = 0; i < calendar.value.length; i += 1) {
          valueDate = new Date(value[i]);
          $wrapperEl.find('.calendar-day[data-date="' + valueDate.getFullYear() + "-" + valueDate.getMonth() + "-" + valueDate.getDate() + '"]').addClass("calendar-day-selected");
        }
      }
    }
    if (!onlyHeader) {
      calendar.emit("local::change calendarChange", calendar, value);
    }
    if ($el && $el.length > 0 && calendar.hasTimePicker) {
      $el.find(".calendar-time-selector a").text(value && value.length ? calendar.timeSelectorFormatter(value[0]) : calendar.params.timePickerPlaceholder);
    }
    if ($inputEl && $inputEl.length || params.header) {
      var inputValue = calendar.formatValue(value);
      if (params.header && $el && $el.length) {
        $el.find(".calendar-selected-date").text(inputValue);
      }
      if ($inputEl && $inputEl.length && !onlyHeader) {
        $inputEl.val(inputValue);
        $inputEl.trigger("change");
      }
    }
  };
  _proto.updateCurrentMonthYear = function updateCurrentMonthYear(dir) {
    var calendar = this;
    var $months = calendar.$months, $el = calendar.$el, monthNames = calendar.monthNames;
    var currentLocaleMonth;
    var currentLocaleYear;
    if (typeof dir === "undefined") {
      calendar.currentMonth = parseInt($months.eq(1).attr("data-month"), 10);
      calendar.currentYear = parseInt($months.eq(1).attr("data-year"), 10);
      currentLocaleMonth = $months.eq(1).attr("data-locale-month");
      currentLocaleYear = $months.eq(1).attr("data-locale-year");
    } else {
      calendar.currentMonth = parseInt($months.eq(dir === "next" ? $months.length - 1 : 0).attr("data-month"), 10);
      calendar.currentYear = parseInt($months.eq(dir === "next" ? $months.length - 1 : 0).attr("data-year"), 10);
      currentLocaleMonth = $months.eq(dir === "next" ? $months.length - 1 : 0).attr("data-locale-month");
      currentLocaleYear = $months.eq(dir === "next" ? $months.length - 1 : 0).attr("data-locale-year");
    }
    $el.find(".current-month-value").text(monthNames[currentLocaleMonth]);
    $el.find(".current-year-value").text(currentLocaleYear);
  };
  _proto.update = function update22() {
    var calendar = this;
    var currentYear = calendar.currentYear, currentMonth = calendar.currentMonth, $wrapperEl = calendar.$wrapperEl;
    var currentDate = new Date(currentYear, currentMonth);
    var prevMonthHtml = calendar.renderMonth(currentDate, "prev");
    var currentMonthHtml = calendar.renderMonth(currentDate);
    var nextMonthHtml = calendar.renderMonth(currentDate, "next");
    $wrapperEl.transition(0).html("" + prevMonthHtml + currentMonthHtml + nextMonthHtml).transform("translate3d(0,0,0)");
    calendar.$months = $wrapperEl.find(".calendar-month");
    calendar.monthsTranslate = 0;
    calendar.setMonthsTranslate();
    calendar.$months.each(function(monthEl) {
      calendar.emit("local::monthAdd calendarMonthAdd", monthEl);
    });
  };
  _proto.onMonthChangeStart = function onMonthChangeStart(dir) {
    var calendar = this;
    var $months = calendar.$months, currentYear = calendar.currentYear, currentMonth = calendar.currentMonth;
    calendar.updateCurrentMonthYear(dir);
    $months.removeClass("calendar-month-current calendar-month-prev calendar-month-next");
    var currentIndex = dir === "next" ? $months.length - 1 : 0;
    $months.eq(currentIndex).addClass("calendar-month-current");
    $months.eq(dir === "next" ? currentIndex - 1 : currentIndex + 1).addClass(dir === "next" ? "calendar-month-prev" : "calendar-month-next");
    calendar.emit("local::monthYearChangeStart calendarMonthYearChangeStart", calendar, currentYear, currentMonth);
  };
  _proto.onMonthChangeEnd = function onMonthChangeEnd(dir, rebuildBoth) {
    var calendar = this;
    var currentYear = calendar.currentYear, currentMonth = calendar.currentMonth, $wrapperEl = calendar.$wrapperEl, monthsTranslate = calendar.monthsTranslate;
    calendar.animating = false;
    var nextMonthHtml;
    var prevMonthHtml;
    var currentMonthHtml;
    $wrapperEl.find(".calendar-month:not(.calendar-month-prev):not(.calendar-month-current):not(.calendar-month-next)").remove();
    if (typeof dir === "undefined") {
      dir = "next";
      rebuildBoth = true;
    }
    if (!rebuildBoth) {
      currentMonthHtml = calendar.renderMonth(new Date(currentYear, currentMonth), dir);
    } else {
      $wrapperEl.find(".calendar-month-next, .calendar-month-prev").remove();
      prevMonthHtml = calendar.renderMonth(new Date(currentYear, currentMonth), "prev");
      nextMonthHtml = calendar.renderMonth(new Date(currentYear, currentMonth), "next");
    }
    if (dir === "next" || rebuildBoth) {
      $wrapperEl.append(currentMonthHtml || nextMonthHtml);
    }
    if (dir === "prev" || rebuildBoth) {
      $wrapperEl.prepend(currentMonthHtml || prevMonthHtml);
    }
    var $months = $wrapperEl.find(".calendar-month");
    calendar.$months = $months;
    calendar.setMonthsTranslate(monthsTranslate);
    calendar.emit("local::monthAdd calendarMonthAdd", calendar, dir === "next" ? $months.eq($months.length - 1)[0] : $months.eq(0)[0]);
    calendar.emit("local::monthYearChangeEnd calendarMonthYearChangeEnd", calendar, currentYear, currentMonth);
  };
  _proto.setMonthsTranslate = function setMonthsTranslate(translate) {
    var calendar = this;
    var $months = calendar.$months, isH = calendar.isHorizontal, inverter = calendar.inverter;
    translate = translate || calendar.monthsTranslate || 0;
    if (typeof calendar.monthsTranslate === "undefined") {
      calendar.monthsTranslate = translate;
    }
    $months.removeClass("calendar-month-current calendar-month-prev calendar-month-next");
    var prevMonthTranslate = -(translate + 1) * 100 * inverter;
    var currentMonthTranslate = -translate * 100 * inverter;
    var nextMonthTranslate = -(translate - 1) * 100 * inverter;
    $months.eq(0).transform("translate3d(" + (isH ? prevMonthTranslate : 0) + "%, " + (isH ? 0 : prevMonthTranslate) + "%, 0)").addClass("calendar-month-prev");
    $months.eq(1).transform("translate3d(" + (isH ? currentMonthTranslate : 0) + "%, " + (isH ? 0 : currentMonthTranslate) + "%, 0)").addClass("calendar-month-current");
    $months.eq(2).transform("translate3d(" + (isH ? nextMonthTranslate : 0) + "%, " + (isH ? 0 : nextMonthTranslate) + "%, 0)").addClass("calendar-month-next");
  };
  _proto.nextMonth = function nextMonth(transition2) {
    var calendar = this;
    var params = calendar.params, $wrapperEl = calendar.$wrapperEl, inverter = calendar.inverter, isH = calendar.isHorizontal;
    if (typeof transition2 === "undefined" || typeof transition2 === "object") {
      transition2 = "";
      if (!params.animate)
        transition2 = 0;
    }
    var nextMonth2 = parseInt(calendar.$months.eq(calendar.$months.length - 1).attr("data-month"), 10);
    var nextYear = parseInt(calendar.$months.eq(calendar.$months.length - 1).attr("data-year"), 10);
    var nextDate = new Date(nextYear, nextMonth2);
    var nextDateTime = nextDate.getTime();
    var transitionEndCallback = !calendar.animating;
    if (params.maxDate) {
      if (nextDateTime > new Date(params.maxDate).getTime()) {
        calendar.resetMonth();
        return;
      }
    }
    calendar.monthsTranslate -= 1;
    if (nextMonth2 === calendar.currentMonth) {
      var nextMonthTranslate = -calendar.monthsTranslate * 100 * inverter;
      var nextMonthHtml = dom7_default(calendar.renderMonth(nextDateTime, "next")).transform("translate3d(" + (isH ? nextMonthTranslate : 0) + "%, " + (isH ? 0 : nextMonthTranslate) + "%, 0)").addClass("calendar-month-next");
      $wrapperEl.append(nextMonthHtml[0]);
      calendar.$months = $wrapperEl.find(".calendar-month");
      calendar.emit("local::monthAdd calendarMonthAdd", calendar.$months.eq(calendar.$months.length - 1)[0]);
    }
    calendar.animating = true;
    calendar.onMonthChangeStart("next");
    var translate = calendar.monthsTranslate * 100 * inverter;
    $wrapperEl.transition(transition2).transform("translate3d(" + (isH ? translate : 0) + "%, " + (isH ? 0 : translate) + "%, 0)");
    if (transitionEndCallback) {
      $wrapperEl.transitionEnd(function() {
        calendar.onMonthChangeEnd("next");
      });
    }
    if (!params.animate) {
      calendar.onMonthChangeEnd("next");
    }
  };
  _proto.prevMonth = function prevMonth(transition2) {
    var calendar = this;
    var params = calendar.params, $wrapperEl = calendar.$wrapperEl, inverter = calendar.inverter, isH = calendar.isHorizontal;
    if (typeof transition2 === "undefined" || typeof transition2 === "object") {
      transition2 = "";
      if (!params.animate)
        transition2 = 0;
    }
    var prevMonth2 = parseInt(calendar.$months.eq(0).attr("data-month"), 10);
    var prevYear = parseInt(calendar.$months.eq(0).attr("data-year"), 10);
    var prevDate = new Date(prevYear, prevMonth2 + 1, -1);
    var prevDateTime = prevDate.getTime();
    var transitionEndCallback = !calendar.animating;
    if (params.minDate) {
      var minDate = new Date(params.minDate);
      minDate = new Date(minDate.getFullYear(), minDate.getMonth(), 1);
      if (prevDateTime < minDate.getTime()) {
        calendar.resetMonth();
        return;
      }
    }
    calendar.monthsTranslate += 1;
    if (prevMonth2 === calendar.currentMonth) {
      var prevMonthTranslate = -calendar.monthsTranslate * 100 * inverter;
      var prevMonthHtml = dom7_default(calendar.renderMonth(prevDateTime, "prev")).transform("translate3d(" + (isH ? prevMonthTranslate : 0) + "%, " + (isH ? 0 : prevMonthTranslate) + "%, 0)").addClass("calendar-month-prev");
      $wrapperEl.prepend(prevMonthHtml[0]);
      calendar.$months = $wrapperEl.find(".calendar-month");
      calendar.emit("local::monthAdd calendarMonthAdd", calendar.$months.eq(0)[0]);
    }
    calendar.animating = true;
    calendar.onMonthChangeStart("prev");
    var translate = calendar.monthsTranslate * 100 * inverter;
    $wrapperEl.transition(transition2).transform("translate3d(" + (isH ? translate : 0) + "%, " + (isH ? 0 : translate) + "%, 0)");
    if (transitionEndCallback) {
      $wrapperEl.transitionEnd(function() {
        calendar.onMonthChangeEnd("prev");
      });
    }
    if (!params.animate) {
      calendar.onMonthChangeEnd("prev");
    }
  };
  _proto.resetMonth = function resetMonth(transition2) {
    if (transition2 === void 0) {
      transition2 = "";
    }
    var calendar = this;
    var $wrapperEl = calendar.$wrapperEl, inverter = calendar.inverter, isH = calendar.isHorizontal, monthsTranslate = calendar.monthsTranslate;
    var translate = monthsTranslate * 100 * inverter;
    $wrapperEl.transition(transition2).transform("translate3d(" + (isH ? translate : 0) + "%, " + (isH ? 0 : translate) + "%, 0)");
  };
  _proto.setYearMonth = function setYearMonth(year, month, transition2) {
    var calendar = this;
    var params = calendar.params, isH = calendar.isHorizontal, $wrapperEl = calendar.$wrapperEl, inverter = calendar.inverter;
    if (typeof year === "undefined")
      year = calendar.currentYear;
    if (typeof month === "undefined")
      month = calendar.currentMonth;
    if (typeof transition2 === "undefined" || typeof transition2 === "object") {
      transition2 = "";
      if (!params.animate)
        transition2 = 0;
    }
    var targetDate;
    if (year < calendar.currentYear) {
      targetDate = new Date(year, month + 1, -1).getTime();
    } else {
      targetDate = new Date(year, month).getTime();
    }
    if (params.maxDate && targetDate > new Date(params.maxDate).getTime()) {
      return false;
    }
    if (params.minDate) {
      var minDate = new Date(params.minDate);
      minDate = new Date(minDate.getFullYear(), minDate.getMonth(), 1);
      if (targetDate < minDate.getTime()) {
        return false;
      }
    }
    var currentDate = new Date(calendar.currentYear, calendar.currentMonth).getTime();
    var dir = targetDate > currentDate ? "next" : "prev";
    var newMonthHTML = calendar.renderMonth(new Date(year, month));
    calendar.monthsTranslate = calendar.monthsTranslate || 0;
    var prevTranslate = calendar.monthsTranslate;
    var monthTranslate;
    var transitionEndCallback = !calendar.animating && transition2 !== 0;
    if (targetDate > currentDate) {
      calendar.monthsTranslate -= 1;
      if (!calendar.animating)
        calendar.$months.eq(calendar.$months.length - 1).remove();
      $wrapperEl.append(newMonthHTML);
      calendar.$months = $wrapperEl.find(".calendar-month");
      monthTranslate = -(prevTranslate - 1) * 100 * inverter;
      calendar.$months.eq(calendar.$months.length - 1).transform("translate3d(" + (isH ? monthTranslate : 0) + "%, " + (isH ? 0 : monthTranslate) + "%, 0)").addClass("calendar-month-next");
    } else {
      calendar.monthsTranslate += 1;
      if (!calendar.animating)
        calendar.$months.eq(0).remove();
      $wrapperEl.prepend(newMonthHTML);
      calendar.$months = $wrapperEl.find(".calendar-month");
      monthTranslate = -(prevTranslate + 1) * 100 * inverter;
      calendar.$months.eq(0).transform("translate3d(" + (isH ? monthTranslate : 0) + "%, " + (isH ? 0 : monthTranslate) + "%, 0)").addClass("calendar-month-prev");
    }
    calendar.emit("local::monthAdd calendarMonthAdd", dir === "next" ? calendar.$months.eq(calendar.$months.length - 1)[0] : calendar.$months.eq(0)[0]);
    calendar.animating = true;
    calendar.onMonthChangeStart(dir);
    var wrapperTranslate = calendar.monthsTranslate * 100 * inverter;
    $wrapperEl.transition(transition2).transform("translate3d(" + (isH ? wrapperTranslate : 0) + "%, " + (isH ? 0 : wrapperTranslate) + "%, 0)");
    if (transitionEndCallback) {
      $wrapperEl.transitionEnd(function() {
        calendar.onMonthChangeEnd(dir, true);
      });
    }
    if (!params.animate || transition2 === 0) {
      calendar.onMonthChangeEnd(dir, true);
    }
  };
  _proto.nextYear = function nextYear() {
    var calendar = this;
    calendar.setYearMonth(calendar.currentYear + 1);
  };
  _proto.prevYear = function prevYear() {
    var calendar = this;
    calendar.setYearMonth(calendar.currentYear - 1);
  };
  _proto.dateInRange = function dateInRange(dayDate, range) {
    var match = false;
    var i;
    if (!range)
      return false;
    if (Array.isArray(range)) {
      for (i = 0; i < range.length; i += 1) {
        if (range[i].from || range[i].to) {
          if (range[i].from && range[i].to) {
            if (dayDate <= new Date(range[i].to).getTime() && dayDate >= new Date(range[i].from).getTime()) {
              match = true;
            }
          } else if (range[i].from) {
            if (dayDate >= new Date(range[i].from).getTime()) {
              match = true;
            }
          } else if (range[i].to) {
            if (dayDate <= new Date(range[i].to).getTime()) {
              match = true;
            }
          }
        } else if (range[i].date) {
          if (dayDate === new Date(range[i].date).getTime()) {
            match = true;
          }
        } else if (dayDate === new Date(range[i]).getTime()) {
          match = true;
        }
      }
    } else if (range.from || range.to) {
      if (range.from && range.to) {
        if (dayDate <= new Date(range.to).getTime() && dayDate >= new Date(range.from).getTime()) {
          match = true;
        }
      } else if (range.from) {
        if (dayDate >= new Date(range.from).getTime()) {
          match = true;
        }
      } else if (range.to) {
        if (dayDate <= new Date(range.to).getTime()) {
          match = true;
        }
      }
    } else if (range.date) {
      match = dayDate === new Date(range.date).getTime();
    } else if (typeof range === "function") {
      match = range(new Date(dayDate));
    }
    return match;
  };
  _proto.daysInMonth = function daysInMonth(date) {
    var d = new Date(date);
    return new Date(d.getFullYear(), d.getMonth() + 1, 0).getDate();
  };
  _proto.renderMonths = function renderMonths(date) {
    var calendar = this;
    if (calendar.params.renderMonths) {
      return calendar.params.renderMonths.call(calendar, date);
    }
    return jsx_default2("div", {
      class: "calendar-months-wrapper"
    }, calendar.renderMonth(date, "prev"), calendar.renderMonth(date), calendar.renderMonth(date, "next"));
  };
  _proto.renderMonth = function renderMonth(d, offset2) {
    var calendar = this;
    var params = calendar.params, value = calendar.value;
    if (params.renderMonth) {
      return params.renderMonth.call(calendar, d, offset2);
    }
    var date = new Date(d);
    var year = date.getFullYear();
    var month = date.getMonth();
    var localeMonth = calendar.monthNames.indexOf(calendar.monthFormatter(date));
    if (localeMonth < 0)
      localeMonth = month;
    var localeYear = calendar.yearFormatter(date);
    if (offset2 === "next") {
      if (month === 11)
        date = new Date(year + 1, 0);
      else
        date = new Date(year, month + 1, 1);
    }
    if (offset2 === "prev") {
      if (month === 0)
        date = new Date(year - 1, 11);
      else
        date = new Date(year, month - 1, 1);
    }
    if (offset2 === "next" || offset2 === "prev") {
      month = date.getMonth();
      year = date.getFullYear();
      localeMonth = calendar.monthNames.indexOf(calendar.monthFormatter(date));
      if (localeMonth < 0)
        localeMonth = month;
      localeYear = calendar.yearFormatter(date);
    }
    var currentValues = [];
    var today = new Date().setHours(0, 0, 0, 0);
    var minDate = params.minDate ? new Date(params.minDate).getTime() : null;
    var maxDate = params.maxDate ? new Date(params.maxDate).getTime() : null;
    var rows = 6;
    var cols = 7;
    var daysInPrevMonth = calendar.daysInMonth(new Date(date.getFullYear(), date.getMonth()).getTime() - 10 * 24 * 60 * 60 * 1e3);
    var daysInMonth = calendar.daysInMonth(date);
    var minDayNumber = params.firstDay === 6 ? 0 : 1;
    var monthHtml = "";
    var dayIndex = 0 + (params.firstDay - 1);
    var disabled;
    var hasEvents;
    var firstDayOfMonthIndex = new Date(date.getFullYear(), date.getMonth()).getDay();
    if (firstDayOfMonthIndex === 0)
      firstDayOfMonthIndex = 7;
    if (value && value.length) {
      for (var i = 0; i < value.length; i += 1) {
        currentValues.push(new Date(value[i]).setHours(0, 0, 0, 0));
      }
    }
    for (var row = 1; row <= rows; row += 1) {
      var rowHtml = "";
      var _loop = function _loop2(col2) {
        dayIndex += 1;
        var dayDate = void 0;
        var dayNumber = dayIndex - firstDayOfMonthIndex;
        var addClass2 = "";
        if (row === 1 && col2 === 1 && dayNumber > minDayNumber && params.firstDay !== 1) {
          dayIndex -= 7;
          dayNumber = dayIndex - firstDayOfMonthIndex;
        }
        var weekDayIndex = col2 - 1 + params.firstDay > 6 ? col2 - 1 - 7 + params.firstDay : col2 - 1 + params.firstDay;
        if (dayNumber < 0) {
          dayNumber = daysInPrevMonth + dayNumber + 1;
          addClass2 += " calendar-day-prev";
          dayDate = new Date(month - 1 < 0 ? year - 1 : year, month - 1 < 0 ? 11 : month - 1, dayNumber).getTime();
        } else {
          dayNumber += 1;
          if (dayNumber > daysInMonth) {
            dayNumber -= daysInMonth;
            addClass2 += " calendar-day-next";
            dayDate = new Date(month + 1 > 11 ? year + 1 : year, month + 1 > 11 ? 0 : month + 1, dayNumber).getTime();
          } else {
            dayDate = new Date(year, month, dayNumber).getTime();
          }
        }
        if (dayDate === today)
          addClass2 += " calendar-day-today";
        if (params.rangePicker && currentValues.length === 2) {
          if (dayDate >= currentValues[0] && dayDate <= currentValues[1]) {
            addClass2 += " calendar-day-selected";
          }
          if (currentValues[0] !== currentValues[1]) {
            if (dayDate > currentValues[0] && dayDate < currentValues[1]) {
              addClass2 += " calendar-day-selected-range";
            }
            if (dayDate === currentValues[0]) {
              addClass2 += " calendar-day-selected-left";
            }
            if (dayDate === currentValues[1]) {
              addClass2 += " calendar-day-selected-right";
            }
          }
        } else if (currentValues.indexOf(dayDate) >= 0)
          addClass2 += " calendar-day-selected";
        if (params.weekendDays.indexOf(weekDayIndex) >= 0) {
          addClass2 += " calendar-day-weekend";
        }
        var eventsHtml = "";
        hasEvents = false;
        if (params.events) {
          if (calendar.dateInRange(dayDate, params.events)) {
            hasEvents = true;
          }
        }
        if (hasEvents) {
          addClass2 += " calendar-day-has-events";
          eventsHtml = '\n            <span class="calendar-day-events">\n              <span class="calendar-day-event"></span>\n            </span>\n          ';
          if (Array.isArray(params.events)) {
            var eventDots = [];
            params.events.forEach(function(ev) {
              var color = ev.color || "";
              if (eventDots.indexOf(color) < 0 && calendar.dateInRange(dayDate, ev)) {
                eventDots.push(color);
              }
            });
            eventsHtml = '\n              <span class="calendar-day-events">\n                ' + eventDots.map(function(color) {
              return ('\n                  <span class="calendar-day-event" style="' + (color ? "background-color: " + color : "") + '"></span>\n                ').trim();
            }).join("") + "\n              </span>\n            ";
          }
        }
        if (params.rangesClasses) {
          for (var k = 0; k < params.rangesClasses.length; k += 1) {
            if (calendar.dateInRange(dayDate, params.rangesClasses[k].range)) {
              addClass2 += " " + params.rangesClasses[k].cssClass;
            }
          }
        }
        disabled = false;
        if (minDate && dayDate < minDate || maxDate && dayDate > maxDate) {
          disabled = true;
        }
        if (params.disabled) {
          if (calendar.dateInRange(dayDate, params.disabled)) {
            disabled = true;
          }
        }
        if (disabled) {
          addClass2 += " calendar-day-disabled";
        }
        dayDate = new Date(dayDate);
        var dayYear = dayDate.getFullYear();
        var dayMonth = dayDate.getMonth();
        var dayNumberDisplay = calendar.dayFormatter(dayDate);
        rowHtml += ('\n          <div data-year="' + dayYear + '" data-month="' + dayMonth + '" data-day="' + dayNumber + '" class="calendar-day' + addClass2 + '" data-date="' + dayYear + "-" + dayMonth + "-" + dayNumber + '">\n            <span class="calendar-day-number">' + dayNumberDisplay + eventsHtml + "</span>\n          </div>").trim();
      };
      for (var col = 1; col <= cols; col += 1) {
        _loop(col);
      }
      monthHtml += '<div class="calendar-row">' + rowHtml + "</div>";
    }
    monthHtml = '<div class="calendar-month" data-year="' + year + '" data-month="' + month + '" data-locale-year="' + localeYear + '" data-locale-month="' + localeMonth + '">' + monthHtml + "</div>";
    return monthHtml;
  };
  _proto.renderWeekHeader = function renderWeekHeader() {
    var calendar = this;
    if (calendar.params.renderWeekHeader) {
      return calendar.params.renderWeekHeader.call(calendar);
    }
    var params = calendar.params;
    var weekDaysHtml = "";
    for (var i = 0; i < 7; i += 1) {
      var dayIndex = i + params.firstDay > 6 ? i - 7 + params.firstDay : i + params.firstDay;
      var dayName = calendar.dayNamesShort[dayIndex];
      weekDaysHtml += '<div class="calendar-week-day">' + dayName + "</div>";
    }
    return jsx_default2("div", {
      class: "calendar-week-header"
    }, weekDaysHtml);
  };
  _proto.renderMonthSelector = function renderMonthSelector() {
    var calendar = this;
    if (calendar.params.renderMonthSelector) {
      return calendar.params.renderMonthSelector.call(calendar);
    }
    return jsx_default2("div", {
      class: "calendar-month-selector"
    }, jsx_default2("a", {
      class: "link icon-only calendar-prev-month-button"
    }, jsx_default2("i", {
      class: "icon icon-prev"
    })), calendar.params.monthPicker ? jsx_default2("a", {
      class: "current-month-value link"
    }) : jsx_default2("span", {
      class: "current-month-value"
    }), jsx_default2("a", {
      class: "link icon-only calendar-next-month-button"
    }, jsx_default2("i", {
      class: "icon icon-next"
    })));
  };
  _proto.renderMonthPicker = function renderMonthPicker() {
    var calendar = this;
    var localeMonth = parseInt(calendar.$el.find(".calendar-month-current").attr("data-locale-month"), 10);
    return jsx_default2("div", {
      class: "calendar-month-picker"
    }, calendar.monthNames.map(function(m, index2) {
      return jsx_default2("div", {
        class: "calendar-month-picker-item " + (localeMonth === index2 ? "calendar-month-picker-item-current" : "")
      }, jsx_default2("span", null, m));
    }));
  };
  _proto.renderYearSelector = function renderYearSelector() {
    var calendar = this;
    if (calendar.params.renderYearSelector) {
      return calendar.params.renderYearSelector.call(calendar);
    }
    return jsx_default2("div", {
      class: "calendar-year-selector"
    }, jsx_default2("a", {
      class: "link icon-only calendar-prev-year-button"
    }, jsx_default2("i", {
      class: "icon icon-prev"
    })), calendar.params.yearPicker ? jsx_default2("a", {
      class: "current-year-value link"
    }) : jsx_default2("span", {
      class: "current-year-value"
    }), jsx_default2("a", {
      class: "link icon-only calendar-next-year-button"
    }, jsx_default2("i", {
      class: "icon icon-next"
    })));
  };
  _proto.renderYearPicker = function renderYearPicker() {
    var calendar = this;
    var currentYear = calendar.currentYear;
    var yearMin = calendar.params.yearPickerMin || new Date().getFullYear() - 100;
    if (calendar.params.minDate) {
      yearMin = Math.max(yearMin, new Date(calendar.params.minDate).getFullYear());
    }
    var yearMax = calendar.params.yearPickerMax || new Date().getFullYear() + 100;
    if (calendar.params.maxDate) {
      yearMax = Math.min(yearMax, new Date(calendar.params.maxDate).getFullYear());
    }
    var years = [];
    for (var i = yearMin; i <= yearMax; i += 1) {
      years.push(i);
    }
    return jsx_default2("div", {
      class: "calendar-year-picker"
    }, years.map(function(year) {
      return jsx_default2("div", {
        "data-year": year,
        class: "calendar-year-picker-item " + (year === currentYear ? "calendar-year-picker-item-current" : "")
      }, jsx_default2("span", null, calendar.yearFormatter(new Date().setFullYear(year))));
    }));
  };
  _proto.renderTimeSelector = function renderTimeSelector() {
    var calendar = this;
    var value = calendar.value && calendar.value[0];
    var timeString;
    if (value)
      timeString = calendar.timeSelectorFormatter(value);
    return jsx_default2("div", {
      class: "calendar-time-selector"
    }, jsx_default2("a", {
      class: "link"
    }, timeString || calendar.params.timePickerPlaceholder));
  };
  _proto.renderHeader = function renderHeader() {
    var calendar = this;
    if (calendar.params.renderHeader) {
      return calendar.params.renderHeader.call(calendar);
    }
    return jsx_default2("div", {
      class: "calendar-header"
    }, jsx_default2("div", {
      class: "calendar-selected-date"
    }, calendar.params.headerPlaceholder));
  };
  _proto.renderFooter = function renderFooter() {
    var calendar = this;
    var app = calendar.app;
    if (calendar.params.renderFooter) {
      return calendar.params.renderFooter.call(calendar);
    }
    return jsx_default2("div", {
      class: "calendar-footer"
    }, jsx_default2("a", {
      class: (app.theme === "md" ? "button" : "link") + " calendar-close sheet-close popover-close"
    }, calendar.params.toolbarCloseText));
  };
  _proto.renderToolbar = function renderToolbar() {
    var calendar = this;
    if (calendar.params.renderToolbar) {
      return calendar.params.renderToolbar.call(calendar, calendar);
    }
    return jsx_default2("div", {
      class: "toolbar toolbar-top no-shadow"
    }, jsx_default2("div", {
      class: "toolbar-inner"
    }, calendar.params.monthSelector ? calendar.renderMonthSelector() : "", calendar.params.yearSelector ? calendar.renderYearSelector() : ""));
  };
  _proto.renderInline = function renderInline() {
    var calendar = this;
    var _calendar$params4 = calendar.params, cssClass = _calendar$params4.cssClass, toolbar = _calendar$params4.toolbar, header = _calendar$params4.header, footer = _calendar$params4.footer, rangePicker = _calendar$params4.rangePicker, weekHeader = _calendar$params4.weekHeader;
    var value = calendar.value, hasTimePicker = calendar.hasTimePicker;
    var date = value && value.length ? value[0] : new Date().setHours(0, 0, 0);
    return jsx_default2("div", {
      class: "calendar calendar-inline " + (rangePicker ? "calendar-range" : "") + " " + (cssClass || "")
    }, header && calendar.renderHeader(), toolbar && calendar.renderToolbar(), weekHeader && calendar.renderWeekHeader(), jsx_default2("div", {
      class: "calendar-months"
    }, calendar.renderMonths(date)), hasTimePicker && calendar.renderTimeSelector(), footer && calendar.renderFooter());
  };
  _proto.renderCustomModal = function renderCustomModal() {
    var calendar = this;
    var _calendar$params5 = calendar.params, cssClass = _calendar$params5.cssClass, toolbar = _calendar$params5.toolbar, header = _calendar$params5.header, footer = _calendar$params5.footer, rangePicker = _calendar$params5.rangePicker, weekHeader = _calendar$params5.weekHeader;
    var value = calendar.value, hasTimePicker = calendar.hasTimePicker;
    var date = value && value.length ? value[0] : new Date().setHours(0, 0, 0);
    return jsx_default2("div", {
      class: "calendar calendar-modal " + (rangePicker ? "calendar-range" : "") + " " + (cssClass || "")
    }, header && calendar.renderHeader(), toolbar && calendar.renderToolbar(), weekHeader && calendar.renderWeekHeader(), jsx_default2("div", {
      class: "calendar-months"
    }, calendar.renderMonths(date)), hasTimePicker && calendar.renderTimeSelector(), footer && calendar.renderFooter());
  };
  _proto.renderSheet = function renderSheet() {
    var calendar = this;
    var _calendar$params6 = calendar.params, cssClass = _calendar$params6.cssClass, toolbar = _calendar$params6.toolbar, header = _calendar$params6.header, footer = _calendar$params6.footer, rangePicker = _calendar$params6.rangePicker, weekHeader = _calendar$params6.weekHeader;
    var value = calendar.value, hasTimePicker = calendar.hasTimePicker;
    var date = value && value.length ? value[0] : new Date().setHours(0, 0, 0);
    return jsx_default2("div", {
      class: "sheet-modal calendar calendar-sheet " + (rangePicker ? "calendar-range" : "") + " " + (cssClass || "")
    }, header && calendar.renderHeader(), toolbar && calendar.renderToolbar(), weekHeader && calendar.renderWeekHeader(), jsx_default2("div", {
      class: "sheet-modal-inner calendar-months"
    }, calendar.renderMonths(date)), hasTimePicker && calendar.renderTimeSelector(), footer && calendar.renderFooter());
  };
  _proto.renderPopover = function renderPopover() {
    var calendar = this;
    var _calendar$params7 = calendar.params, cssClass = _calendar$params7.cssClass, toolbar = _calendar$params7.toolbar, header = _calendar$params7.header, footer = _calendar$params7.footer, rangePicker = _calendar$params7.rangePicker, weekHeader = _calendar$params7.weekHeader;
    var value = calendar.value, hasTimePicker = calendar.hasTimePicker;
    var date = value && value.length ? value[0] : new Date().setHours(0, 0, 0);
    return jsx_default2("div", {
      class: "popover calendar-popover"
    }, jsx_default2("div", {
      class: "popover-inner"
    }, jsx_default2("div", {
      class: "calendar " + (rangePicker ? "calendar-range" : "") + " " + (cssClass || "")
    }, header && calendar.renderHeader(), toolbar && calendar.renderToolbar(), weekHeader && calendar.renderWeekHeader(), jsx_default2("div", {
      class: "calendar-months"
    }, calendar.renderMonths(date)), hasTimePicker && calendar.renderTimeSelector(), footer && calendar.renderFooter())));
  };
  _proto.render = function render15() {
    var calendar = this;
    var params = calendar.params;
    if (params.render)
      return params.render.call(calendar);
    if (!calendar.inline) {
      var modalType = params.openIn;
      if (modalType === "auto")
        modalType = calendar.isPopover() ? "popover" : "sheet";
      if (modalType === "popover")
        return calendar.renderPopover();
      if (modalType === "sheet")
        return calendar.renderSheet();
      return calendar.renderCustomModal();
    }
    return calendar.renderInline();
  };
  _proto.openTimePicker = function openTimePicker() {
    var calendar = this;
    var $el = calendar.$el, app = calendar.app, is12HoursFormat = calendar.is12HoursFormat;
    if (!$el || !$el.length)
      return;
    $el.append('<div class="calendar-time-picker"></div>');
    var hoursArr = [];
    var minutesArr = [];
    var hoursMin = is12HoursFormat ? 1 : 0;
    var hoursMax = is12HoursFormat ? 12 : 23;
    for (var i = hoursMin; i <= hoursMax; i += 1) {
      hoursArr.push(i);
    }
    for (var _i2 = 0; _i2 <= 59; _i2 += 1) {
      minutesArr.push(_i2);
    }
    var value;
    if (calendar.value && calendar.value.length) {
      value = [calendar.value[0].getHours(), calendar.value[0].getMinutes()];
    } else {
      value = [new Date().getHours(), new Date().getMinutes()];
    }
    if (is12HoursFormat) {
      value.push(value[0] < 12 ? "AM" : "PM");
      if (value[0] > 12)
        value[0] -= 12;
      if (value[0] === 0)
        value[0] = 12;
    }
    calendar.timePickerInstance = app.picker.create({
      containerEl: $el.find(".calendar-time-picker"),
      value,
      toolbar: true,
      rotateEffect: false,
      toolbarCloseText: calendar.params.toolbarCloseText,
      cols: [{
        values: hoursArr
      }, {
        divider: true,
        content: ":"
      }, {
        values: minutesArr,
        displayValues: minutesArr.map(function(m) {
          return m < 10 ? "0" + m : m;
        })
      }].concat(is12HoursFormat ? [{
        values: ["AM", "PM"]
      }] : [])
    });
    calendar.timePickerInstance.$el.find(".toolbar a").removeClass("sheet-close popover-close").addClass("calendar-time-picker-close");
  };
  _proto.closeTimePicker = function closeTimePicker() {
    var calendar = this;
    var is12HoursFormat = calendar.is12HoursFormat;
    if (calendar.timePickerInstance) {
      var timePickerValue = calendar.timePickerInstance.value;
      var hours = parseInt(timePickerValue[0], 10);
      var minutes = parseInt(timePickerValue[1], 10);
      var period = calendar.timePickerInstance.value[2];
      if (is12HoursFormat) {
        if (period === "AM" && hours === 12) {
          hours = 0;
        } else if (period === "PM" && hours !== 12) {
          hours += 12;
        }
      }
      var value = calendar.value && calendar.value.length && calendar.value[0];
      if (!value) {
        value = new Date();
        value.setHours(hours, minutes, 0, 0);
      } else {
        value = new Date(value);
        value.setHours(hours, minutes);
      }
      calendar.setValue([value]);
      calendar.timePickerInstance.close();
      calendar.timePickerInstance.destroy();
      delete calendar.timePickerInstance;
    }
    if (calendar.$el && calendar.$el.length) {
      calendar.$el.find(".calendar-time-picker").remove();
    }
  };
  _proto.onOpen = function onOpen() {
    var calendar = this;
    var initialized = calendar.initialized, $el = calendar.$el, app = calendar.app, $inputEl = calendar.$inputEl, inline = calendar.inline, value = calendar.value, params = calendar.params;
    calendar.closing = false;
    calendar.opened = true;
    calendar.opening = true;
    calendar.attachCalendarEvents();
    var updateValue = !value && params.value;
    if (!initialized) {
      if (value)
        calendar.setValue(value, 0);
      else if (params.value) {
        calendar.setValue(calendar.normalizeValues(params.value), 0);
      }
    } else if (value) {
      calendar.setValue(value, 0);
    }
    calendar.updateCurrentMonthYear();
    calendar.monthsTranslate = 0;
    calendar.setMonthsTranslate();
    if (updateValue)
      calendar.updateValue();
    else if (params.header && value) {
      calendar.updateValue(true);
    }
    if (!inline && $inputEl && $inputEl.length && app.theme === "md") {
      $inputEl.trigger("focus");
    }
    calendar.initialized = true;
    calendar.$months.each(function(monthEl) {
      calendar.emit("local::monthAdd calendarMonthAdd", monthEl);
    });
    if ($el) {
      $el.trigger("calendar:open");
    }
    if ($inputEl) {
      $inputEl.trigger("calendar:open");
    }
    calendar.emit("local::open calendarOpen", calendar);
  };
  _proto.onOpened = function onOpened() {
    var calendar = this;
    calendar.opening = false;
    if (calendar.$el) {
      calendar.$el.trigger("calendar:opened");
    }
    if (calendar.$inputEl) {
      calendar.$inputEl.trigger("calendar:opened");
    }
    calendar.emit("local::opened calendarOpened", calendar);
  };
  _proto.onClose = function onClose() {
    var calendar = this;
    var app = calendar.app;
    calendar.opening = false;
    calendar.closing = true;
    if (calendar.$inputEl) {
      if (app.theme === "md") {
        calendar.$inputEl.trigger("blur");
      } else {
        var validate2 = calendar.$inputEl.attr("validate");
        var required = calendar.$inputEl.attr("required");
        if (validate2 && required) {
          app.input.validate(calendar.$inputEl);
        }
      }
    }
    if (calendar.detachCalendarEvents) {
      calendar.detachCalendarEvents();
    }
    if (calendar.$el) {
      calendar.$el.trigger("calendar:close");
    }
    if (calendar.$inputEl) {
      calendar.$inputEl.trigger("calendar:close");
    }
    calendar.emit("local::close calendarClose", calendar);
  };
  _proto.onClosed = function onClosed() {
    var calendar = this;
    calendar.opened = false;
    calendar.closing = false;
    if (!calendar.inline) {
      nextTick(function() {
        if (calendar.modal && calendar.modal.el && calendar.modal.destroy) {
          if (!calendar.params.routableModals) {
            calendar.modal.destroy();
          }
        }
        delete calendar.modal;
      });
    }
    if (calendar.timePickerInstance) {
      if (calendar.timePickerInstance.destroy)
        calendar.timePickerInstance.destroy();
      delete calendar.timePickerInstance;
    }
    if (calendar.$el) {
      calendar.$el.trigger("calendar:closed");
    }
    if (calendar.$inputEl) {
      calendar.$inputEl.trigger("calendar:closed");
    }
    calendar.emit("local::closed calendarClosed", calendar);
  };
  _proto.open = function open12() {
    var calendar = this;
    var app = calendar.app, opened = calendar.opened, inline = calendar.inline, $inputEl = calendar.$inputEl, params = calendar.params;
    if (opened)
      return;
    if (inline) {
      calendar.$el = dom7_default(calendar.render());
      calendar.$el[0].f7Calendar = calendar;
      calendar.$wrapperEl = calendar.$el.find(".calendar-months-wrapper");
      calendar.$months = calendar.$wrapperEl.find(".calendar-month");
      calendar.$containerEl.append(calendar.$el);
      calendar.onOpen();
      calendar.onOpened();
      return;
    }
    var modalType = params.openIn;
    if (modalType === "auto") {
      modalType = calendar.isPopover() ? "popover" : "sheet";
    }
    var modalContent = calendar.render();
    var modalParams = {
      targetEl: $inputEl,
      scrollToEl: params.scrollToInput ? $inputEl : void 0,
      content: modalContent,
      backdrop: params.backdrop === true || modalType === "popover" && app.params.popover.backdrop !== false && params.backdrop !== false,
      closeByBackdropClick: params.closeByBackdropClick,
      on: {
        open: function open13() {
          var modal = this;
          calendar.modal = modal;
          calendar.$el = modalType === "popover" ? modal.$el.find(".calendar") : modal.$el;
          calendar.$wrapperEl = calendar.$el.find(".calendar-months-wrapper");
          calendar.$months = calendar.$wrapperEl.find(".calendar-month");
          calendar.$el[0].f7Calendar = calendar;
          if (modalType === "customModal") {
            dom7_default(calendar.$el).find(".calendar-close").once("click", function() {
              calendar.close();
            });
          }
          calendar.onOpen();
        },
        opened: function opened2() {
          calendar.onOpened();
        },
        close: function close12() {
          calendar.onClose();
        },
        closed: function closed() {
          calendar.onClosed();
        }
      }
    };
    if (modalType === "sheet") {
      modalParams.push = params.sheetPush;
      modalParams.swipeToClose = params.sheetSwipeToClose;
    }
    if (params.routableModals && calendar.view) {
      var _route;
      calendar.view.router.navigate({
        url: calendar.url,
        route: (_route = {
          path: calendar.url
        }, _route[modalType] = modalParams, _route)
      });
    } else {
      calendar.modal = app[modalType].create(modalParams);
      calendar.modal.open();
    }
  };
  _proto.close = function close12() {
    var calendar = this;
    var opened = calendar.opened, inline = calendar.inline;
    if (!opened)
      return;
    if (inline) {
      calendar.onClose();
      calendar.onClosed();
      return;
    }
    if (calendar.params.routableModals && calendar.view) {
      calendar.view.router.back();
    } else {
      calendar.modal.close();
    }
  };
  _proto.init = function init46() {
    var calendar = this;
    calendar.initInput();
    if (calendar.inline) {
      calendar.open();
      calendar.emit("local::init calendarInit", calendar);
      return;
    }
    if (!calendar.initialized && calendar.params.value) {
      calendar.setValue(calendar.normalizeValues(calendar.params.value));
    }
    if (calendar.$inputEl) {
      calendar.attachInputEvents();
    }
    if (calendar.params.closeByOutsideClick) {
      calendar.attachHtmlEvents();
    }
    calendar.emit("local::init calendarInit", calendar);
  };
  _proto.destroy = function destroy50() {
    var calendar = this;
    if (calendar.destroyed)
      return;
    var $el = calendar.$el;
    calendar.emit("local::beforeDestroy calendarBeforeDestroy", calendar);
    if ($el)
      $el.trigger("calendar:beforedestroy");
    calendar.close();
    if (calendar.$inputEl) {
      calendar.detachInputEvents();
    }
    if (calendar.params.closeByOutsideClick) {
      calendar.detachHtmlEvents();
    }
    if (calendar.timePickerInstance) {
      if (calendar.timePickerInstance.destroy)
        calendar.timePickerInstance.destroy();
      delete calendar.timePickerInstance;
    }
    if ($el && $el.length)
      delete calendar.$el[0].f7Calendar;
    deleteProps(calendar);
    calendar.destroyed = true;
  };
  _createClass2(Calendar2, [{
    key: "view",
    get: function get2() {
      var $inputEl = this.$inputEl, app = this.app, params = this.params;
      var view;
      if (params.view) {
        view = params.view;
      } else if ($inputEl) {
        view = $inputEl.parents(".view").length && $inputEl.parents(".view")[0].f7View;
      }
      if (!view)
        view = app.views.main;
      return view;
    }
  }]);
  return Calendar2;
}(class_default);
var calendar_class_default = Calendar;

// node_modules/framework7/esm/components/calendar/calendar.js
var calendar_default = {
  name: "calendar",
  static: {
    Calendar: calendar_class_default
  },
  create: function create25() {
    var app = this;
    app.calendar = ConstructorMethods({
      defaultSelector: ".calendar",
      constructor: calendar_class_default,
      app,
      domProp: "f7Calendar"
    });
    app.calendar.close = function close12(el) {
      if (el === void 0) {
        el = ".calendar";
      }
      var $el = dom7_default(el);
      if ($el.length === 0)
        return;
      var calendar = $el[0].f7Calendar;
      if (!calendar || calendar && !calendar.opened)
        return;
      calendar.close();
    };
  },
  params: {
    calendar: {
      dateFormat: void 0,
      monthNames: "auto",
      monthNamesShort: "auto",
      dayNames: "auto",
      dayNamesShort: "auto",
      locale: void 0,
      firstDay: 1,
      weekendDays: [0, 6],
      multiple: false,
      rangePicker: false,
      rangePickerMinDays: 1,
      rangePickerMaxDays: 0,
      direction: "horizontal",
      minDate: null,
      maxDate: null,
      disabled: null,
      events: null,
      rangesClasses: null,
      touchMove: true,
      animate: true,
      closeOnSelect: false,
      monthSelector: true,
      monthPicker: true,
      yearSelector: true,
      yearPicker: true,
      yearPickerMin: void 0,
      yearPickerMax: void 0,
      timePicker: false,
      timePickerFormat: {
        hour: "numeric",
        minute: "numeric"
      },
      timePickerPlaceholder: "Select time",
      weekHeader: true,
      value: null,
      containerEl: null,
      openIn: "auto",
      sheetPush: false,
      sheetSwipeToClose: void 0,
      formatValue: null,
      inputEl: null,
      inputReadOnly: true,
      closeByOutsideClick: true,
      scrollToInput: true,
      header: false,
      headerPlaceholder: "Select date",
      toolbar: true,
      toolbarCloseText: "Done",
      footer: false,
      cssClass: null,
      routableModals: false,
      view: null,
      url: "date/",
      backdrop: null,
      closeByBackdropClick: true,
      renderWeekHeader: null,
      renderMonths: null,
      renderMonth: null,
      renderMonthSelector: null,
      renderYearSelector: null,
      renderHeader: null,
      renderFooter: null,
      renderToolbar: null,
      renderInline: null,
      renderPopover: null,
      renderSheet: null,
      render: null
    }
  }
};

// node_modules/framework7/esm/components/picker/picker-column.js
function pickerColumn(colEl, updateItems) {
  var picker = this;
  var app = picker.app;
  var $colEl = dom7_default(colEl);
  var colIndex = $colEl.index();
  var col = picker.cols[colIndex];
  if (col.divider)
    return;
  col.$el = $colEl;
  col.el = $colEl[0];
  col.$itemsEl = col.$el.find(".picker-items");
  col.items = col.$itemsEl.find(".picker-item");
  var itemHeight;
  var itemsHeight;
  var minTranslate2;
  var maxTranslate2;
  var animationFrameId;
  function updateDuringScroll() {
    animationFrameId = requestAnimationFrame(function() {
      col.updateItems(void 0, void 0, 0);
      updateDuringScroll();
    });
  }
  col.replaceValues = function replaceColValues(values, displayValues) {
    col.detachEvents();
    col.values = values;
    col.displayValues = displayValues;
    col.$itemsEl.html(picker.renderColumn(col, true));
    col.items = col.$itemsEl.find(".picker-item");
    col.calcSize();
    col.setValue(col.values[0], 0, true);
    col.attachEvents();
  };
  col.calcSize = function calcColSize() {
    if (picker.params.rotateEffect) {
      col.$el.removeClass("picker-column-absolute");
      if (!col.width)
        col.$el.css({
          width: ""
        });
    }
    var colWidth = 0;
    var colHeight = col.$el[0].offsetHeight;
    itemHeight = col.items[0].offsetHeight;
    itemsHeight = itemHeight * col.items.length;
    minTranslate2 = colHeight / 2 - itemsHeight + itemHeight / 2;
    maxTranslate2 = colHeight / 2 - itemHeight / 2;
    if (col.width) {
      colWidth = col.width;
      if (parseInt(colWidth, 10) === colWidth)
        colWidth += "px";
      col.$el.css({
        width: colWidth
      });
    }
    if (picker.params.rotateEffect) {
      if (!col.width) {
        col.items.each(function(itemEl) {
          var item = dom7_default(itemEl).children("span");
          colWidth = Math.max(colWidth, item[0].offsetWidth);
        });
        col.$el.css({
          width: colWidth + 2 + "px"
        });
      }
      col.$el.addClass("picker-column-absolute");
    }
  };
  col.setValue = function setColValue(newValue, transition2, valueCallbacks) {
    if (transition2 === void 0) {
      transition2 = "";
    }
    var newActiveIndex = col.$itemsEl.find('.picker-item[data-picker-value="' + newValue + '"]').index();
    if (typeof newActiveIndex === "undefined" || newActiveIndex === -1) {
      return;
    }
    var newTranslate = -newActiveIndex * itemHeight + maxTranslate2;
    col.$itemsEl.transition(transition2);
    col.$itemsEl.transform("translate3d(0," + newTranslate + "px,0)");
    if (picker.params.updateValuesOnMomentum && col.activeIndex && col.activeIndex !== newActiveIndex) {
      cancelAnimationFrame(animationFrameId);
      col.$itemsEl.transitionEnd(function() {
        cancelAnimationFrame(animationFrameId);
      });
      updateDuringScroll();
    }
    col.updateItems(newActiveIndex, newTranslate, transition2, valueCallbacks);
  };
  col.updateItems = function updateColItems(activeIndex, translate, transition2, valueCallbacks) {
    if (typeof translate === "undefined") {
      translate = getTranslate(col.$itemsEl[0], "y");
    }
    if (typeof activeIndex === "undefined")
      activeIndex = -Math.round((translate - maxTranslate2) / itemHeight);
    if (activeIndex < 0)
      activeIndex = 0;
    if (activeIndex >= col.items.length)
      activeIndex = col.items.length - 1;
    var previousActiveIndex = col.activeIndex;
    col.activeIndex = activeIndex;
    col.$itemsEl.find(".picker-item-selected").removeClass("picker-item-selected");
    col.items.transition(transition2);
    var selectedItem = col.items.eq(activeIndex).addClass("picker-item-selected").transform("");
    if (picker.params.rotateEffect) {
      col.items.each(function(itemEl) {
        var $itemEl = dom7_default(itemEl);
        var itemOffsetTop = $itemEl.index() * itemHeight;
        var translateOffset = maxTranslate2 - translate;
        var itemOffset = itemOffsetTop - translateOffset;
        var percentage = itemOffset / itemHeight;
        var itemsFit = Math.ceil(col.height / itemHeight / 2) + 1;
        var angle = -18 * percentage;
        if (angle > 180)
          angle = 180;
        if (angle < -180)
          angle = -180;
        if (Math.abs(percentage) > itemsFit) {
          $itemEl.addClass("picker-item-far");
        } else {
          $itemEl.removeClass("picker-item-far");
        }
        $itemEl.transform("translate3d(0, " + (-translate + maxTranslate2) + "px, " + (picker.needsOriginFix ? -110 : 0) + "px) rotateX(" + angle + "deg)");
      });
    }
    if (valueCallbacks || typeof valueCallbacks === "undefined") {
      col.value = selectedItem.attr("data-picker-value");
      col.displayValue = col.displayValues ? col.displayValues[activeIndex] : col.value;
      if (previousActiveIndex !== activeIndex) {
        if (col.onChange) {
          col.onChange(picker, col.value, col.displayValue);
        }
        picker.updateValue();
      }
    }
  };
  var allowItemClick = true;
  var isTouched;
  var isMoved;
  var touchStartY;
  var touchCurrentY;
  var touchStartTime;
  var touchEndTime;
  var startTranslate;
  var returnTo;
  var currentTranslate;
  var prevTranslate;
  var velocityTranslate;
  function handleTouchStart(e) {
    if (isMoved || isTouched)
      return;
    e.preventDefault();
    isTouched = true;
    touchStartY = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
    touchCurrentY = touchStartY;
    touchStartTime = new Date().getTime();
    allowItemClick = true;
    startTranslate = getTranslate(col.$itemsEl[0], "y");
    currentTranslate = startTranslate;
  }
  function handleTouchMove(e) {
    if (!isTouched)
      return;
    e.preventDefault();
    allowItemClick = false;
    touchCurrentY = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
    if (!isMoved) {
      cancelAnimationFrame(animationFrameId);
      isMoved = true;
      startTranslate = getTranslate(col.$itemsEl[0], "y");
      currentTranslate = startTranslate;
      col.$itemsEl.transition(0);
    }
    var diff = touchCurrentY - touchStartY;
    currentTranslate = startTranslate + diff;
    returnTo = void 0;
    if (currentTranslate < minTranslate2) {
      currentTranslate = minTranslate2 - Math.pow(minTranslate2 - currentTranslate, 0.8);
      returnTo = "min";
    }
    if (currentTranslate > maxTranslate2) {
      currentTranslate = maxTranslate2 + Math.pow(currentTranslate - maxTranslate2, 0.8);
      returnTo = "max";
    }
    col.$itemsEl.transform("translate3d(0," + currentTranslate + "px,0)");
    col.updateItems(void 0, currentTranslate, 0, picker.params.updateValuesOnTouchmove);
    velocityTranslate = currentTranslate - prevTranslate || currentTranslate;
    prevTranslate = currentTranslate;
  }
  function handleTouchEnd() {
    if (!isTouched || !isMoved) {
      isTouched = false;
      isMoved = false;
      return;
    }
    isTouched = false;
    isMoved = false;
    col.$itemsEl.transition("");
    if (returnTo) {
      if (returnTo === "min") {
        col.$itemsEl.transform("translate3d(0," + minTranslate2 + "px,0)");
      } else {
        col.$itemsEl.transform("translate3d(0," + maxTranslate2 + "px,0)");
      }
    }
    touchEndTime = new Date().getTime();
    var newTranslate;
    if (touchEndTime - touchStartTime > 300) {
      newTranslate = currentTranslate;
    } else {
      newTranslate = currentTranslate + velocityTranslate * picker.params.momentumRatio;
    }
    newTranslate = Math.max(Math.min(newTranslate, maxTranslate2), minTranslate2);
    var activeIndex = Math.round(Math.abs((newTranslate - maxTranslate2) / itemHeight));
    if (!picker.params.freeMode)
      newTranslate = -activeIndex * itemHeight + maxTranslate2;
    col.$itemsEl.transform("translate3d(0," + parseInt(newTranslate, 10) + "px,0)");
    col.updateItems(activeIndex, newTranslate, "", true);
    if (picker.params.updateValuesOnMomentum) {
      updateDuringScroll();
      col.$itemsEl.transitionEnd(function() {
        cancelAnimationFrame(animationFrameId);
      });
    }
    setTimeout(function() {
      allowItemClick = true;
    }, 100);
  }
  var mousewheelTimeout;
  function handleMouseWheel(e) {
    var deltaX = e.deltaX, deltaY = e.deltaY;
    if (Math.abs(deltaX) > Math.abs(deltaY))
      return;
    clearTimeout(mousewheelTimeout);
    e.preventDefault();
    cancelAnimationFrame(animationFrameId);
    startTranslate = getTranslate(col.$itemsEl[0], "y");
    col.$itemsEl.transition(0);
    currentTranslate = startTranslate - deltaY;
    returnTo = void 0;
    if (currentTranslate < minTranslate2) {
      currentTranslate = minTranslate2;
      returnTo = "min";
    }
    if (currentTranslate > maxTranslate2) {
      currentTranslate = maxTranslate2;
      returnTo = "max";
    }
    col.$itemsEl.transform("translate3d(0," + currentTranslate + "px,0)");
    col.updateItems(void 0, currentTranslate, 0, picker.params.updateValuesOnMousewheel);
    mousewheelTimeout = setTimeout(function() {
      col.$itemsEl.transition("");
      if (returnTo) {
        if (returnTo === "min") {
          col.$itemsEl.transform("translate3d(0," + minTranslate2 + "px,0)");
        } else {
          col.$itemsEl.transform("translate3d(0," + maxTranslate2 + "px,0)");
        }
      }
      touchEndTime = new Date().getTime();
      var newTranslate = currentTranslate;
      newTranslate = Math.max(Math.min(newTranslate, maxTranslate2), minTranslate2);
      var activeIndex = Math.round(Math.abs((newTranslate - maxTranslate2) / itemHeight));
      if (!picker.params.freeMode)
        newTranslate = -activeIndex * itemHeight + maxTranslate2;
      col.$itemsEl.transform("translate3d(0," + parseInt(newTranslate, 10) + "px,0)");
      col.updateItems(activeIndex, newTranslate, "", true);
    }, 200);
  }
  function handleClick() {
    if (!allowItemClick)
      return;
    cancelAnimationFrame(animationFrameId);
    var value = dom7_default(this).attr("data-picker-value");
    col.setValue(value);
  }
  var activeListener = getSupport().passiveListener ? {
    passive: false,
    capture: false
  } : false;
  col.attachEvents = function attachColEvents() {
    col.$el.on(app.touchEvents.start, handleTouchStart, activeListener);
    app.on("touchmove:active", handleTouchMove);
    app.on("touchend:passive", handleTouchEnd);
    if (picker.params.mousewheel) {
      col.$el.on("wheel", handleMouseWheel);
    }
    col.items.on("click", handleClick);
  };
  col.detachEvents = function detachColEvents() {
    col.$el.off(app.touchEvents.start, handleTouchStart, activeListener);
    app.off("touchmove:active", handleTouchMove);
    app.off("touchend:passive", handleTouchEnd);
    if (picker.params.mousewheel) {
      col.$el.off("wheel", handleMouseWheel);
    }
    col.items.off("click", handleClick);
  };
  col.init = function initCol() {
    col.calcSize();
    col.$itemsEl.transform("translate3d(0," + maxTranslate2 + "px,0)").transition(0);
    if (colIndex === 0)
      col.$el.addClass("picker-column-first");
    if (colIndex === picker.cols.length - 1)
      col.$el.addClass("picker-column-last");
    if (updateItems)
      col.updateItems(0, maxTranslate2, 0);
    col.attachEvents();
  };
  col.destroy = function destroyCol() {
    col.detachEvents();
  };
  col.init();
}

// node_modules/framework7/esm/components/picker/picker-class.js
function _defineProperties3(target, props) {
  for (var i = 0; i < props.length; i++) {
    var descriptor = props[i];
    descriptor.enumerable = descriptor.enumerable || false;
    descriptor.configurable = true;
    if ("value" in descriptor)
      descriptor.writable = true;
    Object.defineProperty(target, descriptor.key, descriptor);
  }
}
function _createClass3(Constructor, protoProps, staticProps) {
  if (protoProps)
    _defineProperties3(Constructor.prototype, protoProps);
  if (staticProps)
    _defineProperties3(Constructor, staticProps);
  return Constructor;
}
function _assertThisInitialized16(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose16(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf16(subClass, superClass);
}
function _setPrototypeOf16(o, p) {
  _setPrototypeOf16 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf16(o, p);
}
var Picker = function(_Framework7Class) {
  _inheritsLoose16(Picker2, _Framework7Class);
  function Picker2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var picker = _assertThisInitialized16(_this);
    var device2 = getDevice();
    var window2 = getWindow();
    picker.params = extend({}, app.params.picker, params);
    var $containerEl;
    if (picker.params.containerEl) {
      $containerEl = dom7_default(picker.params.containerEl);
      if ($containerEl.length === 0)
        return picker || _assertThisInitialized16(_this);
    }
    var $inputEl;
    if (picker.params.inputEl) {
      $inputEl = dom7_default(picker.params.inputEl);
    }
    var $scrollToEl = picker.params.scrollToInput ? $inputEl : void 0;
    if (picker.params.scrollToEl) {
      var scrollToEl = dom7_default(picker.params.scrollToEl);
      if (scrollToEl.length > 0) {
        $scrollToEl = scrollToEl;
      }
    }
    extend(picker, {
      app,
      $containerEl,
      containerEl: $containerEl && $containerEl[0],
      inline: $containerEl && $containerEl.length > 0,
      needsOriginFix: device2.ios || window2.navigator.userAgent.toLowerCase().indexOf("safari") >= 0 && window2.navigator.userAgent.toLowerCase().indexOf("chrome") < 0 && !device2.android,
      cols: [],
      $inputEl,
      inputEl: $inputEl && $inputEl[0],
      $scrollToEl,
      initialized: false,
      opened: false,
      url: picker.params.url
    });
    function onResize2() {
      picker.resizeCols();
    }
    function onInputClick() {
      picker.open();
    }
    function onInputFocus(e) {
      e.preventDefault();
    }
    var htmlTouchStartTarget = null;
    function onHtmlTouchStart(e) {
      htmlTouchStartTarget = e.target;
    }
    function onHtmlClick(e) {
      if (picker.destroyed || !picker.params)
        return;
      var $targetEl = dom7_default(e.target);
      if (picker.isPopover())
        return;
      if (!picker.opened || picker.closing)
        return;
      if ($targetEl.closest('[class*="backdrop"]').length)
        return;
      if ($inputEl && $inputEl.length > 0) {
        if (htmlTouchStartTarget === e.target && $targetEl[0] !== $inputEl[0] && $targetEl.closest(".sheet-modal").length === 0) {
          picker.close();
        }
      } else if (dom7_default(e.target).closest(".sheet-modal").length === 0) {
        picker.close();
      }
    }
    extend(picker, {
      attachResizeEvent: function attachResizeEvent() {
        app.on("resize", onResize2);
      },
      detachResizeEvent: function detachResizeEvent() {
        app.off("resize", onResize2);
      },
      attachInputEvents: function attachInputEvents() {
        picker.$inputEl.on("click", onInputClick);
        if (picker.params.inputReadOnly) {
          picker.$inputEl.on("focus mousedown", onInputFocus);
          if (picker.$inputEl[0]) {
            picker.$inputEl[0].f7ValidateReadonly = true;
          }
        }
      },
      detachInputEvents: function detachInputEvents() {
        picker.$inputEl.off("click", onInputClick);
        if (picker.params.inputReadOnly) {
          picker.$inputEl.off("focus mousedown", onInputFocus);
          if (picker.$inputEl[0]) {
            delete picker.$inputEl[0].f7ValidateReadonly;
          }
        }
      },
      attachHtmlEvents: function attachHtmlEvents() {
        app.on("click", onHtmlClick);
        app.on("touchstart", onHtmlTouchStart);
      },
      detachHtmlEvents: function detachHtmlEvents() {
        app.off("click", onHtmlClick);
        app.off("touchstart", onHtmlTouchStart);
      }
    });
    picker.init();
    return picker || _assertThisInitialized16(_this);
  }
  var _proto = Picker2.prototype;
  _proto.initInput = function initInput() {
    var picker = this;
    if (!picker.$inputEl)
      return;
    if (picker.params.inputReadOnly)
      picker.$inputEl.prop("readOnly", true);
  };
  _proto.resizeCols = function resizeCols() {
    var picker = this;
    if (!picker.opened)
      return;
    for (var i = 0; i < picker.cols.length; i += 1) {
      if (!picker.cols[i].divider) {
        picker.cols[i].calcSize();
        picker.cols[i].setValue(picker.cols[i].value, 0, false);
      }
    }
  };
  _proto.isPopover = function isPopover() {
    var picker = this;
    var app = picker.app, modal = picker.modal, params = picker.params;
    var device2 = getDevice();
    if (params.openIn === "sheet")
      return false;
    if (modal && modal.type !== "popover")
      return false;
    if (!picker.inline && picker.inputEl) {
      if (params.openIn === "popover")
        return true;
      if (device2.ios) {
        return !!device2.ipad;
      }
      if (app.width >= 768) {
        return true;
      }
      if (device2.desktop && app.theme === "aurora") {
        return true;
      }
    }
    return false;
  };
  _proto.formatValue = function formatValue() {
    var picker = this;
    var value = picker.value, displayValue = picker.displayValue;
    if (picker.params.formatValue) {
      return picker.params.formatValue.call(picker, value, displayValue);
    }
    return value.join(" ");
  };
  _proto.setValue = function setValue(values, transition2) {
    var picker = this;
    var valueIndex = 0;
    if (picker.cols.length === 0) {
      picker.value = values;
      picker.updateValue(values);
      return;
    }
    for (var i = 0; i < picker.cols.length; i += 1) {
      if (picker.cols[i] && !picker.cols[i].divider) {
        picker.cols[i].setValue(values[valueIndex], transition2);
        valueIndex += 1;
      }
    }
  };
  _proto.getValue = function getValue() {
    var picker = this;
    return picker.value;
  };
  _proto.updateValue = function updateValue(forceValues) {
    var picker = this;
    var newValue = forceValues || [];
    var newDisplayValue = [];
    var column;
    if (picker.cols.length === 0) {
      var noDividerColumns = picker.params.cols.filter(function(c) {
        return !c.divider;
      });
      for (var i = 0; i < noDividerColumns.length; i += 1) {
        column = noDividerColumns[i];
        if (column.displayValues !== void 0 && column.values !== void 0 && column.values.indexOf(newValue[i]) !== -1) {
          newDisplayValue.push(column.displayValues[column.values.indexOf(newValue[i])]);
        } else {
          newDisplayValue.push(newValue[i]);
        }
      }
    } else {
      for (var _i = 0; _i < picker.cols.length; _i += 1) {
        if (!picker.cols[_i].divider) {
          newValue.push(picker.cols[_i].value);
          newDisplayValue.push(picker.cols[_i].displayValue);
        }
      }
    }
    if (newValue.indexOf(void 0) >= 0) {
      return;
    }
    picker.value = newValue;
    picker.displayValue = newDisplayValue;
    picker.emit("local::change pickerChange", picker, picker.value, picker.displayValue);
    if (picker.inputEl) {
      picker.$inputEl.val(picker.formatValue());
      picker.$inputEl.trigger("change");
    }
  };
  _proto.initColumn = function initColumn(colEl, updateItems) {
    var picker = this;
    pickerColumn.call(picker, colEl, updateItems);
  };
  _proto.destroyColumn = function destroyColumn(colEl) {
    var picker = this;
    var $colEl = dom7_default(colEl);
    var index2 = $colEl.index();
    if (picker.cols[index2] && picker.cols[index2].destroy) {
      picker.cols[index2].destroy();
    }
  };
  _proto.renderToolbar = function renderToolbar() {
    var picker = this;
    if (picker.params.renderToolbar)
      return picker.params.renderToolbar.call(picker, picker);
    return jsx_default2("div", {
      class: "toolbar toolbar-top no-shadow"
    }, jsx_default2("div", {
      class: "toolbar-inner"
    }, jsx_default2("div", {
      class: "left"
    }), jsx_default2("div", {
      class: "right"
    }, jsx_default2("a", {
      class: "link sheet-close popover-close"
    }, picker.params.toolbarCloseText))));
  };
  _proto.renderColumn = function renderColumn(col, onlyItems) {
    var colClasses = "picker-column " + (col.textAlign ? "picker-column-" + col.textAlign : "") + " " + (col.cssClass || "");
    var columnHtml;
    var columnItemsHtml;
    if (col.divider) {
      columnHtml = '\n        <div class="' + colClasses + ' picker-column-divider">' + col.content + "</div>\n      ";
    } else {
      columnItemsHtml = col.values.map(function(value, index2) {
        return '\n        <div class="picker-item" data-picker-value="' + value + '">\n          <span>' + (col.displayValues ? col.displayValues[index2] : value) + "</span>\n        </div>\n      ";
      }).join("");
      columnHtml = '\n        <div class="' + colClasses + '">\n          <div class="picker-items">' + columnItemsHtml + "</div>\n        </div>\n      ";
    }
    return onlyItems ? columnItemsHtml.trim() : columnHtml.trim();
  };
  _proto.renderInline = function renderInline() {
    var picker = this;
    var _picker$params = picker.params, rotateEffect = _picker$params.rotateEffect, cssClass = _picker$params.cssClass, toolbar = _picker$params.toolbar;
    var inlineHtml = jsx_default2("div", {
      class: "picker picker-inline " + (rotateEffect ? "picker-3d" : "") + " " + (cssClass || "")
    }, toolbar && picker.renderToolbar(), jsx_default2("div", {
      class: "picker-columns"
    }, picker.cols.map(function(col) {
      return picker.renderColumn(col);
    }), jsx_default2("div", {
      class: "picker-center-highlight"
    })));
    return inlineHtml;
  };
  _proto.renderSheet = function renderSheet() {
    var picker = this;
    var _picker$params2 = picker.params, rotateEffect = _picker$params2.rotateEffect, cssClass = _picker$params2.cssClass, toolbar = _picker$params2.toolbar;
    var sheetHtml = jsx_default2("div", {
      class: "sheet-modal picker picker-sheet " + (rotateEffect ? "picker-3d" : "") + " " + (cssClass || "")
    }, toolbar && picker.renderToolbar(), jsx_default2("div", {
      class: "sheet-modal-inner picker-columns"
    }, picker.cols.map(function(col) {
      return picker.renderColumn(col);
    }), jsx_default2("div", {
      class: "picker-center-highlight"
    })));
    return sheetHtml;
  };
  _proto.renderPopover = function renderPopover() {
    var picker = this;
    var _picker$params3 = picker.params, rotateEffect = _picker$params3.rotateEffect, cssClass = _picker$params3.cssClass, toolbar = _picker$params3.toolbar;
    var popoverHtml = jsx_default2("div", {
      class: "popover picker-popover"
    }, jsx_default2("div", {
      class: "popover-inner"
    }, jsx_default2("div", {
      class: "picker " + (rotateEffect ? "picker-3d" : "") + " " + (cssClass || "")
    }, toolbar && picker.renderToolbar(), jsx_default2("div", {
      class: "picker-columns"
    }, picker.cols.map(function(col) {
      return picker.renderColumn(col);
    }), jsx_default2("div", {
      class: "picker-center-highlight"
    })))));
    return popoverHtml;
  };
  _proto.render = function render15() {
    var picker = this;
    if (picker.params.render)
      return picker.params.render.call(picker);
    if (!picker.inline) {
      if (picker.isPopover())
        return picker.renderPopover();
      return picker.renderSheet();
    }
    return picker.renderInline();
  };
  _proto.onOpen = function onOpen() {
    var picker = this;
    var initialized = picker.initialized, $el = picker.$el, app = picker.app, $inputEl = picker.$inputEl, inline = picker.inline, value = picker.value, params = picker.params;
    picker.opened = true;
    picker.closing = false;
    picker.opening = true;
    picker.attachResizeEvent();
    $el.find(".picker-column").each(function(colEl) {
      var updateItems = true;
      if (!initialized && params.value || initialized && value) {
        updateItems = false;
      }
      picker.initColumn(colEl, updateItems);
    });
    if (!initialized) {
      if (value)
        picker.setValue(value, 0);
      else if (params.value) {
        picker.setValue(params.value, 0);
      }
    } else if (value) {
      picker.setValue(value, 0);
    }
    if (!inline && $inputEl && $inputEl.length && app.theme === "md") {
      $inputEl.trigger("focus");
    }
    picker.initialized = true;
    if ($el) {
      $el.trigger("picker:open");
    }
    if ($inputEl) {
      $inputEl.trigger("picker:open");
    }
    picker.emit("local::open pickerOpen", picker);
  };
  _proto.onOpened = function onOpened() {
    var picker = this;
    picker.opening = false;
    if (picker.$el) {
      picker.$el.trigger("picker:opened");
    }
    if (picker.$inputEl) {
      picker.$inputEl.trigger("picker:opened");
    }
    picker.emit("local::opened pickerOpened", picker);
  };
  _proto.onClose = function onClose() {
    var picker = this;
    var app = picker.app;
    picker.opening = false;
    picker.closing = true;
    picker.detachResizeEvent();
    picker.cols.forEach(function(col) {
      if (col.destroy)
        col.destroy();
    });
    if (picker.$inputEl) {
      if (app.theme === "md") {
        picker.$inputEl.trigger("blur");
      } else {
        var validate2 = picker.$inputEl.attr("validate");
        var required = picker.$inputEl.attr("required");
        if (validate2 && required) {
          app.input.validate(picker.$inputEl);
        }
      }
    }
    if (picker.$el) {
      picker.$el.trigger("picker:close");
    }
    if (picker.$inputEl) {
      picker.$inputEl.trigger("picker:close");
    }
    picker.emit("local::close pickerClose", picker);
  };
  _proto.onClosed = function onClosed() {
    var picker = this;
    picker.opened = false;
    picker.closing = false;
    if (!picker.inline) {
      nextTick(function() {
        if (picker.modal && picker.modal.el && picker.modal.destroy) {
          if (!picker.params.routableModals) {
            picker.modal.destroy();
          }
        }
        delete picker.modal;
      });
    }
    if (picker.$el) {
      picker.$el.trigger("picker:closed");
    }
    if (picker.$inputEl) {
      picker.$inputEl.trigger("picker:closed");
    }
    picker.emit("local::closed pickerClosed", picker);
  };
  _proto.open = function open12() {
    var picker = this;
    var app = picker.app, opened = picker.opened, inline = picker.inline, $inputEl = picker.$inputEl, $scrollToEl = picker.$scrollToEl, params = picker.params;
    if (opened)
      return;
    if (picker.cols.length === 0 && params.cols.length) {
      params.cols.forEach(function(col) {
        picker.cols.push(col);
      });
    }
    if (inline) {
      picker.$el = dom7_default(picker.render());
      picker.$el[0].f7Picker = picker;
      picker.$containerEl.append(picker.$el);
      picker.onOpen();
      picker.onOpened();
      return;
    }
    var isPopover = picker.isPopover();
    var modalType = isPopover ? "popover" : "sheet";
    var modalParams = {
      targetEl: $inputEl,
      scrollToEl: $scrollToEl,
      content: picker.render(),
      backdrop: typeof params.backdrop !== "undefined" ? params.backdrop : isPopover,
      on: {
        open: function open13() {
          var modal = this;
          picker.modal = modal;
          picker.$el = isPopover ? modal.$el.find(".picker") : modal.$el;
          picker.$el[0].f7Picker = picker;
          picker.onOpen();
        },
        opened: function opened2() {
          picker.onOpened();
        },
        close: function close12() {
          picker.onClose();
        },
        closed: function closed() {
          picker.onClosed();
        }
      }
    };
    if (modalType === "sheet") {
      modalParams.push = params.sheetPush;
      modalParams.swipeToClose = params.sheetSwipeToClose;
    }
    if (params.routableModals && picker.view) {
      var _route;
      picker.view.router.navigate({
        url: picker.url,
        route: (_route = {
          path: picker.url
        }, _route[modalType] = modalParams, _route)
      });
    } else {
      picker.modal = app[modalType].create(modalParams);
      picker.modal.open();
    }
  };
  _proto.close = function close12() {
    var picker = this;
    var opened = picker.opened, inline = picker.inline;
    if (!opened)
      return;
    if (inline) {
      picker.onClose();
      picker.onClosed();
      return;
    }
    if (picker.params.routableModals && picker.view) {
      picker.view.router.back();
    } else {
      picker.modal.close();
    }
  };
  _proto.init = function init46() {
    var picker = this;
    picker.initInput();
    if (picker.inline) {
      picker.open();
      picker.emit("local::init pickerInit", picker);
      return;
    }
    if (!picker.initialized && picker.params.value) {
      picker.setValue(picker.params.value);
    }
    if (picker.$inputEl) {
      picker.attachInputEvents();
    }
    if (picker.params.closeByOutsideClick) {
      picker.attachHtmlEvents();
    }
    picker.emit("local::init pickerInit", picker);
  };
  _proto.destroy = function destroy50() {
    var picker = this;
    if (picker.destroyed)
      return;
    var $el = picker.$el;
    picker.emit("local::beforeDestroy pickerBeforeDestroy", picker);
    if ($el)
      $el.trigger("picker:beforedestroy");
    picker.close();
    if (picker.$inputEl) {
      picker.detachInputEvents();
    }
    if (picker.params.closeByOutsideClick) {
      picker.detachHtmlEvents();
    }
    if ($el && $el.length)
      delete picker.$el[0].f7Picker;
    deleteProps(picker);
    picker.destroyed = true;
  };
  _createClass3(Picker2, [{
    key: "view",
    get: function get2() {
      var app = this.app, params = this.params, $inputEl = this.$inputEl;
      var view;
      if (params.view) {
        view = params.view;
      } else if ($inputEl) {
        view = $inputEl.parents(".view").length && $inputEl.parents(".view")[0].f7View;
      }
      if (!view)
        view = app.views.main;
      return view;
    }
  }]);
  return Picker2;
}(class_default);
var picker_class_default = Picker;

// node_modules/framework7/esm/components/picker/picker.js
var picker_default = {
  name: "picker",
  static: {
    Picker: picker_class_default
  },
  create: function create26() {
    var app = this;
    app.picker = ConstructorMethods({
      defaultSelector: ".picker",
      constructor: picker_class_default,
      app,
      domProp: "f7Picker"
    });
    app.picker.close = function close12(el) {
      if (el === void 0) {
        el = ".picker";
      }
      var $el = dom7_default(el);
      if ($el.length === 0)
        return;
      var picker = $el[0].f7Picker;
      if (!picker || picker && !picker.opened)
        return;
      picker.close();
    };
  },
  params: {
    picker: {
      updateValuesOnMomentum: false,
      updateValuesOnTouchmove: true,
      updateValuesOnMousewheel: true,
      mousewheel: true,
      rotateEffect: false,
      momentumRatio: 7,
      freeMode: false,
      cols: [],
      containerEl: null,
      openIn: "auto",
      sheetPush: false,
      sheetSwipeToClose: void 0,
      backdrop: void 0,
      formatValue: null,
      inputEl: null,
      inputReadOnly: true,
      closeByOutsideClick: true,
      scrollToInput: true,
      scrollToEl: void 0,
      toolbar: true,
      toolbarCloseText: "Done",
      cssClass: null,
      routableModals: false,
      view: null,
      url: "select/",
      renderToolbar: null,
      render: null
    }
  }
};

// node_modules/framework7/esm/components/infinite-scroll/infinite-scroll.js
var InfiniteScroll = {
  handle: function handle(el, e) {
    var app = this;
    var $el = dom7_default(el);
    var scrollTop = $el[0].scrollTop;
    var scrollHeight = $el[0].scrollHeight;
    var height = $el[0].offsetHeight;
    var distance = $el[0].getAttribute("data-infinite-distance");
    var virtualListContainer = $el.find(".virtual-list");
    var virtualList;
    var onTop = $el.hasClass("infinite-scroll-top");
    if (!distance)
      distance = 50;
    if (typeof distance === "string" && distance.indexOf("%") >= 0) {
      distance = parseInt(distance, 10) / 100 * height;
    }
    if (distance > height)
      distance = height;
    if (onTop) {
      if (scrollTop < distance) {
        $el.trigger("infinite", e);
        app.emit("infinite", $el[0], e);
      }
    } else if (scrollTop + height >= scrollHeight - distance) {
      if (virtualListContainer.length > 0) {
        virtualList = virtualListContainer.eq(-1)[0].f7VirtualList;
        if (virtualList && !virtualList.reachEnd && !virtualList.params.updatableScroll) {
          return;
        }
      }
      $el.trigger("infinite", e);
      app.emit("infinite", $el[0], e);
    }
  },
  create: function create27(el) {
    var $el = dom7_default(el);
    var app = this;
    function scrollHandler(e) {
      app.infiniteScroll.handle(this, e);
    }
    $el.each(function(element) {
      element.f7InfiniteScrollHandler = scrollHandler;
      element.addEventListener("scroll", element.f7InfiniteScrollHandler);
    });
  },
  destroy: function destroy8(el) {
    var $el = dom7_default(el);
    $el.each(function(element) {
      element.removeEventListener("scroll", element.f7InfiniteScrollHandler);
      delete element.f7InfiniteScrollHandler;
    });
  }
};
var infinite_scroll_default = {
  name: "infiniteScroll",
  create: function create28() {
    var app = this;
    bindMethods(app, {
      infiniteScroll: InfiniteScroll
    });
  },
  on: {
    tabMounted: function tabMounted10(tabEl) {
      var app = this;
      var $tabEl = dom7_default(tabEl);
      var $isEls = $tabEl.find(".infinite-scroll-content");
      if ($tabEl.is(".infinite-scroll-content"))
        $isEls.add($tabEl);
      $isEls.each(function(el) {
        app.infiniteScroll.create(el);
      });
    },
    tabBeforeRemove: function tabBeforeRemove7(tabEl) {
      var $tabEl = dom7_default(tabEl);
      var app = this;
      var $isEls = $tabEl.find(".infinite-scroll-content");
      if ($tabEl.is(".infinite-scroll-content"))
        $isEls.add($tabEl);
      $isEls.each(function(el) {
        app.infiniteScroll.destroy(el);
      });
    },
    pageInit: function pageInit11(page) {
      var app = this;
      page.$el.find(".infinite-scroll-content").each(function(el) {
        app.infiniteScroll.create(el);
      });
    },
    pageBeforeRemove: function pageBeforeRemove8(page) {
      var app = this;
      page.$el.find(".infinite-scroll-content").each(function(el) {
        app.infiniteScroll.destroy(el);
      });
    }
  }
};

// node_modules/framework7/esm/components/pull-to-refresh/pull-to-refresh-class.js
function _assertThisInitialized17(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose17(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf17(subClass, superClass);
}
function _setPrototypeOf17(o, p) {
  _setPrototypeOf17 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf17(o, p);
}
var PullToRefresh = function(_Framework7Class) {
  _inheritsLoose17(PullToRefresh2, _Framework7Class);
  function PullToRefresh2(app, el) {
    var _this;
    _this = _Framework7Class.call(this, {}, [app]) || this;
    var ptr = _assertThisInitialized17(_this);
    var device2 = getDevice();
    var support2 = getSupport();
    var $el = dom7_default(el);
    var $preloaderEl = $el.find(".ptr-preloader");
    ptr.$el = $el;
    ptr.el = $el[0];
    ptr.app = app;
    ptr.bottom = ptr.$el.hasClass("ptr-bottom");
    ptr.useModulesParams({});
    var isMaterial = app.theme === "md";
    var isIos = app.theme === "ios";
    var isAurora = app.theme === "aurora";
    ptr.done = function done() {
      var $transitionTarget = isMaterial ? $preloaderEl : $el;
      var onTranstionEnd = function onTranstionEnd2(e) {
        if (dom7_default(e.target).closest($preloaderEl).length)
          return;
        $el.removeClass("ptr-transitioning ptr-pull-up ptr-pull-down ptr-closing");
        $el.trigger("ptr:done");
        ptr.emit("local::done ptrDone", $el[0]);
        $transitionTarget.off("transitionend", onTranstionEnd2);
      };
      $transitionTarget.on("transitionend", onTranstionEnd);
      $el.removeClass("ptr-refreshing").addClass("ptr-transitioning ptr-closing");
      return ptr;
    };
    ptr.refresh = function refresh2() {
      if ($el.hasClass("ptr-refreshing"))
        return ptr;
      $el.addClass("ptr-transitioning ptr-refreshing");
      $el.trigger("ptr:refresh", ptr.done);
      ptr.emit("local::refresh ptrRefresh", $el[0], ptr.done);
      return ptr;
    };
    ptr.mousewheel = $el.attr("data-ptr-mousewheel") === "true";
    var touchId;
    var isTouched;
    var isMoved;
    var touchesStart = {};
    var isScrolling;
    var touchesDiff;
    var refresh = false;
    var useTranslate = false;
    var forceUseTranslate = false;
    var startTranslate = 0;
    var translate;
    var scrollTop;
    var wasScrolled;
    var triggerDistance;
    var dynamicTriggerDistance;
    var pullStarted;
    var hasNavbar = false;
    var scrollHeight;
    var offsetHeight;
    var maxScrollTop;
    var $pageEl = $el.parents(".page");
    if ($pageEl.find(".navbar").length > 0 || $pageEl.parents(".view").children(".navbars").length > 0)
      hasNavbar = true;
    if ($pageEl.hasClass("no-navbar"))
      hasNavbar = false;
    if (!ptr.bottom) {
      var pageNavbarEl = app.navbar.getElByPage($pageEl[0]);
      if (pageNavbarEl) {
        var $pageNavbarEl = dom7_default(pageNavbarEl);
        var isLargeTransparent = $pageNavbarEl.hasClass("navbar-large-transparent") || $pageNavbarEl.hasClass("navbar-large") && $pageNavbarEl.hasClass("navbar-transparent");
        var isTransparent = $pageNavbarEl.hasClass("navbar-transparent") && !$pageNavbarEl.hasClass("navbar-large");
        if (isLargeTransparent) {
          $el.addClass("ptr-with-navbar-large-transparent");
        } else if (isTransparent) {
          $el.addClass("ptr-with-navbar-transparent");
        }
      }
    }
    if (!hasNavbar && !ptr.bottom)
      $el.addClass("ptr-no-navbar");
    if ($el.attr("data-ptr-distance")) {
      dynamicTriggerDistance = true;
    } else if (isMaterial) {
      triggerDistance = 66;
    } else if (isIos) {
      triggerDistance = 44;
    } else if (isAurora) {
      triggerDistance = 38;
    }
    function setPreloaderProgress(progress) {
      if (progress === void 0) {
        progress = 0;
      }
      var $bars = $preloaderEl.find(".preloader-inner-line");
      var perBarProgress = 1 / $bars.length;
      $bars.forEach(function(barEl, barIndex) {
        var barProgress = (progress - barIndex * perBarProgress) / perBarProgress;
        barEl.style.opacity = Math.max(Math.min(barProgress, 1), 0) * 0.27;
      });
    }
    function unsetPreloaderProgress() {
      $preloaderEl.find(".preloader-inner-line").css("opacity", "");
    }
    function handleTouchStart(e) {
      if (isTouched) {
        if (device2.os === "android") {
          if ("targetTouches" in e && e.targetTouches.length > 1)
            return;
        } else
          return;
      }
      if ($el.hasClass("ptr-refreshing")) {
        return;
      }
      if (dom7_default(e.target).closest(".sortable-handler, .ptr-ignore, .card-expandable.card-opened").length)
        return;
      isMoved = false;
      pullStarted = false;
      isTouched = true;
      isScrolling = void 0;
      wasScrolled = void 0;
      if (e.type === "touchstart")
        touchId = e.targetTouches[0].identifier;
      touchesStart.x = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
      touchesStart.y = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
    }
    function handleTouchMove(e) {
      if (!isTouched)
        return;
      var pageX;
      var pageY;
      var touch;
      if (e.type === "touchmove") {
        if (touchId && e.touches) {
          for (var i = 0; i < e.touches.length; i += 1) {
            if (e.touches[i].identifier === touchId) {
              touch = e.touches[i];
            }
          }
        }
        if (!touch)
          touch = e.targetTouches[0];
        pageX = touch.pageX;
        pageY = touch.pageY;
      } else {
        pageX = e.pageX;
        pageY = e.pageY;
      }
      if (!pageX || !pageY)
        return;
      if (typeof isScrolling === "undefined") {
        isScrolling = !!(isScrolling || Math.abs(pageY - touchesStart.y) > Math.abs(pageX - touchesStart.x));
      }
      if (!isScrolling) {
        isTouched = false;
        return;
      }
      scrollTop = $el[0].scrollTop;
      if (!isMoved) {
        $el.removeClass("ptr-transitioning");
        if (isIos) {
          setPreloaderProgress(0);
        }
        var targetIsScrollable;
        scrollHeight = $el[0].scrollHeight;
        offsetHeight = $el[0].offsetHeight;
        if (ptr.bottom) {
          maxScrollTop = scrollHeight - offsetHeight;
        }
        if (scrollTop > scrollHeight) {
          isTouched = false;
          return;
        }
        var $ptrWatchScrollable = dom7_default(e.target).closest(".ptr-watch-scroll");
        if ($ptrWatchScrollable.length) {
          $ptrWatchScrollable.each(function(ptrScrollableEl) {
            if (ptrScrollableEl === el)
              return;
            if (ptrScrollableEl.scrollHeight > ptrScrollableEl.offsetHeight && dom7_default(ptrScrollableEl).css("overflow") === "auto" && (!ptr.bottom && ptrScrollableEl.scrollTop > 0 || ptr.bottom && ptrScrollableEl.scrollTop < ptrScrollableEl.scrollHeight - ptrScrollableEl.offsetHeight)) {
              targetIsScrollable = true;
            }
          });
        }
        if (targetIsScrollable) {
          isTouched = false;
          return;
        }
        if (dynamicTriggerDistance) {
          triggerDistance = $el.attr("data-ptr-distance");
          if (triggerDistance.indexOf("%") >= 0)
            triggerDistance = scrollHeight * parseInt(triggerDistance, 10) / 100;
        }
        startTranslate = $el.hasClass("ptr-refreshing") ? triggerDistance : 0;
        if (scrollHeight === offsetHeight || device2.os !== "ios" || isMaterial) {
          useTranslate = true;
        } else {
          useTranslate = false;
        }
        forceUseTranslate = false;
      }
      isMoved = true;
      touchesDiff = pageY - touchesStart.y;
      if (typeof wasScrolled === "undefined" && (ptr.bottom ? scrollTop !== maxScrollTop : scrollTop !== 0))
        wasScrolled = true;
      var ptrStarted = ptr.bottom ? touchesDiff < 0 && scrollTop >= maxScrollTop || scrollTop > maxScrollTop : touchesDiff > 0 && scrollTop <= 0 || scrollTop < 0;
      if (ptrStarted) {
        if (device2.os === "ios" && parseInt(device2.osVersion.split(".")[0], 10) > 7) {
          if (!ptr.bottom && scrollTop === 0 && !wasScrolled)
            useTranslate = true;
          if (ptr.bottom && scrollTop === maxScrollTop && !wasScrolled)
            useTranslate = true;
        }
        if (!useTranslate && ptr.bottom && !isMaterial) {
          $el.css("-webkit-overflow-scrolling", "auto");
          $el.scrollTop(maxScrollTop);
          forceUseTranslate = true;
        }
        if (useTranslate || forceUseTranslate) {
          if (e.cancelable) {
            e.preventDefault();
          }
          translate = (ptr.bottom ? -1 * Math.pow(Math.abs(touchesDiff), 0.85) : Math.pow(touchesDiff, 0.85)) + startTranslate;
          if (isMaterial) {
            $preloaderEl.transform("translate3d(0," + translate + "px,0)").find(".ptr-arrow").transform("rotate(" + (180 * (Math.abs(touchesDiff) / 66) + 100) + "deg)");
          } else {
            if (ptr.bottom || isIos) {
              $el.children().transform("translate3d(0," + translate + "px,0)");
            } else {
              $el.transform("translate3d(0," + translate + "px,0)");
            }
            if (isIos) {
              $preloaderEl.transform("translate3d(0,0px,0)");
            }
          }
        } else if (isIos && !ptr.bottom) {
          $preloaderEl.transform("translate3d(0," + scrollTop + "px,0)");
        }
        var progress;
        if (isIos && !refresh) {
          progress = useTranslate || forceUseTranslate ? Math.pow(Math.abs(touchesDiff), 0.85) / triggerDistance : Math.abs(touchesDiff) / (triggerDistance * 2);
          setPreloaderProgress(progress);
        }
        if ((useTranslate || forceUseTranslate) && Math.pow(Math.abs(touchesDiff), 0.85) > triggerDistance || !useTranslate && Math.abs(touchesDiff) >= triggerDistance * 2) {
          refresh = true;
          $el.addClass("ptr-pull-up").removeClass("ptr-pull-down");
          unsetPreloaderProgress();
        } else {
          refresh = false;
          $el.removeClass("ptr-pull-up").addClass("ptr-pull-down");
        }
        if (!pullStarted) {
          $el.trigger("ptr:pullstart");
          ptr.emit("local::pullStart ptrPullStart", $el[0]);
          pullStarted = true;
        }
        $el.trigger("ptr:pullmove", {
          event: e,
          scrollTop,
          translate,
          touchesDiff
        });
        ptr.emit("local::pullMove ptrPullMove", $el[0], {
          event: e,
          scrollTop,
          translate,
          touchesDiff
        });
      } else {
        pullStarted = false;
        $el.removeClass("ptr-pull-up ptr-pull-down");
        refresh = false;
      }
    }
    function handleTouchEnd(e) {
      if (e.type === "touchend" && e.changedTouches && e.changedTouches.length > 0 && touchId) {
        if (e.changedTouches[0].identifier !== touchId) {
          isTouched = false;
          isScrolling = false;
          isMoved = false;
          touchId = null;
          return;
        }
      }
      if (!isTouched || !isMoved) {
        isTouched = false;
        isMoved = false;
        return;
      }
      if (translate) {
        $el.addClass("ptr-transitioning");
        translate = 0;
      }
      if (isMaterial) {
        $preloaderEl.transform("").find(".ptr-arrow").transform("");
      } else {
        $preloaderEl.transform("");
        if (ptr.bottom || isIos) {
          $el.children().transform("");
        } else {
          $el.transform("");
        }
      }
      if (!useTranslate && ptr.bottom && !isMaterial) {
        $el.css("-webkit-overflow-scrolling", "");
      }
      if (refresh) {
        $el.addClass("ptr-refreshing");
        $el.trigger("ptr:refresh", ptr.done);
        ptr.emit("local::refresh ptrRefresh", $el[0], ptr.done);
      } else {
        $el.removeClass("ptr-pull-down");
      }
      isTouched = false;
      isMoved = false;
      if (pullStarted) {
        $el.trigger("ptr:pullend");
        ptr.emit("local::pullEnd ptrPullEnd", $el[0]);
      }
    }
    var mousewheelTimeout;
    var mousewheelMoved;
    var mousewheelAllow = true;
    var mousewheelTranslate = 0;
    function handleMouseWheelRelease() {
      mousewheelAllow = true;
      mousewheelMoved = false;
      mousewheelTranslate = 0;
      if (translate) {
        $el.addClass("ptr-transitioning");
        translate = 0;
      }
      if (isMaterial) {
        $preloaderEl.transform("").find(".ptr-arrow").transform("");
      } else {
        $preloaderEl.transform("");
        if (ptr.bottom) {
          $el.children().transform("");
        } else {
          $el.transform("");
        }
      }
      if (refresh) {
        $el.addClass("ptr-refreshing");
        $el.trigger("ptr:refresh", ptr.done);
        ptr.emit("local::refresh ptrRefresh", $el[0], ptr.done);
      } else {
        $el.removeClass("ptr-pull-down");
      }
      if (pullStarted) {
        $el.trigger("ptr:pullend");
        ptr.emit("local::pullEnd ptrPullEnd", $el[0]);
      }
    }
    function handleMouseWheel(e) {
      if (!mousewheelAllow)
        return;
      var deltaX = e.deltaX, deltaY = e.deltaY;
      if (Math.abs(deltaX) > Math.abs(deltaY))
        return;
      if ($el.hasClass("ptr-refreshing")) {
        return;
      }
      if (dom7_default(e.target).closest(".sortable-handler, .ptr-ignore, .card-expandable.card-opened").length)
        return;
      clearTimeout(mousewheelTimeout);
      scrollTop = $el[0].scrollTop;
      if (!mousewheelMoved) {
        $el.removeClass("ptr-transitioning");
        if (isIos) {
          setPreloaderProgress(0);
        }
        var targetIsScrollable;
        scrollHeight = $el[0].scrollHeight;
        offsetHeight = $el[0].offsetHeight;
        if (ptr.bottom) {
          maxScrollTop = scrollHeight - offsetHeight;
        }
        if (scrollTop > scrollHeight) {
          mousewheelAllow = false;
          return;
        }
        var $ptrWatchScrollable = dom7_default(e.target).closest(".ptr-watch-scroll");
        if ($ptrWatchScrollable.length) {
          $ptrWatchScrollable.each(function(ptrScrollableEl) {
            if (ptrScrollableEl === el)
              return;
            if (ptrScrollableEl.scrollHeight > ptrScrollableEl.offsetHeight && dom7_default(ptrScrollableEl).css("overflow") === "auto" && (!ptr.bottom && ptrScrollableEl.scrollTop > 0 || ptr.bottom && ptrScrollableEl.scrollTop < ptrScrollableEl.scrollHeight - ptrScrollableEl.offsetHeight)) {
              targetIsScrollable = true;
            }
          });
        }
        if (targetIsScrollable) {
          mousewheelAllow = false;
          return;
        }
        if (dynamicTriggerDistance) {
          triggerDistance = $el.attr("data-ptr-distance");
          if (triggerDistance.indexOf("%") >= 0)
            triggerDistance = scrollHeight * parseInt(triggerDistance, 10) / 100;
        }
      }
      isMoved = true;
      mousewheelTranslate -= deltaY;
      touchesDiff = mousewheelTranslate;
      if (typeof wasScrolled === "undefined" && (ptr.bottom ? scrollTop !== maxScrollTop : scrollTop !== 0))
        wasScrolled = true;
      var ptrStarted = ptr.bottom ? touchesDiff < 0 && scrollTop >= maxScrollTop || scrollTop > maxScrollTop : touchesDiff > 0 && scrollTop <= 0 || scrollTop < 0;
      if (ptrStarted) {
        if (e.cancelable) {
          e.preventDefault();
        }
        translate = touchesDiff;
        if (Math.abs(translate) > triggerDistance) {
          translate = triggerDistance + Math.pow(Math.abs(translate) - triggerDistance, 0.7);
          if (ptr.bottom)
            translate = -translate;
        }
        if (isMaterial) {
          $preloaderEl.transform("translate3d(0," + translate + "px,0)").find(".ptr-arrow").transform("rotate(" + (180 * (Math.abs(touchesDiff) / 66) + 100) + "deg)");
        } else {
          if (ptr.bottom) {
            $el.children().transform("translate3d(0," + translate + "px,0)");
          } else {
            $el.transform("translate3d(0," + translate + "px,0)");
            if (isIos) {
              $preloaderEl.transform("translate3d(0," + -translate + "px,0)");
            }
          }
        }
        var progress;
        if (isIos && !refresh) {
          progress = Math.abs(translate) / triggerDistance;
          setPreloaderProgress(progress);
        }
        if (Math.abs(translate) > triggerDistance) {
          refresh = true;
          $el.addClass("ptr-pull-up").removeClass("ptr-pull-down");
          unsetPreloaderProgress();
        } else {
          refresh = false;
          $el.removeClass("ptr-pull-up").addClass("ptr-pull-down");
        }
        if (!pullStarted) {
          $el.trigger("ptr:pullstart");
          ptr.emit("local::pullStart ptrPullStart", $el[0]);
          pullStarted = true;
        }
        $el.trigger("ptr:pullmove", {
          event: e,
          scrollTop,
          translate,
          touchesDiff
        });
        ptr.emit("local::pullMove ptrPullMove", $el[0], {
          event: e,
          scrollTop,
          translate,
          touchesDiff
        });
      } else {
        pullStarted = false;
        $el.removeClass("ptr-pull-up ptr-pull-down");
        refresh = false;
      }
      mousewheelTimeout = setTimeout(handleMouseWheelRelease, 300);
    }
    if (!$pageEl.length || !$el.length)
      return ptr || _assertThisInitialized17(_this);
    $el[0].f7PullToRefresh = ptr;
    ptr.attachEvents = function attachEvents2() {
      var passive = support2.passiveListener ? {
        passive: true
      } : false;
      $el.on(app.touchEvents.start, handleTouchStart, passive);
      app.on("touchmove:active", handleTouchMove);
      app.on("touchend:passive", handleTouchEnd);
      if (ptr.mousewheel && !ptr.bottom) {
        $el.on("wheel", handleMouseWheel);
      }
    };
    ptr.detachEvents = function detachEvents2() {
      var passive = support2.passiveListener ? {
        passive: true
      } : false;
      $el.off(app.touchEvents.start, handleTouchStart, passive);
      app.off("touchmove:active", handleTouchMove);
      app.off("touchend:passive", handleTouchEnd);
      if (ptr.mousewheel && !ptr.bottom) {
        $el.off("wheel", handleMouseWheel);
      }
    };
    ptr.useModules();
    ptr.init();
    return ptr || _assertThisInitialized17(_this);
  }
  var _proto = PullToRefresh2.prototype;
  _proto.init = function init46() {
    var ptr = this;
    ptr.attachEvents();
  };
  _proto.destroy = function destroy50() {
    var ptr = this;
    ptr.emit("local::beforeDestroy ptrBeforeDestroy", ptr);
    ptr.$el.trigger("ptr:beforedestroy");
    delete ptr.el.f7PullToRefresh;
    ptr.detachEvents();
    deleteProps(ptr);
    ptr = null;
  };
  return PullToRefresh2;
}(class_default);
var pull_to_refresh_class_default = PullToRefresh;

// node_modules/framework7/esm/components/pull-to-refresh/pull-to-refresh.js
var pull_to_refresh_default = {
  name: "pullToRefresh",
  create: function create29() {
    var app = this;
    app.ptr = extend(ConstructorMethods({
      defaultSelector: ".ptr-content",
      constructor: pull_to_refresh_class_default,
      app,
      domProp: "f7PullToRefresh"
    }), {
      done: function done(el) {
        var ptr = app.ptr.get(el);
        if (ptr)
          return ptr.done();
        return void 0;
      },
      refresh: function refresh(el) {
        var ptr = app.ptr.get(el);
        if (ptr)
          return ptr.refresh();
        return void 0;
      }
    });
  },
  static: {
    PullToRefresh: pull_to_refresh_class_default
  },
  on: {
    tabMounted: function tabMounted11(tabEl) {
      var app = this;
      var $tabEl = dom7_default(tabEl);
      var $ptrEls = $tabEl.find(".ptr-content");
      if ($tabEl.is(".ptr-content"))
        $ptrEls.add($tabEl);
      $ptrEls.each(function(el) {
        app.ptr.create(el);
      });
    },
    tabBeforeRemove: function tabBeforeRemove8(tabEl) {
      var $tabEl = dom7_default(tabEl);
      var app = this;
      var $ptrEls = $tabEl.find(".ptr-content");
      if ($tabEl.is(".ptr-content"))
        $ptrEls.add($tabEl);
      $ptrEls.each(function(el) {
        app.ptr.destroy(el);
      });
    },
    pageInit: function pageInit12(page) {
      var app = this;
      page.$el.find(".ptr-content").each(function(el) {
        app.ptr.create(el);
      });
    },
    pageBeforeRemove: function pageBeforeRemove9(page) {
      var app = this;
      page.$el.find(".ptr-content").each(function(el) {
        app.ptr.destroy(el);
      });
    }
  }
};

// node_modules/framework7/esm/components/lazy/lazy.js
var Lazy = {
  destroy: function destroy9(pageEl) {
    var $pageEl = dom7_default(pageEl).closest(".page");
    if (!$pageEl.length)
      return;
    if ($pageEl[0].f7LazyDestroy) {
      $pageEl[0].f7LazyDestroy();
    }
  },
  create: function create30(pageEl) {
    var app = this;
    var window2 = getWindow();
    var support2 = getSupport();
    var $pageEl = dom7_default(pageEl).closest(".page").eq(0);
    var $lazyLoadImages = $pageEl.find(".lazy");
    if ($lazyLoadImages.length === 0 && !$pageEl.hasClass("lazy"))
      return;
    var placeholderSrc = app.params.lazy.placeholder;
    if (placeholderSrc !== false) {
      $lazyLoadImages.each(function(lazyEl) {
        if (dom7_default(lazyEl).attr("data-src") && !dom7_default(lazyEl).attr("src"))
          dom7_default(lazyEl).attr("src", placeholderSrc);
      });
    }
    var imagesSequence = [];
    var imageIsLoading = false;
    function onImageComplete(lazyEl) {
      if (imagesSequence.indexOf(lazyEl) >= 0) {
        imagesSequence.splice(imagesSequence.indexOf(lazyEl), 1);
      }
      imageIsLoading = false;
      if (app.params.lazy.sequential && imagesSequence.length > 0) {
        imageIsLoading = true;
        app.lazy.loadImage(imagesSequence[0], onImageComplete);
      }
    }
    function observerCallback(entries, observer2) {
      entries.forEach(function(entry) {
        if (entry.isIntersecting) {
          if (app.params.lazy.sequential && imageIsLoading) {
            if (imagesSequence.indexOf(entry.target) < 0)
              imagesSequence.push(entry.target);
            return;
          }
          imageIsLoading = true;
          app.lazy.loadImage(entry.target, onImageComplete);
          observer2.unobserve(entry.target);
        }
      });
    }
    if (app.params.lazy.observer && support2.intersectionObserver) {
      var observer = $pageEl[0].f7LazyObserver;
      if (!observer) {
        observer = new window2.IntersectionObserver(observerCallback, {
          root: $pageEl[0]
        });
      }
      $lazyLoadImages.each(function(el) {
        if (el.f7LazyObserverAdded)
          return;
        el.f7LazyObserverAdded = true;
        observer.observe(el);
      });
      if (!$pageEl[0].f7LazyDestroy) {
        $pageEl[0].f7LazyDestroy = function() {
          observer.disconnect();
          delete $pageEl[0].f7LazyDestroy;
          delete $pageEl[0].f7LazyObserver;
        };
      }
      return;
    }
    function lazyHandler() {
      app.lazy.load($pageEl, function(lazyEl) {
        if (app.params.lazy.sequential && imageIsLoading) {
          if (imagesSequence.indexOf(lazyEl) < 0)
            imagesSequence.push(lazyEl);
          return;
        }
        imageIsLoading = true;
        app.lazy.loadImage(lazyEl, onImageComplete);
      });
    }
    function attachEvents2() {
      $pageEl[0].f7LazyAttached = true;
      $pageEl.on("lazy", lazyHandler);
      $pageEl.on("scroll", lazyHandler, true);
      $pageEl.find(".tab").on("tab:mounted tab:show", lazyHandler);
      app.on("resize", lazyHandler);
    }
    function detachEvents2() {
      $pageEl[0].f7LazyAttached = false;
      delete $pageEl[0].f7LazyAttached;
      $pageEl.off("lazy", lazyHandler);
      $pageEl.off("scroll", lazyHandler, true);
      $pageEl.find(".tab").off("tab:mounted tab:show", lazyHandler);
      app.off("resize", lazyHandler);
    }
    if (!$pageEl[0].f7LazyDestroy) {
      $pageEl[0].f7LazyDestroy = detachEvents2;
    }
    if (!$pageEl[0].f7LazyAttached) {
      attachEvents2();
    }
    lazyHandler();
  },
  isInViewport: function isInViewport(lazyEl) {
    var app = this;
    var rect = lazyEl.getBoundingClientRect();
    var threshold = app.params.lazy.threshold || 0;
    return rect.top >= 0 - threshold && rect.left >= 0 - threshold && rect.top <= app.height + threshold && rect.left <= app.width + threshold;
  },
  loadImage: function loadImage(imageEl, callback) {
    var app = this;
    var window2 = getWindow();
    var $imageEl = dom7_default(imageEl);
    var bg = $imageEl.attr("data-background");
    var src = bg || $imageEl.attr("data-src");
    function onLoad() {
      $imageEl.removeClass("lazy").addClass("lazy-loaded");
      if (bg) {
        $imageEl.css("background-image", "url(" + src + ")");
      } else if (src) {
        $imageEl.attr("src", src);
      }
      if (callback)
        callback(imageEl);
      $imageEl.trigger("lazy:loaded");
      app.emit("lazyLoaded", $imageEl[0]);
    }
    if (!src) {
      $imageEl.trigger("lazy:load");
      app.emit("lazyLoad", $imageEl[0]);
      onLoad();
      return;
    }
    function onError() {
      $imageEl.removeClass("lazy").addClass("lazy-loaded");
      if (bg) {
        $imageEl.css("background-image", "url(" + (app.params.lazy.placeholder || "") + ")");
      } else {
        $imageEl.attr("src", app.params.lazy.placeholder || "");
      }
      if (callback)
        callback(imageEl);
      $imageEl.trigger("lazy:error");
      app.emit("lazyError", $imageEl[0]);
    }
    var image = new window2.Image();
    image.onload = onLoad;
    image.onerror = onError;
    image.src = src;
    $imageEl.removeAttr("data-src").removeAttr("data-background");
    $imageEl.trigger("lazy:load");
    app.emit("lazyLoad", $imageEl[0]);
  },
  load: function load(pageEl, callback) {
    var app = this;
    var $pageEl = dom7_default(pageEl);
    if (!$pageEl.hasClass("page"))
      $pageEl = $pageEl.parents(".page").eq(0);
    if ($pageEl.length === 0) {
      return;
    }
    $pageEl.find(".lazy").each(function(lazyEl) {
      var $lazyEl = dom7_default(lazyEl);
      if ($lazyEl.parents(".tab:not(.tab-active)").length > 0) {
        return;
      }
      if (app.lazy.isInViewport(lazyEl)) {
        if (callback)
          callback(lazyEl);
        else
          app.lazy.loadImage(lazyEl);
      }
    });
  }
};
var lazy_default = {
  name: "lazy",
  params: {
    lazy: {
      placeholder: "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABAQMAAAAl21bKAAAAA1BMVEXCwsK592mkAAAACklEQVQI12NgAAAAAgAB4iG8MwAAAABJRU5ErkJggg==",
      threshold: 0,
      sequential: true,
      observer: true
    }
  },
  create: function create31() {
    var app = this;
    bindMethods(app, {
      lazy: Lazy
    });
  },
  on: {
    pageInit: function pageInit13(page) {
      var app = this;
      if (page.$el.find(".lazy").length > 0 || page.$el.hasClass("lazy")) {
        app.lazy.create(page.$el);
      }
    },
    pageAfterIn: function pageAfterIn(page) {
      var app = this;
      var support2 = getSupport();
      if (app.params.lazy.observer && support2.intersectionObserver)
        return;
      if (page.$el.find(".lazy").length > 0 || page.$el.hasClass("lazy")) {
        app.lazy.create(page.$el);
      }
    },
    pageBeforeRemove: function pageBeforeRemove10(page) {
      var app = this;
      if (page.$el.find(".lazy").length > 0 || page.$el.hasClass("lazy")) {
        app.lazy.destroy(page.$el);
      }
    },
    tabMounted: function tabMounted12(tabEl) {
      var app = this;
      var $tabEl = dom7_default(tabEl);
      if ($tabEl.find(".lazy").length > 0 || $tabEl.hasClass("lazy")) {
        app.lazy.create($tabEl);
      }
    },
    tabBeforeRemove: function tabBeforeRemove9(tabEl) {
      var app = this;
      var support2 = getSupport();
      if (app.params.lazy.observer && support2.intersectionObserver)
        return;
      var $tabEl = dom7_default(tabEl);
      if ($tabEl.find(".lazy").length > 0 || $tabEl.hasClass("lazy")) {
        app.lazy.destroy($tabEl);
      }
    }
  }
};

// node_modules/framework7/esm/components/data-table/data-table-class.js
function _assertThisInitialized18(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose18(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf18(subClass, superClass);
}
function _setPrototypeOf18(o, p) {
  _setPrototypeOf18 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf18(o, p);
}
var DataTable = function(_Framework7Class) {
  _inheritsLoose18(DataTable2, _Framework7Class);
  function DataTable2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var table = _assertThisInitialized18(_this);
    var defaults = {};
    table.useModulesParams(defaults);
    table.params = extend(defaults, params);
    var $el = dom7_default(table.params.el);
    if ($el.length === 0)
      return _assertThisInitialized18(_this);
    table.$el = $el;
    table.el = $el[0];
    if (table.$el[0].f7DataTable) {
      var instance = table.$el[0].f7DataTable;
      table.destroy();
      return instance || _assertThisInitialized18(_this);
    }
    table.$el[0].f7DataTable = table;
    extend(table, {
      collapsible: $el.hasClass("data-table-collapsible"),
      $headerEl: $el.find(".data-table-header"),
      $headerSelectedEl: $el.find(".data-table-header-selected")
    });
    function handleChange(e) {
      if (e.detail && e.detail.sentByF7DataTable) {
        return;
      }
      var $inputEl = dom7_default(this);
      var checked = $inputEl[0].checked;
      var columnIndex = $inputEl.parents("td,th").index();
      if ($inputEl.parents("thead").length > 0) {
        if (columnIndex === 0) {
          $el.find("tbody tr")[checked ? "addClass" : "removeClass"]("data-table-row-selected");
        }
        $el.find("tbody tr td:nth-child(" + (columnIndex + 1) + ") input").prop("checked", checked).trigger("change", {
          sentByF7DataTable: true
        });
        $inputEl.prop("indeterminate", false);
      } else {
        if (columnIndex === 0) {
          $inputEl.parents("tr")[checked ? "addClass" : "removeClass"]("data-table-row-selected");
        }
        var checkedRows = $el.find("tbody .checkbox-cell:nth-child(" + (columnIndex + 1) + ') input[type="checkbox"]:checked').length;
        var totalRows = $el.find("tbody tr").length;
        var $headCheckboxEl = $el.find("thead .checkbox-cell:nth-child(" + (columnIndex + 1) + ') input[type="checkbox"]');
        if (!checked) {
          $headCheckboxEl.prop("checked", false);
        } else if (checkedRows === totalRows) {
          $headCheckboxEl.prop("checked", true).trigger("change", {
            sentByF7DataTable: true
          });
        }
        $headCheckboxEl.prop("indeterminate", checkedRows > 0 && checkedRows < totalRows);
      }
      table.checkSelectedHeader();
    }
    function handleSortableClick() {
      var $cellEl = dom7_default(this);
      var isActive = $cellEl.hasClass("sortable-cell-active");
      var currentSort = $cellEl.hasClass("sortable-desc") ? "desc" : "asc";
      var newSort;
      if (isActive) {
        newSort = currentSort === "desc" ? "asc" : "desc";
        $cellEl.removeClass("sortable-desc sortable-asc").addClass("sortable-" + newSort);
      } else {
        $el.find("thead .sortable-cell-active").removeClass("sortable-cell-active");
        $cellEl.addClass("sortable-cell-active");
        newSort = currentSort;
      }
      $cellEl.trigger("datatable:sort", newSort);
      table.emit("local::sort dataTableSort", table, newSort);
    }
    table.attachEvents = function attachEvents2() {
      table.$el.on("change", '.checkbox-cell input[type="checkbox"]', handleChange);
      table.$el.find("thead .sortable-cell").on("click", handleSortableClick);
    };
    table.detachEvents = function detachEvents2() {
      table.$el.off("change", '.checkbox-cell input[type="checkbox"]', handleChange);
      table.$el.find("thead .sortable-cell").off("click", handleSortableClick);
    };
    table.useModules();
    table.init();
    return table || _assertThisInitialized18(_this);
  }
  var _proto = DataTable2.prototype;
  _proto.setCollapsibleLabels = function setCollapsibleLabels() {
    var table = this;
    if (!table.collapsible)
      return;
    table.$el.find("tbody td:not(.checkbox-cell)").each(function(el) {
      var $el = dom7_default(el);
      var elIndex = $el.index();
      var collpsibleTitle = $el.attr("data-collapsible-title");
      if (!collpsibleTitle && collpsibleTitle !== "") {
        $el.attr("data-collapsible-title", table.$el.find("thead th").eq(elIndex).text());
      }
    });
  };
  _proto.checkSelectedHeader = function checkSelectedHeader() {
    var table = this;
    if (table.$headerEl.length > 0 && table.$headerSelectedEl.length > 0) {
      var checkedItems = table.$el.find("tbody .checkbox-cell input:checked").length;
      table.$el[checkedItems > 0 ? "addClass" : "removeClass"]("data-table-has-checked");
      table.$headerSelectedEl.find(".data-table-selected-count").text(checkedItems);
    }
  };
  _proto.init = function init46() {
    var table = this;
    table.attachEvents();
    table.setCollapsibleLabels();
    table.checkSelectedHeader();
  };
  _proto.destroy = function destroy50() {
    var table = this;
    table.$el.trigger("datatable:beforedestroy");
    table.emit("local::beforeDestroy dataTableBeforeDestroy", table);
    table.attachEvents();
    if (table.$el[0]) {
      table.$el[0].f7DataTable = null;
      delete table.$el[0].f7DataTable;
    }
    deleteProps(table);
    table = null;
  };
  return DataTable2;
}(class_default);
var data_table_class_default = DataTable;

// node_modules/framework7/esm/components/data-table/data-table.js
var data_table_default = {
  name: "dataTable",
  static: {
    DataTable: data_table_class_default
  },
  create: function create32() {
    var app = this;
    app.dataTable = ConstructorMethods({
      defaultSelector: ".data-table",
      constructor: data_table_class_default,
      app,
      domProp: "f7DataTable"
    });
  },
  on: {
    tabBeforeRemove: function tabBeforeRemove10(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".data-table-init").each(function(tableEl) {
        app.dataTable.destroy(tableEl);
      });
    },
    tabMounted: function tabMounted13(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".data-table-init").each(function(tableEl) {
        app.dataTable.create({
          el: tableEl
        });
      });
    },
    pageBeforeRemove: function pageBeforeRemove11(page) {
      var app = this;
      page.$el.find(".data-table-init").each(function(tableEl) {
        app.dataTable.destroy(tableEl);
      });
    },
    pageInit: function pageInit14(page) {
      var app = this;
      page.$el.find(".data-table-init").each(function(tableEl) {
        app.dataTable.create({
          el: tableEl
        });
      });
    }
  },
  vnode: {
    "data-table-init": {
      insert: function insert9(vnode) {
        var app = this;
        var tableEl = vnode.elm;
        app.dataTable.create({
          el: tableEl
        });
      },
      destroy: function destroy10(vnode) {
        var app = this;
        var tableEl = vnode.elm;
        app.dataTable.destroy(tableEl);
      }
    }
  }
};

// node_modules/framework7/esm/components/fab/fab.js
function _extends2() {
  _extends2 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends2.apply(this, arguments);
}
var Fab = {
  morphOpen: function morphOpen(fabEl, targetEl) {
    var app = this;
    var $fabEl = dom7_default(fabEl);
    var $targetEl = dom7_default(targetEl);
    if ($targetEl.length === 0)
      return;
    $targetEl.transition(0).addClass("fab-morph-target-visible");
    var target = {
      width: $targetEl[0].offsetWidth,
      height: $targetEl[0].offsetHeight,
      offset: $targetEl.offset(),
      borderRadius: $targetEl.css("border-radius"),
      zIndex: $targetEl.css("z-index")
    };
    var fab = {
      width: $fabEl[0].offsetWidth,
      height: $fabEl[0].offsetHeight,
      offset: $fabEl.offset(),
      translateX: getTranslate($fabEl[0], "x"),
      translateY: getTranslate($fabEl[0], "y")
    };
    $fabEl[0].f7FabMorphData = {
      $targetEl,
      target,
      fab
    };
    var diffX = fab.offset.left + fab.width / 2 - (target.offset.left + target.width / 2) - fab.translateX;
    var diffY = fab.offset.top + fab.height / 2 - (target.offset.top + target.height / 2) - fab.translateY;
    var scaleX = target.width / fab.width;
    var scaleY = target.height / fab.height;
    var borderRadius = Math.ceil(parseInt(target.borderRadius, 10) / Math.max(scaleX, scaleY));
    if (borderRadius > 0)
      borderRadius += 2;
    $fabEl[0].f7FabMorphResizeHandler = function resizeHandler() {
      $fabEl.transition(0).transform("");
      $targetEl.transition(0);
      target.width = $targetEl[0].offsetWidth;
      target.height = $targetEl[0].offsetHeight;
      target.offset = $targetEl.offset();
      fab.offset = $fabEl.offset();
      var diffXNew = fab.offset.left + fab.width / 2 - (target.offset.left + target.width / 2) - fab.translateX;
      var diffYNew = fab.offset.top + fab.height / 2 - (target.offset.top + target.height / 2) - fab.translateY;
      var scaleXNew = target.width / fab.width;
      var scaleYNew = target.height / fab.height;
      $fabEl.transform("translate3d(" + -diffXNew + "px, " + -diffYNew + "px, 0) scale(" + scaleXNew + ", " + scaleYNew + ")");
    };
    $targetEl.css("opacity", 0).transform("scale(" + 1 / scaleX + ", " + 1 / scaleY + ")");
    $fabEl.addClass("fab-opened").css("z-index", target.zIndex - 1).transform("translate3d(" + -diffX + "px, " + -diffY + "px, 0)");
    $fabEl.transitionEnd(function() {
      $targetEl.transition("");
      nextFrame(function() {
        $targetEl.css("opacity", 1).transform("scale(1,1)");
        $fabEl.transform("translate3d(" + -diffX + "px, " + -diffY + "px, 0) scale(" + scaleX + ", " + scaleY + ")").css("border-radius", borderRadius + "px").css("box-shadow", "none").css("opacity", "0");
      });
      app.on("resize", $fabEl[0].f7FabMorphResizeHandler);
      if ($targetEl.parents(".page-content").length > 0) {
        $targetEl.parents(".page-content").on("scroll", $fabEl[0].f7FabMorphResizeHandler);
      }
    });
  },
  morphClose: function morphClose(fabEl) {
    var app = this;
    var $fabEl = dom7_default(fabEl);
    var morphData = $fabEl[0].f7FabMorphData;
    if (!morphData)
      return;
    var $targetEl = morphData.$targetEl, target = morphData.target, fab = morphData.fab;
    if ($targetEl.length === 0)
      return;
    var diffX = fab.offset.left + fab.width / 2 - (target.offset.left + target.width / 2) - fab.translateX;
    var diffY = fab.offset.top + fab.height / 2 - (target.offset.top + target.height / 2) - fab.translateY;
    var scaleX = target.width / fab.width;
    var scaleY = target.height / fab.height;
    app.off("resize", $fabEl[0].f7FabMorphResizeHandler);
    if ($targetEl.parents(".page-content").length > 0) {
      $targetEl.parents(".page-content").off("scroll", $fabEl[0].f7FabMorphResizeHandler);
    }
    $targetEl.css("opacity", 0).transform("scale(" + 1 / scaleX + ", " + 1 / scaleY + ")");
    $fabEl.transition("").css("box-shadow", "").css("border-radius", "").css("opacity", "1").transform("translate3d(" + -diffX + "px, " + -diffY + "px, 0)");
    $fabEl.transitionEnd(function() {
      $fabEl.css("z-index", "").removeClass("fab-opened").transform("");
      nextFrame(function() {
        $fabEl.transitionEnd(function() {
          $targetEl.removeClass("fab-morph-target-visible").css("opacity", "").transform("").transition("");
        });
      });
    });
  },
  open: function open7(fabEl, targetEl) {
    var app = this;
    var $fabEl = dom7_default(fabEl).eq(0);
    var $buttonsEl = $fabEl.find(".fab-buttons");
    if (!$fabEl.length)
      return;
    if ($fabEl.hasClass("fab-opened"))
      return;
    if (!$buttonsEl.length && !$fabEl.hasClass("fab-morph"))
      return;
    if (app.fab.openedEl) {
      if (app.fab.openedEl === $fabEl[0])
        return;
      app.fab.close(app.fab.openedEl);
    }
    app.fab.openedEl = $fabEl[0];
    if ($fabEl.hasClass("fab-morph")) {
      app.fab.morphOpen($fabEl, targetEl || $fabEl.attr("data-morph-to"));
    } else {
      $fabEl.addClass("fab-opened");
    }
    $fabEl.siblings(".fab-backdrop").addClass("backdrop-in");
    $fabEl.trigger("fab:open");
  },
  close: function close7(fabEl) {
    if (fabEl === void 0) {
      fabEl = ".fab-opened";
    }
    var app = this;
    var $fabEl = dom7_default(fabEl).eq(0);
    var $buttonsEl = $fabEl.find(".fab-buttons");
    if (!$fabEl.length)
      return;
    if (!$fabEl.hasClass("fab-opened"))
      return;
    if (!$buttonsEl.length && !$fabEl.hasClass("fab-morph"))
      return;
    app.fab.openedEl = null;
    if ($fabEl.hasClass("fab-morph")) {
      app.fab.morphClose($fabEl);
    } else {
      $fabEl.removeClass("fab-opened");
    }
    $fabEl.siblings(".fab-backdrop").removeClass("backdrop-in");
    $fabEl.trigger("fab:close");
  },
  toggle: function toggle5(fabEl) {
    var app = this;
    var $fabEl = dom7_default(fabEl);
    if (!$fabEl.hasClass("fab-opened"))
      app.fab.open(fabEl);
    else
      app.fab.close(fabEl);
  }
};
var fab_default = {
  name: "fab",
  create: function create33() {
    var app = this;
    bindMethods(app, {
      fab: _extends2({
        openedEl: null
      }, Fab)
    });
  },
  clicks: {
    ".fab > a": function open8($clickedEl) {
      var app = this;
      app.fab.toggle($clickedEl.parents(".fab"));
    },
    ".fab-open": function open9($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.fab.open(data.fab);
    },
    ".fab-close": function close8($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      app.fab.close(data.fab);
    },
    ".fab-backdrop": function close9() {
      var app = this;
      app.fab.close();
    }
  }
};

// node_modules/framework7/esm/components/searchbar/remove-diacritics.js
var defaultDiacriticsRemovalap = [{
  base: "A",
  letters: "A\u24B6\uFF21\xC0\xC1\xC2\u1EA6\u1EA4\u1EAA\u1EA8\xC3\u0100\u0102\u1EB0\u1EAE\u1EB4\u1EB2\u0226\u01E0\xC4\u01DE\u1EA2\xC5\u01FA\u01CD\u0200\u0202\u1EA0\u1EAC\u1EB6\u1E00\u0104\u023A\u2C6F"
}, {
  base: "AA",
  letters: "\uA732"
}, {
  base: "AE",
  letters: "\xC6\u01FC\u01E2"
}, {
  base: "AO",
  letters: "\uA734"
}, {
  base: "AU",
  letters: "\uA736"
}, {
  base: "AV",
  letters: "\uA738\uA73A"
}, {
  base: "AY",
  letters: "\uA73C"
}, {
  base: "B",
  letters: "B\u24B7\uFF22\u1E02\u1E04\u1E06\u0243\u0182\u0181"
}, {
  base: "C",
  letters: "C\u24B8\uFF23\u0106\u0108\u010A\u010C\xC7\u1E08\u0187\u023B\uA73E"
}, {
  base: "D",
  letters: "D\u24B9\uFF24\u1E0A\u010E\u1E0C\u1E10\u1E12\u1E0E\u0110\u018B\u018A\u0189\uA779"
}, {
  base: "DZ",
  letters: "\u01F1\u01C4"
}, {
  base: "Dz",
  letters: "\u01F2\u01C5"
}, {
  base: "E",
  letters: "E\u24BA\uFF25\xC8\xC9\xCA\u1EC0\u1EBE\u1EC4\u1EC2\u1EBC\u0112\u1E14\u1E16\u0114\u0116\xCB\u1EBA\u011A\u0204\u0206\u1EB8\u1EC6\u0228\u1E1C\u0118\u1E18\u1E1A\u0190\u018E"
}, {
  base: "F",
  letters: "F\u24BB\uFF26\u1E1E\u0191\uA77B"
}, {
  base: "G",
  letters: "G\u24BC\uFF27\u01F4\u011C\u1E20\u011E\u0120\u01E6\u0122\u01E4\u0193\uA7A0\uA77D\uA77E"
}, {
  base: "H",
  letters: "H\u24BD\uFF28\u0124\u1E22\u1E26\u021E\u1E24\u1E28\u1E2A\u0126\u2C67\u2C75\uA78D"
}, {
  base: "I",
  letters: "I\u24BE\uFF29\xCC\xCD\xCE\u0128\u012A\u012C\u0130\xCF\u1E2E\u1EC8\u01CF\u0208\u020A\u1ECA\u012E\u1E2C\u0197"
}, {
  base: "J",
  letters: "J\u24BF\uFF2A\u0134\u0248"
}, {
  base: "K",
  letters: "K\u24C0\uFF2B\u1E30\u01E8\u1E32\u0136\u1E34\u0198\u2C69\uA740\uA742\uA744\uA7A2"
}, {
  base: "L",
  letters: "L\u24C1\uFF2C\u013F\u0139\u013D\u1E36\u1E38\u013B\u1E3C\u1E3A\u0141\u023D\u2C62\u2C60\uA748\uA746\uA780"
}, {
  base: "LJ",
  letters: "\u01C7"
}, {
  base: "Lj",
  letters: "\u01C8"
}, {
  base: "M",
  letters: "M\u24C2\uFF2D\u1E3E\u1E40\u1E42\u2C6E\u019C"
}, {
  base: "N",
  letters: "N\u24C3\uFF2E\u01F8\u0143\xD1\u1E44\u0147\u1E46\u0145\u1E4A\u1E48\u0220\u019D\uA790\uA7A4"
}, {
  base: "NJ",
  letters: "\u01CA"
}, {
  base: "Nj",
  letters: "\u01CB"
}, {
  base: "O",
  letters: "O\u24C4\uFF2F\xD2\xD3\xD4\u1ED2\u1ED0\u1ED6\u1ED4\xD5\u1E4C\u022C\u1E4E\u014C\u1E50\u1E52\u014E\u022E\u0230\xD6\u022A\u1ECE\u0150\u01D1\u020C\u020E\u01A0\u1EDC\u1EDA\u1EE0\u1EDE\u1EE2\u1ECC\u1ED8\u01EA\u01EC\xD8\u01FE\u0186\u019F\uA74A\uA74C"
}, {
  base: "OI",
  letters: "\u01A2"
}, {
  base: "OO",
  letters: "\uA74E"
}, {
  base: "OU",
  letters: "\u0222"
}, {
  base: "OE",
  letters: "\x8C\u0152"
}, {
  base: "oe",
  letters: "\x9C\u0153"
}, {
  base: "P",
  letters: "P\u24C5\uFF30\u1E54\u1E56\u01A4\u2C63\uA750\uA752\uA754"
}, {
  base: "Q",
  letters: "Q\u24C6\uFF31\uA756\uA758\u024A"
}, {
  base: "R",
  letters: "R\u24C7\uFF32\u0154\u1E58\u0158\u0210\u0212\u1E5A\u1E5C\u0156\u1E5E\u024C\u2C64\uA75A\uA7A6\uA782"
}, {
  base: "S",
  letters: "S\u24C8\uFF33\u1E9E\u015A\u1E64\u015C\u1E60\u0160\u1E66\u1E62\u1E68\u0218\u015E\u2C7E\uA7A8\uA784"
}, {
  base: "T",
  letters: "T\u24C9\uFF34\u1E6A\u0164\u1E6C\u021A\u0162\u1E70\u1E6E\u0166\u01AC\u01AE\u023E\uA786"
}, {
  base: "TZ",
  letters: "\uA728"
}, {
  base: "U",
  letters: "U\u24CA\uFF35\xD9\xDA\xDB\u0168\u1E78\u016A\u1E7A\u016C\xDC\u01DB\u01D7\u01D5\u01D9\u1EE6\u016E\u0170\u01D3\u0214\u0216\u01AF\u1EEA\u1EE8\u1EEE\u1EEC\u1EF0\u1EE4\u1E72\u0172\u1E76\u1E74\u0244"
}, {
  base: "V",
  letters: "V\u24CB\uFF36\u1E7C\u1E7E\u01B2\uA75E\u0245"
}, {
  base: "VY",
  letters: "\uA760"
}, {
  base: "W",
  letters: "W\u24CC\uFF37\u1E80\u1E82\u0174\u1E86\u1E84\u1E88\u2C72"
}, {
  base: "X",
  letters: "X\u24CD\uFF38\u1E8A\u1E8C"
}, {
  base: "Y",
  letters: "Y\u24CE\uFF39\u1EF2\xDD\u0176\u1EF8\u0232\u1E8E\u0178\u1EF6\u1EF4\u01B3\u024E\u1EFE"
}, {
  base: "Z",
  letters: "Z\u24CF\uFF3A\u0179\u1E90\u017B\u017D\u1E92\u1E94\u01B5\u0224\u2C7F\u2C6B\uA762"
}, {
  base: "a",
  letters: "a\u24D0\uFF41\u1E9A\xE0\xE1\xE2\u1EA7\u1EA5\u1EAB\u1EA9\xE3\u0101\u0103\u1EB1\u1EAF\u1EB5\u1EB3\u0227\u01E1\xE4\u01DF\u1EA3\xE5\u01FB\u01CE\u0201\u0203\u1EA1\u1EAD\u1EB7\u1E01\u0105\u2C65\u0250"
}, {
  base: "aa",
  letters: "\uA733"
}, {
  base: "ae",
  letters: "\xE6\u01FD\u01E3"
}, {
  base: "ao",
  letters: "\uA735"
}, {
  base: "au",
  letters: "\uA737"
}, {
  base: "av",
  letters: "\uA739\uA73B"
}, {
  base: "ay",
  letters: "\uA73D"
}, {
  base: "b",
  letters: "b\u24D1\uFF42\u1E03\u1E05\u1E07\u0180\u0183\u0253"
}, {
  base: "c",
  letters: "c\u24D2\uFF43\u0107\u0109\u010B\u010D\xE7\u1E09\u0188\u023C\uA73F\u2184"
}, {
  base: "d",
  letters: "d\u24D3\uFF44\u1E0B\u010F\u1E0D\u1E11\u1E13\u1E0F\u0111\u018C\u0256\u0257\uA77A"
}, {
  base: "dz",
  letters: "\u01F3\u01C6"
}, {
  base: "e",
  letters: "e\u24D4\uFF45\xE8\xE9\xEA\u1EC1\u1EBF\u1EC5\u1EC3\u1EBD\u0113\u1E15\u1E17\u0115\u0117\xEB\u1EBB\u011B\u0205\u0207\u1EB9\u1EC7\u0229\u1E1D\u0119\u1E19\u1E1B\u0247\u025B\u01DD"
}, {
  base: "f",
  letters: "f\u24D5\uFF46\u1E1F\u0192\uA77C"
}, {
  base: "g",
  letters: "g\u24D6\uFF47\u01F5\u011D\u1E21\u011F\u0121\u01E7\u0123\u01E5\u0260\uA7A1\u1D79\uA77F"
}, {
  base: "h",
  letters: "h\u24D7\uFF48\u0125\u1E23\u1E27\u021F\u1E25\u1E29\u1E2B\u1E96\u0127\u2C68\u2C76\u0265"
}, {
  base: "hv",
  letters: "\u0195"
}, {
  base: "i",
  letters: "i\u24D8\uFF49\xEC\xED\xEE\u0129\u012B\u012D\xEF\u1E2F\u1EC9\u01D0\u0209\u020B\u1ECB\u012F\u1E2D\u0268\u0131"
}, {
  base: "j",
  letters: "j\u24D9\uFF4A\u0135\u01F0\u0249"
}, {
  base: "k",
  letters: "k\u24DA\uFF4B\u1E31\u01E9\u1E33\u0137\u1E35\u0199\u2C6A\uA741\uA743\uA745\uA7A3"
}, {
  base: "l",
  letters: "l\u24DB\uFF4C\u0140\u013A\u013E\u1E37\u1E39\u013C\u1E3D\u1E3B\u017F\u0142\u019A\u026B\u2C61\uA749\uA781\uA747"
}, {
  base: "lj",
  letters: "\u01C9"
}, {
  base: "m",
  letters: "m\u24DC\uFF4D\u1E3F\u1E41\u1E43\u0271\u026F"
}, {
  base: "n",
  letters: "n\u24DD\uFF4E\u01F9\u0144\xF1\u1E45\u0148\u1E47\u0146\u1E4B\u1E49\u019E\u0272\u0149\uA791\uA7A5"
}, {
  base: "nj",
  letters: "\u01CC"
}, {
  base: "o",
  letters: "o\u24DE\uFF4F\xF2\xF3\xF4\u1ED3\u1ED1\u1ED7\u1ED5\xF5\u1E4D\u022D\u1E4F\u014D\u1E51\u1E53\u014F\u022F\u0231\xF6\u022B\u1ECF\u0151\u01D2\u020D\u020F\u01A1\u1EDD\u1EDB\u1EE1\u1EDF\u1EE3\u1ECD\u1ED9\u01EB\u01ED\xF8\u01FF\u0254\uA74B\uA74D\u0275"
}, {
  base: "oi",
  letters: "\u01A3"
}, {
  base: "ou",
  letters: "\u0223"
}, {
  base: "oo",
  letters: "\uA74F"
}, {
  base: "p",
  letters: "p\u24DF\uFF50\u1E55\u1E57\u01A5\u1D7D\uA751\uA753\uA755"
}, {
  base: "q",
  letters: "q\u24E0\uFF51\u024B\uA757\uA759"
}, {
  base: "r",
  letters: "r\u24E1\uFF52\u0155\u1E59\u0159\u0211\u0213\u1E5B\u1E5D\u0157\u1E5F\u024D\u027D\uA75B\uA7A7\uA783"
}, {
  base: "s",
  letters: "s\u24E2\uFF53\xDF\u015B\u1E65\u015D\u1E61\u0161\u1E67\u1E63\u1E69\u0219\u015F\u023F\uA7A9\uA785\u1E9B"
}, {
  base: "t",
  letters: "t\u24E3\uFF54\u1E6B\u1E97\u0165\u1E6D\u021B\u0163\u1E71\u1E6F\u0167\u01AD\u0288\u2C66\uA787"
}, {
  base: "tz",
  letters: "\uA729"
}, {
  base: "u",
  letters: "u\u24E4\uFF55\xF9\xFA\xFB\u0169\u1E79\u016B\u1E7B\u016D\xFC\u01DC\u01D8\u01D6\u01DA\u1EE7\u016F\u0171\u01D4\u0215\u0217\u01B0\u1EEB\u1EE9\u1EEF\u1EED\u1EF1\u1EE5\u1E73\u0173\u1E77\u1E75\u0289"
}, {
  base: "v",
  letters: "v\u24E5\uFF56\u1E7D\u1E7F\u028B\uA75F\u028C"
}, {
  base: "vy",
  letters: "\uA761"
}, {
  base: "w",
  letters: "w\u24E6\uFF57\u1E81\u1E83\u0175\u1E87\u1E85\u1E98\u1E89\u2C73"
}, {
  base: "x",
  letters: "x\u24E7\uFF58\u1E8B\u1E8D"
}, {
  base: "y",
  letters: "y\u24E8\uFF59\u1EF3\xFD\u0177\u1EF9\u0233\u1E8F\xFF\u1EF7\u1E99\u1EF5\u01B4\u024F\u1EFF"
}, {
  base: "z",
  letters: "z\u24E9\uFF5A\u017A\u1E91\u017C\u017E\u1E93\u1E95\u01B6\u0225\u0240\u2C6C\uA763"
}];
var diacriticsMap = {};
for (i = 0; i < defaultDiacriticsRemovalap.length; i += 1) {
  letters = defaultDiacriticsRemovalap[i].letters;
  for (j = 0; j < letters.length; j += 1) {
    diacriticsMap[letters[j]] = defaultDiacriticsRemovalap[i].base;
  }
}
var letters;
var j;
var i;
function removeDiacritics(str) {
  return str.replace(/[^\u0000-\u007E]/g, function(a) {
    return diacriticsMap[a] || a;
  });
}
var remove_diacritics_default = removeDiacritics;

// node_modules/framework7/esm/components/searchbar/searchbar-class.js
function _assertThisInitialized19(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose19(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf19(subClass, superClass);
}
function _setPrototypeOf19(o, p) {
  _setPrototypeOf19 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf19(o, p);
}
var Searchbar = function(_FrameworkClass) {
  _inheritsLoose19(Searchbar2, _FrameworkClass);
  function Searchbar2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _FrameworkClass.call(this, params, [app]) || this;
    var sb = _assertThisInitialized19(_this);
    var defaults = {
      el: void 0,
      inputEl: void 0,
      inputEvents: "change input compositionend",
      disableButton: true,
      disableButtonEl: void 0,
      backdropEl: void 0,
      searchContainer: void 0,
      searchItem: "li",
      searchIn: void 0,
      searchGroup: ".list-group",
      searchGroupTitle: ".item-divider, .list-group-title",
      ignore: ".searchbar-ignore",
      foundEl: ".searchbar-found",
      notFoundEl: ".searchbar-not-found",
      hideOnEnableEl: ".searchbar-hide-on-enable",
      hideOnSearchEl: ".searchbar-hide-on-search",
      backdrop: void 0,
      removeDiacritics: true,
      customSearch: false,
      hideDividers: true,
      hideGroups: true,
      disableOnBackdropClick: true,
      expandable: false,
      inline: false
    };
    sb.useModulesParams(defaults);
    sb.params = extend(defaults, params);
    var $el = dom7_default(sb.params.el);
    if ($el.length === 0)
      return sb || _assertThisInitialized19(_this);
    if ($el[0].f7Searchbar)
      return $el[0].f7Searchbar || _assertThisInitialized19(_this);
    $el[0].f7Searchbar = sb;
    var $pageEl;
    var $navbarEl = $el.parents(".navbar");
    if ($el.parents(".page").length > 0) {
      $pageEl = $el.parents(".page");
    } else if ($navbarEl.length > 0) {
      $pageEl = dom7_default(app.navbar.getPageByEl($navbarEl[0]));
      if (!$pageEl.length) {
        var $currentPageEl = $el.parents(".view").find(".page-current");
        if ($currentPageEl[0] && $currentPageEl[0].f7Page && $currentPageEl[0].f7Page.navbarEl === $navbarEl[0]) {
          $pageEl = $currentPageEl;
        }
      }
    }
    var $foundEl;
    if (params.foundEl) {
      $foundEl = dom7_default(params.foundEl);
    } else if (typeof sb.params.foundEl === "string" && $pageEl) {
      $foundEl = $pageEl.find(sb.params.foundEl);
    }
    var $notFoundEl;
    if (params.notFoundEl) {
      $notFoundEl = dom7_default(params.notFoundEl);
    } else if (typeof sb.params.notFoundEl === "string" && $pageEl) {
      $notFoundEl = $pageEl.find(sb.params.notFoundEl);
    }
    var $hideOnEnableEl;
    if (params.hideOnEnableEl) {
      $hideOnEnableEl = dom7_default(params.hideOnEnableEl);
    } else if (typeof sb.params.hideOnEnableEl === "string" && $pageEl) {
      $hideOnEnableEl = $pageEl.find(sb.params.hideOnEnableEl);
    }
    var $hideOnSearchEl;
    if (params.hideOnSearchEl) {
      $hideOnSearchEl = dom7_default(params.hideOnSearchEl);
    } else if (typeof sb.params.hideOnSearchEl === "string" && $pageEl) {
      $hideOnSearchEl = $pageEl.find(sb.params.hideOnSearchEl);
    }
    var expandable = sb.params.expandable || $el.hasClass("searchbar-expandable");
    var inline = sb.params.inline || $el.hasClass("searchbar-inline");
    if (typeof sb.params.backdrop === "undefined") {
      if (!inline)
        sb.params.backdrop = app.theme !== "aurora";
      else
        sb.params.backdrop = false;
    }
    var $backdropEl;
    if (sb.params.backdrop) {
      if (sb.params.backdropEl) {
        $backdropEl = dom7_default(sb.params.backdropEl);
      } else if ($pageEl && $pageEl.length > 0) {
        $backdropEl = $pageEl.find(".searchbar-backdrop");
      } else {
        $backdropEl = $el.siblings(".searchbar-backdrop");
      }
      if ($backdropEl.length === 0) {
        $backdropEl = dom7_default('<div class="searchbar-backdrop"></div>');
        if ($pageEl && $pageEl.length) {
          if ($el.parents($pageEl).length > 0 && $navbarEl && $el.parents($navbarEl).length === 0) {
            $backdropEl.insertBefore($el);
          } else {
            $backdropEl.insertBefore($pageEl.find(".page-content").eq(0));
          }
        } else {
          $backdropEl.insertBefore($el);
        }
      }
    }
    var $searchContainer;
    if (sb.params.searchContainer) {
      $searchContainer = dom7_default(sb.params.searchContainer);
    }
    var $inputEl;
    if (sb.params.inputEl) {
      $inputEl = dom7_default(sb.params.inputEl);
    } else {
      $inputEl = $el.find('input[type="search"]').eq(0);
    }
    var $disableButtonEl;
    if (sb.params.disableButton) {
      if (sb.params.disableButtonEl) {
        $disableButtonEl = dom7_default(sb.params.disableButtonEl);
      } else {
        $disableButtonEl = $el.find(".searchbar-disable-button");
      }
    }
    extend(sb, {
      app,
      view: app.views.get($el.parents(".view")),
      $el,
      el: $el[0],
      $backdropEl,
      backdropEl: $backdropEl && $backdropEl[0],
      $searchContainer,
      searchContainer: $searchContainer && $searchContainer[0],
      $inputEl,
      inputEl: $inputEl[0],
      $disableButtonEl,
      disableButtonEl: $disableButtonEl && $disableButtonEl[0],
      disableButtonHasMargin: false,
      $pageEl,
      pageEl: $pageEl && $pageEl[0],
      $navbarEl,
      navbarEl: $navbarEl && $navbarEl[0],
      $foundEl,
      foundEl: $foundEl && $foundEl[0],
      $notFoundEl,
      notFoundEl: $notFoundEl && $notFoundEl[0],
      $hideOnEnableEl,
      hideOnEnableEl: $hideOnEnableEl && $hideOnEnableEl[0],
      $hideOnSearchEl,
      hideOnSearchEl: $hideOnSearchEl && $hideOnSearchEl[0],
      previousQuery: "",
      query: "",
      isVirtualList: $searchContainer && $searchContainer.hasClass("virtual-list"),
      virtualList: void 0,
      enabled: false,
      expandable,
      inline
    });
    function preventSubmit(e) {
      e.preventDefault();
    }
    function onInputFocus(e) {
      sb.enable(e);
      sb.$el.addClass("searchbar-focused");
    }
    function onInputBlur() {
      sb.$el.removeClass("searchbar-focused");
      if (app.theme === "aurora" && (!$disableButtonEl || !$disableButtonEl.length || !sb.params.disableButton) && !sb.query) {
        sb.disable();
      }
    }
    function onInputChange() {
      var value = sb.$inputEl.val().trim();
      if (sb.$searchContainer && sb.$searchContainer.length > 0 && (sb.params.searchIn || sb.isVirtualList || sb.params.searchIn === sb.params.searchItem) || sb.params.customSearch) {
        sb.search(value, true);
      }
    }
    function onInputClear(e, previousValue) {
      sb.$el.trigger("searchbar:clear", previousValue);
      sb.emit("local::clear searchbarClear", sb, previousValue);
    }
    function disableOnClick(e) {
      sb.disable(e);
    }
    function onPageBeforeOut() {
      if (!sb || sb && !sb.$el)
        return;
      if (sb.enabled) {
        sb.$el.removeClass("searchbar-enabled");
        if (sb.expandable) {
          sb.$el.parents(".navbar").removeClass("with-searchbar-expandable-enabled with-searchbar-expandable-enabled-no-transition");
        }
      }
    }
    function onPageBeforeIn() {
      if (!sb || sb && !sb.$el)
        return;
      if (sb.enabled) {
        sb.$el.addClass("searchbar-enabled");
        if (sb.expandable) {
          sb.$el.parents(".navbar").addClass("with-searchbar-expandable-enabled-no-transition");
        }
      }
    }
    sb.attachEvents = function attachEvents2() {
      $el.on("submit", preventSubmit);
      if (sb.params.disableButton) {
        sb.$disableButtonEl.on("click", disableOnClick);
      }
      if (sb.params.disableOnBackdropClick && sb.$backdropEl) {
        sb.$backdropEl.on("click", disableOnClick);
      }
      if (sb.expandable && app.theme === "ios" && sb.view && $navbarEl.length && sb.$pageEl) {
        sb.$pageEl.on("page:beforeout", onPageBeforeOut);
        sb.$pageEl.on("page:beforein", onPageBeforeIn);
      }
      sb.$inputEl.on("focus", onInputFocus);
      sb.$inputEl.on("blur", onInputBlur);
      sb.$inputEl.on(sb.params.inputEvents, onInputChange);
      sb.$inputEl.on("input:clear", onInputClear);
    };
    sb.detachEvents = function detachEvents2() {
      $el.off("submit", preventSubmit);
      if (sb.params.disableButton) {
        sb.$disableButtonEl.off("click", disableOnClick);
      }
      if (sb.params.disableOnBackdropClick && sb.$backdropEl) {
        sb.$backdropEl.off("click", disableOnClick);
      }
      if (sb.expandable && app.theme === "ios" && sb.view && $navbarEl.length && sb.$pageEl) {
        sb.$pageEl.off("page:beforeout", onPageBeforeOut);
        sb.$pageEl.off("page:beforein", onPageBeforeIn);
      }
      sb.$inputEl.off("focus", onInputFocus);
      sb.$inputEl.off("blur", onInputBlur);
      sb.$inputEl.off(sb.params.inputEvents, onInputChange);
      sb.$inputEl.off("input:clear", onInputClear);
    };
    sb.useModules();
    sb.init();
    return sb || _assertThisInitialized19(_this);
  }
  var _proto = Searchbar2.prototype;
  _proto.clear = function clear2(e) {
    var sb = this;
    if (!sb.query && e && dom7_default(e.target).hasClass("searchbar-clear")) {
      sb.disable();
      return sb;
    }
    var previousQuery = sb.value;
    sb.$inputEl.val("").trigger("change").focus();
    sb.$el.trigger("searchbar:clear", previousQuery);
    sb.emit("local::clear searchbarClear", sb, previousQuery);
    return sb;
  };
  _proto.setDisableButtonMargin = function setDisableButtonMargin() {
    var sb = this;
    if (sb.expandable)
      return;
    var app = sb.app;
    sb.$disableButtonEl.transition(0).show();
    sb.$disableButtonEl.css("margin-" + (app.rtl ? "left" : "right"), -sb.disableButtonEl.offsetWidth + "px");
    sb._clientLeft = sb.$disableButtonEl[0].clientLeft;
    sb.$disableButtonEl.transition("");
    sb.disableButtonHasMargin = true;
  };
  _proto.enable = function enable7(setFocus) {
    var sb = this;
    if (sb.enabled)
      return sb;
    var app = sb.app;
    var document = getDocument();
    var device2 = getDevice();
    sb.enabled = true;
    function enable8() {
      if (sb.$backdropEl && (sb.$searchContainer && sb.$searchContainer.length || sb.params.customSearch) && !sb.$el.hasClass("searchbar-enabled") && !sb.query) {
        sb.backdropShow();
      }
      sb.$el.addClass("searchbar-enabled");
      if (!sb.$disableButtonEl || sb.$disableButtonEl && sb.$disableButtonEl.length === 0) {
        sb.$el.addClass("searchbar-enabled-no-disable-button");
      }
      if (!sb.expandable && sb.$disableButtonEl && sb.$disableButtonEl.length > 0 && app.theme !== "md") {
        if (!sb.disableButtonHasMargin) {
          sb.setDisableButtonMargin();
        }
        sb.$disableButtonEl.css("margin-" + (app.rtl ? "left" : "right"), "0px");
      }
      if (sb.expandable) {
        var $navbarEl = sb.$el.parents(".navbar");
        if ($navbarEl.hasClass("navbar-large") && sb.$pageEl) {
          var $pageContentEl = sb.$pageEl.find(".page-content");
          var $titleLargeEl = $navbarEl.find(".title-large");
          $pageContentEl.addClass("with-searchbar-expandable-enabled");
          if ($navbarEl.hasClass("navbar-large") && $navbarEl.hasClass("navbar-large-collapsed") && $titleLargeEl.length && $pageContentEl.length) {
            $pageContentEl.transition(0);
            $pageContentEl[0].scrollTop -= $titleLargeEl[0].offsetHeight;
            setTimeout(function() {
              $pageContentEl.transition("");
            }, 200);
          }
        }
        if (app.theme === "md" && $navbarEl.length) {
          $navbarEl.addClass("with-searchbar-expandable-enabled");
        } else {
          $navbarEl.addClass("with-searchbar-expandable-enabled");
          if ($navbarEl.hasClass("navbar-large")) {
            $navbarEl.addClass("navbar-large-collapsed");
          }
        }
      }
      if (sb.$hideOnEnableEl)
        sb.$hideOnEnableEl.addClass("hidden-by-searchbar");
      sb.$el.trigger("searchbar:enable");
      sb.emit("local::enable searchbarEnable", sb);
    }
    var needsFocus = false;
    if (setFocus === true) {
      if (document.activeElement !== sb.inputEl) {
        needsFocus = true;
      }
    }
    var isIos = device2.ios && app.theme === "ios";
    if (isIos) {
      if (sb.expandable) {
        if (needsFocus)
          sb.$inputEl.focus();
        enable8();
      } else {
        if (needsFocus)
          sb.$inputEl.focus();
        if (setFocus && (setFocus.type === "focus" || setFocus === true)) {
          nextTick(function() {
            enable8();
          }, 400);
        } else {
          enable8();
        }
      }
    } else {
      if (needsFocus)
        sb.$inputEl.focus();
      if (app.theme === "md" && sb.expandable) {
        sb.$el.parents(".page, .view, .navbar-inner, .navbar").scrollLeft(app.rtl ? 100 : 0);
      }
      enable8();
    }
    return sb;
  };
  _proto.disable = function disable7() {
    var sb = this;
    if (!sb.enabled)
      return sb;
    var app = sb.app;
    sb.$inputEl.val("").trigger("change");
    sb.$el.removeClass("searchbar-enabled searchbar-focused searchbar-enabled-no-disable-button");
    if (sb.expandable) {
      var $navbarEl = sb.$el.parents(".navbar");
      var $pageContentEl = sb.$pageEl && sb.$pageEl.find(".page-content");
      if ($navbarEl.hasClass("navbar-large") && $pageContentEl.length) {
        var $titleLargeEl = $navbarEl.find(".title-large");
        sb.$el.transitionEnd(function() {
          $pageContentEl.removeClass("with-searchbar-expandable-closing");
        });
        if ($navbarEl.hasClass("navbar-large") && $navbarEl.hasClass("navbar-large-collapsed") && $titleLargeEl.length) {
          var scrollTop = $pageContentEl[0].scrollTop;
          var titleLargeHeight = $titleLargeEl[0].offsetHeight;
          if (scrollTop > titleLargeHeight) {
            $pageContentEl.transition(0);
            $pageContentEl[0].scrollTop = scrollTop + titleLargeHeight;
            setTimeout(function() {
              $pageContentEl.transition("");
            }, 200);
          }
        }
        $pageContentEl.removeClass("with-searchbar-expandable-enabled").addClass("with-searchbar-expandable-closing");
      }
      if (app.theme === "md" && $navbarEl.length) {
        $navbarEl.removeClass("with-searchbar-expandable-enabled with-searchbar-expandable-enabled-no-transition").addClass("with-searchbar-expandable-closing");
        sb.$el.transitionEnd(function() {
          $navbarEl.removeClass("with-searchbar-expandable-closing");
        });
      } else {
        $navbarEl.removeClass("with-searchbar-expandable-enabled with-searchbar-expandable-enabled-no-transition").addClass("with-searchbar-expandable-closing");
        sb.$el.transitionEnd(function() {
          $navbarEl.removeClass("with-searchbar-expandable-closing");
        });
        if (sb.$pageEl) {
          sb.$pageEl.find(".page-content").trigger("scroll");
        }
      }
    }
    if (!sb.expandable && sb.$disableButtonEl && sb.$disableButtonEl.length > 0 && app.theme !== "md") {
      sb.$disableButtonEl.css("margin-" + (app.rtl ? "left" : "right"), -sb.disableButtonEl.offsetWidth + "px");
    }
    if (sb.$backdropEl && (sb.$searchContainer && sb.$searchContainer.length || sb.params.customSearch)) {
      sb.backdropHide();
    }
    sb.enabled = false;
    sb.$inputEl.blur();
    if (sb.$hideOnEnableEl)
      sb.$hideOnEnableEl.removeClass("hidden-by-searchbar");
    sb.$el.trigger("searchbar:disable");
    sb.emit("local::disable searchbarDisable", sb);
    return sb;
  };
  _proto.toggle = function toggle11() {
    var sb = this;
    if (sb.enabled)
      sb.disable();
    else
      sb.enable(true);
    return sb;
  };
  _proto.backdropShow = function backdropShow() {
    var sb = this;
    if (sb.$backdropEl) {
      sb.$backdropEl.addClass("searchbar-backdrop-in");
    }
    return sb;
  };
  _proto.backdropHide = function backdropHide() {
    var sb = this;
    if (sb.$backdropEl) {
      sb.$backdropEl.removeClass("searchbar-backdrop-in");
    }
    return sb;
  };
  _proto.search = function search(query, internal) {
    var sb = this;
    sb.previousQuery = sb.query || "";
    if (query === sb.previousQuery)
      return sb;
    if (!internal) {
      if (!sb.enabled) {
        sb.enable();
      }
      sb.$inputEl.val(query);
      sb.$inputEl.trigger("input");
    }
    sb.query = query;
    sb.value = query;
    var $searchContainer = sb.$searchContainer, $el = sb.$el, $foundEl = sb.$foundEl, $notFoundEl = sb.$notFoundEl, $hideOnSearchEl = sb.$hideOnSearchEl, isVirtualList = sb.isVirtualList;
    if (query.length > 0 && $hideOnSearchEl) {
      $hideOnSearchEl.addClass("hidden-by-searchbar");
    } else if ($hideOnSearchEl) {
      $hideOnSearchEl.removeClass("hidden-by-searchbar");
    }
    if ($searchContainer && $searchContainer.length && $el.hasClass("searchbar-enabled") || sb.params.customSearch && $el.hasClass("searchbar-enabled")) {
      if (query.length === 0) {
        sb.backdropShow();
      } else {
        sb.backdropHide();
      }
    }
    if (sb.params.customSearch) {
      $el.trigger("searchbar:search", {
        query,
        previousQuery: sb.previousQuery
      });
      sb.emit("local::search searchbarSearch", sb, query, sb.previousQuery);
      return sb;
    }
    var foundItems = [];
    var vlQuery;
    if (isVirtualList) {
      sb.virtualList = $searchContainer[0].f7VirtualList;
      if (query.trim() === "") {
        sb.virtualList.resetFilter();
        if ($notFoundEl)
          $notFoundEl.hide();
        if ($foundEl)
          $foundEl.show();
        $el.trigger("searchbar:search", {
          query,
          previousQuery: sb.previousQuery
        });
        sb.emit("local::search searchbarSearch", sb, query, sb.previousQuery);
        return sb;
      }
      vlQuery = sb.params.removeDiacritics ? remove_diacritics_default(query) : query;
      if (sb.virtualList.params.searchAll) {
        foundItems = sb.virtualList.params.searchAll(vlQuery, sb.virtualList.items) || [];
      } else if (sb.virtualList.params.searchByItem) {
        for (var i = 0; i < sb.virtualList.items.length; i += 1) {
          if (sb.virtualList.params.searchByItem(vlQuery, sb.virtualList.items[i], i)) {
            foundItems.push(i);
          }
        }
      }
    } else {
      var values;
      if (sb.params.removeDiacritics)
        values = remove_diacritics_default(query.trim().toLowerCase()).split(" ");
      else {
        values = query.trim().toLowerCase().split(" ");
      }
      $searchContainer.find(sb.params.searchItem).removeClass("hidden-by-searchbar").each(function(itemEl) {
        var $itemEl = dom7_default(itemEl);
        var compareWithText = [];
        var $searchIn = sb.params.searchIn ? $itemEl.find(sb.params.searchIn) : $itemEl;
        if (sb.params.searchIn === sb.params.searchItem) {
          $searchIn = $itemEl;
        }
        $searchIn.each(function(searchInEl) {
          var itemText = dom7_default(searchInEl).text().trim().toLowerCase();
          if (sb.params.removeDiacritics)
            itemText = remove_diacritics_default(itemText);
          compareWithText.push(itemText);
        });
        compareWithText = compareWithText.join(" ");
        var wordsMatch = 0;
        for (var _i = 0; _i < values.length; _i += 1) {
          if (compareWithText.indexOf(values[_i]) >= 0)
            wordsMatch += 1;
        }
        if (wordsMatch !== values.length && !(sb.params.ignore && $itemEl.is(sb.params.ignore))) {
          $itemEl.addClass("hidden-by-searchbar");
        } else {
          foundItems.push($itemEl[0]);
        }
      });
      if (sb.params.hideDividers) {
        $searchContainer.find(sb.params.searchGroupTitle).each(function(titleEl) {
          var $titleEl = dom7_default(titleEl);
          var $nextElements = $titleEl.nextAll(sb.params.searchItem);
          var hide3 = true;
          for (var _i2 = 0; _i2 < $nextElements.length; _i2 += 1) {
            var $nextEl = $nextElements.eq(_i2);
            if ($nextEl.is(sb.params.searchGroupTitle))
              break;
            if (!$nextEl.hasClass("hidden-by-searchbar")) {
              hide3 = false;
            }
          }
          var ignore = sb.params.ignore && $titleEl.is(sb.params.ignore);
          if (hide3 && !ignore)
            $titleEl.addClass("hidden-by-searchbar");
          else
            $titleEl.removeClass("hidden-by-searchbar");
        });
      }
      if (sb.params.hideGroups) {
        $searchContainer.find(sb.params.searchGroup).each(function(groupEl) {
          var $groupEl = dom7_default(groupEl);
          var ignore = sb.params.ignore && $groupEl.is(sb.params.ignore);
          var notHidden = $groupEl.find(sb.params.searchItem).filter(function(el) {
            return !dom7_default(el).hasClass("hidden-by-searchbar");
          });
          if (notHidden.length === 0 && !ignore) {
            $groupEl.addClass("hidden-by-searchbar");
          } else {
            $groupEl.removeClass("hidden-by-searchbar");
          }
        });
      }
    }
    if (foundItems.length === 0) {
      if ($notFoundEl)
        $notFoundEl.show();
      if ($foundEl)
        $foundEl.hide();
    } else {
      if ($notFoundEl)
        $notFoundEl.hide();
      if ($foundEl)
        $foundEl.show();
    }
    if (isVirtualList && sb.virtualList) {
      sb.virtualList.filterItems(foundItems);
    }
    $el.trigger("searchbar:search", {
      query,
      previousQuery: sb.previousQuery,
      foundItems
    });
    sb.emit("local::search searchbarSearch", sb, query, sb.previousQuery, foundItems);
    return sb;
  };
  _proto.init = function init46() {
    var sb = this;
    if (sb.expandable && sb.$el)
      sb.$el.addClass("searchbar-expandable");
    if (sb.inline && sb.$el)
      sb.$el.addClass("searchbar-inline");
    sb.attachEvents();
  };
  _proto.destroy = function destroy50() {
    var sb = this;
    sb.emit("local::beforeDestroy searchbarBeforeDestroy", sb);
    sb.$el.trigger("searchbar:beforedestroy");
    sb.detachEvents();
    if (sb.$el[0]) {
      sb.$el[0].f7Searchbar = null;
      delete sb.$el[0].f7Searchbar;
    }
    deleteProps(sb);
  };
  return Searchbar2;
}(class_default);
var searchbar_class_default = Searchbar;

// node_modules/framework7/esm/components/searchbar/searchbar.js
var searchbar_default = {
  name: "searchbar",
  static: {
    Searchbar: searchbar_class_default
  },
  create: function create34() {
    var app = this;
    app.searchbar = ConstructorMethods({
      defaultSelector: ".searchbar",
      constructor: searchbar_class_default,
      app,
      domProp: "f7Searchbar",
      addMethods: "clear enable disable toggle search".split(" ")
    });
  },
  on: {
    tabMounted: function tabMounted14(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".searchbar-init").each(function(searchbarEl) {
        var $searchbarEl = dom7_default(searchbarEl);
        app.searchbar.create(extend($searchbarEl.dataset(), {
          el: searchbarEl
        }));
      });
    },
    tabBeforeRemove: function tabBeforeRemove11(tabEl) {
      dom7_default(tabEl).find(".searchbar-init").each(function(searchbarEl) {
        if (searchbarEl.f7Searchbar && searchbarEl.f7Searchbar.destroy) {
          searchbarEl.f7Searchbar.destroy();
        }
      });
    },
    pageInit: function pageInit15(page) {
      var app = this;
      page.$el.find(".searchbar-init").each(function(searchbarEl) {
        var $searchbarEl = dom7_default(searchbarEl);
        app.searchbar.create(extend($searchbarEl.dataset(), {
          el: searchbarEl
        }));
      });
      if (app.theme === "ios" && page.view && page.view.router.dynamicNavbar && page.$navbarEl && page.$navbarEl.length > 0) {
        page.$navbarEl.find(".searchbar-init").each(function(searchbarEl) {
          var $searchbarEl = dom7_default(searchbarEl);
          app.searchbar.create(extend($searchbarEl.dataset(), {
            el: searchbarEl
          }));
        });
      }
    },
    pageBeforeRemove: function pageBeforeRemove12(page) {
      var app = this;
      page.$el.find(".searchbar-init").each(function(searchbarEl) {
        if (searchbarEl.f7Searchbar && searchbarEl.f7Searchbar.destroy) {
          searchbarEl.f7Searchbar.destroy();
        }
      });
      if (app.theme === "ios" && page.view && page.view.router.dynamicNavbar && page.$navbarEl && page.$navbarEl.length > 0) {
        page.$navbarEl.find(".searchbar-init").each(function(searchbarEl) {
          if (searchbarEl.f7Searchbar && searchbarEl.f7Searchbar.destroy) {
            searchbarEl.f7Searchbar.destroy();
          }
        });
      }
    }
  },
  clicks: {
    ".searchbar-clear": function clear($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      var sb = app.searchbar.get(data.searchbar);
      if (sb)
        sb.clear();
    },
    ".searchbar-enable": function enable3($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      var sb = app.searchbar.get(data.searchbar);
      if (sb)
        sb.enable(true);
    },
    ".searchbar-disable": function disable3($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      var sb = app.searchbar.get(data.searchbar);
      if (sb)
        sb.disable();
    },
    ".searchbar-toggle": function toggle6($clickedEl, data) {
      if (data === void 0) {
        data = {};
      }
      var app = this;
      var sb = app.searchbar.get(data.searchbar);
      if (sb)
        sb.toggle();
    }
  },
  vnode: {
    "searchbar-init": {
      insert: function insert10(vnode) {
        var app = this;
        var searchbarEl = vnode.elm;
        var $searchbarEl = dom7_default(searchbarEl);
        app.searchbar.create(extend($searchbarEl.dataset(), {
          el: searchbarEl
        }));
      },
      destroy: function destroy11(vnode) {
        var searchbarEl = vnode.elm;
        if (searchbarEl.f7Searchbar && searchbarEl.f7Searchbar.destroy) {
          searchbarEl.f7Searchbar.destroy();
        }
      }
    }
  }
};

// node_modules/framework7/esm/components/messages/messages-class.js
function _extends3() {
  _extends3 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends3.apply(this, arguments);
}
function _assertThisInitialized20(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose20(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf20(subClass, superClass);
}
function _setPrototypeOf20(o, p) {
  _setPrototypeOf20 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf20(o, p);
}
var Messages = function(_Framework7Class) {
  _inheritsLoose20(Messages2, _Framework7Class);
  function Messages2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var m = _assertThisInitialized20(_this);
    var defaults = {
      autoLayout: true,
      messages: [],
      newMessagesFirst: false,
      scrollMessages: true,
      scrollMessagesOnEdge: true,
      firstMessageRule: void 0,
      lastMessageRule: void 0,
      tailMessageRule: void 0,
      sameNameMessageRule: void 0,
      sameHeaderMessageRule: void 0,
      sameFooterMessageRule: void 0,
      sameAvatarMessageRule: void 0,
      customClassMessageRule: void 0,
      renderMessage: void 0
    };
    m.useModulesParams(defaults);
    m.params = extend(defaults, params);
    var $el = dom7_default(params.el).eq(0);
    if ($el.length === 0)
      return m || _assertThisInitialized20(_this);
    if ($el[0].f7Messages)
      return $el[0].f7Messages || _assertThisInitialized20(_this);
    $el[0].f7Messages = m;
    var $pageContentEl = $el.closest(".page-content").eq(0);
    extend(m, {
      messages: m.params.messages,
      $el,
      el: $el[0],
      $pageContentEl,
      pageContentEl: $pageContentEl[0]
    });
    m.useModules();
    m.init();
    return m || _assertThisInitialized20(_this);
  }
  var _proto = Messages2.prototype;
  _proto.getMessageData = function getMessageData(messageEl) {
    var $messageEl = dom7_default(messageEl);
    var data = {
      name: $messageEl.find(".message-name").html(),
      header: $messageEl.find(".message-header").html(),
      textHeader: $messageEl.find(".message-text-header").html(),
      textFooter: $messageEl.find(".message-text-footer").html(),
      footer: $messageEl.find(".message-footer").html(),
      isTitle: $messageEl.hasClass("messages-title"),
      type: $messageEl.hasClass("message-sent") ? "sent" : "received",
      text: $messageEl.find(".message-text").html(),
      image: $messageEl.find(".message-image").html(),
      imageSrc: $messageEl.find(".message-image img").attr("src"),
      typing: $messageEl.hasClass("message-typing")
    };
    if (data.isTitle) {
      data.text = $messageEl.html();
    }
    if (data.text && data.textHeader) {
      data.text = data.text.replace('<div class="message-text-header">' + data.textHeader + "</div>", "");
    }
    if (data.text && data.textFooter) {
      data.text = data.text.replace('<div class="message-text-footer">' + data.textFooter + "</div>", "");
    }
    var avatar = $messageEl.find(".message-avatar").css("background-image");
    if (avatar === "none" || avatar === "")
      avatar = void 0;
    if (avatar && typeof avatar === "string") {
      avatar = avatar.replace("url(", "").replace(")", "").replace(/"/g, "").replace(/'/g, "");
    } else {
      avatar = void 0;
    }
    data.avatar = avatar;
    return data;
  };
  _proto.getMessagesData = function getMessagesData() {
    var m = this;
    var data = [];
    m.$el.find(".message, .messages-title").each(function(messageEl) {
      data.push(m.getMessageData(messageEl));
    });
    return data;
  };
  _proto.renderMessage = function renderMessage(messageToRender) {
    var m = this;
    var message = extend({
      type: "sent",
      attrs: {}
    }, messageToRender);
    if (m.params.renderMessage) {
      return m.params.renderMessage.call(m, message);
    }
    if (message.isTitle) {
      return '<div class="messages-title">' + message.text + "</div>";
    }
    return jsx_default2("div", _extends3({
      class: "message message-" + message.type + " " + (message.isTyping ? "message-typing" : "") + " " + (message.cssClass || "")
    }, message.attrs), message.avatar && jsx_default2("div", {
      class: "message-avatar",
      style: "background-image:url(" + message.avatar + ")"
    }), jsx_default2("div", {
      class: "message-content"
    }, message.name && jsx_default2("div", {
      class: "message-name"
    }, message.name), message.header && jsx_default2("div", {
      class: "message-header"
    }, message.header), jsx_default2("div", {
      class: "message-bubble"
    }, message.textHeader && jsx_default2("div", {
      class: "message-text-header"
    }, message.textHeader), message.image && jsx_default2("div", {
      class: "message-image"
    }, message.image), message.imageSrc && !message.image && jsx_default2("div", {
      class: "message-image"
    }, jsx_default2("img", {
      src: message.imageSrc
    })), (message.text || message.isTyping) && jsx_default2("div", {
      class: "message-text"
    }, message.text || "", message.isTyping && jsx_default2("div", {
      class: "message-typing-indicator"
    }, jsx_default2("div", null), jsx_default2("div", null), jsx_default2("div", null))), message.textFooter && jsx_default2("div", {
      class: "message-text-footer"
    }, message.textFooter)), message.footer && jsx_default2("div", {
      class: "message-footer"
    }, message.footer)));
  };
  _proto.renderMessages = function renderMessages(messagesToRender, method) {
    if (messagesToRender === void 0) {
      messagesToRender = this.messages;
    }
    if (method === void 0) {
      method = this.params.newMessagesFirst ? "prepend" : "append";
    }
    var m = this;
    var html2 = messagesToRender.map(function(message) {
      return m.renderMessage(message);
    }).join("");
    m.$el[method](html2);
  };
  _proto.isFirstMessage = function isFirstMessage() {
    var _m$params;
    var m = this;
    if (m.params.firstMessageRule)
      return (_m$params = m.params).firstMessageRule.apply(_m$params, arguments);
    return false;
  };
  _proto.isLastMessage = function isLastMessage() {
    var _m$params2;
    var m = this;
    if (m.params.lastMessageRule)
      return (_m$params2 = m.params).lastMessageRule.apply(_m$params2, arguments);
    return false;
  };
  _proto.isTailMessage = function isTailMessage() {
    var _m$params3;
    var m = this;
    if (m.params.tailMessageRule)
      return (_m$params3 = m.params).tailMessageRule.apply(_m$params3, arguments);
    return false;
  };
  _proto.isSameNameMessage = function isSameNameMessage() {
    var _m$params4;
    var m = this;
    if (m.params.sameNameMessageRule)
      return (_m$params4 = m.params).sameNameMessageRule.apply(_m$params4, arguments);
    return false;
  };
  _proto.isSameHeaderMessage = function isSameHeaderMessage() {
    var _m$params5;
    var m = this;
    if (m.params.sameHeaderMessageRule)
      return (_m$params5 = m.params).sameHeaderMessageRule.apply(_m$params5, arguments);
    return false;
  };
  _proto.isSameFooterMessage = function isSameFooterMessage() {
    var _m$params6;
    var m = this;
    if (m.params.sameFooterMessageRule)
      return (_m$params6 = m.params).sameFooterMessageRule.apply(_m$params6, arguments);
    return false;
  };
  _proto.isSameAvatarMessage = function isSameAvatarMessage() {
    var _m$params7;
    var m = this;
    if (m.params.sameAvatarMessageRule)
      return (_m$params7 = m.params).sameAvatarMessageRule.apply(_m$params7, arguments);
    return false;
  };
  _proto.isCustomClassMessage = function isCustomClassMessage() {
    var _m$params8;
    var m = this;
    if (m.params.customClassMessageRule)
      return (_m$params8 = m.params).customClassMessageRule.apply(_m$params8, arguments);
    return void 0;
  };
  _proto.layout = function layout() {
    var m = this;
    m.$el.find(".message, .messages-title").each(function(messageEl, index2) {
      var $messageEl = dom7_default(messageEl);
      if (!m.messages) {
        m.messages = m.getMessagesData();
      }
      var classes = [];
      var message = m.messages[index2];
      var previousMessage = m.messages[index2 - 1];
      var nextMessage = m.messages[index2 + 1];
      if (m.isFirstMessage(message, previousMessage, nextMessage)) {
        classes.push("message-first");
      }
      if (m.isLastMessage(message, previousMessage, nextMessage)) {
        classes.push("message-last");
      }
      if (m.isTailMessage(message, previousMessage, nextMessage)) {
        classes.push("message-tail");
      }
      if (m.isSameNameMessage(message, previousMessage, nextMessage)) {
        classes.push("message-same-name");
      }
      if (m.isSameHeaderMessage(message, previousMessage, nextMessage)) {
        classes.push("message-same-header");
      }
      if (m.isSameFooterMessage(message, previousMessage, nextMessage)) {
        classes.push("message-same-footer");
      }
      if (m.isSameAvatarMessage(message, previousMessage, nextMessage)) {
        classes.push("message-same-avatar");
      }
      var customMessageClasses = m.isCustomClassMessage(message, previousMessage, nextMessage);
      if (customMessageClasses && customMessageClasses.length) {
        if (typeof customMessageClasses === "string") {
          customMessageClasses = customMessageClasses.split(" ");
        }
        customMessageClasses.forEach(function(customClass) {
          classes.push(customClass);
        });
      }
      $messageEl.removeClass("message-first message-last message-tail message-same-name message-same-header message-same-footer message-same-avatar");
      classes.forEach(function(className) {
        $messageEl.addClass(className);
      });
    });
  };
  _proto.clear = function clear2() {
    var m = this;
    m.messages = [];
    m.$el.html("");
  };
  _proto.removeMessage = function removeMessage(messageToRemove, layout) {
    if (layout === void 0) {
      layout = true;
    }
    var m = this;
    var index2;
    var $el;
    if (typeof messageToRemove === "number") {
      index2 = messageToRemove;
      $el = m.$el.find(".message, .messages-title").eq(index2);
    } else if (m.messages && m.messages.indexOf(messageToRemove) >= 0) {
      index2 = m.messages.indexOf(messageToRemove);
      $el = m.$el.children().eq(index2);
    } else {
      $el = dom7_default(messageToRemove);
      index2 = $el.index();
    }
    if ($el.length === 0) {
      return m;
    }
    $el.remove();
    m.messages.splice(index2, 1);
    if (m.params.autoLayout && layout)
      m.layout();
    return m;
  };
  _proto.removeMessages = function removeMessages(messagesToRemove, layout) {
    if (layout === void 0) {
      layout = true;
    }
    var m = this;
    if (Array.isArray(messagesToRemove)) {
      var messagesToRemoveEls = [];
      messagesToRemove.forEach(function(messageToRemoveIndex) {
        messagesToRemoveEls.push(m.$el.find(".message, .messages-title").eq(messageToRemoveIndex));
      });
      messagesToRemoveEls.forEach(function(messageToRemove) {
        m.removeMessage(messageToRemove, false);
      });
    } else {
      dom7_default(messagesToRemove).each(function(messageToRemove) {
        m.removeMessage(messageToRemove, false);
      });
    }
    if (m.params.autoLayout && layout)
      m.layout();
    return m;
  };
  _proto.addMessage = function addMessage() {
    var m = this;
    var messageToAdd;
    var animate;
    var method;
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    if (typeof args[1] === "boolean") {
      messageToAdd = args[0];
      animate = args[1];
      method = args[2];
    } else {
      messageToAdd = args[0];
      method = args[1];
      animate = args[2];
    }
    if (typeof animate === "undefined") {
      animate = true;
    }
    if (typeof method === "undefined") {
      method = m.params.newMessagesFirst ? "prepend" : "append";
    }
    return m.addMessages([messageToAdd], animate, method);
  };
  _proto.setScrollData = function setScrollData() {
    var m = this;
    var scrollHeightBefore = m.pageContentEl.scrollHeight;
    var heightBefore = m.pageContentEl.offsetHeight;
    var scrollBefore = m.pageContentEl.scrollTop;
    m.scrollData = {
      scrollHeightBefore,
      heightBefore,
      scrollBefore
    };
    return {
      scrollHeightBefore,
      heightBefore,
      scrollBefore
    };
  };
  _proto.addMessages = function addMessages() {
    var m = this;
    var messagesToAdd;
    var animate;
    var method;
    for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      args[_key2] = arguments[_key2];
    }
    if (typeof args[1] === "boolean") {
      messagesToAdd = args[0];
      animate = args[1];
      method = args[2];
    } else {
      messagesToAdd = args[0];
      method = args[1];
      animate = args[2];
    }
    if (typeof animate === "undefined") {
      animate = true;
    }
    if (typeof method === "undefined") {
      method = m.params.newMessagesFirst ? "prepend" : "append";
    }
    var _m$setScrollData = m.setScrollData(), scrollHeightBefore = _m$setScrollData.scrollHeightBefore, scrollBefore = _m$setScrollData.scrollBefore;
    var messagesHTML = "";
    var typingMessage = m.messages.filter(function(el) {
      return el.isTyping;
    })[0];
    messagesToAdd.forEach(function(messageToAdd) {
      if (typingMessage) {
        if (method === "append") {
          m.messages.splice(m.messages.indexOf(typingMessage), 0, messageToAdd);
        } else {
          m.messages.splice(m.messages.indexOf(typingMessage) + 1, 0, messageToAdd);
        }
      } else {
        m.messages[method === "append" ? "push" : "unshift"](messageToAdd);
      }
      messagesHTML += m.renderMessage(messageToAdd);
    });
    var $messagesEls = dom7_default(messagesHTML);
    if (animate) {
      if (method === "append" && !m.params.newMessagesFirst) {
        $messagesEls.addClass("message-appear-from-bottom");
      }
      if (method === "prepend" && m.params.newMessagesFirst) {
        $messagesEls.addClass("message-appear-from-top");
      }
    }
    if (typingMessage) {
      if (method === "append") {
        $messagesEls.insertBefore(m.$el.find(".message-typing"));
      } else {
        $messagesEls.insertAfter(m.$el.find(".message-typing"));
      }
    } else {
      m.$el[method]($messagesEls);
    }
    if (m.params.autoLayout)
      m.layout();
    if (method === "prepend" && !typingMessage) {
      m.pageContentEl.scrollTop = scrollBefore + (m.pageContentEl.scrollHeight - scrollHeightBefore);
    }
    if (m.params.scrollMessages && (method === "append" && !m.params.newMessagesFirst || method === "prepend" && m.params.newMessagesFirst && !typingMessage)) {
      m.scrollWithEdgeCheck(animate);
    }
    return m;
  };
  _proto.showTyping = function showTyping(message) {
    if (message === void 0) {
      message = {};
    }
    var m = this;
    var typingMessage = m.messages.filter(function(el) {
      return el.isTyping;
    })[0];
    if (typingMessage) {
      m.removeMessage(m.messages.indexOf(typingMessage));
    }
    m.addMessage(extend({
      type: "received",
      isTyping: true
    }, message));
    return m;
  };
  _proto.hideTyping = function hideTyping() {
    var m = this;
    var typingMessageIndex;
    var typingFound;
    m.messages.forEach(function(message, index2) {
      if (message.isTyping)
        typingMessageIndex = index2;
    });
    if (typeof typingMessageIndex !== "undefined") {
      if (m.$el.find(".message").eq(typingMessageIndex).hasClass("message-typing")) {
        typingFound = true;
        m.removeMessage(typingMessageIndex);
      }
    }
    if (!typingFound) {
      var $typingMessageEl = m.$el.find(".message-typing");
      if ($typingMessageEl.length) {
        m.removeMessage($typingMessageEl);
      }
    }
    return m;
  };
  _proto.scrollWithEdgeCheck = function scrollWithEdgeCheck(animate) {
    var m = this;
    var _m$scrollData = m.scrollData, scrollBefore = _m$scrollData.scrollBefore, scrollHeightBefore = _m$scrollData.scrollHeightBefore, heightBefore = _m$scrollData.heightBefore;
    if (m.params.scrollMessagesOnEdge) {
      var onEdge = false;
      if (m.params.newMessagesFirst && scrollBefore === 0) {
        onEdge = true;
      }
      if (!m.params.newMessagesFirst && scrollBefore - (scrollHeightBefore - heightBefore) >= -10) {
        onEdge = true;
      }
      if (onEdge)
        m.scroll(animate ? void 0 : 0);
    } else {
      m.scroll(animate ? void 0 : 0);
    }
  };
  _proto.scroll = function scroll2(duration, scrollTop) {
    if (duration === void 0) {
      duration = 300;
    }
    var m = this;
    var currentScroll = m.pageContentEl.scrollTop;
    var newScrollTop;
    if (typeof scrollTop !== "undefined")
      newScrollTop = scrollTop;
    else {
      newScrollTop = m.params.newMessagesFirst ? 0 : m.pageContentEl.scrollHeight - m.pageContentEl.offsetHeight;
      if (newScrollTop === currentScroll)
        return m;
    }
    m.$pageContentEl.scrollTop(newScrollTop, duration);
    return m;
  };
  _proto.init = function init46() {
    var m = this;
    if (!m.messages || m.messages.length === 0) {
      m.messages = m.getMessagesData();
    }
    if (m.params.messages && m.params.messages.length) {
      m.renderMessages();
    }
    if (m.params.autoLayout)
      m.layout();
    if (m.params.scrollMessages)
      m.scroll(0);
  };
  _proto.destroy = function destroy50() {
    var m = this;
    m.emit("local::beforeDestroy messagesBeforeDestroy", m);
    m.$el.trigger("messages:beforedestroy");
    if (m.$el[0]) {
      m.$el[0].f7Messages = null;
      delete m.$el[0].f7Messages;
    }
    deleteProps(m);
  };
  return Messages2;
}(class_default);
var messages_class_default = Messages;

// node_modules/framework7/esm/components/messages/messages.js
var messages_default = {
  name: "messages",
  static: {
    Messages: messages_class_default
  },
  create: function create35() {
    var app = this;
    app.messages = ConstructorMethods({
      defaultSelector: ".messages",
      constructor: messages_class_default,
      app,
      domProp: "f7Messages",
      addMethods: "renderMessages layout scroll clear removeMessage removeMessages addMessage addMessages".split(" ")
    });
  },
  on: {
    tabBeforeRemove: function tabBeforeRemove12(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".messages-init").each(function(messagesEl) {
        app.messages.destroy(messagesEl);
      });
    },
    tabMounted: function tabMounted15(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".messages-init").each(function(messagesEl) {
        app.messages.create({
          el: messagesEl
        });
      });
    },
    pageBeforeRemove: function pageBeforeRemove13(page) {
      var app = this;
      page.$el.find(".messages-init").each(function(messagesEl) {
        app.messages.destroy(messagesEl);
      });
    },
    pageInit: function pageInit16(page) {
      var app = this;
      page.$el.find(".messages-init").each(function(messagesEl) {
        app.messages.create({
          el: messagesEl
        });
      });
    }
  },
  vnode: {
    "messages-init": {
      insert: function insert11(vnode) {
        var app = this;
        var messagesEl = vnode.elm;
        app.messages.create({
          el: messagesEl
        });
      },
      destroy: function destroy12(vnode) {
        var app = this;
        var messagesEl = vnode.elm;
        app.messages.destroy(messagesEl);
      }
    }
  }
};

// node_modules/framework7/esm/components/messagebar/messagebar-class.js
function _assertThisInitialized21(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose21(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf21(subClass, superClass);
}
function _setPrototypeOf21(o, p) {
  _setPrototypeOf21 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf21(o, p);
}
var Messagebar = function(_Framework7Class) {
  _inheritsLoose21(Messagebar2, _Framework7Class);
  function Messagebar2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var messagebar = _assertThisInitialized21(_this);
    var defaults = {
      top: false,
      topOffset: 0,
      bottomOffset: 0,
      attachments: [],
      renderAttachments: void 0,
      renderAttachment: void 0,
      maxHeight: null,
      resizePage: true
    };
    messagebar.useModulesParams(defaults);
    messagebar.params = extend(defaults, params);
    var $el = dom7_default(messagebar.params.el);
    if ($el.length === 0)
      return messagebar || _assertThisInitialized21(_this);
    if ($el[0].f7Messagebar)
      return $el[0].f7Messagebar || _assertThisInitialized21(_this);
    $el[0].f7Messagebar = messagebar;
    var $pageEl = $el.parents(".page").eq(0);
    var $pageContentEl = $pageEl.find(".page-content").eq(0);
    var $areaEl = $el.find(".messagebar-area");
    var $textareaEl;
    if (messagebar.params.textareaEl) {
      $textareaEl = dom7_default(messagebar.params.textareaEl);
    } else {
      $textareaEl = $el.find("textarea");
    }
    var $attachmentsEl = $el.find(".messagebar-attachments");
    var $sheetEl = $el.find(".messagebar-sheet");
    if (messagebar.params.top) {
      $el.addClass("messagebar-top");
    }
    extend(messagebar, {
      $el,
      el: $el[0],
      $areaEl,
      areaEl: $areaEl[0],
      $textareaEl,
      textareaEl: $textareaEl[0],
      $attachmentsEl,
      attachmentsEl: $attachmentsEl[0],
      attachmentsVisible: $attachmentsEl.hasClass("messagebar-attachments-visible"),
      $sheetEl,
      sheetEl: $sheetEl[0],
      sheetVisible: $sheetEl.hasClass("messagebar-sheet-visible"),
      $pageEl,
      pageEl: $pageEl[0],
      $pageContentEl,
      pageContentEl: $pageContentEl,
      top: $el.hasClass("messagebar-top") || messagebar.params.top,
      attachments: []
    });
    function onAppResize() {
      if (messagebar.params.resizePage) {
        messagebar.resizePage();
      }
    }
    function onSubmit(e) {
      e.preventDefault();
    }
    function onAttachmentClick(e) {
      var index2 = dom7_default(this).index();
      if (dom7_default(e.target).closest(".messagebar-attachment-delete").length) {
        dom7_default(this).trigger("messagebar:attachmentdelete", index2);
        messagebar.emit("local::attachmentDelete messagebarAttachmentDelete", messagebar, this, index2);
      } else {
        dom7_default(this).trigger("messagebar:attachmentclick", index2);
        messagebar.emit("local::attachmentClick messagebarAttachmentClick", messagebar, this, index2);
      }
    }
    function onTextareaChange() {
      messagebar.checkEmptyState();
      messagebar.$el.trigger("messagebar:change");
      messagebar.emit("local::change messagebarChange", messagebar);
    }
    function onTextareaFocus() {
      messagebar.sheetHide();
      messagebar.$el.addClass("messagebar-focused");
      messagebar.$el.trigger("messagebar:focus");
      messagebar.emit("local::focus messagebarFocus", messagebar);
    }
    function onTextareaBlur() {
      messagebar.$el.removeClass("messagebar-focused");
      messagebar.$el.trigger("messagebar:blur");
      messagebar.emit("local::blur messagebarBlur", messagebar);
    }
    messagebar.attachEvents = function attachEvents2() {
      $el.on("textarea:resize", onAppResize);
      $el.on("submit", onSubmit);
      $el.on("click", ".messagebar-attachment", onAttachmentClick);
      $textareaEl.on("change input", onTextareaChange);
      $textareaEl.on("focus", onTextareaFocus);
      $textareaEl.on("blur", onTextareaBlur);
      app.on("resize", onAppResize);
    };
    messagebar.detachEvents = function detachEvents2() {
      $el.off("textarea:resize", onAppResize);
      $el.off("submit", onSubmit);
      $el.off("click", ".messagebar-attachment", onAttachmentClick);
      $textareaEl.off("change input", onTextareaChange);
      $textareaEl.off("focus", onTextareaFocus);
      $textareaEl.off("blur", onTextareaBlur);
      app.off("resize", onAppResize);
    };
    messagebar.useModules();
    messagebar.init();
    return messagebar || _assertThisInitialized21(_this);
  }
  var _proto = Messagebar2.prototype;
  _proto.focus = function focus2() {
    var messagebar = this;
    messagebar.$textareaEl.focus();
    return messagebar;
  };
  _proto.blur = function blur2() {
    var messagebar = this;
    messagebar.$textareaEl.blur();
    return messagebar;
  };
  _proto.clear = function clear2() {
    var messagebar = this;
    messagebar.$textareaEl.val("").trigger("change");
    return messagebar;
  };
  _proto.getValue = function getValue() {
    var messagebar = this;
    return messagebar.$textareaEl.val().trim();
  };
  _proto.setValue = function setValue(value) {
    var messagebar = this;
    messagebar.$textareaEl.val(value).trigger("change");
    return messagebar;
  };
  _proto.setPlaceholder = function setPlaceholder(placeholder) {
    var messagebar = this;
    messagebar.$textareaEl.attr("placeholder", placeholder);
    return messagebar;
  };
  _proto.resizePage = function resizePage() {
    var messagebar = this;
    var params = messagebar.params, $el = messagebar.$el, top = messagebar.top, $pageEl = messagebar.$pageEl, $pageContentEl = messagebar.$pageContentEl, $areaEl = messagebar.$areaEl, $textareaEl = messagebar.$textareaEl, $sheetEl = messagebar.$sheetEl, $attachmentsEl = messagebar.$attachmentsEl;
    var elHeight = $el[0].offsetHeight;
    var maxHeight = params.maxHeight;
    if (top) {
    } else {
      var currentPaddingBottom = parseInt($pageContentEl.css("padding-bottom"), 10);
      var requiredPaddingBottom = elHeight + params.bottomOffset;
      if (requiredPaddingBottom !== currentPaddingBottom && $pageContentEl.length) {
        var currentPaddingTop = parseInt($pageContentEl.css("padding-top"), 10);
        var pageScrollHeight = $pageContentEl[0].scrollHeight;
        var pageOffsetHeight = $pageContentEl[0].offsetHeight;
        var pageScrollTop = $pageContentEl[0].scrollTop;
        var scrollOnBottom = pageScrollTop === pageScrollHeight - pageOffsetHeight;
        if (!maxHeight) {
          maxHeight = $pageEl[0].offsetHeight - currentPaddingTop - $sheetEl.outerHeight() - $attachmentsEl.outerHeight() - parseInt($areaEl.css("margin-top"), 10) - parseInt($areaEl.css("margin-bottom"), 10);
        }
        $textareaEl.css("max-height", maxHeight + "px");
        $pageContentEl.css("padding-bottom", requiredPaddingBottom + "px");
        if (scrollOnBottom) {
          $pageContentEl.scrollTop($pageContentEl[0].scrollHeight - pageOffsetHeight);
        }
        $el.trigger("messagebar:resizepage");
        messagebar.emit("local::resizePage messagebarResizePage", messagebar);
      }
    }
  };
  _proto.checkEmptyState = function checkEmptyState2() {
    var messagebar = this;
    var $el = messagebar.$el, $textareaEl = messagebar.$textareaEl;
    var value = $textareaEl.val().trim();
    if (value && value.length) {
      $el.addClass("messagebar-with-value");
    } else {
      $el.removeClass("messagebar-with-value");
    }
  };
  _proto.attachmentsCreate = function attachmentsCreate(innerHTML) {
    if (innerHTML === void 0) {
      innerHTML = "";
    }
    var messagebar = this;
    var $attachmentsEl = dom7_default('<div class="messagebar-attachments">' + innerHTML + "</div>");
    $attachmentsEl.insertBefore(messagebar.$textareaEl);
    extend(messagebar, {
      $attachmentsEl,
      attachmentsEl: $attachmentsEl[0]
    });
    return messagebar;
  };
  _proto.attachmentsShow = function attachmentsShow(innerHTML) {
    if (innerHTML === void 0) {
      innerHTML = "";
    }
    var messagebar = this;
    messagebar.$attachmentsEl = messagebar.$el.find(".messagebar-attachments");
    if (messagebar.$attachmentsEl.length === 0) {
      messagebar.attachmentsCreate(innerHTML);
    }
    messagebar.$el.addClass("messagebar-attachments-visible");
    messagebar.attachmentsVisible = true;
    if (messagebar.params.resizePage) {
      messagebar.resizePage();
    }
    return messagebar;
  };
  _proto.attachmentsHide = function attachmentsHide() {
    var messagebar = this;
    messagebar.$el.removeClass("messagebar-attachments-visible");
    messagebar.attachmentsVisible = false;
    if (messagebar.params.resizePage) {
      messagebar.resizePage();
    }
    return messagebar;
  };
  _proto.attachmentsToggle = function attachmentsToggle() {
    var messagebar = this;
    if (messagebar.attachmentsVisible) {
      messagebar.attachmentsHide();
    } else {
      messagebar.attachmentsShow();
    }
    return messagebar;
  };
  _proto.renderAttachment = function renderAttachment(attachment) {
    var messagebar = this;
    if (messagebar.params.renderAttachment) {
      return messagebar.params.renderAttachment.call(messagebar, attachment);
    }
    return '\n      <div class="messagebar-attachment">\n        <img src="' + attachment + '">\n        <span class="messagebar-attachment-delete"></span>\n      </div>\n    ';
  };
  _proto.renderAttachments = function renderAttachments() {
    var messagebar = this;
    var html2;
    if (messagebar.params.renderAttachments) {
      html2 = messagebar.params.renderAttachments.call(messagebar, messagebar.attachments);
    } else {
      html2 = "" + messagebar.attachments.map(function(attachment) {
        return messagebar.renderAttachment(attachment);
      }).join("");
    }
    if (messagebar.$attachmentsEl.length === 0) {
      messagebar.attachmentsCreate(html2);
    } else {
      messagebar.$attachmentsEl.html(html2);
    }
  };
  _proto.sheetCreate = function sheetCreate(innerHTML) {
    if (innerHTML === void 0) {
      innerHTML = "";
    }
    var messagebar = this;
    var $sheetEl = dom7_default('<div class="messagebar-sheet">' + innerHTML + "</div>");
    messagebar.$el.append($sheetEl);
    extend(messagebar, {
      $sheetEl,
      sheetEl: $sheetEl[0]
    });
    return messagebar;
  };
  _proto.sheetShow = function sheetShow(innerHTML) {
    if (innerHTML === void 0) {
      innerHTML = "";
    }
    var messagebar = this;
    messagebar.$sheetEl = messagebar.$el.find(".messagebar-sheet");
    if (messagebar.$sheetEl.length === 0) {
      messagebar.sheetCreate(innerHTML);
    }
    messagebar.$el.addClass("messagebar-sheet-visible");
    messagebar.sheetVisible = true;
    if (messagebar.params.resizePage) {
      messagebar.resizePage();
    }
    return messagebar;
  };
  _proto.sheetHide = function sheetHide() {
    var messagebar = this;
    messagebar.$el.removeClass("messagebar-sheet-visible");
    messagebar.sheetVisible = false;
    if (messagebar.params.resizePage) {
      messagebar.resizePage();
    }
    return messagebar;
  };
  _proto.sheetToggle = function sheetToggle() {
    var messagebar = this;
    if (messagebar.sheetVisible) {
      messagebar.sheetHide();
    } else {
      messagebar.sheetShow();
    }
    return messagebar;
  };
  _proto.init = function init46() {
    var messagebar = this;
    messagebar.attachEvents();
    messagebar.checkEmptyState();
    return messagebar;
  };
  _proto.destroy = function destroy50() {
    var messagebar = this;
    messagebar.emit("local::beforeDestroy messagebarBeforeDestroy", messagebar);
    messagebar.$el.trigger("messagebar:beforedestroy");
    messagebar.detachEvents();
    if (messagebar.$el[0]) {
      messagebar.$el[0].f7Messagebar = null;
      delete messagebar.$el[0].f7Messagebar;
    }
    deleteProps(messagebar);
  };
  return Messagebar2;
}(class_default);
var messagebar_class_default = Messagebar;

// node_modules/framework7/esm/components/messagebar/messagebar.js
var messagebar_default = {
  name: "messagebar",
  static: {
    Messagebar: messagebar_class_default
  },
  create: function create36() {
    var app = this;
    app.messagebar = ConstructorMethods({
      defaultSelector: ".messagebar",
      constructor: messagebar_class_default,
      app,
      domProp: "f7Messagebar",
      addMethods: "clear getValue setValue setPlaceholder resizePage focus blur attachmentsCreate attachmentsShow attachmentsHide attachmentsToggle renderAttachments sheetCreate sheetShow sheetHide sheetToggle".split(" ")
    });
  },
  on: {
    tabBeforeRemove: function tabBeforeRemove13(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".messagebar-init").each(function(messagebarEl) {
        app.messagebar.destroy(messagebarEl);
      });
    },
    tabMounted: function tabMounted16(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".messagebar-init").each(function(messagebarEl) {
        app.messagebar.create(extend({
          el: messagebarEl
        }, dom7_default(messagebarEl).dataset()));
      });
    },
    pageBeforeRemove: function pageBeforeRemove14(page) {
      var app = this;
      page.$el.find(".messagebar-init").each(function(messagebarEl) {
        app.messagebar.destroy(messagebarEl);
      });
    },
    pageInit: function pageInit17(page) {
      var app = this;
      page.$el.find(".messagebar-init").each(function(messagebarEl) {
        app.messagebar.create(extend({
          el: messagebarEl
        }, dom7_default(messagebarEl).dataset()));
      });
    }
  },
  vnode: {
    "messagebar-init": {
      insert: function insert12(vnode) {
        var app = this;
        var messagebarEl = vnode.elm;
        app.messagebar.create(extend({
          el: messagebarEl
        }, dom7_default(messagebarEl).dataset()));
      },
      destroy: function destroy13(vnode) {
        var app = this;
        var messagebarEl = vnode.elm;
        app.messagebar.destroy(messagebarEl);
      }
    }
  }
};

// node_modules/swiper/esm/utils/dom.js
var Methods = {
  addClass,
  removeClass,
  hasClass,
  toggleClass,
  attr,
  removeAttr,
  transform,
  transition,
  on,
  off,
  trigger,
  transitionEnd,
  outerWidth,
  outerHeight,
  styles,
  offset,
  css,
  each,
  html,
  text,
  is,
  index,
  eq,
  append,
  prepend,
  next,
  nextAll,
  prev,
  prevAll,
  parent,
  parents,
  closest,
  find,
  children,
  filter,
  remove
};
Object.keys(Methods).forEach(function(methodName) {
  Object.defineProperty($.fn, methodName, {
    value: Methods[methodName],
    writable: true
  });
});
var dom_default = $;

// node_modules/swiper/esm/utils/utils.js
function deleteProps2(obj) {
  var object = obj;
  Object.keys(object).forEach(function(key) {
    try {
      object[key] = null;
    } catch (e) {
    }
    try {
      delete object[key];
    } catch (e) {
    }
  });
}
function nextTick2(callback, delay) {
  if (delay === void 0) {
    delay = 0;
  }
  return setTimeout(callback, delay);
}
function now2() {
  return Date.now();
}
function getComputedStyle2(el) {
  var window2 = getWindow();
  var style;
  if (window2.getComputedStyle) {
    style = window2.getComputedStyle(el, null);
  }
  if (!style && el.currentStyle) {
    style = el.currentStyle;
  }
  if (!style) {
    style = el.style;
  }
  return style;
}
function getTranslate2(el, axis) {
  if (axis === void 0) {
    axis = "x";
  }
  var window2 = getWindow();
  var matrix;
  var curTransform;
  var transformMatrix;
  var curStyle = getComputedStyle2(el, null);
  if (window2.WebKitCSSMatrix) {
    curTransform = curStyle.transform || curStyle.webkitTransform;
    if (curTransform.split(",").length > 6) {
      curTransform = curTransform.split(", ").map(function(a) {
        return a.replace(",", ".");
      }).join(", ");
    }
    transformMatrix = new window2.WebKitCSSMatrix(curTransform === "none" ? "" : curTransform);
  } else {
    transformMatrix = curStyle.MozTransform || curStyle.OTransform || curStyle.MsTransform || curStyle.msTransform || curStyle.transform || curStyle.getPropertyValue("transform").replace("translate(", "matrix(1, 0, 0, 1,");
    matrix = transformMatrix.toString().split(",");
  }
  if (axis === "x") {
    if (window2.WebKitCSSMatrix)
      curTransform = transformMatrix.m41;
    else if (matrix.length === 16)
      curTransform = parseFloat(matrix[12]);
    else
      curTransform = parseFloat(matrix[4]);
  }
  if (axis === "y") {
    if (window2.WebKitCSSMatrix)
      curTransform = transformMatrix.m42;
    else if (matrix.length === 16)
      curTransform = parseFloat(matrix[13]);
    else
      curTransform = parseFloat(matrix[5]);
  }
  return curTransform || 0;
}
function isObject(o) {
  return typeof o === "object" && o !== null && o.constructor && Object.prototype.toString.call(o).slice(8, -1) === "Object";
}
function isNode(node) {
  if (typeof window !== "undefined" && typeof window.HTMLElement !== "undefined") {
    return node instanceof HTMLElement;
  }
  return node && (node.nodeType === 1 || node.nodeType === 11);
}
function extend2() {
  var to = Object(arguments.length <= 0 ? void 0 : arguments[0]);
  var noExtend = ["__proto__", "constructor", "prototype"];
  for (var i = 1; i < arguments.length; i += 1) {
    var nextSource = i < 0 || arguments.length <= i ? void 0 : arguments[i];
    if (nextSource !== void 0 && nextSource !== null && !isNode(nextSource)) {
      var keysArray = Object.keys(Object(nextSource)).filter(function(key) {
        return noExtend.indexOf(key) < 0;
      });
      for (var nextIndex = 0, len = keysArray.length; nextIndex < len; nextIndex += 1) {
        var nextKey = keysArray[nextIndex];
        var desc = Object.getOwnPropertyDescriptor(nextSource, nextKey);
        if (desc !== void 0 && desc.enumerable) {
          if (isObject(to[nextKey]) && isObject(nextSource[nextKey])) {
            if (nextSource[nextKey].__swiper__) {
              to[nextKey] = nextSource[nextKey];
            } else {
              extend2(to[nextKey], nextSource[nextKey]);
            }
          } else if (!isObject(to[nextKey]) && isObject(nextSource[nextKey])) {
            to[nextKey] = {};
            if (nextSource[nextKey].__swiper__) {
              to[nextKey] = nextSource[nextKey];
            } else {
              extend2(to[nextKey], nextSource[nextKey]);
            }
          } else {
            to[nextKey] = nextSource[nextKey];
          }
        }
      }
    }
  }
  return to;
}
function bindModuleMethods(instance, obj) {
  Object.keys(obj).forEach(function(key) {
    if (isObject(obj[key])) {
      Object.keys(obj[key]).forEach(function(subKey) {
        if (typeof obj[key][subKey] === "function") {
          obj[key][subKey] = obj[key][subKey].bind(instance);
        }
      });
    }
    instance[key] = obj[key];
  });
}
function classesToSelector(classes) {
  if (classes === void 0) {
    classes = "";
  }
  return "." + classes.trim().replace(/([\.:!\/])/g, "\\$1").replace(/ /g, ".");
}
function createElementIfNotDefined($container, params, createElements, checkProps) {
  var document = getDocument();
  if (createElements) {
    Object.keys(checkProps).forEach(function(key) {
      if (!params[key] && params.auto === true) {
        var element = document.createElement("div");
        element.className = checkProps[key];
        $container.append(element);
        params[key] = element;
      }
    });
  }
  return params;
}

// node_modules/swiper/esm/utils/get-support.js
var support;
function calcSupport() {
  var window2 = getWindow();
  var document = getDocument();
  return {
    touch: !!("ontouchstart" in window2 || window2.DocumentTouch && document instanceof window2.DocumentTouch),
    pointerEvents: !!window2.PointerEvent && "maxTouchPoints" in window2.navigator && window2.navigator.maxTouchPoints >= 0,
    observer: function checkObserver() {
      return "MutationObserver" in window2 || "WebkitMutationObserver" in window2;
    }(),
    passiveListener: function checkPassiveListener() {
      var supportsPassive = false;
      try {
        var opts = Object.defineProperty({}, "passive", {
          get: function get2() {
            supportsPassive = true;
          }
        });
        window2.addEventListener("testPassiveListener", null, opts);
      } catch (e) {
      }
      return supportsPassive;
    }(),
    gestures: function checkGestures() {
      return "ongesturestart" in window2;
    }()
  };
}
function getSupport2() {
  if (!support) {
    support = calcSupport();
  }
  return support;
}

// node_modules/swiper/esm/utils/get-device.js
var device;
function calcDevice(_temp) {
  var _ref = _temp === void 0 ? {} : _temp, userAgent = _ref.userAgent;
  var support2 = getSupport2();
  var window2 = getWindow();
  var platform = window2.navigator.platform;
  var ua = userAgent || window2.navigator.userAgent;
  var device2 = {
    ios: false,
    android: false
  };
  var screenWidth = window2.screen.width;
  var screenHeight = window2.screen.height;
  var android = ua.match(/(Android);?[\s\/]+([\d.]+)?/);
  var ipad = ua.match(/(iPad).*OS\s([\d_]+)/);
  var ipod = ua.match(/(iPod)(.*OS\s([\d_]+))?/);
  var iphone = !ipad && ua.match(/(iPhone\sOS|iOS)\s([\d_]+)/);
  var windows = platform === "Win32";
  var macos = platform === "MacIntel";
  var iPadScreens = ["1024x1366", "1366x1024", "834x1194", "1194x834", "834x1112", "1112x834", "768x1024", "1024x768", "820x1180", "1180x820", "810x1080", "1080x810"];
  if (!ipad && macos && support2.touch && iPadScreens.indexOf(screenWidth + "x" + screenHeight) >= 0) {
    ipad = ua.match(/(Version)\/([\d.]+)/);
    if (!ipad)
      ipad = [0, 1, "13_0_0"];
    macos = false;
  }
  if (android && !windows) {
    device2.os = "android";
    device2.android = true;
  }
  if (ipad || iphone || ipod) {
    device2.os = "ios";
    device2.ios = true;
  }
  return device2;
}
function getDevice2(overrides) {
  if (overrides === void 0) {
    overrides = {};
  }
  if (!device) {
    device = calcDevice(overrides);
  }
  return device;
}

// node_modules/swiper/esm/utils/get-browser.js
var browser;
function calcBrowser() {
  var window2 = getWindow();
  function isSafari() {
    var ua = window2.navigator.userAgent.toLowerCase();
    return ua.indexOf("safari") >= 0 && ua.indexOf("chrome") < 0 && ua.indexOf("android") < 0;
  }
  return {
    isEdge: !!window2.navigator.userAgent.match(/Edge/g),
    isSafari: isSafari(),
    isWebView: /(iPhone|iPod|iPad).*AppleWebKit(?!.*Safari)/i.test(window2.navigator.userAgent)
  };
}
function getBrowser() {
  if (!browser) {
    browser = calcBrowser();
  }
  return browser;
}

// node_modules/swiper/esm/modules/resize/resize.js
var supportsResizeObserver = function supportsResizeObserver2() {
  var window2 = getWindow();
  return typeof window2.ResizeObserver !== "undefined";
};
var resize_default2 = {
  name: "resize",
  create: function create37() {
    var swiper = this;
    extend2(swiper, {
      resize: {
        observer: null,
        createObserver: function createObserver() {
          if (!swiper || swiper.destroyed || !swiper.initialized)
            return;
          swiper.resize.observer = new ResizeObserver(function(entries) {
            var width = swiper.width, height = swiper.height;
            var newWidth = width;
            var newHeight = height;
            entries.forEach(function(_ref) {
              var contentBoxSize = _ref.contentBoxSize, contentRect = _ref.contentRect, target = _ref.target;
              if (target && target !== swiper.el)
                return;
              newWidth = contentRect ? contentRect.width : (contentBoxSize[0] || contentBoxSize).inlineSize;
              newHeight = contentRect ? contentRect.height : (contentBoxSize[0] || contentBoxSize).blockSize;
            });
            if (newWidth !== width || newHeight !== height) {
              swiper.resize.resizeHandler();
            }
          });
          swiper.resize.observer.observe(swiper.el);
        },
        removeObserver: function removeObserver() {
          if (swiper.resize.observer && swiper.resize.observer.unobserve && swiper.el) {
            swiper.resize.observer.unobserve(swiper.el);
            swiper.resize.observer = null;
          }
        },
        resizeHandler: function resizeHandler() {
          if (!swiper || swiper.destroyed || !swiper.initialized)
            return;
          swiper.emit("beforeResize");
          swiper.emit("resize");
        },
        orientationChangeHandler: function orientationChangeHandler() {
          if (!swiper || swiper.destroyed || !swiper.initialized)
            return;
          swiper.emit("orientationchange");
        }
      }
    });
  },
  on: {
    init: function init13(swiper) {
      var window2 = getWindow();
      if (swiper.params.resizeObserver && supportsResizeObserver()) {
        swiper.resize.createObserver();
        return;
      }
      window2.addEventListener("resize", swiper.resize.resizeHandler);
      window2.addEventListener("orientationchange", swiper.resize.orientationChangeHandler);
    },
    destroy: function destroy14(swiper) {
      var window2 = getWindow();
      swiper.resize.removeObserver();
      window2.removeEventListener("resize", swiper.resize.resizeHandler);
      window2.removeEventListener("orientationchange", swiper.resize.orientationChangeHandler);
    }
  }
};

// node_modules/swiper/esm/modules/observer/observer.js
function _extends4() {
  _extends4 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends4.apply(this, arguments);
}
var Observer = {
  attach: function attach(target, options) {
    if (options === void 0) {
      options = {};
    }
    var window2 = getWindow();
    var swiper = this;
    var ObserverFunc = window2.MutationObserver || window2.WebkitMutationObserver;
    var observer = new ObserverFunc(function(mutations) {
      if (mutations.length === 1) {
        swiper.emit("observerUpdate", mutations[0]);
        return;
      }
      var observerUpdate4 = function observerUpdate5() {
        swiper.emit("observerUpdate", mutations[0]);
      };
      if (window2.requestAnimationFrame) {
        window2.requestAnimationFrame(observerUpdate4);
      } else {
        window2.setTimeout(observerUpdate4, 0);
      }
    });
    observer.observe(target, {
      attributes: typeof options.attributes === "undefined" ? true : options.attributes,
      childList: typeof options.childList === "undefined" ? true : options.childList,
      characterData: typeof options.characterData === "undefined" ? true : options.characterData
    });
    swiper.observer.observers.push(observer);
  },
  init: function init14() {
    var swiper = this;
    if (!swiper.support.observer || !swiper.params.observer)
      return;
    if (swiper.params.observeParents) {
      var containerParents = swiper.$el.parents();
      for (var i = 0; i < containerParents.length; i += 1) {
        swiper.observer.attach(containerParents[i]);
      }
    }
    swiper.observer.attach(swiper.$el[0], {
      childList: swiper.params.observeSlideChildren
    });
    swiper.observer.attach(swiper.$wrapperEl[0], {
      attributes: false
    });
  },
  destroy: function destroy15() {
    var swiper = this;
    swiper.observer.observers.forEach(function(observer) {
      observer.disconnect();
    });
    swiper.observer.observers = [];
  }
};
var observer_default = {
  name: "observer",
  params: {
    observer: false,
    observeParents: false,
    observeSlideChildren: false
  },
  create: function create38() {
    var swiper = this;
    bindModuleMethods(swiper, {
      observer: _extends4({}, Observer, {
        observers: []
      })
    });
  },
  on: {
    init: function init15(swiper) {
      swiper.observer.init();
    },
    destroy: function destroy16(swiper) {
      swiper.observer.destroy();
    }
  }
};

// node_modules/swiper/esm/components/core/modular.js
var modular_default = {
  useParams: function useParams(instanceParams) {
    var instance = this;
    if (!instance.modules)
      return;
    Object.keys(instance.modules).forEach(function(moduleName) {
      var module = instance.modules[moduleName];
      if (module.params) {
        extend2(instanceParams, module.params);
      }
    });
  },
  useModules: function useModules(modulesParams) {
    if (modulesParams === void 0) {
      modulesParams = {};
    }
    var instance = this;
    if (!instance.modules)
      return;
    Object.keys(instance.modules).forEach(function(moduleName) {
      var module = instance.modules[moduleName];
      var moduleParams = modulesParams[moduleName] || {};
      if (module.on && instance.on) {
        Object.keys(module.on).forEach(function(moduleEventName) {
          instance.on(moduleEventName, module.on[moduleEventName]);
        });
      }
      if (module.create) {
        module.create.bind(instance)(moduleParams);
      }
    });
  }
};

// node_modules/swiper/esm/components/core/events-emitter.js
var events_emitter_default = {
  on: function on2(events, handler, priority) {
    var self = this;
    if (typeof handler !== "function")
      return self;
    var method = priority ? "unshift" : "push";
    events.split(" ").forEach(function(event2) {
      if (!self.eventsListeners[event2])
        self.eventsListeners[event2] = [];
      self.eventsListeners[event2][method](handler);
    });
    return self;
  },
  once: function once(events, handler, priority) {
    var self = this;
    if (typeof handler !== "function")
      return self;
    function onceHandler() {
      self.off(events, onceHandler);
      if (onceHandler.__emitterProxy) {
        delete onceHandler.__emitterProxy;
      }
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      handler.apply(self, args);
    }
    onceHandler.__emitterProxy = handler;
    return self.on(events, onceHandler, priority);
  },
  onAny: function onAny(handler, priority) {
    var self = this;
    if (typeof handler !== "function")
      return self;
    var method = priority ? "unshift" : "push";
    if (self.eventsAnyListeners.indexOf(handler) < 0) {
      self.eventsAnyListeners[method](handler);
    }
    return self;
  },
  offAny: function offAny(handler) {
    var self = this;
    if (!self.eventsAnyListeners)
      return self;
    var index2 = self.eventsAnyListeners.indexOf(handler);
    if (index2 >= 0) {
      self.eventsAnyListeners.splice(index2, 1);
    }
    return self;
  },
  off: function off2(events, handler) {
    var self = this;
    if (!self.eventsListeners)
      return self;
    events.split(" ").forEach(function(event2) {
      if (typeof handler === "undefined") {
        self.eventsListeners[event2] = [];
      } else if (self.eventsListeners[event2]) {
        self.eventsListeners[event2].forEach(function(eventHandler, index2) {
          if (eventHandler === handler || eventHandler.__emitterProxy && eventHandler.__emitterProxy === handler) {
            self.eventsListeners[event2].splice(index2, 1);
          }
        });
      }
    });
    return self;
  },
  emit: function emit() {
    var self = this;
    if (!self.eventsListeners)
      return self;
    var events;
    var data;
    var context;
    for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      args[_key2] = arguments[_key2];
    }
    if (typeof args[0] === "string" || Array.isArray(args[0])) {
      events = args[0];
      data = args.slice(1, args.length);
      context = self;
    } else {
      events = args[0].events;
      data = args[0].data;
      context = args[0].context || self;
    }
    data.unshift(context);
    var eventsArray = Array.isArray(events) ? events : events.split(" ");
    eventsArray.forEach(function(event2) {
      if (self.eventsAnyListeners && self.eventsAnyListeners.length) {
        self.eventsAnyListeners.forEach(function(eventHandler) {
          eventHandler.apply(context, [event2].concat(data));
        });
      }
      if (self.eventsListeners && self.eventsListeners[event2]) {
        self.eventsListeners[event2].forEach(function(eventHandler) {
          eventHandler.apply(context, data);
        });
      }
    });
    return self;
  }
};

// node_modules/swiper/esm/components/core/update/updateSize.js
function updateSize() {
  var swiper = this;
  var width;
  var height;
  var $el = swiper.$el;
  if (typeof swiper.params.width !== "undefined" && swiper.params.width !== null) {
    width = swiper.params.width;
  } else {
    width = $el[0].clientWidth;
  }
  if (typeof swiper.params.height !== "undefined" && swiper.params.height !== null) {
    height = swiper.params.height;
  } else {
    height = $el[0].clientHeight;
  }
  if (width === 0 && swiper.isHorizontal() || height === 0 && swiper.isVertical()) {
    return;
  }
  width = width - parseInt($el.css("padding-left") || 0, 10) - parseInt($el.css("padding-right") || 0, 10);
  height = height - parseInt($el.css("padding-top") || 0, 10) - parseInt($el.css("padding-bottom") || 0, 10);
  if (Number.isNaN(width))
    width = 0;
  if (Number.isNaN(height))
    height = 0;
  extend2(swiper, {
    width,
    height,
    size: swiper.isHorizontal() ? width : height
  });
}

// node_modules/swiper/esm/components/core/update/updateSlides.js
function updateSlides() {
  var swiper = this;
  function getDirectionLabel(property) {
    if (swiper.isHorizontal()) {
      return property;
    }
    return {
      "width": "height",
      "margin-top": "margin-left",
      "margin-bottom ": "margin-right",
      "margin-left": "margin-top",
      "margin-right": "margin-bottom",
      "padding-left": "padding-top",
      "padding-right": "padding-bottom",
      "marginRight": "marginBottom"
    }[property];
  }
  function getDirectionPropertyValue(node, label) {
    return parseFloat(node.getPropertyValue(getDirectionLabel(label)) || 0);
  }
  var params = swiper.params;
  var $wrapperEl = swiper.$wrapperEl, swiperSize = swiper.size, rtl = swiper.rtlTranslate, wrongRTL = swiper.wrongRTL;
  var isVirtual = swiper.virtual && params.virtual.enabled;
  var previousSlidesLength = isVirtual ? swiper.virtual.slides.length : swiper.slides.length;
  var slides = $wrapperEl.children("." + swiper.params.slideClass);
  var slidesLength = isVirtual ? swiper.virtual.slides.length : slides.length;
  var snapGrid = [];
  var slidesGrid = [];
  var slidesSizesGrid = [];
  var offsetBefore = params.slidesOffsetBefore;
  if (typeof offsetBefore === "function") {
    offsetBefore = params.slidesOffsetBefore.call(swiper);
  }
  var offsetAfter = params.slidesOffsetAfter;
  if (typeof offsetAfter === "function") {
    offsetAfter = params.slidesOffsetAfter.call(swiper);
  }
  var previousSnapGridLength = swiper.snapGrid.length;
  var previousSlidesGridLength = swiper.slidesGrid.length;
  var spaceBetween = params.spaceBetween;
  var slidePosition = -offsetBefore;
  var prevSlideSize = 0;
  var index2 = 0;
  if (typeof swiperSize === "undefined") {
    return;
  }
  if (typeof spaceBetween === "string" && spaceBetween.indexOf("%") >= 0) {
    spaceBetween = parseFloat(spaceBetween.replace("%", "")) / 100 * swiperSize;
  }
  swiper.virtualSize = -spaceBetween;
  if (rtl)
    slides.css({
      marginLeft: "",
      marginBottom: "",
      marginTop: ""
    });
  else
    slides.css({
      marginRight: "",
      marginBottom: "",
      marginTop: ""
    });
  var slidesNumberEvenToRows;
  if (params.slidesPerColumn > 1) {
    if (Math.floor(slidesLength / params.slidesPerColumn) === slidesLength / swiper.params.slidesPerColumn) {
      slidesNumberEvenToRows = slidesLength;
    } else {
      slidesNumberEvenToRows = Math.ceil(slidesLength / params.slidesPerColumn) * params.slidesPerColumn;
    }
    if (params.slidesPerView !== "auto" && params.slidesPerColumnFill === "row") {
      slidesNumberEvenToRows = Math.max(slidesNumberEvenToRows, params.slidesPerView * params.slidesPerColumn);
    }
  }
  var slideSize;
  var slidesPerColumn = params.slidesPerColumn;
  var slidesPerRow = slidesNumberEvenToRows / slidesPerColumn;
  var numFullColumns = Math.floor(slidesLength / params.slidesPerColumn);
  for (var i = 0; i < slidesLength; i += 1) {
    slideSize = 0;
    var slide = slides.eq(i);
    if (params.slidesPerColumn > 1) {
      var newSlideOrderIndex = void 0;
      var column = void 0;
      var row = void 0;
      if (params.slidesPerColumnFill === "row" && params.slidesPerGroup > 1) {
        var groupIndex = Math.floor(i / (params.slidesPerGroup * params.slidesPerColumn));
        var slideIndexInGroup = i - params.slidesPerColumn * params.slidesPerGroup * groupIndex;
        var columnsInGroup = groupIndex === 0 ? params.slidesPerGroup : Math.min(Math.ceil((slidesLength - groupIndex * slidesPerColumn * params.slidesPerGroup) / slidesPerColumn), params.slidesPerGroup);
        row = Math.floor(slideIndexInGroup / columnsInGroup);
        column = slideIndexInGroup - row * columnsInGroup + groupIndex * params.slidesPerGroup;
        newSlideOrderIndex = column + row * slidesNumberEvenToRows / slidesPerColumn;
        slide.css({
          "-webkit-box-ordinal-group": newSlideOrderIndex,
          "-moz-box-ordinal-group": newSlideOrderIndex,
          "-ms-flex-order": newSlideOrderIndex,
          "-webkit-order": newSlideOrderIndex,
          order: newSlideOrderIndex
        });
      } else if (params.slidesPerColumnFill === "column") {
        column = Math.floor(i / slidesPerColumn);
        row = i - column * slidesPerColumn;
        if (column > numFullColumns || column === numFullColumns && row === slidesPerColumn - 1) {
          row += 1;
          if (row >= slidesPerColumn) {
            row = 0;
            column += 1;
          }
        }
      } else {
        row = Math.floor(i / slidesPerRow);
        column = i - row * slidesPerRow;
      }
      slide.css(getDirectionLabel("margin-top"), row !== 0 ? params.spaceBetween && params.spaceBetween + "px" : "");
    }
    if (slide.css("display") === "none")
      continue;
    if (params.slidesPerView === "auto") {
      var slideStyles = getComputedStyle(slide[0]);
      var currentTransform = slide[0].style.transform;
      var currentWebKitTransform = slide[0].style.webkitTransform;
      if (currentTransform) {
        slide[0].style.transform = "none";
      }
      if (currentWebKitTransform) {
        slide[0].style.webkitTransform = "none";
      }
      if (params.roundLengths) {
        slideSize = swiper.isHorizontal() ? slide.outerWidth(true) : slide.outerHeight(true);
      } else {
        var width = getDirectionPropertyValue(slideStyles, "width");
        var paddingLeft = getDirectionPropertyValue(slideStyles, "padding-left");
        var paddingRight = getDirectionPropertyValue(slideStyles, "padding-right");
        var marginLeft = getDirectionPropertyValue(slideStyles, "margin-left");
        var marginRight = getDirectionPropertyValue(slideStyles, "margin-right");
        var boxSizing = slideStyles.getPropertyValue("box-sizing");
        if (boxSizing && boxSizing === "border-box") {
          slideSize = width + marginLeft + marginRight;
        } else {
          var _slide$ = slide[0], clientWidth = _slide$.clientWidth, offsetWidth = _slide$.offsetWidth;
          slideSize = width + paddingLeft + paddingRight + marginLeft + marginRight + (offsetWidth - clientWidth);
        }
      }
      if (currentTransform) {
        slide[0].style.transform = currentTransform;
      }
      if (currentWebKitTransform) {
        slide[0].style.webkitTransform = currentWebKitTransform;
      }
      if (params.roundLengths)
        slideSize = Math.floor(slideSize);
    } else {
      slideSize = (swiperSize - (params.slidesPerView - 1) * spaceBetween) / params.slidesPerView;
      if (params.roundLengths)
        slideSize = Math.floor(slideSize);
      if (slides[i]) {
        slides[i].style[getDirectionLabel("width")] = slideSize + "px";
      }
    }
    if (slides[i]) {
      slides[i].swiperSlideSize = slideSize;
    }
    slidesSizesGrid.push(slideSize);
    if (params.centeredSlides) {
      slidePosition = slidePosition + slideSize / 2 + prevSlideSize / 2 + spaceBetween;
      if (prevSlideSize === 0 && i !== 0)
        slidePosition = slidePosition - swiperSize / 2 - spaceBetween;
      if (i === 0)
        slidePosition = slidePosition - swiperSize / 2 - spaceBetween;
      if (Math.abs(slidePosition) < 1 / 1e3)
        slidePosition = 0;
      if (params.roundLengths)
        slidePosition = Math.floor(slidePosition);
      if (index2 % params.slidesPerGroup === 0)
        snapGrid.push(slidePosition);
      slidesGrid.push(slidePosition);
    } else {
      if (params.roundLengths)
        slidePosition = Math.floor(slidePosition);
      if ((index2 - Math.min(swiper.params.slidesPerGroupSkip, index2)) % swiper.params.slidesPerGroup === 0)
        snapGrid.push(slidePosition);
      slidesGrid.push(slidePosition);
      slidePosition = slidePosition + slideSize + spaceBetween;
    }
    swiper.virtualSize += slideSize + spaceBetween;
    prevSlideSize = slideSize;
    index2 += 1;
  }
  swiper.virtualSize = Math.max(swiper.virtualSize, swiperSize) + offsetAfter;
  var newSlidesGrid;
  if (rtl && wrongRTL && (params.effect === "slide" || params.effect === "coverflow")) {
    $wrapperEl.css({
      width: swiper.virtualSize + params.spaceBetween + "px"
    });
  }
  if (params.setWrapperSize) {
    var _$wrapperEl$css;
    $wrapperEl.css((_$wrapperEl$css = {}, _$wrapperEl$css[getDirectionLabel("width")] = swiper.virtualSize + params.spaceBetween + "px", _$wrapperEl$css));
  }
  if (params.slidesPerColumn > 1) {
    var _$wrapperEl$css2;
    swiper.virtualSize = (slideSize + params.spaceBetween) * slidesNumberEvenToRows;
    swiper.virtualSize = Math.ceil(swiper.virtualSize / params.slidesPerColumn) - params.spaceBetween;
    $wrapperEl.css((_$wrapperEl$css2 = {}, _$wrapperEl$css2[getDirectionLabel("width")] = swiper.virtualSize + params.spaceBetween + "px", _$wrapperEl$css2));
    if (params.centeredSlides) {
      newSlidesGrid = [];
      for (var _i = 0; _i < snapGrid.length; _i += 1) {
        var slidesGridItem = snapGrid[_i];
        if (params.roundLengths)
          slidesGridItem = Math.floor(slidesGridItem);
        if (snapGrid[_i] < swiper.virtualSize + snapGrid[0])
          newSlidesGrid.push(slidesGridItem);
      }
      snapGrid = newSlidesGrid;
    }
  }
  if (!params.centeredSlides) {
    newSlidesGrid = [];
    for (var _i2 = 0; _i2 < snapGrid.length; _i2 += 1) {
      var _slidesGridItem = snapGrid[_i2];
      if (params.roundLengths)
        _slidesGridItem = Math.floor(_slidesGridItem);
      if (snapGrid[_i2] <= swiper.virtualSize - swiperSize) {
        newSlidesGrid.push(_slidesGridItem);
      }
    }
    snapGrid = newSlidesGrid;
    if (Math.floor(swiper.virtualSize - swiperSize) - Math.floor(snapGrid[snapGrid.length - 1]) > 1) {
      snapGrid.push(swiper.virtualSize - swiperSize);
    }
  }
  if (snapGrid.length === 0)
    snapGrid = [0];
  if (params.spaceBetween !== 0) {
    var _slides$filter$css;
    var key = swiper.isHorizontal() && rtl ? "marginLeft" : getDirectionLabel("marginRight");
    slides.filter(function(_, slideIndex) {
      if (!params.cssMode)
        return true;
      if (slideIndex === slides.length - 1) {
        return false;
      }
      return true;
    }).css((_slides$filter$css = {}, _slides$filter$css[key] = spaceBetween + "px", _slides$filter$css));
  }
  if (params.centeredSlides && params.centeredSlidesBounds) {
    var allSlidesSize = 0;
    slidesSizesGrid.forEach(function(slideSizeValue) {
      allSlidesSize += slideSizeValue + (params.spaceBetween ? params.spaceBetween : 0);
    });
    allSlidesSize -= params.spaceBetween;
    var maxSnap = allSlidesSize - swiperSize;
    snapGrid = snapGrid.map(function(snap) {
      if (snap < 0)
        return -offsetBefore;
      if (snap > maxSnap)
        return maxSnap + offsetAfter;
      return snap;
    });
  }
  if (params.centerInsufficientSlides) {
    var _allSlidesSize = 0;
    slidesSizesGrid.forEach(function(slideSizeValue) {
      _allSlidesSize += slideSizeValue + (params.spaceBetween ? params.spaceBetween : 0);
    });
    _allSlidesSize -= params.spaceBetween;
    if (_allSlidesSize < swiperSize) {
      var allSlidesOffset = (swiperSize - _allSlidesSize) / 2;
      snapGrid.forEach(function(snap, snapIndex) {
        snapGrid[snapIndex] = snap - allSlidesOffset;
      });
      slidesGrid.forEach(function(snap, snapIndex) {
        slidesGrid[snapIndex] = snap + allSlidesOffset;
      });
    }
  }
  extend2(swiper, {
    slides,
    snapGrid,
    slidesGrid,
    slidesSizesGrid
  });
  if (slidesLength !== previousSlidesLength) {
    swiper.emit("slidesLengthChange");
  }
  if (snapGrid.length !== previousSnapGridLength) {
    if (swiper.params.watchOverflow)
      swiper.checkOverflow();
    swiper.emit("snapGridLengthChange");
  }
  if (slidesGrid.length !== previousSlidesGridLength) {
    swiper.emit("slidesGridLengthChange");
  }
  if (params.watchSlidesProgress || params.watchSlidesVisibility) {
    swiper.updateSlidesOffset();
  }
}

// node_modules/swiper/esm/components/core/update/updateAutoHeight.js
function updateAutoHeight(speed) {
  var swiper = this;
  var activeSlides = [];
  var isVirtual = swiper.virtual && swiper.params.virtual.enabled;
  var newHeight = 0;
  var i;
  if (typeof speed === "number") {
    swiper.setTransition(speed);
  } else if (speed === true) {
    swiper.setTransition(swiper.params.speed);
  }
  var getSlideByIndex = function getSlideByIndex2(index3) {
    if (isVirtual) {
      return swiper.slides.filter(function(el) {
        return parseInt(el.getAttribute("data-swiper-slide-index"), 10) === index3;
      })[0];
    }
    return swiper.slides.eq(index3)[0];
  };
  if (swiper.params.slidesPerView !== "auto" && swiper.params.slidesPerView > 1) {
    if (swiper.params.centeredSlides) {
      swiper.visibleSlides.each(function(slide) {
        activeSlides.push(slide);
      });
    } else {
      for (i = 0; i < Math.ceil(swiper.params.slidesPerView); i += 1) {
        var index2 = swiper.activeIndex + i;
        if (index2 > swiper.slides.length && !isVirtual)
          break;
        activeSlides.push(getSlideByIndex(index2));
      }
    }
  } else {
    activeSlides.push(getSlideByIndex(swiper.activeIndex));
  }
  for (i = 0; i < activeSlides.length; i += 1) {
    if (typeof activeSlides[i] !== "undefined") {
      var height = activeSlides[i].offsetHeight;
      newHeight = height > newHeight ? height : newHeight;
    }
  }
  if (newHeight)
    swiper.$wrapperEl.css("height", newHeight + "px");
}

// node_modules/swiper/esm/components/core/update/updateSlidesOffset.js
function updateSlidesOffset() {
  var swiper = this;
  var slides = swiper.slides;
  for (var i = 0; i < slides.length; i += 1) {
    slides[i].swiperSlideOffset = swiper.isHorizontal() ? slides[i].offsetLeft : slides[i].offsetTop;
  }
}

// node_modules/swiper/esm/components/core/update/updateSlidesProgress.js
function updateSlidesProgress(translate) {
  if (translate === void 0) {
    translate = this && this.translate || 0;
  }
  var swiper = this;
  var params = swiper.params;
  var slides = swiper.slides, rtl = swiper.rtlTranslate;
  if (slides.length === 0)
    return;
  if (typeof slides[0].swiperSlideOffset === "undefined")
    swiper.updateSlidesOffset();
  var offsetCenter = -translate;
  if (rtl)
    offsetCenter = translate;
  slides.removeClass(params.slideVisibleClass);
  swiper.visibleSlidesIndexes = [];
  swiper.visibleSlides = [];
  for (var i = 0; i < slides.length; i += 1) {
    var slide = slides[i];
    var slideProgress = (offsetCenter + (params.centeredSlides ? swiper.minTranslate() : 0) - slide.swiperSlideOffset) / (slide.swiperSlideSize + params.spaceBetween);
    if (params.watchSlidesVisibility || params.centeredSlides && params.autoHeight) {
      var slideBefore = -(offsetCenter - slide.swiperSlideOffset);
      var slideAfter = slideBefore + swiper.slidesSizesGrid[i];
      var isVisible = slideBefore >= 0 && slideBefore < swiper.size - 1 || slideAfter > 1 && slideAfter <= swiper.size || slideBefore <= 0 && slideAfter >= swiper.size;
      if (isVisible) {
        swiper.visibleSlides.push(slide);
        swiper.visibleSlidesIndexes.push(i);
        slides.eq(i).addClass(params.slideVisibleClass);
      }
    }
    slide.progress = rtl ? -slideProgress : slideProgress;
  }
  swiper.visibleSlides = dom_default(swiper.visibleSlides);
}

// node_modules/swiper/esm/components/core/update/updateProgress.js
function updateProgress(translate) {
  var swiper = this;
  if (typeof translate === "undefined") {
    var multiplier = swiper.rtlTranslate ? -1 : 1;
    translate = swiper && swiper.translate && swiper.translate * multiplier || 0;
  }
  var params = swiper.params;
  var translatesDiff = swiper.maxTranslate() - swiper.minTranslate();
  var progress = swiper.progress, isBeginning = swiper.isBeginning, isEnd = swiper.isEnd;
  var wasBeginning = isBeginning;
  var wasEnd = isEnd;
  if (translatesDiff === 0) {
    progress = 0;
    isBeginning = true;
    isEnd = true;
  } else {
    progress = (translate - swiper.minTranslate()) / translatesDiff;
    isBeginning = progress <= 0;
    isEnd = progress >= 1;
  }
  extend2(swiper, {
    progress,
    isBeginning,
    isEnd
  });
  if (params.watchSlidesProgress || params.watchSlidesVisibility || params.centeredSlides && params.autoHeight)
    swiper.updateSlidesProgress(translate);
  if (isBeginning && !wasBeginning) {
    swiper.emit("reachBeginning toEdge");
  }
  if (isEnd && !wasEnd) {
    swiper.emit("reachEnd toEdge");
  }
  if (wasBeginning && !isBeginning || wasEnd && !isEnd) {
    swiper.emit("fromEdge");
  }
  swiper.emit("progress", progress);
}

// node_modules/swiper/esm/components/core/update/updateSlidesClasses.js
function updateSlidesClasses() {
  var swiper = this;
  var slides = swiper.slides, params = swiper.params, $wrapperEl = swiper.$wrapperEl, activeIndex = swiper.activeIndex, realIndex = swiper.realIndex;
  var isVirtual = swiper.virtual && params.virtual.enabled;
  slides.removeClass(params.slideActiveClass + " " + params.slideNextClass + " " + params.slidePrevClass + " " + params.slideDuplicateActiveClass + " " + params.slideDuplicateNextClass + " " + params.slideDuplicatePrevClass);
  var activeSlide;
  if (isVirtual) {
    activeSlide = swiper.$wrapperEl.find("." + params.slideClass + '[data-swiper-slide-index="' + activeIndex + '"]');
  } else {
    activeSlide = slides.eq(activeIndex);
  }
  activeSlide.addClass(params.slideActiveClass);
  if (params.loop) {
    if (activeSlide.hasClass(params.slideDuplicateClass)) {
      $wrapperEl.children("." + params.slideClass + ":not(." + params.slideDuplicateClass + ')[data-swiper-slide-index="' + realIndex + '"]').addClass(params.slideDuplicateActiveClass);
    } else {
      $wrapperEl.children("." + params.slideClass + "." + params.slideDuplicateClass + '[data-swiper-slide-index="' + realIndex + '"]').addClass(params.slideDuplicateActiveClass);
    }
  }
  var nextSlide = activeSlide.nextAll("." + params.slideClass).eq(0).addClass(params.slideNextClass);
  if (params.loop && nextSlide.length === 0) {
    nextSlide = slides.eq(0);
    nextSlide.addClass(params.slideNextClass);
  }
  var prevSlide = activeSlide.prevAll("." + params.slideClass).eq(0).addClass(params.slidePrevClass);
  if (params.loop && prevSlide.length === 0) {
    prevSlide = slides.eq(-1);
    prevSlide.addClass(params.slidePrevClass);
  }
  if (params.loop) {
    if (nextSlide.hasClass(params.slideDuplicateClass)) {
      $wrapperEl.children("." + params.slideClass + ":not(." + params.slideDuplicateClass + ')[data-swiper-slide-index="' + nextSlide.attr("data-swiper-slide-index") + '"]').addClass(params.slideDuplicateNextClass);
    } else {
      $wrapperEl.children("." + params.slideClass + "." + params.slideDuplicateClass + '[data-swiper-slide-index="' + nextSlide.attr("data-swiper-slide-index") + '"]').addClass(params.slideDuplicateNextClass);
    }
    if (prevSlide.hasClass(params.slideDuplicateClass)) {
      $wrapperEl.children("." + params.slideClass + ":not(." + params.slideDuplicateClass + ')[data-swiper-slide-index="' + prevSlide.attr("data-swiper-slide-index") + '"]').addClass(params.slideDuplicatePrevClass);
    } else {
      $wrapperEl.children("." + params.slideClass + "." + params.slideDuplicateClass + '[data-swiper-slide-index="' + prevSlide.attr("data-swiper-slide-index") + '"]').addClass(params.slideDuplicatePrevClass);
    }
  }
  swiper.emitSlidesClasses();
}

// node_modules/swiper/esm/components/core/update/updateActiveIndex.js
function updateActiveIndex(newActiveIndex) {
  var swiper = this;
  var translate = swiper.rtlTranslate ? swiper.translate : -swiper.translate;
  var slidesGrid = swiper.slidesGrid, snapGrid = swiper.snapGrid, params = swiper.params, previousIndex = swiper.activeIndex, previousRealIndex = swiper.realIndex, previousSnapIndex = swiper.snapIndex;
  var activeIndex = newActiveIndex;
  var snapIndex;
  if (typeof activeIndex === "undefined") {
    for (var i = 0; i < slidesGrid.length; i += 1) {
      if (typeof slidesGrid[i + 1] !== "undefined") {
        if (translate >= slidesGrid[i] && translate < slidesGrid[i + 1] - (slidesGrid[i + 1] - slidesGrid[i]) / 2) {
          activeIndex = i;
        } else if (translate >= slidesGrid[i] && translate < slidesGrid[i + 1]) {
          activeIndex = i + 1;
        }
      } else if (translate >= slidesGrid[i]) {
        activeIndex = i;
      }
    }
    if (params.normalizeSlideIndex) {
      if (activeIndex < 0 || typeof activeIndex === "undefined")
        activeIndex = 0;
    }
  }
  if (snapGrid.indexOf(translate) >= 0) {
    snapIndex = snapGrid.indexOf(translate);
  } else {
    var skip = Math.min(params.slidesPerGroupSkip, activeIndex);
    snapIndex = skip + Math.floor((activeIndex - skip) / params.slidesPerGroup);
  }
  if (snapIndex >= snapGrid.length)
    snapIndex = snapGrid.length - 1;
  if (activeIndex === previousIndex) {
    if (snapIndex !== previousSnapIndex) {
      swiper.snapIndex = snapIndex;
      swiper.emit("snapIndexChange");
    }
    return;
  }
  var realIndex = parseInt(swiper.slides.eq(activeIndex).attr("data-swiper-slide-index") || activeIndex, 10);
  extend2(swiper, {
    snapIndex,
    realIndex,
    previousIndex,
    activeIndex
  });
  swiper.emit("activeIndexChange");
  swiper.emit("snapIndexChange");
  if (previousRealIndex !== realIndex) {
    swiper.emit("realIndexChange");
  }
  if (swiper.initialized || swiper.params.runCallbacksOnInit) {
    swiper.emit("slideChange");
  }
}

// node_modules/swiper/esm/components/core/update/updateClickedSlide.js
function updateClickedSlide(e) {
  var swiper = this;
  var params = swiper.params;
  var slide = dom_default(e.target).closest("." + params.slideClass)[0];
  var slideFound = false;
  var slideIndex;
  if (slide) {
    for (var i = 0; i < swiper.slides.length; i += 1) {
      if (swiper.slides[i] === slide) {
        slideFound = true;
        slideIndex = i;
        break;
      }
    }
  }
  if (slide && slideFound) {
    swiper.clickedSlide = slide;
    if (swiper.virtual && swiper.params.virtual.enabled) {
      swiper.clickedIndex = parseInt(dom_default(slide).attr("data-swiper-slide-index"), 10);
    } else {
      swiper.clickedIndex = slideIndex;
    }
  } else {
    swiper.clickedSlide = void 0;
    swiper.clickedIndex = void 0;
    return;
  }
  if (params.slideToClickedSlide && swiper.clickedIndex !== void 0 && swiper.clickedIndex !== swiper.activeIndex) {
    swiper.slideToClickedSlide();
  }
}

// node_modules/swiper/esm/components/core/update/index.js
var update_default = {
  updateSize,
  updateSlides,
  updateAutoHeight,
  updateSlidesOffset,
  updateSlidesProgress,
  updateProgress,
  updateSlidesClasses,
  updateActiveIndex,
  updateClickedSlide
};

// node_modules/swiper/esm/components/core/translate/getTranslate.js
function getSwiperTranslate(axis) {
  if (axis === void 0) {
    axis = this.isHorizontal() ? "x" : "y";
  }
  var swiper = this;
  var params = swiper.params, rtl = swiper.rtlTranslate, translate = swiper.translate, $wrapperEl = swiper.$wrapperEl;
  if (params.virtualTranslate) {
    return rtl ? -translate : translate;
  }
  if (params.cssMode) {
    return translate;
  }
  var currentTranslate = getTranslate2($wrapperEl[0], axis);
  if (rtl)
    currentTranslate = -currentTranslate;
  return currentTranslate || 0;
}

// node_modules/swiper/esm/components/core/translate/setTranslate.js
function setTranslate(translate, byController) {
  var swiper = this;
  var rtl = swiper.rtlTranslate, params = swiper.params, $wrapperEl = swiper.$wrapperEl, wrapperEl = swiper.wrapperEl, progress = swiper.progress;
  var x = 0;
  var y = 0;
  var z = 0;
  if (swiper.isHorizontal()) {
    x = rtl ? -translate : translate;
  } else {
    y = translate;
  }
  if (params.roundLengths) {
    x = Math.floor(x);
    y = Math.floor(y);
  }
  if (params.cssMode) {
    wrapperEl[swiper.isHorizontal() ? "scrollLeft" : "scrollTop"] = swiper.isHorizontal() ? -x : -y;
  } else if (!params.virtualTranslate) {
    $wrapperEl.transform("translate3d(" + x + "px, " + y + "px, " + z + "px)");
  }
  swiper.previousTranslate = swiper.translate;
  swiper.translate = swiper.isHorizontal() ? x : y;
  var newProgress;
  var translatesDiff = swiper.maxTranslate() - swiper.minTranslate();
  if (translatesDiff === 0) {
    newProgress = 0;
  } else {
    newProgress = (translate - swiper.minTranslate()) / translatesDiff;
  }
  if (newProgress !== progress) {
    swiper.updateProgress(translate);
  }
  swiper.emit("setTranslate", swiper.translate, byController);
}

// node_modules/swiper/esm/components/core/translate/minTranslate.js
function minTranslate() {
  return -this.snapGrid[0];
}

// node_modules/swiper/esm/components/core/translate/maxTranslate.js
function maxTranslate() {
  return -this.snapGrid[this.snapGrid.length - 1];
}

// node_modules/swiper/esm/components/core/translate/translateTo.js
function translateTo(translate, speed, runCallbacks, translateBounds, internal) {
  if (translate === void 0) {
    translate = 0;
  }
  if (speed === void 0) {
    speed = this.params.speed;
  }
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  if (translateBounds === void 0) {
    translateBounds = true;
  }
  var swiper = this;
  var params = swiper.params, wrapperEl = swiper.wrapperEl;
  if (swiper.animating && params.preventInteractionOnTransition) {
    return false;
  }
  var minTranslate2 = swiper.minTranslate();
  var maxTranslate2 = swiper.maxTranslate();
  var newTranslate;
  if (translateBounds && translate > minTranslate2)
    newTranslate = minTranslate2;
  else if (translateBounds && translate < maxTranslate2)
    newTranslate = maxTranslate2;
  else
    newTranslate = translate;
  swiper.updateProgress(newTranslate);
  if (params.cssMode) {
    var isH = swiper.isHorizontal();
    if (speed === 0) {
      wrapperEl[isH ? "scrollLeft" : "scrollTop"] = -newTranslate;
    } else {
      if (wrapperEl.scrollTo) {
        var _wrapperEl$scrollTo;
        wrapperEl.scrollTo((_wrapperEl$scrollTo = {}, _wrapperEl$scrollTo[isH ? "left" : "top"] = -newTranslate, _wrapperEl$scrollTo.behavior = "smooth", _wrapperEl$scrollTo));
      } else {
        wrapperEl[isH ? "scrollLeft" : "scrollTop"] = -newTranslate;
      }
    }
    return true;
  }
  if (speed === 0) {
    swiper.setTransition(0);
    swiper.setTranslate(newTranslate);
    if (runCallbacks) {
      swiper.emit("beforeTransitionStart", speed, internal);
      swiper.emit("transitionEnd");
    }
  } else {
    swiper.setTransition(speed);
    swiper.setTranslate(newTranslate);
    if (runCallbacks) {
      swiper.emit("beforeTransitionStart", speed, internal);
      swiper.emit("transitionStart");
    }
    if (!swiper.animating) {
      swiper.animating = true;
      if (!swiper.onTranslateToWrapperTransitionEnd) {
        swiper.onTranslateToWrapperTransitionEnd = function transitionEnd5(e) {
          if (!swiper || swiper.destroyed)
            return;
          if (e.target !== this)
            return;
          swiper.$wrapperEl[0].removeEventListener("transitionend", swiper.onTranslateToWrapperTransitionEnd);
          swiper.$wrapperEl[0].removeEventListener("webkitTransitionEnd", swiper.onTranslateToWrapperTransitionEnd);
          swiper.onTranslateToWrapperTransitionEnd = null;
          delete swiper.onTranslateToWrapperTransitionEnd;
          if (runCallbacks) {
            swiper.emit("transitionEnd");
          }
        };
      }
      swiper.$wrapperEl[0].addEventListener("transitionend", swiper.onTranslateToWrapperTransitionEnd);
      swiper.$wrapperEl[0].addEventListener("webkitTransitionEnd", swiper.onTranslateToWrapperTransitionEnd);
    }
  }
  return true;
}

// node_modules/swiper/esm/components/core/translate/index.js
var translate_default = {
  getTranslate: getSwiperTranslate,
  setTranslate,
  minTranslate,
  maxTranslate,
  translateTo
};

// node_modules/swiper/esm/components/core/transition/setTransition.js
function setTransition(duration, byController) {
  var swiper = this;
  if (!swiper.params.cssMode) {
    swiper.$wrapperEl.transition(duration);
  }
  swiper.emit("setTransition", duration, byController);
}

// node_modules/swiper/esm/components/core/transition/transitionStart.js
function transitionStart(runCallbacks, direction) {
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  var swiper = this;
  var activeIndex = swiper.activeIndex, params = swiper.params, previousIndex = swiper.previousIndex;
  if (params.cssMode)
    return;
  if (params.autoHeight) {
    swiper.updateAutoHeight();
  }
  var dir = direction;
  if (!dir) {
    if (activeIndex > previousIndex)
      dir = "next";
    else if (activeIndex < previousIndex)
      dir = "prev";
    else
      dir = "reset";
  }
  swiper.emit("transitionStart");
  if (runCallbacks && activeIndex !== previousIndex) {
    if (dir === "reset") {
      swiper.emit("slideResetTransitionStart");
      return;
    }
    swiper.emit("slideChangeTransitionStart");
    if (dir === "next") {
      swiper.emit("slideNextTransitionStart");
    } else {
      swiper.emit("slidePrevTransitionStart");
    }
  }
}

// node_modules/swiper/esm/components/core/transition/transitionEnd.js
function transitionEnd2(runCallbacks, direction) {
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  var swiper = this;
  var activeIndex = swiper.activeIndex, previousIndex = swiper.previousIndex, params = swiper.params;
  swiper.animating = false;
  if (params.cssMode)
    return;
  swiper.setTransition(0);
  var dir = direction;
  if (!dir) {
    if (activeIndex > previousIndex)
      dir = "next";
    else if (activeIndex < previousIndex)
      dir = "prev";
    else
      dir = "reset";
  }
  swiper.emit("transitionEnd");
  if (runCallbacks && activeIndex !== previousIndex) {
    if (dir === "reset") {
      swiper.emit("slideResetTransitionEnd");
      return;
    }
    swiper.emit("slideChangeTransitionEnd");
    if (dir === "next") {
      swiper.emit("slideNextTransitionEnd");
    } else {
      swiper.emit("slidePrevTransitionEnd");
    }
  }
}

// node_modules/swiper/esm/components/core/transition/index.js
var transition_default = {
  setTransition,
  transitionStart,
  transitionEnd: transitionEnd2
};

// node_modules/swiper/esm/components/core/slide/slideTo.js
function slideTo(index2, speed, runCallbacks, internal, initial) {
  if (index2 === void 0) {
    index2 = 0;
  }
  if (speed === void 0) {
    speed = this.params.speed;
  }
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  if (typeof index2 !== "number" && typeof index2 !== "string") {
    throw new Error("The 'index' argument cannot have type other than 'number' or 'string'. [" + typeof index2 + "] given.");
  }
  if (typeof index2 === "string") {
    var indexAsNumber = parseInt(index2, 10);
    var isValidNumber = isFinite(indexAsNumber);
    if (!isValidNumber) {
      throw new Error("The passed-in 'index' (string) couldn't be converted to 'number'. [" + index2 + "] given.");
    }
    index2 = indexAsNumber;
  }
  var swiper = this;
  var slideIndex = index2;
  if (slideIndex < 0)
    slideIndex = 0;
  var params = swiper.params, snapGrid = swiper.snapGrid, slidesGrid = swiper.slidesGrid, previousIndex = swiper.previousIndex, activeIndex = swiper.activeIndex, rtl = swiper.rtlTranslate, wrapperEl = swiper.wrapperEl, enabled = swiper.enabled;
  if (swiper.animating && params.preventInteractionOnTransition || !enabled && !internal && !initial) {
    return false;
  }
  var skip = Math.min(swiper.params.slidesPerGroupSkip, slideIndex);
  var snapIndex = skip + Math.floor((slideIndex - skip) / swiper.params.slidesPerGroup);
  if (snapIndex >= snapGrid.length)
    snapIndex = snapGrid.length - 1;
  if ((activeIndex || params.initialSlide || 0) === (previousIndex || 0) && runCallbacks) {
    swiper.emit("beforeSlideChangeStart");
  }
  var translate = -snapGrid[snapIndex];
  swiper.updateProgress(translate);
  if (params.normalizeSlideIndex) {
    for (var i = 0; i < slidesGrid.length; i += 1) {
      var normalizedTranslate = -Math.floor(translate * 100);
      var normalizedGird = Math.floor(slidesGrid[i] * 100);
      var normalizedGridNext = Math.floor(slidesGrid[i + 1] * 100);
      if (typeof slidesGrid[i + 1] !== "undefined") {
        if (normalizedTranslate >= normalizedGird && normalizedTranslate < normalizedGridNext - (normalizedGridNext - normalizedGird) / 2) {
          slideIndex = i;
        } else if (normalizedTranslate >= normalizedGird && normalizedTranslate < normalizedGridNext) {
          slideIndex = i + 1;
        }
      } else if (normalizedTranslate >= normalizedGird) {
        slideIndex = i;
      }
    }
  }
  if (swiper.initialized && slideIndex !== activeIndex) {
    if (!swiper.allowSlideNext && translate < swiper.translate && translate < swiper.minTranslate()) {
      return false;
    }
    if (!swiper.allowSlidePrev && translate > swiper.translate && translate > swiper.maxTranslate()) {
      if ((activeIndex || 0) !== slideIndex)
        return false;
    }
  }
  var direction;
  if (slideIndex > activeIndex)
    direction = "next";
  else if (slideIndex < activeIndex)
    direction = "prev";
  else
    direction = "reset";
  if (rtl && -translate === swiper.translate || !rtl && translate === swiper.translate) {
    swiper.updateActiveIndex(slideIndex);
    if (params.autoHeight) {
      swiper.updateAutoHeight();
    }
    swiper.updateSlidesClasses();
    if (params.effect !== "slide") {
      swiper.setTranslate(translate);
    }
    if (direction !== "reset") {
      swiper.transitionStart(runCallbacks, direction);
      swiper.transitionEnd(runCallbacks, direction);
    }
    return false;
  }
  if (params.cssMode) {
    var isH = swiper.isHorizontal();
    var t = -translate;
    if (rtl) {
      t = wrapperEl.scrollWidth - wrapperEl.offsetWidth - t;
    }
    if (speed === 0) {
      wrapperEl[isH ? "scrollLeft" : "scrollTop"] = t;
    } else {
      if (wrapperEl.scrollTo) {
        var _wrapperEl$scrollTo;
        wrapperEl.scrollTo((_wrapperEl$scrollTo = {}, _wrapperEl$scrollTo[isH ? "left" : "top"] = t, _wrapperEl$scrollTo.behavior = "smooth", _wrapperEl$scrollTo));
      } else {
        wrapperEl[isH ? "scrollLeft" : "scrollTop"] = t;
      }
    }
    return true;
  }
  if (speed === 0) {
    swiper.setTransition(0);
    swiper.setTranslate(translate);
    swiper.updateActiveIndex(slideIndex);
    swiper.updateSlidesClasses();
    swiper.emit("beforeTransitionStart", speed, internal);
    swiper.transitionStart(runCallbacks, direction);
    swiper.transitionEnd(runCallbacks, direction);
  } else {
    swiper.setTransition(speed);
    swiper.setTranslate(translate);
    swiper.updateActiveIndex(slideIndex);
    swiper.updateSlidesClasses();
    swiper.emit("beforeTransitionStart", speed, internal);
    swiper.transitionStart(runCallbacks, direction);
    if (!swiper.animating) {
      swiper.animating = true;
      if (!swiper.onSlideToWrapperTransitionEnd) {
        swiper.onSlideToWrapperTransitionEnd = function transitionEnd5(e) {
          if (!swiper || swiper.destroyed)
            return;
          if (e.target !== this)
            return;
          swiper.$wrapperEl[0].removeEventListener("transitionend", swiper.onSlideToWrapperTransitionEnd);
          swiper.$wrapperEl[0].removeEventListener("webkitTransitionEnd", swiper.onSlideToWrapperTransitionEnd);
          swiper.onSlideToWrapperTransitionEnd = null;
          delete swiper.onSlideToWrapperTransitionEnd;
          swiper.transitionEnd(runCallbacks, direction);
        };
      }
      swiper.$wrapperEl[0].addEventListener("transitionend", swiper.onSlideToWrapperTransitionEnd);
      swiper.$wrapperEl[0].addEventListener("webkitTransitionEnd", swiper.onSlideToWrapperTransitionEnd);
    }
  }
  return true;
}

// node_modules/swiper/esm/components/core/slide/slideToLoop.js
function slideToLoop(index2, speed, runCallbacks, internal) {
  if (index2 === void 0) {
    index2 = 0;
  }
  if (speed === void 0) {
    speed = this.params.speed;
  }
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  var swiper = this;
  var newIndex = index2;
  if (swiper.params.loop) {
    newIndex += swiper.loopedSlides;
  }
  return swiper.slideTo(newIndex, speed, runCallbacks, internal);
}

// node_modules/swiper/esm/components/core/slide/slideNext.js
function slideNext(speed, runCallbacks, internal) {
  if (speed === void 0) {
    speed = this.params.speed;
  }
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  var swiper = this;
  var params = swiper.params, animating = swiper.animating, enabled = swiper.enabled;
  if (!enabled)
    return swiper;
  var increment = swiper.activeIndex < params.slidesPerGroupSkip ? 1 : params.slidesPerGroup;
  if (params.loop) {
    if (animating && params.loopPreventsSlide)
      return false;
    swiper.loopFix();
    swiper._clientLeft = swiper.$wrapperEl[0].clientLeft;
  }
  return swiper.slideTo(swiper.activeIndex + increment, speed, runCallbacks, internal);
}

// node_modules/swiper/esm/components/core/slide/slidePrev.js
function slidePrev(speed, runCallbacks, internal) {
  if (speed === void 0) {
    speed = this.params.speed;
  }
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  var swiper = this;
  var params = swiper.params, animating = swiper.animating, snapGrid = swiper.snapGrid, slidesGrid = swiper.slidesGrid, rtlTranslate = swiper.rtlTranslate, enabled = swiper.enabled;
  if (!enabled)
    return swiper;
  if (params.loop) {
    if (animating && params.loopPreventsSlide)
      return false;
    swiper.loopFix();
    swiper._clientLeft = swiper.$wrapperEl[0].clientLeft;
  }
  var translate = rtlTranslate ? swiper.translate : -swiper.translate;
  function normalize2(val) {
    if (val < 0)
      return -Math.floor(Math.abs(val));
    return Math.floor(val);
  }
  var normalizedTranslate = normalize2(translate);
  var normalizedSnapGrid = snapGrid.map(function(val) {
    return normalize2(val);
  });
  var prevSnap = snapGrid[normalizedSnapGrid.indexOf(normalizedTranslate) - 1];
  if (typeof prevSnap === "undefined" && params.cssMode) {
    snapGrid.forEach(function(snap) {
      if (!prevSnap && normalizedTranslate >= snap)
        prevSnap = snap;
    });
  }
  var prevIndex;
  if (typeof prevSnap !== "undefined") {
    prevIndex = slidesGrid.indexOf(prevSnap);
    if (prevIndex < 0)
      prevIndex = swiper.activeIndex - 1;
  }
  return swiper.slideTo(prevIndex, speed, runCallbacks, internal);
}

// node_modules/swiper/esm/components/core/slide/slideReset.js
function slideReset(speed, runCallbacks, internal) {
  if (speed === void 0) {
    speed = this.params.speed;
  }
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  var swiper = this;
  return swiper.slideTo(swiper.activeIndex, speed, runCallbacks, internal);
}

// node_modules/swiper/esm/components/core/slide/slideToClosest.js
function slideToClosest(speed, runCallbacks, internal, threshold) {
  if (speed === void 0) {
    speed = this.params.speed;
  }
  if (runCallbacks === void 0) {
    runCallbacks = true;
  }
  if (threshold === void 0) {
    threshold = 0.5;
  }
  var swiper = this;
  var index2 = swiper.activeIndex;
  var skip = Math.min(swiper.params.slidesPerGroupSkip, index2);
  var snapIndex = skip + Math.floor((index2 - skip) / swiper.params.slidesPerGroup);
  var translate = swiper.rtlTranslate ? swiper.translate : -swiper.translate;
  if (translate >= swiper.snapGrid[snapIndex]) {
    var currentSnap = swiper.snapGrid[snapIndex];
    var nextSnap = swiper.snapGrid[snapIndex + 1];
    if (translate - currentSnap > (nextSnap - currentSnap) * threshold) {
      index2 += swiper.params.slidesPerGroup;
    }
  } else {
    var prevSnap = swiper.snapGrid[snapIndex - 1];
    var _currentSnap = swiper.snapGrid[snapIndex];
    if (translate - prevSnap <= (_currentSnap - prevSnap) * threshold) {
      index2 -= swiper.params.slidesPerGroup;
    }
  }
  index2 = Math.max(index2, 0);
  index2 = Math.min(index2, swiper.slidesGrid.length - 1);
  return swiper.slideTo(index2, speed, runCallbacks, internal);
}

// node_modules/swiper/esm/components/core/slide/slideToClickedSlide.js
function slideToClickedSlide() {
  var swiper = this;
  var params = swiper.params, $wrapperEl = swiper.$wrapperEl;
  var slidesPerView = params.slidesPerView === "auto" ? swiper.slidesPerViewDynamic() : params.slidesPerView;
  var slideToIndex = swiper.clickedIndex;
  var realIndex;
  if (params.loop) {
    if (swiper.animating)
      return;
    realIndex = parseInt(dom_default(swiper.clickedSlide).attr("data-swiper-slide-index"), 10);
    if (params.centeredSlides) {
      if (slideToIndex < swiper.loopedSlides - slidesPerView / 2 || slideToIndex > swiper.slides.length - swiper.loopedSlides + slidesPerView / 2) {
        swiper.loopFix();
        slideToIndex = $wrapperEl.children("." + params.slideClass + '[data-swiper-slide-index="' + realIndex + '"]:not(.' + params.slideDuplicateClass + ")").eq(0).index();
        nextTick2(function() {
          swiper.slideTo(slideToIndex);
        });
      } else {
        swiper.slideTo(slideToIndex);
      }
    } else if (slideToIndex > swiper.slides.length - slidesPerView) {
      swiper.loopFix();
      slideToIndex = $wrapperEl.children("." + params.slideClass + '[data-swiper-slide-index="' + realIndex + '"]:not(.' + params.slideDuplicateClass + ")").eq(0).index();
      nextTick2(function() {
        swiper.slideTo(slideToIndex);
      });
    } else {
      swiper.slideTo(slideToIndex);
    }
  } else {
    swiper.slideTo(slideToIndex);
  }
}

// node_modules/swiper/esm/components/core/slide/index.js
var slide_default = {
  slideTo,
  slideToLoop,
  slideNext,
  slidePrev,
  slideReset,
  slideToClosest,
  slideToClickedSlide
};

// node_modules/swiper/esm/components/core/loop/loopCreate.js
function loopCreate() {
  var swiper = this;
  var document = getDocument();
  var params = swiper.params, $wrapperEl = swiper.$wrapperEl;
  $wrapperEl.children("." + params.slideClass + "." + params.slideDuplicateClass).remove();
  var slides = $wrapperEl.children("." + params.slideClass);
  if (params.loopFillGroupWithBlank) {
    var blankSlidesNum = params.slidesPerGroup - slides.length % params.slidesPerGroup;
    if (blankSlidesNum !== params.slidesPerGroup) {
      for (var i = 0; i < blankSlidesNum; i += 1) {
        var blankNode = dom_default(document.createElement("div")).addClass(params.slideClass + " " + params.slideBlankClass);
        $wrapperEl.append(blankNode);
      }
      slides = $wrapperEl.children("." + params.slideClass);
    }
  }
  if (params.slidesPerView === "auto" && !params.loopedSlides)
    params.loopedSlides = slides.length;
  swiper.loopedSlides = Math.ceil(parseFloat(params.loopedSlides || params.slidesPerView, 10));
  swiper.loopedSlides += params.loopAdditionalSlides;
  if (swiper.loopedSlides > slides.length) {
    swiper.loopedSlides = slides.length;
  }
  var prependSlides = [];
  var appendSlides = [];
  slides.each(function(el, index2) {
    var slide = dom_default(el);
    if (index2 < swiper.loopedSlides) {
      appendSlides.push(el);
    }
    if (index2 < slides.length && index2 >= slides.length - swiper.loopedSlides) {
      prependSlides.push(el);
    }
    slide.attr("data-swiper-slide-index", index2);
  });
  for (var _i = 0; _i < appendSlides.length; _i += 1) {
    $wrapperEl.append(dom_default(appendSlides[_i].cloneNode(true)).addClass(params.slideDuplicateClass));
  }
  for (var _i2 = prependSlides.length - 1; _i2 >= 0; _i2 -= 1) {
    $wrapperEl.prepend(dom_default(prependSlides[_i2].cloneNode(true)).addClass(params.slideDuplicateClass));
  }
}

// node_modules/swiper/esm/components/core/loop/loopFix.js
function loopFix() {
  var swiper = this;
  swiper.emit("beforeLoopFix");
  var activeIndex = swiper.activeIndex, slides = swiper.slides, loopedSlides = swiper.loopedSlides, allowSlidePrev = swiper.allowSlidePrev, allowSlideNext = swiper.allowSlideNext, snapGrid = swiper.snapGrid, rtl = swiper.rtlTranslate;
  var newIndex;
  swiper.allowSlidePrev = true;
  swiper.allowSlideNext = true;
  var snapTranslate = -snapGrid[activeIndex];
  var diff = snapTranslate - swiper.getTranslate();
  if (activeIndex < loopedSlides) {
    newIndex = slides.length - loopedSlides * 3 + activeIndex;
    newIndex += loopedSlides;
    var slideChanged = swiper.slideTo(newIndex, 0, false, true);
    if (slideChanged && diff !== 0) {
      swiper.setTranslate((rtl ? -swiper.translate : swiper.translate) - diff);
    }
  } else if (activeIndex >= slides.length - loopedSlides) {
    newIndex = -slides.length + activeIndex + loopedSlides;
    newIndex += loopedSlides;
    var _slideChanged = swiper.slideTo(newIndex, 0, false, true);
    if (_slideChanged && diff !== 0) {
      swiper.setTranslate((rtl ? -swiper.translate : swiper.translate) - diff);
    }
  }
  swiper.allowSlidePrev = allowSlidePrev;
  swiper.allowSlideNext = allowSlideNext;
  swiper.emit("loopFix");
}

// node_modules/swiper/esm/components/core/loop/loopDestroy.js
function loopDestroy() {
  var swiper = this;
  var $wrapperEl = swiper.$wrapperEl, params = swiper.params, slides = swiper.slides;
  $wrapperEl.children("." + params.slideClass + "." + params.slideDuplicateClass + ",." + params.slideClass + "." + params.slideBlankClass).remove();
  slides.removeAttr("data-swiper-slide-index");
}

// node_modules/swiper/esm/components/core/loop/index.js
var loop_default = {
  loopCreate,
  loopFix,
  loopDestroy
};

// node_modules/swiper/esm/components/core/grab-cursor/setGrabCursor.js
function setGrabCursor(moving) {
  var swiper = this;
  if (swiper.support.touch || !swiper.params.simulateTouch || swiper.params.watchOverflow && swiper.isLocked || swiper.params.cssMode)
    return;
  var el = swiper.el;
  el.style.cursor = "move";
  el.style.cursor = moving ? "-webkit-grabbing" : "-webkit-grab";
  el.style.cursor = moving ? "-moz-grabbin" : "-moz-grab";
  el.style.cursor = moving ? "grabbing" : "grab";
}

// node_modules/swiper/esm/components/core/grab-cursor/unsetGrabCursor.js
function unsetGrabCursor() {
  var swiper = this;
  if (swiper.support.touch || swiper.params.watchOverflow && swiper.isLocked || swiper.params.cssMode) {
    return;
  }
  swiper.el.style.cursor = "";
}

// node_modules/swiper/esm/components/core/grab-cursor/index.js
var grab_cursor_default = {
  setGrabCursor,
  unsetGrabCursor
};

// node_modules/swiper/esm/components/core/manipulation/appendSlide.js
function appendSlide(slides) {
  var swiper = this;
  var $wrapperEl = swiper.$wrapperEl, params = swiper.params;
  if (params.loop) {
    swiper.loopDestroy();
  }
  if (typeof slides === "object" && "length" in slides) {
    for (var i = 0; i < slides.length; i += 1) {
      if (slides[i])
        $wrapperEl.append(slides[i]);
    }
  } else {
    $wrapperEl.append(slides);
  }
  if (params.loop) {
    swiper.loopCreate();
  }
  if (!(params.observer && swiper.support.observer)) {
    swiper.update();
  }
}

// node_modules/swiper/esm/components/core/manipulation/prependSlide.js
function prependSlide(slides) {
  var swiper = this;
  var params = swiper.params, $wrapperEl = swiper.$wrapperEl, activeIndex = swiper.activeIndex;
  if (params.loop) {
    swiper.loopDestroy();
  }
  var newActiveIndex = activeIndex + 1;
  if (typeof slides === "object" && "length" in slides) {
    for (var i = 0; i < slides.length; i += 1) {
      if (slides[i])
        $wrapperEl.prepend(slides[i]);
    }
    newActiveIndex = activeIndex + slides.length;
  } else {
    $wrapperEl.prepend(slides);
  }
  if (params.loop) {
    swiper.loopCreate();
  }
  if (!(params.observer && swiper.support.observer)) {
    swiper.update();
  }
  swiper.slideTo(newActiveIndex, 0, false);
}

// node_modules/swiper/esm/components/core/manipulation/addSlide.js
function addSlide(index2, slides) {
  var swiper = this;
  var $wrapperEl = swiper.$wrapperEl, params = swiper.params, activeIndex = swiper.activeIndex;
  var activeIndexBuffer = activeIndex;
  if (params.loop) {
    activeIndexBuffer -= swiper.loopedSlides;
    swiper.loopDestroy();
    swiper.slides = $wrapperEl.children("." + params.slideClass);
  }
  var baseLength = swiper.slides.length;
  if (index2 <= 0) {
    swiper.prependSlide(slides);
    return;
  }
  if (index2 >= baseLength) {
    swiper.appendSlide(slides);
    return;
  }
  var newActiveIndex = activeIndexBuffer > index2 ? activeIndexBuffer + 1 : activeIndexBuffer;
  var slidesBuffer = [];
  for (var i = baseLength - 1; i >= index2; i -= 1) {
    var currentSlide = swiper.slides.eq(i);
    currentSlide.remove();
    slidesBuffer.unshift(currentSlide);
  }
  if (typeof slides === "object" && "length" in slides) {
    for (var _i = 0; _i < slides.length; _i += 1) {
      if (slides[_i])
        $wrapperEl.append(slides[_i]);
    }
    newActiveIndex = activeIndexBuffer > index2 ? activeIndexBuffer + slides.length : activeIndexBuffer;
  } else {
    $wrapperEl.append(slides);
  }
  for (var _i2 = 0; _i2 < slidesBuffer.length; _i2 += 1) {
    $wrapperEl.append(slidesBuffer[_i2]);
  }
  if (params.loop) {
    swiper.loopCreate();
  }
  if (!(params.observer && swiper.support.observer)) {
    swiper.update();
  }
  if (params.loop) {
    swiper.slideTo(newActiveIndex + swiper.loopedSlides, 0, false);
  } else {
    swiper.slideTo(newActiveIndex, 0, false);
  }
}

// node_modules/swiper/esm/components/core/manipulation/removeSlide.js
function removeSlide(slidesIndexes) {
  var swiper = this;
  var params = swiper.params, $wrapperEl = swiper.$wrapperEl, activeIndex = swiper.activeIndex;
  var activeIndexBuffer = activeIndex;
  if (params.loop) {
    activeIndexBuffer -= swiper.loopedSlides;
    swiper.loopDestroy();
    swiper.slides = $wrapperEl.children("." + params.slideClass);
  }
  var newActiveIndex = activeIndexBuffer;
  var indexToRemove;
  if (typeof slidesIndexes === "object" && "length" in slidesIndexes) {
    for (var i = 0; i < slidesIndexes.length; i += 1) {
      indexToRemove = slidesIndexes[i];
      if (swiper.slides[indexToRemove])
        swiper.slides.eq(indexToRemove).remove();
      if (indexToRemove < newActiveIndex)
        newActiveIndex -= 1;
    }
    newActiveIndex = Math.max(newActiveIndex, 0);
  } else {
    indexToRemove = slidesIndexes;
    if (swiper.slides[indexToRemove])
      swiper.slides.eq(indexToRemove).remove();
    if (indexToRemove < newActiveIndex)
      newActiveIndex -= 1;
    newActiveIndex = Math.max(newActiveIndex, 0);
  }
  if (params.loop) {
    swiper.loopCreate();
  }
  if (!(params.observer && swiper.support.observer)) {
    swiper.update();
  }
  if (params.loop) {
    swiper.slideTo(newActiveIndex + swiper.loopedSlides, 0, false);
  } else {
    swiper.slideTo(newActiveIndex, 0, false);
  }
}

// node_modules/swiper/esm/components/core/manipulation/removeAllSlides.js
function removeAllSlides() {
  var swiper = this;
  var slidesIndexes = [];
  for (var i = 0; i < swiper.slides.length; i += 1) {
    slidesIndexes.push(i);
  }
  swiper.removeSlide(slidesIndexes);
}

// node_modules/swiper/esm/components/core/manipulation/index.js
var manipulation_default = {
  appendSlide,
  prependSlide,
  addSlide,
  removeSlide,
  removeAllSlides
};

// node_modules/swiper/esm/components/core/events/onTouchStart.js
function closestElement(selector, base) {
  if (base === void 0) {
    base = this;
  }
  function __closestFrom(el) {
    if (!el || el === getDocument() || el === getWindow())
      return null;
    if (el.assignedSlot)
      el = el.assignedSlot;
    var found = el.closest(selector);
    return found || __closestFrom(el.getRootNode().host);
  }
  return __closestFrom(base);
}
function onTouchStart(event2) {
  var swiper = this;
  var document = getDocument();
  var window2 = getWindow();
  var data = swiper.touchEventsData;
  var params = swiper.params, touches = swiper.touches, enabled = swiper.enabled;
  if (!enabled)
    return;
  if (swiper.animating && params.preventInteractionOnTransition) {
    return;
  }
  var e = event2;
  if (e.originalEvent)
    e = e.originalEvent;
  var $targetEl = dom_default(e.target);
  if (params.touchEventsTarget === "wrapper") {
    if (!$targetEl.closest(swiper.wrapperEl).length)
      return;
  }
  data.isTouchEvent = e.type === "touchstart";
  if (!data.isTouchEvent && "which" in e && e.which === 3)
    return;
  if (!data.isTouchEvent && "button" in e && e.button > 0)
    return;
  if (data.isTouched && data.isMoved)
    return;
  var swipingClassHasValue = !!params.noSwipingClass && params.noSwipingClass !== "";
  if (swipingClassHasValue && e.target && e.target.shadowRoot && event2.path && event2.path[0]) {
    $targetEl = dom_default(event2.path[0]);
  }
  var noSwipingSelector = params.noSwipingSelector ? params.noSwipingSelector : "." + params.noSwipingClass;
  var isTargetShadow = !!(e.target && e.target.shadowRoot);
  if (params.noSwiping && (isTargetShadow ? closestElement(noSwipingSelector, e.target) : $targetEl.closest(noSwipingSelector)[0])) {
    swiper.allowClick = true;
    return;
  }
  if (params.swipeHandler) {
    if (!$targetEl.closest(params.swipeHandler)[0])
      return;
  }
  touches.currentX = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
  touches.currentY = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
  var startX = touches.currentX;
  var startY = touches.currentY;
  var edgeSwipeDetection = params.edgeSwipeDetection || params.iOSEdgeSwipeDetection;
  var edgeSwipeThreshold = params.edgeSwipeThreshold || params.iOSEdgeSwipeThreshold;
  if (edgeSwipeDetection && (startX <= edgeSwipeThreshold || startX >= window2.innerWidth - edgeSwipeThreshold)) {
    if (edgeSwipeDetection === "prevent") {
      event2.preventDefault();
    } else {
      return;
    }
  }
  extend2(data, {
    isTouched: true,
    isMoved: false,
    allowTouchCallbacks: true,
    isScrolling: void 0,
    startMoving: void 0
  });
  touches.startX = startX;
  touches.startY = startY;
  data.touchStartTime = now2();
  swiper.allowClick = true;
  swiper.updateSize();
  swiper.swipeDirection = void 0;
  if (params.threshold > 0)
    data.allowThresholdMove = false;
  if (e.type !== "touchstart") {
    var preventDefault = true;
    if ($targetEl.is(data.focusableElements))
      preventDefault = false;
    if (document.activeElement && dom_default(document.activeElement).is(data.focusableElements) && document.activeElement !== $targetEl[0]) {
      document.activeElement.blur();
    }
    var shouldPreventDefault = preventDefault && swiper.allowTouchMove && params.touchStartPreventDefault;
    if ((params.touchStartForcePreventDefault || shouldPreventDefault) && !$targetEl[0].isContentEditable) {
      e.preventDefault();
    }
  }
  swiper.emit("touchStart", e);
}

// node_modules/swiper/esm/components/core/events/onTouchMove.js
function onTouchMove(event2) {
  var document = getDocument();
  var swiper = this;
  var data = swiper.touchEventsData;
  var params = swiper.params, touches = swiper.touches, rtl = swiper.rtlTranslate, enabled = swiper.enabled;
  if (!enabled)
    return;
  var e = event2;
  if (e.originalEvent)
    e = e.originalEvent;
  if (!data.isTouched) {
    if (data.startMoving && data.isScrolling) {
      swiper.emit("touchMoveOpposite", e);
    }
    return;
  }
  if (data.isTouchEvent && e.type !== "touchmove")
    return;
  var targetTouch = e.type === "touchmove" && e.targetTouches && (e.targetTouches[0] || e.changedTouches[0]);
  var pageX = e.type === "touchmove" ? targetTouch.pageX : e.pageX;
  var pageY = e.type === "touchmove" ? targetTouch.pageY : e.pageY;
  if (e.preventedByNestedSwiper) {
    touches.startX = pageX;
    touches.startY = pageY;
    return;
  }
  if (!swiper.allowTouchMove) {
    swiper.allowClick = false;
    if (data.isTouched) {
      extend2(touches, {
        startX: pageX,
        startY: pageY,
        currentX: pageX,
        currentY: pageY
      });
      data.touchStartTime = now2();
    }
    return;
  }
  if (data.isTouchEvent && params.touchReleaseOnEdges && !params.loop) {
    if (swiper.isVertical()) {
      if (pageY < touches.startY && swiper.translate <= swiper.maxTranslate() || pageY > touches.startY && swiper.translate >= swiper.minTranslate()) {
        data.isTouched = false;
        data.isMoved = false;
        return;
      }
    } else if (pageX < touches.startX && swiper.translate <= swiper.maxTranslate() || pageX > touches.startX && swiper.translate >= swiper.minTranslate()) {
      return;
    }
  }
  if (data.isTouchEvent && document.activeElement) {
    if (e.target === document.activeElement && dom_default(e.target).is(data.focusableElements)) {
      data.isMoved = true;
      swiper.allowClick = false;
      return;
    }
  }
  if (data.allowTouchCallbacks) {
    swiper.emit("touchMove", e);
  }
  if (e.targetTouches && e.targetTouches.length > 1)
    return;
  touches.currentX = pageX;
  touches.currentY = pageY;
  var diffX = touches.currentX - touches.startX;
  var diffY = touches.currentY - touches.startY;
  if (swiper.params.threshold && Math.sqrt(Math.pow(diffX, 2) + Math.pow(diffY, 2)) < swiper.params.threshold)
    return;
  if (typeof data.isScrolling === "undefined") {
    var touchAngle;
    if (swiper.isHorizontal() && touches.currentY === touches.startY || swiper.isVertical() && touches.currentX === touches.startX) {
      data.isScrolling = false;
    } else {
      if (diffX * diffX + diffY * diffY >= 25) {
        touchAngle = Math.atan2(Math.abs(diffY), Math.abs(diffX)) * 180 / Math.PI;
        data.isScrolling = swiper.isHorizontal() ? touchAngle > params.touchAngle : 90 - touchAngle > params.touchAngle;
      }
    }
  }
  if (data.isScrolling) {
    swiper.emit("touchMoveOpposite", e);
  }
  if (typeof data.startMoving === "undefined") {
    if (touches.currentX !== touches.startX || touches.currentY !== touches.startY) {
      data.startMoving = true;
    }
  }
  if (data.isScrolling) {
    data.isTouched = false;
    return;
  }
  if (!data.startMoving) {
    return;
  }
  swiper.allowClick = false;
  if (!params.cssMode && e.cancelable) {
    e.preventDefault();
  }
  if (params.touchMoveStopPropagation && !params.nested) {
    e.stopPropagation();
  }
  if (!data.isMoved) {
    if (params.loop) {
      swiper.loopFix();
    }
    data.startTranslate = swiper.getTranslate();
    swiper.setTransition(0);
    if (swiper.animating) {
      swiper.$wrapperEl.trigger("webkitTransitionEnd transitionend");
    }
    data.allowMomentumBounce = false;
    if (params.grabCursor && (swiper.allowSlideNext === true || swiper.allowSlidePrev === true)) {
      swiper.setGrabCursor(true);
    }
    swiper.emit("sliderFirstMove", e);
  }
  swiper.emit("sliderMove", e);
  data.isMoved = true;
  var diff = swiper.isHorizontal() ? diffX : diffY;
  touches.diff = diff;
  diff *= params.touchRatio;
  if (rtl)
    diff = -diff;
  swiper.swipeDirection = diff > 0 ? "prev" : "next";
  data.currentTranslate = diff + data.startTranslate;
  var disableParentSwiper = true;
  var resistanceRatio = params.resistanceRatio;
  if (params.touchReleaseOnEdges) {
    resistanceRatio = 0;
  }
  if (diff > 0 && data.currentTranslate > swiper.minTranslate()) {
    disableParentSwiper = false;
    if (params.resistance)
      data.currentTranslate = swiper.minTranslate() - 1 + Math.pow(-swiper.minTranslate() + data.startTranslate + diff, resistanceRatio);
  } else if (diff < 0 && data.currentTranslate < swiper.maxTranslate()) {
    disableParentSwiper = false;
    if (params.resistance)
      data.currentTranslate = swiper.maxTranslate() + 1 - Math.pow(swiper.maxTranslate() - data.startTranslate - diff, resistanceRatio);
  }
  if (disableParentSwiper) {
    e.preventedByNestedSwiper = true;
  }
  if (!swiper.allowSlideNext && swiper.swipeDirection === "next" && data.currentTranslate < data.startTranslate) {
    data.currentTranslate = data.startTranslate;
  }
  if (!swiper.allowSlidePrev && swiper.swipeDirection === "prev" && data.currentTranslate > data.startTranslate) {
    data.currentTranslate = data.startTranslate;
  }
  if (!swiper.allowSlidePrev && !swiper.allowSlideNext) {
    data.currentTranslate = data.startTranslate;
  }
  if (params.threshold > 0) {
    if (Math.abs(diff) > params.threshold || data.allowThresholdMove) {
      if (!data.allowThresholdMove) {
        data.allowThresholdMove = true;
        touches.startX = touches.currentX;
        touches.startY = touches.currentY;
        data.currentTranslate = data.startTranslate;
        touches.diff = swiper.isHorizontal() ? touches.currentX - touches.startX : touches.currentY - touches.startY;
        return;
      }
    } else {
      data.currentTranslate = data.startTranslate;
      return;
    }
  }
  if (!params.followFinger || params.cssMode)
    return;
  if (params.freeMode || params.watchSlidesProgress || params.watchSlidesVisibility) {
    swiper.updateActiveIndex();
    swiper.updateSlidesClasses();
  }
  if (params.freeMode) {
    if (data.velocities.length === 0) {
      data.velocities.push({
        position: touches[swiper.isHorizontal() ? "startX" : "startY"],
        time: data.touchStartTime
      });
    }
    data.velocities.push({
      position: touches[swiper.isHorizontal() ? "currentX" : "currentY"],
      time: now2()
    });
  }
  swiper.updateProgress(data.currentTranslate);
  swiper.setTranslate(data.currentTranslate);
}

// node_modules/swiper/esm/components/core/events/onTouchEnd.js
function onTouchEnd(event2) {
  var swiper = this;
  var data = swiper.touchEventsData;
  var params = swiper.params, touches = swiper.touches, rtl = swiper.rtlTranslate, $wrapperEl = swiper.$wrapperEl, slidesGrid = swiper.slidesGrid, snapGrid = swiper.snapGrid, enabled = swiper.enabled;
  if (!enabled)
    return;
  var e = event2;
  if (e.originalEvent)
    e = e.originalEvent;
  if (data.allowTouchCallbacks) {
    swiper.emit("touchEnd", e);
  }
  data.allowTouchCallbacks = false;
  if (!data.isTouched) {
    if (data.isMoved && params.grabCursor) {
      swiper.setGrabCursor(false);
    }
    data.isMoved = false;
    data.startMoving = false;
    return;
  }
  if (params.grabCursor && data.isMoved && data.isTouched && (swiper.allowSlideNext === true || swiper.allowSlidePrev === true)) {
    swiper.setGrabCursor(false);
  }
  var touchEndTime = now2();
  var timeDiff = touchEndTime - data.touchStartTime;
  if (swiper.allowClick) {
    swiper.updateClickedSlide(e);
    swiper.emit("tap click", e);
    if (timeDiff < 300 && touchEndTime - data.lastClickTime < 300) {
      swiper.emit("doubleTap doubleClick", e);
    }
  }
  data.lastClickTime = now2();
  nextTick2(function() {
    if (!swiper.destroyed)
      swiper.allowClick = true;
  });
  if (!data.isTouched || !data.isMoved || !swiper.swipeDirection || touches.diff === 0 || data.currentTranslate === data.startTranslate) {
    data.isTouched = false;
    data.isMoved = false;
    data.startMoving = false;
    return;
  }
  data.isTouched = false;
  data.isMoved = false;
  data.startMoving = false;
  var currentPos;
  if (params.followFinger) {
    currentPos = rtl ? swiper.translate : -swiper.translate;
  } else {
    currentPos = -data.currentTranslate;
  }
  if (params.cssMode) {
    return;
  }
  if (params.freeMode) {
    if (currentPos < -swiper.minTranslate()) {
      swiper.slideTo(swiper.activeIndex);
      return;
    }
    if (currentPos > -swiper.maxTranslate()) {
      if (swiper.slides.length < snapGrid.length) {
        swiper.slideTo(snapGrid.length - 1);
      } else {
        swiper.slideTo(swiper.slides.length - 1);
      }
      return;
    }
    if (params.freeModeMomentum) {
      if (data.velocities.length > 1) {
        var lastMoveEvent = data.velocities.pop();
        var velocityEvent = data.velocities.pop();
        var distance = lastMoveEvent.position - velocityEvent.position;
        var time = lastMoveEvent.time - velocityEvent.time;
        swiper.velocity = distance / time;
        swiper.velocity /= 2;
        if (Math.abs(swiper.velocity) < params.freeModeMinimumVelocity) {
          swiper.velocity = 0;
        }
        if (time > 150 || now2() - lastMoveEvent.time > 300) {
          swiper.velocity = 0;
        }
      } else {
        swiper.velocity = 0;
      }
      swiper.velocity *= params.freeModeMomentumVelocityRatio;
      data.velocities.length = 0;
      var momentumDuration = 1e3 * params.freeModeMomentumRatio;
      var momentumDistance = swiper.velocity * momentumDuration;
      var newPosition = swiper.translate + momentumDistance;
      if (rtl)
        newPosition = -newPosition;
      var doBounce = false;
      var afterBouncePosition;
      var bounceAmount = Math.abs(swiper.velocity) * 20 * params.freeModeMomentumBounceRatio;
      var needsLoopFix;
      if (newPosition < swiper.maxTranslate()) {
        if (params.freeModeMomentumBounce) {
          if (newPosition + swiper.maxTranslate() < -bounceAmount) {
            newPosition = swiper.maxTranslate() - bounceAmount;
          }
          afterBouncePosition = swiper.maxTranslate();
          doBounce = true;
          data.allowMomentumBounce = true;
        } else {
          newPosition = swiper.maxTranslate();
        }
        if (params.loop && params.centeredSlides)
          needsLoopFix = true;
      } else if (newPosition > swiper.minTranslate()) {
        if (params.freeModeMomentumBounce) {
          if (newPosition - swiper.minTranslate() > bounceAmount) {
            newPosition = swiper.minTranslate() + bounceAmount;
          }
          afterBouncePosition = swiper.minTranslate();
          doBounce = true;
          data.allowMomentumBounce = true;
        } else {
          newPosition = swiper.minTranslate();
        }
        if (params.loop && params.centeredSlides)
          needsLoopFix = true;
      } else if (params.freeModeSticky) {
        var nextSlide;
        for (var j = 0; j < snapGrid.length; j += 1) {
          if (snapGrid[j] > -newPosition) {
            nextSlide = j;
            break;
          }
        }
        if (Math.abs(snapGrid[nextSlide] - newPosition) < Math.abs(snapGrid[nextSlide - 1] - newPosition) || swiper.swipeDirection === "next") {
          newPosition = snapGrid[nextSlide];
        } else {
          newPosition = snapGrid[nextSlide - 1];
        }
        newPosition = -newPosition;
      }
      if (needsLoopFix) {
        swiper.once("transitionEnd", function() {
          swiper.loopFix();
        });
      }
      if (swiper.velocity !== 0) {
        if (rtl) {
          momentumDuration = Math.abs((-newPosition - swiper.translate) / swiper.velocity);
        } else {
          momentumDuration = Math.abs((newPosition - swiper.translate) / swiper.velocity);
        }
        if (params.freeModeSticky) {
          var moveDistance = Math.abs((rtl ? -newPosition : newPosition) - swiper.translate);
          var currentSlideSize = swiper.slidesSizesGrid[swiper.activeIndex];
          if (moveDistance < currentSlideSize) {
            momentumDuration = params.speed;
          } else if (moveDistance < 2 * currentSlideSize) {
            momentumDuration = params.speed * 1.5;
          } else {
            momentumDuration = params.speed * 2.5;
          }
        }
      } else if (params.freeModeSticky) {
        swiper.slideToClosest();
        return;
      }
      if (params.freeModeMomentumBounce && doBounce) {
        swiper.updateProgress(afterBouncePosition);
        swiper.setTransition(momentumDuration);
        swiper.setTranslate(newPosition);
        swiper.transitionStart(true, swiper.swipeDirection);
        swiper.animating = true;
        $wrapperEl.transitionEnd(function() {
          if (!swiper || swiper.destroyed || !data.allowMomentumBounce)
            return;
          swiper.emit("momentumBounce");
          swiper.setTransition(params.speed);
          setTimeout(function() {
            swiper.setTranslate(afterBouncePosition);
            $wrapperEl.transitionEnd(function() {
              if (!swiper || swiper.destroyed)
                return;
              swiper.transitionEnd();
            });
          }, 0);
        });
      } else if (swiper.velocity) {
        swiper.updateProgress(newPosition);
        swiper.setTransition(momentumDuration);
        swiper.setTranslate(newPosition);
        swiper.transitionStart(true, swiper.swipeDirection);
        if (!swiper.animating) {
          swiper.animating = true;
          $wrapperEl.transitionEnd(function() {
            if (!swiper || swiper.destroyed)
              return;
            swiper.transitionEnd();
          });
        }
      } else {
        swiper.emit("_freeModeNoMomentumRelease");
        swiper.updateProgress(newPosition);
      }
      swiper.updateActiveIndex();
      swiper.updateSlidesClasses();
    } else if (params.freeModeSticky) {
      swiper.slideToClosest();
      return;
    } else if (params.freeMode) {
      swiper.emit("_freeModeNoMomentumRelease");
    }
    if (!params.freeModeMomentum || timeDiff >= params.longSwipesMs) {
      swiper.updateProgress();
      swiper.updateActiveIndex();
      swiper.updateSlidesClasses();
    }
    return;
  }
  var stopIndex = 0;
  var groupSize = swiper.slidesSizesGrid[0];
  for (var i = 0; i < slidesGrid.length; i += i < params.slidesPerGroupSkip ? 1 : params.slidesPerGroup) {
    var _increment = i < params.slidesPerGroupSkip - 1 ? 1 : params.slidesPerGroup;
    if (typeof slidesGrid[i + _increment] !== "undefined") {
      if (currentPos >= slidesGrid[i] && currentPos < slidesGrid[i + _increment]) {
        stopIndex = i;
        groupSize = slidesGrid[i + _increment] - slidesGrid[i];
      }
    } else if (currentPos >= slidesGrid[i]) {
      stopIndex = i;
      groupSize = slidesGrid[slidesGrid.length - 1] - slidesGrid[slidesGrid.length - 2];
    }
  }
  var ratio = (currentPos - slidesGrid[stopIndex]) / groupSize;
  var increment = stopIndex < params.slidesPerGroupSkip - 1 ? 1 : params.slidesPerGroup;
  if (timeDiff > params.longSwipesMs) {
    if (!params.longSwipes) {
      swiper.slideTo(swiper.activeIndex);
      return;
    }
    if (swiper.swipeDirection === "next") {
      if (ratio >= params.longSwipesRatio)
        swiper.slideTo(stopIndex + increment);
      else
        swiper.slideTo(stopIndex);
    }
    if (swiper.swipeDirection === "prev") {
      if (ratio > 1 - params.longSwipesRatio)
        swiper.slideTo(stopIndex + increment);
      else
        swiper.slideTo(stopIndex);
    }
  } else {
    if (!params.shortSwipes) {
      swiper.slideTo(swiper.activeIndex);
      return;
    }
    var isNavButtonTarget = swiper.navigation && (e.target === swiper.navigation.nextEl || e.target === swiper.navigation.prevEl);
    if (!isNavButtonTarget) {
      if (swiper.swipeDirection === "next") {
        swiper.slideTo(stopIndex + increment);
      }
      if (swiper.swipeDirection === "prev") {
        swiper.slideTo(stopIndex);
      }
    } else if (e.target === swiper.navigation.nextEl) {
      swiper.slideTo(stopIndex + increment);
    } else {
      swiper.slideTo(stopIndex);
    }
  }
}

// node_modules/swiper/esm/components/core/events/onResize.js
function onResize() {
  var swiper = this;
  var params = swiper.params, el = swiper.el;
  if (el && el.offsetWidth === 0)
    return;
  if (params.breakpoints) {
    swiper.setBreakpoint();
  }
  var allowSlideNext = swiper.allowSlideNext, allowSlidePrev = swiper.allowSlidePrev, snapGrid = swiper.snapGrid;
  swiper.allowSlideNext = true;
  swiper.allowSlidePrev = true;
  swiper.updateSize();
  swiper.updateSlides();
  swiper.updateSlidesClasses();
  if ((params.slidesPerView === "auto" || params.slidesPerView > 1) && swiper.isEnd && !swiper.isBeginning && !swiper.params.centeredSlides) {
    swiper.slideTo(swiper.slides.length - 1, 0, false, true);
  } else {
    swiper.slideTo(swiper.activeIndex, 0, false, true);
  }
  if (swiper.autoplay && swiper.autoplay.running && swiper.autoplay.paused) {
    swiper.autoplay.run();
  }
  swiper.allowSlidePrev = allowSlidePrev;
  swiper.allowSlideNext = allowSlideNext;
  if (swiper.params.watchOverflow && snapGrid !== swiper.snapGrid) {
    swiper.checkOverflow();
  }
}

// node_modules/swiper/esm/components/core/events/onClick.js
function onClick(e) {
  var swiper = this;
  if (!swiper.enabled)
    return;
  if (!swiper.allowClick) {
    if (swiper.params.preventClicks)
      e.preventDefault();
    if (swiper.params.preventClicksPropagation && swiper.animating) {
      e.stopPropagation();
      e.stopImmediatePropagation();
    }
  }
}

// node_modules/swiper/esm/components/core/events/onScroll.js
function onScroll() {
  var swiper = this;
  var wrapperEl = swiper.wrapperEl, rtlTranslate = swiper.rtlTranslate, enabled = swiper.enabled;
  if (!enabled)
    return;
  swiper.previousTranslate = swiper.translate;
  if (swiper.isHorizontal()) {
    if (rtlTranslate) {
      swiper.translate = wrapperEl.scrollWidth - wrapperEl.offsetWidth - wrapperEl.scrollLeft;
    } else {
      swiper.translate = -wrapperEl.scrollLeft;
    }
  } else {
    swiper.translate = -wrapperEl.scrollTop;
  }
  if (swiper.translate === -0)
    swiper.translate = 0;
  swiper.updateActiveIndex();
  swiper.updateSlidesClasses();
  var newProgress;
  var translatesDiff = swiper.maxTranslate() - swiper.minTranslate();
  if (translatesDiff === 0) {
    newProgress = 0;
  } else {
    newProgress = (swiper.translate - swiper.minTranslate()) / translatesDiff;
  }
  if (newProgress !== swiper.progress) {
    swiper.updateProgress(rtlTranslate ? -swiper.translate : swiper.translate);
  }
  swiper.emit("setTranslate", swiper.translate, false);
}

// node_modules/swiper/esm/components/core/events/index.js
var dummyEventAttached = false;
function dummyEventListener() {
}
function attachEvents() {
  var swiper = this;
  var document = getDocument();
  var params = swiper.params, touchEvents = swiper.touchEvents, el = swiper.el, wrapperEl = swiper.wrapperEl, device2 = swiper.device, support2 = swiper.support;
  swiper.onTouchStart = onTouchStart.bind(swiper);
  swiper.onTouchMove = onTouchMove.bind(swiper);
  swiper.onTouchEnd = onTouchEnd.bind(swiper);
  if (params.cssMode) {
    swiper.onScroll = onScroll.bind(swiper);
  }
  swiper.onClick = onClick.bind(swiper);
  var capture = !!params.nested;
  if (!support2.touch && support2.pointerEvents) {
    el.addEventListener(touchEvents.start, swiper.onTouchStart, false);
    document.addEventListener(touchEvents.move, swiper.onTouchMove, capture);
    document.addEventListener(touchEvents.end, swiper.onTouchEnd, false);
  } else {
    if (support2.touch) {
      var passiveListener = touchEvents.start === "touchstart" && support2.passiveListener && params.passiveListeners ? {
        passive: true,
        capture: false
      } : false;
      el.addEventListener(touchEvents.start, swiper.onTouchStart, passiveListener);
      el.addEventListener(touchEvents.move, swiper.onTouchMove, support2.passiveListener ? {
        passive: false,
        capture
      } : capture);
      el.addEventListener(touchEvents.end, swiper.onTouchEnd, passiveListener);
      if (touchEvents.cancel) {
        el.addEventListener(touchEvents.cancel, swiper.onTouchEnd, passiveListener);
      }
      if (!dummyEventAttached) {
        document.addEventListener("touchstart", dummyEventListener);
        dummyEventAttached = true;
      }
    }
    if (params.simulateTouch && !device2.ios && !device2.android || params.simulateTouch && !support2.touch && device2.ios) {
      el.addEventListener("mousedown", swiper.onTouchStart, false);
      document.addEventListener("mousemove", swiper.onTouchMove, capture);
      document.addEventListener("mouseup", swiper.onTouchEnd, false);
    }
  }
  if (params.preventClicks || params.preventClicksPropagation) {
    el.addEventListener("click", swiper.onClick, true);
  }
  if (params.cssMode) {
    wrapperEl.addEventListener("scroll", swiper.onScroll);
  }
  if (params.updateOnWindowResize) {
    swiper.on(device2.ios || device2.android ? "resize orientationchange observerUpdate" : "resize observerUpdate", onResize, true);
  } else {
    swiper.on("observerUpdate", onResize, true);
  }
}
function detachEvents() {
  var swiper = this;
  var document = getDocument();
  var params = swiper.params, touchEvents = swiper.touchEvents, el = swiper.el, wrapperEl = swiper.wrapperEl, device2 = swiper.device, support2 = swiper.support;
  var capture = !!params.nested;
  if (!support2.touch && support2.pointerEvents) {
    el.removeEventListener(touchEvents.start, swiper.onTouchStart, false);
    document.removeEventListener(touchEvents.move, swiper.onTouchMove, capture);
    document.removeEventListener(touchEvents.end, swiper.onTouchEnd, false);
  } else {
    if (support2.touch) {
      var passiveListener = touchEvents.start === "onTouchStart" && support2.passiveListener && params.passiveListeners ? {
        passive: true,
        capture: false
      } : false;
      el.removeEventListener(touchEvents.start, swiper.onTouchStart, passiveListener);
      el.removeEventListener(touchEvents.move, swiper.onTouchMove, capture);
      el.removeEventListener(touchEvents.end, swiper.onTouchEnd, passiveListener);
      if (touchEvents.cancel) {
        el.removeEventListener(touchEvents.cancel, swiper.onTouchEnd, passiveListener);
      }
    }
    if (params.simulateTouch && !device2.ios && !device2.android || params.simulateTouch && !support2.touch && device2.ios) {
      el.removeEventListener("mousedown", swiper.onTouchStart, false);
      document.removeEventListener("mousemove", swiper.onTouchMove, capture);
      document.removeEventListener("mouseup", swiper.onTouchEnd, false);
    }
  }
  if (params.preventClicks || params.preventClicksPropagation) {
    el.removeEventListener("click", swiper.onClick, true);
  }
  if (params.cssMode) {
    wrapperEl.removeEventListener("scroll", swiper.onScroll);
  }
  swiper.off(device2.ios || device2.android ? "resize orientationchange observerUpdate" : "resize observerUpdate", onResize);
}
var events_default = {
  attachEvents,
  detachEvents
};

// node_modules/swiper/esm/components/core/breakpoints/setBreakpoint.js
function setBreakpoint() {
  var swiper = this;
  var activeIndex = swiper.activeIndex, initialized = swiper.initialized, _swiper$loopedSlides = swiper.loopedSlides, loopedSlides = _swiper$loopedSlides === void 0 ? 0 : _swiper$loopedSlides, params = swiper.params, $el = swiper.$el;
  var breakpoints = params.breakpoints;
  if (!breakpoints || breakpoints && Object.keys(breakpoints).length === 0)
    return;
  var breakpoint = swiper.getBreakpoint(breakpoints, swiper.params.breakpointsBase, swiper.el);
  if (!breakpoint || swiper.currentBreakpoint === breakpoint)
    return;
  var breakpointOnlyParams = breakpoint in breakpoints ? breakpoints[breakpoint] : void 0;
  if (breakpointOnlyParams) {
    ["slidesPerView", "spaceBetween", "slidesPerGroup", "slidesPerGroupSkip", "slidesPerColumn"].forEach(function(param) {
      var paramValue = breakpointOnlyParams[param];
      if (typeof paramValue === "undefined")
        return;
      if (param === "slidesPerView" && (paramValue === "AUTO" || paramValue === "auto")) {
        breakpointOnlyParams[param] = "auto";
      } else if (param === "slidesPerView") {
        breakpointOnlyParams[param] = parseFloat(paramValue);
      } else {
        breakpointOnlyParams[param] = parseInt(paramValue, 10);
      }
    });
  }
  var breakpointParams = breakpointOnlyParams || swiper.originalParams;
  var wasMultiRow = params.slidesPerColumn > 1;
  var isMultiRow = breakpointParams.slidesPerColumn > 1;
  var wasEnabled = params.enabled;
  if (wasMultiRow && !isMultiRow) {
    $el.removeClass(params.containerModifierClass + "multirow " + params.containerModifierClass + "multirow-column");
    swiper.emitContainerClasses();
  } else if (!wasMultiRow && isMultiRow) {
    $el.addClass(params.containerModifierClass + "multirow");
    if (breakpointParams.slidesPerColumnFill && breakpointParams.slidesPerColumnFill === "column" || !breakpointParams.slidesPerColumnFill && params.slidesPerColumnFill === "column") {
      $el.addClass(params.containerModifierClass + "multirow-column");
    }
    swiper.emitContainerClasses();
  }
  var directionChanged = breakpointParams.direction && breakpointParams.direction !== params.direction;
  var needsReLoop = params.loop && (breakpointParams.slidesPerView !== params.slidesPerView || directionChanged);
  if (directionChanged && initialized) {
    swiper.changeDirection();
  }
  extend2(swiper.params, breakpointParams);
  var isEnabled = swiper.params.enabled;
  extend2(swiper, {
    allowTouchMove: swiper.params.allowTouchMove,
    allowSlideNext: swiper.params.allowSlideNext,
    allowSlidePrev: swiper.params.allowSlidePrev
  });
  if (wasEnabled && !isEnabled) {
    swiper.disable();
  } else if (!wasEnabled && isEnabled) {
    swiper.enable();
  }
  swiper.currentBreakpoint = breakpoint;
  swiper.emit("_beforeBreakpoint", breakpointParams);
  if (needsReLoop && initialized) {
    swiper.loopDestroy();
    swiper.loopCreate();
    swiper.updateSlides();
    swiper.slideTo(activeIndex - loopedSlides + swiper.loopedSlides, 0, false);
  }
  swiper.emit("breakpoint", breakpointParams);
}

// node_modules/swiper/esm/components/core/breakpoints/getBreakpoint.js
function getBreakpoint(breakpoints, base, containerEl) {
  if (base === void 0) {
    base = "window";
  }
  if (!breakpoints || base === "container" && !containerEl)
    return void 0;
  var breakpoint = false;
  var window2 = getWindow();
  var currentHeight = base === "window" ? window2.innerHeight : containerEl.clientHeight;
  var points = Object.keys(breakpoints).map(function(point2) {
    if (typeof point2 === "string" && point2.indexOf("@") === 0) {
      var minRatio = parseFloat(point2.substr(1));
      var value2 = currentHeight * minRatio;
      return {
        value: value2,
        point: point2
      };
    }
    return {
      value: point2,
      point: point2
    };
  });
  points.sort(function(a, b) {
    return parseInt(a.value, 10) - parseInt(b.value, 10);
  });
  for (var i = 0; i < points.length; i += 1) {
    var _points$i = points[i], point = _points$i.point, value = _points$i.value;
    if (base === "window") {
      if (window2.matchMedia("(min-width: " + value + "px)").matches) {
        breakpoint = point;
      }
    } else if (value <= containerEl.clientWidth) {
      breakpoint = point;
    }
  }
  return breakpoint || "max";
}

// node_modules/swiper/esm/components/core/breakpoints/index.js
var breakpoints_default = {
  setBreakpoint,
  getBreakpoint
};

// node_modules/swiper/esm/components/core/classes/addClasses.js
function prepareClasses(entries, prefix) {
  var resultClasses = [];
  entries.forEach(function(item) {
    if (typeof item === "object") {
      Object.keys(item).forEach(function(classNames) {
        if (item[classNames]) {
          resultClasses.push(prefix + classNames);
        }
      });
    } else if (typeof item === "string") {
      resultClasses.push(prefix + item);
    }
  });
  return resultClasses;
}
function addClasses() {
  var swiper = this;
  var classNames = swiper.classNames, params = swiper.params, rtl = swiper.rtl, $el = swiper.$el, device2 = swiper.device, support2 = swiper.support;
  var suffixes = prepareClasses(["initialized", params.direction, {
    "pointer-events": support2.pointerEvents && !support2.touch
  }, {
    "free-mode": params.freeMode
  }, {
    "autoheight": params.autoHeight
  }, {
    "rtl": rtl
  }, {
    "multirow": params.slidesPerColumn > 1
  }, {
    "multirow-column": params.slidesPerColumn > 1 && params.slidesPerColumnFill === "column"
  }, {
    "android": device2.android
  }, {
    "ios": device2.ios
  }, {
    "css-mode": params.cssMode
  }], params.containerModifierClass);
  classNames.push.apply(classNames, suffixes);
  $el.addClass([].concat(classNames).join(" "));
  swiper.emitContainerClasses();
}

// node_modules/swiper/esm/components/core/classes/removeClasses.js
function removeClasses() {
  var swiper = this;
  var $el = swiper.$el, classNames = swiper.classNames;
  $el.removeClass(classNames.join(" "));
  swiper.emitContainerClasses();
}

// node_modules/swiper/esm/components/core/classes/index.js
var classes_default = {
  addClasses,
  removeClasses
};

// node_modules/swiper/esm/components/core/images/loadImage.js
function loadImage2(imageEl, src, srcset, sizes, checkForComplete, callback) {
  var window2 = getWindow();
  var image;
  function onReady() {
    if (callback)
      callback();
  }
  var isPicture = dom_default(imageEl).parent("picture")[0];
  if (!isPicture && (!imageEl.complete || !checkForComplete)) {
    if (src) {
      image = new window2.Image();
      image.onload = onReady;
      image.onerror = onReady;
      if (sizes) {
        image.sizes = sizes;
      }
      if (srcset) {
        image.srcset = srcset;
      }
      if (src) {
        image.src = src;
      }
    } else {
      onReady();
    }
  } else {
    onReady();
  }
}

// node_modules/swiper/esm/components/core/images/preloadImages.js
function preloadImages() {
  var swiper = this;
  swiper.imagesToLoad = swiper.$el.find("img");
  function onReady() {
    if (typeof swiper === "undefined" || swiper === null || !swiper || swiper.destroyed)
      return;
    if (swiper.imagesLoaded !== void 0)
      swiper.imagesLoaded += 1;
    if (swiper.imagesLoaded === swiper.imagesToLoad.length) {
      if (swiper.params.updateOnImagesReady)
        swiper.update();
      swiper.emit("imagesReady");
    }
  }
  for (var i = 0; i < swiper.imagesToLoad.length; i += 1) {
    var imageEl = swiper.imagesToLoad[i];
    swiper.loadImage(imageEl, imageEl.currentSrc || imageEl.getAttribute("src"), imageEl.srcset || imageEl.getAttribute("srcset"), imageEl.sizes || imageEl.getAttribute("sizes"), true, onReady);
  }
}

// node_modules/swiper/esm/components/core/images/index.js
var images_default = {
  loadImage: loadImage2,
  preloadImages
};

// node_modules/swiper/esm/components/core/check-overflow/index.js
function checkOverflow() {
  var swiper = this;
  var params = swiper.params;
  var wasLocked = swiper.isLocked;
  var lastSlidePosition = swiper.slides.length > 0 && params.slidesOffsetBefore + params.spaceBetween * (swiper.slides.length - 1) + swiper.slides[0].offsetWidth * swiper.slides.length;
  if (params.slidesOffsetBefore && params.slidesOffsetAfter && lastSlidePosition) {
    swiper.isLocked = lastSlidePosition <= swiper.size;
  } else {
    swiper.isLocked = swiper.snapGrid.length === 1;
  }
  swiper.allowSlideNext = !swiper.isLocked;
  swiper.allowSlidePrev = !swiper.isLocked;
  if (wasLocked !== swiper.isLocked)
    swiper.emit(swiper.isLocked ? "lock" : "unlock");
  if (wasLocked && wasLocked !== swiper.isLocked) {
    swiper.isEnd = false;
    if (swiper.navigation)
      swiper.navigation.update();
  }
}
var check_overflow_default = {
  checkOverflow
};

// node_modules/swiper/esm/components/core/defaults.js
var defaults_default = {
  init: true,
  direction: "horizontal",
  touchEventsTarget: "container",
  initialSlide: 0,
  speed: 300,
  cssMode: false,
  updateOnWindowResize: true,
  resizeObserver: false,
  nested: false,
  createElements: false,
  enabled: true,
  focusableElements: "input, select, option, textarea, button, video, label",
  width: null,
  height: null,
  preventInteractionOnTransition: false,
  userAgent: null,
  url: null,
  edgeSwipeDetection: false,
  edgeSwipeThreshold: 20,
  freeMode: false,
  freeModeMomentum: true,
  freeModeMomentumRatio: 1,
  freeModeMomentumBounce: true,
  freeModeMomentumBounceRatio: 1,
  freeModeMomentumVelocityRatio: 1,
  freeModeSticky: false,
  freeModeMinimumVelocity: 0.02,
  autoHeight: false,
  setWrapperSize: false,
  virtualTranslate: false,
  effect: "slide",
  breakpoints: void 0,
  breakpointsBase: "window",
  spaceBetween: 0,
  slidesPerView: 1,
  slidesPerColumn: 1,
  slidesPerColumnFill: "column",
  slidesPerGroup: 1,
  slidesPerGroupSkip: 0,
  centeredSlides: false,
  centeredSlidesBounds: false,
  slidesOffsetBefore: 0,
  slidesOffsetAfter: 0,
  normalizeSlideIndex: true,
  centerInsufficientSlides: false,
  watchOverflow: false,
  roundLengths: false,
  touchRatio: 1,
  touchAngle: 45,
  simulateTouch: true,
  shortSwipes: true,
  longSwipes: true,
  longSwipesRatio: 0.5,
  longSwipesMs: 300,
  followFinger: true,
  allowTouchMove: true,
  threshold: 0,
  touchMoveStopPropagation: false,
  touchStartPreventDefault: true,
  touchStartForcePreventDefault: false,
  touchReleaseOnEdges: false,
  uniqueNavElements: true,
  resistance: true,
  resistanceRatio: 0.85,
  watchSlidesProgress: false,
  watchSlidesVisibility: false,
  grabCursor: false,
  preventClicks: true,
  preventClicksPropagation: true,
  slideToClickedSlide: false,
  preloadImages: true,
  updateOnImagesReady: true,
  loop: false,
  loopAdditionalSlides: 0,
  loopedSlides: null,
  loopFillGroupWithBlank: false,
  loopPreventsSlide: true,
  allowSlidePrev: true,
  allowSlideNext: true,
  swipeHandler: null,
  noSwiping: true,
  noSwipingClass: "swiper-no-swiping",
  noSwipingSelector: null,
  passiveListeners: true,
  containerModifierClass: "swiper-container-",
  slideClass: "swiper-slide",
  slideBlankClass: "swiper-slide-invisible-blank",
  slideActiveClass: "swiper-slide-active",
  slideDuplicateActiveClass: "swiper-slide-duplicate-active",
  slideVisibleClass: "swiper-slide-visible",
  slideDuplicateClass: "swiper-slide-duplicate",
  slideNextClass: "swiper-slide-next",
  slideDuplicateNextClass: "swiper-slide-duplicate-next",
  slidePrevClass: "swiper-slide-prev",
  slideDuplicatePrevClass: "swiper-slide-duplicate-prev",
  wrapperClass: "swiper-wrapper",
  runCallbacksOnInit: true,
  _emitClasses: false
};

// node_modules/swiper/esm/components/core/core-class.js
function _defineProperties4(target, props) {
  for (var i = 0; i < props.length; i++) {
    var descriptor = props[i];
    descriptor.enumerable = descriptor.enumerable || false;
    descriptor.configurable = true;
    if ("value" in descriptor)
      descriptor.writable = true;
    Object.defineProperty(target, descriptor.key, descriptor);
  }
}
function _createClass4(Constructor, protoProps, staticProps) {
  if (protoProps)
    _defineProperties4(Constructor.prototype, protoProps);
  if (staticProps)
    _defineProperties4(Constructor, staticProps);
  return Constructor;
}
var prototypes = {
  modular: modular_default,
  eventsEmitter: events_emitter_default,
  update: update_default,
  translate: translate_default,
  transition: transition_default,
  slide: slide_default,
  loop: loop_default,
  grabCursor: grab_cursor_default,
  manipulation: manipulation_default,
  events: events_default,
  breakpoints: breakpoints_default,
  checkOverflow: check_overflow_default,
  classes: classes_default,
  images: images_default
};
var extendedDefaults = {};
var Swiper = function() {
  function Swiper2() {
    var el;
    var params;
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    if (args.length === 1 && args[0].constructor && Object.prototype.toString.call(args[0]).slice(8, -1) === "Object") {
      params = args[0];
    } else {
      el = args[0];
      params = args[1];
    }
    if (!params)
      params = {};
    params = extend2({}, params);
    if (el && !params.el)
      params.el = el;
    if (params.el && dom_default(params.el).length > 1) {
      var swipers = [];
      dom_default(params.el).each(function(containerEl) {
        var newParams = extend2({}, params, {
          el: containerEl
        });
        swipers.push(new Swiper2(newParams));
      });
      return swipers;
    }
    var swiper = this;
    swiper.__swiper__ = true;
    swiper.support = getSupport2();
    swiper.device = getDevice2({
      userAgent: params.userAgent
    });
    swiper.browser = getBrowser();
    swiper.eventsListeners = {};
    swiper.eventsAnyListeners = [];
    if (typeof swiper.modules === "undefined") {
      swiper.modules = {};
    }
    Object.keys(swiper.modules).forEach(function(moduleName) {
      var module = swiper.modules[moduleName];
      if (module.params) {
        var moduleParamName = Object.keys(module.params)[0];
        var moduleParams = module.params[moduleParamName];
        if (typeof moduleParams !== "object" || moduleParams === null)
          return;
        if (["navigation", "pagination", "scrollbar"].indexOf(moduleParamName) >= 0 && params[moduleParamName] === true) {
          params[moduleParamName] = {
            auto: true
          };
        }
        if (!(moduleParamName in params && "enabled" in moduleParams))
          return;
        if (params[moduleParamName] === true) {
          params[moduleParamName] = {
            enabled: true
          };
        }
        if (typeof params[moduleParamName] === "object" && !("enabled" in params[moduleParamName])) {
          params[moduleParamName].enabled = true;
        }
        if (!params[moduleParamName])
          params[moduleParamName] = {
            enabled: false
          };
      }
    });
    var swiperParams = extend2({}, defaults_default);
    swiper.useParams(swiperParams);
    swiper.params = extend2({}, swiperParams, extendedDefaults, params);
    swiper.originalParams = extend2({}, swiper.params);
    swiper.passedParams = extend2({}, params);
    if (swiper.params && swiper.params.on) {
      Object.keys(swiper.params.on).forEach(function(eventName) {
        swiper.on(eventName, swiper.params.on[eventName]);
      });
    }
    if (swiper.params && swiper.params.onAny) {
      swiper.onAny(swiper.params.onAny);
    }
    swiper.$ = dom_default;
    extend2(swiper, {
      enabled: swiper.params.enabled,
      el,
      classNames: [],
      slides: dom_default(),
      slidesGrid: [],
      snapGrid: [],
      slidesSizesGrid: [],
      isHorizontal: function isHorizontal() {
        return swiper.params.direction === "horizontal";
      },
      isVertical: function isVertical() {
        return swiper.params.direction === "vertical";
      },
      activeIndex: 0,
      realIndex: 0,
      isBeginning: true,
      isEnd: false,
      translate: 0,
      previousTranslate: 0,
      progress: 0,
      velocity: 0,
      animating: false,
      allowSlideNext: swiper.params.allowSlideNext,
      allowSlidePrev: swiper.params.allowSlidePrev,
      touchEvents: function touchEvents() {
        var touch = ["touchstart", "touchmove", "touchend", "touchcancel"];
        var desktop = ["mousedown", "mousemove", "mouseup"];
        if (swiper.support.pointerEvents) {
          desktop = ["pointerdown", "pointermove", "pointerup"];
        }
        swiper.touchEventsTouch = {
          start: touch[0],
          move: touch[1],
          end: touch[2],
          cancel: touch[3]
        };
        swiper.touchEventsDesktop = {
          start: desktop[0],
          move: desktop[1],
          end: desktop[2]
        };
        return swiper.support.touch || !swiper.params.simulateTouch ? swiper.touchEventsTouch : swiper.touchEventsDesktop;
      }(),
      touchEventsData: {
        isTouched: void 0,
        isMoved: void 0,
        allowTouchCallbacks: void 0,
        touchStartTime: void 0,
        isScrolling: void 0,
        currentTranslate: void 0,
        startTranslate: void 0,
        allowThresholdMove: void 0,
        focusableElements: swiper.params.focusableElements,
        lastClickTime: now2(),
        clickTimeout: void 0,
        velocities: [],
        allowMomentumBounce: void 0,
        isTouchEvent: void 0,
        startMoving: void 0
      },
      allowClick: true,
      allowTouchMove: swiper.params.allowTouchMove,
      touches: {
        startX: 0,
        startY: 0,
        currentX: 0,
        currentY: 0,
        diff: 0
      },
      imagesToLoad: [],
      imagesLoaded: 0
    });
    swiper.useModules();
    swiper.emit("_swiper");
    if (swiper.params.init) {
      swiper.init();
    }
    return swiper;
  }
  var _proto = Swiper2.prototype;
  _proto.enable = function enable7() {
    var swiper = this;
    if (swiper.enabled)
      return;
    swiper.enabled = true;
    if (swiper.params.grabCursor) {
      swiper.setGrabCursor();
    }
    swiper.emit("enable");
  };
  _proto.disable = function disable7() {
    var swiper = this;
    if (!swiper.enabled)
      return;
    swiper.enabled = false;
    if (swiper.params.grabCursor) {
      swiper.unsetGrabCursor();
    }
    swiper.emit("disable");
  };
  _proto.setProgress = function setProgress(progress, speed) {
    var swiper = this;
    progress = Math.min(Math.max(progress, 0), 1);
    var min = swiper.minTranslate();
    var max = swiper.maxTranslate();
    var current = (max - min) * progress + min;
    swiper.translateTo(current, typeof speed === "undefined" ? 0 : speed);
    swiper.updateActiveIndex();
    swiper.updateSlidesClasses();
  };
  _proto.emitContainerClasses = function emitContainerClasses() {
    var swiper = this;
    if (!swiper.params._emitClasses || !swiper.el)
      return;
    var classes = swiper.el.className.split(" ").filter(function(className) {
      return className.indexOf("swiper-container") === 0 || className.indexOf(swiper.params.containerModifierClass) === 0;
    });
    swiper.emit("_containerClasses", classes.join(" "));
  };
  _proto.getSlideClasses = function getSlideClasses(slideEl) {
    var swiper = this;
    return slideEl.className.split(" ").filter(function(className) {
      return className.indexOf("swiper-slide") === 0 || className.indexOf(swiper.params.slideClass) === 0;
    }).join(" ");
  };
  _proto.emitSlidesClasses = function emitSlidesClasses() {
    var swiper = this;
    if (!swiper.params._emitClasses || !swiper.el)
      return;
    var updates = [];
    swiper.slides.each(function(slideEl) {
      var classNames = swiper.getSlideClasses(slideEl);
      updates.push({
        slideEl,
        classNames
      });
      swiper.emit("_slideClass", slideEl, classNames);
    });
    swiper.emit("_slideClasses", updates);
  };
  _proto.slidesPerViewDynamic = function slidesPerViewDynamic() {
    var swiper = this;
    var params = swiper.params, slides = swiper.slides, slidesGrid = swiper.slidesGrid, swiperSize = swiper.size, activeIndex = swiper.activeIndex;
    var spv = 1;
    if (params.centeredSlides) {
      var slideSize = slides[activeIndex].swiperSlideSize;
      var breakLoop;
      for (var i = activeIndex + 1; i < slides.length; i += 1) {
        if (slides[i] && !breakLoop) {
          slideSize += slides[i].swiperSlideSize;
          spv += 1;
          if (slideSize > swiperSize)
            breakLoop = true;
        }
      }
      for (var _i = activeIndex - 1; _i >= 0; _i -= 1) {
        if (slides[_i] && !breakLoop) {
          slideSize += slides[_i].swiperSlideSize;
          spv += 1;
          if (slideSize > swiperSize)
            breakLoop = true;
        }
      }
    } else {
      for (var _i2 = activeIndex + 1; _i2 < slides.length; _i2 += 1) {
        if (slidesGrid[_i2] - slidesGrid[activeIndex] < swiperSize) {
          spv += 1;
        }
      }
    }
    return spv;
  };
  _proto.update = function update22() {
    var swiper = this;
    if (!swiper || swiper.destroyed)
      return;
    var snapGrid = swiper.snapGrid, params = swiper.params;
    if (params.breakpoints) {
      swiper.setBreakpoint();
    }
    swiper.updateSize();
    swiper.updateSlides();
    swiper.updateProgress();
    swiper.updateSlidesClasses();
    function setTranslate17() {
      var translateValue = swiper.rtlTranslate ? swiper.translate * -1 : swiper.translate;
      var newTranslate = Math.min(Math.max(translateValue, swiper.maxTranslate()), swiper.minTranslate());
      swiper.setTranslate(newTranslate);
      swiper.updateActiveIndex();
      swiper.updateSlidesClasses();
    }
    var translated;
    if (swiper.params.freeMode) {
      setTranslate17();
      if (swiper.params.autoHeight) {
        swiper.updateAutoHeight();
      }
    } else {
      if ((swiper.params.slidesPerView === "auto" || swiper.params.slidesPerView > 1) && swiper.isEnd && !swiper.params.centeredSlides) {
        translated = swiper.slideTo(swiper.slides.length - 1, 0, false, true);
      } else {
        translated = swiper.slideTo(swiper.activeIndex, 0, false, true);
      }
      if (!translated) {
        setTranslate17();
      }
    }
    if (params.watchOverflow && snapGrid !== swiper.snapGrid) {
      swiper.checkOverflow();
    }
    swiper.emit("update");
  };
  _proto.changeDirection = function changeDirection(newDirection, needUpdate) {
    if (needUpdate === void 0) {
      needUpdate = true;
    }
    var swiper = this;
    var currentDirection = swiper.params.direction;
    if (!newDirection) {
      newDirection = currentDirection === "horizontal" ? "vertical" : "horizontal";
    }
    if (newDirection === currentDirection || newDirection !== "horizontal" && newDirection !== "vertical") {
      return swiper;
    }
    swiper.$el.removeClass("" + swiper.params.containerModifierClass + currentDirection).addClass("" + swiper.params.containerModifierClass + newDirection);
    swiper.emitContainerClasses();
    swiper.params.direction = newDirection;
    swiper.slides.each(function(slideEl) {
      if (newDirection === "vertical") {
        slideEl.style.width = "";
      } else {
        slideEl.style.height = "";
      }
    });
    swiper.emit("changeDirection");
    if (needUpdate)
      swiper.update();
    return swiper;
  };
  _proto.mount = function mount(el) {
    var swiper = this;
    if (swiper.mounted)
      return true;
    var $el = dom_default(el || swiper.params.el);
    el = $el[0];
    if (!el) {
      return false;
    }
    el.swiper = swiper;
    var getWrapperSelector = function getWrapperSelector2() {
      return "." + (swiper.params.wrapperClass || "").trim().split(" ").join(".");
    };
    var getWrapper = function getWrapper2() {
      if (el && el.shadowRoot && el.shadowRoot.querySelector) {
        var res = dom_default(el.shadowRoot.querySelector(getWrapperSelector()));
        res.children = function(options) {
          return $el.children(options);
        };
        return res;
      }
      return $el.children(getWrapperSelector());
    };
    var $wrapperEl = getWrapper();
    if ($wrapperEl.length === 0 && swiper.params.createElements) {
      var document = getDocument();
      var wrapper = document.createElement("div");
      $wrapperEl = dom_default(wrapper);
      wrapper.className = swiper.params.wrapperClass;
      $el.append(wrapper);
      $el.children("." + swiper.params.slideClass).each(function(slideEl) {
        $wrapperEl.append(slideEl);
      });
    }
    extend2(swiper, {
      $el,
      el,
      $wrapperEl,
      wrapperEl: $wrapperEl[0],
      mounted: true,
      rtl: el.dir.toLowerCase() === "rtl" || $el.css("direction") === "rtl",
      rtlTranslate: swiper.params.direction === "horizontal" && (el.dir.toLowerCase() === "rtl" || $el.css("direction") === "rtl"),
      wrongRTL: $wrapperEl.css("display") === "-webkit-box"
    });
    return true;
  };
  _proto.init = function init46(el) {
    var swiper = this;
    if (swiper.initialized)
      return swiper;
    var mounted = swiper.mount(el);
    if (mounted === false)
      return swiper;
    swiper.emit("beforeInit");
    if (swiper.params.breakpoints) {
      swiper.setBreakpoint();
    }
    swiper.addClasses();
    if (swiper.params.loop) {
      swiper.loopCreate();
    }
    swiper.updateSize();
    swiper.updateSlides();
    if (swiper.params.watchOverflow) {
      swiper.checkOverflow();
    }
    if (swiper.params.grabCursor && swiper.enabled) {
      swiper.setGrabCursor();
    }
    if (swiper.params.preloadImages) {
      swiper.preloadImages();
    }
    if (swiper.params.loop) {
      swiper.slideTo(swiper.params.initialSlide + swiper.loopedSlides, 0, swiper.params.runCallbacksOnInit, false, true);
    } else {
      swiper.slideTo(swiper.params.initialSlide, 0, swiper.params.runCallbacksOnInit, false, true);
    }
    swiper.attachEvents();
    swiper.initialized = true;
    swiper.emit("init");
    swiper.emit("afterInit");
    return swiper;
  };
  _proto.destroy = function destroy50(deleteInstance, cleanStyles) {
    if (deleteInstance === void 0) {
      deleteInstance = true;
    }
    if (cleanStyles === void 0) {
      cleanStyles = true;
    }
    var swiper = this;
    var params = swiper.params, $el = swiper.$el, $wrapperEl = swiper.$wrapperEl, slides = swiper.slides;
    if (typeof swiper.params === "undefined" || swiper.destroyed) {
      return null;
    }
    swiper.emit("beforeDestroy");
    swiper.initialized = false;
    swiper.detachEvents();
    if (params.loop) {
      swiper.loopDestroy();
    }
    if (cleanStyles) {
      swiper.removeClasses();
      $el.removeAttr("style");
      $wrapperEl.removeAttr("style");
      if (slides && slides.length) {
        slides.removeClass([params.slideVisibleClass, params.slideActiveClass, params.slideNextClass, params.slidePrevClass].join(" ")).removeAttr("style").removeAttr("data-swiper-slide-index");
      }
    }
    swiper.emit("destroy");
    Object.keys(swiper.eventsListeners).forEach(function(eventName) {
      swiper.off(eventName);
    });
    if (deleteInstance !== false) {
      swiper.$el[0].swiper = null;
      deleteProps2(swiper);
    }
    swiper.destroyed = true;
    return null;
  };
  Swiper2.extendDefaults = function extendDefaults(newDefaults) {
    extend2(extendedDefaults, newDefaults);
  };
  Swiper2.installModule = function installModule(module) {
    if (!Swiper2.prototype.modules)
      Swiper2.prototype.modules = {};
    var name = module.name || Object.keys(Swiper2.prototype.modules).length + "_" + now2();
    Swiper2.prototype.modules[name] = module;
  };
  Swiper2.use = function use(module) {
    if (Array.isArray(module)) {
      module.forEach(function(m) {
        return Swiper2.installModule(m);
      });
      return Swiper2;
    }
    Swiper2.installModule(module);
    return Swiper2;
  };
  _createClass4(Swiper2, null, [{
    key: "extendedDefaults",
    get: function get2() {
      return extendedDefaults;
    }
  }, {
    key: "defaults",
    get: function get2() {
      return defaults_default;
    }
  }]);
  return Swiper2;
}();
Object.keys(prototypes).forEach(function(prototypeGroup) {
  Object.keys(prototypes[prototypeGroup]).forEach(function(protoMethod) {
    Swiper.prototype[protoMethod] = prototypes[prototypeGroup][protoMethod];
  });
});
Swiper.use([resize_default2, observer_default]);
var core_class_default = Swiper;

// node_modules/swiper/esm/components/virtual/virtual.js
function _extends5() {
  _extends5 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends5.apply(this, arguments);
}
var Virtual = {
  update: function update2(force) {
    var swiper = this;
    var _swiper$params = swiper.params, slidesPerView = _swiper$params.slidesPerView, slidesPerGroup = _swiper$params.slidesPerGroup, centeredSlides = _swiper$params.centeredSlides;
    var _swiper$params$virtua = swiper.params.virtual, addSlidesBefore = _swiper$params$virtua.addSlidesBefore, addSlidesAfter = _swiper$params$virtua.addSlidesAfter;
    var _swiper$virtual = swiper.virtual, previousFrom = _swiper$virtual.from, previousTo = _swiper$virtual.to, slides = _swiper$virtual.slides, previousSlidesGrid = _swiper$virtual.slidesGrid, renderSlide2 = _swiper$virtual.renderSlide, previousOffset = _swiper$virtual.offset;
    swiper.updateActiveIndex();
    var activeIndex = swiper.activeIndex || 0;
    var offsetProp;
    if (swiper.rtlTranslate)
      offsetProp = "right";
    else
      offsetProp = swiper.isHorizontal() ? "left" : "top";
    var slidesAfter;
    var slidesBefore;
    if (centeredSlides) {
      slidesAfter = Math.floor(slidesPerView / 2) + slidesPerGroup + addSlidesAfter;
      slidesBefore = Math.floor(slidesPerView / 2) + slidesPerGroup + addSlidesBefore;
    } else {
      slidesAfter = slidesPerView + (slidesPerGroup - 1) + addSlidesAfter;
      slidesBefore = slidesPerGroup + addSlidesBefore;
    }
    var from = Math.max((activeIndex || 0) - slidesBefore, 0);
    var to = Math.min((activeIndex || 0) + slidesAfter, slides.length - 1);
    var offset2 = (swiper.slidesGrid[from] || 0) - (swiper.slidesGrid[0] || 0);
    extend2(swiper.virtual, {
      from,
      to,
      offset: offset2,
      slidesGrid: swiper.slidesGrid
    });
    function onRendered() {
      swiper.updateSlides();
      swiper.updateProgress();
      swiper.updateSlidesClasses();
      if (swiper.lazy && swiper.params.lazy.enabled) {
        swiper.lazy.load();
      }
    }
    if (previousFrom === from && previousTo === to && !force) {
      if (swiper.slidesGrid !== previousSlidesGrid && offset2 !== previousOffset) {
        swiper.slides.css(offsetProp, offset2 + "px");
      }
      swiper.updateProgress();
      return;
    }
    if (swiper.params.virtual.renderExternal) {
      swiper.params.virtual.renderExternal.call(swiper, {
        offset: offset2,
        from,
        to,
        slides: function getSlides() {
          var slidesToRender = [];
          for (var i2 = from; i2 <= to; i2 += 1) {
            slidesToRender.push(slides[i2]);
          }
          return slidesToRender;
        }()
      });
      if (swiper.params.virtual.renderExternalUpdate) {
        onRendered();
      }
      return;
    }
    var prependIndexes = [];
    var appendIndexes = [];
    if (force) {
      swiper.$wrapperEl.find("." + swiper.params.slideClass).remove();
    } else {
      for (var i = previousFrom; i <= previousTo; i += 1) {
        if (i < from || i > to) {
          swiper.$wrapperEl.find("." + swiper.params.slideClass + '[data-swiper-slide-index="' + i + '"]').remove();
        }
      }
    }
    for (var _i = 0; _i < slides.length; _i += 1) {
      if (_i >= from && _i <= to) {
        if (typeof previousTo === "undefined" || force) {
          appendIndexes.push(_i);
        } else {
          if (_i > previousTo)
            appendIndexes.push(_i);
          if (_i < previousFrom)
            prependIndexes.push(_i);
        }
      }
    }
    appendIndexes.forEach(function(index2) {
      swiper.$wrapperEl.append(renderSlide2(slides[index2], index2));
    });
    prependIndexes.sort(function(a, b) {
      return b - a;
    }).forEach(function(index2) {
      swiper.$wrapperEl.prepend(renderSlide2(slides[index2], index2));
    });
    swiper.$wrapperEl.children(".swiper-slide").css(offsetProp, offset2 + "px");
    onRendered();
  },
  renderSlide: function renderSlide(slide, index2) {
    var swiper = this;
    var params = swiper.params.virtual;
    if (params.cache && swiper.virtual.cache[index2]) {
      return swiper.virtual.cache[index2];
    }
    var $slideEl = params.renderSlide ? dom_default(params.renderSlide.call(swiper, slide, index2)) : dom_default('<div class="' + swiper.params.slideClass + '" data-swiper-slide-index="' + index2 + '">' + slide + "</div>");
    if (!$slideEl.attr("data-swiper-slide-index"))
      $slideEl.attr("data-swiper-slide-index", index2);
    if (params.cache)
      swiper.virtual.cache[index2] = $slideEl;
    return $slideEl;
  },
  appendSlide: function appendSlide2(slides) {
    var swiper = this;
    if (typeof slides === "object" && "length" in slides) {
      for (var i = 0; i < slides.length; i += 1) {
        if (slides[i])
          swiper.virtual.slides.push(slides[i]);
      }
    } else {
      swiper.virtual.slides.push(slides);
    }
    swiper.virtual.update(true);
  },
  prependSlide: function prependSlide2(slides) {
    var swiper = this;
    var activeIndex = swiper.activeIndex;
    var newActiveIndex = activeIndex + 1;
    var numberOfNewSlides = 1;
    if (Array.isArray(slides)) {
      for (var i = 0; i < slides.length; i += 1) {
        if (slides[i])
          swiper.virtual.slides.unshift(slides[i]);
      }
      newActiveIndex = activeIndex + slides.length;
      numberOfNewSlides = slides.length;
    } else {
      swiper.virtual.slides.unshift(slides);
    }
    if (swiper.params.virtual.cache) {
      var cache = swiper.virtual.cache;
      var newCache = {};
      Object.keys(cache).forEach(function(cachedIndex) {
        var $cachedEl = cache[cachedIndex];
        var cachedElIndex = $cachedEl.attr("data-swiper-slide-index");
        if (cachedElIndex) {
          $cachedEl.attr("data-swiper-slide-index", parseInt(cachedElIndex, 10) + 1);
        }
        newCache[parseInt(cachedIndex, 10) + numberOfNewSlides] = $cachedEl;
      });
      swiper.virtual.cache = newCache;
    }
    swiper.virtual.update(true);
    swiper.slideTo(newActiveIndex, 0);
  },
  removeSlide: function removeSlide2(slidesIndexes) {
    var swiper = this;
    if (typeof slidesIndexes === "undefined" || slidesIndexes === null)
      return;
    var activeIndex = swiper.activeIndex;
    if (Array.isArray(slidesIndexes)) {
      for (var i = slidesIndexes.length - 1; i >= 0; i -= 1) {
        swiper.virtual.slides.splice(slidesIndexes[i], 1);
        if (swiper.params.virtual.cache) {
          delete swiper.virtual.cache[slidesIndexes[i]];
        }
        if (slidesIndexes[i] < activeIndex)
          activeIndex -= 1;
        activeIndex = Math.max(activeIndex, 0);
      }
    } else {
      swiper.virtual.slides.splice(slidesIndexes, 1);
      if (swiper.params.virtual.cache) {
        delete swiper.virtual.cache[slidesIndexes];
      }
      if (slidesIndexes < activeIndex)
        activeIndex -= 1;
      activeIndex = Math.max(activeIndex, 0);
    }
    swiper.virtual.update(true);
    swiper.slideTo(activeIndex, 0);
  },
  removeAllSlides: function removeAllSlides2() {
    var swiper = this;
    swiper.virtual.slides = [];
    if (swiper.params.virtual.cache) {
      swiper.virtual.cache = {};
    }
    swiper.virtual.update(true);
    swiper.slideTo(0, 0);
  }
};
var virtual_default = {
  name: "virtual",
  params: {
    virtual: {
      enabled: false,
      slides: [],
      cache: true,
      renderSlide: null,
      renderExternal: null,
      renderExternalUpdate: true,
      addSlidesBefore: 0,
      addSlidesAfter: 0
    }
  },
  create: function create39() {
    var swiper = this;
    bindModuleMethods(swiper, {
      virtual: _extends5({}, Virtual, {
        slides: swiper.params.virtual.slides,
        cache: {}
      })
    });
  },
  on: {
    beforeInit: function beforeInit(swiper) {
      if (!swiper.params.virtual.enabled)
        return;
      swiper.classNames.push(swiper.params.containerModifierClass + "virtual");
      var overwriteParams = {
        watchSlidesProgress: true
      };
      extend2(swiper.params, overwriteParams);
      extend2(swiper.originalParams, overwriteParams);
      if (!swiper.params.initialSlide) {
        swiper.virtual.update();
      }
    },
    setTranslate: function setTranslate2(swiper) {
      if (!swiper.params.virtual.enabled)
        return;
      swiper.virtual.update();
    }
  }
};

// node_modules/swiper/esm/components/keyboard/keyboard.js
function _extends6() {
  _extends6 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends6.apply(this, arguments);
}
var Keyboard = {
  handle: function handle2(event2) {
    var swiper = this;
    if (!swiper.enabled)
      return;
    var window2 = getWindow();
    var document = getDocument();
    var rtl = swiper.rtlTranslate;
    var e = event2;
    if (e.originalEvent)
      e = e.originalEvent;
    var kc = e.keyCode || e.charCode;
    var pageUpDown = swiper.params.keyboard.pageUpDown;
    var isPageUp = pageUpDown && kc === 33;
    var isPageDown = pageUpDown && kc === 34;
    var isArrowLeft = kc === 37;
    var isArrowRight = kc === 39;
    var isArrowUp = kc === 38;
    var isArrowDown = kc === 40;
    if (!swiper.allowSlideNext && (swiper.isHorizontal() && isArrowRight || swiper.isVertical() && isArrowDown || isPageDown)) {
      return false;
    }
    if (!swiper.allowSlidePrev && (swiper.isHorizontal() && isArrowLeft || swiper.isVertical() && isArrowUp || isPageUp)) {
      return false;
    }
    if (e.shiftKey || e.altKey || e.ctrlKey || e.metaKey) {
      return void 0;
    }
    if (document.activeElement && document.activeElement.nodeName && (document.activeElement.nodeName.toLowerCase() === "input" || document.activeElement.nodeName.toLowerCase() === "textarea")) {
      return void 0;
    }
    if (swiper.params.keyboard.onlyInViewport && (isPageUp || isPageDown || isArrowLeft || isArrowRight || isArrowUp || isArrowDown)) {
      var inView = false;
      if (swiper.$el.parents("." + swiper.params.slideClass).length > 0 && swiper.$el.parents("." + swiper.params.slideActiveClass).length === 0) {
        return void 0;
      }
      var $el = swiper.$el;
      var swiperWidth = $el[0].clientWidth;
      var swiperHeight = $el[0].clientHeight;
      var windowWidth = window2.innerWidth;
      var windowHeight = window2.innerHeight;
      var swiperOffset = swiper.$el.offset();
      if (rtl)
        swiperOffset.left -= swiper.$el[0].scrollLeft;
      var swiperCoord = [[swiperOffset.left, swiperOffset.top], [swiperOffset.left + swiperWidth, swiperOffset.top], [swiperOffset.left, swiperOffset.top + swiperHeight], [swiperOffset.left + swiperWidth, swiperOffset.top + swiperHeight]];
      for (var i = 0; i < swiperCoord.length; i += 1) {
        var point = swiperCoord[i];
        if (point[0] >= 0 && point[0] <= windowWidth && point[1] >= 0 && point[1] <= windowHeight) {
          if (point[0] === 0 && point[1] === 0)
            continue;
          inView = true;
        }
      }
      if (!inView)
        return void 0;
    }
    if (swiper.isHorizontal()) {
      if (isPageUp || isPageDown || isArrowLeft || isArrowRight) {
        if (e.preventDefault)
          e.preventDefault();
        else
          e.returnValue = false;
      }
      if ((isPageDown || isArrowRight) && !rtl || (isPageUp || isArrowLeft) && rtl)
        swiper.slideNext();
      if ((isPageUp || isArrowLeft) && !rtl || (isPageDown || isArrowRight) && rtl)
        swiper.slidePrev();
    } else {
      if (isPageUp || isPageDown || isArrowUp || isArrowDown) {
        if (e.preventDefault)
          e.preventDefault();
        else
          e.returnValue = false;
      }
      if (isPageDown || isArrowDown)
        swiper.slideNext();
      if (isPageUp || isArrowUp)
        swiper.slidePrev();
    }
    swiper.emit("keyPress", kc);
    return void 0;
  },
  enable: function enable4() {
    var swiper = this;
    var document = getDocument();
    if (swiper.keyboard.enabled)
      return;
    dom_default(document).on("keydown", swiper.keyboard.handle);
    swiper.keyboard.enabled = true;
  },
  disable: function disable4() {
    var swiper = this;
    var document = getDocument();
    if (!swiper.keyboard.enabled)
      return;
    dom_default(document).off("keydown", swiper.keyboard.handle);
    swiper.keyboard.enabled = false;
  }
};
var keyboard_default = {
  name: "keyboard",
  params: {
    keyboard: {
      enabled: false,
      onlyInViewport: true,
      pageUpDown: true
    }
  },
  create: function create40() {
    var swiper = this;
    bindModuleMethods(swiper, {
      keyboard: _extends6({
        enabled: false
      }, Keyboard)
    });
  },
  on: {
    init: function init16(swiper) {
      if (swiper.params.keyboard.enabled) {
        swiper.keyboard.enable();
      }
    },
    destroy: function destroy17(swiper) {
      if (swiper.keyboard.enabled) {
        swiper.keyboard.disable();
      }
    }
  }
};

// node_modules/swiper/esm/components/mousewheel/mousewheel.js
function isEventSupported() {
  var document = getDocument();
  var eventName = "onwheel";
  var isSupported = eventName in document;
  if (!isSupported) {
    var element = document.createElement("div");
    element.setAttribute(eventName, "return;");
    isSupported = typeof element[eventName] === "function";
  }
  if (!isSupported && document.implementation && document.implementation.hasFeature && document.implementation.hasFeature("", "") !== true) {
    isSupported = document.implementation.hasFeature("Events.wheel", "3.0");
  }
  return isSupported;
}
var Mousewheel = {
  lastScrollTime: now2(),
  lastEventBeforeSnap: void 0,
  recentWheelEvents: [],
  event: function event() {
    var window2 = getWindow();
    if (window2.navigator.userAgent.indexOf("firefox") > -1)
      return "DOMMouseScroll";
    return isEventSupported() ? "wheel" : "mousewheel";
  },
  normalize: function normalize(e) {
    var PIXEL_STEP = 10;
    var LINE_HEIGHT = 40;
    var PAGE_HEIGHT = 800;
    var sX = 0;
    var sY = 0;
    var pX = 0;
    var pY = 0;
    if ("detail" in e) {
      sY = e.detail;
    }
    if ("wheelDelta" in e) {
      sY = -e.wheelDelta / 120;
    }
    if ("wheelDeltaY" in e) {
      sY = -e.wheelDeltaY / 120;
    }
    if ("wheelDeltaX" in e) {
      sX = -e.wheelDeltaX / 120;
    }
    if ("axis" in e && e.axis === e.HORIZONTAL_AXIS) {
      sX = sY;
      sY = 0;
    }
    pX = sX * PIXEL_STEP;
    pY = sY * PIXEL_STEP;
    if ("deltaY" in e) {
      pY = e.deltaY;
    }
    if ("deltaX" in e) {
      pX = e.deltaX;
    }
    if (e.shiftKey && !pX) {
      pX = pY;
      pY = 0;
    }
    if ((pX || pY) && e.deltaMode) {
      if (e.deltaMode === 1) {
        pX *= LINE_HEIGHT;
        pY *= LINE_HEIGHT;
      } else {
        pX *= PAGE_HEIGHT;
        pY *= PAGE_HEIGHT;
      }
    }
    if (pX && !sX) {
      sX = pX < 1 ? -1 : 1;
    }
    if (pY && !sY) {
      sY = pY < 1 ? -1 : 1;
    }
    return {
      spinX: sX,
      spinY: sY,
      pixelX: pX,
      pixelY: pY
    };
  },
  handleMouseEnter: function handleMouseEnter() {
    var swiper = this;
    if (!swiper.enabled)
      return;
    swiper.mouseEntered = true;
  },
  handleMouseLeave: function handleMouseLeave() {
    var swiper = this;
    if (!swiper.enabled)
      return;
    swiper.mouseEntered = false;
  },
  handle: function handle3(event2) {
    var e = event2;
    var disableParentSwiper = true;
    var swiper = this;
    if (!swiper.enabled)
      return;
    var params = swiper.params.mousewheel;
    if (swiper.params.cssMode) {
      e.preventDefault();
    }
    var target = swiper.$el;
    if (swiper.params.mousewheel.eventsTarget !== "container") {
      target = dom_default(swiper.params.mousewheel.eventsTarget);
    }
    if (!swiper.mouseEntered && !target[0].contains(e.target) && !params.releaseOnEdges)
      return true;
    if (e.originalEvent)
      e = e.originalEvent;
    var delta = 0;
    var rtlFactor = swiper.rtlTranslate ? -1 : 1;
    var data = Mousewheel.normalize(e);
    if (params.forceToAxis) {
      if (swiper.isHorizontal()) {
        if (Math.abs(data.pixelX) > Math.abs(data.pixelY))
          delta = -data.pixelX * rtlFactor;
        else
          return true;
      } else if (Math.abs(data.pixelY) > Math.abs(data.pixelX))
        delta = -data.pixelY;
      else
        return true;
    } else {
      delta = Math.abs(data.pixelX) > Math.abs(data.pixelY) ? -data.pixelX * rtlFactor : -data.pixelY;
    }
    if (delta === 0)
      return true;
    if (params.invert)
      delta = -delta;
    var positions = swiper.getTranslate() + delta * params.sensitivity;
    if (positions >= swiper.minTranslate())
      positions = swiper.minTranslate();
    if (positions <= swiper.maxTranslate())
      positions = swiper.maxTranslate();
    disableParentSwiper = swiper.params.loop ? true : !(positions === swiper.minTranslate() || positions === swiper.maxTranslate());
    if (disableParentSwiper && swiper.params.nested)
      e.stopPropagation();
    if (!swiper.params.freeMode) {
      var newEvent = {
        time: now2(),
        delta: Math.abs(delta),
        direction: Math.sign(delta),
        raw: event2
      };
      var recentWheelEvents = swiper.mousewheel.recentWheelEvents;
      if (recentWheelEvents.length >= 2) {
        recentWheelEvents.shift();
      }
      var prevEvent = recentWheelEvents.length ? recentWheelEvents[recentWheelEvents.length - 1] : void 0;
      recentWheelEvents.push(newEvent);
      if (prevEvent) {
        if (newEvent.direction !== prevEvent.direction || newEvent.delta > prevEvent.delta || newEvent.time > prevEvent.time + 150) {
          swiper.mousewheel.animateSlider(newEvent);
        }
      } else {
        swiper.mousewheel.animateSlider(newEvent);
      }
      if (swiper.mousewheel.releaseScroll(newEvent)) {
        return true;
      }
    } else {
      var _newEvent = {
        time: now2(),
        delta: Math.abs(delta),
        direction: Math.sign(delta)
      };
      var lastEventBeforeSnap = swiper.mousewheel.lastEventBeforeSnap;
      var ignoreWheelEvents = lastEventBeforeSnap && _newEvent.time < lastEventBeforeSnap.time + 500 && _newEvent.delta <= lastEventBeforeSnap.delta && _newEvent.direction === lastEventBeforeSnap.direction;
      if (!ignoreWheelEvents) {
        swiper.mousewheel.lastEventBeforeSnap = void 0;
        if (swiper.params.loop) {
          swiper.loopFix();
        }
        var position = swiper.getTranslate() + delta * params.sensitivity;
        var wasBeginning = swiper.isBeginning;
        var wasEnd = swiper.isEnd;
        if (position >= swiper.minTranslate())
          position = swiper.minTranslate();
        if (position <= swiper.maxTranslate())
          position = swiper.maxTranslate();
        swiper.setTransition(0);
        swiper.setTranslate(position);
        swiper.updateProgress();
        swiper.updateActiveIndex();
        swiper.updateSlidesClasses();
        if (!wasBeginning && swiper.isBeginning || !wasEnd && swiper.isEnd) {
          swiper.updateSlidesClasses();
        }
        if (swiper.params.freeModeSticky) {
          clearTimeout(swiper.mousewheel.timeout);
          swiper.mousewheel.timeout = void 0;
          var _recentWheelEvents = swiper.mousewheel.recentWheelEvents;
          if (_recentWheelEvents.length >= 15) {
            _recentWheelEvents.shift();
          }
          var _prevEvent = _recentWheelEvents.length ? _recentWheelEvents[_recentWheelEvents.length - 1] : void 0;
          var firstEvent = _recentWheelEvents[0];
          _recentWheelEvents.push(_newEvent);
          if (_prevEvent && (_newEvent.delta > _prevEvent.delta || _newEvent.direction !== _prevEvent.direction)) {
            _recentWheelEvents.splice(0);
          } else if (_recentWheelEvents.length >= 15 && _newEvent.time - firstEvent.time < 500 && firstEvent.delta - _newEvent.delta >= 1 && _newEvent.delta <= 6) {
            var snapToThreshold = delta > 0 ? 0.8 : 0.2;
            swiper.mousewheel.lastEventBeforeSnap = _newEvent;
            _recentWheelEvents.splice(0);
            swiper.mousewheel.timeout = nextTick2(function() {
              swiper.slideToClosest(swiper.params.speed, true, void 0, snapToThreshold);
            }, 0);
          }
          if (!swiper.mousewheel.timeout) {
            swiper.mousewheel.timeout = nextTick2(function() {
              var snapToThreshold2 = 0.5;
              swiper.mousewheel.lastEventBeforeSnap = _newEvent;
              _recentWheelEvents.splice(0);
              swiper.slideToClosest(swiper.params.speed, true, void 0, snapToThreshold2);
            }, 500);
          }
        }
        if (!ignoreWheelEvents)
          swiper.emit("scroll", e);
        if (swiper.params.autoplay && swiper.params.autoplayDisableOnInteraction)
          swiper.autoplay.stop();
        if (position === swiper.minTranslate() || position === swiper.maxTranslate())
          return true;
      }
    }
    if (e.preventDefault)
      e.preventDefault();
    else
      e.returnValue = false;
    return false;
  },
  animateSlider: function animateSlider(newEvent) {
    var swiper = this;
    var window2 = getWindow();
    if (this.params.mousewheel.thresholdDelta && newEvent.delta < this.params.mousewheel.thresholdDelta) {
      return false;
    }
    if (this.params.mousewheel.thresholdTime && now2() - swiper.mousewheel.lastScrollTime < this.params.mousewheel.thresholdTime) {
      return false;
    }
    if (newEvent.delta >= 6 && now2() - swiper.mousewheel.lastScrollTime < 60) {
      return true;
    }
    if (newEvent.direction < 0) {
      if ((!swiper.isEnd || swiper.params.loop) && !swiper.animating) {
        swiper.slideNext();
        swiper.emit("scroll", newEvent.raw);
      }
    } else if ((!swiper.isBeginning || swiper.params.loop) && !swiper.animating) {
      swiper.slidePrev();
      swiper.emit("scroll", newEvent.raw);
    }
    swiper.mousewheel.lastScrollTime = new window2.Date().getTime();
    return false;
  },
  releaseScroll: function releaseScroll(newEvent) {
    var swiper = this;
    var params = swiper.params.mousewheel;
    if (newEvent.direction < 0) {
      if (swiper.isEnd && !swiper.params.loop && params.releaseOnEdges) {
        return true;
      }
    } else if (swiper.isBeginning && !swiper.params.loop && params.releaseOnEdges) {
      return true;
    }
    return false;
  },
  enable: function enable5() {
    var swiper = this;
    var event2 = Mousewheel.event();
    if (swiper.params.cssMode) {
      swiper.wrapperEl.removeEventListener(event2, swiper.mousewheel.handle);
      return true;
    }
    if (!event2)
      return false;
    if (swiper.mousewheel.enabled)
      return false;
    var target = swiper.$el;
    if (swiper.params.mousewheel.eventsTarget !== "container") {
      target = dom_default(swiper.params.mousewheel.eventsTarget);
    }
    target.on("mouseenter", swiper.mousewheel.handleMouseEnter);
    target.on("mouseleave", swiper.mousewheel.handleMouseLeave);
    target.on(event2, swiper.mousewheel.handle);
    swiper.mousewheel.enabled = true;
    return true;
  },
  disable: function disable5() {
    var swiper = this;
    var event2 = Mousewheel.event();
    if (swiper.params.cssMode) {
      swiper.wrapperEl.addEventListener(event2, swiper.mousewheel.handle);
      return true;
    }
    if (!event2)
      return false;
    if (!swiper.mousewheel.enabled)
      return false;
    var target = swiper.$el;
    if (swiper.params.mousewheel.eventsTarget !== "container") {
      target = dom_default(swiper.params.mousewheel.eventsTarget);
    }
    target.off(event2, swiper.mousewheel.handle);
    swiper.mousewheel.enabled = false;
    return true;
  }
};
var mousewheel_default = {
  name: "mousewheel",
  params: {
    mousewheel: {
      enabled: false,
      releaseOnEdges: false,
      invert: false,
      forceToAxis: false,
      sensitivity: 1,
      eventsTarget: "container",
      thresholdDelta: null,
      thresholdTime: null
    }
  },
  create: function create41() {
    var swiper = this;
    bindModuleMethods(swiper, {
      mousewheel: {
        enabled: false,
        lastScrollTime: now2(),
        lastEventBeforeSnap: void 0,
        recentWheelEvents: [],
        enable: Mousewheel.enable,
        disable: Mousewheel.disable,
        handle: Mousewheel.handle,
        handleMouseEnter: Mousewheel.handleMouseEnter,
        handleMouseLeave: Mousewheel.handleMouseLeave,
        animateSlider: Mousewheel.animateSlider,
        releaseScroll: Mousewheel.releaseScroll
      }
    });
  },
  on: {
    init: function init17(swiper) {
      if (!swiper.params.mousewheel.enabled && swiper.params.cssMode) {
        swiper.mousewheel.disable();
      }
      if (swiper.params.mousewheel.enabled)
        swiper.mousewheel.enable();
    },
    destroy: function destroy18(swiper) {
      if (swiper.params.cssMode) {
        swiper.mousewheel.enable();
      }
      if (swiper.mousewheel.enabled)
        swiper.mousewheel.disable();
    }
  }
};

// node_modules/swiper/esm/components/navigation/navigation.js
function _extends7() {
  _extends7 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends7.apply(this, arguments);
}
var Navigation = {
  toggleEl: function toggleEl($el, disabled) {
    $el[disabled ? "addClass" : "removeClass"](this.params.navigation.disabledClass);
    if ($el[0] && $el[0].tagName === "BUTTON")
      $el[0].disabled = disabled;
  },
  update: function update3() {
    var swiper = this;
    var params = swiper.params.navigation;
    var toggleEl2 = swiper.navigation.toggleEl;
    if (swiper.params.loop)
      return;
    var _swiper$navigation = swiper.navigation, $nextEl = _swiper$navigation.$nextEl, $prevEl = _swiper$navigation.$prevEl;
    if ($prevEl && $prevEl.length > 0) {
      if (swiper.isBeginning) {
        toggleEl2($prevEl, true);
      } else {
        toggleEl2($prevEl, false);
      }
      if (swiper.params.watchOverflow && swiper.enabled) {
        $prevEl[swiper.isLocked ? "addClass" : "removeClass"](params.lockClass);
      }
    }
    if ($nextEl && $nextEl.length > 0) {
      if (swiper.isEnd) {
        toggleEl2($nextEl, true);
      } else {
        toggleEl2($nextEl, false);
      }
      if (swiper.params.watchOverflow && swiper.enabled) {
        $nextEl[swiper.isLocked ? "addClass" : "removeClass"](params.lockClass);
      }
    }
  },
  onPrevClick: function onPrevClick(e) {
    var swiper = this;
    e.preventDefault();
    if (swiper.isBeginning && !swiper.params.loop)
      return;
    swiper.slidePrev();
  },
  onNextClick: function onNextClick(e) {
    var swiper = this;
    e.preventDefault();
    if (swiper.isEnd && !swiper.params.loop)
      return;
    swiper.slideNext();
  },
  init: function init18() {
    var swiper = this;
    var params = swiper.params.navigation;
    swiper.params.navigation = createElementIfNotDefined(swiper.$el, swiper.params.navigation, swiper.params.createElements, {
      nextEl: "swiper-button-next",
      prevEl: "swiper-button-prev"
    });
    if (!(params.nextEl || params.prevEl))
      return;
    var $nextEl;
    var $prevEl;
    if (params.nextEl) {
      $nextEl = dom_default(params.nextEl);
      if (swiper.params.uniqueNavElements && typeof params.nextEl === "string" && $nextEl.length > 1 && swiper.$el.find(params.nextEl).length === 1) {
        $nextEl = swiper.$el.find(params.nextEl);
      }
    }
    if (params.prevEl) {
      $prevEl = dom_default(params.prevEl);
      if (swiper.params.uniqueNavElements && typeof params.prevEl === "string" && $prevEl.length > 1 && swiper.$el.find(params.prevEl).length === 1) {
        $prevEl = swiper.$el.find(params.prevEl);
      }
    }
    if ($nextEl && $nextEl.length > 0) {
      $nextEl.on("click", swiper.navigation.onNextClick);
    }
    if ($prevEl && $prevEl.length > 0) {
      $prevEl.on("click", swiper.navigation.onPrevClick);
    }
    extend2(swiper.navigation, {
      $nextEl,
      nextEl: $nextEl && $nextEl[0],
      $prevEl,
      prevEl: $prevEl && $prevEl[0]
    });
    if (!swiper.enabled) {
      if ($nextEl)
        $nextEl.addClass(params.lockClass);
      if ($prevEl)
        $prevEl.addClass(params.lockClass);
    }
  },
  destroy: function destroy19() {
    var swiper = this;
    var _swiper$navigation2 = swiper.navigation, $nextEl = _swiper$navigation2.$nextEl, $prevEl = _swiper$navigation2.$prevEl;
    if ($nextEl && $nextEl.length) {
      $nextEl.off("click", swiper.navigation.onNextClick);
      $nextEl.removeClass(swiper.params.navigation.disabledClass);
    }
    if ($prevEl && $prevEl.length) {
      $prevEl.off("click", swiper.navigation.onPrevClick);
      $prevEl.removeClass(swiper.params.navigation.disabledClass);
    }
  }
};
var navigation_default = {
  name: "navigation",
  params: {
    navigation: {
      nextEl: null,
      prevEl: null,
      hideOnClick: false,
      disabledClass: "swiper-button-disabled",
      hiddenClass: "swiper-button-hidden",
      lockClass: "swiper-button-lock"
    }
  },
  create: function create42() {
    var swiper = this;
    bindModuleMethods(swiper, {
      navigation: _extends7({}, Navigation)
    });
  },
  on: {
    init: function init19(swiper) {
      swiper.navigation.init();
      swiper.navigation.update();
    },
    toEdge: function toEdge(swiper) {
      swiper.navigation.update();
    },
    fromEdge: function fromEdge(swiper) {
      swiper.navigation.update();
    },
    destroy: function destroy20(swiper) {
      swiper.navigation.destroy();
    },
    "enable disable": function enableDisable(swiper) {
      var _swiper$navigation3 = swiper.navigation, $nextEl = _swiper$navigation3.$nextEl, $prevEl = _swiper$navigation3.$prevEl;
      if ($nextEl) {
        $nextEl[swiper.enabled ? "removeClass" : "addClass"](swiper.params.navigation.lockClass);
      }
      if ($prevEl) {
        $prevEl[swiper.enabled ? "removeClass" : "addClass"](swiper.params.navigation.lockClass);
      }
    },
    click: function click(swiper, e) {
      var _swiper$navigation4 = swiper.navigation, $nextEl = _swiper$navigation4.$nextEl, $prevEl = _swiper$navigation4.$prevEl;
      var targetEl = e.target;
      if (swiper.params.navigation.hideOnClick && !dom_default(targetEl).is($prevEl) && !dom_default(targetEl).is($nextEl)) {
        if (swiper.pagination && swiper.params.pagination && swiper.params.pagination.clickable && (swiper.pagination.el === targetEl || swiper.pagination.el.contains(targetEl)))
          return;
        var isHidden;
        if ($nextEl) {
          isHidden = $nextEl.hasClass(swiper.params.navigation.hiddenClass);
        } else if ($prevEl) {
          isHidden = $prevEl.hasClass(swiper.params.navigation.hiddenClass);
        }
        if (isHidden === true) {
          swiper.emit("navigationShow");
        } else {
          swiper.emit("navigationHide");
        }
        if ($nextEl) {
          $nextEl.toggleClass(swiper.params.navigation.hiddenClass);
        }
        if ($prevEl) {
          $prevEl.toggleClass(swiper.params.navigation.hiddenClass);
        }
      }
    }
  }
};

// node_modules/swiper/esm/components/pagination/pagination.js
function _extends8() {
  _extends8 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends8.apply(this, arguments);
}
var Pagination = {
  update: function update4() {
    var swiper = this;
    var rtl = swiper.rtl;
    var params = swiper.params.pagination;
    if (!params.el || !swiper.pagination.el || !swiper.pagination.$el || swiper.pagination.$el.length === 0)
      return;
    var slidesLength = swiper.virtual && swiper.params.virtual.enabled ? swiper.virtual.slides.length : swiper.slides.length;
    var $el = swiper.pagination.$el;
    var current;
    var total = swiper.params.loop ? Math.ceil((slidesLength - swiper.loopedSlides * 2) / swiper.params.slidesPerGroup) : swiper.snapGrid.length;
    if (swiper.params.loop) {
      current = Math.ceil((swiper.activeIndex - swiper.loopedSlides) / swiper.params.slidesPerGroup);
      if (current > slidesLength - 1 - swiper.loopedSlides * 2) {
        current -= slidesLength - swiper.loopedSlides * 2;
      }
      if (current > total - 1)
        current -= total;
      if (current < 0 && swiper.params.paginationType !== "bullets")
        current = total + current;
    } else if (typeof swiper.snapIndex !== "undefined") {
      current = swiper.snapIndex;
    } else {
      current = swiper.activeIndex || 0;
    }
    if (params.type === "bullets" && swiper.pagination.bullets && swiper.pagination.bullets.length > 0) {
      var bullets = swiper.pagination.bullets;
      var firstIndex;
      var lastIndex;
      var midIndex;
      if (params.dynamicBullets) {
        swiper.pagination.bulletSize = bullets.eq(0)[swiper.isHorizontal() ? "outerWidth" : "outerHeight"](true);
        $el.css(swiper.isHorizontal() ? "width" : "height", swiper.pagination.bulletSize * (params.dynamicMainBullets + 4) + "px");
        if (params.dynamicMainBullets > 1 && swiper.previousIndex !== void 0) {
          swiper.pagination.dynamicBulletIndex += current - swiper.previousIndex;
          if (swiper.pagination.dynamicBulletIndex > params.dynamicMainBullets - 1) {
            swiper.pagination.dynamicBulletIndex = params.dynamicMainBullets - 1;
          } else if (swiper.pagination.dynamicBulletIndex < 0) {
            swiper.pagination.dynamicBulletIndex = 0;
          }
        }
        firstIndex = current - swiper.pagination.dynamicBulletIndex;
        lastIndex = firstIndex + (Math.min(bullets.length, params.dynamicMainBullets) - 1);
        midIndex = (lastIndex + firstIndex) / 2;
      }
      bullets.removeClass(params.bulletActiveClass + " " + params.bulletActiveClass + "-next " + params.bulletActiveClass + "-next-next " + params.bulletActiveClass + "-prev " + params.bulletActiveClass + "-prev-prev " + params.bulletActiveClass + "-main");
      if ($el.length > 1) {
        bullets.each(function(bullet) {
          var $bullet2 = dom_default(bullet);
          var bulletIndex2 = $bullet2.index();
          if (bulletIndex2 === current) {
            $bullet2.addClass(params.bulletActiveClass);
          }
          if (params.dynamicBullets) {
            if (bulletIndex2 >= firstIndex && bulletIndex2 <= lastIndex) {
              $bullet2.addClass(params.bulletActiveClass + "-main");
            }
            if (bulletIndex2 === firstIndex) {
              $bullet2.prev().addClass(params.bulletActiveClass + "-prev").prev().addClass(params.bulletActiveClass + "-prev-prev");
            }
            if (bulletIndex2 === lastIndex) {
              $bullet2.next().addClass(params.bulletActiveClass + "-next").next().addClass(params.bulletActiveClass + "-next-next");
            }
          }
        });
      } else {
        var $bullet = bullets.eq(current);
        var bulletIndex = $bullet.index();
        $bullet.addClass(params.bulletActiveClass);
        if (params.dynamicBullets) {
          var $firstDisplayedBullet = bullets.eq(firstIndex);
          var $lastDisplayedBullet = bullets.eq(lastIndex);
          for (var i = firstIndex; i <= lastIndex; i += 1) {
            bullets.eq(i).addClass(params.bulletActiveClass + "-main");
          }
          if (swiper.params.loop) {
            if (bulletIndex >= bullets.length - params.dynamicMainBullets) {
              for (var _i = params.dynamicMainBullets; _i >= 0; _i -= 1) {
                bullets.eq(bullets.length - _i).addClass(params.bulletActiveClass + "-main");
              }
              bullets.eq(bullets.length - params.dynamicMainBullets - 1).addClass(params.bulletActiveClass + "-prev");
            } else {
              $firstDisplayedBullet.prev().addClass(params.bulletActiveClass + "-prev").prev().addClass(params.bulletActiveClass + "-prev-prev");
              $lastDisplayedBullet.next().addClass(params.bulletActiveClass + "-next").next().addClass(params.bulletActiveClass + "-next-next");
            }
          } else {
            $firstDisplayedBullet.prev().addClass(params.bulletActiveClass + "-prev").prev().addClass(params.bulletActiveClass + "-prev-prev");
            $lastDisplayedBullet.next().addClass(params.bulletActiveClass + "-next").next().addClass(params.bulletActiveClass + "-next-next");
          }
        }
      }
      if (params.dynamicBullets) {
        var dynamicBulletsLength = Math.min(bullets.length, params.dynamicMainBullets + 4);
        var bulletsOffset = (swiper.pagination.bulletSize * dynamicBulletsLength - swiper.pagination.bulletSize) / 2 - midIndex * swiper.pagination.bulletSize;
        var offsetProp = rtl ? "right" : "left";
        bullets.css(swiper.isHorizontal() ? offsetProp : "top", bulletsOffset + "px");
      }
    }
    if (params.type === "fraction") {
      $el.find(classesToSelector(params.currentClass)).text(params.formatFractionCurrent(current + 1));
      $el.find(classesToSelector(params.totalClass)).text(params.formatFractionTotal(total));
    }
    if (params.type === "progressbar") {
      var progressbarDirection;
      if (params.progressbarOpposite) {
        progressbarDirection = swiper.isHorizontal() ? "vertical" : "horizontal";
      } else {
        progressbarDirection = swiper.isHorizontal() ? "horizontal" : "vertical";
      }
      var scale = (current + 1) / total;
      var scaleX = 1;
      var scaleY = 1;
      if (progressbarDirection === "horizontal") {
        scaleX = scale;
      } else {
        scaleY = scale;
      }
      $el.find(classesToSelector(params.progressbarFillClass)).transform("translate3d(0,0,0) scaleX(" + scaleX + ") scaleY(" + scaleY + ")").transition(swiper.params.speed);
    }
    if (params.type === "custom" && params.renderCustom) {
      $el.html(params.renderCustom(swiper, current + 1, total));
      swiper.emit("paginationRender", $el[0]);
    } else {
      swiper.emit("paginationUpdate", $el[0]);
    }
    if (swiper.params.watchOverflow && swiper.enabled) {
      $el[swiper.isLocked ? "addClass" : "removeClass"](params.lockClass);
    }
  },
  render: function render() {
    var swiper = this;
    var params = swiper.params.pagination;
    if (!params.el || !swiper.pagination.el || !swiper.pagination.$el || swiper.pagination.$el.length === 0)
      return;
    var slidesLength = swiper.virtual && swiper.params.virtual.enabled ? swiper.virtual.slides.length : swiper.slides.length;
    var $el = swiper.pagination.$el;
    var paginationHTML = "";
    if (params.type === "bullets") {
      var numberOfBullets = swiper.params.loop ? Math.ceil((slidesLength - swiper.loopedSlides * 2) / swiper.params.slidesPerGroup) : swiper.snapGrid.length;
      if (swiper.params.freeMode && !swiper.params.loop && numberOfBullets > slidesLength) {
        numberOfBullets = slidesLength;
      }
      for (var i = 0; i < numberOfBullets; i += 1) {
        if (params.renderBullet) {
          paginationHTML += params.renderBullet.call(swiper, i, params.bulletClass);
        } else {
          paginationHTML += "<" + params.bulletElement + ' class="' + params.bulletClass + '"></' + params.bulletElement + ">";
        }
      }
      $el.html(paginationHTML);
      swiper.pagination.bullets = $el.find(classesToSelector(params.bulletClass));
    }
    if (params.type === "fraction") {
      if (params.renderFraction) {
        paginationHTML = params.renderFraction.call(swiper, params.currentClass, params.totalClass);
      } else {
        paginationHTML = '<span class="' + params.currentClass + '"></span> / ' + ('<span class="' + params.totalClass + '"></span>');
      }
      $el.html(paginationHTML);
    }
    if (params.type === "progressbar") {
      if (params.renderProgressbar) {
        paginationHTML = params.renderProgressbar.call(swiper, params.progressbarFillClass);
      } else {
        paginationHTML = '<span class="' + params.progressbarFillClass + '"></span>';
      }
      $el.html(paginationHTML);
    }
    if (params.type !== "custom") {
      swiper.emit("paginationRender", swiper.pagination.$el[0]);
    }
  },
  init: function init20() {
    var swiper = this;
    swiper.params.pagination = createElementIfNotDefined(swiper.$el, swiper.params.pagination, swiper.params.createElements, {
      el: "swiper-pagination"
    });
    var params = swiper.params.pagination;
    if (!params.el)
      return;
    var $el = dom_default(params.el);
    if ($el.length === 0)
      return;
    if (swiper.params.uniqueNavElements && typeof params.el === "string" && $el.length > 1) {
      $el = swiper.$el.find(params.el);
    }
    if (params.type === "bullets" && params.clickable) {
      $el.addClass(params.clickableClass);
    }
    $el.addClass(params.modifierClass + params.type);
    if (params.type === "bullets" && params.dynamicBullets) {
      $el.addClass("" + params.modifierClass + params.type + "-dynamic");
      swiper.pagination.dynamicBulletIndex = 0;
      if (params.dynamicMainBullets < 1) {
        params.dynamicMainBullets = 1;
      }
    }
    if (params.type === "progressbar" && params.progressbarOpposite) {
      $el.addClass(params.progressbarOppositeClass);
    }
    if (params.clickable) {
      $el.on("click", classesToSelector(params.bulletClass), function onClick4(e) {
        e.preventDefault();
        var index2 = dom_default(this).index() * swiper.params.slidesPerGroup;
        if (swiper.params.loop)
          index2 += swiper.loopedSlides;
        swiper.slideTo(index2);
      });
    }
    extend2(swiper.pagination, {
      $el,
      el: $el[0]
    });
    if (!swiper.enabled) {
      $el.addClass(params.lockClass);
    }
  },
  destroy: function destroy21() {
    var swiper = this;
    var params = swiper.params.pagination;
    if (!params.el || !swiper.pagination.el || !swiper.pagination.$el || swiper.pagination.$el.length === 0)
      return;
    var $el = swiper.pagination.$el;
    $el.removeClass(params.hiddenClass);
    $el.removeClass(params.modifierClass + params.type);
    if (swiper.pagination.bullets)
      swiper.pagination.bullets.removeClass(params.bulletActiveClass);
    if (params.clickable) {
      $el.off("click", classesToSelector(params.bulletClass));
    }
  }
};
var pagination_default = {
  name: "pagination",
  params: {
    pagination: {
      el: null,
      bulletElement: "span",
      clickable: false,
      hideOnClick: false,
      renderBullet: null,
      renderProgressbar: null,
      renderFraction: null,
      renderCustom: null,
      progressbarOpposite: false,
      type: "bullets",
      dynamicBullets: false,
      dynamicMainBullets: 1,
      formatFractionCurrent: function formatFractionCurrent(number) {
        return number;
      },
      formatFractionTotal: function formatFractionTotal(number) {
        return number;
      },
      bulletClass: "swiper-pagination-bullet",
      bulletActiveClass: "swiper-pagination-bullet-active",
      modifierClass: "swiper-pagination-",
      currentClass: "swiper-pagination-current",
      totalClass: "swiper-pagination-total",
      hiddenClass: "swiper-pagination-hidden",
      progressbarFillClass: "swiper-pagination-progressbar-fill",
      progressbarOppositeClass: "swiper-pagination-progressbar-opposite",
      clickableClass: "swiper-pagination-clickable",
      lockClass: "swiper-pagination-lock"
    }
  },
  create: function create43() {
    var swiper = this;
    bindModuleMethods(swiper, {
      pagination: _extends8({
        dynamicBulletIndex: 0
      }, Pagination)
    });
  },
  on: {
    init: function init21(swiper) {
      swiper.pagination.init();
      swiper.pagination.render();
      swiper.pagination.update();
    },
    activeIndexChange: function activeIndexChange(swiper) {
      if (swiper.params.loop) {
        swiper.pagination.update();
      } else if (typeof swiper.snapIndex === "undefined") {
        swiper.pagination.update();
      }
    },
    snapIndexChange: function snapIndexChange(swiper) {
      if (!swiper.params.loop) {
        swiper.pagination.update();
      }
    },
    slidesLengthChange: function slidesLengthChange(swiper) {
      if (swiper.params.loop) {
        swiper.pagination.render();
        swiper.pagination.update();
      }
    },
    snapGridLengthChange: function snapGridLengthChange(swiper) {
      if (!swiper.params.loop) {
        swiper.pagination.render();
        swiper.pagination.update();
      }
    },
    destroy: function destroy22(swiper) {
      swiper.pagination.destroy();
    },
    "enable disable": function enableDisable2(swiper) {
      var $el = swiper.pagination.$el;
      if ($el) {
        $el[swiper.enabled ? "removeClass" : "addClass"](swiper.params.pagination.lockClass);
      }
    },
    click: function click2(swiper, e) {
      var targetEl = e.target;
      if (swiper.params.pagination.el && swiper.params.pagination.hideOnClick && swiper.pagination.$el.length > 0 && !dom_default(targetEl).hasClass(swiper.params.pagination.bulletClass)) {
        if (swiper.navigation && (swiper.navigation.nextEl && targetEl === swiper.navigation.nextEl || swiper.navigation.prevEl && targetEl === swiper.navigation.prevEl))
          return;
        var isHidden = swiper.pagination.$el.hasClass(swiper.params.pagination.hiddenClass);
        if (isHidden === true) {
          swiper.emit("paginationShow");
        } else {
          swiper.emit("paginationHide");
        }
        swiper.pagination.$el.toggleClass(swiper.params.pagination.hiddenClass);
      }
    }
  }
};

// node_modules/swiper/esm/components/scrollbar/scrollbar.js
function _extends9() {
  _extends9 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends9.apply(this, arguments);
}
var Scrollbar = {
  setTranslate: function setTranslate3() {
    var swiper = this;
    if (!swiper.params.scrollbar.el || !swiper.scrollbar.el)
      return;
    var scrollbar = swiper.scrollbar, rtl = swiper.rtlTranslate, progress = swiper.progress;
    var dragSize = scrollbar.dragSize, trackSize = scrollbar.trackSize, $dragEl = scrollbar.$dragEl, $el = scrollbar.$el;
    var params = swiper.params.scrollbar;
    var newSize = dragSize;
    var newPos = (trackSize - dragSize) * progress;
    if (rtl) {
      newPos = -newPos;
      if (newPos > 0) {
        newSize = dragSize - newPos;
        newPos = 0;
      } else if (-newPos + dragSize > trackSize) {
        newSize = trackSize + newPos;
      }
    } else if (newPos < 0) {
      newSize = dragSize + newPos;
      newPos = 0;
    } else if (newPos + dragSize > trackSize) {
      newSize = trackSize - newPos;
    }
    if (swiper.isHorizontal()) {
      $dragEl.transform("translate3d(" + newPos + "px, 0, 0)");
      $dragEl[0].style.width = newSize + "px";
    } else {
      $dragEl.transform("translate3d(0px, " + newPos + "px, 0)");
      $dragEl[0].style.height = newSize + "px";
    }
    if (params.hide) {
      clearTimeout(swiper.scrollbar.timeout);
      $el[0].style.opacity = 1;
      swiper.scrollbar.timeout = setTimeout(function() {
        $el[0].style.opacity = 0;
        $el.transition(400);
      }, 1e3);
    }
  },
  setTransition: function setTransition2(duration) {
    var swiper = this;
    if (!swiper.params.scrollbar.el || !swiper.scrollbar.el)
      return;
    swiper.scrollbar.$dragEl.transition(duration);
  },
  updateSize: function updateSize2() {
    var swiper = this;
    if (!swiper.params.scrollbar.el || !swiper.scrollbar.el)
      return;
    var scrollbar = swiper.scrollbar;
    var $dragEl = scrollbar.$dragEl, $el = scrollbar.$el;
    $dragEl[0].style.width = "";
    $dragEl[0].style.height = "";
    var trackSize = swiper.isHorizontal() ? $el[0].offsetWidth : $el[0].offsetHeight;
    var divider = swiper.size / swiper.virtualSize;
    var moveDivider = divider * (trackSize / swiper.size);
    var dragSize;
    if (swiper.params.scrollbar.dragSize === "auto") {
      dragSize = trackSize * divider;
    } else {
      dragSize = parseInt(swiper.params.scrollbar.dragSize, 10);
    }
    if (swiper.isHorizontal()) {
      $dragEl[0].style.width = dragSize + "px";
    } else {
      $dragEl[0].style.height = dragSize + "px";
    }
    if (divider >= 1) {
      $el[0].style.display = "none";
    } else {
      $el[0].style.display = "";
    }
    if (swiper.params.scrollbar.hide) {
      $el[0].style.opacity = 0;
    }
    extend2(scrollbar, {
      trackSize,
      divider,
      moveDivider,
      dragSize
    });
    if (swiper.params.watchOverflow && swiper.enabled) {
      scrollbar.$el[swiper.isLocked ? "addClass" : "removeClass"](swiper.params.scrollbar.lockClass);
    }
  },
  getPointerPosition: function getPointerPosition(e) {
    var swiper = this;
    if (swiper.isHorizontal()) {
      return e.type === "touchstart" || e.type === "touchmove" ? e.targetTouches[0].clientX : e.clientX;
    }
    return e.type === "touchstart" || e.type === "touchmove" ? e.targetTouches[0].clientY : e.clientY;
  },
  setDragPosition: function setDragPosition(e) {
    var swiper = this;
    var scrollbar = swiper.scrollbar, rtl = swiper.rtlTranslate;
    var $el = scrollbar.$el, dragSize = scrollbar.dragSize, trackSize = scrollbar.trackSize, dragStartPos = scrollbar.dragStartPos;
    var positionRatio;
    positionRatio = (scrollbar.getPointerPosition(e) - $el.offset()[swiper.isHorizontal() ? "left" : "top"] - (dragStartPos !== null ? dragStartPos : dragSize / 2)) / (trackSize - dragSize);
    positionRatio = Math.max(Math.min(positionRatio, 1), 0);
    if (rtl) {
      positionRatio = 1 - positionRatio;
    }
    var position = swiper.minTranslate() + (swiper.maxTranslate() - swiper.minTranslate()) * positionRatio;
    swiper.updateProgress(position);
    swiper.setTranslate(position);
    swiper.updateActiveIndex();
    swiper.updateSlidesClasses();
  },
  onDragStart: function onDragStart(e) {
    var swiper = this;
    var params = swiper.params.scrollbar;
    var scrollbar = swiper.scrollbar, $wrapperEl = swiper.$wrapperEl;
    var $el = scrollbar.$el, $dragEl = scrollbar.$dragEl;
    swiper.scrollbar.isTouched = true;
    swiper.scrollbar.dragStartPos = e.target === $dragEl[0] || e.target === $dragEl ? scrollbar.getPointerPosition(e) - e.target.getBoundingClientRect()[swiper.isHorizontal() ? "left" : "top"] : null;
    e.preventDefault();
    e.stopPropagation();
    $wrapperEl.transition(100);
    $dragEl.transition(100);
    scrollbar.setDragPosition(e);
    clearTimeout(swiper.scrollbar.dragTimeout);
    $el.transition(0);
    if (params.hide) {
      $el.css("opacity", 1);
    }
    if (swiper.params.cssMode) {
      swiper.$wrapperEl.css("scroll-snap-type", "none");
    }
    swiper.emit("scrollbarDragStart", e);
  },
  onDragMove: function onDragMove(e) {
    var swiper = this;
    var scrollbar = swiper.scrollbar, $wrapperEl = swiper.$wrapperEl;
    var $el = scrollbar.$el, $dragEl = scrollbar.$dragEl;
    if (!swiper.scrollbar.isTouched)
      return;
    if (e.preventDefault)
      e.preventDefault();
    else
      e.returnValue = false;
    scrollbar.setDragPosition(e);
    $wrapperEl.transition(0);
    $el.transition(0);
    $dragEl.transition(0);
    swiper.emit("scrollbarDragMove", e);
  },
  onDragEnd: function onDragEnd(e) {
    var swiper = this;
    var params = swiper.params.scrollbar;
    var scrollbar = swiper.scrollbar, $wrapperEl = swiper.$wrapperEl;
    var $el = scrollbar.$el;
    if (!swiper.scrollbar.isTouched)
      return;
    swiper.scrollbar.isTouched = false;
    if (swiper.params.cssMode) {
      swiper.$wrapperEl.css("scroll-snap-type", "");
      $wrapperEl.transition("");
    }
    if (params.hide) {
      clearTimeout(swiper.scrollbar.dragTimeout);
      swiper.scrollbar.dragTimeout = nextTick2(function() {
        $el.css("opacity", 0);
        $el.transition(400);
      }, 1e3);
    }
    swiper.emit("scrollbarDragEnd", e);
    if (params.snapOnRelease) {
      swiper.slideToClosest();
    }
  },
  enableDraggable: function enableDraggable() {
    var swiper = this;
    if (!swiper.params.scrollbar.el)
      return;
    var document = getDocument();
    var scrollbar = swiper.scrollbar, touchEventsTouch = swiper.touchEventsTouch, touchEventsDesktop = swiper.touchEventsDesktop, params = swiper.params, support2 = swiper.support;
    var $el = scrollbar.$el;
    var target = $el[0];
    var activeListener = support2.passiveListener && params.passiveListeners ? {
      passive: false,
      capture: false
    } : false;
    var passiveListener = support2.passiveListener && params.passiveListeners ? {
      passive: true,
      capture: false
    } : false;
    if (!target)
      return;
    if (!support2.touch) {
      target.addEventListener(touchEventsDesktop.start, swiper.scrollbar.onDragStart, activeListener);
      document.addEventListener(touchEventsDesktop.move, swiper.scrollbar.onDragMove, activeListener);
      document.addEventListener(touchEventsDesktop.end, swiper.scrollbar.onDragEnd, passiveListener);
    } else {
      target.addEventListener(touchEventsTouch.start, swiper.scrollbar.onDragStart, activeListener);
      target.addEventListener(touchEventsTouch.move, swiper.scrollbar.onDragMove, activeListener);
      target.addEventListener(touchEventsTouch.end, swiper.scrollbar.onDragEnd, passiveListener);
    }
  },
  disableDraggable: function disableDraggable() {
    var swiper = this;
    if (!swiper.params.scrollbar.el)
      return;
    var document = getDocument();
    var scrollbar = swiper.scrollbar, touchEventsTouch = swiper.touchEventsTouch, touchEventsDesktop = swiper.touchEventsDesktop, params = swiper.params, support2 = swiper.support;
    var $el = scrollbar.$el;
    var target = $el[0];
    var activeListener = support2.passiveListener && params.passiveListeners ? {
      passive: false,
      capture: false
    } : false;
    var passiveListener = support2.passiveListener && params.passiveListeners ? {
      passive: true,
      capture: false
    } : false;
    if (!target)
      return;
    if (!support2.touch) {
      target.removeEventListener(touchEventsDesktop.start, swiper.scrollbar.onDragStart, activeListener);
      document.removeEventListener(touchEventsDesktop.move, swiper.scrollbar.onDragMove, activeListener);
      document.removeEventListener(touchEventsDesktop.end, swiper.scrollbar.onDragEnd, passiveListener);
    } else {
      target.removeEventListener(touchEventsTouch.start, swiper.scrollbar.onDragStart, activeListener);
      target.removeEventListener(touchEventsTouch.move, swiper.scrollbar.onDragMove, activeListener);
      target.removeEventListener(touchEventsTouch.end, swiper.scrollbar.onDragEnd, passiveListener);
    }
  },
  init: function init22() {
    var swiper = this;
    var scrollbar = swiper.scrollbar, $swiperEl = swiper.$el;
    swiper.params.scrollbar = createElementIfNotDefined($swiperEl, swiper.params.scrollbar, swiper.params.createElements, {
      el: "swiper-scrollbar"
    });
    var params = swiper.params.scrollbar;
    if (!params.el)
      return;
    var $el = dom_default(params.el);
    if (swiper.params.uniqueNavElements && typeof params.el === "string" && $el.length > 1 && $swiperEl.find(params.el).length === 1) {
      $el = $swiperEl.find(params.el);
    }
    var $dragEl = $el.find("." + swiper.params.scrollbar.dragClass);
    if ($dragEl.length === 0) {
      $dragEl = dom_default('<div class="' + swiper.params.scrollbar.dragClass + '"></div>');
      $el.append($dragEl);
    }
    extend2(scrollbar, {
      $el,
      el: $el[0],
      $dragEl,
      dragEl: $dragEl[0]
    });
    if (params.draggable) {
      scrollbar.enableDraggable();
    }
    if ($el) {
      $el[swiper.enabled ? "removeClass" : "addClass"](swiper.params.scrollbar.lockClass);
    }
  },
  destroy: function destroy23() {
    var swiper = this;
    swiper.scrollbar.disableDraggable();
  }
};
var scrollbar_default = {
  name: "scrollbar",
  params: {
    scrollbar: {
      el: null,
      dragSize: "auto",
      hide: false,
      draggable: false,
      snapOnRelease: true,
      lockClass: "swiper-scrollbar-lock",
      dragClass: "swiper-scrollbar-drag"
    }
  },
  create: function create44() {
    var swiper = this;
    bindModuleMethods(swiper, {
      scrollbar: _extends9({
        isTouched: false,
        timeout: null,
        dragTimeout: null
      }, Scrollbar)
    });
  },
  on: {
    init: function init23(swiper) {
      swiper.scrollbar.init();
      swiper.scrollbar.updateSize();
      swiper.scrollbar.setTranslate();
    },
    update: function update5(swiper) {
      swiper.scrollbar.updateSize();
    },
    resize: function resize(swiper) {
      swiper.scrollbar.updateSize();
    },
    observerUpdate: function observerUpdate(swiper) {
      swiper.scrollbar.updateSize();
    },
    setTranslate: function setTranslate4(swiper) {
      swiper.scrollbar.setTranslate();
    },
    setTransition: function setTransition3(swiper, duration) {
      swiper.scrollbar.setTransition(duration);
    },
    "enable disable": function enableDisable3(swiper) {
      var $el = swiper.scrollbar.$el;
      if ($el) {
        $el[swiper.enabled ? "removeClass" : "addClass"](swiper.params.scrollbar.lockClass);
      }
    },
    destroy: function destroy24(swiper) {
      swiper.scrollbar.destroy();
    }
  }
};

// node_modules/swiper/esm/components/parallax/parallax.js
function _extends10() {
  _extends10 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends10.apply(this, arguments);
}
var Parallax = {
  setTransform: function setTransform(el, progress) {
    var swiper = this;
    var rtl = swiper.rtl;
    var $el = dom_default(el);
    var rtlFactor = rtl ? -1 : 1;
    var p = $el.attr("data-swiper-parallax") || "0";
    var x = $el.attr("data-swiper-parallax-x");
    var y = $el.attr("data-swiper-parallax-y");
    var scale = $el.attr("data-swiper-parallax-scale");
    var opacity = $el.attr("data-swiper-parallax-opacity");
    if (x || y) {
      x = x || "0";
      y = y || "0";
    } else if (swiper.isHorizontal()) {
      x = p;
      y = "0";
    } else {
      y = p;
      x = "0";
    }
    if (x.indexOf("%") >= 0) {
      x = parseInt(x, 10) * progress * rtlFactor + "%";
    } else {
      x = x * progress * rtlFactor + "px";
    }
    if (y.indexOf("%") >= 0) {
      y = parseInt(y, 10) * progress + "%";
    } else {
      y = y * progress + "px";
    }
    if (typeof opacity !== "undefined" && opacity !== null) {
      var currentOpacity = opacity - (opacity - 1) * (1 - Math.abs(progress));
      $el[0].style.opacity = currentOpacity;
    }
    if (typeof scale === "undefined" || scale === null) {
      $el.transform("translate3d(" + x + ", " + y + ", 0px)");
    } else {
      var currentScale = scale - (scale - 1) * (1 - Math.abs(progress));
      $el.transform("translate3d(" + x + ", " + y + ", 0px) scale(" + currentScale + ")");
    }
  },
  setTranslate: function setTranslate5() {
    var swiper = this;
    var $el = swiper.$el, slides = swiper.slides, progress = swiper.progress, snapGrid = swiper.snapGrid;
    $el.children("[data-swiper-parallax], [data-swiper-parallax-x], [data-swiper-parallax-y], [data-swiper-parallax-opacity], [data-swiper-parallax-scale]").each(function(el) {
      swiper.parallax.setTransform(el, progress);
    });
    slides.each(function(slideEl, slideIndex) {
      var slideProgress = slideEl.progress;
      if (swiper.params.slidesPerGroup > 1 && swiper.params.slidesPerView !== "auto") {
        slideProgress += Math.ceil(slideIndex / 2) - progress * (snapGrid.length - 1);
      }
      slideProgress = Math.min(Math.max(slideProgress, -1), 1);
      dom_default(slideEl).find("[data-swiper-parallax], [data-swiper-parallax-x], [data-swiper-parallax-y], [data-swiper-parallax-opacity], [data-swiper-parallax-scale]").each(function(el) {
        swiper.parallax.setTransform(el, slideProgress);
      });
    });
  },
  setTransition: function setTransition4(duration) {
    if (duration === void 0) {
      duration = this.params.speed;
    }
    var swiper = this;
    var $el = swiper.$el;
    $el.find("[data-swiper-parallax], [data-swiper-parallax-x], [data-swiper-parallax-y], [data-swiper-parallax-opacity], [data-swiper-parallax-scale]").each(function(parallaxEl) {
      var $parallaxEl = dom_default(parallaxEl);
      var parallaxDuration = parseInt($parallaxEl.attr("data-swiper-parallax-duration"), 10) || duration;
      if (duration === 0)
        parallaxDuration = 0;
      $parallaxEl.transition(parallaxDuration);
    });
  }
};
var parallax_default = {
  name: "parallax",
  params: {
    parallax: {
      enabled: false
    }
  },
  create: function create45() {
    var swiper = this;
    bindModuleMethods(swiper, {
      parallax: _extends10({}, Parallax)
    });
  },
  on: {
    beforeInit: function beforeInit2(swiper) {
      if (!swiper.params.parallax.enabled)
        return;
      swiper.params.watchSlidesProgress = true;
      swiper.originalParams.watchSlidesProgress = true;
    },
    init: function init24(swiper) {
      if (!swiper.params.parallax.enabled)
        return;
      swiper.parallax.setTranslate();
    },
    setTranslate: function setTranslate6(swiper) {
      if (!swiper.params.parallax.enabled)
        return;
      swiper.parallax.setTranslate();
    },
    setTransition: function setTransition5(swiper, duration) {
      if (!swiper.params.parallax.enabled)
        return;
      swiper.parallax.setTransition(duration);
    }
  }
};

// node_modules/swiper/esm/components/zoom/zoom.js
function _extends11() {
  _extends11 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends11.apply(this, arguments);
}
var Zoom = {
  getDistanceBetweenTouches: function getDistanceBetweenTouches(e) {
    if (e.targetTouches.length < 2)
      return 1;
    var x1 = e.targetTouches[0].pageX;
    var y1 = e.targetTouches[0].pageY;
    var x2 = e.targetTouches[1].pageX;
    var y2 = e.targetTouches[1].pageY;
    var distance = Math.sqrt(Math.pow(x2 - x1, 2) + Math.pow(y2 - y1, 2));
    return distance;
  },
  onGestureStart: function onGestureStart(e) {
    var swiper = this;
    var support2 = swiper.support;
    var params = swiper.params.zoom;
    var zoom = swiper.zoom;
    var gesture = zoom.gesture;
    zoom.fakeGestureTouched = false;
    zoom.fakeGestureMoved = false;
    if (!support2.gestures) {
      if (e.type !== "touchstart" || e.type === "touchstart" && e.targetTouches.length < 2) {
        return;
      }
      zoom.fakeGestureTouched = true;
      gesture.scaleStart = Zoom.getDistanceBetweenTouches(e);
    }
    if (!gesture.$slideEl || !gesture.$slideEl.length) {
      gesture.$slideEl = dom_default(e.target).closest("." + swiper.params.slideClass);
      if (gesture.$slideEl.length === 0)
        gesture.$slideEl = swiper.slides.eq(swiper.activeIndex);
      gesture.$imageEl = gesture.$slideEl.find("img, svg, canvas, picture, .swiper-zoom-target");
      gesture.$imageWrapEl = gesture.$imageEl.parent("." + params.containerClass);
      gesture.maxRatio = gesture.$imageWrapEl.attr("data-swiper-zoom") || params.maxRatio;
      if (gesture.$imageWrapEl.length === 0) {
        gesture.$imageEl = void 0;
        return;
      }
    }
    if (gesture.$imageEl) {
      gesture.$imageEl.transition(0);
    }
    swiper.zoom.isScaling = true;
  },
  onGestureChange: function onGestureChange(e) {
    var swiper = this;
    var support2 = swiper.support;
    var params = swiper.params.zoom;
    var zoom = swiper.zoom;
    var gesture = zoom.gesture;
    if (!support2.gestures) {
      if (e.type !== "touchmove" || e.type === "touchmove" && e.targetTouches.length < 2) {
        return;
      }
      zoom.fakeGestureMoved = true;
      gesture.scaleMove = Zoom.getDistanceBetweenTouches(e);
    }
    if (!gesture.$imageEl || gesture.$imageEl.length === 0) {
      if (e.type === "gesturechange")
        zoom.onGestureStart(e);
      return;
    }
    if (support2.gestures) {
      zoom.scale = e.scale * zoom.currentScale;
    } else {
      zoom.scale = gesture.scaleMove / gesture.scaleStart * zoom.currentScale;
    }
    if (zoom.scale > gesture.maxRatio) {
      zoom.scale = gesture.maxRatio - 1 + Math.pow(zoom.scale - gesture.maxRatio + 1, 0.5);
    }
    if (zoom.scale < params.minRatio) {
      zoom.scale = params.minRatio + 1 - Math.pow(params.minRatio - zoom.scale + 1, 0.5);
    }
    gesture.$imageEl.transform("translate3d(0,0,0) scale(" + zoom.scale + ")");
  },
  onGestureEnd: function onGestureEnd(e) {
    var swiper = this;
    var device2 = swiper.device;
    var support2 = swiper.support;
    var params = swiper.params.zoom;
    var zoom = swiper.zoom;
    var gesture = zoom.gesture;
    if (!support2.gestures) {
      if (!zoom.fakeGestureTouched || !zoom.fakeGestureMoved) {
        return;
      }
      if (e.type !== "touchend" || e.type === "touchend" && e.changedTouches.length < 2 && !device2.android) {
        return;
      }
      zoom.fakeGestureTouched = false;
      zoom.fakeGestureMoved = false;
    }
    if (!gesture.$imageEl || gesture.$imageEl.length === 0)
      return;
    zoom.scale = Math.max(Math.min(zoom.scale, gesture.maxRatio), params.minRatio);
    gesture.$imageEl.transition(swiper.params.speed).transform("translate3d(0,0,0) scale(" + zoom.scale + ")");
    zoom.currentScale = zoom.scale;
    zoom.isScaling = false;
    if (zoom.scale === 1)
      gesture.$slideEl = void 0;
  },
  onTouchStart: function onTouchStart2(e) {
    var swiper = this;
    var device2 = swiper.device;
    var zoom = swiper.zoom;
    var gesture = zoom.gesture, image = zoom.image;
    if (!gesture.$imageEl || gesture.$imageEl.length === 0)
      return;
    if (image.isTouched)
      return;
    if (device2.android && e.cancelable)
      e.preventDefault();
    image.isTouched = true;
    image.touchesStart.x = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
    image.touchesStart.y = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
  },
  onTouchMove: function onTouchMove2(e) {
    var swiper = this;
    var zoom = swiper.zoom;
    var gesture = zoom.gesture, image = zoom.image, velocity = zoom.velocity;
    if (!gesture.$imageEl || gesture.$imageEl.length === 0)
      return;
    swiper.allowClick = false;
    if (!image.isTouched || !gesture.$slideEl)
      return;
    if (!image.isMoved) {
      image.width = gesture.$imageEl[0].offsetWidth;
      image.height = gesture.$imageEl[0].offsetHeight;
      image.startX = getTranslate2(gesture.$imageWrapEl[0], "x") || 0;
      image.startY = getTranslate2(gesture.$imageWrapEl[0], "y") || 0;
      gesture.slideWidth = gesture.$slideEl[0].offsetWidth;
      gesture.slideHeight = gesture.$slideEl[0].offsetHeight;
      gesture.$imageWrapEl.transition(0);
    }
    var scaledWidth = image.width * zoom.scale;
    var scaledHeight = image.height * zoom.scale;
    if (scaledWidth < gesture.slideWidth && scaledHeight < gesture.slideHeight)
      return;
    image.minX = Math.min(gesture.slideWidth / 2 - scaledWidth / 2, 0);
    image.maxX = -image.minX;
    image.minY = Math.min(gesture.slideHeight / 2 - scaledHeight / 2, 0);
    image.maxY = -image.minY;
    image.touchesCurrent.x = e.type === "touchmove" ? e.targetTouches[0].pageX : e.pageX;
    image.touchesCurrent.y = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
    if (!image.isMoved && !zoom.isScaling) {
      if (swiper.isHorizontal() && (Math.floor(image.minX) === Math.floor(image.startX) && image.touchesCurrent.x < image.touchesStart.x || Math.floor(image.maxX) === Math.floor(image.startX) && image.touchesCurrent.x > image.touchesStart.x)) {
        image.isTouched = false;
        return;
      }
      if (!swiper.isHorizontal() && (Math.floor(image.minY) === Math.floor(image.startY) && image.touchesCurrent.y < image.touchesStart.y || Math.floor(image.maxY) === Math.floor(image.startY) && image.touchesCurrent.y > image.touchesStart.y)) {
        image.isTouched = false;
        return;
      }
    }
    if (e.cancelable) {
      e.preventDefault();
    }
    e.stopPropagation();
    image.isMoved = true;
    image.currentX = image.touchesCurrent.x - image.touchesStart.x + image.startX;
    image.currentY = image.touchesCurrent.y - image.touchesStart.y + image.startY;
    if (image.currentX < image.minX) {
      image.currentX = image.minX + 1 - Math.pow(image.minX - image.currentX + 1, 0.8);
    }
    if (image.currentX > image.maxX) {
      image.currentX = image.maxX - 1 + Math.pow(image.currentX - image.maxX + 1, 0.8);
    }
    if (image.currentY < image.minY) {
      image.currentY = image.minY + 1 - Math.pow(image.minY - image.currentY + 1, 0.8);
    }
    if (image.currentY > image.maxY) {
      image.currentY = image.maxY - 1 + Math.pow(image.currentY - image.maxY + 1, 0.8);
    }
    if (!velocity.prevPositionX)
      velocity.prevPositionX = image.touchesCurrent.x;
    if (!velocity.prevPositionY)
      velocity.prevPositionY = image.touchesCurrent.y;
    if (!velocity.prevTime)
      velocity.prevTime = Date.now();
    velocity.x = (image.touchesCurrent.x - velocity.prevPositionX) / (Date.now() - velocity.prevTime) / 2;
    velocity.y = (image.touchesCurrent.y - velocity.prevPositionY) / (Date.now() - velocity.prevTime) / 2;
    if (Math.abs(image.touchesCurrent.x - velocity.prevPositionX) < 2)
      velocity.x = 0;
    if (Math.abs(image.touchesCurrent.y - velocity.prevPositionY) < 2)
      velocity.y = 0;
    velocity.prevPositionX = image.touchesCurrent.x;
    velocity.prevPositionY = image.touchesCurrent.y;
    velocity.prevTime = Date.now();
    gesture.$imageWrapEl.transform("translate3d(" + image.currentX + "px, " + image.currentY + "px,0)");
  },
  onTouchEnd: function onTouchEnd2() {
    var swiper = this;
    var zoom = swiper.zoom;
    var gesture = zoom.gesture, image = zoom.image, velocity = zoom.velocity;
    if (!gesture.$imageEl || gesture.$imageEl.length === 0)
      return;
    if (!image.isTouched || !image.isMoved) {
      image.isTouched = false;
      image.isMoved = false;
      return;
    }
    image.isTouched = false;
    image.isMoved = false;
    var momentumDurationX = 300;
    var momentumDurationY = 300;
    var momentumDistanceX = velocity.x * momentumDurationX;
    var newPositionX = image.currentX + momentumDistanceX;
    var momentumDistanceY = velocity.y * momentumDurationY;
    var newPositionY = image.currentY + momentumDistanceY;
    if (velocity.x !== 0)
      momentumDurationX = Math.abs((newPositionX - image.currentX) / velocity.x);
    if (velocity.y !== 0)
      momentumDurationY = Math.abs((newPositionY - image.currentY) / velocity.y);
    var momentumDuration = Math.max(momentumDurationX, momentumDurationY);
    image.currentX = newPositionX;
    image.currentY = newPositionY;
    var scaledWidth = image.width * zoom.scale;
    var scaledHeight = image.height * zoom.scale;
    image.minX = Math.min(gesture.slideWidth / 2 - scaledWidth / 2, 0);
    image.maxX = -image.minX;
    image.minY = Math.min(gesture.slideHeight / 2 - scaledHeight / 2, 0);
    image.maxY = -image.minY;
    image.currentX = Math.max(Math.min(image.currentX, image.maxX), image.minX);
    image.currentY = Math.max(Math.min(image.currentY, image.maxY), image.minY);
    gesture.$imageWrapEl.transition(momentumDuration).transform("translate3d(" + image.currentX + "px, " + image.currentY + "px,0)");
  },
  onTransitionEnd: function onTransitionEnd() {
    var swiper = this;
    var zoom = swiper.zoom;
    var gesture = zoom.gesture;
    if (gesture.$slideEl && swiper.previousIndex !== swiper.activeIndex) {
      if (gesture.$imageEl) {
        gesture.$imageEl.transform("translate3d(0,0,0) scale(1)");
      }
      if (gesture.$imageWrapEl) {
        gesture.$imageWrapEl.transform("translate3d(0,0,0)");
      }
      zoom.scale = 1;
      zoom.currentScale = 1;
      gesture.$slideEl = void 0;
      gesture.$imageEl = void 0;
      gesture.$imageWrapEl = void 0;
    }
  },
  toggle: function toggle7(e) {
    var swiper = this;
    var zoom = swiper.zoom;
    if (zoom.scale && zoom.scale !== 1) {
      zoom.out();
    } else {
      zoom.in(e);
    }
  },
  in: function _in(e) {
    var swiper = this;
    var window2 = getWindow();
    var zoom = swiper.zoom;
    var params = swiper.params.zoom;
    var gesture = zoom.gesture, image = zoom.image;
    if (!gesture.$slideEl) {
      if (e && e.target) {
        gesture.$slideEl = dom_default(e.target).closest("." + swiper.params.slideClass);
      }
      if (!gesture.$slideEl) {
        if (swiper.params.virtual && swiper.params.virtual.enabled && swiper.virtual) {
          gesture.$slideEl = swiper.$wrapperEl.children("." + swiper.params.slideActiveClass);
        } else {
          gesture.$slideEl = swiper.slides.eq(swiper.activeIndex);
        }
      }
      gesture.$imageEl = gesture.$slideEl.find("img, svg, canvas, picture, .swiper-zoom-target");
      gesture.$imageWrapEl = gesture.$imageEl.parent("." + params.containerClass);
    }
    if (!gesture.$imageEl || gesture.$imageEl.length === 0 || !gesture.$imageWrapEl || gesture.$imageWrapEl.length === 0)
      return;
    gesture.$slideEl.addClass("" + params.zoomedSlideClass);
    var touchX;
    var touchY;
    var offsetX;
    var offsetY;
    var diffX;
    var diffY;
    var translateX;
    var translateY;
    var imageWidth;
    var imageHeight;
    var scaledWidth;
    var scaledHeight;
    var translateMinX;
    var translateMinY;
    var translateMaxX;
    var translateMaxY;
    var slideWidth;
    var slideHeight;
    if (typeof image.touchesStart.x === "undefined" && e) {
      touchX = e.type === "touchend" ? e.changedTouches[0].pageX : e.pageX;
      touchY = e.type === "touchend" ? e.changedTouches[0].pageY : e.pageY;
    } else {
      touchX = image.touchesStart.x;
      touchY = image.touchesStart.y;
    }
    zoom.scale = gesture.$imageWrapEl.attr("data-swiper-zoom") || params.maxRatio;
    zoom.currentScale = gesture.$imageWrapEl.attr("data-swiper-zoom") || params.maxRatio;
    if (e) {
      slideWidth = gesture.$slideEl[0].offsetWidth;
      slideHeight = gesture.$slideEl[0].offsetHeight;
      offsetX = gesture.$slideEl.offset().left + window2.scrollX;
      offsetY = gesture.$slideEl.offset().top + window2.scrollY;
      diffX = offsetX + slideWidth / 2 - touchX;
      diffY = offsetY + slideHeight / 2 - touchY;
      imageWidth = gesture.$imageEl[0].offsetWidth;
      imageHeight = gesture.$imageEl[0].offsetHeight;
      scaledWidth = imageWidth * zoom.scale;
      scaledHeight = imageHeight * zoom.scale;
      translateMinX = Math.min(slideWidth / 2 - scaledWidth / 2, 0);
      translateMinY = Math.min(slideHeight / 2 - scaledHeight / 2, 0);
      translateMaxX = -translateMinX;
      translateMaxY = -translateMinY;
      translateX = diffX * zoom.scale;
      translateY = diffY * zoom.scale;
      if (translateX < translateMinX) {
        translateX = translateMinX;
      }
      if (translateX > translateMaxX) {
        translateX = translateMaxX;
      }
      if (translateY < translateMinY) {
        translateY = translateMinY;
      }
      if (translateY > translateMaxY) {
        translateY = translateMaxY;
      }
    } else {
      translateX = 0;
      translateY = 0;
    }
    gesture.$imageWrapEl.transition(300).transform("translate3d(" + translateX + "px, " + translateY + "px,0)");
    gesture.$imageEl.transition(300).transform("translate3d(0,0,0) scale(" + zoom.scale + ")");
  },
  out: function out() {
    var swiper = this;
    var zoom = swiper.zoom;
    var params = swiper.params.zoom;
    var gesture = zoom.gesture;
    if (!gesture.$slideEl) {
      if (swiper.params.virtual && swiper.params.virtual.enabled && swiper.virtual) {
        gesture.$slideEl = swiper.$wrapperEl.children("." + swiper.params.slideActiveClass);
      } else {
        gesture.$slideEl = swiper.slides.eq(swiper.activeIndex);
      }
      gesture.$imageEl = gesture.$slideEl.find("img, svg, canvas, picture, .swiper-zoom-target");
      gesture.$imageWrapEl = gesture.$imageEl.parent("." + params.containerClass);
    }
    if (!gesture.$imageEl || gesture.$imageEl.length === 0 || !gesture.$imageWrapEl || gesture.$imageWrapEl.length === 0)
      return;
    zoom.scale = 1;
    zoom.currentScale = 1;
    gesture.$imageWrapEl.transition(300).transform("translate3d(0,0,0)");
    gesture.$imageEl.transition(300).transform("translate3d(0,0,0) scale(1)");
    gesture.$slideEl.removeClass("" + params.zoomedSlideClass);
    gesture.$slideEl = void 0;
  },
  toggleGestures: function toggleGestures(method) {
    var swiper = this;
    var zoom = swiper.zoom;
    var selector = zoom.slideSelector, passive = zoom.passiveListener;
    swiper.$wrapperEl[method]("gesturestart", selector, zoom.onGestureStart, passive);
    swiper.$wrapperEl[method]("gesturechange", selector, zoom.onGestureChange, passive);
    swiper.$wrapperEl[method]("gestureend", selector, zoom.onGestureEnd, passive);
  },
  enableGestures: function enableGestures() {
    if (this.zoom.gesturesEnabled)
      return;
    this.zoom.gesturesEnabled = true;
    this.zoom.toggleGestures("on");
  },
  disableGestures: function disableGestures() {
    if (!this.zoom.gesturesEnabled)
      return;
    this.zoom.gesturesEnabled = false;
    this.zoom.toggleGestures("off");
  },
  enable: function enable6() {
    var swiper = this;
    var support2 = swiper.support;
    var zoom = swiper.zoom;
    if (zoom.enabled)
      return;
    zoom.enabled = true;
    var passiveListener = swiper.touchEvents.start === "touchstart" && support2.passiveListener && swiper.params.passiveListeners ? {
      passive: true,
      capture: false
    } : false;
    var activeListenerWithCapture = support2.passiveListener ? {
      passive: false,
      capture: true
    } : true;
    var slideSelector = "." + swiper.params.slideClass;
    swiper.zoom.passiveListener = passiveListener;
    swiper.zoom.slideSelector = slideSelector;
    if (support2.gestures) {
      swiper.$wrapperEl.on(swiper.touchEvents.start, swiper.zoom.enableGestures, passiveListener);
      swiper.$wrapperEl.on(swiper.touchEvents.end, swiper.zoom.disableGestures, passiveListener);
    } else if (swiper.touchEvents.start === "touchstart") {
      swiper.$wrapperEl.on(swiper.touchEvents.start, slideSelector, zoom.onGestureStart, passiveListener);
      swiper.$wrapperEl.on(swiper.touchEvents.move, slideSelector, zoom.onGestureChange, activeListenerWithCapture);
      swiper.$wrapperEl.on(swiper.touchEvents.end, slideSelector, zoom.onGestureEnd, passiveListener);
      if (swiper.touchEvents.cancel) {
        swiper.$wrapperEl.on(swiper.touchEvents.cancel, slideSelector, zoom.onGestureEnd, passiveListener);
      }
    }
    swiper.$wrapperEl.on(swiper.touchEvents.move, "." + swiper.params.zoom.containerClass, zoom.onTouchMove, activeListenerWithCapture);
  },
  disable: function disable6() {
    var swiper = this;
    var zoom = swiper.zoom;
    if (!zoom.enabled)
      return;
    var support2 = swiper.support;
    swiper.zoom.enabled = false;
    var passiveListener = swiper.touchEvents.start === "touchstart" && support2.passiveListener && swiper.params.passiveListeners ? {
      passive: true,
      capture: false
    } : false;
    var activeListenerWithCapture = support2.passiveListener ? {
      passive: false,
      capture: true
    } : true;
    var slideSelector = "." + swiper.params.slideClass;
    if (support2.gestures) {
      swiper.$wrapperEl.off(swiper.touchEvents.start, swiper.zoom.enableGestures, passiveListener);
      swiper.$wrapperEl.off(swiper.touchEvents.end, swiper.zoom.disableGestures, passiveListener);
    } else if (swiper.touchEvents.start === "touchstart") {
      swiper.$wrapperEl.off(swiper.touchEvents.start, slideSelector, zoom.onGestureStart, passiveListener);
      swiper.$wrapperEl.off(swiper.touchEvents.move, slideSelector, zoom.onGestureChange, activeListenerWithCapture);
      swiper.$wrapperEl.off(swiper.touchEvents.end, slideSelector, zoom.onGestureEnd, passiveListener);
      if (swiper.touchEvents.cancel) {
        swiper.$wrapperEl.off(swiper.touchEvents.cancel, slideSelector, zoom.onGestureEnd, passiveListener);
      }
    }
    swiper.$wrapperEl.off(swiper.touchEvents.move, "." + swiper.params.zoom.containerClass, zoom.onTouchMove, activeListenerWithCapture);
  }
};
var zoom_default = {
  name: "zoom",
  params: {
    zoom: {
      enabled: false,
      maxRatio: 3,
      minRatio: 1,
      toggle: true,
      containerClass: "swiper-zoom-container",
      zoomedSlideClass: "swiper-slide-zoomed"
    }
  },
  create: function create46() {
    var swiper = this;
    bindModuleMethods(swiper, {
      zoom: _extends11({
        enabled: false,
        scale: 1,
        currentScale: 1,
        isScaling: false,
        gesture: {
          $slideEl: void 0,
          slideWidth: void 0,
          slideHeight: void 0,
          $imageEl: void 0,
          $imageWrapEl: void 0,
          maxRatio: 3
        },
        image: {
          isTouched: void 0,
          isMoved: void 0,
          currentX: void 0,
          currentY: void 0,
          minX: void 0,
          minY: void 0,
          maxX: void 0,
          maxY: void 0,
          width: void 0,
          height: void 0,
          startX: void 0,
          startY: void 0,
          touchesStart: {},
          touchesCurrent: {}
        },
        velocity: {
          x: void 0,
          y: void 0,
          prevPositionX: void 0,
          prevPositionY: void 0,
          prevTime: void 0
        }
      }, Zoom)
    });
    var scale = 1;
    Object.defineProperty(swiper.zoom, "scale", {
      get: function get2() {
        return scale;
      },
      set: function set2(value) {
        if (scale !== value) {
          var imageEl = swiper.zoom.gesture.$imageEl ? swiper.zoom.gesture.$imageEl[0] : void 0;
          var slideEl = swiper.zoom.gesture.$slideEl ? swiper.zoom.gesture.$slideEl[0] : void 0;
          swiper.emit("zoomChange", value, imageEl, slideEl);
        }
        scale = value;
      }
    });
  },
  on: {
    init: function init25(swiper) {
      if (swiper.params.zoom.enabled) {
        swiper.zoom.enable();
      }
    },
    destroy: function destroy25(swiper) {
      swiper.zoom.disable();
    },
    touchStart: function touchStart(swiper, e) {
      if (!swiper.zoom.enabled)
        return;
      swiper.zoom.onTouchStart(e);
    },
    touchEnd: function touchEnd(swiper, e) {
      if (!swiper.zoom.enabled)
        return;
      swiper.zoom.onTouchEnd(e);
    },
    doubleTap: function doubleTap(swiper, e) {
      if (!swiper.animating && swiper.params.zoom.enabled && swiper.zoom.enabled && swiper.params.zoom.toggle) {
        swiper.zoom.toggle(e);
      }
    },
    transitionEnd: function transitionEnd3(swiper) {
      if (swiper.zoom.enabled && swiper.params.zoom.enabled) {
        swiper.zoom.onTransitionEnd();
      }
    },
    slideChange: function slideChange(swiper) {
      if (swiper.zoom.enabled && swiper.params.zoom.enabled && swiper.params.cssMode) {
        swiper.zoom.onTransitionEnd();
      }
    }
  }
};

// node_modules/swiper/esm/components/lazy/lazy.js
function _extends12() {
  _extends12 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends12.apply(this, arguments);
}
var Lazy2 = {
  loadInSlide: function loadInSlide(index2, loadInDuplicate) {
    if (loadInDuplicate === void 0) {
      loadInDuplicate = true;
    }
    var swiper = this;
    var params = swiper.params.lazy;
    if (typeof index2 === "undefined")
      return;
    if (swiper.slides.length === 0)
      return;
    var isVirtual = swiper.virtual && swiper.params.virtual.enabled;
    var $slideEl = isVirtual ? swiper.$wrapperEl.children("." + swiper.params.slideClass + '[data-swiper-slide-index="' + index2 + '"]') : swiper.slides.eq(index2);
    var $images = $slideEl.find("." + params.elementClass + ":not(." + params.loadedClass + "):not(." + params.loadingClass + ")");
    if ($slideEl.hasClass(params.elementClass) && !$slideEl.hasClass(params.loadedClass) && !$slideEl.hasClass(params.loadingClass)) {
      $images.push($slideEl[0]);
    }
    if ($images.length === 0)
      return;
    $images.each(function(imageEl) {
      var $imageEl = dom_default(imageEl);
      $imageEl.addClass(params.loadingClass);
      var background = $imageEl.attr("data-background");
      var src = $imageEl.attr("data-src");
      var srcset = $imageEl.attr("data-srcset");
      var sizes = $imageEl.attr("data-sizes");
      var $pictureEl = $imageEl.parent("picture");
      swiper.loadImage($imageEl[0], src || background, srcset, sizes, false, function() {
        if (typeof swiper === "undefined" || swiper === null || !swiper || swiper && !swiper.params || swiper.destroyed)
          return;
        if (background) {
          $imageEl.css("background-image", 'url("' + background + '")');
          $imageEl.removeAttr("data-background");
        } else {
          if (srcset) {
            $imageEl.attr("srcset", srcset);
            $imageEl.removeAttr("data-srcset");
          }
          if (sizes) {
            $imageEl.attr("sizes", sizes);
            $imageEl.removeAttr("data-sizes");
          }
          if ($pictureEl.length) {
            $pictureEl.children("source").each(function(sourceEl) {
              var $source = dom_default(sourceEl);
              if ($source.attr("data-srcset")) {
                $source.attr("srcset", $source.attr("data-srcset"));
                $source.removeAttr("data-srcset");
              }
            });
          }
          if (src) {
            $imageEl.attr("src", src);
            $imageEl.removeAttr("data-src");
          }
        }
        $imageEl.addClass(params.loadedClass).removeClass(params.loadingClass);
        $slideEl.find("." + params.preloaderClass).remove();
        if (swiper.params.loop && loadInDuplicate) {
          var slideOriginalIndex = $slideEl.attr("data-swiper-slide-index");
          if ($slideEl.hasClass(swiper.params.slideDuplicateClass)) {
            var originalSlide = swiper.$wrapperEl.children('[data-swiper-slide-index="' + slideOriginalIndex + '"]:not(.' + swiper.params.slideDuplicateClass + ")");
            swiper.lazy.loadInSlide(originalSlide.index(), false);
          } else {
            var duplicatedSlide = swiper.$wrapperEl.children("." + swiper.params.slideDuplicateClass + '[data-swiper-slide-index="' + slideOriginalIndex + '"]');
            swiper.lazy.loadInSlide(duplicatedSlide.index(), false);
          }
        }
        swiper.emit("lazyImageReady", $slideEl[0], $imageEl[0]);
        if (swiper.params.autoHeight) {
          swiper.updateAutoHeight();
        }
      });
      swiper.emit("lazyImageLoad", $slideEl[0], $imageEl[0]);
    });
  },
  load: function load2() {
    var swiper = this;
    var $wrapperEl = swiper.$wrapperEl, swiperParams = swiper.params, slides = swiper.slides, activeIndex = swiper.activeIndex;
    var isVirtual = swiper.virtual && swiperParams.virtual.enabled;
    var params = swiperParams.lazy;
    var slidesPerView = swiperParams.slidesPerView;
    if (slidesPerView === "auto") {
      slidesPerView = 0;
    }
    function slideExist(index2) {
      if (isVirtual) {
        if ($wrapperEl.children("." + swiperParams.slideClass + '[data-swiper-slide-index="' + index2 + '"]').length) {
          return true;
        }
      } else if (slides[index2])
        return true;
      return false;
    }
    function slideIndex(slideEl) {
      if (isVirtual) {
        return dom_default(slideEl).attr("data-swiper-slide-index");
      }
      return dom_default(slideEl).index();
    }
    if (!swiper.lazy.initialImageLoaded)
      swiper.lazy.initialImageLoaded = true;
    if (swiper.params.watchSlidesVisibility) {
      $wrapperEl.children("." + swiperParams.slideVisibleClass).each(function(slideEl) {
        var index2 = isVirtual ? dom_default(slideEl).attr("data-swiper-slide-index") : dom_default(slideEl).index();
        swiper.lazy.loadInSlide(index2);
      });
    } else if (slidesPerView > 1) {
      for (var i = activeIndex; i < activeIndex + slidesPerView; i += 1) {
        if (slideExist(i))
          swiper.lazy.loadInSlide(i);
      }
    } else {
      swiper.lazy.loadInSlide(activeIndex);
    }
    if (params.loadPrevNext) {
      if (slidesPerView > 1 || params.loadPrevNextAmount && params.loadPrevNextAmount > 1) {
        var amount = params.loadPrevNextAmount;
        var spv = slidesPerView;
        var maxIndex = Math.min(activeIndex + spv + Math.max(amount, spv), slides.length);
        var minIndex = Math.max(activeIndex - Math.max(spv, amount), 0);
        for (var _i = activeIndex + slidesPerView; _i < maxIndex; _i += 1) {
          if (slideExist(_i))
            swiper.lazy.loadInSlide(_i);
        }
        for (var _i2 = minIndex; _i2 < activeIndex; _i2 += 1) {
          if (slideExist(_i2))
            swiper.lazy.loadInSlide(_i2);
        }
      } else {
        var nextSlide = $wrapperEl.children("." + swiperParams.slideNextClass);
        if (nextSlide.length > 0)
          swiper.lazy.loadInSlide(slideIndex(nextSlide));
        var prevSlide = $wrapperEl.children("." + swiperParams.slidePrevClass);
        if (prevSlide.length > 0)
          swiper.lazy.loadInSlide(slideIndex(prevSlide));
      }
    }
  },
  checkInViewOnLoad: function checkInViewOnLoad() {
    var window2 = getWindow();
    var swiper = this;
    if (!swiper || swiper.destroyed)
      return;
    var $scrollElement = swiper.params.lazy.scrollingElement ? dom_default(swiper.params.lazy.scrollingElement) : dom_default(window2);
    var isWindow = $scrollElement[0] === window2;
    var scrollElementWidth = isWindow ? window2.innerWidth : $scrollElement[0].offsetWidth;
    var scrollElementHeight = isWindow ? window2.innerHeight : $scrollElement[0].offsetHeight;
    var swiperOffset = swiper.$el.offset();
    var rtl = swiper.rtlTranslate;
    var inView = false;
    if (rtl)
      swiperOffset.left -= swiper.$el[0].scrollLeft;
    var swiperCoord = [[swiperOffset.left, swiperOffset.top], [swiperOffset.left + swiper.width, swiperOffset.top], [swiperOffset.left, swiperOffset.top + swiper.height], [swiperOffset.left + swiper.width, swiperOffset.top + swiper.height]];
    for (var i = 0; i < swiperCoord.length; i += 1) {
      var point = swiperCoord[i];
      if (point[0] >= 0 && point[0] <= scrollElementWidth && point[1] >= 0 && point[1] <= scrollElementHeight) {
        if (point[0] === 0 && point[1] === 0)
          continue;
        inView = true;
      }
    }
    var passiveListener = swiper.touchEvents.start === "touchstart" && swiper.support.passiveListener && swiper.params.passiveListeners ? {
      passive: true,
      capture: false
    } : false;
    if (inView) {
      swiper.lazy.load();
      $scrollElement.off("scroll", swiper.lazy.checkInViewOnLoad, passiveListener);
    } else if (!swiper.lazy.scrollHandlerAttached) {
      swiper.lazy.scrollHandlerAttached = true;
      $scrollElement.on("scroll", swiper.lazy.checkInViewOnLoad, passiveListener);
    }
  }
};
var lazy_default2 = {
  name: "lazy",
  params: {
    lazy: {
      checkInView: false,
      enabled: false,
      loadPrevNext: false,
      loadPrevNextAmount: 1,
      loadOnTransitionStart: false,
      scrollingElement: "",
      elementClass: "swiper-lazy",
      loadingClass: "swiper-lazy-loading",
      loadedClass: "swiper-lazy-loaded",
      preloaderClass: "swiper-lazy-preloader"
    }
  },
  create: function create47() {
    var swiper = this;
    bindModuleMethods(swiper, {
      lazy: _extends12({
        initialImageLoaded: false
      }, Lazy2)
    });
  },
  on: {
    beforeInit: function beforeInit3(swiper) {
      if (swiper.params.lazy.enabled && swiper.params.preloadImages) {
        swiper.params.preloadImages = false;
      }
    },
    init: function init26(swiper) {
      if (swiper.params.lazy.enabled && !swiper.params.loop && swiper.params.initialSlide === 0) {
        if (swiper.params.lazy.checkInView) {
          swiper.lazy.checkInViewOnLoad();
        } else {
          swiper.lazy.load();
        }
      }
    },
    scroll: function scroll(swiper) {
      if (swiper.params.freeMode && !swiper.params.freeModeSticky) {
        swiper.lazy.load();
      }
    },
    "scrollbarDragMove resize _freeModeNoMomentumRelease": function lazyLoad(swiper) {
      if (swiper.params.lazy.enabled) {
        swiper.lazy.load();
      }
    },
    transitionStart: function transitionStart2(swiper) {
      if (swiper.params.lazy.enabled) {
        if (swiper.params.lazy.loadOnTransitionStart || !swiper.params.lazy.loadOnTransitionStart && !swiper.lazy.initialImageLoaded) {
          swiper.lazy.load();
        }
      }
    },
    transitionEnd: function transitionEnd4(swiper) {
      if (swiper.params.lazy.enabled && !swiper.params.lazy.loadOnTransitionStart) {
        swiper.lazy.load();
      }
    },
    slideChange: function slideChange2(swiper) {
      var _swiper$params = swiper.params, lazy = _swiper$params.lazy, cssMode = _swiper$params.cssMode, watchSlidesVisibility = _swiper$params.watchSlidesVisibility, watchSlidesProgress = _swiper$params.watchSlidesProgress, touchReleaseOnEdges = _swiper$params.touchReleaseOnEdges, resistanceRatio = _swiper$params.resistanceRatio;
      if (lazy.enabled && (cssMode || (watchSlidesVisibility || watchSlidesProgress) && (touchReleaseOnEdges || resistanceRatio === 0))) {
        swiper.lazy.load();
      }
    }
  }
};

// node_modules/swiper/esm/components/controller/controller.js
function _extends13() {
  _extends13 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends13.apply(this, arguments);
}
var Controller = {
  LinearSpline: function LinearSpline(x, y) {
    var binarySearch = function search() {
      var maxIndex;
      var minIndex;
      var guess;
      return function(array, val) {
        minIndex = -1;
        maxIndex = array.length;
        while (maxIndex - minIndex > 1) {
          guess = maxIndex + minIndex >> 1;
          if (array[guess] <= val) {
            minIndex = guess;
          } else {
            maxIndex = guess;
          }
        }
        return maxIndex;
      };
    }();
    this.x = x;
    this.y = y;
    this.lastIndex = x.length - 1;
    var i1;
    var i3;
    this.interpolate = function interpolate(x2) {
      if (!x2)
        return 0;
      i3 = binarySearch(this.x, x2);
      i1 = i3 - 1;
      return (x2 - this.x[i1]) * (this.y[i3] - this.y[i1]) / (this.x[i3] - this.x[i1]) + this.y[i1];
    };
    return this;
  },
  getInterpolateFunction: function getInterpolateFunction(c) {
    var swiper = this;
    if (!swiper.controller.spline) {
      swiper.controller.spline = swiper.params.loop ? new Controller.LinearSpline(swiper.slidesGrid, c.slidesGrid) : new Controller.LinearSpline(swiper.snapGrid, c.snapGrid);
    }
  },
  setTranslate: function setTranslate7(_setTranslate, byController) {
    var swiper = this;
    var controlled = swiper.controller.control;
    var multiplier;
    var controlledTranslate;
    var Swiper2 = swiper.constructor;
    function setControlledTranslate(c) {
      var translate = swiper.rtlTranslate ? -swiper.translate : swiper.translate;
      if (swiper.params.controller.by === "slide") {
        swiper.controller.getInterpolateFunction(c);
        controlledTranslate = -swiper.controller.spline.interpolate(-translate);
      }
      if (!controlledTranslate || swiper.params.controller.by === "container") {
        multiplier = (c.maxTranslate() - c.minTranslate()) / (swiper.maxTranslate() - swiper.minTranslate());
        controlledTranslate = (translate - swiper.minTranslate()) * multiplier + c.minTranslate();
      }
      if (swiper.params.controller.inverse) {
        controlledTranslate = c.maxTranslate() - controlledTranslate;
      }
      c.updateProgress(controlledTranslate);
      c.setTranslate(controlledTranslate, swiper);
      c.updateActiveIndex();
      c.updateSlidesClasses();
    }
    if (Array.isArray(controlled)) {
      for (var i = 0; i < controlled.length; i += 1) {
        if (controlled[i] !== byController && controlled[i] instanceof Swiper2) {
          setControlledTranslate(controlled[i]);
        }
      }
    } else if (controlled instanceof Swiper2 && byController !== controlled) {
      setControlledTranslate(controlled);
    }
  },
  setTransition: function setTransition6(duration, byController) {
    var swiper = this;
    var Swiper2 = swiper.constructor;
    var controlled = swiper.controller.control;
    var i;
    function setControlledTransition(c) {
      c.setTransition(duration, swiper);
      if (duration !== 0) {
        c.transitionStart();
        if (c.params.autoHeight) {
          nextTick2(function() {
            c.updateAutoHeight();
          });
        }
        c.$wrapperEl.transitionEnd(function() {
          if (!controlled)
            return;
          if (c.params.loop && swiper.params.controller.by === "slide") {
            c.loopFix();
          }
          c.transitionEnd();
        });
      }
    }
    if (Array.isArray(controlled)) {
      for (i = 0; i < controlled.length; i += 1) {
        if (controlled[i] !== byController && controlled[i] instanceof Swiper2) {
          setControlledTransition(controlled[i]);
        }
      }
    } else if (controlled instanceof Swiper2 && byController !== controlled) {
      setControlledTransition(controlled);
    }
  }
};
var controller_default = {
  name: "controller",
  params: {
    controller: {
      control: void 0,
      inverse: false,
      by: "slide"
    }
  },
  create: function create48() {
    var swiper = this;
    bindModuleMethods(swiper, {
      controller: _extends13({
        control: swiper.params.controller.control
      }, Controller)
    });
  },
  on: {
    update: function update6(swiper) {
      if (!swiper.controller.control)
        return;
      if (swiper.controller.spline) {
        swiper.controller.spline = void 0;
        delete swiper.controller.spline;
      }
    },
    resize: function resize2(swiper) {
      if (!swiper.controller.control)
        return;
      if (swiper.controller.spline) {
        swiper.controller.spline = void 0;
        delete swiper.controller.spline;
      }
    },
    observerUpdate: function observerUpdate2(swiper) {
      if (!swiper.controller.control)
        return;
      if (swiper.controller.spline) {
        swiper.controller.spline = void 0;
        delete swiper.controller.spline;
      }
    },
    setTranslate: function setTranslate8(swiper, translate, byController) {
      if (!swiper.controller.control)
        return;
      swiper.controller.setTranslate(translate, byController);
    },
    setTransition: function setTransition7(swiper, duration, byController) {
      if (!swiper.controller.control)
        return;
      swiper.controller.setTransition(duration, byController);
    }
  }
};

// node_modules/swiper/esm/components/a11y/a11y.js
function _extends14() {
  _extends14 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends14.apply(this, arguments);
}
var A11y = {
  getRandomNumber: function getRandomNumber(size) {
    if (size === void 0) {
      size = 16;
    }
    var randomChar = function randomChar2() {
      return Math.round(16 * Math.random()).toString(16);
    };
    return "x".repeat(size).replace(/x/g, randomChar);
  },
  makeElFocusable: function makeElFocusable($el) {
    $el.attr("tabIndex", "0");
    return $el;
  },
  makeElNotFocusable: function makeElNotFocusable($el) {
    $el.attr("tabIndex", "-1");
    return $el;
  },
  addElRole: function addElRole($el, role) {
    $el.attr("role", role);
    return $el;
  },
  addElRoleDescription: function addElRoleDescription($el, description) {
    $el.attr("aria-roledescription", description);
    return $el;
  },
  addElControls: function addElControls($el, controls) {
    $el.attr("aria-controls", controls);
    return $el;
  },
  addElLabel: function addElLabel($el, label) {
    $el.attr("aria-label", label);
    return $el;
  },
  addElId: function addElId($el, id2) {
    $el.attr("id", id2);
    return $el;
  },
  addElLive: function addElLive($el, live) {
    $el.attr("aria-live", live);
    return $el;
  },
  disableEl: function disableEl($el) {
    $el.attr("aria-disabled", true);
    return $el;
  },
  enableEl: function enableEl($el) {
    $el.attr("aria-disabled", false);
    return $el;
  },
  onEnterOrSpaceKey: function onEnterOrSpaceKey(e) {
    if (e.keyCode !== 13 && e.keyCode !== 32)
      return;
    var swiper = this;
    var params = swiper.params.a11y;
    var $targetEl = dom_default(e.target);
    if (swiper.navigation && swiper.navigation.$nextEl && $targetEl.is(swiper.navigation.$nextEl)) {
      if (!(swiper.isEnd && !swiper.params.loop)) {
        swiper.slideNext();
      }
      if (swiper.isEnd) {
        swiper.a11y.notify(params.lastSlideMessage);
      } else {
        swiper.a11y.notify(params.nextSlideMessage);
      }
    }
    if (swiper.navigation && swiper.navigation.$prevEl && $targetEl.is(swiper.navigation.$prevEl)) {
      if (!(swiper.isBeginning && !swiper.params.loop)) {
        swiper.slidePrev();
      }
      if (swiper.isBeginning) {
        swiper.a11y.notify(params.firstSlideMessage);
      } else {
        swiper.a11y.notify(params.prevSlideMessage);
      }
    }
    if (swiper.pagination && $targetEl.is(classesToSelector(swiper.params.pagination.bulletClass))) {
      $targetEl[0].click();
    }
  },
  notify: function notify(message) {
    var swiper = this;
    var notification = swiper.a11y.liveRegion;
    if (notification.length === 0)
      return;
    notification.html("");
    notification.html(message);
  },
  updateNavigation: function updateNavigation() {
    var swiper = this;
    if (swiper.params.loop || !swiper.navigation)
      return;
    var _swiper$navigation = swiper.navigation, $nextEl = _swiper$navigation.$nextEl, $prevEl = _swiper$navigation.$prevEl;
    if ($prevEl && $prevEl.length > 0) {
      if (swiper.isBeginning) {
        swiper.a11y.disableEl($prevEl);
        swiper.a11y.makeElNotFocusable($prevEl);
      } else {
        swiper.a11y.enableEl($prevEl);
        swiper.a11y.makeElFocusable($prevEl);
      }
    }
    if ($nextEl && $nextEl.length > 0) {
      if (swiper.isEnd) {
        swiper.a11y.disableEl($nextEl);
        swiper.a11y.makeElNotFocusable($nextEl);
      } else {
        swiper.a11y.enableEl($nextEl);
        swiper.a11y.makeElFocusable($nextEl);
      }
    }
  },
  updatePagination: function updatePagination() {
    var swiper = this;
    var params = swiper.params.a11y;
    if (swiper.pagination && swiper.params.pagination.clickable && swiper.pagination.bullets && swiper.pagination.bullets.length) {
      swiper.pagination.bullets.each(function(bulletEl) {
        var $bulletEl = dom_default(bulletEl);
        swiper.a11y.makeElFocusable($bulletEl);
        if (!swiper.params.pagination.renderBullet) {
          swiper.a11y.addElRole($bulletEl, "button");
          swiper.a11y.addElLabel($bulletEl, params.paginationBulletMessage.replace(/\{\{index\}\}/, $bulletEl.index() + 1));
        }
      });
    }
  },
  init: function init27() {
    var swiper = this;
    var params = swiper.params.a11y;
    swiper.$el.append(swiper.a11y.liveRegion);
    var $containerEl = swiper.$el;
    if (params.containerRoleDescriptionMessage) {
      swiper.a11y.addElRoleDescription($containerEl, params.containerRoleDescriptionMessage);
    }
    if (params.containerMessage) {
      swiper.a11y.addElLabel($containerEl, params.containerMessage);
    }
    var $wrapperEl = swiper.$wrapperEl;
    var wrapperId = $wrapperEl.attr("id") || "swiper-wrapper-" + swiper.a11y.getRandomNumber(16);
    var live = swiper.params.autoplay && swiper.params.autoplay.enabled ? "off" : "polite";
    swiper.a11y.addElId($wrapperEl, wrapperId);
    swiper.a11y.addElLive($wrapperEl, live);
    if (params.itemRoleDescriptionMessage) {
      swiper.a11y.addElRoleDescription(dom_default(swiper.slides), params.itemRoleDescriptionMessage);
    }
    swiper.a11y.addElRole(dom_default(swiper.slides), params.slideRole);
    var slidesLength = swiper.params.loop ? swiper.slides.filter(function(el) {
      return !el.classList.contains(swiper.params.slideDuplicateClass);
    }).length : swiper.slides.length;
    swiper.slides.each(function(slideEl, index2) {
      var $slideEl = dom_default(slideEl);
      var slideIndex = swiper.params.loop ? parseInt($slideEl.attr("data-swiper-slide-index"), 10) : index2;
      var ariaLabelMessage = params.slideLabelMessage.replace(/\{\{index\}\}/, slideIndex + 1).replace(/\{\{slidesLength\}\}/, slidesLength);
      swiper.a11y.addElLabel($slideEl, ariaLabelMessage);
    });
    var $nextEl;
    var $prevEl;
    if (swiper.navigation && swiper.navigation.$nextEl) {
      $nextEl = swiper.navigation.$nextEl;
    }
    if (swiper.navigation && swiper.navigation.$prevEl) {
      $prevEl = swiper.navigation.$prevEl;
    }
    if ($nextEl && $nextEl.length) {
      swiper.a11y.makeElFocusable($nextEl);
      if ($nextEl[0].tagName !== "BUTTON") {
        swiper.a11y.addElRole($nextEl, "button");
        $nextEl.on("keydown", swiper.a11y.onEnterOrSpaceKey);
      }
      swiper.a11y.addElLabel($nextEl, params.nextSlideMessage);
      swiper.a11y.addElControls($nextEl, wrapperId);
    }
    if ($prevEl && $prevEl.length) {
      swiper.a11y.makeElFocusable($prevEl);
      if ($prevEl[0].tagName !== "BUTTON") {
        swiper.a11y.addElRole($prevEl, "button");
        $prevEl.on("keydown", swiper.a11y.onEnterOrSpaceKey);
      }
      swiper.a11y.addElLabel($prevEl, params.prevSlideMessage);
      swiper.a11y.addElControls($prevEl, wrapperId);
    }
    if (swiper.pagination && swiper.params.pagination.clickable && swiper.pagination.bullets && swiper.pagination.bullets.length) {
      swiper.pagination.$el.on("keydown", classesToSelector(swiper.params.pagination.bulletClass), swiper.a11y.onEnterOrSpaceKey);
    }
  },
  destroy: function destroy26() {
    var swiper = this;
    if (swiper.a11y.liveRegion && swiper.a11y.liveRegion.length > 0)
      swiper.a11y.liveRegion.remove();
    var $nextEl;
    var $prevEl;
    if (swiper.navigation && swiper.navigation.$nextEl) {
      $nextEl = swiper.navigation.$nextEl;
    }
    if (swiper.navigation && swiper.navigation.$prevEl) {
      $prevEl = swiper.navigation.$prevEl;
    }
    if ($nextEl) {
      $nextEl.off("keydown", swiper.a11y.onEnterOrSpaceKey);
    }
    if ($prevEl) {
      $prevEl.off("keydown", swiper.a11y.onEnterOrSpaceKey);
    }
    if (swiper.pagination && swiper.params.pagination.clickable && swiper.pagination.bullets && swiper.pagination.bullets.length) {
      swiper.pagination.$el.off("keydown", classesToSelector(swiper.params.pagination.bulletClass), swiper.a11y.onEnterOrSpaceKey);
    }
  }
};
var a11y_default = {
  name: "a11y",
  params: {
    a11y: {
      enabled: true,
      notificationClass: "swiper-notification",
      prevSlideMessage: "Previous slide",
      nextSlideMessage: "Next slide",
      firstSlideMessage: "This is the first slide",
      lastSlideMessage: "This is the last slide",
      paginationBulletMessage: "Go to slide {{index}}",
      slideLabelMessage: "{{index}} / {{slidesLength}}",
      containerMessage: null,
      containerRoleDescriptionMessage: null,
      itemRoleDescriptionMessage: null,
      slideRole: "group"
    }
  },
  create: function create49() {
    var swiper = this;
    bindModuleMethods(swiper, {
      a11y: _extends14({}, A11y, {
        liveRegion: dom_default('<span class="' + swiper.params.a11y.notificationClass + '" aria-live="assertive" aria-atomic="true"></span>')
      })
    });
  },
  on: {
    afterInit: function afterInit(swiper) {
      if (!swiper.params.a11y.enabled)
        return;
      swiper.a11y.init();
      swiper.a11y.updateNavigation();
    },
    toEdge: function toEdge2(swiper) {
      if (!swiper.params.a11y.enabled)
        return;
      swiper.a11y.updateNavigation();
    },
    fromEdge: function fromEdge2(swiper) {
      if (!swiper.params.a11y.enabled)
        return;
      swiper.a11y.updateNavigation();
    },
    paginationUpdate: function paginationUpdate(swiper) {
      if (!swiper.params.a11y.enabled)
        return;
      swiper.a11y.updatePagination();
    },
    destroy: function destroy27(swiper) {
      if (!swiper.params.a11y.enabled)
        return;
      swiper.a11y.destroy();
    }
  }
};

// node_modules/swiper/esm/components/history/history.js
function _extends15() {
  _extends15 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends15.apply(this, arguments);
}
var History = {
  init: function init28() {
    var swiper = this;
    var window2 = getWindow();
    if (!swiper.params.history)
      return;
    if (!window2.history || !window2.history.pushState) {
      swiper.params.history.enabled = false;
      swiper.params.hashNavigation.enabled = true;
      return;
    }
    var history = swiper.history;
    history.initialized = true;
    history.paths = History.getPathValues(swiper.params.url);
    if (!history.paths.key && !history.paths.value)
      return;
    history.scrollToSlide(0, history.paths.value, swiper.params.runCallbacksOnInit);
    if (!swiper.params.history.replaceState) {
      window2.addEventListener("popstate", swiper.history.setHistoryPopState);
    }
  },
  destroy: function destroy28() {
    var swiper = this;
    var window2 = getWindow();
    if (!swiper.params.history.replaceState) {
      window2.removeEventListener("popstate", swiper.history.setHistoryPopState);
    }
  },
  setHistoryPopState: function setHistoryPopState() {
    var swiper = this;
    swiper.history.paths = History.getPathValues(swiper.params.url);
    swiper.history.scrollToSlide(swiper.params.speed, swiper.history.paths.value, false);
  },
  getPathValues: function getPathValues(urlOverride) {
    var window2 = getWindow();
    var location;
    if (urlOverride) {
      location = new URL(urlOverride);
    } else {
      location = window2.location;
    }
    var pathArray = location.pathname.slice(1).split("/").filter(function(part) {
      return part !== "";
    });
    var total = pathArray.length;
    var key = pathArray[total - 2];
    var value = pathArray[total - 1];
    return {
      key,
      value
    };
  },
  setHistory: function setHistory(key, index2) {
    var swiper = this;
    var window2 = getWindow();
    if (!swiper.history.initialized || !swiper.params.history.enabled)
      return;
    var location;
    if (swiper.params.url) {
      location = new URL(swiper.params.url);
    } else {
      location = window2.location;
    }
    var slide = swiper.slides.eq(index2);
    var value = History.slugify(slide.attr("data-history"));
    if (swiper.params.history.root.length > 0) {
      var root = swiper.params.history.root;
      if (root[root.length - 1] === "/")
        root = root.slice(0, root.length - 1);
      value = root + "/" + key + "/" + value;
    } else if (!location.pathname.includes(key)) {
      value = key + "/" + value;
    }
    var currentState = window2.history.state;
    if (currentState && currentState.value === value) {
      return;
    }
    if (swiper.params.history.replaceState) {
      window2.history.replaceState({
        value
      }, null, value);
    } else {
      window2.history.pushState({
        value
      }, null, value);
    }
  },
  slugify: function slugify(text2) {
    return text2.toString().replace(/\s+/g, "-").replace(/[^\w-]+/g, "").replace(/--+/g, "-").replace(/^-+/, "").replace(/-+$/, "");
  },
  scrollToSlide: function scrollToSlide(speed, value, runCallbacks) {
    var swiper = this;
    if (value) {
      for (var i = 0, length = swiper.slides.length; i < length; i += 1) {
        var slide = swiper.slides.eq(i);
        var slideHistory = History.slugify(slide.attr("data-history"));
        if (slideHistory === value && !slide.hasClass(swiper.params.slideDuplicateClass)) {
          var index2 = slide.index();
          swiper.slideTo(index2, speed, runCallbacks);
        }
      }
    } else {
      swiper.slideTo(0, speed, runCallbacks);
    }
  }
};
var history_default2 = {
  name: "history",
  params: {
    history: {
      enabled: false,
      root: "",
      replaceState: false,
      key: "slides"
    }
  },
  create: function create50() {
    var swiper = this;
    bindModuleMethods(swiper, {
      history: _extends15({}, History)
    });
  },
  on: {
    init: function init29(swiper) {
      if (swiper.params.history.enabled) {
        swiper.history.init();
      }
    },
    destroy: function destroy29(swiper) {
      if (swiper.params.history.enabled) {
        swiper.history.destroy();
      }
    },
    "transitionEnd _freeModeNoMomentumRelease": function transitionEnd_freeModeNoMomentumRelease(swiper) {
      if (swiper.history.initialized) {
        swiper.history.setHistory(swiper.params.history.key, swiper.activeIndex);
      }
    },
    slideChange: function slideChange3(swiper) {
      if (swiper.history.initialized && swiper.params.cssMode) {
        swiper.history.setHistory(swiper.params.history.key, swiper.activeIndex);
      }
    }
  }
};

// node_modules/swiper/esm/components/hash-navigation/hash-navigation.js
function _extends16() {
  _extends16 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends16.apply(this, arguments);
}
var HashNavigation = {
  onHashChange: function onHashChange() {
    var swiper = this;
    var document = getDocument();
    swiper.emit("hashChange");
    var newHash = document.location.hash.replace("#", "");
    var activeSlideHash = swiper.slides.eq(swiper.activeIndex).attr("data-hash");
    if (newHash !== activeSlideHash) {
      var newIndex = swiper.$wrapperEl.children("." + swiper.params.slideClass + '[data-hash="' + newHash + '"]').index();
      if (typeof newIndex === "undefined")
        return;
      swiper.slideTo(newIndex);
    }
  },
  setHash: function setHash() {
    var swiper = this;
    var window2 = getWindow();
    var document = getDocument();
    if (!swiper.hashNavigation.initialized || !swiper.params.hashNavigation.enabled)
      return;
    if (swiper.params.hashNavigation.replaceState && window2.history && window2.history.replaceState) {
      window2.history.replaceState(null, null, "#" + swiper.slides.eq(swiper.activeIndex).attr("data-hash") || "");
      swiper.emit("hashSet");
    } else {
      var slide = swiper.slides.eq(swiper.activeIndex);
      var hash = slide.attr("data-hash") || slide.attr("data-history");
      document.location.hash = hash || "";
      swiper.emit("hashSet");
    }
  },
  init: function init30() {
    var swiper = this;
    var document = getDocument();
    var window2 = getWindow();
    if (!swiper.params.hashNavigation.enabled || swiper.params.history && swiper.params.history.enabled)
      return;
    swiper.hashNavigation.initialized = true;
    var hash = document.location.hash.replace("#", "");
    if (hash) {
      var speed = 0;
      for (var i = 0, length = swiper.slides.length; i < length; i += 1) {
        var slide = swiper.slides.eq(i);
        var slideHash = slide.attr("data-hash") || slide.attr("data-history");
        if (slideHash === hash && !slide.hasClass(swiper.params.slideDuplicateClass)) {
          var index2 = slide.index();
          swiper.slideTo(index2, speed, swiper.params.runCallbacksOnInit, true);
        }
      }
    }
    if (swiper.params.hashNavigation.watchState) {
      dom_default(window2).on("hashchange", swiper.hashNavigation.onHashChange);
    }
  },
  destroy: function destroy30() {
    var swiper = this;
    var window2 = getWindow();
    if (swiper.params.hashNavigation.watchState) {
      dom_default(window2).off("hashchange", swiper.hashNavigation.onHashChange);
    }
  }
};
var hash_navigation_default = {
  name: "hash-navigation",
  params: {
    hashNavigation: {
      enabled: false,
      replaceState: false,
      watchState: false
    }
  },
  create: function create51() {
    var swiper = this;
    bindModuleMethods(swiper, {
      hashNavigation: _extends16({
        initialized: false
      }, HashNavigation)
    });
  },
  on: {
    init: function init31(swiper) {
      if (swiper.params.hashNavigation.enabled) {
        swiper.hashNavigation.init();
      }
    },
    destroy: function destroy31(swiper) {
      if (swiper.params.hashNavigation.enabled) {
        swiper.hashNavigation.destroy();
      }
    },
    "transitionEnd _freeModeNoMomentumRelease": function transitionEnd_freeModeNoMomentumRelease2(swiper) {
      if (swiper.hashNavigation.initialized) {
        swiper.hashNavigation.setHash();
      }
    },
    slideChange: function slideChange4(swiper) {
      if (swiper.hashNavigation.initialized && swiper.params.cssMode) {
        swiper.hashNavigation.setHash();
      }
    }
  }
};

// node_modules/swiper/esm/components/autoplay/autoplay.js
function _extends17() {
  _extends17 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends17.apply(this, arguments);
}
var Autoplay = {
  run: function run() {
    var swiper = this;
    var $activeSlideEl = swiper.slides.eq(swiper.activeIndex);
    var delay = swiper.params.autoplay.delay;
    if ($activeSlideEl.attr("data-swiper-autoplay")) {
      delay = $activeSlideEl.attr("data-swiper-autoplay") || swiper.params.autoplay.delay;
    }
    clearTimeout(swiper.autoplay.timeout);
    swiper.autoplay.timeout = nextTick2(function() {
      var autoplayResult;
      if (swiper.params.autoplay.reverseDirection) {
        if (swiper.params.loop) {
          swiper.loopFix();
          autoplayResult = swiper.slidePrev(swiper.params.speed, true, true);
          swiper.emit("autoplay");
        } else if (!swiper.isBeginning) {
          autoplayResult = swiper.slidePrev(swiper.params.speed, true, true);
          swiper.emit("autoplay");
        } else if (!swiper.params.autoplay.stopOnLastSlide) {
          autoplayResult = swiper.slideTo(swiper.slides.length - 1, swiper.params.speed, true, true);
          swiper.emit("autoplay");
        } else {
          swiper.autoplay.stop();
        }
      } else if (swiper.params.loop) {
        swiper.loopFix();
        autoplayResult = swiper.slideNext(swiper.params.speed, true, true);
        swiper.emit("autoplay");
      } else if (!swiper.isEnd) {
        autoplayResult = swiper.slideNext(swiper.params.speed, true, true);
        swiper.emit("autoplay");
      } else if (!swiper.params.autoplay.stopOnLastSlide) {
        autoplayResult = swiper.slideTo(0, swiper.params.speed, true, true);
        swiper.emit("autoplay");
      } else {
        swiper.autoplay.stop();
      }
      if (swiper.params.cssMode && swiper.autoplay.running)
        swiper.autoplay.run();
      else if (autoplayResult === false) {
        swiper.autoplay.run();
      }
    }, delay);
  },
  start: function start() {
    var swiper = this;
    if (typeof swiper.autoplay.timeout !== "undefined")
      return false;
    if (swiper.autoplay.running)
      return false;
    swiper.autoplay.running = true;
    swiper.emit("autoplayStart");
    swiper.autoplay.run();
    return true;
  },
  stop: function stop() {
    var swiper = this;
    if (!swiper.autoplay.running)
      return false;
    if (typeof swiper.autoplay.timeout === "undefined")
      return false;
    if (swiper.autoplay.timeout) {
      clearTimeout(swiper.autoplay.timeout);
      swiper.autoplay.timeout = void 0;
    }
    swiper.autoplay.running = false;
    swiper.emit("autoplayStop");
    return true;
  },
  pause: function pause(speed) {
    var swiper = this;
    if (!swiper.autoplay.running)
      return;
    if (swiper.autoplay.paused)
      return;
    if (swiper.autoplay.timeout)
      clearTimeout(swiper.autoplay.timeout);
    swiper.autoplay.paused = true;
    if (speed === 0 || !swiper.params.autoplay.waitForTransition) {
      swiper.autoplay.paused = false;
      swiper.autoplay.run();
    } else {
      ["transitionend", "webkitTransitionEnd"].forEach(function(event2) {
        swiper.$wrapperEl[0].addEventListener(event2, swiper.autoplay.onTransitionEnd);
      });
    }
  },
  onVisibilityChange: function onVisibilityChange() {
    var swiper = this;
    var document = getDocument();
    if (document.visibilityState === "hidden" && swiper.autoplay.running) {
      swiper.autoplay.pause();
    }
    if (document.visibilityState === "visible" && swiper.autoplay.paused) {
      swiper.autoplay.run();
      swiper.autoplay.paused = false;
    }
  },
  onTransitionEnd: function onTransitionEnd2(e) {
    var swiper = this;
    if (!swiper || swiper.destroyed || !swiper.$wrapperEl)
      return;
    if (e.target !== swiper.$wrapperEl[0])
      return;
    ["transitionend", "webkitTransitionEnd"].forEach(function(event2) {
      swiper.$wrapperEl[0].removeEventListener(event2, swiper.autoplay.onTransitionEnd);
    });
    swiper.autoplay.paused = false;
    if (!swiper.autoplay.running) {
      swiper.autoplay.stop();
    } else {
      swiper.autoplay.run();
    }
  },
  onMouseEnter: function onMouseEnter() {
    var swiper = this;
    if (swiper.params.autoplay.disableOnInteraction) {
      swiper.autoplay.stop();
    } else {
      swiper.autoplay.pause();
    }
    ["transitionend", "webkitTransitionEnd"].forEach(function(event2) {
      swiper.$wrapperEl[0].removeEventListener(event2, swiper.autoplay.onTransitionEnd);
    });
  },
  onMouseLeave: function onMouseLeave() {
    var swiper = this;
    if (swiper.params.autoplay.disableOnInteraction) {
      return;
    }
    swiper.autoplay.paused = false;
    swiper.autoplay.run();
  },
  attachMouseEvents: function attachMouseEvents() {
    var swiper = this;
    if (swiper.params.autoplay.pauseOnMouseEnter) {
      swiper.$el.on("mouseenter", swiper.autoplay.onMouseEnter);
      swiper.$el.on("mouseleave", swiper.autoplay.onMouseLeave);
    }
  },
  detachMouseEvents: function detachMouseEvents() {
    var swiper = this;
    swiper.$el.off("mouseenter", swiper.autoplay.onMouseEnter);
    swiper.$el.off("mouseleave", swiper.autoplay.onMouseLeave);
  }
};
var autoplay_default = {
  name: "autoplay",
  params: {
    autoplay: {
      enabled: false,
      delay: 3e3,
      waitForTransition: true,
      disableOnInteraction: true,
      stopOnLastSlide: false,
      reverseDirection: false,
      pauseOnMouseEnter: false
    }
  },
  create: function create52() {
    var swiper = this;
    bindModuleMethods(swiper, {
      autoplay: _extends17({}, Autoplay, {
        running: false,
        paused: false
      })
    });
  },
  on: {
    init: function init32(swiper) {
      if (swiper.params.autoplay.enabled) {
        swiper.autoplay.start();
        var document = getDocument();
        document.addEventListener("visibilitychange", swiper.autoplay.onVisibilityChange);
        swiper.autoplay.attachMouseEvents();
      }
    },
    beforeTransitionStart: function beforeTransitionStart(swiper, speed, internal) {
      if (swiper.autoplay.running) {
        if (internal || !swiper.params.autoplay.disableOnInteraction) {
          swiper.autoplay.pause(speed);
        } else {
          swiper.autoplay.stop();
        }
      }
    },
    sliderFirstMove: function sliderFirstMove(swiper) {
      if (swiper.autoplay.running) {
        if (swiper.params.autoplay.disableOnInteraction) {
          swiper.autoplay.stop();
        } else {
          swiper.autoplay.pause();
        }
      }
    },
    touchEnd: function touchEnd2(swiper) {
      if (swiper.params.cssMode && swiper.autoplay.paused && !swiper.params.autoplay.disableOnInteraction) {
        swiper.autoplay.run();
      }
    },
    destroy: function destroy32(swiper) {
      swiper.autoplay.detachMouseEvents();
      if (swiper.autoplay.running) {
        swiper.autoplay.stop();
      }
      var document = getDocument();
      document.removeEventListener("visibilitychange", swiper.autoplay.onVisibilityChange);
    }
  }
};

// node_modules/swiper/esm/components/effect-fade/effect-fade.js
function _extends18() {
  _extends18 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends18.apply(this, arguments);
}
var Fade = {
  setTranslate: function setTranslate9() {
    var swiper = this;
    var slides = swiper.slides;
    for (var i = 0; i < slides.length; i += 1) {
      var $slideEl = swiper.slides.eq(i);
      var offset2 = $slideEl[0].swiperSlideOffset;
      var tx = -offset2;
      if (!swiper.params.virtualTranslate)
        tx -= swiper.translate;
      var ty = 0;
      if (!swiper.isHorizontal()) {
        ty = tx;
        tx = 0;
      }
      var slideOpacity = swiper.params.fadeEffect.crossFade ? Math.max(1 - Math.abs($slideEl[0].progress), 0) : 1 + Math.min(Math.max($slideEl[0].progress, -1), 0);
      $slideEl.css({
        opacity: slideOpacity
      }).transform("translate3d(" + tx + "px, " + ty + "px, 0px)");
    }
  },
  setTransition: function setTransition8(duration) {
    var swiper = this;
    var slides = swiper.slides, $wrapperEl = swiper.$wrapperEl;
    slides.transition(duration);
    if (swiper.params.virtualTranslate && duration !== 0) {
      var eventTriggered = false;
      slides.transitionEnd(function() {
        if (eventTriggered)
          return;
        if (!swiper || swiper.destroyed)
          return;
        eventTriggered = true;
        swiper.animating = false;
        var triggerEvents = ["webkitTransitionEnd", "transitionend"];
        for (var i = 0; i < triggerEvents.length; i += 1) {
          $wrapperEl.trigger(triggerEvents[i]);
        }
      });
    }
  }
};
var effect_fade_default = {
  name: "effect-fade",
  params: {
    fadeEffect: {
      crossFade: false
    }
  },
  create: function create53() {
    var swiper = this;
    bindModuleMethods(swiper, {
      fadeEffect: _extends18({}, Fade)
    });
  },
  on: {
    beforeInit: function beforeInit4(swiper) {
      if (swiper.params.effect !== "fade")
        return;
      swiper.classNames.push(swiper.params.containerModifierClass + "fade");
      var overwriteParams = {
        slidesPerView: 1,
        slidesPerColumn: 1,
        slidesPerGroup: 1,
        watchSlidesProgress: true,
        spaceBetween: 0,
        virtualTranslate: true
      };
      extend2(swiper.params, overwriteParams);
      extend2(swiper.originalParams, overwriteParams);
    },
    setTranslate: function setTranslate10(swiper) {
      if (swiper.params.effect !== "fade")
        return;
      swiper.fadeEffect.setTranslate();
    },
    setTransition: function setTransition9(swiper, duration) {
      if (swiper.params.effect !== "fade")
        return;
      swiper.fadeEffect.setTransition(duration);
    }
  }
};

// node_modules/swiper/esm/components/effect-cube/effect-cube.js
function _extends19() {
  _extends19 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends19.apply(this, arguments);
}
var Cube = {
  setTranslate: function setTranslate11() {
    var swiper = this;
    var $el = swiper.$el, $wrapperEl = swiper.$wrapperEl, slides = swiper.slides, swiperWidth = swiper.width, swiperHeight = swiper.height, rtl = swiper.rtlTranslate, swiperSize = swiper.size, browser2 = swiper.browser;
    var params = swiper.params.cubeEffect;
    var isHorizontal = swiper.isHorizontal();
    var isVirtual = swiper.virtual && swiper.params.virtual.enabled;
    var wrapperRotate = 0;
    var $cubeShadowEl;
    if (params.shadow) {
      if (isHorizontal) {
        $cubeShadowEl = $wrapperEl.find(".swiper-cube-shadow");
        if ($cubeShadowEl.length === 0) {
          $cubeShadowEl = dom_default('<div class="swiper-cube-shadow"></div>');
          $wrapperEl.append($cubeShadowEl);
        }
        $cubeShadowEl.css({
          height: swiperWidth + "px"
        });
      } else {
        $cubeShadowEl = $el.find(".swiper-cube-shadow");
        if ($cubeShadowEl.length === 0) {
          $cubeShadowEl = dom_default('<div class="swiper-cube-shadow"></div>');
          $el.append($cubeShadowEl);
        }
      }
    }
    for (var i = 0; i < slides.length; i += 1) {
      var $slideEl = slides.eq(i);
      var slideIndex = i;
      if (isVirtual) {
        slideIndex = parseInt($slideEl.attr("data-swiper-slide-index"), 10);
      }
      var slideAngle = slideIndex * 90;
      var round = Math.floor(slideAngle / 360);
      if (rtl) {
        slideAngle = -slideAngle;
        round = Math.floor(-slideAngle / 360);
      }
      var progress = Math.max(Math.min($slideEl[0].progress, 1), -1);
      var tx = 0;
      var ty = 0;
      var tz = 0;
      if (slideIndex % 4 === 0) {
        tx = -round * 4 * swiperSize;
        tz = 0;
      } else if ((slideIndex - 1) % 4 === 0) {
        tx = 0;
        tz = -round * 4 * swiperSize;
      } else if ((slideIndex - 2) % 4 === 0) {
        tx = swiperSize + round * 4 * swiperSize;
        tz = swiperSize;
      } else if ((slideIndex - 3) % 4 === 0) {
        tx = -swiperSize;
        tz = 3 * swiperSize + swiperSize * 4 * round;
      }
      if (rtl) {
        tx = -tx;
      }
      if (!isHorizontal) {
        ty = tx;
        tx = 0;
      }
      var transform2 = "rotateX(" + (isHorizontal ? 0 : -slideAngle) + "deg) rotateY(" + (isHorizontal ? slideAngle : 0) + "deg) translate3d(" + tx + "px, " + ty + "px, " + tz + "px)";
      if (progress <= 1 && progress > -1) {
        wrapperRotate = slideIndex * 90 + progress * 90;
        if (rtl)
          wrapperRotate = -slideIndex * 90 - progress * 90;
      }
      $slideEl.transform(transform2);
      if (params.slideShadows) {
        var shadowBefore = isHorizontal ? $slideEl.find(".swiper-slide-shadow-left") : $slideEl.find(".swiper-slide-shadow-top");
        var shadowAfter = isHorizontal ? $slideEl.find(".swiper-slide-shadow-right") : $slideEl.find(".swiper-slide-shadow-bottom");
        if (shadowBefore.length === 0) {
          shadowBefore = dom_default('<div class="swiper-slide-shadow-' + (isHorizontal ? "left" : "top") + '"></div>');
          $slideEl.append(shadowBefore);
        }
        if (shadowAfter.length === 0) {
          shadowAfter = dom_default('<div class="swiper-slide-shadow-' + (isHorizontal ? "right" : "bottom") + '"></div>');
          $slideEl.append(shadowAfter);
        }
        if (shadowBefore.length)
          shadowBefore[0].style.opacity = Math.max(-progress, 0);
        if (shadowAfter.length)
          shadowAfter[0].style.opacity = Math.max(progress, 0);
      }
    }
    $wrapperEl.css({
      "-webkit-transform-origin": "50% 50% -" + swiperSize / 2 + "px",
      "-moz-transform-origin": "50% 50% -" + swiperSize / 2 + "px",
      "-ms-transform-origin": "50% 50% -" + swiperSize / 2 + "px",
      "transform-origin": "50% 50% -" + swiperSize / 2 + "px"
    });
    if (params.shadow) {
      if (isHorizontal) {
        $cubeShadowEl.transform("translate3d(0px, " + (swiperWidth / 2 + params.shadowOffset) + "px, " + -swiperWidth / 2 + "px) rotateX(90deg) rotateZ(0deg) scale(" + params.shadowScale + ")");
      } else {
        var shadowAngle = Math.abs(wrapperRotate) - Math.floor(Math.abs(wrapperRotate) / 90) * 90;
        var multiplier = 1.5 - (Math.sin(shadowAngle * 2 * Math.PI / 360) / 2 + Math.cos(shadowAngle * 2 * Math.PI / 360) / 2);
        var scale1 = params.shadowScale;
        var scale2 = params.shadowScale / multiplier;
        var offset2 = params.shadowOffset;
        $cubeShadowEl.transform("scale3d(" + scale1 + ", 1, " + scale2 + ") translate3d(0px, " + (swiperHeight / 2 + offset2) + "px, " + -swiperHeight / 2 / scale2 + "px) rotateX(-90deg)");
      }
    }
    var zFactor = browser2.isSafari || browser2.isWebView ? -swiperSize / 2 : 0;
    $wrapperEl.transform("translate3d(0px,0," + zFactor + "px) rotateX(" + (swiper.isHorizontal() ? 0 : wrapperRotate) + "deg) rotateY(" + (swiper.isHorizontal() ? -wrapperRotate : 0) + "deg)");
  },
  setTransition: function setTransition10(duration) {
    var swiper = this;
    var $el = swiper.$el, slides = swiper.slides;
    slides.transition(duration).find(".swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left").transition(duration);
    if (swiper.params.cubeEffect.shadow && !swiper.isHorizontal()) {
      $el.find(".swiper-cube-shadow").transition(duration);
    }
  }
};
var effect_cube_default = {
  name: "effect-cube",
  params: {
    cubeEffect: {
      slideShadows: true,
      shadow: true,
      shadowOffset: 20,
      shadowScale: 0.94
    }
  },
  create: function create54() {
    var swiper = this;
    bindModuleMethods(swiper, {
      cubeEffect: _extends19({}, Cube)
    });
  },
  on: {
    beforeInit: function beforeInit5(swiper) {
      if (swiper.params.effect !== "cube")
        return;
      swiper.classNames.push(swiper.params.containerModifierClass + "cube");
      swiper.classNames.push(swiper.params.containerModifierClass + "3d");
      var overwriteParams = {
        slidesPerView: 1,
        slidesPerColumn: 1,
        slidesPerGroup: 1,
        watchSlidesProgress: true,
        resistanceRatio: 0,
        spaceBetween: 0,
        centeredSlides: false,
        virtualTranslate: true
      };
      extend2(swiper.params, overwriteParams);
      extend2(swiper.originalParams, overwriteParams);
    },
    setTranslate: function setTranslate12(swiper) {
      if (swiper.params.effect !== "cube")
        return;
      swiper.cubeEffect.setTranslate();
    },
    setTransition: function setTransition11(swiper, duration) {
      if (swiper.params.effect !== "cube")
        return;
      swiper.cubeEffect.setTransition(duration);
    }
  }
};

// node_modules/swiper/esm/components/effect-flip/effect-flip.js
function _extends20() {
  _extends20 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends20.apply(this, arguments);
}
var Flip = {
  setTranslate: function setTranslate13() {
    var swiper = this;
    var slides = swiper.slides, rtl = swiper.rtlTranslate;
    for (var i = 0; i < slides.length; i += 1) {
      var $slideEl = slides.eq(i);
      var progress = $slideEl[0].progress;
      if (swiper.params.flipEffect.limitRotation) {
        progress = Math.max(Math.min($slideEl[0].progress, 1), -1);
      }
      var offset2 = $slideEl[0].swiperSlideOffset;
      var rotate = -180 * progress;
      var rotateY = rotate;
      var rotateX = 0;
      var tx = -offset2;
      var ty = 0;
      if (!swiper.isHorizontal()) {
        ty = tx;
        tx = 0;
        rotateX = -rotateY;
        rotateY = 0;
      } else if (rtl) {
        rotateY = -rotateY;
      }
      $slideEl[0].style.zIndex = -Math.abs(Math.round(progress)) + slides.length;
      if (swiper.params.flipEffect.slideShadows) {
        var shadowBefore = swiper.isHorizontal() ? $slideEl.find(".swiper-slide-shadow-left") : $slideEl.find(".swiper-slide-shadow-top");
        var shadowAfter = swiper.isHorizontal() ? $slideEl.find(".swiper-slide-shadow-right") : $slideEl.find(".swiper-slide-shadow-bottom");
        if (shadowBefore.length === 0) {
          shadowBefore = dom_default('<div class="swiper-slide-shadow-' + (swiper.isHorizontal() ? "left" : "top") + '"></div>');
          $slideEl.append(shadowBefore);
        }
        if (shadowAfter.length === 0) {
          shadowAfter = dom_default('<div class="swiper-slide-shadow-' + (swiper.isHorizontal() ? "right" : "bottom") + '"></div>');
          $slideEl.append(shadowAfter);
        }
        if (shadowBefore.length)
          shadowBefore[0].style.opacity = Math.max(-progress, 0);
        if (shadowAfter.length)
          shadowAfter[0].style.opacity = Math.max(progress, 0);
      }
      $slideEl.transform("translate3d(" + tx + "px, " + ty + "px, 0px) rotateX(" + rotateX + "deg) rotateY(" + rotateY + "deg)");
    }
  },
  setTransition: function setTransition12(duration) {
    var swiper = this;
    var slides = swiper.slides, activeIndex = swiper.activeIndex, $wrapperEl = swiper.$wrapperEl;
    slides.transition(duration).find(".swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left").transition(duration);
    if (swiper.params.virtualTranslate && duration !== 0) {
      var eventTriggered = false;
      slides.eq(activeIndex).transitionEnd(function onTransitionEnd3() {
        if (eventTriggered)
          return;
        if (!swiper || swiper.destroyed)
          return;
        eventTriggered = true;
        swiper.animating = false;
        var triggerEvents = ["webkitTransitionEnd", "transitionend"];
        for (var i = 0; i < triggerEvents.length; i += 1) {
          $wrapperEl.trigger(triggerEvents[i]);
        }
      });
    }
  }
};
var effect_flip_default = {
  name: "effect-flip",
  params: {
    flipEffect: {
      slideShadows: true,
      limitRotation: true
    }
  },
  create: function create55() {
    var swiper = this;
    bindModuleMethods(swiper, {
      flipEffect: _extends20({}, Flip)
    });
  },
  on: {
    beforeInit: function beforeInit6(swiper) {
      if (swiper.params.effect !== "flip")
        return;
      swiper.classNames.push(swiper.params.containerModifierClass + "flip");
      swiper.classNames.push(swiper.params.containerModifierClass + "3d");
      var overwriteParams = {
        slidesPerView: 1,
        slidesPerColumn: 1,
        slidesPerGroup: 1,
        watchSlidesProgress: true,
        spaceBetween: 0,
        virtualTranslate: true
      };
      extend2(swiper.params, overwriteParams);
      extend2(swiper.originalParams, overwriteParams);
    },
    setTranslate: function setTranslate14(swiper) {
      if (swiper.params.effect !== "flip")
        return;
      swiper.flipEffect.setTranslate();
    },
    setTransition: function setTransition13(swiper, duration) {
      if (swiper.params.effect !== "flip")
        return;
      swiper.flipEffect.setTransition(duration);
    }
  }
};

// node_modules/swiper/esm/components/effect-coverflow/effect-coverflow.js
function _extends21() {
  _extends21 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends21.apply(this, arguments);
}
var Coverflow = {
  setTranslate: function setTranslate15() {
    var swiper = this;
    var swiperWidth = swiper.width, swiperHeight = swiper.height, slides = swiper.slides, slidesSizesGrid = swiper.slidesSizesGrid;
    var params = swiper.params.coverflowEffect;
    var isHorizontal = swiper.isHorizontal();
    var transform2 = swiper.translate;
    var center = isHorizontal ? -transform2 + swiperWidth / 2 : -transform2 + swiperHeight / 2;
    var rotate = isHorizontal ? params.rotate : -params.rotate;
    var translate = params.depth;
    for (var i = 0, length = slides.length; i < length; i += 1) {
      var $slideEl = slides.eq(i);
      var slideSize = slidesSizesGrid[i];
      var slideOffset = $slideEl[0].swiperSlideOffset;
      var offsetMultiplier = (center - slideOffset - slideSize / 2) / slideSize * params.modifier;
      var rotateY = isHorizontal ? rotate * offsetMultiplier : 0;
      var rotateX = isHorizontal ? 0 : rotate * offsetMultiplier;
      var translateZ = -translate * Math.abs(offsetMultiplier);
      var stretch = params.stretch;
      if (typeof stretch === "string" && stretch.indexOf("%") !== -1) {
        stretch = parseFloat(params.stretch) / 100 * slideSize;
      }
      var translateY = isHorizontal ? 0 : stretch * offsetMultiplier;
      var translateX = isHorizontal ? stretch * offsetMultiplier : 0;
      var scale = 1 - (1 - params.scale) * Math.abs(offsetMultiplier);
      if (Math.abs(translateX) < 1e-3)
        translateX = 0;
      if (Math.abs(translateY) < 1e-3)
        translateY = 0;
      if (Math.abs(translateZ) < 1e-3)
        translateZ = 0;
      if (Math.abs(rotateY) < 1e-3)
        rotateY = 0;
      if (Math.abs(rotateX) < 1e-3)
        rotateX = 0;
      if (Math.abs(scale) < 1e-3)
        scale = 0;
      var slideTransform = "translate3d(" + translateX + "px," + translateY + "px," + translateZ + "px)  rotateX(" + rotateX + "deg) rotateY(" + rotateY + "deg) scale(" + scale + ")";
      $slideEl.transform(slideTransform);
      $slideEl[0].style.zIndex = -Math.abs(Math.round(offsetMultiplier)) + 1;
      if (params.slideShadows) {
        var $shadowBeforeEl = isHorizontal ? $slideEl.find(".swiper-slide-shadow-left") : $slideEl.find(".swiper-slide-shadow-top");
        var $shadowAfterEl = isHorizontal ? $slideEl.find(".swiper-slide-shadow-right") : $slideEl.find(".swiper-slide-shadow-bottom");
        if ($shadowBeforeEl.length === 0) {
          $shadowBeforeEl = dom_default('<div class="swiper-slide-shadow-' + (isHorizontal ? "left" : "top") + '"></div>');
          $slideEl.append($shadowBeforeEl);
        }
        if ($shadowAfterEl.length === 0) {
          $shadowAfterEl = dom_default('<div class="swiper-slide-shadow-' + (isHorizontal ? "right" : "bottom") + '"></div>');
          $slideEl.append($shadowAfterEl);
        }
        if ($shadowBeforeEl.length)
          $shadowBeforeEl[0].style.opacity = offsetMultiplier > 0 ? offsetMultiplier : 0;
        if ($shadowAfterEl.length)
          $shadowAfterEl[0].style.opacity = -offsetMultiplier > 0 ? -offsetMultiplier : 0;
      }
    }
  },
  setTransition: function setTransition14(duration) {
    var swiper = this;
    swiper.slides.transition(duration).find(".swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left").transition(duration);
  }
};
var effect_coverflow_default = {
  name: "effect-coverflow",
  params: {
    coverflowEffect: {
      rotate: 50,
      stretch: 0,
      depth: 100,
      scale: 1,
      modifier: 1,
      slideShadows: true
    }
  },
  create: function create56() {
    var swiper = this;
    bindModuleMethods(swiper, {
      coverflowEffect: _extends21({}, Coverflow)
    });
  },
  on: {
    beforeInit: function beforeInit7(swiper) {
      if (swiper.params.effect !== "coverflow")
        return;
      swiper.classNames.push(swiper.params.containerModifierClass + "coverflow");
      swiper.classNames.push(swiper.params.containerModifierClass + "3d");
      swiper.params.watchSlidesProgress = true;
      swiper.originalParams.watchSlidesProgress = true;
    },
    setTranslate: function setTranslate16(swiper) {
      if (swiper.params.effect !== "coverflow")
        return;
      swiper.coverflowEffect.setTranslate();
    },
    setTransition: function setTransition15(swiper, duration) {
      if (swiper.params.effect !== "coverflow")
        return;
      swiper.coverflowEffect.setTransition(duration);
    }
  }
};

// node_modules/swiper/esm/components/thumbs/thumbs.js
function _extends22() {
  _extends22 = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends22.apply(this, arguments);
}
var Thumbs = {
  init: function init33() {
    var swiper = this;
    var thumbsParams = swiper.params.thumbs;
    if (swiper.thumbs.initialized)
      return false;
    swiper.thumbs.initialized = true;
    var SwiperClass = swiper.constructor;
    if (thumbsParams.swiper instanceof SwiperClass) {
      swiper.thumbs.swiper = thumbsParams.swiper;
      extend2(swiper.thumbs.swiper.originalParams, {
        watchSlidesProgress: true,
        slideToClickedSlide: false
      });
      extend2(swiper.thumbs.swiper.params, {
        watchSlidesProgress: true,
        slideToClickedSlide: false
      });
    } else if (isObject(thumbsParams.swiper)) {
      swiper.thumbs.swiper = new SwiperClass(extend2({}, thumbsParams.swiper, {
        watchSlidesVisibility: true,
        watchSlidesProgress: true,
        slideToClickedSlide: false
      }));
      swiper.thumbs.swiperCreated = true;
    }
    swiper.thumbs.swiper.$el.addClass(swiper.params.thumbs.thumbsContainerClass);
    swiper.thumbs.swiper.on("tap", swiper.thumbs.onThumbClick);
    return true;
  },
  onThumbClick: function onThumbClick() {
    var swiper = this;
    var thumbsSwiper = swiper.thumbs.swiper;
    if (!thumbsSwiper)
      return;
    var clickedIndex = thumbsSwiper.clickedIndex;
    var clickedSlide = thumbsSwiper.clickedSlide;
    if (clickedSlide && dom_default(clickedSlide).hasClass(swiper.params.thumbs.slideThumbActiveClass))
      return;
    if (typeof clickedIndex === "undefined" || clickedIndex === null)
      return;
    var slideToIndex;
    if (thumbsSwiper.params.loop) {
      slideToIndex = parseInt(dom_default(thumbsSwiper.clickedSlide).attr("data-swiper-slide-index"), 10);
    } else {
      slideToIndex = clickedIndex;
    }
    if (swiper.params.loop) {
      var currentIndex = swiper.activeIndex;
      if (swiper.slides.eq(currentIndex).hasClass(swiper.params.slideDuplicateClass)) {
        swiper.loopFix();
        swiper._clientLeft = swiper.$wrapperEl[0].clientLeft;
        currentIndex = swiper.activeIndex;
      }
      var prevIndex = swiper.slides.eq(currentIndex).prevAll('[data-swiper-slide-index="' + slideToIndex + '"]').eq(0).index();
      var nextIndex = swiper.slides.eq(currentIndex).nextAll('[data-swiper-slide-index="' + slideToIndex + '"]').eq(0).index();
      if (typeof prevIndex === "undefined")
        slideToIndex = nextIndex;
      else if (typeof nextIndex === "undefined")
        slideToIndex = prevIndex;
      else if (nextIndex - currentIndex < currentIndex - prevIndex)
        slideToIndex = nextIndex;
      else
        slideToIndex = prevIndex;
    }
    swiper.slideTo(slideToIndex);
  },
  update: function update7(initial) {
    var swiper = this;
    var thumbsSwiper = swiper.thumbs.swiper;
    if (!thumbsSwiper)
      return;
    var slidesPerView = thumbsSwiper.params.slidesPerView === "auto" ? thumbsSwiper.slidesPerViewDynamic() : thumbsSwiper.params.slidesPerView;
    var autoScrollOffset = swiper.params.thumbs.autoScrollOffset;
    var useOffset = autoScrollOffset && !thumbsSwiper.params.loop;
    if (swiper.realIndex !== thumbsSwiper.realIndex || useOffset) {
      var currentThumbsIndex = thumbsSwiper.activeIndex;
      var newThumbsIndex;
      var direction;
      if (thumbsSwiper.params.loop) {
        if (thumbsSwiper.slides.eq(currentThumbsIndex).hasClass(thumbsSwiper.params.slideDuplicateClass)) {
          thumbsSwiper.loopFix();
          thumbsSwiper._clientLeft = thumbsSwiper.$wrapperEl[0].clientLeft;
          currentThumbsIndex = thumbsSwiper.activeIndex;
        }
        var prevThumbsIndex = thumbsSwiper.slides.eq(currentThumbsIndex).prevAll('[data-swiper-slide-index="' + swiper.realIndex + '"]').eq(0).index();
        var nextThumbsIndex = thumbsSwiper.slides.eq(currentThumbsIndex).nextAll('[data-swiper-slide-index="' + swiper.realIndex + '"]').eq(0).index();
        if (typeof prevThumbsIndex === "undefined") {
          newThumbsIndex = nextThumbsIndex;
        } else if (typeof nextThumbsIndex === "undefined") {
          newThumbsIndex = prevThumbsIndex;
        } else if (nextThumbsIndex - currentThumbsIndex === currentThumbsIndex - prevThumbsIndex) {
          newThumbsIndex = thumbsSwiper.params.slidesPerGroup > 1 ? nextThumbsIndex : currentThumbsIndex;
        } else if (nextThumbsIndex - currentThumbsIndex < currentThumbsIndex - prevThumbsIndex) {
          newThumbsIndex = nextThumbsIndex;
        } else {
          newThumbsIndex = prevThumbsIndex;
        }
        direction = swiper.activeIndex > swiper.previousIndex ? "next" : "prev";
      } else {
        newThumbsIndex = swiper.realIndex;
        direction = newThumbsIndex > swiper.previousIndex ? "next" : "prev";
      }
      if (useOffset) {
        newThumbsIndex += direction === "next" ? autoScrollOffset : -1 * autoScrollOffset;
      }
      if (thumbsSwiper.visibleSlidesIndexes && thumbsSwiper.visibleSlidesIndexes.indexOf(newThumbsIndex) < 0) {
        if (thumbsSwiper.params.centeredSlides) {
          if (newThumbsIndex > currentThumbsIndex) {
            newThumbsIndex = newThumbsIndex - Math.floor(slidesPerView / 2) + 1;
          } else {
            newThumbsIndex = newThumbsIndex + Math.floor(slidesPerView / 2) - 1;
          }
        } else if (newThumbsIndex > currentThumbsIndex && thumbsSwiper.params.slidesPerGroup === 1) {
        }
        thumbsSwiper.slideTo(newThumbsIndex, initial ? 0 : void 0);
      }
    }
    var thumbsToActivate = 1;
    var thumbActiveClass = swiper.params.thumbs.slideThumbActiveClass;
    if (swiper.params.slidesPerView > 1 && !swiper.params.centeredSlides) {
      thumbsToActivate = swiper.params.slidesPerView;
    }
    if (!swiper.params.thumbs.multipleActiveThumbs) {
      thumbsToActivate = 1;
    }
    thumbsToActivate = Math.floor(thumbsToActivate);
    thumbsSwiper.slides.removeClass(thumbActiveClass);
    if (thumbsSwiper.params.loop || thumbsSwiper.params.virtual && thumbsSwiper.params.virtual.enabled) {
      for (var i = 0; i < thumbsToActivate; i += 1) {
        thumbsSwiper.$wrapperEl.children('[data-swiper-slide-index="' + (swiper.realIndex + i) + '"]').addClass(thumbActiveClass);
      }
    } else {
      for (var _i = 0; _i < thumbsToActivate; _i += 1) {
        thumbsSwiper.slides.eq(swiper.realIndex + _i).addClass(thumbActiveClass);
      }
    }
  }
};
var thumbs_default = {
  name: "thumbs",
  params: {
    thumbs: {
      swiper: null,
      multipleActiveThumbs: true,
      autoScrollOffset: 0,
      slideThumbActiveClass: "swiper-slide-thumb-active",
      thumbsContainerClass: "swiper-container-thumbs"
    }
  },
  create: function create57() {
    var swiper = this;
    bindModuleMethods(swiper, {
      thumbs: _extends22({
        swiper: null,
        initialized: false
      }, Thumbs)
    });
  },
  on: {
    beforeInit: function beforeInit8(swiper) {
      var thumbs = swiper.params.thumbs;
      if (!thumbs || !thumbs.swiper)
        return;
      swiper.thumbs.init();
      swiper.thumbs.update(true);
    },
    slideChange: function slideChange5(swiper) {
      if (!swiper.thumbs.swiper)
        return;
      swiper.thumbs.update();
    },
    update: function update8(swiper) {
      if (!swiper.thumbs.swiper)
        return;
      swiper.thumbs.update();
    },
    resize: function resize3(swiper) {
      if (!swiper.thumbs.swiper)
        return;
      swiper.thumbs.update();
    },
    observerUpdate: function observerUpdate3(swiper) {
      if (!swiper.thumbs.swiper)
        return;
      swiper.thumbs.update();
    },
    setTransition: function setTransition16(swiper, duration) {
      var thumbsSwiper = swiper.thumbs.swiper;
      if (!thumbsSwiper)
        return;
      thumbsSwiper.setTransition(duration);
    },
    beforeDestroy: function beforeDestroy(swiper) {
      var thumbsSwiper = swiper.thumbs.swiper;
      if (!thumbsSwiper)
        return;
      if (swiper.thumbs.swiperCreated && thumbsSwiper) {
        thumbsSwiper.destroy();
      }
    }
  }
};

// node_modules/swiper/swiper-bundle.esm.js
var components = [virtual_default, keyboard_default, mousewheel_default, navigation_default, pagination_default, scrollbar_default, parallax_default, zoom_default, lazy_default2, controller_default, a11y_default, history_default2, hash_navigation_default, autoplay_default, effect_fade_default, effect_cube_default, effect_flip_default, effect_coverflow_default, thumbs_default];
core_class_default.use(components);

// node_modules/framework7/esm/components/swiper/swiper.js
function initSwiper(swiperEl) {
  var app = this;
  var $swiperEl = dom7_default(swiperEl);
  if ($swiperEl.length === 0)
    return;
  if ($swiperEl[0].swiper)
    return;
  var initialSlide;
  var params = {};
  var isTabs;
  var isRoutableTabs;
  if ($swiperEl.hasClass("tabs-swipeable-wrap")) {
    $swiperEl.addClass("swiper-container").children(".tabs").addClass("swiper-wrapper").children(".tab").addClass("swiper-slide");
    initialSlide = $swiperEl.children(".tabs").children(".tab-active").index();
    isTabs = true;
    isRoutableTabs = $swiperEl.find(".tabs-routable").length > 0;
  }
  if ($swiperEl.attr("data-swiper")) {
    params = JSON.parse($swiperEl.attr("data-swiper"));
  } else if ($swiperEl[0].f7SwiperParams) {
    params = $swiperEl[0].f7SwiperParams;
  } else {
    params = $swiperEl.dataset();
    Object.keys(params).forEach(function(key) {
      var value = params[key];
      if (typeof value === "string" && value.indexOf("{") === 0 && value.indexOf("}") > 0) {
        try {
          params[key] = JSON.parse(value);
        } catch (e) {
        }
      }
    });
  }
  if (typeof params.initialSlide === "undefined" && typeof initialSlide !== "undefined") {
    params.initialSlide = initialSlide;
  }
  var swiper = app.swiper.create($swiperEl[0], params);
  function updateSwiper() {
    swiper.update();
  }
  var $tabEl = $swiperEl.parents(".tab").filter(function(tabEl) {
    return dom7_default(tabEl).parent(".tabs").parent(".tabs-animated-wrap, .tabs-swipeable-wrap").length === 0;
  }).eq(0);
  $swiperEl.parents(".popup, .login-screen, .sheet-modal, .popover").on("modal:open", updateSwiper);
  $swiperEl.parents(".panel").on("panel:open", updateSwiper);
  if ($tabEl && $tabEl.length) {
    $tabEl.on("tab:show", updateSwiper);
  }
  swiper.on("beforeDestroy", function() {
    $swiperEl.parents(".popup, .login-screen, .sheet-modal, .popover").off("modal:open", updateSwiper);
    $swiperEl.parents(".panel").off("panel:open", updateSwiper);
    if ($tabEl && $tabEl.length) {
      $tabEl.off("tab:show", updateSwiper);
    }
  });
  if (isTabs) {
    swiper.on("slideChange", function() {
      if (isRoutableTabs) {
        var view = app.views.get($swiperEl.parents(".view"));
        if (!view)
          view = app.views.main;
        var router = view.router;
        var tabRouteUrl = router.findTabRouteUrl(swiper.slides.eq(swiper.activeIndex)[0]);
        if (tabRouteUrl) {
          setTimeout(function() {
            router.navigate(tabRouteUrl);
          }, 0);
        }
      } else {
        app.tab.show({
          tabEl: swiper.slides.eq(swiper.activeIndex)
        });
      }
    });
  }
}
var swiper_default = {
  name: "swiper",
  static: {
    Swiper: core_class_default
  },
  create: function create58() {
    var app = this;
    app.swiper = ConstructorMethods({
      defaultSelector: ".swiper-container",
      constructor: core_class_default,
      domProp: "swiper"
    });
  },
  on: {
    pageBeforeRemove: function pageBeforeRemove15(page) {
      var app = this;
      page.$el.find(".swiper-init, .tabs-swipeable-wrap").each(function(swiperEl) {
        app.swiper.destroy(swiperEl);
      });
    },
    pageMounted: function pageMounted(page) {
      var app = this;
      page.$el.find(".tabs-swipeable-wrap").each(function(swiperEl) {
        initSwiper.call(app, swiperEl);
      });
    },
    pageInit: function pageInit18(page) {
      var app = this;
      page.$el.find(".swiper-init, .tabs-swipeable-wrap").each(function(swiperEl) {
        initSwiper.call(app, swiperEl);
      });
    },
    pageReinit: function pageReinit(page) {
      var app = this;
      page.$el.find(".swiper-init, .tabs-swipeable-wrap").each(function(swiperEl) {
        var swiper = app.swiper.get(swiperEl);
        if (swiper && swiper.update)
          swiper.update();
      });
    },
    tabMounted: function tabMounted17(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".swiper-init, .tabs-swipeable-wrap").each(function(swiperEl) {
        initSwiper.call(app, swiperEl);
      });
    },
    tabShow: function tabShow(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".swiper-init, .tabs-swipeable-wrap").each(function(swiperEl) {
        var swiper = app.swiper.get(swiperEl);
        if (swiper && swiper.update)
          swiper.update();
      });
    },
    tabBeforeRemove: function tabBeforeRemove14(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".swiper-init, .tabs-swipeable-wrap").each(function(swiperEl) {
        app.swiper.destroy(swiperEl);
      });
    }
  },
  vnode: {
    "swiper-init": {
      insert: function insert13(vnode) {
        var app = this;
        var swiperEl = vnode.elm;
        initSwiper.call(app, swiperEl);
      },
      destroy: function destroy33(vnode) {
        var app = this;
        var swiperEl = vnode.elm;
        app.swiper.destroy(swiperEl);
      }
    },
    "tabs-swipeable-wrap": {
      insert: function insert14(vnode) {
        var app = this;
        var swiperEl = vnode.elm;
        initSwiper.call(app, swiperEl);
      },
      destroy: function destroy34(vnode) {
        var app = this;
        var swiperEl = vnode.elm;
        app.swiper.destroy(swiperEl);
      }
    }
  }
};

// node_modules/framework7/esm/components/photo-browser/photo-browser-class.js
function _defineProperties5(target, props) {
  for (var i = 0; i < props.length; i++) {
    var descriptor = props[i];
    descriptor.enumerable = descriptor.enumerable || false;
    descriptor.configurable = true;
    if ("value" in descriptor)
      descriptor.writable = true;
    Object.defineProperty(target, descriptor.key, descriptor);
  }
}
function _createClass5(Constructor, protoProps, staticProps) {
  if (protoProps)
    _defineProperties5(Constructor.prototype, protoProps);
  if (staticProps)
    _defineProperties5(Constructor, staticProps);
  return Constructor;
}
function _assertThisInitialized22(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose22(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf22(subClass, superClass);
}
function _setPrototypeOf22(o, p) {
  _setPrototypeOf22 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf22(o, p);
}
var PhotoBrowser = function(_Framework7Class) {
  _inheritsLoose22(PhotoBrowser2, _Framework7Class);
  function PhotoBrowser2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var pb = _assertThisInitialized22(_this);
    pb.app = app;
    var defaults = extend({
      on: {}
    }, app.params.photoBrowser);
    pb.useModulesParams(defaults);
    pb.params = extend(defaults, params);
    extend(pb, {
      exposed: false,
      opened: false,
      activeIndex: pb.params.swiper.initialSlide,
      url: pb.params.url,
      swipeToClose: {
        allow: true,
        isTouched: false,
        diff: void 0,
        start: void 0,
        current: void 0,
        started: false,
        activeSlide: void 0,
        timeStart: void 0
      }
    });
    pb.useModules();
    pb.init();
    return _this;
  }
  var _proto = PhotoBrowser2.prototype;
  _proto.onSlideChange = function onSlideChange(swiper) {
    var pb = this;
    pb.activeIndex = swiper.activeIndex;
    var current = swiper.activeIndex + 1;
    var total = pb.params.virtualSlides ? pb.params.photos.length : swiper.slides.length;
    if (swiper.params.loop) {
      total -= 2;
      current -= swiper.loopedSlides;
      if (current < 1)
        current = total + current;
      if (current > total)
        current -= total;
    }
    var $activeSlideEl = pb.params.virtualSlides ? swiper.$wrapperEl.find('.swiper-slide[data-swiper-slide-index="' + swiper.activeIndex + '"]') : swiper.slides.eq(swiper.activeIndex);
    var $previousSlideEl = pb.params.virtualSlides ? swiper.$wrapperEl.find('.swiper-slide[data-swiper-slide-index="' + swiper.previousIndex + '"]') : swiper.slides.eq(swiper.previousIndex);
    var $currentEl = pb.$el.find(".photo-browser-current");
    var $totalEl = pb.$el.find(".photo-browser-total");
    var navbarEl;
    if (pb.params.type === "page" && pb.params.navbar && $currentEl.length === 0 && pb.app.theme === "ios") {
      navbarEl = pb.app.navbar.getElByPage(pb.$el);
      if (navbarEl) {
        $currentEl = dom7_default(navbarEl).find(".photo-browser-current");
        $totalEl = dom7_default(navbarEl).find(".photo-browser-total");
      }
    }
    if ($currentEl.length && $totalEl.length) {
      $currentEl.text(current);
      $totalEl.text(total);
      if (!navbarEl)
        navbarEl = $currentEl.parents(".navbar")[0];
      if (navbarEl) {
        pb.app.navbar.size(navbarEl);
      }
    }
    if (pb.captions.length > 0) {
      var captionIndex = swiper.params.loop ? $activeSlideEl.attr("data-swiper-slide-index") : pb.activeIndex;
      pb.$captionsContainerEl.find(".photo-browser-caption-active").removeClass("photo-browser-caption-active");
      pb.$captionsContainerEl.find('[data-caption-index="' + captionIndex + '"]').addClass("photo-browser-caption-active");
    }
    var previousSlideVideo = $previousSlideEl.find("video");
    if (previousSlideVideo.length > 0) {
      if ("pause" in previousSlideVideo[0])
        previousSlideVideo[0].pause();
    }
  };
  _proto.onTouchStart = function onTouchStart3() {
    var pb = this;
    var swipeToClose = pb.swipeToClose;
    if (!swipeToClose.allow)
      return;
    swipeToClose.isTouched = true;
  };
  _proto.onTouchMove = function onTouchMove3(e) {
    var pb = this;
    var swipeToClose = pb.swipeToClose;
    if (!swipeToClose.isTouched)
      return;
    if (!swipeToClose.started) {
      swipeToClose.started = true;
      swipeToClose.start = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
      if (pb.params.virtualSlides) {
        swipeToClose.activeSlide = pb.swiper.$wrapperEl.children(".swiper-slide-active");
      } else {
        swipeToClose.activeSlide = pb.swiper.slides.eq(pb.swiper.activeIndex);
      }
      swipeToClose.timeStart = now();
    }
    e.preventDefault();
    swipeToClose.current = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
    swipeToClose.diff = swipeToClose.start - swipeToClose.current;
    pb.$el.transition(0).transform("translate3d(0," + -swipeToClose.diff + "px,0)");
  };
  _proto.onTouchEnd = function onTouchEnd3() {
    var pb = this;
    var swipeToClose = pb.swipeToClose;
    swipeToClose.isTouched = false;
    if (!swipeToClose.started) {
      swipeToClose.started = false;
      return;
    }
    swipeToClose.started = false;
    swipeToClose.allow = false;
    var diff = Math.abs(swipeToClose.diff);
    var timeDiff = new Date().getTime() - swipeToClose.timeStart;
    if (timeDiff < 300 && diff > 20 || timeDiff >= 300 && diff > 100) {
      nextTick(function() {
        if (pb.$el) {
          if (swipeToClose.diff < 0)
            pb.$el.addClass("swipe-close-to-bottom");
          else
            pb.$el.addClass("swipe-close-to-top");
        }
        pb.emit("local::swipeToClose", pb);
        pb.$el.transform("").transition("");
        pb.close();
        swipeToClose.allow = true;
      });
      return;
    }
    if (diff !== 0) {
      pb.$el.addClass("photo-browser-transitioning").transitionEnd(function() {
        swipeToClose.allow = true;
        pb.$el.removeClass("photo-browser-transitioning");
      });
    } else {
      swipeToClose.allow = true;
    }
    nextTick(function() {
      pb.$el.transform("").transition("");
    });
  };
  _proto.renderNavbar = function renderNavbar() {
    var pb = this;
    if (pb.params.renderNavbar)
      return pb.params.renderNavbar.call(pb);
    var iconsColor = pb.params.iconsColor;
    if (!pb.params.iconsColor && pb.params.theme === "dark")
      iconsColor = "white";
    var pageBackLinkText = (pb.app.theme === "ios" || pb.app.theme === "aurora") && pb.params.pageBackLinkText ? pb.params.pageBackLinkText : "";
    var renderNavbarCount = typeof pb.params.navbarShowCount === "undefined" ? pb.params.photos.length > 1 : pb.params.navbarShowCount;
    var isPopup = pb.params.type !== "page";
    return jsx_default2("div", {
      class: "navbar navbar-photo-browser " + (pb.params.theme === "dark" ? "navbar-photo-browser-dark" : "")
    }, jsx_default2("div", {
      class: "navbar-bg"
    }), jsx_default2("div", {
      class: "navbar-inner navbar-inner-centered-title sliding"
    }, !isPopup && jsx_default2("div", {
      class: "left"
    }, jsx_default2("a", {
      class: "link " + (!pageBackLinkText ? "icon-only" : "") + " back"
    }, jsx_default2("i", {
      class: "icon icon-back " + (iconsColor ? "color-" + iconsColor : "")
    }), pageBackLinkText && jsx_default2("span", null, pageBackLinkText))), renderNavbarCount && jsx_default2("div", {
      class: "title"
    }, jsx_default2("span", {
      class: "photo-browser-current"
    }), jsx_default2("span", {
      class: "photo-browser-of"
    }, pb.params.navbarOfText), jsx_default2("span", {
      class: "photo-browser-total"
    })), isPopup && jsx_default2("div", {
      class: "right"
    }, jsx_default2("a", {
      class: "link popup-close",
      "data-popup": ".photo-browser-popup"
    }, jsx_default2("span", null, pb.params.popupCloseLinkText)))));
  };
  _proto.renderToolbar = function renderToolbar() {
    var pb = this;
    if (pb.params.renderToolbar)
      return pb.params.renderToolbar.call(pb);
    var iconsColor = pb.params.iconsColor;
    if (!pb.params.iconsColor && pb.params.theme === "dark")
      iconsColor = "white";
    return jsx_default2("div", {
      class: "toolbar toolbar-bottom tabbar"
    }, jsx_default2("div", {
      class: "toolbar-inner"
    }, jsx_default2("a", {
      class: "link photo-browser-prev"
    }, jsx_default2("i", {
      class: "icon icon-back " + (iconsColor ? "color-" + iconsColor : "")
    })), jsx_default2("a", {
      class: "link photo-browser-next"
    }, jsx_default2("i", {
      class: "icon icon-forward " + (iconsColor ? "color-" + iconsColor : "")
    }))));
  };
  _proto.renderCaption = function renderCaption(caption, index2) {
    var pb = this;
    if (pb.params.renderCaption)
      return pb.params.renderCaption.call(pb, caption, index2);
    return jsx_default2("div", {
      class: "photo-browser-caption",
      "data-caption-index": index2
    }, caption);
  };
  _proto.renderObject = function renderObject(photo, index2) {
    var pb = this;
    if (pb.params.renderObject)
      return pb.params.renderObject.call(pb, photo, index2);
    return jsx_default2("div", {
      class: "photo-browser-slide photo-browser-object-slide swiper-slide",
      "data-swiper-slide-index": index2
    }, photo.html ? photo.html : photo);
  };
  _proto.renderLazyPhoto = function renderLazyPhoto(photo, index2) {
    var pb = this;
    if (pb.params.renderLazyPhoto)
      return pb.params.renderLazyPhoto.call(pb, photo, index2);
    return jsx_default2("div", {
      class: "photo-browser-slide photo-browser-slide-lazy swiper-slide",
      "data-swiper-slide-index": index2
    }, jsx_default2("div", {
      class: "swiper-lazy-preloader"
    }), jsx_default2("span", {
      class: "swiper-zoom-container"
    }, jsx_default2("img", {
      "data-src": photo.url ? photo.url : photo,
      class: "swiper-lazy"
    })));
  };
  _proto.renderPhoto = function renderPhoto(photo, index2) {
    var pb = this;
    if (pb.params.renderPhoto)
      return pb.params.renderPhoto.call(pb, photo, index2);
    return jsx_default2("div", {
      class: "photo-browser-slide swiper-slide",
      "data-swiper-slide-index": index2
    }, jsx_default2("span", {
      class: "swiper-zoom-container"
    }, jsx_default2("img", {
      src: photo.url ? photo.url : photo
    })));
  };
  _proto.render = function render15() {
    var pb = this;
    if (pb.params.render)
      return pb.params.render.call(pb, pb.params);
    return jsx_default2("div", {
      class: "photo-browser photo-browser-" + pb.params.theme
    }, jsx_default2("div", {
      class: "view"
    }, jsx_default2("div", {
      class: "page photo-browser-page photo-browser-page-" + pb.params.theme + " no-toolbar " + (!pb.params.navbar ? "no-navbar" : ""),
      "data-name": "photo-browser-page"
    }, pb.params.navbar && pb.renderNavbar(), pb.params.toolbar && pb.renderToolbar(), jsx_default2("div", {
      class: "photo-browser-captions photo-browser-captions-" + (pb.params.captionsTheme || pb.params.theme)
    }, pb.params.photos.map(function(photo, index2) {
      if (photo.caption)
        return pb.renderCaption(photo.caption, index2);
      return "";
    })), jsx_default2("div", {
      class: "photo-browser-swiper-container swiper-container"
    }, jsx_default2("div", {
      class: "photo-browser-swiper-wrapper swiper-wrapper"
    }, !pb.params.virtualSlides && pb.params.photos.map(function(photo, index2) {
      if (photo.html || (typeof photo === "string" || photo instanceof String) && photo.indexOf("<") >= 0 && photo.indexOf(">") >= 0) {
        return pb.renderObject(photo, index2);
      }
      if (pb.params.swiper.lazy === true || pb.params.swiper.lazy && pb.params.swiper.lazy.enabled) {
        return pb.renderLazyPhoto(photo, index2);
      }
      return pb.renderPhoto(photo, index2);
    }))))));
  };
  _proto.renderStandalone = function renderStandalone() {
    var pb = this;
    if (pb.params.renderStandalone)
      return pb.params.renderStandalone.call(pb);
    var standaloneHtml = '<div class="popup photo-browser-popup photo-browser-standalone popup-tablet-fullscreen">' + pb.render() + "</div>";
    return standaloneHtml;
  };
  _proto.renderPage = function renderPage() {
    var pb = this;
    if (pb.params.renderPage)
      return pb.params.renderPage.call(pb);
    var pageHtml = pb.render();
    return pageHtml;
  };
  _proto.renderPopup = function renderPopup() {
    var pb = this;
    if (pb.params.renderPopup)
      return pb.params.renderPopup.call(pb);
    var popupHtml = '<div class="popup photo-browser-popup">' + pb.render() + "</div>";
    return popupHtml;
  };
  _proto.onOpen = function onOpen(type, el) {
    var pb = this;
    var app = pb.app;
    var $el = dom7_default(el);
    $el[0].f7PhotoBrowser = pb;
    pb.$el = $el;
    pb.el = $el[0];
    pb.openedIn = type;
    pb.opened = true;
    pb.$swiperContainerEl = pb.$el.find(".photo-browser-swiper-container");
    pb.$swiperWrapperEl = pb.$el.find(".photo-browser-swiper-wrapper");
    pb.slides = pb.$el.find(".photo-browser-slide");
    pb.$captionsContainerEl = pb.$el.find(".photo-browser-captions");
    pb.captions = pb.$el.find(".photo-browser-caption");
    var clickTimeout;
    var swiperParams = extend({}, pb.params.swiper, {
      initialSlide: pb.activeIndex,
      on: {
        click: function click4(e) {
          clearTimeout(clickTimeout);
          if (pb.params.exposition) {
            clickTimeout = setTimeout(function() {
              pb.expositionToggle();
            }, 350);
          }
          pb.emit("local::tap", e);
          pb.emit("local::click", e);
        },
        doubleClick: function doubleClick(e) {
          clearTimeout(clickTimeout);
          pb.emit("local::doubleTap", e);
          pb.emit("local::doubleClick", e);
        },
        slideChange: function slideChange6() {
          var swiper = this;
          pb.onSlideChange(swiper);
          for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
            args[_key] = arguments[_key];
          }
          pb.emit.apply(pb, ["local::slideChange"].concat(args));
        },
        transitionStart: function transitionStart3() {
          for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
            args[_key2] = arguments[_key2];
          }
          pb.emit.apply(pb, ["local::transitionStart"].concat(args));
        },
        transitionEnd: function transitionEnd5() {
          for (var _len3 = arguments.length, args = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
            args[_key3] = arguments[_key3];
          }
          pb.emit.apply(pb, ["local::transitionEnd"].concat(args));
        },
        slideChangeTransitionStart: function slideChangeTransitionStart() {
          var swiper = this;
          pb.onSlideChange(swiper);
          for (var _len4 = arguments.length, args = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
            args[_key4] = arguments[_key4];
          }
          pb.emit.apply(pb, ["local::slideChangeTransitionStart"].concat(args));
        },
        slideChangeTransitionEnd: function slideChangeTransitionEnd() {
          for (var _len5 = arguments.length, args = new Array(_len5), _key5 = 0; _key5 < _len5; _key5++) {
            args[_key5] = arguments[_key5];
          }
          pb.emit.apply(pb, ["local::slideChangeTransitionEnd"].concat(args));
        },
        lazyImageLoad: function lazyImageLoad() {
          for (var _len6 = arguments.length, args = new Array(_len6), _key6 = 0; _key6 < _len6; _key6++) {
            args[_key6] = arguments[_key6];
          }
          pb.emit.apply(pb, ["local::lazyImageLoad"].concat(args));
        },
        lazyImageReady: function lazyImageReady() {
          for (var _len7 = arguments.length, args = new Array(_len7), _key7 = 0; _key7 < _len7; _key7++) {
            args[_key7] = arguments[_key7];
          }
          var slideEl = args[0];
          dom7_default(slideEl).removeClass("photo-browser-slide-lazy");
          pb.emit.apply(pb, ["local::lazyImageReady"].concat(args));
        }
      }
    });
    if (pb.params.swipeToClose && pb.params.type !== "page") {
      extend(swiperParams.on, {
        touchStart: function touchStart2(swiper, e) {
          pb.onTouchStart(e);
          pb.emit("local::touchStart", e);
        },
        touchMoveOpposite: function touchMoveOpposite(swiper, e) {
          pb.onTouchMove(e);
          pb.emit("local::touchMoveOpposite", e);
        },
        touchEnd: function touchEnd3(swiper, e) {
          pb.onTouchEnd(e);
          pb.emit("local::touchEnd", e);
        }
      });
    }
    if (pb.params.virtualSlides) {
      extend(swiperParams, {
        virtual: {
          slides: pb.params.photos,
          renderSlide: function renderSlide2(photo, index2) {
            if (photo.html || (typeof photo === "string" || photo instanceof String) && photo.indexOf("<") >= 0 && photo.indexOf(">") >= 0) {
              return pb.renderObject(photo, index2);
            }
            if (pb.params.swiper.lazy === true || pb.params.swiper.lazy && pb.params.swiper.lazy.enabled) {
              return pb.renderLazyPhoto(photo, index2);
            }
            return pb.renderPhoto(photo, index2);
          }
        }
      });
    }
    var window2 = getWindow();
    pb.swiper = app.swiper ? app.swiper.create(pb.$swiperContainerEl, swiperParams) : new window2.Swiper(pb.$swiperContainerEl, swiperParams);
    if (pb.activeIndex === 0) {
      pb.onSlideChange(pb.swiper);
    }
    if (pb.$el) {
      pb.$el.trigger("photobrowser:open");
    }
    pb.emit("local::open photoBrowserOpen", pb);
  };
  _proto.onOpened = function onOpened() {
    var pb = this;
    if (pb.$el && pb.params.type === "standalone") {
      pb.$el.css("animation", "none");
    }
    if (pb.$el) {
      pb.$el.trigger("photobrowser:opened");
    }
    pb.emit("local::opened photoBrowserOpened", pb);
  };
  _proto.onClose = function onClose() {
    var pb = this;
    if (pb.destroyed)
      return;
    if (pb.swiper && pb.swiper.destroy) {
      pb.swiper.destroy(true, false);
      pb.swiper = null;
      delete pb.swiper;
    }
    if (pb.$el) {
      pb.$el.trigger("photobrowser:close");
    }
    pb.emit("local::close photoBrowserClose", pb);
  };
  _proto.onClosed = function onClosed() {
    var pb = this;
    if (pb.destroyed)
      return;
    pb.opened = false;
    pb.$el = null;
    pb.el = null;
    delete pb.$el;
    delete pb.el;
    if (pb.$el) {
      pb.$el.trigger("photobrowser:closed");
    }
    pb.emit("local::closed photoBrowserClosed", pb);
  };
  _proto.openPage = function openPage() {
    var pb = this;
    if (pb.opened)
      return pb;
    var pageHtml = pb.renderPage();
    pb.view.router.navigate({
      url: pb.url,
      route: {
        content: pageHtml,
        path: pb.url,
        on: {
          pageBeforeIn: function pageBeforeIn2(e, page) {
            pb.view.$el.addClass("with-photo-browser-page with-photo-browser-page-" + pb.params.theme);
            pb.onOpen("page", page.el);
          },
          pageAfterIn: function pageAfterIn2(e, page) {
            pb.onOpened("page", page.el);
          },
          pageBeforeOut: function pageBeforeOut(e, page) {
            pb.view.$el.removeClass("with-photo-browser-page with-photo-browser-page-exposed with-photo-browser-page-" + pb.params.theme);
            pb.onClose("page", page.el);
          },
          pageAfterOut: function pageAfterOut(e, page) {
            pb.onClosed("page", page.el);
          }
        }
      }
    });
    return pb;
  };
  _proto.openStandalone = function openStandalone() {
    var pb = this;
    if (pb.opened)
      return pb;
    var standaloneHtml = pb.renderStandalone();
    var popupParams = {
      backdrop: false,
      content: standaloneHtml,
      on: {
        popupOpen: function popupOpen(popup) {
          pb.onOpen("popup", popup.el);
        },
        popupOpened: function popupOpened(popup) {
          pb.onOpened("popup", popup.el);
        },
        popupClose: function popupClose(popup) {
          pb.onClose("popup", popup.el);
        },
        popupClosed: function popupClosed(popup) {
          pb.onClosed("popup", popup.el);
        }
      }
    };
    if (pb.params.routableModals && pb.view) {
      pb.view.router.navigate({
        url: pb.url,
        route: {
          path: pb.url,
          popup: popupParams
        }
      });
    } else {
      pb.modal = pb.app.popup.create(popupParams).open();
    }
    return pb;
  };
  _proto.openPopup = function openPopup2() {
    var pb = this;
    if (pb.opened)
      return pb;
    var popupHtml = pb.renderPopup();
    var popupParams = {
      content: popupHtml,
      push: pb.params.popupPush,
      on: {
        popupOpen: function popupOpen(popup) {
          pb.onOpen("popup", popup.el);
        },
        popupOpened: function popupOpened(popup) {
          pb.onOpened("popup", popup.el);
        },
        popupClose: function popupClose(popup) {
          pb.onClose("popup", popup.el);
        },
        popupClosed: function popupClosed(popup) {
          pb.onClosed("popup", popup.el);
        }
      }
    };
    if (pb.params.routableModals && pb.view) {
      pb.view.router.navigate({
        url: pb.url,
        route: {
          path: pb.url,
          popup: popupParams
        }
      });
    } else {
      pb.modal = pb.app.popup.create(popupParams).open();
    }
    return pb;
  };
  _proto.expositionEnable = function expositionEnable() {
    var pb = this;
    if (pb.params.type === "page") {
      pb.view.$el.addClass("with-photo-browser-page-exposed");
    }
    if (pb.$el)
      pb.$el.addClass("photo-browser-exposed");
    if (pb.params.expositionHideCaptions)
      pb.$captionsContainerEl.addClass("photo-browser-captions-exposed");
    pb.exposed = true;
    return pb;
  };
  _proto.expositionDisable = function expositionDisable() {
    var pb = this;
    if (pb.params.type === "page") {
      pb.view.$el.removeClass("with-photo-browser-page-exposed");
    }
    if (pb.$el)
      pb.$el.removeClass("photo-browser-exposed");
    if (pb.params.expositionHideCaptions)
      pb.$captionsContainerEl.removeClass("photo-browser-captions-exposed");
    pb.exposed = false;
    return pb;
  };
  _proto.expositionToggle = function expositionToggle() {
    var pb = this;
    if (pb.params.type === "page") {
      pb.view.$el.toggleClass("with-photo-browser-page-exposed");
    }
    if (pb.$el)
      pb.$el.toggleClass("photo-browser-exposed");
    if (pb.params.expositionHideCaptions)
      pb.$captionsContainerEl.toggleClass("photo-browser-captions-exposed");
    pb.exposed = !pb.exposed;
    return pb;
  };
  _proto.open = function open12(index2) {
    var pb = this;
    var type = pb.params.type;
    if (pb.opened) {
      if (pb.swiper && typeof index2 !== "undefined") {
        pb.swiper.slideTo(parseInt(index2, 10));
      }
      return pb;
    }
    if (typeof index2 !== "undefined") {
      pb.activeIndex = index2;
    }
    if (type === "standalone") {
      pb.openStandalone();
    }
    if (type === "page") {
      pb.openPage();
    }
    if (type === "popup") {
      pb.openPopup();
    }
    return pb;
  };
  _proto.close = function close12() {
    var pb = this;
    if (!pb.opened)
      return pb;
    if (pb.params.routableModals && pb.view || pb.openedIn === "page") {
      pb.view.router.back();
    } else {
      pb.modal.once("modalClosed", function() {
        nextTick(function() {
          if (pb.destroyed)
            return;
          pb.modal.destroy();
          delete pb.modal;
        });
      });
      pb.modal.close();
    }
    return pb;
  };
  _proto.init = function init46() {
  };
  _proto.destroy = function destroy50() {
    var pb = this;
    pb.emit("local::beforeDestroy photoBrowserBeforeDestroy", pb);
    if (pb.$el) {
      pb.$el.trigger("photobrowser:beforedestroy");
      pb.$el[0].f7PhotoBrowser = null;
      delete pb.$el[0].f7PhotoBrowser;
    }
    deleteProps(pb);
    pb.destroyed = true;
    pb = null;
  };
  _createClass5(PhotoBrowser2, [{
    key: "view",
    get: function get2() {
      var params = this.params, app = this.app;
      return params.view || app.views.main;
    }
  }]);
  return PhotoBrowser2;
}(class_default);
var photo_browser_class_default = PhotoBrowser;

// node_modules/framework7/esm/components/photo-browser/photo-browser.js
var photo_browser_default = {
  name: "photoBrowser",
  params: {
    photoBrowser: {
      photos: [],
      exposition: true,
      expositionHideCaptions: false,
      type: "standalone",
      navbar: true,
      toolbar: true,
      theme: "light",
      captionsTheme: void 0,
      iconsColor: void 0,
      popupPush: false,
      swipeToClose: true,
      pageBackLinkText: "Back",
      popupCloseLinkText: "Close",
      navbarOfText: "of",
      navbarShowCount: void 0,
      view: void 0,
      url: "photos/",
      routableModals: false,
      virtualSlides: true,
      renderNavbar: void 0,
      renderToolbar: void 0,
      renderCaption: void 0,
      renderObject: void 0,
      renderLazyPhoto: void 0,
      renderPhoto: void 0,
      renderPage: void 0,
      renderPopup: void 0,
      renderStandalone: void 0,
      swiper: {
        initialSlide: 0,
        spaceBetween: 20,
        speed: 300,
        loop: false,
        preloadImages: true,
        navigation: {
          nextEl: ".photo-browser-next",
          prevEl: ".photo-browser-prev"
        },
        zoom: {
          enabled: true,
          maxRatio: 3,
          minRatio: 1
        },
        lazy: {
          enabled: true
        }
      }
    }
  },
  create: function create59() {
    var app = this;
    app.photoBrowser = ConstructorMethods({
      defaultSelector: ".photo-browser-popup, .photo-browser-page",
      constructor: photo_browser_class_default,
      app,
      domProp: "f7PhotoBrowser"
    });
  },
  static: {
    PhotoBrowser: photo_browser_class_default
  }
};

// node_modules/framework7/esm/components/notification/notification-class.js
function _assertThisInitialized23(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose23(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf23(subClass, superClass);
}
function _setPrototypeOf23(o, p) {
  _setPrototypeOf23 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf23(o, p);
}
var Notification = function(_Modal) {
  _inheritsLoose23(Notification2, _Modal);
  function Notification2(app, params) {
    var _this;
    var extendedParams = extend({
      on: {}
    }, app.params.notification, params);
    _this = _Modal.call(this, app, extendedParams) || this;
    var notification = _assertThisInitialized23(_this);
    notification.app = app;
    notification.params = extendedParams;
    var _notification$params = notification.params, icon = _notification$params.icon, title = _notification$params.title, titleRightText = _notification$params.titleRightText, subtitle = _notification$params.subtitle, text2 = _notification$params.text, closeButton = _notification$params.closeButton, closeTimeout = _notification$params.closeTimeout, cssClass = _notification$params.cssClass, closeOnClick = _notification$params.closeOnClick;
    var $el;
    if (!notification.params.el) {
      var notificationHtml = notification.render({
        icon,
        title,
        titleRightText,
        subtitle,
        text: text2,
        closeButton,
        cssClass
      });
      $el = dom7_default(notificationHtml);
    } else {
      $el = dom7_default(notification.params.el);
    }
    if ($el && $el.length > 0 && $el[0].f7Modal) {
      return $el[0].f7Modal || _assertThisInitialized23(_this);
    }
    if ($el.length === 0) {
      return notification.destroy() || _assertThisInitialized23(_this);
    }
    extend(notification, {
      $el,
      el: $el[0],
      type: "notification"
    });
    $el[0].f7Modal = notification;
    if (closeButton) {
      $el.find(".notification-close-button").on("click", function() {
        notification.close();
      });
    }
    $el.on("click", function(e) {
      if (closeButton && dom7_default(e.target).closest(".notification-close-button").length) {
        return;
      }
      notification.emit("local::click notificationClick", notification);
      if (closeOnClick)
        notification.close();
    });
    notification.on("beforeDestroy", function() {
      $el.off("click");
    });
    var isTouched;
    var isMoved;
    var isScrolling;
    var touchesDiff;
    var touchStartTime;
    var notificationHeight;
    var touchesStart = {};
    function handleTouchStart(e) {
      if (isTouched)
        return;
      isTouched = true;
      isMoved = false;
      isScrolling = void 0;
      touchStartTime = now();
      touchesStart.x = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
      touchesStart.y = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
    }
    function handleTouchMove(e) {
      if (!isTouched)
        return;
      var pageX = e.type === "touchmove" ? e.targetTouches[0].pageX : e.pageX;
      var pageY = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
      if (typeof isScrolling === "undefined") {
        isScrolling = !!(isScrolling || Math.abs(pageY - touchesStart.y) < Math.abs(pageX - touchesStart.x));
      }
      if (isScrolling) {
        isTouched = false;
        return;
      }
      e.preventDefault();
      if (!isMoved) {
        notification.$el.removeClass("notification-transitioning");
        notification.$el.transition(0);
        notificationHeight = notification.$el[0].offsetHeight / 2;
      }
      isMoved = true;
      touchesDiff = pageY - touchesStart.y;
      var newTranslate = touchesDiff;
      if (touchesDiff > 0) {
        newTranslate = Math.pow(touchesDiff, 0.8);
      }
      notification.$el.transform("translate3d(0, " + newTranslate + "px, 0)");
    }
    function handleTouchEnd() {
      if (!isTouched || !isMoved) {
        isTouched = false;
        isMoved = false;
        return;
      }
      isTouched = false;
      isMoved = false;
      if (touchesDiff === 0) {
        return;
      }
      var timeDiff = now() - touchStartTime;
      notification.$el.transition("");
      notification.$el.addClass("notification-transitioning");
      notification.$el.transform("");
      if (touchesDiff < -10 && timeDiff < 300 || -touchesDiff >= notificationHeight / 1) {
        notification.close();
      }
    }
    function attachTouchEvents() {
      notification.$el.on(app.touchEvents.start, handleTouchStart, {
        passive: true
      });
      app.on("touchmove:active", handleTouchMove);
      app.on("touchend:passive", handleTouchEnd);
    }
    function detachTouchEvents() {
      notification.$el.off(app.touchEvents.start, handleTouchStart, {
        passive: true
      });
      app.off("touchmove:active", handleTouchMove);
      app.off("touchend:passive", handleTouchEnd);
    }
    var timeoutId;
    function closeOnTimeout() {
      timeoutId = nextTick(function() {
        if (isTouched && isMoved) {
          closeOnTimeout();
          return;
        }
        notification.close();
      }, closeTimeout);
    }
    notification.on("open", function() {
      if (notification.params.swipeToClose) {
        attachTouchEvents();
      }
      dom7_default(".notification.modal-in").each(function(openedEl) {
        var notificationInstance = app.notification.get(openedEl);
        if (openedEl !== notification.el && notificationInstance) {
          notificationInstance.close();
        }
      });
      if (closeTimeout) {
        closeOnTimeout();
      }
    });
    notification.on("close beforeDestroy", function() {
      if (notification.params.swipeToClose) {
        detachTouchEvents();
      }
      clearTimeout(timeoutId);
    });
    return notification || _assertThisInitialized23(_this);
  }
  var _proto = Notification2.prototype;
  _proto.render = function render15() {
    var notification = this;
    if (notification.params.render)
      return notification.params.render.call(notification, notification);
    var _notification$params2 = notification.params, icon = _notification$params2.icon, title = _notification$params2.title, titleRightText = _notification$params2.titleRightText, subtitle = _notification$params2.subtitle, text2 = _notification$params2.text, closeButton = _notification$params2.closeButton, cssClass = _notification$params2.cssClass;
    return jsx_default2("div", {
      class: "notification " + (cssClass || "")
    }, jsx_default2("div", {
      class: "notification-header"
    }, icon && jsx_default2("div", {
      class: "notification-icon"
    }, icon), title && jsx_default2("div", {
      class: "notification-title"
    }, title), titleRightText && jsx_default2("div", {
      class: "notification-title-right-text"
    }, titleRightText), closeButton && jsx_default2("span", {
      class: "notification-close-button"
    })), jsx_default2("div", {
      class: "notification-content"
    }, subtitle && jsx_default2("div", {
      class: "notification-subtitle"
    }, subtitle), text2 && jsx_default2("div", {
      class: "notification-text"
    }, text2)));
  };
  return Notification2;
}(modal_class_default);
var notification_class_default = Notification;

// node_modules/framework7/esm/components/notification/notification.js
var notification_default = {
  name: "notification",
  static: {
    Notification: notification_class_default
  },
  create: function create60() {
    var app = this;
    app.notification = extend({}, ModalMethods({
      app,
      constructor: notification_class_default,
      defaultSelector: ".notification.modal-in"
    }));
  },
  params: {
    notification: {
      icon: null,
      title: null,
      titleRightText: null,
      subtitle: null,
      text: null,
      closeButton: false,
      closeTimeout: null,
      closeOnClick: false,
      swipeToClose: true,
      cssClass: null,
      render: null,
      containerEl: null
    }
  }
};

// node_modules/framework7/esm/components/autocomplete/autocomplete-class.js
function _defineProperties6(target, props) {
  for (var i = 0; i < props.length; i++) {
    var descriptor = props[i];
    descriptor.enumerable = descriptor.enumerable || false;
    descriptor.configurable = true;
    if ("value" in descriptor)
      descriptor.writable = true;
    Object.defineProperty(target, descriptor.key, descriptor);
  }
}
function _createClass6(Constructor, protoProps, staticProps) {
  if (protoProps)
    _defineProperties6(Constructor.prototype, protoProps);
  if (staticProps)
    _defineProperties6(Constructor, staticProps);
  return Constructor;
}
function _assertThisInitialized24(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose24(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf24(subClass, superClass);
}
function _setPrototypeOf24(o, p) {
  _setPrototypeOf24 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf24(o, p);
}
var Autocomplete = function(_Framework7Class) {
  _inheritsLoose24(Autocomplete2, _Framework7Class);
  function Autocomplete2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var ac = _assertThisInitialized24(_this);
    ac.app = app;
    var device2 = getDevice();
    var defaults = extend({
      on: {}
    }, app.params.autocomplete);
    if (typeof defaults.searchbarDisableButton === "undefined") {
      defaults.searchbarDisableButton = app.theme !== "aurora";
    }
    ac.useModulesParams(defaults);
    ac.params = extend(defaults, params);
    var $openerEl;
    if (ac.params.openerEl) {
      $openerEl = dom7_default(ac.params.openerEl);
      if ($openerEl.length)
        $openerEl[0].f7Autocomplete = ac;
    }
    var $inputEl;
    if (ac.params.inputEl) {
      $inputEl = dom7_default(ac.params.inputEl);
      if ($inputEl.length)
        $inputEl[0].f7Autocomplete = ac;
    }
    var uniqueId = id();
    var url = params.url;
    if (!url && $openerEl && $openerEl.length) {
      if ($openerEl.attr("href"))
        url = $openerEl.attr("href");
      else if ($openerEl.find("a").length > 0) {
        url = $openerEl.find("a").attr("href");
      }
    }
    if (!url || url === "#" || url === "")
      url = ac.params.url;
    var inputType = ac.params.multiple ? "checkbox" : "radio";
    extend(ac, {
      $openerEl,
      openerEl: $openerEl && $openerEl[0],
      $inputEl,
      inputEl: $inputEl && $inputEl[0],
      id: uniqueId,
      url,
      value: ac.params.value || [],
      inputType,
      inputName: inputType + "-" + uniqueId,
      $modalEl: void 0,
      $dropdownEl: void 0
    });
    var previousQuery = "";
    function onInputChange() {
      var query = ac.$inputEl.val().trim();
      if (!ac.params.source)
        return;
      ac.params.source.call(ac, query, function(items) {
        var itemsHTML = "";
        var limit = ac.params.limit ? Math.min(ac.params.limit, items.length) : items.length;
        ac.items = items;
        var regExp;
        if (ac.params.highlightMatches) {
          query = query.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g, "\\$&");
          regExp = new RegExp("(" + query + ")", "i");
        }
        var firstValue;
        var firstItem;
        for (var i = 0; i < limit; i += 1) {
          var itemValue = typeof items[i] === "object" ? items[i][ac.params.valueProperty] : items[i];
          var itemText = typeof items[i] === "object" ? items[i][ac.params.textProperty] : items[i];
          if (i === 0) {
            firstValue = itemValue;
            firstItem = ac.items[i];
          }
          itemsHTML += ac.renderItem({
            value: itemValue,
            text: ac.params.highlightMatches ? itemText.replace(regExp, "<b>$1</b>") : itemText
          }, i);
        }
        if (itemsHTML === "" && query === "" && ac.params.dropdownPlaceholderText) {
          itemsHTML += ac.renderItem({
            placeholder: true,
            text: ac.params.dropdownPlaceholderText
          });
        }
        ac.$dropdownEl.find("ul").html(itemsHTML);
        if (ac.params.typeahead) {
          if (!firstValue || !firstItem) {
            return;
          }
          if (firstValue.toLowerCase().indexOf(query.toLowerCase()) !== 0) {
            return;
          }
          if (previousQuery.toLowerCase() === query.toLowerCase()) {
            ac.value = [];
            return;
          }
          if (previousQuery.toLowerCase().indexOf(query.toLowerCase()) === 0) {
            previousQuery = query;
            ac.value = [];
            return;
          }
          $inputEl.val(firstValue);
          $inputEl[0].setSelectionRange(query.length, firstValue.length);
          var previousValue = typeof ac.value[0] === "object" ? ac.value[0][ac.params.valueProperty] : ac.value[0];
          if (!previousValue || firstValue.toLowerCase() !== previousValue.toLowerCase()) {
            ac.value = [firstItem];
            ac.emit("local::change autocompleteChange", [firstItem]);
          }
        }
        previousQuery = query;
      });
    }
    function onPageInputChange() {
      var inputEl = this;
      var value = inputEl.value;
      var isValues = dom7_default(inputEl).parents(".autocomplete-values").length > 0;
      var item;
      var itemValue;
      var aValue;
      if (isValues) {
        if (ac.inputType === "checkbox" && !inputEl.checked) {
          for (var i = 0; i < ac.value.length; i += 1) {
            aValue = typeof ac.value[i] === "string" ? ac.value[i] : ac.value[i][ac.params.valueProperty];
            if (aValue === value || aValue * 1 === value * 1) {
              ac.value.splice(i, 1);
            }
          }
          ac.updateValues();
          ac.emit("local::change autocompleteChange", ac.value);
        }
        return;
      }
      for (var _i = 0; _i < ac.items.length; _i += 1) {
        itemValue = typeof ac.items[_i] === "object" ? ac.items[_i][ac.params.valueProperty] : ac.items[_i];
        if (itemValue === value || itemValue * 1 === value * 1)
          item = ac.items[_i];
      }
      if (ac.inputType === "radio") {
        ac.value = [item];
      } else if (inputEl.checked) {
        ac.value.push(item);
      } else {
        for (var _i2 = 0; _i2 < ac.value.length; _i2 += 1) {
          aValue = typeof ac.value[_i2] === "object" ? ac.value[_i2][ac.params.valueProperty] : ac.value[_i2];
          if (aValue === value || aValue * 1 === value * 1) {
            ac.value.splice(_i2, 1);
          }
        }
      }
      ac.updateValues();
      if (ac.inputType === "radio" && inputEl.checked || ac.inputType === "checkbox") {
        ac.emit("local::change autocompleteChange", ac.value);
      }
    }
    function onHtmlClick(e) {
      var $targetEl = dom7_default(e.target);
      if ($targetEl.is(ac.$inputEl[0]) || ac.$dropdownEl && $targetEl.closest(ac.$dropdownEl[0]).length)
        return;
      ac.close();
    }
    function onOpenerClick() {
      ac.open();
    }
    function onInputFocus() {
      ac.open();
    }
    function onInputBlur() {
      if (ac.$dropdownEl.find("label.active-state").length > 0)
        return;
      setTimeout(function() {
        ac.close();
      }, 0);
    }
    function onResize2() {
      ac.positionDropdown();
    }
    function onKeyDown(e) {
      if (!ac.opened)
        return;
      if (e.keyCode === 27) {
        e.preventDefault();
        ac.$inputEl.blur();
        return;
      }
      if (e.keyCode === 13) {
        var $selectedItemLabel = ac.$dropdownEl.find(".autocomplete-dropdown-selected label");
        if ($selectedItemLabel.length) {
          e.preventDefault();
          $selectedItemLabel.trigger("click");
          ac.$inputEl.blur();
          return;
        }
        if (ac.params.typeahead) {
          e.preventDefault();
          ac.$inputEl.blur();
        }
        return;
      }
      if (e.keyCode !== 40 && e.keyCode !== 38)
        return;
      e.preventDefault();
      var $selectedItem = ac.$dropdownEl.find(".autocomplete-dropdown-selected");
      var $newItem;
      if ($selectedItem.length) {
        $newItem = $selectedItem[e.keyCode === 40 ? "next" : "prev"]("li");
        if (!$newItem.length) {
          $newItem = ac.$dropdownEl.find("li").eq(e.keyCode === 40 ? 0 : ac.$dropdownEl.find("li").length - 1);
        }
      } else {
        $newItem = ac.$dropdownEl.find("li").eq(e.keyCode === 40 ? 0 : ac.$dropdownEl.find("li").length - 1);
      }
      if ($newItem.hasClass("autocomplete-dropdown-placeholder"))
        return;
      $selectedItem.removeClass("autocomplete-dropdown-selected");
      $newItem.addClass("autocomplete-dropdown-selected");
    }
    function onDropdownClick() {
      var $clickedEl = dom7_default(this);
      var clickedItem;
      for (var i = 0; i < ac.items.length; i += 1) {
        var itemValue = typeof ac.items[i] === "object" ? ac.items[i][ac.params.valueProperty] : ac.items[i];
        var value = $clickedEl.attr("data-value");
        if (itemValue === value || itemValue * 1 === value * 1) {
          clickedItem = ac.items[i];
        }
      }
      if (ac.params.updateInputValueOnSelect) {
        ac.$inputEl.val(typeof clickedItem === "object" ? clickedItem[ac.params.valueProperty] : clickedItem);
        ac.$inputEl.trigger("input change");
      }
      ac.value = [clickedItem];
      ac.emit("local::change autocompleteChange", [clickedItem]);
      ac.close();
    }
    ac.attachEvents = function attachEvents2() {
      if (ac.params.openIn !== "dropdown" && ac.$openerEl) {
        ac.$openerEl.on("click", onOpenerClick);
      }
      if (ac.params.openIn === "dropdown" && ac.$inputEl) {
        ac.$inputEl.on("focus", onInputFocus);
        ac.$inputEl.on(ac.params.inputEvents, onInputChange);
        if (device2.android) {
          dom7_default("html").on("click", onHtmlClick);
        } else {
          ac.$inputEl.on("blur", onInputBlur);
        }
        ac.$inputEl.on("keydown", onKeyDown);
      }
    };
    ac.detachEvents = function attachEvents2() {
      if (ac.params.openIn !== "dropdown" && ac.$openerEl) {
        ac.$openerEl.off("click", onOpenerClick);
      }
      if (ac.params.openIn === "dropdown" && ac.$inputEl) {
        ac.$inputEl.off("focus", onInputFocus);
        ac.$inputEl.off(ac.params.inputEvents, onInputChange);
        if (device2.android) {
          dom7_default("html").off("click", onHtmlClick);
        } else {
          ac.$inputEl.off("blur", onInputBlur);
        }
        ac.$inputEl.off("keydown", onKeyDown);
      }
    };
    ac.attachDropdownEvents = function attachDropdownEvents() {
      ac.$dropdownEl.on("click", "label", onDropdownClick);
      app.on("resize", onResize2);
    };
    ac.detachDropdownEvents = function detachDropdownEvents() {
      ac.$dropdownEl.off("click", "label", onDropdownClick);
      app.off("resize", onResize2);
    };
    ac.attachPageEvents = function attachPageEvents() {
      ac.$el.on("change", 'input[type="radio"], input[type="checkbox"]', onPageInputChange);
      if (ac.params.closeOnSelect && !ac.params.multiple) {
        ac.$el.once("click", ".list label", function() {
          nextTick(function() {
            ac.close();
          });
        });
      }
    };
    ac.detachPageEvents = function detachPageEvents() {
      ac.$el.off("change", 'input[type="radio"], input[type="checkbox"]', onPageInputChange);
    };
    ac.useModules();
    ac.init();
    return ac || _assertThisInitialized24(_this);
  }
  var _proto = Autocomplete2.prototype;
  _proto.positionDropdown = function positionDropdown() {
    var _$dropdownEl$children;
    var ac = this;
    var $inputEl = ac.$inputEl, app = ac.app, $dropdownEl = ac.$dropdownEl;
    var $pageContentEl = $inputEl.parents(".page-content");
    if ($pageContentEl.length === 0)
      return;
    var inputOffset = $inputEl.offset();
    var inputOffsetWidth = $inputEl[0].offsetWidth;
    var inputOffsetHeight = $inputEl[0].offsetHeight;
    var $listEl = $inputEl.parents(".list");
    var $listParent;
    $listEl.parents().each(function(parentEl) {
      if ($listParent)
        return;
      var $parentEl = dom7_default(parentEl);
      if ($parentEl.parent($pageContentEl).length)
        $listParent = $parentEl;
    });
    var listOffset = $listEl.offset();
    var paddingBottom = parseInt($pageContentEl.css("padding-bottom"), 10);
    var listOffsetLeft = $listEl.length > 0 ? listOffset.left - $pageContentEl.offset().left : 0;
    var inputOffsetLeft = inputOffset.left - ($listEl.length > 0 ? listOffset.left : 0) - (app.rtl ? 0 : 0);
    var inputOffsetTop = inputOffset.top - ($pageContentEl.offset().top - $pageContentEl[0].scrollTop);
    var maxHeight = $pageContentEl[0].scrollHeight - paddingBottom - (inputOffsetTop + $pageContentEl[0].scrollTop) - $inputEl[0].offsetHeight;
    var paddingProp = app.rtl ? "padding-right" : "padding-left";
    var paddingValue;
    if ($listEl.length && !ac.params.expandInput) {
      paddingValue = (app.rtl ? $listEl[0].offsetWidth - inputOffsetLeft - inputOffsetWidth : inputOffsetLeft) - (app.theme === "md" ? 16 : 15);
    }
    $dropdownEl.css({
      left: ($listEl.length > 0 ? listOffsetLeft : inputOffsetLeft) + "px",
      top: inputOffsetTop + $pageContentEl[0].scrollTop + inputOffsetHeight + "px",
      width: ($listEl.length > 0 ? $listEl[0].offsetWidth : inputOffsetWidth) + "px"
    });
    $dropdownEl.children(".autocomplete-dropdown-inner").css((_$dropdownEl$children = {
      maxHeight: maxHeight + "px"
    }, _$dropdownEl$children[paddingProp] = $listEl.length > 0 && !ac.params.expandInput ? paddingValue + "px" : "", _$dropdownEl$children));
  };
  _proto.focus = function focus2() {
    var ac = this;
    ac.$el.find("input[type=search]").focus();
  };
  _proto.source = function source(query) {
    var ac = this;
    if (!ac.params.source)
      return;
    var $el = ac.$el;
    ac.params.source.call(ac, query, function(items) {
      var itemsHTML = "";
      var limit = ac.params.limit ? Math.min(ac.params.limit, items.length) : items.length;
      ac.items = items;
      for (var i = 0; i < limit; i += 1) {
        var selected = false;
        var itemValue = typeof items[i] === "object" ? items[i][ac.params.valueProperty] : items[i];
        for (var j = 0; j < ac.value.length; j += 1) {
          var aValue = typeof ac.value[j] === "object" ? ac.value[j][ac.params.valueProperty] : ac.value[j];
          if (aValue === itemValue || aValue * 1 === itemValue * 1)
            selected = true;
        }
        itemsHTML += ac.renderItem({
          value: itemValue,
          text: typeof items[i] === "object" ? items[i][ac.params.textProperty] : items[i],
          inputType: ac.inputType,
          id: ac.id,
          inputName: ac.inputName,
          selected
        }, i);
      }
      $el.find(".autocomplete-found ul").html(itemsHTML);
      if (items.length === 0) {
        if (query.length !== 0) {
          $el.find(".autocomplete-not-found").show();
          $el.find(".autocomplete-found, .autocomplete-values").hide();
        } else {
          $el.find(".autocomplete-values").show();
          $el.find(".autocomplete-found, .autocomplete-not-found").hide();
        }
      } else {
        $el.find(".autocomplete-found").show();
        $el.find(".autocomplete-not-found, .autocomplete-values").hide();
      }
    });
  };
  _proto.updateValues = function updateValues() {
    var ac = this;
    var valuesHTML = "";
    for (var i = 0; i < ac.value.length; i += 1) {
      valuesHTML += ac.renderItem({
        value: typeof ac.value[i] === "object" ? ac.value[i][ac.params.valueProperty] : ac.value[i],
        text: typeof ac.value[i] === "object" ? ac.value[i][ac.params.textProperty] : ac.value[i],
        inputType: ac.inputType,
        id: ac.id,
        inputName: ac.inputName + "-checked}",
        selected: true
      }, i);
    }
    ac.$el.find(".autocomplete-values ul").html(valuesHTML);
  };
  _proto.preloaderHide = function preloaderHide() {
    var ac = this;
    if (ac.params.openIn === "dropdown" && ac.$dropdownEl) {
      ac.$dropdownEl.find(".autocomplete-preloader").removeClass("autocomplete-preloader-visible");
    } else {
      dom7_default(".autocomplete-preloader").removeClass("autocomplete-preloader-visible");
    }
  };
  _proto.preloaderShow = function preloaderShow() {
    var ac = this;
    if (ac.params.openIn === "dropdown" && ac.$dropdownEl) {
      ac.$dropdownEl.find(".autocomplete-preloader").addClass("autocomplete-preloader-visible");
    } else {
      dom7_default(".autocomplete-preloader").addClass("autocomplete-preloader-visible");
    }
  };
  _proto.renderPreloader = function renderPreloader() {
    var ac = this;
    var preloaders = {
      iosPreloaderContent,
      mdPreloaderContent,
      auroraPreloaderContent
    };
    return jsx_default2("div", {
      class: "autocomplete-preloader preloader " + (ac.params.preloaderColor ? "color-" + ac.params.preloaderColor : "")
    }, preloaders[ac.app.theme + "PreloaderContent"] || "");
  };
  _proto.renderSearchbar = function renderSearchbar() {
    var ac = this;
    if (ac.params.renderSearchbar)
      return ac.params.renderSearchbar.call(ac);
    return jsx_default2("form", {
      class: "searchbar"
    }, jsx_default2("div", {
      class: "searchbar-inner"
    }, jsx_default2("div", {
      class: "searchbar-input-wrap"
    }, jsx_default2("input", {
      type: "search",
      spellcheck: ac.params.searchbarSpellcheck || "false",
      placeholder: ac.params.searchbarPlaceholder
    }), jsx_default2("i", {
      class: "searchbar-icon"
    }), jsx_default2("span", {
      class: "input-clear-button"
    })), ac.params.searchbarDisableButton && jsx_default2("span", {
      class: "searchbar-disable-button"
    }, ac.params.searchbarDisableText)));
  };
  _proto.renderItem = function renderItem(item, index2) {
    var ac = this;
    if (ac.params.renderItem)
      return ac.params.renderItem.call(ac, item, index2);
    var itemValue = item.value && typeof item.value === "string" ? item.value.replace(/"/g, "&quot;") : item.value;
    if (ac.params.openIn !== "dropdown") {
      return jsx_default2("li", null, jsx_default2("label", {
        class: "item-" + item.inputType + " item-content"
      }, jsx_default2("input", {
        type: item.inputType,
        name: item.inputName,
        value: itemValue,
        _checked: item.selected
      }), jsx_default2("i", {
        class: "icon icon-" + item.inputType
      }), jsx_default2("div", {
        class: "item-inner"
      }, jsx_default2("div", {
        class: "item-title"
      }, item.text))));
    }
    if (!item.placeholder) {
      return jsx_default2("li", null, jsx_default2("label", {
        class: "item-radio item-content",
        "data-value": itemValue
      }, jsx_default2("div", {
        class: "item-inner"
      }, jsx_default2("div", {
        class: "item-title"
      }, item.text))));
    }
    return jsx_default2("li", {
      class: "autocomplete-dropdown-placeholder"
    }, jsx_default2("label", {
      class: "item-content"
    }, jsx_default2("div", {
      class: "item-inner"
    }, jsx_default2("div", {
      class: "item-title"
    }, item.text))));
  };
  _proto.renderNavbar = function renderNavbar() {
    var ac = this;
    if (ac.params.renderNavbar)
      return ac.params.renderNavbar.call(ac);
    var pageTitle = ac.params.pageTitle;
    if (typeof pageTitle === "undefined" && ac.$openerEl && ac.$openerEl.length) {
      pageTitle = ac.$openerEl.find(".item-title").text().trim();
    }
    var inPopup = ac.params.openIn === "popup";
    var navbarLeft = inPopup ? ac.params.preloader && jsx_default2("div", {
      class: "left"
    }, ac.renderPreloader()) : jsx_default2("div", {
      class: "left sliding"
    }, jsx_default2("a", {
      class: "link back"
    }, jsx_default2("i", {
      class: "icon icon-back"
    }), jsx_default2("span", {
      class: "if-not-md"
    }, ac.params.pageBackLinkText)));
    var navbarRight = inPopup ? jsx_default2("div", {
      class: "right"
    }, jsx_default2("a", {
      class: "link popup-close",
      "data-popup": ".autocomplete-popup"
    }, ac.params.popupCloseLinkText)) : ac.params.preloader && jsx_default2("div", {
      class: "right"
    }, ac.renderPreloader());
    return jsx_default2("div", {
      class: "navbar " + (ac.params.navbarColorTheme ? "color-" + ac.params.navbarColorTheme : "")
    }, jsx_default2("div", {
      class: "navbar-bg"
    }), jsx_default2("div", {
      class: "navbar-inner " + (ac.params.navbarColorTheme ? "color-" + ac.params.navbarColorTheme : "")
    }, navbarLeft, pageTitle && jsx_default2("div", {
      class: "title sliding"
    }, pageTitle), navbarRight, jsx_default2("div", {
      class: "subnavbar sliding"
    }, ac.renderSearchbar())));
  };
  _proto.renderDropdown = function renderDropdown() {
    var ac = this;
    if (ac.params.renderDropdown)
      return ac.params.renderDropdown.call(ac, ac.items);
    return jsx_default2("div", {
      class: "autocomplete-dropdown"
    }, jsx_default2("div", {
      class: "autocomplete-dropdown-inner"
    }, jsx_default2("div", {
      class: "list " + (!ac.params.expandInput ? "no-safe-areas" : "")
    }, jsx_default2("ul", null))), ac.params.preloader && ac.renderPreloader());
  };
  _proto.renderPage = function renderPage(inPopup) {
    var ac = this;
    if (ac.params.renderPage)
      return ac.params.renderPage.call(ac, ac.items);
    return jsx_default2("div", {
      class: "page page-with-subnavbar autocomplete-page",
      "data-name": "autocomplete-page"
    }, ac.renderNavbar(inPopup), jsx_default2("div", {
      class: "searchbar-backdrop"
    }), jsx_default2("div", {
      class: "page-content"
    }, jsx_default2("div", {
      class: "list autocomplete-list autocomplete-found autocomplete-list-" + ac.id + " " + (ac.params.formColorTheme ? "color-" + ac.params.formColorTheme : "")
    }, jsx_default2("ul", null)), jsx_default2("div", {
      class: "list autocomplete-not-found"
    }, jsx_default2("ul", null, jsx_default2("li", {
      class: "item-content"
    }, jsx_default2("div", {
      class: "item-inner"
    }, jsx_default2("div", {
      class: "item-title"
    }, ac.params.notFoundText))))), jsx_default2("div", {
      class: "list autocomplete-values"
    }, jsx_default2("ul", null))));
  };
  _proto.renderPopup = function renderPopup() {
    var ac = this;
    if (ac.params.renderPopup)
      return ac.params.renderPopup.call(ac, ac.items);
    return jsx_default2("div", {
      class: "popup autocomplete-popup"
    }, jsx_default2("div", {
      class: "view"
    }, ac.renderPage(true), ";"));
  };
  _proto.onOpen = function onOpen(type, el) {
    var ac = this;
    var app = ac.app;
    var $el = dom7_default(el);
    ac.$el = $el;
    ac.el = $el[0];
    ac.openedIn = type;
    ac.opened = true;
    if (ac.params.openIn === "dropdown") {
      ac.attachDropdownEvents();
      ac.$dropdownEl.addClass("autocomplete-dropdown-in");
      ac.$inputEl.trigger("input");
    } else {
      var $searchbarEl = $el.find(".searchbar");
      if (ac.params.openIn === "page" && app.theme === "ios" && $searchbarEl.length === 0) {
        $searchbarEl = dom7_default(app.navbar.getElByPage($el)).find(".searchbar");
      }
      ac.searchbar = app.searchbar.create({
        el: $searchbarEl,
        backdropEl: $el.find(".searchbar-backdrop"),
        customSearch: true,
        on: {
          search: function search(sb, query) {
            if (query.length === 0 && ac.searchbar.enabled) {
              ac.searchbar.backdropShow();
            } else {
              ac.searchbar.backdropHide();
            }
            ac.source(query);
          }
        }
      });
      ac.attachPageEvents();
      ac.updateValues();
      if (ac.params.requestSourceOnOpen)
        ac.source("");
    }
    ac.emit("local::open autocompleteOpen", ac);
  };
  _proto.autoFocus = function autoFocus() {
    var ac = this;
    if (ac.searchbar && ac.searchbar.$inputEl) {
      ac.searchbar.$inputEl.focus();
    }
    return ac;
  };
  _proto.onOpened = function onOpened() {
    var ac = this;
    if (ac.params.openIn !== "dropdown" && ac.params.autoFocus) {
      ac.autoFocus();
    }
    ac.emit("local::opened autocompleteOpened", ac);
  };
  _proto.onClose = function onClose() {
    var ac = this;
    if (ac.destroyed)
      return;
    if (ac.searchbar && ac.searchbar.destroy) {
      ac.searchbar.destroy();
      ac.searchbar = null;
      delete ac.searchbar;
    }
    if (ac.params.openIn === "dropdown") {
      ac.detachDropdownEvents();
      ac.$dropdownEl.removeClass("autocomplete-dropdown-in").remove();
      ac.$inputEl.parents(".item-content-dropdown-expanded").removeClass("item-content-dropdown-expanded");
    } else {
      ac.detachPageEvents();
    }
    ac.emit("local::close autocompleteClose", ac);
  };
  _proto.onClosed = function onClosed() {
    var ac = this;
    if (ac.destroyed)
      return;
    ac.opened = false;
    ac.$el = null;
    ac.el = null;
    delete ac.$el;
    delete ac.el;
    ac.emit("local::closed autocompleteClosed", ac);
  };
  _proto.openPage = function openPage() {
    var ac = this;
    if (ac.opened)
      return ac;
    var pageHtml = ac.renderPage();
    ac.view.router.navigate({
      url: ac.url,
      route: {
        content: pageHtml,
        path: ac.url,
        on: {
          pageBeforeIn: function pageBeforeIn2(e, page) {
            ac.onOpen("page", page.el);
          },
          pageAfterIn: function pageAfterIn2(e, page) {
            ac.onOpened("page", page.el);
          },
          pageBeforeOut: function pageBeforeOut(e, page) {
            ac.onClose("page", page.el);
          },
          pageAfterOut: function pageAfterOut(e, page) {
            ac.onClosed("page", page.el);
          }
        },
        options: {
          animate: ac.params.animate
        }
      }
    });
    return ac;
  };
  _proto.openPopup = function openPopup2() {
    var ac = this;
    if (ac.opened)
      return ac;
    var popupHtml = ac.renderPopup();
    var popupParams = {
      content: popupHtml,
      animate: ac.params.animate,
      push: ac.params.popupPush,
      swipeToClose: ac.params.popupSwipeToClose,
      on: {
        popupOpen: function popupOpen(popup) {
          ac.onOpen("popup", popup.el);
        },
        popupOpened: function popupOpened(popup) {
          ac.onOpened("popup", popup.el);
        },
        popupClose: function popupClose(popup) {
          ac.onClose("popup", popup.el);
        },
        popupClosed: function popupClosed(popup) {
          ac.onClosed("popup", popup.el);
        }
      }
    };
    if (ac.params.routableModals && ac.view) {
      ac.view.router.navigate({
        url: ac.url,
        route: {
          path: ac.url,
          popup: popupParams
        }
      });
    } else {
      ac.modal = ac.app.popup.create(popupParams).open(ac.params.animate);
    }
    return ac;
  };
  _proto.openDropdown = function openDropdown() {
    var ac = this;
    if (!ac.$dropdownEl) {
      ac.$dropdownEl = dom7_default(ac.renderDropdown());
    }
    var $listEl = ac.$inputEl.parents(".list");
    if ($listEl.length && ac.$inputEl.parents(".item-content").length > 0 && ac.params.expandInput) {
      ac.$inputEl.parents(".item-content").addClass("item-content-dropdown-expanded");
    }
    var $pageContentEl = ac.$inputEl.parents(".page-content");
    if (ac.params.dropdownContainerEl) {
      dom7_default(ac.params.dropdownContainerEl).append(ac.$dropdownEl);
    } else if ($pageContentEl.length === 0) {
      ac.$dropdownEl.insertAfter(ac.$inputEl);
    } else {
      ac.positionDropdown();
      $pageContentEl.append(ac.$dropdownEl);
    }
    ac.onOpen("dropdown", ac.$dropdownEl);
    ac.onOpened("dropdown", ac.$dropdownEl);
  };
  _proto.open = function open12() {
    var ac = this;
    if (ac.opened)
      return ac;
    var openIn = ac.params.openIn;
    ac["open" + openIn.split("").map(function(el, index2) {
      if (index2 === 0)
        return el.toUpperCase();
      return el;
    }).join("")]();
    return ac;
  };
  _proto.close = function close12() {
    var ac = this;
    if (!ac.opened)
      return ac;
    if (ac.params.openIn === "dropdown") {
      ac.onClose();
      ac.onClosed();
    } else if (ac.params.routableModals && ac.view || ac.openedIn === "page") {
      ac.view.router.back({
        animate: ac.params.animate
      });
    } else {
      ac.modal.once("modalClosed", function() {
        nextTick(function() {
          if (ac.destroyed)
            return;
          ac.modal.destroy();
          delete ac.modal;
        });
      });
      ac.modal.close();
    }
    return ac;
  };
  _proto.init = function init46() {
    var ac = this;
    ac.attachEvents();
  };
  _proto.destroy = function destroy50() {
    var ac = this;
    ac.emit("local::beforeDestroy autocompleteBeforeDestroy", ac);
    ac.detachEvents();
    if (ac.$inputEl && ac.$inputEl[0]) {
      delete ac.$inputEl[0].f7Autocomplete;
    }
    if (ac.$openerEl && ac.$openerEl[0]) {
      delete ac.$openerEl[0].f7Autocomplete;
    }
    deleteProps(ac);
    ac.destroyed = true;
  };
  _createClass6(Autocomplete2, [{
    key: "view",
    get: function get2() {
      var ac = this;
      var $openerEl = ac.$openerEl, $inputEl = ac.$inputEl, app = ac.app;
      var view;
      if (ac.params.view) {
        view = ac.params.view;
      } else if ($openerEl || $inputEl) {
        var $el = $openerEl || $inputEl;
        view = $el.closest(".view").length && $el.closest(".view")[0].f7View;
      }
      if (!view)
        view = app.views.main;
      return view;
    }
  }]);
  return Autocomplete2;
}(class_default);
var autocomplete_class_default = Autocomplete;

// node_modules/framework7/esm/components/autocomplete/autocomplete.js
var autocomplete_default = {
  name: "autocomplete",
  params: {
    autocomplete: {
      openerEl: void 0,
      inputEl: void 0,
      view: void 0,
      dropdownContainerEl: void 0,
      dropdownPlaceholderText: void 0,
      typeahead: false,
      highlightMatches: true,
      expandInput: false,
      updateInputValueOnSelect: true,
      inputEvents: "input",
      value: void 0,
      multiple: false,
      source: void 0,
      limit: void 0,
      valueProperty: "id",
      textProperty: "text",
      openIn: "page",
      pageBackLinkText: "Back",
      popupCloseLinkText: "Close",
      pageTitle: void 0,
      searchbarPlaceholder: "Search...",
      searchbarDisableText: "Cancel",
      searchbarDisableButton: void 0,
      searchbarSpellcheck: false,
      popupPush: false,
      popupSwipeToClose: void 0,
      animate: true,
      autoFocus: false,
      closeOnSelect: false,
      notFoundText: "Nothing found",
      requestSourceOnOpen: false,
      preloaderColor: void 0,
      preloader: false,
      formColorTheme: void 0,
      navbarColorTheme: void 0,
      routableModals: false,
      url: "select/",
      renderDropdown: void 0,
      renderPage: void 0,
      renderPopup: void 0,
      renderItem: void 0,
      renderSearchbar: void 0,
      renderNavbar: void 0
    }
  },
  static: {
    Autocomplete: autocomplete_class_default
  },
  create: function create61() {
    var app = this;
    app.autocomplete = extend(ConstructorMethods({
      defaultSelector: void 0,
      constructor: autocomplete_class_default,
      app,
      domProp: "f7Autocomplete"
    }), {
      open: function open12(autocompleteEl) {
        var ac = app.autocomplete.get(autocompleteEl);
        if (ac && ac.open)
          return ac.open();
        return void 0;
      },
      close: function close12(autocompleteEl) {
        var ac = app.autocomplete.get(autocompleteEl);
        if (ac && ac.close)
          return ac.close();
        return void 0;
      }
    });
  }
};

// node_modules/framework7/esm/components/tooltip/tooltip-class.js
function _assertThisInitialized25(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose25(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf25(subClass, superClass);
}
function _setPrototypeOf25(o, p) {
  _setPrototypeOf25 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf25(o, p);
}
var Tooltip = function(_Framework7Class) {
  _inheritsLoose25(Tooltip2, _Framework7Class);
  function Tooltip2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var tooltip = _assertThisInitialized25(_this);
    var support2 = getSupport();
    var defaults = extend({}, app.params.tooltip);
    var document = getDocument();
    tooltip.useModulesParams(defaults);
    tooltip.params = extend(defaults, params);
    if (typeof params.offset === "undefined" && support2.touch && tooltip.params.trigger === "hover") {
      tooltip.params.offset = 10;
    }
    var _tooltip$params = tooltip.params, targetEl = _tooltip$params.targetEl, containerEl = _tooltip$params.containerEl;
    if (!targetEl && !tooltip.params.delegated)
      return tooltip || _assertThisInitialized25(_this);
    var $targetEl = dom7_default(targetEl);
    if ($targetEl.length === 0 && !tooltip.params.delegated)
      return tooltip || _assertThisInitialized25(_this);
    if ($targetEl[0] && $targetEl[0].f7Tooltip && !tooltip.params.delegated)
      return $targetEl[0].f7Tooltip || _assertThisInitialized25(_this);
    var $containerEl = dom7_default(containerEl || app.$el).eq(0);
    if ($containerEl.length === 0) {
      $containerEl = app.$el;
    }
    var $el = dom7_default(tooltip.render()).eq(0);
    extend(tooltip, {
      app,
      $targetEl,
      targetEl: $targetEl && $targetEl[0],
      $containerEl,
      containerEl: $containerEl && $containerEl[0],
      $el,
      el: $el && $el[0],
      text: tooltip.params.text || "",
      visible: false,
      opened: false
    });
    if ($targetEl[0])
      $targetEl[0].f7Tooltip = tooltip;
    var touchesStart = {};
    var isTouched;
    function handleClick() {
      if (tooltip.opened)
        tooltip.hide();
      else
        tooltip.show(this);
    }
    function handleClickOut(e) {
      if (tooltip.opened && (dom7_default(e.target).closest($targetEl).length || dom7_default(e.target).closest(tooltip.$el).length))
        return;
      tooltip.hide();
    }
    function handleTouchStart(e) {
      if (isTouched)
        return;
      isTouched = true;
      touchesStart.x = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
      touchesStart.y = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
      tooltip.show(this);
    }
    function handleTouchMove(e) {
      if (!isTouched)
        return;
      var x = e.type === "touchmove" ? e.targetTouches[0].pageX : e.pageX;
      var y = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
      var distance = Math.pow(Math.pow(x - touchesStart.x, 2) + Math.pow(y - touchesStart.y, 2), 0.5);
      if (distance > 50) {
        isTouched = false;
        tooltip.hide();
      }
    }
    function handleTouchEnd() {
      if (!isTouched)
        return;
      isTouched = false;
      tooltip.hide();
    }
    function handleMouseEnter2() {
      tooltip.show(this);
    }
    function handleMouseLeave2() {
      tooltip.hide();
    }
    function handleTransitionEnd() {
      if (!$el.hasClass("tooltip-in")) {
        $el.removeClass("tooltip-out").remove();
      }
    }
    tooltip.attachEvents = function attachEvents2() {
      $el.on("transitionend", handleTransitionEnd);
      if (tooltip.params.trigger === "click") {
        if (tooltip.params.delegated) {
          dom7_default(document).on("click", tooltip.params.targetEl, handleClick);
        } else {
          tooltip.$targetEl.on("click", handleClick);
        }
        dom7_default("html").on("click", handleClickOut);
        return;
      }
      if (tooltip.params.trigger === "manual")
        return;
      if (support2.touch) {
        var passive = support2.passiveListener ? {
          passive: true
        } : false;
        if (tooltip.params.delegated) {
          dom7_default(document).on(app.touchEvents.start, tooltip.params.targetEl, handleTouchStart, passive);
        } else {
          tooltip.$targetEl.on(app.touchEvents.start, handleTouchStart, passive);
        }
        app.on("touchmove", handleTouchMove);
        app.on("touchend:passive", handleTouchEnd);
      } else {
        if (tooltip.params.delegated) {
          dom7_default(document).on(support2.pointerEvents ? "pointerenter" : "mouseenter", tooltip.params.targetEl, handleMouseEnter2, true);
          dom7_default(document).on(support2.pointerEvents ? "pointerleave" : "mouseleave", tooltip.params.targetEl, handleMouseLeave2, true);
        } else {
          tooltip.$targetEl.on(support2.pointerEvents ? "pointerenter" : "mouseenter", handleMouseEnter2);
          tooltip.$targetEl.on(support2.pointerEvents ? "pointerleave" : "mouseleave", handleMouseLeave2);
        }
      }
    };
    tooltip.detachEvents = function detachEvents2() {
      $el.off("transitionend", handleTransitionEnd);
      if (tooltip.params.trigger === "click") {
        if (tooltip.params.delegated) {
          dom7_default(document).on("click", tooltip.params.targetEl, handleClick);
        } else {
          tooltip.$targetEl.off("click", handleClick);
        }
        dom7_default("html").off("click", handleClickOut);
        return;
      }
      if (tooltip.params.trigger === "manual")
        return;
      if (support2.touch) {
        var passive = support2.passiveListener ? {
          passive: true
        } : false;
        if (tooltip.params.delegated) {
          dom7_default(document).off(app.touchEvents.start, tooltip.params.targetEl, handleTouchStart, passive);
        } else {
          tooltip.$targetEl.off(app.touchEvents.start, handleTouchStart, passive);
        }
        app.off("touchmove", handleTouchMove);
        app.off("touchend:passive", handleTouchEnd);
      } else {
        if (tooltip.params.delegated) {
          dom7_default(document).off(support2.pointerEvents ? "pointerenter" : "mouseenter", tooltip.params.targetEl, handleMouseEnter2, true);
          dom7_default(document).off(support2.pointerEvents ? "pointerleave" : "mouseleave", tooltip.params.targetEl, handleMouseLeave2, true);
        } else {
          tooltip.$targetEl.off(support2.pointerEvents ? "pointerenter" : "mouseenter", handleMouseEnter2);
          tooltip.$targetEl.off(support2.pointerEvents ? "pointerleave" : "mouseleave", handleMouseLeave2);
        }
      }
    };
    tooltip.useModules();
    tooltip.init();
    return tooltip || _assertThisInitialized25(_this);
  }
  var _proto = Tooltip2.prototype;
  _proto.setTargetEl = function setTargetEl(targetEl) {
    var tooltip = this;
    tooltip.detachEvents();
    tooltip.$targetEl = dom7_default(targetEl);
    tooltip.targetEl = tooltip.$targetEl[0];
    tooltip.attachEvents();
    return tooltip;
  };
  _proto.position = function position(targetEl) {
    var tooltip = this;
    var $el = tooltip.$el, app = tooltip.app, $containerEl = tooltip.$containerEl;
    var hasContainerEl = !!tooltip.params.containerEl;
    var tooltipOffset = tooltip.params.offset || 0;
    $el.css({
      left: "",
      top: ""
    });
    var $targetEl = dom7_default(targetEl || tooltip.targetEl);
    var _ref = [$el.width(), $el.height()], width = _ref[0], height = _ref[1];
    $el.css({
      left: "",
      top: ""
    });
    var targetWidth;
    var targetHeight;
    var targetOffsetLeft;
    var targetOffsetTop;
    var boundaries = hasContainerEl && $containerEl.length ? $containerEl[0].getBoundingClientRect() : app;
    if ($targetEl && $targetEl.length > 0) {
      targetWidth = $targetEl.outerWidth();
      targetHeight = $targetEl.outerHeight();
      if (typeof targetWidth === "undefined" && typeof targetHeight === "undefined") {
        var clientRect = $targetEl[0].getBoundingClientRect();
        targetWidth = clientRect.width;
        targetHeight = clientRect.height;
      }
      var targetOffset = $targetEl.offset();
      targetOffsetLeft = targetOffset.left - boundaries.left;
      targetOffsetTop = targetOffset.top - boundaries.top;
      var targetParentPage = $targetEl.parents(".page");
      if (targetParentPage.length > 0) {
        targetOffsetTop -= targetParentPage[0].scrollTop;
      }
    }
    var _ref2 = [0, 0, 0], left = _ref2[0], top = _ref2[1];
    var position2 = "top";
    if (height + tooltipOffset < targetOffsetTop) {
      top = targetOffsetTop - height - tooltipOffset;
    } else if (height < boundaries.height - targetOffsetTop - targetHeight) {
      position2 = "bottom";
      top = targetOffsetTop + targetHeight + tooltipOffset;
    } else {
      position2 = "middle";
      top = targetHeight / 2 + targetOffsetTop - height / 2;
      if (top <= 0) {
        top = 8;
      } else if (top + height >= boundaries.height) {
        top = boundaries.height - height - 8;
      }
    }
    if (position2 === "top" || position2 === "bottom") {
      left = targetWidth / 2 + targetOffsetLeft - width / 2;
      if (left < 8)
        left = 8;
      if (left + width > boundaries.width)
        left = boundaries.width - width - 8;
      if (left < 0)
        left = 0;
    } else if (position2 === "middle") {
      left = targetOffsetLeft - width;
      if (left < 8 || left + width > boundaries.width) {
        if (left < 8)
          left = targetOffsetLeft + targetWidth;
        if (left + width > boundaries.width)
          left = boundaries.width - width - 8;
      }
    }
    $el.css({
      top: top + "px",
      left: left + "px"
    });
  };
  _proto.show = function show4(aroundEl) {
    var tooltip = this;
    var $el = tooltip.$el, $targetEl = tooltip.$targetEl, $containerEl = tooltip.$containerEl;
    if ($containerEl[0] && $el[0] && !$containerEl[0].contains($el[0])) {
      $containerEl.append($el);
    }
    tooltip.position(aroundEl);
    var $aroundEl = dom7_default(aroundEl);
    tooltip.visible = true;
    tooltip.opened = true;
    $targetEl.trigger("tooltip:show");
    $el.trigger("tooltip:show");
    if ($aroundEl.length && $aroundEl[0] !== $targetEl[0]) {
      $aroundEl.trigger("tooltip:show");
    }
    tooltip.emit("local::show tooltipShow", tooltip);
    $el.removeClass("tooltip-out").addClass("tooltip-in");
    return tooltip;
  };
  _proto.hide = function hide3() {
    var tooltip = this;
    var $el = tooltip.$el, $targetEl = tooltip.$targetEl;
    tooltip.visible = false;
    tooltip.opened = false;
    $targetEl.trigger("tooltip:hide");
    $el.trigger("tooltip:hide");
    tooltip.emit("local::hide tooltipHide", tooltip);
    $el.addClass("tooltip-out").removeClass("tooltip-in");
    return tooltip;
  };
  _proto.render = function render15() {
    var tooltip = this;
    if (tooltip.params.render)
      return tooltip.params.render.call(tooltip, tooltip);
    var _tooltip$params2 = tooltip.params, cssClass = _tooltip$params2.cssClass, text2 = _tooltip$params2.text;
    return ('\n      <div class="tooltip ' + (cssClass || "") + '">\n        <div class="tooltip-content">' + (text2 || "") + "</div>\n      </div>\n    ").trim();
  };
  _proto.setText = function setText(newText) {
    var tooltip = this;
    if (typeof newText === "undefined") {
      return tooltip;
    }
    tooltip.params.text = newText;
    tooltip.text = newText;
    if (tooltip.$el) {
      tooltip.$el.children(".tooltip-content").html(newText);
    }
    if (tooltip.opened) {
      tooltip.position();
    }
    return tooltip;
  };
  _proto.init = function init46() {
    var tooltip = this;
    tooltip.attachEvents();
  };
  _proto.destroy = function destroy50() {
    var tooltip = this;
    if (!tooltip.$targetEl || tooltip.destroyed)
      return;
    tooltip.$targetEl.trigger("tooltip:beforedestroy");
    tooltip.emit("local::beforeDestroy tooltipBeforeDestroy", tooltip);
    tooltip.$el.remove();
    if (tooltip.$targetEl[0])
      delete tooltip.$targetEl[0].f7Tooltip;
    tooltip.detachEvents();
    deleteProps(tooltip);
    tooltip.destroyed = true;
  };
  return Tooltip2;
}(class_default);
var tooltip_class_default = Tooltip;

// node_modules/framework7/esm/components/tooltip/tooltip.js
var tooltip_default = {
  name: "tooltip",
  static: {
    Tooltip: tooltip_class_default
  },
  create: function create62() {
    var app = this;
    app.tooltip = ConstructorMethods({
      defaultSelector: ".tooltip",
      constructor: tooltip_class_default,
      app,
      domProp: "f7Tooltip"
    });
    app.tooltip.show = function show4(el) {
      var $el = dom7_default(el);
      if ($el.length === 0)
        return void 0;
      var tooltip = $el[0].f7Tooltip;
      if (!tooltip)
        return void 0;
      tooltip.show($el[0]);
      return tooltip;
    };
    app.tooltip.hide = function hide3(el) {
      var $el = dom7_default(el);
      if ($el.length === 0)
        return void 0;
      var tooltip = $el[0].f7Tooltip;
      if (!tooltip)
        return void 0;
      tooltip.hide();
      return tooltip;
    };
    app.tooltip.setText = function text2(el, newText) {
      var $el = dom7_default(el);
      if ($el.length === 0)
        return void 0;
      var tooltip = $el[0].f7Tooltip;
      if (!tooltip)
        return void 0;
      tooltip.setText(newText);
      return tooltip;
    };
  },
  params: {
    tooltip: {
      targetEl: null,
      delegated: false,
      text: null,
      cssClass: null,
      render: null,
      offset: 0,
      trigger: "hover",
      containerEl: void 0
    }
  },
  on: {
    tabMounted: function tabMounted18(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".tooltip-init").each(function(el) {
        var text2 = dom7_default(el).attr("data-tooltip");
        if (!text2)
          return;
        app.tooltip.create({
          targetEl: el,
          text: text2
        });
      });
    },
    tabBeforeRemove: function tabBeforeRemove15(tabEl) {
      dom7_default(tabEl).find(".tooltip-init").each(function(el) {
        if (el.f7Tooltip)
          el.f7Tooltip.destroy();
      });
    },
    pageInit: function pageInit19(page) {
      var app = this;
      page.$el.find(".tooltip-init").each(function(el) {
        var text2 = dom7_default(el).attr("data-tooltip");
        if (!text2)
          return;
        app.tooltip.create({
          targetEl: el,
          text: text2
        });
      });
      if (app.theme === "ios" && page.view && page.view.router.dynamicNavbar && page.$navbarEl && page.$navbarEl.length > 0) {
        page.$navbarEl.find(".tooltip-init").each(function(el) {
          var text2 = dom7_default(el).attr("data-tooltip");
          if (!text2)
            return;
          app.tooltip.create({
            targetEl: el,
            text: text2
          });
        });
      }
    },
    pageBeforeRemove: function pageBeforeRemove16(page) {
      var app = this;
      page.$el.find(".tooltip-init").each(function(el) {
        if (el.f7Tooltip)
          el.f7Tooltip.destroy();
      });
      if (app.theme === "ios" && page.view && page.view.router.dynamicNavbar && page.$navbarEl && page.$navbarEl.length > 0) {
        page.$navbarEl.find(".tooltip-init").each(function(el) {
          if (el.f7Tooltip)
            el.f7Tooltip.destroy();
        });
      }
    }
  },
  vnode: {
    "tooltip-init": {
      insert: function insert15(vnode) {
        var app = this;
        var el = vnode.elm;
        var text2 = dom7_default(el).attr("data-tooltip");
        if (!text2)
          return;
        app.tooltip.create({
          targetEl: el,
          text: text2
        });
      },
      update: function update9(vnode) {
        var el = vnode.elm;
        if (!el.f7Tooltip)
          return;
        if (vnode && vnode.data && vnode.data.attrs && vnode.data.attrs["data-tooltip"]) {
          el.f7Tooltip.setText(vnode.data.attrs["data-tooltip"]);
        }
      },
      destroy: function destroy35(vnode) {
        var el = vnode.elm;
        if (el.f7Tooltip)
          el.f7Tooltip.destroy();
      }
    }
  }
};

// node_modules/framework7/esm/components/gauge/gauge-class.js
function _assertThisInitialized26(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose26(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf26(subClass, superClass);
}
function _setPrototypeOf26(o, p) {
  _setPrototypeOf26 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf26(o, p);
}
var Gauge = function(_Framework7Class) {
  _inheritsLoose26(Gauge2, _Framework7Class);
  function Gauge2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var self = _assertThisInitialized26(_this);
    var defaults = extend({}, app.params.gauge);
    self.useModulesParams(defaults);
    self.params = extend(defaults, params);
    var el = self.params.el;
    if (!el)
      return self || _assertThisInitialized26(_this);
    var $el = dom7_default(el);
    if ($el.length === 0)
      return self || _assertThisInitialized26(_this);
    if ($el[0].f7Gauge)
      return $el[0].f7Gauge || _assertThisInitialized26(_this);
    extend(self, {
      app,
      $el,
      el: $el && $el[0]
    });
    $el[0].f7Gauge = self;
    self.useModules();
    self.init();
    return self || _assertThisInitialized26(_this);
  }
  var _proto = Gauge2.prototype;
  _proto.calcRadius = function calcRadius() {
    var self = this;
    var _self$params = self.params, size = _self$params.size, borderWidth = _self$params.borderWidth;
    return size / 2 - borderWidth / 2;
  };
  _proto.calcBorderLength = function calcBorderLength() {
    var self = this;
    var radius = self.calcRadius();
    return 2 * Math.PI * radius;
  };
  _proto.render = function render15() {
    var self = this;
    if (self.params.render)
      return self.params.render.call(self, self);
    var _self$params2 = self.params, type = _self$params2.type, value = _self$params2.value, size = _self$params2.size, bgColor = _self$params2.bgColor, borderBgColor = _self$params2.borderBgColor, borderColor = _self$params2.borderColor, borderWidth = _self$params2.borderWidth, valueText = _self$params2.valueText, valueTextColor = _self$params2.valueTextColor, valueFontSize = _self$params2.valueFontSize, valueFontWeight = _self$params2.valueFontWeight, labelText = _self$params2.labelText, labelTextColor = _self$params2.labelTextColor, labelFontSize = _self$params2.labelFontSize, labelFontWeight = _self$params2.labelFontWeight;
    var semiCircle = type === "semicircle";
    var radius = self.calcRadius();
    var length = self.calcBorderLength();
    var progress = Math.max(Math.min(value, 1), 0);
    return jsx_default2("svg", {
      class: "gauge-svg",
      width: size + "px",
      height: (semiCircle ? size / 2 : size) + "px",
      viewBox: "0 0 " + size + " " + (semiCircle ? size / 2 : size)
    }, semiCircle && jsx_default2("path", {
      class: "gauge-back-semi",
      d: "M" + (size - borderWidth / 2) + "," + size / 2 + " a1,1 0 0,0 -" + (size - borderWidth) + ",0",
      stroke: borderBgColor,
      "stroke-width": borderWidth,
      fill: bgColor || "none"
    }), semiCircle && jsx_default2("path", {
      class: "gauge-front-semi",
      d: "M" + (size - borderWidth / 2) + "," + size / 2 + " a1,1 0 0,0 -" + (size - borderWidth) + ",0",
      stroke: borderColor,
      "stroke-width": borderWidth,
      "stroke-dasharray": length / 2,
      "stroke-dashoffset": length / 2 * (1 + progress),
      fill: borderBgColor ? "none" : bgColor || "none"
    }), !semiCircle && borderBgColor && jsx_default2("circle", {
      class: "gauge-back-circle",
      stroke: borderBgColor,
      "stroke-width": borderWidth,
      fill: bgColor || "none",
      cx: size / 2,
      cy: size / 2,
      r: radius
    }), !semiCircle && jsx_default2("circle", {
      class: "gauge-front-circle",
      transform: "rotate(-90 " + size / 2 + " " + size / 2 + ")",
      stroke: borderColor,
      "stroke-width": borderWidth,
      "stroke-dasharray": length,
      "stroke-dashoffset": length * (1 - progress),
      fill: borderBgColor ? "none" : bgColor || "none",
      cx: size / 2,
      cy: size / 2,
      r: radius
    }), valueText && jsx_default2("text", {
      class: "gauge-value-text",
      x: "50%",
      y: semiCircle ? "100%" : "50%",
      "font-weight": valueFontWeight,
      "font-size": valueFontSize,
      fill: valueTextColor,
      dy: semiCircle ? labelText ? -labelFontSize - 15 : -5 : 0,
      "text-anchor": "middle",
      "dominant-baseline": !semiCircle && "middle"
    }, valueText), labelText && jsx_default2("text", {
      class: "gauge-label-text",
      x: "50%",
      y: semiCircle ? "100%" : "50%",
      "font-weight": labelFontWeight,
      "font-size": labelFontSize,
      fill: labelTextColor,
      dy: semiCircle ? -5 : valueText ? valueFontSize / 2 + 10 : 0,
      "text-anchor": "middle",
      "dominant-baseline": !semiCircle && "middle"
    }, labelText));
  };
  _proto.update = function update22(newParams) {
    if (newParams === void 0) {
      newParams = {};
    }
    var self = this;
    var document = getDocument();
    var params = self.params, $svgEl = self.$svgEl;
    Object.keys(newParams).forEach(function(param) {
      if (typeof newParams[param] !== "undefined") {
        params[param] = newParams[param];
      }
    });
    if ($svgEl.length === 0)
      return self;
    var value = params.value, size = params.size, bgColor = params.bgColor, borderBgColor = params.borderBgColor, borderColor = params.borderColor, borderWidth = params.borderWidth, valueText = params.valueText, valueTextColor = params.valueTextColor, valueFontSize = params.valueFontSize, valueFontWeight = params.valueFontWeight, labelText = params.labelText, labelTextColor = params.labelTextColor, labelFontSize = params.labelFontSize, labelFontWeight = params.labelFontWeight;
    var length = self.calcBorderLength();
    var progress = Math.max(Math.min(value, 1), 0);
    var radius = self.calcRadius();
    var semiCircle = params.type === "semicircle";
    var svgAttrs = {
      width: size + "px",
      height: (semiCircle ? size / 2 : size) + "px",
      viewBox: "0 0 " + size + " " + (semiCircle ? size / 2 : size)
    };
    Object.keys(svgAttrs).forEach(function(attr2) {
      $svgEl.attr(attr2, svgAttrs[attr2]);
    });
    if (semiCircle) {
      var backAttrs = {
        d: "M" + (size - borderWidth / 2) + "," + size / 2 + " a1,1 0 0,0 -" + (size - borderWidth) + ",0",
        stroke: borderBgColor,
        "stroke-width": borderWidth,
        fill: bgColor || "none"
      };
      var frontAttrs = {
        d: "M" + (size - borderWidth / 2) + "," + size / 2 + " a1,1 0 0,0 -" + (size - borderWidth) + ",0",
        stroke: borderColor,
        "stroke-width": borderWidth,
        "stroke-dasharray": length / 2,
        "stroke-dashoffset": length / 2 * (1 + progress),
        fill: borderBgColor ? "none" : bgColor || "none"
      };
      Object.keys(backAttrs).forEach(function(attr2) {
        $svgEl.find(".gauge-back-semi").attr(attr2, backAttrs[attr2]);
      });
      Object.keys(frontAttrs).forEach(function(attr2) {
        $svgEl.find(".gauge-front-semi").attr(attr2, frontAttrs[attr2]);
      });
    } else {
      var _backAttrs = {
        stroke: borderBgColor,
        "stroke-width": borderWidth,
        fill: bgColor || "none",
        cx: size / 2,
        cy: size / 2,
        r: radius
      };
      var _frontAttrs = {
        transform: "rotate(-90 " + size / 2 + " " + size / 2 + ")",
        stroke: borderColor,
        "stroke-width": borderWidth,
        "stroke-dasharray": length,
        "stroke-dashoffset": length * (1 - progress),
        fill: borderBgColor ? "none" : bgColor || "none",
        cx: size / 2,
        cy: size / 2,
        r: radius
      };
      Object.keys(_backAttrs).forEach(function(attr2) {
        $svgEl.find(".gauge-back-circle").attr(attr2, _backAttrs[attr2]);
      });
      Object.keys(_frontAttrs).forEach(function(attr2) {
        $svgEl.find(".gauge-front-circle").attr(attr2, _frontAttrs[attr2]);
      });
    }
    if (valueText) {
      if (!$svgEl.find(".gauge-value-text").length) {
        var textEl = document.createElementNS("http://www.w3.org/2000/svg", "text");
        textEl.classList.add("gauge-value-text");
        $svgEl.append(textEl);
      }
      var textAttrs = {
        x: "50%",
        y: semiCircle ? "100%" : "50%",
        "font-weight": valueFontWeight,
        "font-size": valueFontSize,
        fill: valueTextColor,
        dy: semiCircle ? labelText ? -labelFontSize - 15 : -5 : 0,
        "text-anchor": "middle",
        "dominant-baseline": !semiCircle && "middle"
      };
      Object.keys(textAttrs).forEach(function(attr2) {
        $svgEl.find(".gauge-value-text").attr(attr2, textAttrs[attr2]);
      });
      $svgEl.find(".gauge-value-text").text(valueText);
    } else {
      $svgEl.find(".gauge-value-text").remove();
    }
    if (labelText) {
      if (!$svgEl.find(".gauge-label-text").length) {
        var _textEl = document.createElementNS("http://www.w3.org/2000/svg", "text");
        _textEl.classList.add("gauge-label-text");
        $svgEl.append(_textEl);
      }
      var labelAttrs = {
        x: "50%",
        y: semiCircle ? "100%" : "50%",
        "font-weight": labelFontWeight,
        "font-size": labelFontSize,
        fill: labelTextColor,
        dy: semiCircle ? -5 : valueText ? valueFontSize / 2 + 10 : 0,
        "text-anchor": "middle",
        "dominant-baseline": !semiCircle && "middle"
      };
      Object.keys(labelAttrs).forEach(function(attr2) {
        $svgEl.find(".gauge-label-text").attr(attr2, labelAttrs[attr2]);
      });
      $svgEl.find(".gauge-label-text").text(labelText);
    } else {
      $svgEl.find(".gauge-label-text").remove();
    }
    return self;
  };
  _proto.init = function init46() {
    var self = this;
    var $svgEl = dom7_default(self.render()).eq(0);
    $svgEl.f7Gauge = self;
    extend(self, {
      $svgEl,
      svgEl: $svgEl && $svgEl[0]
    });
    self.$el.append($svgEl);
    return self;
  };
  _proto.destroy = function destroy50() {
    var self = this;
    if (!self.$el || self.destroyed)
      return;
    self.$el.trigger("gauge:beforedestroy");
    self.emit("local::beforeDestroy gaugeBeforeDestroy", self);
    self.$svgEl.remove();
    delete self.$el[0].f7Gauge;
    deleteProps(self);
    self.destroyed = true;
  };
  return Gauge2;
}(class_default);
var gauge_class_default = Gauge;

// node_modules/framework7/esm/components/gauge/gauge.js
var gauge_default = {
  name: "gauge",
  static: {
    Gauge: gauge_class_default
  },
  create: function create63() {
    var app = this;
    app.gauge = ConstructorMethods({
      defaultSelector: ".gauge",
      constructor: gauge_class_default,
      app,
      domProp: "f7Gauge"
    });
    app.gauge.update = function update22(el, newParams) {
      var $el = dom7_default(el);
      if ($el.length === 0)
        return void 0;
      var gauge = app.gauge.get(el);
      if (!gauge)
        return void 0;
      gauge.update(newParams);
      return gauge;
    };
  },
  params: {
    gauge: {
      el: null,
      type: "circle",
      value: 0,
      size: 200,
      bgColor: "transparent",
      borderBgColor: "#eeeeee",
      borderColor: "#000000",
      borderWidth: 10,
      valueText: null,
      valueTextColor: "#000000",
      valueFontSize: 31,
      valueFontWeight: 500,
      labelText: null,
      labelTextColor: "#888888",
      labelFontSize: 14,
      labelFontWeight: 400
    }
  },
  on: {
    tabMounted: function tabMounted19(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".gauge-init").each(function(el) {
        app.gauge.create(extend({
          el
        }, dom7_default(el).dataset() || {}));
      });
    },
    tabBeforeRemove: function tabBeforeRemove16(tabEl) {
      dom7_default(tabEl).find(".gauge-init").each(function(el) {
        if (el.f7Gauge)
          el.f7Gauge.destroy();
      });
    },
    pageInit: function pageInit20(page) {
      var app = this;
      page.$el.find(".gauge-init").each(function(el) {
        app.gauge.create(extend({
          el
        }, dom7_default(el).dataset() || {}));
      });
    },
    pageBeforeRemove: function pageBeforeRemove17(page) {
      page.$el.find(".gauge-init").each(function(el) {
        if (el.f7Gauge)
          el.f7Gauge.destroy();
      });
    }
  },
  vnode: {
    "gauge-init": {
      insert: function insert16(vnode) {
        var app = this;
        var el = vnode.elm;
        app.gauge.create(extend({
          el
        }, dom7_default(el).dataset() || {}));
      },
      destroy: function destroy36(vnode) {
        var el = vnode.elm;
        if (el.f7Gauge)
          el.f7Gauge.destroy();
      }
    }
  }
};

// node_modules/framework7/esm/components/skeleton/skeleton.js
var skeleton_default = {
  name: "skeleton"
};

// node_modules/framework7/esm/components/menu/menu.js
var Menu = {
  open: function open10(el) {
    if (el === void 0) {
      el = ".menu-item-dropdown";
    }
    var app = this;
    if (!el)
      return;
    var $el = dom7_default(el).closest(".menu-item-dropdown");
    if (!$el.length)
      return;
    var $menuEl = $el.closest(".menu").eq(0);
    if ($menuEl.length) {
      var zIndex = $menuEl.css("z-index");
      var originalZIndex = $menuEl[0].style.zIndex;
      $menuEl.css("z-index", parseInt(zIndex || 0, 10) + 1);
      $menuEl[0].f7MenuZIndex = originalZIndex;
    }
    $el.eq(0).addClass("menu-item-dropdown-opened").trigger("menu:opened");
    app.emit("menuOpened", $el.eq(0)[0]);
  },
  close: function close10(el) {
    if (el === void 0) {
      el = ".menu-item-dropdown-opened";
    }
    var app = this;
    if (!el)
      return;
    var $el = dom7_default(el).closest(".menu-item-dropdown-opened");
    if (!$el.length)
      return;
    var $menuEl = $el.closest(".menu").eq(0);
    if ($menuEl.length) {
      var zIndex = $menuEl[0].f7MenuZIndex;
      $menuEl.css("z-index", zIndex);
      delete $menuEl[0].f7MenuZIndex;
    }
    $el.eq(0).removeClass("menu-item-dropdown-opened").trigger("menu:closed");
    app.emit("menuClosed", $el.eq(0)[0]);
  }
};
var menu_default = {
  name: "menu",
  create: function create64() {
    var app = this;
    bindMethods(app, {
      menu: Menu
    });
  },
  on: {
    click: function click3(e) {
      var app = this;
      var openedMenus = dom7_default(".menu-item-dropdown-opened");
      if (!openedMenus.length)
        return;
      openedMenus.each(function(el) {
        if (!dom7_default(e.target).closest(".menu-item-dropdown-opened").length) {
          app.menu.close(el);
        }
      });
    }
  },
  clicks: {
    ".menu-item-dropdown": function onClick2($clickedEl, dataset, e) {
      var app = this;
      if ($clickedEl.hasClass("menu-item-dropdown-opened")) {
        if (dom7_default(e.target).closest(".menu-dropdown").length)
          return;
        app.menu.close($clickedEl);
      } else {
        app.menu.open($clickedEl);
      }
    },
    ".menu-close": function onClick3() {
      var app = this;
      app.menu.close();
    }
  }
};

// node_modules/framework7/esm/components/color-picker/modules/alpha-slider.js
var alpha_slider_default = {
  render: function render2(self) {
    var _self$params = self.params, sliderLabel = _self$params.sliderLabel, sliderValue = _self$params.sliderValue, sliderValueEditable = _self$params.sliderValueEditable, alphaLabelText = _self$params.alphaLabelText;
    return jsx_default2("div", {
      class: "color-picker-module color-picker-module-alpha-slider"
    }, jsx_default2("div", {
      class: "color-picker-slider-wrap"
    }, sliderLabel && jsx_default2("div", {
      class: "color-picker-slider-label"
    }, alphaLabelText), jsx_default2("div", {
      class: "range-slider color-picker-slider color-picker-slider-alpha"
    }), sliderValue && jsx_default2("div", {
      class: "color-picker-slider-value"
    }, sliderValueEditable ? jsx_default2("input", {
      type: "number",
      step: "0.01",
      min: "0",
      max: "1",
      class: "color-picker-value-alpha"
    }) : jsx_default2("span", {
      class: "color-picker-value-alpha"
    }))));
  },
  init: function init34(self) {
    self.alphaRangeSlider = self.app.range.create({
      el: self.$el.find(".color-picker-slider-alpha"),
      min: 0,
      max: 1,
      step: 0.01,
      value: 1,
      on: {
        change: function change(range, value) {
          var alpha = Math.floor(value * 100) / 100;
          self.setValue({
            alpha
          });
        }
      }
    });
    function handleInputChange(e) {
      var alpha = self.value.alpha;
      var value = parseFloat(e.target.value);
      if (Number.isNaN(value)) {
        e.target.value = alpha;
        return;
      }
      value = Math.max(0, Math.min(1, value));
      self.setValue({
        alpha: value
      });
    }
    self.$el.on("change", ".color-picker-module-alpha-slider input", handleInputChange);
    self.destroyAlphaSliderEvents = function destroyAlphaSliderEvents() {
      self.$el.off("change", ".color-picker-module-alpha-slider input", handleInputChange);
    };
  },
  update: function update10(self) {
    var value = self.value;
    var _self$params2 = self.params, sliderValue = _self$params2.sliderValue, sliderValueEditable = _self$params2.sliderValueEditable;
    var alpha = value.alpha;
    self.alphaRangeSlider.value = alpha;
    self.alphaRangeSlider.layout();
    if (sliderValue && sliderValueEditable) {
      self.$el.find("input.color-picker-value-alpha").val(alpha);
    } else {
      self.$el.find("span.color-picker-value-alpha").text(alpha);
    }
  },
  destroy: function destroy37(self) {
    if (self.alphaRangeSlider && self.alphaRangeSlider.destroy) {
      self.alphaRangeSlider.destroy();
    }
    delete self.alphaRangeSlider;
    if (self.destroyAlphaSliderEvents)
      self.destroyAlphaSliderEvents();
    delete self.destroyAlphaSliderEvents;
  }
};

// node_modules/framework7/esm/components/color-picker/modules/current-color.js
var current_color_default = {
  render: function render3() {
    return jsx_default2("div", {
      class: "color-picker-module color-picker-module-current-color"
    }, jsx_default2("div", {
      class: "color-picker-current-color"
    }));
  },
  update: function update11(self) {
    self.$el.find(".color-picker-module-current-color .color-picker-current-color").css("background-color", self.value.hex);
  }
};

// node_modules/framework7/esm/components/color-picker/modules/hex.js
var hex_default = {
  render: function render4(self) {
    var _self$params = self.params, hexLabel = _self$params.hexLabel, hexLabelText = _self$params.hexLabelText, hexValueEditable = _self$params.hexValueEditable;
    return jsx_default2("div", {
      class: "color-picker-module color-picker-module-hex"
    }, jsx_default2("div", {
      class: "color-picker-hex-wrap"
    }, hexLabel && jsx_default2("div", {
      class: "color-picker-hex-label"
    }, hexLabelText), jsx_default2("div", {
      class: "color-picker-hex-value"
    }, hexValueEditable ? jsx_default2("input", {
      type: "text",
      class: "color-picker-value-hex"
    }) : jsx_default2("span", {
      class: "color-picker-value-hex"
    }))));
  },
  init: function init35(self) {
    function handleInputChange(e) {
      var hex = self.value.hex;
      var value = e.target.value.replace(/#/g, "");
      if (Number.isNaN(value) || !value || value.length !== 3 && value.length !== 6) {
        e.target.value = hex;
        return;
      }
      var min = 0;
      var current = parseInt(value, 16);
      var max = parseInt("ffffff", 16);
      if (current > max) {
        value = "fff";
      }
      if (current < min) {
        value = "000";
      }
      self.setValue({
        hex: value
      });
    }
    self.$el.on("change", ".color-picker-module-hex input", handleInputChange);
    self.destroyHexEvents = function destroyHexEvents() {
      self.$el.off("change", ".color-picker-module-hex input", handleInputChange);
    };
  },
  update: function update12(self) {
    var value = self.value;
    var hexValueEditable = self.params.hexValueEditable;
    var hex = value.hex;
    if (hexValueEditable) {
      self.$el.find("input.color-picker-value-hex").val(hex);
    } else {
      self.$el.find("span.color-picker-value-hex").text(hex);
    }
  },
  destroy: function destroy38(self) {
    if (self.destroyHexEvents)
      self.destroyHexEvents();
    delete self.destroyHexEvents;
  }
};

// node_modules/framework7/esm/components/color-picker/modules/hsb-sliders.js
var hsb_sliders_default = {
  render: function render5(self) {
    var _self$params = self.params, sliderLabel = _self$params.sliderLabel, sliderValue = _self$params.sliderValue, sliderValueEditable = _self$params.sliderValueEditable, hueLabelText = _self$params.hueLabelText, saturationLabelText = _self$params.saturationLabelText, brightnessLabelText = _self$params.brightnessLabelText;
    return jsx_default2("div", {
      class: "color-picker-module color-picker-module-hsb-sliders"
    }, jsx_default2("div", {
      class: "color-picker-slider-wrap"
    }, sliderLabel && jsx_default2("div", {
      class: "color-picker-slider-label"
    }, hueLabelText), jsx_default2("div", {
      class: "range-slider color-picker-slider color-picker-slider-hue"
    }), sliderValue && jsx_default2("div", {
      class: "color-picker-slider-value"
    }, sliderValueEditable ? jsx_default2("input", {
      type: "number",
      step: "0.1",
      min: "0",
      max: "360",
      class: "color-picker-value-hue",
      "data-color-index": "0"
    }) : jsx_default2("span", {
      class: "color-picker-value-hue"
    }))), jsx_default2("div", {
      class: "color-picker-slider-wrap"
    }, sliderLabel && jsx_default2("div", {
      class: "color-picker-slider-label"
    }, saturationLabelText), jsx_default2("div", {
      class: "range-slider color-picker-slider color-picker-slider-saturation"
    }), sliderValue && jsx_default2("div", {
      class: "color-picker-slider-value"
    }, sliderValueEditable ? jsx_default2("input", {
      type: "number",
      step: "0.1",
      min: "0",
      max: "100",
      class: "color-picker-value-saturation",
      "data-color-index": "1"
    }) : jsx_default2("span", {
      class: "color-picker-value-saturation"
    }))), jsx_default2("div", {
      class: "color-picker-slider-wrap"
    }, sliderLabel && jsx_default2("div", {
      class: "color-picker-slider-label"
    }, brightnessLabelText), jsx_default2("div", {
      class: "range-slider color-picker-slider color-picker-slider-brightness"
    }), sliderValue && jsx_default2("div", {
      class: "color-picker-slider-value"
    }, sliderValueEditable ? jsx_default2("input", {
      type: "number",
      step: "0.1",
      min: "0",
      max: "100",
      class: "color-picker-value-brightness",
      "data-color-index": "2"
    }) : jsx_default2("span", {
      class: "color-picker-value-brightness"
    }))));
  },
  init: function init36(self) {
    self.hueRangeSlider = self.app.range.create({
      el: self.$el.find(".color-picker-slider-hue"),
      min: 0,
      max: 360,
      step: 0.1,
      value: 0,
      on: {
        change: function change(range, value) {
          self.setValue({
            hue: value
          });
        }
      }
    });
    self.saturationRangeSlider = self.app.range.create({
      el: self.$el.find(".color-picker-slider-saturation"),
      min: 0,
      max: 1,
      step: 1e-3,
      value: 0,
      on: {
        change: function change(range, value) {
          var s = Math.floor(value * 1e3) / 1e3;
          self.setValue({
            hsb: [self.value.hsb[0], s, self.value.hsb[2]]
          });
        }
      }
    });
    self.brightnessRangeSlider = self.app.range.create({
      el: self.$el.find(".color-picker-slider-brightness"),
      min: 0,
      max: 1,
      step: 1e-3,
      value: 0,
      on: {
        change: function change(range, value) {
          var b = Math.floor(value * 1e3) / 1e3;
          self.setValue({
            hsb: [self.value.hsb[0], self.value.hsb[1], b]
          });
        }
      }
    });
    function handleInputChange(e) {
      var hsb = [].concat(self.value.hsb);
      var index2 = parseInt(dom7_default(e.target).attr("data-color-index"), 10);
      var value = parseFloat(e.target.value);
      if (Number.isNaN(value)) {
        e.target.value = hsb[index2];
        return;
      }
      if (index2 === 0) {
        value = Math.max(0, Math.min(360, value));
      } else {
        value = Math.max(0, Math.min(100, value)) / 100;
      }
      hsb[index2] = value;
      self.setValue({
        hsb
      });
    }
    self.$el.on("change", ".color-picker-module-hsb-sliders input", handleInputChange);
    self.destroyHsbSlidersEvents = function destroyHsbSlidersEvents() {
      self.$el.off("change", ".color-picker-module-hsb-sliders input", handleInputChange);
    };
  },
  update: function update13(self) {
    var app = self.app, value = self.value;
    var _self$params2 = self.params, sliderValue = _self$params2.sliderValue, sliderValueEditable = _self$params2.sliderValueEditable;
    var hsb = value.hsb, hue = value.hue;
    self.hueRangeSlider.value = hue;
    self.saturationRangeSlider.value = hsb[1];
    self.brightnessRangeSlider.value = hsb[2];
    self.hueRangeSlider.layout();
    self.saturationRangeSlider.layout();
    self.brightnessRangeSlider.layout();
    var hslCurrent = colorHsbToHsl(hsb[0], hsb[1], 1);
    var hslLeft = colorHsbToHsl(hsb[0], 0, 1);
    var hslRight = colorHsbToHsl(hsb[0], 1, 1);
    var brightness = hsb[2];
    self.hueRangeSlider.$el[0].style.setProperty("--f7-range-knob-color", "hsl(" + hue + ", 100%, 50%)");
    self.saturationRangeSlider.$el[0].style.setProperty("--f7-range-knob-color", "hsl(" + hslCurrent[0] + ", " + hslCurrent[1] * 100 + "%, " + hslCurrent[2] * 100 + "%)");
    self.brightnessRangeSlider.$el[0].style.setProperty("--f7-range-knob-color", "rgb(" + brightness * 255 + ", " + brightness * 255 + ", " + brightness * 255 + ")");
    self.saturationRangeSlider.$el.find(".range-bar").css("background-image", "linear-gradient(" + (app.rtl ? "to left" : "to right") + ", hsl(" + hslLeft[0] + ", " + hslLeft[1] * 100 + "%, " + hslLeft[2] * 100 + "%), hsl(" + hslRight[0] + ", " + hslRight[1] * 100 + "%, " + hslRight[2] * 100 + "%))");
    if (sliderValue && sliderValueEditable) {
      self.$el.find("input.color-picker-value-hue").val("" + hue);
      self.$el.find("input.color-picker-value-saturation").val("" + hsb[1] * 1e3 / 10);
      self.$el.find("input.color-picker-value-brightness").val("" + hsb[2] * 1e3 / 10);
    } else if (sliderValue) {
      self.$el.find("span.color-picker-value-hue").text("" + hue);
      self.$el.find("span.color-picker-value-saturation").text("" + hsb[1] * 1e3 / 10);
      self.$el.find("span.color-picker-value-brightness").text("" + hsb[2] * 1e3 / 10);
    }
  },
  destroy: function destroy39(self) {
    if (self.hueRangeSlider && self.hueRangeSlider.destroy) {
      self.hueRangeSlider.destroy();
    }
    if (self.saturationRangeSlider && self.saturationRangeSlider.destroy) {
      self.saturationRangeSlider.destroy();
    }
    if (self.brightnessRangeSlider && self.brightnessRangeSlider.destroy) {
      self.brightnessRangeSlider.destroy();
    }
    delete self.hueRangeSlider;
    delete self.saturationRangeSlider;
    delete self.brightnessRangeSlider;
    if (self.destroyHsbSlidersEvents)
      self.destroyHsbSlidersEvents();
    delete self.destroyHsbSlidersEvents;
  }
};

// node_modules/framework7/esm/components/color-picker/modules/hue-slider.js
var hue_slider_default = {
  render: function render6(self) {
    var _self$params = self.params, sliderLabel = _self$params.sliderLabel, sliderValue = _self$params.sliderValue, sliderValueEditable = _self$params.sliderValueEditable, hueLabelText = _self$params.hueLabelText;
    return jsx_default2("div", {
      class: "color-picker-module color-picker-module-hue-slider"
    }, jsx_default2("div", {
      class: "color-picker-slider-wrap"
    }, sliderLabel && jsx_default2("div", {
      class: "color-picker-slider-label"
    }, hueLabelText), jsx_default2("div", {
      class: "range-slider color-picker-slider color-picker-slider-hue"
    }), sliderValue && jsx_default2("div", {
      class: "color-picker-slider-value"
    }, sliderValueEditable ? jsx_default2("input", {
      type: "number",
      step: "0.1",
      min: "0",
      max: "360",
      class: "color-picker-value-hue"
    }) : jsx_default2("span", {
      class: "color-picker-value-hue"
    }))));
  },
  init: function init37(self) {
    self.hueRangeSlider = self.app.range.create({
      el: self.$el.find(".color-picker-slider-hue"),
      min: 0,
      max: 360,
      step: 0.1,
      value: 0,
      on: {
        change: function change(range, value) {
          self.setValue({
            hue: value
          });
        }
      }
    });
  },
  update: function update14(self) {
    var value = self.value;
    var _self$params2 = self.params, sliderValue = _self$params2.sliderValue, sliderValueEditable = _self$params2.sliderValueEditable;
    var hue = value.hue;
    self.hueRangeSlider.value = hue;
    self.hueRangeSlider.layout();
    self.hueRangeSlider.$el[0].style.setProperty("--f7-range-knob-color", "hsl(" + hue + ", 100%, 50%)");
    if (sliderValue && sliderValueEditable) {
      self.$el.find("input.color-picker-value-hue").val("" + hue);
    } else if (sliderValue) {
      self.$el.find("span.color-picker-value-hue").text("" + hue);
    }
  },
  destroy: function destroy40(self) {
    if (self.hueRangeSlider && self.hueRangeSlider.destroy) {
      self.hueRangeSlider.destroy();
    }
    delete self.hueRangeSlider;
  }
};

// node_modules/framework7/esm/components/color-picker/modules/brightness-slider.js
var brightness_slider_default = {
  render: function render7(self) {
    var _self$params = self.params, sliderLabel = _self$params.sliderLabel, sliderValue = _self$params.sliderValue, sliderValueEditable = _self$params.sliderValueEditable, brightnessLabelText = _self$params.brightnessLabelText;
    return jsx_default2("div", {
      class: "color-picker-module color-picker-module-brightness-slider"
    }, jsx_default2("div", {
      class: "color-picker-slider-wrap"
    }, sliderLabel && jsx_default2("div", {
      class: "color-picker-slider-label"
    }, brightnessLabelText), jsx_default2("div", {
      class: "range-slider color-picker-slider color-picker-slider-brightness"
    }), sliderValue && jsx_default2("div", {
      class: "color-picker-slider-value"
    }, sliderValueEditable ? jsx_default2("input", {
      type: "number",
      step: "0.1",
      min: "0",
      max: "100",
      class: "color-picker-value-brightness"
    }) : jsx_default2("span", {
      class: "color-picker-value-brightness"
    }))));
  },
  init: function init38(self) {
    self.brightnessRangeSlider = self.app.range.create({
      el: self.$el.find(".color-picker-slider-brightness"),
      min: 0,
      max: 1,
      step: 1e-3,
      value: 0,
      on: {
        change: function change(range, value) {
          var b = Math.floor(value * 1e3) / 1e3;
          self.setValue({
            hsb: [self.value.hsb[0], self.value.hsb[1], b]
          });
        }
      }
    });
  },
  update: function update15(self) {
    var value = self.value, app = self.app;
    var _self$params2 = self.params, sliderValue = _self$params2.sliderValue, sliderValueEditable = _self$params2.sliderValueEditable;
    var hsb = value.hsb;
    self.brightnessRangeSlider.value = hsb[2];
    self.brightnessRangeSlider.layout();
    var hslCurrent = colorHsbToHsl(hsb[0], hsb[1], hsb[2]);
    var hslLeft = colorHsbToHsl(hsb[0], hsb[1], 0);
    var hslRight = colorHsbToHsl(hsb[0], hsb[1], 1);
    self.brightnessRangeSlider.$el[0].style.setProperty("--f7-range-knob-color", "hsl(" + hslCurrent[0] + ", " + hslCurrent[1] * 100 + "%, " + hslCurrent[2] * 100 + "%)");
    self.brightnessRangeSlider.$el.find(".range-bar").css("background-image", "linear-gradient(" + (app.rtl ? "to left" : "to right") + ", hsl(" + hslLeft[0] + ", " + hslLeft[1] * 100 + "%, " + hslLeft[2] * 100 + "%), hsl(" + hslRight[0] + ", " + hslRight[1] * 100 + "%, " + hslRight[2] * 100 + "%))");
    if (sliderValue && sliderValueEditable) {
      self.$el.find("input.color-picker-value-brightness").val("" + hsb[2] * 1e3 / 10);
    } else if (sliderValue) {
      self.$el.find("span.color-picker-value-brightness").text("" + hsb[2] * 1e3 / 10);
    }
  },
  destroy: function destroy41(self) {
    if (self.brightnessRangeSlider && self.brightnessRangeSlider.destroy) {
      self.brightnessRangeSlider.destroy();
    }
    delete self.brightnessRangeSlider;
  }
};

// node_modules/framework7/esm/components/color-picker/modules/palette.js
var palette_default = {
  render: function render8(self) {
    return jsx_default2("div", {
      class: "color-picker-module color-picker-module-palette"
    }, jsx_default2("div", {
      class: "color-picker-palette"
    }, self.params.palette.map(function(p) {
      if (Array.isArray(p)) {
        var row = '<div class="color-picker-palette-row">';
        row += p.map(function(c) {
          return '\n                <div class="color-picker-palette-value" data-palette-color="' + c + '" style="background-color: ' + c + '"></div>\n              ';
        }).join("");
        row += "</div>";
        return row;
      }
      return jsx_default2("div", {
        class: "color-picker-palette-value",
        "data-palette-color": p,
        style: "background-color: " + p
      });
    })));
  },
  init: function init39(self) {
    function handlePaletteClick(e) {
      var hex = dom7_default(e.target).attr("data-palette-color");
      self.setValue({
        hex
      });
    }
    self.$el.on("click", ".color-picker-module-palette .color-picker-palette-value", handlePaletteClick);
    self.destroyPaletteEvents = function destroyPaletteEvents() {
      self.$el.off("click", ".color-picker-module-hex input", handlePaletteClick);
    };
  },
  destroy: function destroy42(self) {
    if (self.destroyPaletteEvents) {
      self.destroyPaletteEvents();
    }
    delete self.destroyPaletteEvents;
  }
};

// node_modules/framework7/esm/components/color-picker/modules/initial-current-colors.js
var initial_current_colors_default = {
  render: function render9() {
    return jsx_default2("div", {
      class: "color-picker-module color-picker-module-initial-current-colors"
    }, jsx_default2("div", {
      class: "color-picker-initial-current-colors"
    }, jsx_default2("div", {
      class: "color-picker-initial-color"
    }), jsx_default2("div", {
      class: "color-picker-current-color"
    })));
  },
  init: function init40(self) {
    function handleInitialColorClick() {
      if (self.initialValue) {
        var _self$initialValue = self.initialValue, hex = _self$initialValue.hex, alpha = _self$initialValue.alpha;
        self.setValue({
          hex,
          alpha
        });
      }
    }
    self.$el.on("click", ".color-picker-initial-color", handleInitialColorClick);
    self.destroyInitialCurrentEvents = function destroyInitialCurrentEvents() {
      self.$el.off("click", ".color-picker-initial-color", handleInitialColorClick);
    };
  },
  update: function update16(self) {
    self.$el.find(".color-picker-module-initial-current-colors .color-picker-initial-color").css("background-color", self.initialValue.hex);
    self.$el.find(".color-picker-module-initial-current-colors .color-picker-current-color").css("background-color", self.value.hex);
  },
  destroy: function destroy43(self) {
    if (self.destroyInitialCurrentEvents) {
      self.destroyInitialCurrentEvents();
    }
    delete self.destroyInitialCurrentEvents;
  }
};

// node_modules/framework7/esm/components/color-picker/modules/rgb-bars.js
var rgb_bars_default = {
  render: function render10(self) {
    var _self$params = self.params, barLabel = _self$params.barLabel, barValue = _self$params.barValue, barValueEditable = _self$params.barValueEditable, redLabelText = _self$params.redLabelText, greenLabelText = _self$params.greenLabelText, blueLabelText = _self$params.blueLabelText;
    return jsx_default2("div", {
      class: "color-picker-module color-picker-module-rgb-bars"
    }, jsx_default2("div", {
      class: "color-picker-bar-wrap"
    }, barLabel && jsx_default2("div", {
      class: "color-picker-bar-label"
    }, redLabelText), jsx_default2("div", {
      class: "range-slider color-picker-bar color-picker-bar-red"
    }), barValue && jsx_default2("div", {
      class: "color-picker-bar-value"
    }, barValueEditable ? jsx_default2("input", {
      type: "number",
      step: "1",
      min: "0",
      max: "255",
      class: "color-picker-value-bar-red",
      "data-color-index": "0"
    }) : jsx_default2("span", {
      class: "color-picker-value-bar-red"
    }))), jsx_default2("div", {
      class: "color-picker-bar-wrap"
    }, barLabel && jsx_default2("div", {
      class: "color-picker-bar-label"
    }, greenLabelText), jsx_default2("div", {
      class: "range-slider color-picker-bar color-picker-bar-green"
    }), barValue && jsx_default2("div", {
      class: "color-picker-bar-value"
    }, barValueEditable ? jsx_default2("input", {
      type: "number",
      step: "1",
      min: "0",
      max: "255",
      class: "color-picker-value-bar-green",
      "data-color-index": "1"
    }) : jsx_default2("span", {
      class: "color-picker-value-bar-green"
    }))), jsx_default2("div", {
      class: "color-picker-bar-wrap"
    }, barLabel && jsx_default2("div", {
      class: "color-picker-bar-label"
    }, blueLabelText), jsx_default2("div", {
      class: "range-slider color-picker-bar color-picker-bar-blue"
    }), barValue && jsx_default2("div", {
      class: "color-picker-bar-value"
    }, barValueEditable ? jsx_default2("input", {
      type: "number",
      step: "1",
      min: "0",
      max: "255",
      class: "color-picker-value-bar-blue",
      "data-color-index": "2"
    }) : jsx_default2("span", {
      class: "color-picker-value-bar-blue"
    }))));
  },
  init: function init41(self) {
    self.redBar = self.app.range.create({
      el: self.$el.find(".color-picker-bar-red"),
      min: 0,
      max: 255,
      step: 1,
      value: 0,
      vertical: true,
      on: {
        change: function change(range, value) {
          self.setValue({
            rgb: [value, self.value.rgb[1], self.value.rgb[2]]
          });
        }
      }
    });
    self.greenBar = self.app.range.create({
      el: self.$el.find(".color-picker-bar-green"),
      min: 0,
      max: 255,
      step: 1,
      value: 0,
      vertical: true,
      on: {
        change: function change(range, value) {
          self.setValue({
            rgb: [self.value.rgb[0], value, self.value.rgb[2]]
          });
        }
      }
    });
    self.blueBar = self.app.range.create({
      el: self.$el.find(".color-picker-bar-blue"),
      min: 0,
      max: 255,
      step: 1,
      value: 0,
      vertical: true,
      on: {
        change: function change(range, value) {
          self.setValue({
            rgb: [self.value.rgb[0], self.value.rgb[1], value]
          });
        }
      }
    });
    function handleInputChange(e) {
      var rgb = [].concat(self.value.rgb);
      var index2 = parseInt(dom7_default(e.target).attr("data-color-index"), 10);
      var value = parseInt(e.target.value, 10);
      if (Number.isNaN(value)) {
        e.target.value = rgb[index2];
        return;
      }
      value = Math.max(0, Math.min(255, value));
      rgb[index2] = value;
      self.setValue({
        rgb
      });
    }
    self.$el.on("change", ".color-picker-module-rgb-bars input", handleInputChange);
    self.destroyRgbBarsEvents = function destroyRgbBarsEvents() {
      self.$el.off("change", ".color-picker-module-rgb-bars input", handleInputChange);
    };
  },
  update: function update17(self) {
    var value = self.value, redBar = self.redBar, greenBar = self.greenBar, blueBar = self.blueBar;
    var _self$params2 = self.params, barValue = _self$params2.barValue, barValueEditable = _self$params2.barValueEditable;
    var rgb = value.rgb;
    redBar.value = rgb[0];
    greenBar.value = rgb[1];
    blueBar.value = rgb[2];
    redBar.layout();
    greenBar.layout();
    blueBar.layout();
    redBar.$el.find(".range-bar").css("background-image", "linear-gradient(to top, rgb(0, " + rgb[1] + ", " + rgb[2] + "), rgb(255, " + rgb[1] + ", " + rgb[2] + "))");
    greenBar.$el.find(".range-bar").css("background-image", "linear-gradient(to top, rgb(" + rgb[0] + ", 0, " + rgb[2] + "), rgb(" + rgb[0] + ", 255, " + rgb[2] + "))");
    blueBar.$el.find(".range-bar").css("background-image", "linear-gradient(to top, rgb(" + rgb[0] + ", " + rgb[1] + ", 0), rgb(" + rgb[0] + ", " + rgb[1] + ", 255))");
    if (barValue && barValueEditable) {
      self.$el.find("input.color-picker-value-bar-red").val(rgb[0]);
      self.$el.find("input.color-picker-value-bar-green").val(rgb[1]);
      self.$el.find("input.color-picker-value-bar-blue").val(rgb[2]);
    } else if (barValue) {
      self.$el.find("span.color-picker-value-bar-red").text(rgb[0]);
      self.$el.find("span.color-picker-value-bar-green").text(rgb[1]);
      self.$el.find("span.color-picker-value-bar-blue").text(rgb[2]);
    }
  },
  destroy: function destroy44(self) {
    if (self.redBar && self.redBar.destroy) {
      self.redBar.destroy();
    }
    if (self.greenBar && self.greenBar.destroy) {
      self.greenBar.destroy();
    }
    if (self.blueBar && self.blueBar.destroy) {
      self.blueBar.destroy();
    }
    delete self.redBar;
    delete self.greenBar;
    delete self.blueBar;
    if (self.destroyRgbBarsEvents)
      self.destroyRgbBarsEvents();
    delete self.destroyRgbBarsEvents;
  }
};

// node_modules/framework7/esm/components/color-picker/modules/rgb-sliders.js
var rgb_sliders_default = {
  render: function render11(self) {
    var _self$params = self.params, sliderLabel = _self$params.sliderLabel, sliderValue = _self$params.sliderValue, sliderValueEditable = _self$params.sliderValueEditable, redLabelText = _self$params.redLabelText, greenLabelText = _self$params.greenLabelText, blueLabelText = _self$params.blueLabelText;
    return jsx_default2("div", {
      class: "color-picker-module color-picker-module-rgb-sliders"
    }, jsx_default2("div", {
      class: "color-picker-slider-wrap"
    }, sliderLabel && jsx_default2("div", {
      class: "color-picker-slider-label"
    }, redLabelText), jsx_default2("div", {
      class: "range-slider color-picker-slider color-picker-slider-red"
    }), sliderValue && jsx_default2("div", {
      class: "color-picker-slider-value"
    }, sliderValueEditable ? jsx_default2("input", {
      type: "number",
      step: "1",
      min: "0",
      max: "255",
      class: "color-picker-value-red",
      "data-color-index": "0"
    }) : jsx_default2("span", {
      class: "color-picker-value-red"
    }))), jsx_default2("div", {
      class: "color-picker-slider-wrap"
    }, sliderLabel && jsx_default2("div", {
      class: "color-picker-slider-label"
    }, greenLabelText), jsx_default2("div", {
      class: "range-slider color-picker-slider color-picker-slider-green"
    }), sliderValue && jsx_default2("div", {
      class: "color-picker-slider-value"
    }, sliderValueEditable ? jsx_default2("input", {
      type: "number",
      step: "1",
      min: "0",
      max: "255",
      class: "color-picker-value-green",
      "data-color-index": "1"
    }) : jsx_default2("span", {
      class: "color-picker-value-green"
    }))), jsx_default2("div", {
      class: "color-picker-slider-wrap"
    }, sliderLabel && jsx_default2("div", {
      class: "color-picker-slider-label"
    }, blueLabelText), jsx_default2("div", {
      class: "range-slider color-picker-slider color-picker-slider-blue"
    }), sliderValue && jsx_default2("div", {
      class: "color-picker-slider-value"
    }, sliderValueEditable ? jsx_default2("input", {
      type: "number",
      step: "1",
      min: "0",
      max: "255",
      class: "color-picker-value-blue",
      "data-color-index": "2"
    }) : jsx_default2("span", {
      class: "color-picker-value-blue"
    }))));
  },
  init: function init42(self) {
    self.redRangeSlider = self.app.range.create({
      el: self.$el.find(".color-picker-slider-red"),
      min: 0,
      max: 255,
      step: 1,
      value: 0,
      on: {
        change: function change(range, value) {
          self.setValue({
            rgb: [value, self.value.rgb[1], self.value.rgb[2]]
          });
        }
      }
    });
    self.greenRangeSlider = self.app.range.create({
      el: self.$el.find(".color-picker-slider-green"),
      min: 0,
      max: 255,
      step: 1,
      value: 0,
      on: {
        change: function change(range, value) {
          self.setValue({
            rgb: [self.value.rgb[0], value, self.value.rgb[2]]
          });
        }
      }
    });
    self.blueRangeSlider = self.app.range.create({
      el: self.$el.find(".color-picker-slider-blue"),
      min: 0,
      max: 255,
      step: 1,
      value: 0,
      on: {
        change: function change(range, value) {
          self.setValue({
            rgb: [self.value.rgb[0], self.value.rgb[1], value]
          });
        }
      }
    });
    function handleInputChange(e) {
      var rgb = [].concat(self.value.rgb);
      var index2 = parseInt(dom7_default(e.target).attr("data-color-index"), 10);
      var value = parseInt(e.target.value, 10);
      if (Number.isNaN(value)) {
        e.target.value = rgb[index2];
        return;
      }
      value = Math.max(0, Math.min(255, value));
      rgb[index2] = value;
      self.setValue({
        rgb
      });
    }
    self.$el.on("change", ".color-picker-module-rgb-sliders input", handleInputChange);
    self.destroyRgbSlidersEvents = function destroyRgbSlidersEvents() {
      self.$el.off("change", ".color-picker-module-rgb-sliders input", handleInputChange);
    };
  },
  update: function update18(self) {
    var app = self.app, value = self.value, redRangeSlider = self.redRangeSlider, greenRangeSlider = self.greenRangeSlider, blueRangeSlider = self.blueRangeSlider;
    var _self$params2 = self.params, sliderValue = _self$params2.sliderValue, sliderValueEditable = _self$params2.sliderValueEditable;
    var rgb = value.rgb;
    redRangeSlider.value = rgb[0];
    greenRangeSlider.value = rgb[1];
    blueRangeSlider.value = rgb[2];
    redRangeSlider.layout();
    greenRangeSlider.layout();
    blueRangeSlider.layout();
    redRangeSlider.$el[0].style.setProperty("--f7-range-knob-color", "rgb(" + rgb[0] + ", " + rgb[1] + ", " + rgb[2] + ")");
    greenRangeSlider.$el[0].style.setProperty("--f7-range-knob-color", "rgb(" + rgb[0] + ", " + rgb[1] + ", " + rgb[2] + ")");
    blueRangeSlider.$el[0].style.setProperty("--f7-range-knob-color", "rgb(" + rgb[0] + ", " + rgb[1] + ", " + rgb[2] + ")");
    var direction = app.rtl ? "to left" : "to right";
    redRangeSlider.$el.find(".range-bar").css("background-image", "linear-gradient(" + direction + ", rgb(0, " + rgb[1] + ", " + rgb[2] + "), rgb(255, " + rgb[1] + ", " + rgb[2] + "))");
    greenRangeSlider.$el.find(".range-bar").css("background-image", "linear-gradient(" + direction + ", rgb(" + rgb[0] + ", 0, " + rgb[2] + "), rgb(" + rgb[0] + ", 255, " + rgb[2] + "))");
    blueRangeSlider.$el.find(".range-bar").css("background-image", "linear-gradient(" + direction + ", rgb(" + rgb[0] + ", " + rgb[1] + ", 0), rgb(" + rgb[0] + ", " + rgb[1] + ", 255))");
    if (sliderValue && sliderValueEditable) {
      self.$el.find("input.color-picker-value-red").val(rgb[0]);
      self.$el.find("input.color-picker-value-green").val(rgb[1]);
      self.$el.find("input.color-picker-value-blue").val(rgb[2]);
    } else if (sliderValue) {
      self.$el.find("span.color-picker-value-red").text(rgb[0]);
      self.$el.find("span.color-picker-value-green").text(rgb[1]);
      self.$el.find("span.color-picker-value-blue").text(rgb[2]);
    }
  },
  destroy: function destroy45(self) {
    if (self.redRangeSlider && self.redRangeSlider.destroy) {
      self.redRangeSlider.destroy();
    }
    if (self.greenRangeSlider && self.greenRangeSlider.destroy) {
      self.greenRangeSlider.destroy();
    }
    if (self.blueRangeSlider && self.blueRangeSlider.destroy) {
      self.blueRangeSlider.destroy();
    }
    delete self.redRangeSlider;
    delete self.greenRangeSlider;
    delete self.blueRangeSlider;
    if (self.destroyRgbSlidersEvents)
      self.destroyRgbSlidersEvents();
    delete self.destroyRgbSlidersEvents;
  }
};

// node_modules/framework7/esm/components/color-picker/modules/sb-spectrum.js
var sb_spectrum_default = {
  render: function render12() {
    return jsx_default2("div", {
      class: "color-picker-module color-picker-module-sb-spectrum"
    }, jsx_default2("div", {
      class: "color-picker-sb-spectrum",
      style: "background-color: hsl(0, 100%, 50%)"
    }, jsx_default2("div", {
      class: "color-picker-sb-spectrum-handle"
    })));
  },
  init: function init43(self) {
    var app = self.app;
    var isTouched;
    var isMoved;
    var touchStartX;
    var touchStartY;
    var touchCurrentX;
    var touchCurrentY;
    var specterRect;
    var specterIsTouched;
    var specterHandleIsTouched;
    var $el = self.$el;
    function setSBFromSpecterCoords(x, y) {
      var s = (x - specterRect.left) / specterRect.width;
      var b = (y - specterRect.top) / specterRect.height;
      s = Math.max(0, Math.min(1, s));
      b = 1 - Math.max(0, Math.min(1, b));
      self.setValue({
        hsb: [self.value.hue, s, b]
      });
    }
    function handleTouchStart(e) {
      if (isMoved || isTouched)
        return;
      touchStartX = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
      touchCurrentX = touchStartX;
      touchStartY = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
      touchCurrentY = touchStartY;
      var $targetEl = dom7_default(e.target);
      specterHandleIsTouched = $targetEl.closest(".color-picker-sb-spectrum-handle").length > 0;
      if (!specterHandleIsTouched) {
        specterIsTouched = $targetEl.closest(".color-picker-sb-spectrum").length > 0;
      }
      if (specterIsTouched) {
        specterRect = $el.find(".color-picker-sb-spectrum")[0].getBoundingClientRect();
        setSBFromSpecterCoords(touchStartX, touchStartY);
      }
      if (specterHandleIsTouched || specterIsTouched) {
        $el.find(".color-picker-sb-spectrum-handle").addClass("color-picker-sb-spectrum-handle-pressed");
      }
    }
    function handleTouchMove(e) {
      if (!(specterIsTouched || specterHandleIsTouched))
        return;
      touchCurrentX = e.type === "touchmove" ? e.targetTouches[0].pageX : e.pageX;
      touchCurrentY = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
      e.preventDefault();
      if (!isMoved) {
        isMoved = true;
        if (specterHandleIsTouched) {
          specterRect = $el.find(".color-picker-sb-spectrum")[0].getBoundingClientRect();
        }
      }
      if (specterIsTouched || specterHandleIsTouched) {
        setSBFromSpecterCoords(touchCurrentX, touchCurrentY);
      }
    }
    function handleTouchEnd() {
      isMoved = false;
      if (specterIsTouched || specterHandleIsTouched) {
        $el.find(".color-picker-sb-spectrum-handle").removeClass("color-picker-sb-spectrum-handle-pressed");
      }
      specterIsTouched = false;
      specterHandleIsTouched = false;
    }
    function handleResize() {
      self.modules["sb-spectrum"].update(self);
    }
    var passiveListener = app.touchEvents.start === "touchstart" && getSupport().passiveListener ? {
      passive: true,
      capture: false
    } : false;
    self.$el.on(app.touchEvents.start, handleTouchStart, passiveListener);
    app.on("touchmove:active", handleTouchMove);
    app.on("touchend:passive", handleTouchEnd);
    app.on("resize", handleResize);
    self.destroySpectrumEvents = function destroySpectrumEvents() {
      self.$el.off(app.touchEvents.start, handleTouchStart, passiveListener);
      app.off("touchmove:active", handleTouchMove);
      app.off("touchend:passive", handleTouchEnd);
      app.off("resize", handleResize);
    };
  },
  update: function update19(self) {
    var value = self.value;
    var hsl = value.hsl, hsb = value.hsb;
    var specterWidth = self.$el.find(".color-picker-sb-spectrum")[0].offsetWidth;
    var specterHeight = self.$el.find(".color-picker-sb-spectrum")[0].offsetHeight;
    self.$el.find(".color-picker-sb-spectrum").css("background-color", "hsl(" + hsl[0] + ", 100%, 50%)");
    self.$el.find(".color-picker-sb-spectrum-handle").css("background-color", "hsl(" + hsl[0] + ", " + hsl[1] * 100 + "%, " + hsl[2] * 100 + "%)").transform("translate(" + specterWidth * hsb[1] + "px, " + specterHeight * (1 - hsb[2]) + "px)");
  },
  destroy: function destroy46(self) {
    if (self.destroySpectrumEvents)
      self.destroySpectrumEvents();
    delete self.destroySpectrumEvents;
  }
};

// node_modules/framework7/esm/components/color-picker/modules/hs-spectrum.js
var hs_spectrum_default = {
  render: function render13() {
    return jsx_default2("div", {
      class: "color-picker-module color-picker-module-hs-spectrum"
    }, jsx_default2("div", {
      class: "color-picker-hs-spectrum"
    }, jsx_default2("div", {
      class: "color-picker-hs-spectrum-handle"
    })));
  },
  init: function init44(self) {
    var app = self.app;
    var isTouched;
    var isMoved;
    var touchStartX;
    var touchStartY;
    var touchCurrentX;
    var touchCurrentY;
    var specterRect;
    var specterIsTouched;
    var specterHandleIsTouched;
    var $el = self.$el;
    function setHSFromSpecterCoords(x, y) {
      var h = (x - specterRect.left) / specterRect.width * 360;
      var s = (y - specterRect.top) / specterRect.height;
      h = Math.max(0, Math.min(360, h));
      s = 1 - Math.max(0, Math.min(1, s));
      self.setValue({
        hsb: [h, s, self.value.hsb[2]]
      });
    }
    function handleTouchStart(e) {
      if (isMoved || isTouched)
        return;
      touchStartX = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
      touchCurrentX = touchStartX;
      touchStartY = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
      touchCurrentY = touchStartY;
      var $targetEl = dom7_default(e.target);
      specterHandleIsTouched = $targetEl.closest(".color-picker-hs-spectrum-handle").length > 0;
      if (!specterHandleIsTouched) {
        specterIsTouched = $targetEl.closest(".color-picker-hs-spectrum").length > 0;
      }
      if (specterIsTouched) {
        specterRect = $el.find(".color-picker-hs-spectrum")[0].getBoundingClientRect();
        setHSFromSpecterCoords(touchStartX, touchStartY);
      }
      if (specterHandleIsTouched || specterIsTouched) {
        $el.find(".color-picker-hs-spectrum-handle").addClass("color-picker-hs-spectrum-handle-pressed");
      }
    }
    function handleTouchMove(e) {
      if (!(specterIsTouched || specterHandleIsTouched))
        return;
      touchCurrentX = e.type === "touchmove" ? e.targetTouches[0].pageX : e.pageX;
      touchCurrentY = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
      e.preventDefault();
      if (!isMoved) {
        isMoved = true;
        if (specterHandleIsTouched) {
          specterRect = $el.find(".color-picker-hs-spectrum")[0].getBoundingClientRect();
        }
      }
      if (specterIsTouched || specterHandleIsTouched) {
        setHSFromSpecterCoords(touchCurrentX, touchCurrentY);
      }
    }
    function handleTouchEnd() {
      isMoved = false;
      if (specterIsTouched || specterHandleIsTouched) {
        $el.find(".color-picker-hs-spectrum-handle").removeClass("color-picker-hs-spectrum-handle-pressed");
      }
      specterIsTouched = false;
      specterHandleIsTouched = false;
    }
    function handleResize() {
      self.modules["hs-spectrum"].update(self);
    }
    var passiveListener = app.touchEvents.start === "touchstart" && getSupport().passiveListener ? {
      passive: true,
      capture: false
    } : false;
    self.$el.on(app.touchEvents.start, handleTouchStart, passiveListener);
    app.on("touchmove:active", handleTouchMove);
    app.on("touchend:passive", handleTouchEnd);
    app.on("resize", handleResize);
    self.destroySpectrumEvents = function destroySpectrumEvents() {
      self.$el.off(app.touchEvents.start, handleTouchStart, passiveListener);
      app.off("touchmove:active", handleTouchMove);
      app.off("touchend:passive", handleTouchEnd);
      app.off("resize", handleResize);
    };
  },
  update: function update20(self) {
    var value = self.value;
    var hsb = value.hsb;
    var specterWidth = self.$el.find(".color-picker-hs-spectrum")[0].offsetWidth;
    var specterHeight = self.$el.find(".color-picker-hs-spectrum")[0].offsetHeight;
    var hslBright = colorHsbToHsl(hsb[0], hsb[1], 1);
    self.$el.find(".color-picker-hs-spectrum-handle").css("background-color", "hsl(" + hslBright[0] + ", " + hslBright[1] * 100 + "%, " + hslBright[2] * 100 + "%)").transform("translate(" + specterWidth * (hsb[0] / 360) + "px, " + specterHeight * (1 - hsb[1]) + "px)");
  },
  destroy: function destroy47(self) {
    if (self.destroySpectrumEvents)
      self.destroySpectrumEvents();
    delete self.destroySpectrumEvents;
  }
};

// node_modules/framework7/esm/components/color-picker/modules/wheel.js
function svgWheelCircles() {
  var total = 256;
  var circles = "";
  for (var i = total; i > 0; i -= 1) {
    var angle = i * Math.PI / (total / 2);
    var hue = 360 / total * i;
    circles += '<circle cx="' + (150 - Math.sin(angle) * 125) + '" cy="' + (150 - Math.cos(angle) * 125) + '" r="25" fill="hsl(' + hue + ', 100%, 50%)"></circle>';
  }
  return circles;
}
var wheel_default = {
  render: function render14() {
    return jsx_default2("div", {
      class: "color-picker-module color-picker-module-wheel"
    }, jsx_default2("div", {
      class: "color-picker-wheel"
    }, jsx_default2("svg", {
      viewBox: "0 0 300 300",
      width: "300",
      height: "300"
    }, svgWheelCircles()), jsx_default2("div", {
      class: "color-picker-wheel-handle"
    }), jsx_default2("div", {
      class: "color-picker-sb-spectrum",
      style: "background-color: hsl(0, 100%, 50%)"
    }, jsx_default2("div", {
      class: "color-picker-sb-spectrum-handle"
    }))));
  },
  init: function init45(self) {
    var app = self.app;
    var isTouched;
    var isMoved;
    var touchStartX;
    var touchStartY;
    var touchCurrentX;
    var touchCurrentY;
    var wheelRect;
    var wheelIsTouched;
    var wheelHandleIsTouched;
    var specterRect;
    var specterIsTouched;
    var specterHandleIsTouched;
    var $el = self.$el;
    function setHueFromWheelCoords(x, y) {
      var wheelCenterX = wheelRect.left + wheelRect.width / 2;
      var wheelCenterY = wheelRect.top + wheelRect.height / 2;
      var angleRad = Math.atan2(y - wheelCenterY, x - wheelCenterX);
      var angleDeg = angleRad * 180 / Math.PI + 90;
      if (angleDeg < 0)
        angleDeg += 360;
      angleDeg = 360 - angleDeg;
      self.setValue({
        hue: angleDeg
      });
    }
    function setSBFromSpecterCoords(x, y) {
      var s = (x - specterRect.left) / specterRect.width;
      var b = (y - specterRect.top) / specterRect.height;
      s = Math.max(0, Math.min(1, s));
      b = 1 - Math.max(0, Math.min(1, b));
      self.setValue({
        hsb: [self.value.hue, s, b]
      });
    }
    function handleTouchStart(e) {
      if (isMoved || isTouched)
        return;
      touchStartX = e.type === "touchstart" ? e.targetTouches[0].pageX : e.pageX;
      touchCurrentX = touchStartX;
      touchStartY = e.type === "touchstart" ? e.targetTouches[0].pageY : e.pageY;
      touchCurrentY = touchStartY;
      var $targetEl = dom7_default(e.target);
      wheelHandleIsTouched = $targetEl.closest(".color-picker-wheel-handle").length > 0;
      wheelIsTouched = $targetEl.closest("circle").length > 0;
      specterHandleIsTouched = $targetEl.closest(".color-picker-sb-spectrum-handle").length > 0;
      if (!specterHandleIsTouched) {
        specterIsTouched = $targetEl.closest(".color-picker-sb-spectrum").length > 0;
      }
      if (wheelIsTouched) {
        wheelRect = $el.find(".color-picker-wheel")[0].getBoundingClientRect();
        setHueFromWheelCoords(touchStartX, touchStartY);
      }
      if (specterIsTouched) {
        specterRect = $el.find(".color-picker-sb-spectrum")[0].getBoundingClientRect();
        setSBFromSpecterCoords(touchStartX, touchStartY);
      }
      if (specterHandleIsTouched || specterIsTouched) {
        $el.find(".color-picker-sb-spectrum-handle").addClass("color-picker-sb-spectrum-handle-pressed");
      }
    }
    function handleTouchMove(e) {
      if (!(wheelIsTouched || wheelHandleIsTouched) && !(specterIsTouched || specterHandleIsTouched))
        return;
      touchCurrentX = e.type === "touchmove" ? e.targetTouches[0].pageX : e.pageX;
      touchCurrentY = e.type === "touchmove" ? e.targetTouches[0].pageY : e.pageY;
      e.preventDefault();
      if (!isMoved) {
        isMoved = true;
        if (wheelHandleIsTouched) {
          wheelRect = $el.find(".color-picker-wheel")[0].getBoundingClientRect();
        }
        if (specterHandleIsTouched) {
          specterRect = $el.find(".color-picker-sb-spectrum")[0].getBoundingClientRect();
        }
      }
      if (wheelIsTouched || wheelHandleIsTouched) {
        setHueFromWheelCoords(touchCurrentX, touchCurrentY);
      }
      if (specterIsTouched || specterHandleIsTouched) {
        setSBFromSpecterCoords(touchCurrentX, touchCurrentY);
      }
    }
    function handleTouchEnd() {
      isMoved = false;
      if (specterIsTouched || specterHandleIsTouched) {
        $el.find(".color-picker-sb-spectrum-handle").removeClass("color-picker-sb-spectrum-handle-pressed");
      }
      wheelIsTouched = false;
      wheelHandleIsTouched = false;
      specterIsTouched = false;
      specterHandleIsTouched = false;
    }
    function handleResize() {
      self.modules.wheel.update(self);
    }
    var passiveListener = app.touchEvents.start === "touchstart" && getSupport().passiveListener ? {
      passive: true,
      capture: false
    } : false;
    self.$el.on(app.touchEvents.start, handleTouchStart, passiveListener);
    app.on("touchmove:active", handleTouchMove);
    app.on("touchend:passive", handleTouchEnd);
    app.on("resize", handleResize);
    self.destroyWheelEvents = function destroyWheelEvents() {
      self.$el.off(app.touchEvents.start, handleTouchStart, passiveListener);
      app.off("touchmove:active", handleTouchMove);
      app.off("touchend:passive", handleTouchEnd);
      app.off("resize", handleResize);
    };
  },
  update: function update21(self) {
    var value = self.value;
    var hsl = value.hsl, hsb = value.hsb;
    var specterWidth = self.$el.find(".color-picker-sb-spectrum")[0].offsetWidth;
    var specterHeight = self.$el.find(".color-picker-sb-spectrum")[0].offsetHeight;
    var wheelSize = self.$el.find(".color-picker-wheel")[0].offsetWidth;
    var wheelHalfSize = wheelSize / 2;
    var angleRad = value.hue * Math.PI / 180;
    var handleSize = wheelSize / 6;
    var handleHalfSize = handleSize / 2;
    var tX = wheelHalfSize - Math.sin(angleRad) * (wheelHalfSize - handleHalfSize) - handleHalfSize;
    var tY = wheelHalfSize - Math.cos(angleRad) * (wheelHalfSize - handleHalfSize) - handleHalfSize;
    self.$el.find(".color-picker-wheel-handle").css("background-color", "hsl(" + hsl[0] + ", 100%, 50%)").transform("translate(" + tX + "px, " + tY + "px)");
    self.$el.find(".color-picker-sb-spectrum").css("background-color", "hsl(" + hsl[0] + ", 100%, 50%)");
    self.$el.find(".color-picker-sb-spectrum-handle").css("background-color", "hsl(" + hsl[0] + ", " + hsl[1] * 100 + "%, " + hsl[2] * 100 + "%)").transform("translate(" + specterWidth * hsb[1] + "px, " + specterHeight * (1 - hsb[2]) + "px)");
  },
  destroy: function destroy48(self) {
    if (self.destroyWheelEvents)
      self.destroyWheelEvents();
    delete self.destroyWheelEvents;
  }
};

// node_modules/framework7/esm/components/color-picker/color-picker-class.js
function _defineProperties7(target, props) {
  for (var i = 0; i < props.length; i++) {
    var descriptor = props[i];
    descriptor.enumerable = descriptor.enumerable || false;
    descriptor.configurable = true;
    if ("value" in descriptor)
      descriptor.writable = true;
    Object.defineProperty(target, descriptor.key, descriptor);
  }
}
function _createClass7(Constructor, protoProps, staticProps) {
  if (protoProps)
    _defineProperties7(Constructor.prototype, protoProps);
  if (staticProps)
    _defineProperties7(Constructor, staticProps);
  return Constructor;
}
function _assertThisInitialized27(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose27(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf27(subClass, superClass);
}
function _setPrototypeOf27(o, p) {
  _setPrototypeOf27 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf27(o, p);
}
var ColorPicker = function(_Framework7Class) {
  _inheritsLoose27(ColorPicker2, _Framework7Class);
  function ColorPicker2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var self = _assertThisInitialized27(_this);
    self.params = extend({}, app.params.colorPicker, params);
    var $containerEl;
    if (self.params.containerEl) {
      $containerEl = dom7_default(self.params.containerEl);
      if ($containerEl.length === 0)
        return self || _assertThisInitialized27(_this);
    }
    var $inputEl;
    if (self.params.inputEl) {
      $inputEl = dom7_default(self.params.inputEl);
    }
    var $targetEl;
    if (self.params.targetEl) {
      $targetEl = dom7_default(self.params.targetEl);
    }
    extend(self, {
      app,
      $containerEl,
      containerEl: $containerEl && $containerEl[0],
      inline: $containerEl && $containerEl.length > 0,
      $inputEl,
      inputEl: $inputEl && $inputEl[0],
      $targetEl,
      targetEl: $targetEl && $targetEl[0],
      initialized: false,
      opened: false,
      url: self.params.url,
      modules: {
        "alpha-slider": alpha_slider_default,
        "current-color": current_color_default,
        hex: hex_default,
        "hsb-sliders": hsb_sliders_default,
        "hue-slider": hue_slider_default,
        "brightness-slider": brightness_slider_default,
        palette: palette_default,
        "initial-current-colors": initial_current_colors_default,
        "rgb-bars": rgb_bars_default,
        "rgb-sliders": rgb_sliders_default,
        "sb-spectrum": sb_spectrum_default,
        "hs-spectrum": hs_spectrum_default,
        wheel: wheel_default
      }
    });
    function onInputClick() {
      self.open();
    }
    function onInputFocus(e) {
      e.preventDefault();
    }
    function onTargetClick() {
      self.open();
    }
    function onHtmlClick(e) {
      if (self.destroyed || !self.params)
        return;
      if (self.params.openIn === "page")
        return;
      var $clickTargetEl = dom7_default(e.target);
      if (!self.opened || self.closing)
        return;
      if ($clickTargetEl.closest('[class*="backdrop"]').length)
        return;
      if ($clickTargetEl.closest(".color-picker-popup, .color-picker-popover").length)
        return;
      if ($inputEl && $inputEl.length > 0) {
        if ($clickTargetEl[0] !== $inputEl[0] && $clickTargetEl.closest(".sheet-modal").length === 0) {
          self.close();
        }
      } else if (dom7_default(e.target).closest(".sheet-modal").length === 0) {
        self.close();
      }
    }
    extend(self, {
      attachInputEvents: function attachInputEvents() {
        self.$inputEl.on("click", onInputClick);
        if (self.params.inputReadOnly) {
          self.$inputEl.on("focus mousedown", onInputFocus);
          if (self.$inputEl[0]) {
            self.$inputEl[0].f7ValidateReadonly = true;
          }
        }
      },
      detachInputEvents: function detachInputEvents() {
        self.$inputEl.off("click", onInputClick);
        if (self.params.inputReadOnly) {
          self.$inputEl.off("focus mousedown", onInputFocus);
          if (self.$inputEl[0]) {
            delete self.$inputEl[0].f7ValidateReadonly;
          }
        }
      },
      attachTargetEvents: function attachTargetEvents() {
        self.$targetEl.on("click", onTargetClick);
      },
      detachTargetEvents: function detachTargetEvents() {
        self.$targetEl.off("click", onTargetClick);
      },
      attachHtmlEvents: function attachHtmlEvents() {
        app.on("click", onHtmlClick);
      },
      detachHtmlEvents: function detachHtmlEvents() {
        app.off("click", onHtmlClick);
      }
    });
    self.init();
    return self || _assertThisInitialized27(_this);
  }
  var _proto = ColorPicker2.prototype;
  _proto.attachEvents = function attachEvents2() {
    var self = this;
    self.centerModules = self.centerModules.bind(self);
    if (self.params.centerModules) {
      self.app.on("resize", self.centerModules);
    }
  };
  _proto.detachEvents = function detachEvents2() {
    var self = this;
    if (self.params.centerModules) {
      self.app.off("resize", self.centerModules);
    }
  };
  _proto.centerModules = function centerModules() {
    var self = this;
    if (!self.opened || !self.$el || self.inline)
      return;
    var $pageContentEl = self.$el.find(".page-content");
    if (!$pageContentEl.length)
      return;
    var _$pageContentEl$ = $pageContentEl[0], scrollHeight = _$pageContentEl$.scrollHeight, offsetHeight = _$pageContentEl$.offsetHeight;
    if (scrollHeight <= offsetHeight) {
      $pageContentEl.addClass("justify-content-center");
    } else {
      $pageContentEl.removeClass("justify-content-center");
    }
  };
  _proto.initInput = function initInput() {
    var self = this;
    if (!self.$inputEl)
      return;
    if (self.params.inputReadOnly)
      self.$inputEl.prop("readOnly", true);
  };
  _proto.getModalType = function getModalType() {
    var self = this;
    var app = self.app, modal = self.modal, params = self.params;
    var openIn = params.openIn, openInPhone = params.openInPhone;
    var device2 = getDevice();
    if (modal && modal.type)
      return modal.type;
    if (openIn !== "auto")
      return openIn;
    if (self.inline)
      return null;
    if (device2.ios) {
      return device2.ipad ? "popover" : openInPhone;
    }
    if (app.width >= 768 || device2.desktop && app.theme === "aurora") {
      return "popover";
    }
    return openInPhone;
  };
  _proto.formatValue = function formatValue() {
    var self = this;
    var value = self.value;
    if (self.params.formatValue) {
      return self.params.formatValue.call(self, value);
    }
    return value.hex;
  };
  _proto.normalizeHsValues = function normalizeHsValues(arr) {
    return [Math.floor(arr[0] * 10) / 10, Math.floor(arr[1] * 1e3) / 1e3, Math.floor(arr[2] * 1e3) / 1e3];
  };
  _proto.setValue = function setValue(value, updateModules) {
    if (value === void 0) {
      value = {};
    }
    if (updateModules === void 0) {
      updateModules = true;
    }
    var self = this;
    if (typeof value === "undefined")
      return;
    var _ref = self.value || {}, hex = _ref.hex, rgb = _ref.rgb, hsl = _ref.hsl, hsb = _ref.hsb, _ref$alpha = _ref.alpha, alpha = _ref$alpha === void 0 ? 1 : _ref$alpha, hue = _ref.hue, rgba = _ref.rgba, hsla = _ref.hsla;
    var needChangeEvent = self.value || !self.value && !self.params.value;
    var valueChanged;
    Object.keys(value).forEach(function(k) {
      if (!self.value || typeof self.value[k] === "undefined") {
        valueChanged = true;
        return;
      }
      var v = value[k];
      if (Array.isArray(v)) {
        v.forEach(function(subV, subIndex) {
          if (subV !== self.value[k][subIndex]) {
            valueChanged = true;
          }
        });
      } else if (v !== self.value[k]) {
        valueChanged = true;
      }
    });
    if (!valueChanged)
      return;
    if (value.rgb || value.rgba) {
      var _ref2 = value.rgb || value.rgba, r = _ref2[0], g = _ref2[1], b = _ref2[2], _ref2$ = _ref2[3], a = _ref2$ === void 0 ? alpha : _ref2$;
      rgb = [r, g, b];
      hex = colorRgbToHex.apply(void 0, rgb);
      hsl = colorRgbToHsl.apply(void 0, rgb);
      hsb = colorHslToHsb.apply(void 0, hsl);
      hsl = self.normalizeHsValues(hsl);
      hsb = self.normalizeHsValues(hsb);
      hue = hsb[0];
      alpha = a;
      rgba = [rgb[0], rgb[1], rgb[2], a];
      hsla = [hsl[0], hsl[1], hsl[2], a];
    }
    if (value.hsl || value.hsla) {
      var _ref3 = value.hsl || value.hsla, h = _ref3[0], s = _ref3[1], l = _ref3[2], _ref3$ = _ref3[3], _a = _ref3$ === void 0 ? alpha : _ref3$;
      hsl = [h, s, l];
      rgb = colorHslToRgb.apply(void 0, hsl);
      hex = colorRgbToHex.apply(void 0, rgb);
      hsb = colorHslToHsb.apply(void 0, hsl);
      hsl = self.normalizeHsValues(hsl);
      hsb = self.normalizeHsValues(hsb);
      hue = hsb[0];
      alpha = _a;
      rgba = [rgb[0], rgb[1], rgb[2], _a];
      hsla = [hsl[0], hsl[1], hsl[2], _a];
    }
    if (value.hsb) {
      var _value$hsb = value.hsb, _h = _value$hsb[0], _s = _value$hsb[1], _b = _value$hsb[2], _value$hsb$ = _value$hsb[3], _a2 = _value$hsb$ === void 0 ? alpha : _value$hsb$;
      hsb = [_h, _s, _b];
      hsl = colorHsbToHsl.apply(void 0, hsb);
      rgb = colorHslToRgb.apply(void 0, hsl);
      hex = colorRgbToHex.apply(void 0, rgb);
      hsl = self.normalizeHsValues(hsl);
      hsb = self.normalizeHsValues(hsb);
      hue = hsb[0];
      alpha = _a2;
      rgba = [rgb[0], rgb[1], rgb[2], _a2];
      hsla = [hsl[0], hsl[1], hsl[2], _a2];
    }
    if (value.hex) {
      rgb = colorHexToRgb(value.hex);
      hex = colorRgbToHex.apply(void 0, rgb);
      hsl = colorRgbToHsl.apply(void 0, rgb);
      hsb = colorHslToHsb.apply(void 0, hsl);
      hsl = self.normalizeHsValues(hsl);
      hsb = self.normalizeHsValues(hsb);
      hue = hsb[0];
      rgba = [rgb[0], rgb[1], rgb[2], alpha];
      hsla = [hsl[0], hsl[1], hsl[2], alpha];
    }
    if (typeof value.alpha !== "undefined") {
      alpha = value.alpha;
      if (typeof rgb !== "undefined") {
        rgba = [rgb[0], rgb[1], rgb[2], alpha];
      }
      if (typeof hsl !== "undefined") {
        hsla = [hsl[0], hsl[1], hsl[2], alpha];
      }
    }
    if (typeof value.hue !== "undefined") {
      var _hsl = hsl, _h2 = _hsl[0], _s2 = _hsl[1], _l = _hsl[2];
      hsl = [value.hue, _s2, _l];
      hsb = colorHslToHsb.apply(void 0, hsl);
      rgb = colorHslToRgb.apply(void 0, hsl);
      hex = colorRgbToHex.apply(void 0, rgb);
      hsl = self.normalizeHsValues(hsl);
      hsb = self.normalizeHsValues(hsb);
      hue = hsb[0];
      rgba = [rgb[0], rgb[1], rgb[2], alpha];
      hsla = [hsl[0], hsl[1], hsl[2], alpha];
    }
    self.value = {
      hex,
      alpha,
      hue,
      rgb,
      hsl,
      hsb,
      rgba,
      hsla
    };
    if (!self.initialValue)
      self.initialValue = extend({}, self.value);
    self.updateValue(needChangeEvent);
    if (self.opened && updateModules) {
      self.updateModules();
    }
  };
  _proto.getValue = function getValue() {
    var self = this;
    return self.value;
  };
  _proto.updateValue = function updateValue(fireEvents) {
    if (fireEvents === void 0) {
      fireEvents = true;
    }
    var self = this;
    var $inputEl = self.$inputEl, value = self.value, $targetEl = self.$targetEl;
    if ($targetEl && self.params.targetElSetBackgroundColor) {
      var rgba = value.rgba;
      $targetEl.css("background-color", "rgba(" + rgba.join(", ") + ")");
    }
    if (fireEvents) {
      self.emit("local::change colorPickerChange", self, value);
    }
    if ($inputEl && $inputEl.length) {
      var inputValue = self.formatValue(value);
      if ($inputEl && $inputEl.length) {
        $inputEl.val(inputValue);
        if (fireEvents) {
          $inputEl.trigger("change");
        }
      }
    }
  };
  _proto.updateModules = function updateModules() {
    var self = this;
    var modules = self.modules;
    self.params.modules.forEach(function(m) {
      if (typeof m === "string" && modules[m] && modules[m].update) {
        modules[m].update(self);
      } else if (m && m.update) {
        m.update(self);
      }
    });
  };
  _proto.update = function update22() {
    var self = this;
    self.updateModules();
  };
  _proto.renderPicker = function renderPicker() {
    var self = this;
    var params = self.params, modules = self.modules;
    var html2 = "";
    params.modules.forEach(function(m) {
      if (typeof m === "string" && modules[m] && modules[m].render) {
        html2 += modules[m].render(self);
      } else if (m && m.render) {
        html2 += m.render(self);
      }
    });
    return html2;
  };
  _proto.renderNavbar = function renderNavbar() {
    var self = this;
    if (self.params.renderNavbar) {
      return self.params.renderNavbar.call(self, self);
    }
    var _self$params = self.params, openIn = _self$params.openIn, navbarTitleText = _self$params.navbarTitleText, navbarBackLinkText = _self$params.navbarBackLinkText, navbarCloseText = _self$params.navbarCloseText;
    return jsx_default2("div", {
      class: "navbar"
    }, jsx_default2("div", {
      class: "navbar-bg"
    }), jsx_default2("div", {
      class: "navbar-inner sliding"
    }, openIn === "page" && jsx_default2("div", {
      class: "left"
    }, jsx_default2("a", {
      class: "link back"
    }, jsx_default2("i", {
      class: "icon icon-back"
    }), jsx_default2("span", {
      class: "if-not-md"
    }, navbarBackLinkText))), jsx_default2("div", {
      class: "title"
    }, navbarTitleText), openIn !== "page" && jsx_default2("div", {
      class: "right"
    }, jsx_default2("a", {
      class: "link popup-close",
      "data-popup": ".color-picker-popup"
    }, navbarCloseText))));
  };
  _proto.renderToolbar = function renderToolbar() {
    var self = this;
    if (self.params.renderToolbar) {
      return self.params.renderToolbar.call(self, self);
    }
    return jsx_default2("div", {
      class: "toolbar toolbar-top no-shadow"
    }, jsx_default2("div", {
      class: "toolbar-inner"
    }, jsx_default2("div", {
      class: "left"
    }), jsx_default2("div", {
      class: "right"
    }, jsx_default2("a", {
      class: "link sheet-close popover-close",
      "data-sheet": ".color-picker-sheet-modal",
      "data-popover": ".color-picker-popover"
    }, self.params.toolbarCloseText))));
  };
  _proto.renderInline = function renderInline() {
    var self = this;
    var _self$params2 = self.params, cssClass = _self$params2.cssClass, groupedModules = _self$params2.groupedModules;
    return jsx_default2("div", {
      class: "color-picker color-picker-inline " + (groupedModules ? "color-picker-grouped-modules" : "") + " " + (cssClass || "")
    }, self.renderPicker());
  };
  _proto.renderSheet = function renderSheet() {
    var self = this;
    var _self$params3 = self.params, cssClass = _self$params3.cssClass, toolbarSheet = _self$params3.toolbarSheet, groupedModules = _self$params3.groupedModules;
    return jsx_default2("div", {
      class: "sheet-modal color-picker color-picker-sheet-modal " + (groupedModules ? "color-picker-grouped-modules" : "") + " " + (cssClass || "")
    }, toolbarSheet && self.renderToolbar(), jsx_default2("div", {
      class: "sheet-modal-inner"
    }, jsx_default2("div", {
      class: "page-content"
    }, self.renderPicker())));
  };
  _proto.renderPopover = function renderPopover() {
    var self = this;
    var _self$params4 = self.params, cssClass = _self$params4.cssClass, toolbarPopover = _self$params4.toolbarPopover, groupedModules = _self$params4.groupedModules;
    return jsx_default2("div", {
      class: "popover color-picker-popover " + (cssClass || "")
    }, jsx_default2("div", {
      class: "popover-inner"
    }, jsx_default2("div", {
      class: "color-picker " + (groupedModules ? "color-picker-grouped-modules" : "")
    }, toolbarPopover && self.renderToolbar(), jsx_default2("div", {
      class: "page-content"
    }, self.renderPicker()))));
  };
  _proto.renderPopup = function renderPopup() {
    var self = this;
    var _self$params5 = self.params, cssClass = _self$params5.cssClass, navbarPopup = _self$params5.navbarPopup, groupedModules = _self$params5.groupedModules;
    return jsx_default2("div", {
      class: "popup color-picker-popup " + (cssClass || "")
    }, jsx_default2("div", {
      class: "page"
    }, navbarPopup && self.renderNavbar(), jsx_default2("div", {
      class: "color-picker " + (groupedModules ? "color-picker-grouped-modules" : "")
    }, jsx_default2("div", {
      class: "page-content"
    }, self.renderPicker()))));
  };
  _proto.renderPage = function renderPage() {
    var self = this;
    var _self$params6 = self.params, cssClass = _self$params6.cssClass, groupedModules = _self$params6.groupedModules;
    return jsx_default2("div", {
      class: "page color-picker-page " + (cssClass || ""),
      "data-name": "color-picker-page"
    }, self.renderNavbar(), jsx_default2("div", {
      class: "color-picker " + (groupedModules ? "color-picker-grouped-modules" : "")
    }, jsx_default2("div", {
      class: "page-content"
    }, self.renderPicker())));
  };
  _proto.render = function render15() {
    var self = this;
    var params = self.params;
    if (params.render)
      return params.render.call(self);
    if (self.inline)
      return self.renderInline();
    if (params.openIn === "page") {
      return self.renderPage();
    }
    var modalType = self.getModalType();
    if (modalType === "popover")
      return self.renderPopover();
    if (modalType === "sheet")
      return self.renderSheet();
    if (modalType === "popup")
      return self.renderPopup();
  };
  _proto.onOpen = function onOpen() {
    var self = this;
    var initialized = self.initialized, $el = self.$el, app = self.app, $inputEl = self.$inputEl, inline = self.inline, value = self.value, params = self.params, modules = self.modules;
    self.closing = false;
    self.opened = true;
    self.opening = true;
    self.attachEvents();
    params.modules.forEach(function(m) {
      if (typeof m === "string" && modules[m] && modules[m].init) {
        modules[m].init(self);
      } else if (m && m.init) {
        m.init(self);
      }
    });
    var updateValue = !value && params.value;
    if (!initialized) {
      if (value)
        self.setValue(value);
      else if (params.value) {
        self.setValue(params.value, false);
      } else if (!params.value) {
        self.setValue({
          hex: "#ff0000"
        }, false);
      }
    } else if (value) {
      self.initialValue = extend({}, value);
      self.setValue(value, false);
    }
    if (updateValue)
      self.updateValue();
    self.updateModules();
    if (params.centerModules) {
      self.centerModules();
    }
    if (!inline && $inputEl && $inputEl.length && app.theme === "md") {
      $inputEl.trigger("focus");
    }
    self.initialized = true;
    if ($el) {
      $el.trigger("colorpicker:open");
    }
    if ($inputEl) {
      $inputEl.trigger("colorpicker:open");
    }
    self.emit("local::open colorPickerOpen", self);
  };
  _proto.onOpened = function onOpened() {
    var self = this;
    self.opening = false;
    if (self.$el) {
      self.$el.trigger("colorpicker:opened");
    }
    if (self.$inputEl) {
      self.$inputEl.trigger("colorpicker:opened");
    }
    self.emit("local::opened colorPickerOpened", self);
  };
  _proto.onClose = function onClose() {
    var self = this;
    var app = self.app, params = self.params, modules = self.modules;
    self.opening = false;
    self.closing = true;
    self.detachEvents();
    if (self.$inputEl) {
      if (app.theme === "md") {
        self.$inputEl.trigger("blur");
      } else {
        var validate2 = self.$inputEl.attr("validate");
        var required = self.$inputEl.attr("required");
        if (validate2 && required) {
          app.input.validate(self.$inputEl);
        }
      }
    }
    params.modules.forEach(function(m) {
      if (typeof m === "string" && modules[m] && modules[m].destroy) {
        modules[m].destroy(self);
      } else if (m && m.destroy) {
        m.destroy(self);
      }
    });
    if (self.$el) {
      self.$el.trigger("colorpicker:close");
    }
    if (self.$inputEl) {
      self.$inputEl.trigger("colorpicker:close");
    }
    self.emit("local::close colorPickerClose", self);
  };
  _proto.onClosed = function onClosed() {
    var self = this;
    self.opened = false;
    self.closing = false;
    if (!self.inline) {
      nextTick(function() {
        if (self.modal && self.modal.el && self.modal.destroy) {
          if (!self.params.routableModals) {
            self.modal.destroy();
          }
        }
        delete self.modal;
      });
    }
    if (self.$el) {
      self.$el.trigger("colorpicker:closed");
    }
    if (self.$inputEl) {
      self.$inputEl.trigger("colorpicker:closed");
    }
    self.emit("local::closed colorPickerClosed", self);
  };
  _proto.open = function open12() {
    var self = this;
    var app = self.app, opened = self.opened, inline = self.inline, $inputEl = self.$inputEl, $targetEl = self.$targetEl, params = self.params;
    if (opened)
      return;
    if (inline) {
      self.$el = dom7_default(self.render());
      self.$el[0].f7ColorPicker = self;
      self.$containerEl.append(self.$el);
      self.onOpen();
      self.onOpened();
      return;
    }
    var colorPickerContent = self.render();
    if (params.openIn === "page") {
      self.view.router.navigate({
        url: self.url,
        route: {
          content: colorPickerContent,
          path: self.url,
          on: {
            pageBeforeIn: function pageBeforeIn2(e, page) {
              self.$el = page.$el.find(".color-picker");
              self.$el[0].f7ColorPicker = self;
              self.onOpen();
            },
            pageAfterIn: function pageAfterIn2() {
              self.onOpened();
            },
            pageBeforeOut: function pageBeforeOut() {
              self.onClose();
            },
            pageAfterOut: function pageAfterOut() {
              self.onClosed();
              if (self.$el && self.$el[0]) {
                self.$el[0].f7ColorPicker = null;
                delete self.$el[0].f7ColorPicker;
              }
            }
          }
        }
      });
    } else {
      var modalType = self.getModalType();
      var backdrop = params.backdrop;
      if (backdrop === null || typeof backdrop === "undefined") {
        if (modalType === "popover" && app.params.popover.backdrop !== false)
          backdrop = true;
        if (modalType === "popup")
          backdrop = true;
      }
      var modalParams = {
        targetEl: $targetEl || $inputEl,
        scrollToEl: params.scrollToInput ? $targetEl || $inputEl : void 0,
        content: colorPickerContent,
        backdrop,
        closeByBackdropClick: params.closeByBackdropClick,
        on: {
          open: function open13() {
            var modal = this;
            self.modal = modal;
            self.$el = modalType === "popover" || modalType === "popup" ? modal.$el.find(".color-picker") : modal.$el;
            self.$el[0].f7ColorPicker = self;
            self.onOpen();
          },
          opened: function opened2() {
            self.onOpened();
          },
          close: function close12() {
            self.onClose();
          },
          closed: function closed() {
            self.onClosed();
            if (self.$el && self.$el[0]) {
              self.$el[0].f7ColorPicker = null;
              delete self.$el[0].f7ColorPicker;
            }
          }
        }
      };
      if (modalType === "popup") {
        modalParams.push = params.popupPush;
        modalParams.swipeToClose = params.popupSwipeToClose;
      }
      if (modalType === "sheet") {
        modalParams.push = params.sheetPush;
        modalParams.swipeToClose = params.sheetSwipeToClose;
      }
      if (params.routableModals && self.view) {
        var _route;
        self.view.router.navigate({
          url: self.url,
          route: (_route = {
            path: self.url
          }, _route[modalType] = modalParams, _route)
        });
      } else {
        self.modal = app[modalType].create(modalParams);
        self.modal.open();
      }
    }
  };
  _proto.close = function close12() {
    var self = this;
    var opened = self.opened, inline = self.inline;
    if (!opened)
      return;
    if (inline) {
      self.onClose();
      self.onClosed();
      return;
    }
    if (self.params.routableModals && self.view || self.params.openIn === "page") {
      self.view.router.back();
    } else {
      self.modal.close();
    }
  };
  _proto.init = function init46() {
    var self = this;
    self.initInput();
    if (self.inline) {
      self.open();
      self.emit("local::init colorPickerInit", self);
      return;
    }
    if (!self.initialized && self.params.value) {
      self.setValue(self.params.value);
    }
    if (self.$inputEl) {
      self.attachInputEvents();
    }
    if (self.$targetEl) {
      self.attachTargetEvents();
    }
    if (self.params.closeByOutsideClick) {
      self.attachHtmlEvents();
    }
    self.emit("local::init colorPickerInit", self);
  };
  _proto.destroy = function destroy50() {
    var self = this;
    if (self.destroyed)
      return;
    var $el = self.$el;
    self.emit("local::beforeDestroy colorPickerBeforeDestroy", self);
    if ($el)
      $el.trigger("colorpicker:beforedestroy");
    self.close();
    self.detachEvents();
    if (self.$inputEl) {
      self.detachInputEvents();
    }
    if (self.$targetEl) {
      self.detachTargetEvents();
    }
    if (self.params.closeByOutsideClick) {
      self.detachHtmlEvents();
    }
    if ($el && $el.length)
      delete self.$el[0].f7ColorPicker;
    deleteProps(self);
    self.destroyed = true;
  };
  _createClass7(ColorPicker2, [{
    key: "view",
    get: function get2() {
      var $inputEl = this.$inputEl, $targetEl = this.$targetEl, app = this.app, params = this.params;
      var view;
      if (params.view) {
        view = params.view;
      } else {
        if ($inputEl) {
          view = $inputEl.parents(".view").length && $inputEl.parents(".view")[0].f7View;
        }
        if (!view && $targetEl) {
          view = $targetEl.parents(".view").length && $targetEl.parents(".view")[0].f7View;
        }
      }
      if (!view)
        view = app.views.main;
      return view;
    }
  }]);
  return ColorPicker2;
}(class_default);
var color_picker_class_default = ColorPicker;

// node_modules/framework7/esm/components/color-picker/color-picker.js
var color_picker_default = {
  name: "colorPicker",
  static: {
    ColorPicker: color_picker_class_default
  },
  create: function create65() {
    var app = this;
    app.colorPicker = ConstructorMethods({
      defaultSelector: ".color-picker",
      constructor: color_picker_class_default,
      app,
      domProp: "f7ColorPicker"
    });
    app.colorPicker.close = function close12(el) {
      if (el === void 0) {
        el = ".color-picker";
      }
      var $el = dom7_default(el);
      if ($el.length === 0)
        return;
      var colorPicker = $el[0].f7ColorPicker;
      if (!colorPicker || colorPicker && !colorPicker.opened)
        return;
      colorPicker.close();
    };
  },
  params: {
    colorPicker: {
      value: null,
      modules: ["wheel"],
      palette: [["#FFEBEE", "#FFCDD2", "#EF9A9A", "#E57373", "#EF5350", "#F44336", "#E53935", "#D32F2F", "#C62828", "#B71C1C"], ["#F3E5F5", "#E1BEE7", "#CE93D8", "#BA68C8", "#AB47BC", "#9C27B0", "#8E24AA", "#7B1FA2", "#6A1B9A", "#4A148C"], ["#E8EAF6", "#C5CAE9", "#9FA8DA", "#7986CB", "#5C6BC0", "#3F51B5", "#3949AB", "#303F9F", "#283593", "#1A237E"], ["#E1F5FE", "#B3E5FC", "#81D4FA", "#4FC3F7", "#29B6F6", "#03A9F4", "#039BE5", "#0288D1", "#0277BD", "#01579B"], ["#E0F2F1", "#B2DFDB", "#80CBC4", "#4DB6AC", "#26A69A", "#009688", "#00897B", "#00796B", "#00695C", "#004D40"], ["#F1F8E9", "#DCEDC8", "#C5E1A5", "#AED581", "#9CCC65", "#8BC34A", "#7CB342", "#689F38", "#558B2F", "#33691E"], ["#FFFDE7", "#FFF9C4", "#FFF59D", "#FFF176", "#FFEE58", "#FFEB3B", "#FDD835", "#FBC02D", "#F9A825", "#F57F17"], ["#FFF3E0", "#FFE0B2", "#FFCC80", "#FFB74D", "#FFA726", "#FF9800", "#FB8C00", "#F57C00", "#EF6C00", "#E65100"]],
      groupedModules: false,
      centerModules: true,
      sliderLabel: false,
      sliderValue: false,
      sliderValueEdiable: false,
      barLabel: false,
      barValue: false,
      barValueEdiable: false,
      hexLabel: false,
      hexValueEditable: false,
      redLabelText: "R",
      greenLabelText: "G",
      blueLabelText: "B",
      hueLabelText: "H",
      saturationLabelText: "S",
      brightnessLabelText: "B",
      hexLabelText: "HEX",
      alphaLabelText: "A",
      containerEl: null,
      openIn: "popover",
      openInPhone: "popup",
      popupPush: false,
      popupSwipeToClose: void 0,
      sheetPush: false,
      sheetSwipeToClose: void 0,
      formatValue: null,
      targetEl: null,
      targetElSetBackgroundColor: false,
      inputEl: null,
      inputReadOnly: true,
      closeByOutsideClick: true,
      scrollToInput: true,
      toolbarSheet: true,
      toolbarPopover: false,
      toolbarCloseText: "Done",
      navbarPopup: true,
      navbarCloseText: "Done",
      navbarTitleText: "Color",
      navbarBackLinkText: "Back",
      cssClass: null,
      routableModals: false,
      view: null,
      url: "color/",
      backdrop: null,
      closeByBackdropClick: true,
      renderToolbar: null,
      renderNavbar: null,
      renderInline: null,
      renderPopover: null,
      renderSheet: null,
      renderPopup: null,
      render: null
    }
  }
};

// node_modules/framework7/esm/components/treeview/treeview.js
var Treeview = {
  open: function open11(itemEl) {
    var app = this;
    var $itemEl = dom7_default(itemEl).eq(0);
    if (!$itemEl.length)
      return;
    $itemEl.addClass("treeview-item-opened");
    $itemEl.trigger("treeview:open");
    app.emit("treeviewOpen", $itemEl[0]);
    function done(cancel) {
      if (cancel) {
        $itemEl.removeClass("treeview-item-opened");
        $itemEl.trigger("treeview:close");
        app.emit("treeviewClose", $itemEl[0]);
      } else {
        $itemEl[0].f7TreeviewChildrenLoaded = true;
      }
      $itemEl.find(".treeview-toggle").removeClass("treeview-toggle-hidden");
      $itemEl.find(".treeview-preloader").remove();
    }
    if ($itemEl.hasClass("treeview-load-children") && !$itemEl[0].f7TreeviewChildrenLoaded) {
      var preloaders = {
        iosPreloaderContent,
        mdPreloaderContent,
        auroraPreloaderContent
      };
      $itemEl.trigger("treeview:loadchildren", done);
      app.emit("treeviewLoadChildren", $itemEl[0], done);
      $itemEl.find(".treeview-toggle").addClass("treeview-toggle-hidden");
      $itemEl.find(".treeview-item-root").prepend('<div class="preloader treeview-preloader">' + preloaders[app.theme + "PreloaderContent"] + "</div>");
    }
  },
  close: function close11(itemEl) {
    var app = this;
    var $itemEl = dom7_default(itemEl).eq(0);
    if (!$itemEl.length)
      return;
    $itemEl.removeClass("treeview-item-opened");
    $itemEl.trigger("treeview:close");
    app.emit("treeviewClose", $itemEl[0]);
  },
  toggle: function toggle8(itemEl) {
    var app = this;
    var $itemEl = dom7_default(itemEl).eq(0);
    if (!$itemEl.length)
      return;
    var wasOpened = $itemEl.hasClass("treeview-item-opened");
    app.treeview[wasOpened ? "close" : "open"]($itemEl);
  }
};
var treeview_default = {
  name: "treeview",
  create: function create66() {
    var app = this;
    bindMethods(app, {
      treeview: Treeview
    });
  },
  clicks: {
    ".treeview-toggle": function toggle9($clickedEl, clickedData, e) {
      var app = this;
      if ($clickedEl.parents(".treeview-item-toggle").length)
        return;
      var $treeviewItemEl = $clickedEl.parents(".treeview-item").eq(0);
      if (!$treeviewItemEl.length)
        return;
      e.preventF7Router = true;
      app.treeview.toggle($treeviewItemEl[0]);
    },
    ".treeview-item-toggle": function toggle10($clickedEl, clickedData, e) {
      var app = this;
      var $treeviewItemEl = $clickedEl.closest(".treeview-item").eq(0);
      if (!$treeviewItemEl.length)
        return;
      e.preventF7Router = true;
      app.treeview.toggle($treeviewItemEl[0]);
    }
  }
};

// node_modules/framework7/esm/components/text-editor/text-editor-class.js
function _assertThisInitialized28(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose28(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf28(subClass, superClass);
}
function _setPrototypeOf28(o, p) {
  _setPrototypeOf28 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf28(o, p);
}
var textEditorButtonsMap = {
  bold: ["bold", "format_bold", "bold"],
  italic: ["italic", "format_italic", "italic"],
  underline: ["underline", "format_underlined", "underline"],
  strikeThrough: ["strikethrough", "strikethrough_s", "strikeThrough"],
  orderedList: ["list_number", "format_list_numbered", "insertOrderedList"],
  unorderedList: ["list_bullet", "format_list_bulleted", "insertUnorderedList"],
  link: ["link", "link", "createLink"],
  image: ["photo", "image", "insertImage"],
  paragraph: ["paragraph", '<i class="icon">\xB6</i>', "formatBlock.P"],
  h1: ['<i class="icon">H<sub>1</sub></i>', '<i class="icon">H<sub>1</sub></i>', "formatBlock.H1"],
  h2: ['<i class="icon">H<sub>2</sub></i>', '<i class="icon">H<sub>2</sub></i>', "formatBlock.H2"],
  h3: ['<i class="icon">H<sub>3</sub></i>', '<i class="icon">H<sub>3</sub></i>', "formatBlock.H3"],
  alignLeft: ["text_alignleft", "format_align_left", "justifyLeft"],
  alignCenter: ["text_aligncenter", "format_align_center", "justifyCenter"],
  alignRight: ["text_alignright", "format_align_right", "justifyRight"],
  alignJustify: ["text_justify", "format_align_justify", "justifyFull"],
  subscript: ["textformat_subscript", '<i class="icon">A<sub>1</sub></i>', "subscript"],
  superscript: ["textformat_superscript", '<i class="icon">A<sup>1</sup></i>', "superscript"],
  indent: ["increase_indent", "format_indent_increase", "indent"],
  outdent: ["decrease_indent", "format_indent_decrease", "outdent"]
};
var TextEditor = function(_Framework7Class) {
  _inheritsLoose28(TextEditor2, _Framework7Class);
  function TextEditor2(app, params) {
    var _this;
    _this = _Framework7Class.call(this, params, [app]) || this;
    var self = _assertThisInitialized28(_this);
    var document = getDocument();
    var device2 = getDevice();
    var defaults = extend({}, app.params.textEditor);
    self.useModulesParams(defaults);
    self.params = extend(defaults, params);
    var el = self.params.el;
    if (!el)
      return self || _assertThisInitialized28(_this);
    var $el = dom7_default(el);
    if ($el.length === 0)
      return self || _assertThisInitialized28(_this);
    if ($el[0].f7TextEditor)
      return $el[0].f7TextEditor || _assertThisInitialized28(_this);
    var $contentEl = $el.children(".text-editor-content");
    if (!$contentEl.length) {
      $el.append('<div class="text-editor-content" contenteditable></div>');
      $contentEl = $el.children(".text-editor-content");
    }
    extend(self, {
      app,
      $el,
      el: $el[0],
      $contentEl,
      contentEl: $contentEl[0]
    });
    if ("value" in params) {
      self.value = self.params.value;
    }
    if (self.params.mode === "keyboard-toolbar") {
      if (!(device2.cordova || device2.capacitor) && !device2.android) {
        self.params.mode = "popover";
      }
    }
    if (typeof self.params.buttons === "string") {
      try {
        self.params.buttons = JSON.parse(self.params.buttons);
      } catch (err) {
        throw new Error('Framework7: TextEditor: wrong "buttons" parameter format');
      }
    }
    $el[0].f7TextEditor = self;
    self.onButtonClick = self.onButtonClick.bind(self);
    self.onFocus = self.onFocus.bind(self);
    self.onBlur = self.onBlur.bind(self);
    self.onInput = self.onInput.bind(self);
    self.onPaste = self.onPaste.bind(self);
    self.onSelectionChange = self.onSelectionChange.bind(self);
    self.closeKeyboardToolbar = self.closeKeyboardToolbar.bind(self);
    self.attachEvents = function attachEvents2() {
      if (self.params.mode === "toolbar") {
        self.$el.find(".text-editor-toolbar").on("click", "button", self.onButtonClick);
      }
      if (self.params.mode === "keyboard-toolbar") {
        self.$keyboardToolbarEl.on("click", "button", self.onButtonClick);
        self.$el.parents(".page").on("page:beforeout", self.closeKeyboardToolbar);
      }
      if (self.params.mode === "popover" && self.popover) {
        self.popover.$el.on("click", "button", self.onButtonClick);
      }
      self.$contentEl.on("paste", self.onPaste);
      self.$contentEl.on("focus", self.onFocus);
      self.$contentEl.on("blur", self.onBlur);
      self.$contentEl.on("input", self.onInput, true);
      dom7_default(document).on("selectionchange", self.onSelectionChange);
    };
    self.detachEvents = function detachEvents2() {
      if (self.params.mode === "toolbar") {
        self.$el.find(".text-editor-toolbar").off("click", "button", self.onButtonClick);
      }
      if (self.params.mode === "keyboard-toolbar") {
        self.$keyboardToolbarEl.off("click", "button", self.onButtonClick);
        self.$el.parents(".page").off("page:beforeout", self.closeKeyboardToolbar);
      }
      if (self.params.mode === "popover" && self.popover) {
        self.popover.$el.off("click", "button", self.onButtonClick);
      }
      self.$contentEl.off("paste", self.onPaste);
      self.$contentEl.off("focus", self.onFocus);
      self.$contentEl.off("blur", self.onBlur);
      self.$contentEl.off("input", self.onInput, true);
      dom7_default(document).off("selectionchange", self.onSelectionChange);
    };
    self.useModules();
    self.init();
    return self || _assertThisInitialized28(_this);
  }
  var _proto = TextEditor2.prototype;
  _proto.setValue = function setValue(newValue) {
    var self = this;
    var currentValue = self.value;
    if (currentValue === newValue)
      return self;
    self.value = newValue;
    self.$contentEl.html(newValue);
    self.$el.trigger("texteditor:change", self.value);
    self.emit("local::change textEditorChange", self, self.value);
    return self;
  };
  _proto.getValue = function getValue() {
    var self = this;
    return self.value;
  };
  _proto.clearValue = function clearValue() {
    var self = this;
    self.setValue("");
    if (self.params.placeholder && !self.$contentEl.html()) {
      self.insertPlaceholder();
    }
    return self;
  };
  _proto.createLink = function createLink() {
    var self = this;
    var window2 = getWindow();
    var document = getDocument();
    var currentSelection = window2.getSelection();
    var selectedNodes = [];
    var $selectedLinks;
    if (currentSelection && currentSelection.anchorNode && dom7_default(currentSelection.anchorNode).parents(self.$el).length) {
      var anchorNode = currentSelection.anchorNode;
      while (anchorNode) {
        selectedNodes.push(anchorNode);
        if (!anchorNode.nextSibling || anchorNode === currentSelection.focusNode) {
          anchorNode = null;
        }
        if (anchorNode) {
          anchorNode = anchorNode.nextSibling;
        }
      }
      var selectedNodesLinks = [];
      var $selectedNodes = dom7_default(selectedNodes);
      for (var i = 0; i < $selectedNodes.length; i += 1) {
        var childNodes = $selectedNodes[i].children;
        if (childNodes) {
          for (var j = 0; j < childNodes.length; j += 1) {
            if (dom7_default(childNodes[j]).is("a")) {
              selectedNodesLinks.push(childNodes[j]);
            }
          }
        }
      }
      $selectedLinks = $selectedNodes.closest("a").add(dom7_default(selectedNodesLinks));
    }
    if ($selectedLinks && $selectedLinks.length) {
      $selectedLinks.each(function(linkNode) {
        var selection = window2.getSelection();
        var range = document.createRange();
        range.selectNodeContents(linkNode);
        selection.removeAllRanges();
        selection.addRange(range);
        document.execCommand("unlink", false);
        selection.removeAllRanges();
      });
      return self;
    }
    var currentRange = self.getSelectionRange();
    if (!currentRange)
      return self;
    var dialog = self.app.dialog.prompt(self.params.linkUrlText, "", function(link) {
      if (link && link.trim().length) {
        self.setSelectionRange(currentRange);
        document.execCommand("createLink", false, link.trim());
        self.$el.trigger("texteditor:insertlink", {
          url: link.trim()
        });
        self.emit("local:insertLink textEditorInsertLink", self, link.trim());
      }
    });
    dialog.$el.find("input").focus();
    return self;
  };
  _proto.insertImage = function insertImage() {
    var self = this;
    var document = getDocument();
    var currentRange = self.getSelectionRange();
    if (!currentRange)
      return self;
    var dialog = self.app.dialog.prompt(self.params.imageUrlText, "", function(imageUrl) {
      if (imageUrl && imageUrl.trim().length) {
        self.setSelectionRange(currentRange);
        document.execCommand("insertImage", false, imageUrl.trim());
        self.$el.trigger("texteditor:insertimage", {
          url: imageUrl.trim()
        });
        self.emit("local:insertImage textEditorInsertImage", self, imageUrl.trim());
      }
    });
    dialog.$el.find("input").focus();
    return self;
  };
  _proto.removePlaceholder = function removePlaceholder() {
    var self = this;
    self.$contentEl.find(".text-editor-placeholder").remove();
  };
  _proto.insertPlaceholder = function insertPlaceholder() {
    var self = this;
    self.$contentEl.append('<div class="text-editor-placeholder">' + self.params.placeholder + "</div>");
  };
  _proto.onSelectionChange = function onSelectionChange() {
    var self = this;
    var window2 = getWindow();
    var document = getDocument();
    if (self.params.mode === "toolbar")
      return;
    var selection = window2.getSelection();
    var selectionIsInContent = dom7_default(selection.anchorNode).parents(self.contentEl).length || selection.anchorNode === self.contentEl;
    if (self.params.mode === "keyboard-toolbar") {
      if (!selectionIsInContent) {
        self.closeKeyboardToolbar();
      } else {
        self.openKeyboardToolbar();
      }
      return;
    }
    if (self.params.mode === "popover") {
      var selectionIsInPopover = dom7_default(selection.anchorNode).parents(self.popover.el).length || selection.anchorNode === self.popover.el;
      if (!selectionIsInContent && !selectionIsInPopover) {
        self.closePopover();
        return;
      }
      if (!selection.isCollapsed && selection.rangeCount) {
        var range = selection.getRangeAt(0);
        var rect = range.getBoundingClientRect();
        var rootEl = self.app.$el[0] || document.body;
        self.openPopover(rect.x + (window2.scrollX || 0) - rootEl.offsetLeft, rect.y + (window2.scrollY || 0) - rootEl.offsetTop, rect.width, rect.height);
      } else if (selection.isCollapsed) {
        self.closePopover();
      }
    }
  };
  _proto.onPaste = function onPaste(e) {
    var self = this;
    var document = getDocument();
    if (self.params.clearFormattingOnPaste && e.clipboardData && e.clipboardData.getData) {
      var text2 = e.clipboardData.getData("text/plain");
      e.preventDefault();
      document.execCommand("insertText", false, text2);
    }
  };
  _proto.onInput = function onInput() {
    var self = this;
    var value = self.$contentEl.html();
    self.value = value;
    self.$el.trigger("texteditor:input");
    self.emit("local:input textEditorInput", self, self.value);
    self.$el.trigger("texteditor:change", self.value);
    self.emit("local::change textEditorChange", self, self.value);
  };
  _proto.onFocus = function onFocus() {
    var self = this;
    self.removePlaceholder();
    self.$contentEl.focus();
    self.$el.trigger("texteditor:focus");
    self.emit("local::focus textEditorFocus", self);
  };
  _proto.onBlur = function onBlur() {
    var self = this;
    var window2 = getWindow();
    var document = getDocument();
    if (self.params.placeholder && self.$contentEl.html() === "") {
      self.insertPlaceholder();
    }
    if (self.params.mode === "popover") {
      var selection = window2.getSelection();
      var selectionIsInContent = dom7_default(selection.anchorNode).parents(self.contentEl).length || selection.anchorNode === self.contentEl;
      var inPopover = document.activeElement && self.popover && dom7_default(document.activeElement).closest(self.popover.$el).length;
      if (!inPopover && !selectionIsInContent) {
        self.closePopover();
      }
    }
    if (self.params.mode === "keyboard-toolbar") {
      var _selection = window2.getSelection();
      var _selectionIsInContent = dom7_default(_selection.anchorNode).parents(self.contentEl).length || _selection.anchorNode === self.contentEl;
      if (!_selectionIsInContent) {
        self.closeKeyboardToolbar();
      }
    }
    self.$el.trigger("texteditor:blur");
    self.emit("local::blur textEditorBlur", self);
  };
  _proto.onButtonClick = function onButtonClick(e) {
    var self = this;
    var window2 = getWindow();
    var document = getDocument();
    var selection = window2.getSelection();
    var selectionIsInContent = dom7_default(selection.anchorNode).parents(self.contentEl).length || selection.anchorNode === self.contentEl;
    if (!selectionIsInContent)
      return;
    var $buttonEl = dom7_default(e.target).closest("button");
    if ($buttonEl.parents("form").length) {
      e.preventDefault();
    }
    var button = $buttonEl.attr("data-button");
    var buttonData = self.params.customButtons && self.params.customButtons[button];
    if (!button || !(textEditorButtonsMap[button] || buttonData))
      return;
    $buttonEl.trigger("texteditor:buttonclick", button);
    self.emit("local::buttonClick textEditorButtonClick", self, button);
    if (buttonData) {
      if (buttonData.onClick)
        buttonData.onClick(self, $buttonEl[0]);
      return;
    }
    var command = textEditorButtonsMap[button][2];
    if (command === "createLink") {
      self.createLink();
      return;
    }
    if (command === "insertImage") {
      self.insertImage();
      return;
    }
    if (command.indexOf("formatBlock") === 0) {
      var tagName = command.split(".")[1];
      var $anchorNode = dom7_default(selection.anchorNode);
      if ($anchorNode.parents(tagName.toLowerCase()).length || $anchorNode.is(tagName)) {
        document.execCommand("formatBlock", false, "div");
      } else {
        document.execCommand("formatBlock", false, tagName);
      }
      return;
    }
    document.execCommand(command, false);
  };
  _proto.getSelectionRange = function getSelectionRange() {
    var window2 = getWindow();
    var document = getDocument();
    if (window2.getSelection) {
      var sel = window2.getSelection();
      if (sel.getRangeAt && sel.rangeCount) {
        return sel.getRangeAt(0);
      }
    } else if (document.selection && document.selection.createRange) {
      return document.selection.createRange();
    }
    return null;
  };
  _proto.setSelectionRange = function setSelectionRange(range) {
    var window2 = getWindow();
    var document = getDocument();
    if (range) {
      if (window2.getSelection) {
        var sel = window2.getSelection();
        sel.removeAllRanges();
        sel.addRange(range);
      } else if (document.selection && range.select) {
        range.select();
      }
    }
  };
  _proto.renderButtons = function renderButtons() {
    var self = this;
    var html2 = "";
    function renderButton(button) {
      var iconClass = self.app.theme === "md" ? "material-icons" : "f7-icons";
      if (self.params.customButtons && self.params.customButtons[button]) {
        var buttonData = self.params.customButtons[button];
        return '<button type="button" class="text-editor-button" data-button="' + button + '">' + (buttonData.content || "") + "</button>";
      }
      if (!textEditorButtonsMap[button])
        return "";
      var iconContent = textEditorButtonsMap[button][self.app.theme === "md" ? 1 : 0];
      return ('<button type="button" class="text-editor-button" data-button="' + button + '">' + (iconContent.indexOf("<") >= 0 ? iconContent : '<i class="' + iconClass + '">' + iconContent + "</i>") + "</button>").trim();
    }
    self.params.buttons.forEach(function(button, buttonIndex) {
      if (Array.isArray(button)) {
        button.forEach(function(b) {
          html2 += renderButton(b);
        });
        if (buttonIndex < self.params.buttons.length - 1 && self.params.dividers) {
          html2 += '<div class="text-editor-button-divider"></div>';
        }
      } else {
        html2 += renderButton(button);
      }
    });
    return html2;
  };
  _proto.createToolbar = function createToolbar() {
    var self = this;
    self.$el.prepend('<div class="text-editor-toolbar">' + self.renderButtons() + "</div>");
  };
  _proto.createKeyboardToolbar = function createKeyboardToolbar() {
    var self = this;
    self.$keyboardToolbarEl = dom7_default('<div class="toolbar toolbar-bottom text-editor-keyboard-toolbar"><div class="toolbar-inner">' + self.renderButtons() + "</div></div>");
  };
  _proto.createPopover = function createPopover() {
    var self = this;
    self.popover = self.app.popover.create({
      content: '\n        <div class="popover theme-dark text-editor-popover">\n          <div class="popover-inner">' + self.renderButtons() + "</div>\n        </div>\n      ",
      closeByOutsideClick: false,
      backdrop: false
    });
  };
  _proto.openKeyboardToolbar = function openKeyboardToolbar() {
    var self = this;
    if (self.$keyboardToolbarEl.parent(self.app.$el).length)
      return;
    self.$el.trigger("texteditor:keyboardopen");
    self.emit("local::keyboardOpen textEditorKeyboardOpen", self);
    self.app.$el.append(self.$keyboardToolbarEl);
  };
  _proto.closeKeyboardToolbar = function closeKeyboardToolbar() {
    var self = this;
    self.$keyboardToolbarEl.remove();
    self.$el.trigger("texteditor:keyboardclose");
    self.emit("local::keyboardClose textEditorKeyboardClose", self);
  };
  _proto.openPopover = function openPopover2(targetX, targetY, targetWidth, targetHeight) {
    var self = this;
    if (!self.popover)
      return;
    Object.assign(self.popover.params, {
      targetX,
      targetY,
      targetWidth,
      targetHeight
    });
    clearTimeout(self.popoverTimeout);
    self.popoverTimeout = setTimeout(function() {
      if (!self.popover)
        return;
      if (self.popover.opened) {
        self.popover.resize();
      } else {
        self.$el.trigger("texteditor:popoveropen");
        self.emit("local::popoverOpen textEditorPopoverOpen", self);
        self.popover.open();
      }
    }, 400);
  };
  _proto.closePopover = function closePopover2() {
    var self = this;
    clearTimeout(self.popoverTimeout);
    if (!self.popover || !self.popover.opened)
      return;
    self.popoverTimeout = setTimeout(function() {
      if (!self.popover)
        return;
      self.$el.trigger("texteditor:popoverclose");
      self.emit("local::popoverClose textEditorPopoverClose", self);
      self.popover.close();
    }, 400);
  };
  _proto.init = function init46() {
    var self = this;
    if (self.value) {
      self.$contentEl.html(self.value);
    } else {
      self.value = self.$contentEl.html();
    }
    if (self.params.placeholder && self.value === "") {
      self.insertPlaceholder();
    }
    if (self.params.mode === "toolbar") {
      self.createToolbar();
    } else if (self.params.mode === "popover") {
      self.createPopover();
    } else if (self.params.mode === "keyboard-toolbar") {
      self.createKeyboardToolbar();
    }
    self.attachEvents();
    self.$el.trigger("texteditor:init");
    self.emit("local::init textEditorInit", self);
    return self;
  };
  _proto.destroy = function destroy50() {
    var self = this;
    self.$el.trigger("texteditor:beforedestroy");
    self.emit("local::beforeDestroy textEditorBeforeDestroy", self);
    self.detachEvents();
    if (self.params.mode === "keyboard-toolbar" && self.$keyboardToolbarEl) {
      self.$keyboardToolbarEl.remove();
    }
    if (self.popover) {
      self.popover.close(false);
      self.popover.destroy();
    }
    delete self.$el[0].f7TextEditor;
    deleteProps(self);
    self = null;
  };
  return TextEditor2;
}(class_default);
var text_editor_class_default = TextEditor;

// node_modules/framework7/esm/components/text-editor/text-editor.js
var text_editor_default = {
  name: "textEditor",
  params: {
    textEditor: {
      el: null,
      mode: "toolbar",
      value: void 0,
      customButtons: null,
      buttons: [["bold", "italic", "underline", "strikeThrough"], ["orderedList", "unorderedList"], ["link", "image"], ["paragraph", "h1", "h2", "h3"], ["alignLeft", "alignCenter", "alignRight", "alignJustify"], ["subscript", "superscript"], ["indent", "outdent"]],
      dividers: true,
      imageUrlText: "Insert image URL",
      linkUrlText: "Insert link URL",
      placeholder: null,
      clearFormattingOnPaste: true
    }
  },
  create: function create67() {
    var app = this;
    app.textEditor = extend(ConstructorMethods({
      defaultSelector: ".text-editor",
      constructor: text_editor_class_default,
      app,
      domProp: "f7TextEditor"
    }));
  },
  static: {
    TextEditor: text_editor_class_default
  },
  on: {
    tabMounted: function tabMounted20(tabEl) {
      var app = this;
      dom7_default(tabEl).find(".text-editor-init").each(function(editorEl) {
        var dataset = dom7_default(editorEl).dataset();
        app.textEditor.create(extend({
          el: editorEl
        }, dataset || {}));
      });
    },
    tabBeforeRemove: function tabBeforeRemove17(tabEl) {
      dom7_default(tabEl).find(".text-editor-init").each(function(editorEl) {
        if (editorEl.f7TextEditor)
          editorEl.f7TextEditor.destroy();
      });
    },
    pageInit: function pageInit21(page) {
      var app = this;
      page.$el.find(".text-editor-init").each(function(editorEl) {
        var dataset = dom7_default(editorEl).dataset();
        app.textEditor.create(extend({
          el: editorEl
        }, dataset || {}));
      });
    },
    pageBeforeRemove: function pageBeforeRemove18(page) {
      page.$el.find(".text-editor-init").each(function(editorEl) {
        if (editorEl.f7TextEditor)
          editorEl.f7TextEditor.destroy();
      });
    }
  },
  vnode: {
    "text-editor-init": {
      insert: function insert17(vnode) {
        var app = this;
        var editorEl = vnode.elm;
        var dataset = dom7_default(editorEl).dataset();
        app.textEditor.create(extend({
          el: editorEl
        }, dataset || {}));
      },
      destroy: function destroy49(vnode) {
        var editorEl = vnode.elm;
        if (editorEl.f7TextEditor)
          editorEl.f7TextEditor.destroy();
      }
    }
  }
};

// node_modules/framework7/esm/components/pie-chart/pie-chart-class.js
function _assertThisInitialized29(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose29(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf29(subClass, superClass);
}
function _setPrototypeOf29(o, p) {
  _setPrototypeOf29 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf29(o, p);
}
var PieChart = function(_Framework7Class) {
  _inheritsLoose29(PieChart2, _Framework7Class);
  function PieChart2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var self = _assertThisInitialized29(_this);
    var defaults = extend({}, app.params.pieChart);
    self.useModulesParams(defaults);
    self.params = extend(defaults, params);
    var el = self.params.el;
    if (!el)
      return self || _assertThisInitialized29(_this);
    var $el = dom7_default(el);
    if ($el.length === 0)
      return self || _assertThisInitialized29(_this);
    if ($el[0].f7PieChart)
      return $el[0].f7PieChart || _assertThisInitialized29(_this);
    extend(self, {
      app,
      $el,
      el: $el && $el[0],
      currentIndex: null,
      f7Tooltip: null
    });
    $el[0].f7PieChart = self;
    self.useModules();
    self.showTooltip = self.showTooltip.bind(_assertThisInitialized29(_this));
    self.hideTooltip = self.hideTooltip.bind(_assertThisInitialized29(_this));
    self.init();
    return self || _assertThisInitialized29(_this);
  }
  var _proto = PieChart2.prototype;
  _proto.getSummValue = function getSummValue() {
    var datasets = this.params.datasets;
    var summ = 0;
    datasets.map(function(d) {
      return d.value || 0;
    }).forEach(function(value) {
      summ += value;
    });
    return summ;
  };
  _proto.getPaths = function getPaths() {
    var _this2 = this;
    var _this$params = this.params, datasets = _this$params.datasets, size = _this$params.size;
    var paths = [];
    var cumulativePercentage = 0;
    function getCoordinatesForPercentage(percentage) {
      var x = Math.cos(2 * Math.PI * percentage) * (size / 3);
      var y = Math.sin(2 * Math.PI * percentage) * (size / 3);
      return [x, y];
    }
    datasets.forEach(function(_ref) {
      var value = _ref.value, label = _ref.label, color = _ref.color;
      var percentage = value / _this2.getSummValue();
      var _getCoordinatesForPer = getCoordinatesForPercentage(cumulativePercentage), startX = _getCoordinatesForPer[0], startY = _getCoordinatesForPer[1];
      cumulativePercentage += percentage;
      var _getCoordinatesForPer2 = getCoordinatesForPercentage(cumulativePercentage), endX = _getCoordinatesForPer2[0], endY = _getCoordinatesForPer2[1];
      var largeArcFlag = percentage > 0.5 ? 1 : 0;
      var points = [
        "M " + startX + " " + startY,
        "A " + size / 3 + " " + size / 3 + " 0 " + largeArcFlag + " 1 " + endX + " " + endY,
        "L 0 0"
      ].join(" ");
      paths.push({
        points,
        label,
        color
      });
    });
    return paths;
  };
  _proto.formatTooltipText = function formatTooltipText() {
    var datasets = this.params.datasets;
    var currentIndex = this.currentIndex;
    if (currentIndex === null)
      return "";
    var _datasets$currentInde = datasets[currentIndex], value = _datasets$currentInde.value, label = _datasets$currentInde.label, color = _datasets$currentInde.color;
    var percentage = value / this.getSummValue() * 100;
    var round = function round2(v) {
      if (parseInt(v, 10) === v)
        return v;
      return Math.round(v * 100) / 100;
    };
    if (this.params.formatTooltip) {
      return this.params.formatTooltip.call(this, {
        index: currentIndex,
        value,
        label,
        color,
        percentage
      });
    }
    var tooltipText = (label ? label + ": " : "") + round(value) + " (" + round(percentage) + "%)";
    return '\n      <div class="pie-chart-tooltip-label">\n        <span class="pie-chart-tooltip-color" style="background-color: ' + color + ';"></span> ' + tooltipText + "\n      </div>\n    ";
  };
  _proto.setTooltip = function setTooltip() {
    var self = this;
    var currentIndex = self.currentIndex, el = self.el, app = self.app, params = self.params;
    var tooltip = params.tooltip;
    if (currentIndex === null && !self.f7Tooltip)
      return;
    if (!tooltip || !el)
      return;
    if (currentIndex !== null && !self.f7Tooltip) {
      self.f7Tooltip = app.tooltip.create({
        trigger: "manual",
        containerEl: el,
        targetEl: el.querySelector('path[data-index="' + currentIndex + '"]'),
        text: self.formatTooltipText(),
        cssClass: "pie-chart-tooltip"
      });
      self.f7Tooltip.show();
      return;
    }
    if (!self.f7Tooltip)
      return;
    if (currentIndex !== null) {
      self.f7Tooltip.setText(self.formatTooltipText());
      self.f7Tooltip.setTargetEl(el.querySelector('path[data-index="' + currentIndex + '"]'));
      self.f7Tooltip.show();
    } else {
      self.f7Tooltip.hide();
    }
  };
  _proto.render = function render15() {
    var self = this;
    var size = self.params.size;
    var paths = self.getPaths();
    return jsx_default2("svg", {
      xmlns: "http://www.w3.org/2000/svg",
      width: size,
      viewBox: "-" + size / 3 + " -" + size / 3 + " " + size * 2 / 3 + " " + size * 2 / 3,
      style: "transform: rotate(-90deg)"
    }, paths.map(function(path, index2) {
      return jsx_default2("path", {
        d: path.points,
        fill: path.color,
        "data-index": index2
      });
    }));
  };
  _proto.update = function update22(newParams) {
    if (newParams === void 0) {
      newParams = {};
    }
    var self = this;
    var params = self.params;
    Object.keys(newParams).forEach(function(param) {
      if (typeof newParams[param] !== "undefined") {
        params[param] = newParams[param];
      }
    });
    if (self.$svgEl.length === 0)
      return self;
    self.$svgEl.remove();
    delete self.$svgEl.f7PieChart;
    var $svgEl = dom7_default(self.render()).eq(0);
    $svgEl.f7PieChart = self;
    extend(self, {
      $svgEl,
      svgEl: $svgEl && $svgEl[0]
    });
    self.$el.append($svgEl);
    return self;
  };
  _proto.setCurrentIndex = function setCurrentIndex(index2) {
    var self = this;
    if (index2 === self.currentIndex)
      return;
    var datasets = self.params.datasets;
    self.currentIndex = index2;
    self.$el.trigger("piechart:select", {
      index: index2,
      dataset: datasets[index2]
    });
    self.emit("local::select pieChartSelect", self, index2, datasets[index2]);
  };
  _proto.showTooltip = function showTooltip(e) {
    var _this3 = this;
    var newIndex = parseInt(e.target.getAttribute("data-index"), 10);
    this.setCurrentIndex(newIndex);
    this.$svgEl.find("path").removeClass("pie-chart-hidden").forEach(function(el, index2) {
      if (index2 !== _this3.currentIndex)
        dom7_default(el).addClass("pie-chart-hidden");
    });
    this.setTooltip();
  };
  _proto.hideTooltip = function hideTooltip() {
    this.setCurrentIndex(null);
    this.$svgEl.find("path").removeClass("pie-chart-hidden");
    this.setTooltip();
  };
  _proto.init = function init46() {
    var self = this;
    var $svgEl = dom7_default(self.render()).eq(0);
    $svgEl.f7PieChart = self;
    extend(self, {
      $svgEl,
      svgEl: $svgEl && $svgEl[0]
    });
    self.$el.append($svgEl);
    self.$el.on("click mouseenter", "path", self.showTooltip, true);
    self.$el.on("mouseleave", "path", self.hideTooltip, true);
    return self;
  };
  _proto.destroy = function destroy50() {
    var self = this;
    if (!self.$el || self.destroyed)
      return;
    self.$el.trigger("piechart:beforedestroy");
    self.emit("local::beforeDestroy pieChartBeforeDestroy", self);
    self.$el.off("click mouseenter", "path", self.showTooltip, true);
    self.$el.off("mouseleave", "path", self.hideTooltip, true);
    self.$svgEl.remove();
    if (self.f7Tooltip && self.f7Tooltip.destroy) {
      self.f7Tooltip.destroy();
    }
    delete self.$el[0].f7PieChart;
    deleteProps(self);
    self.destroyed = true;
  };
  return PieChart2;
}(class_default);
var pie_chart_class_default = PieChart;

// node_modules/framework7/esm/components/pie-chart/pie-chart.js
var pie_chart_default = {
  name: "pieChart",
  params: {
    pieChart: {
      el: null,
      datasets: [],
      size: 320,
      tooltip: false,
      formatTooltip: null
    }
  },
  create: function create68() {
    var app = this;
    app.pieChart = ConstructorMethods({
      defaultSelector: ".pie-chart",
      constructor: pie_chart_class_default,
      app,
      domProp: "f7PieChart"
    });
    app.pieChart.update = function update22(el, newParams) {
      var $el = dom7_default(el);
      if ($el.length === 0)
        return void 0;
      var pieChart = app.pieChart.get(el);
      if (!pieChart)
        return void 0;
      pieChart.update(newParams);
      return pieChart;
    };
  }
};

// node_modules/framework7/esm/components/area-chart/area-chart-class.js
function _assertThisInitialized30(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}
function _inheritsLoose30(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf30(subClass, superClass);
}
function _setPrototypeOf30(o, p) {
  _setPrototypeOf30 = Object.setPrototypeOf || function _setPrototypeOf31(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf30(o, p);
}
var AreaChart = function(_Framework7Class) {
  _inheritsLoose30(AreaChart2, _Framework7Class);
  function AreaChart2(app, params) {
    var _this;
    if (params === void 0) {
      params = {};
    }
    _this = _Framework7Class.call(this, params, [app]) || this;
    var self = _assertThisInitialized30(_this);
    var defaults = extend({}, app.params.areaChart);
    self.useModulesParams(defaults);
    self.params = extend(defaults, params);
    var el = self.params.el;
    if (!el)
      return self || _assertThisInitialized30(_this);
    var $el = dom7_default(el);
    if ($el.length === 0)
      return self || _assertThisInitialized30(_this);
    if ($el[0].f7AreaChart)
      return $el[0].f7AreaChart || _assertThisInitialized30(_this);
    extend(self, {
      app,
      $el,
      el: $el && $el[0],
      currentIndex: null,
      hiddenDatasets: [],
      f7Tooltip: null,
      linesOffsets: null
    });
    $el[0].f7AreaChart = self;
    self.useModules();
    self.onMouseEnter = self.onMouseEnter.bind(self);
    self.onMouseMove = self.onMouseMove.bind(self);
    self.onMouseLeave = self.onMouseLeave.bind(self);
    self.onLegendClick = self.onLegendClick.bind(self);
    self.init();
    return self || _assertThisInitialized30(_this);
  }
  var _proto = AreaChart2.prototype;
  _proto.getVisibleLabels = function getVisibleLabels() {
    var _this$params = this.params, maxAxisLabels = _this$params.maxAxisLabels, axisLabels = _this$params.axisLabels;
    if (!maxAxisLabels || axisLabels.length <= maxAxisLabels)
      return axisLabels;
    var skipStep = Math.ceil(axisLabels.length / maxAxisLabels);
    var filtered = axisLabels.filter(function(label, index2) {
      return index2 % skipStep === 0;
    });
    return filtered;
  };
  _proto.getSummValues = function getSummValues() {
    var datasets = this.params.datasets;
    var hiddenDatasets = this.hiddenDatasets;
    var summValues = [];
    datasets.filter(function(dataset, index2) {
      return !hiddenDatasets.includes(index2);
    }).forEach(function(_ref) {
      var values = _ref.values;
      values.forEach(function(value, valueIndex) {
        if (!summValues[valueIndex])
          summValues[valueIndex] = 0;
        summValues[valueIndex] += value;
      });
    });
    return summValues;
  };
  _proto.getChartData = function getChartData() {
    var _this$params2 = this.params, datasets = _this$params2.datasets, lineChart = _this$params2.lineChart, width = _this$params2.width, height = _this$params2.height;
    var hiddenDatasets = this.hiddenDatasets;
    var data = [];
    if (!datasets.length) {
      return data;
    }
    var lastValues = datasets[0].values.map(function() {
      return 0;
    });
    var maxValue = 0;
    if (lineChart) {
      datasets.filter(function(dataset, index2) {
        return !hiddenDatasets.includes(index2);
      }).forEach(function(_ref2) {
        var values = _ref2.values;
        var datasetMaxValue = Math.max.apply(Math, values);
        if (datasetMaxValue > maxValue)
          maxValue = datasetMaxValue;
      });
    } else {
      maxValue = Math.max.apply(Math, this.getSummValues());
    }
    datasets.filter(function(dataset, index2) {
      return !hiddenDatasets.includes(index2);
    }).forEach(function(_ref3) {
      var label = _ref3.label, values = _ref3.values, color = _ref3.color;
      var points = values.map(function(originalValue, valueIndex) {
        lastValues[valueIndex] += originalValue;
        var value = lineChart ? originalValue : lastValues[valueIndex];
        var x = valueIndex / (values.length - 1) * width;
        var y = height - value / maxValue * height;
        if (lineChart) {
          return (valueIndex === 0 ? "M" : "L") + x + "," + y;
        }
        return x + " " + y;
      });
      if (!lineChart) {
        points.push(width + " " + height + " 0 " + height);
      }
      data.push({
        label,
        points: points.join(" "),
        color
      });
    });
    return data.reverse();
  };
  _proto.getVerticalLines = function getVerticalLines() {
    var _this$params3 = this.params, datasets = _this$params3.datasets, width = _this$params3.width;
    var lines = [];
    if (!datasets.length) {
      return lines;
    }
    var values = datasets[0].values;
    values.forEach(function(value, valueIndex) {
      var x = valueIndex / (values.length - 1) * width;
      lines.push(x);
    });
    return lines;
  };
  _proto.toggleDataset = function toggleDataset(index2) {
    var _this2 = this;
    var hiddenDatasets = this.hiddenDatasets, toggleDatasets = this.params.toggleDatasets;
    if (!toggleDatasets)
      return;
    if (hiddenDatasets.includes(index2)) {
      hiddenDatasets.splice(hiddenDatasets.indexOf(index2), 1);
    } else {
      hiddenDatasets.push(index2);
    }
    if (this.$legendEl) {
      this.$legendEl.find(".area-chart-legend-item").removeClass("area-chart-legend-item-hidden");
      hiddenDatasets.forEach(function(i) {
        _this2.$legendEl.find('.area-chart-legend-item[data-index="' + i + '"]').addClass("area-chart-legend-item-hidden");
      });
    }
    this.update({}, true);
  };
  _proto.formatAxisLabel = function formatAxisLabel(label) {
    var formatAxisLabel2 = this.params.formatAxisLabel;
    if (formatAxisLabel2)
      return formatAxisLabel2.call(this, label);
    return label;
  };
  _proto.formatLegendLabel = function formatLegendLabel(label) {
    var formatLegendLabel2 = this.params.formatLegendLabel;
    if (formatLegendLabel2)
      return formatLegendLabel2.call(this, label);
    return label;
  };
  _proto.calcLinesOffsets = function calcLinesOffsets() {
    var lines = this.svgEl.querySelectorAll("line");
    this.linesOffsets = [];
    for (var i = 0; i < lines.length; i += 1) {
      this.linesOffsets.push(lines[i].getBoundingClientRect().left);
    }
  };
  _proto.formatTooltip = function formatTooltip() {
    var self = this;
    var currentIndex = self.currentIndex, hiddenDatasets = self.hiddenDatasets, _self$params = self.params, datasets = _self$params.datasets, axisLabels = _self$params.axisLabels, formatTooltip2 = _self$params.formatTooltip, formatTooltipTotal = _self$params.formatTooltipTotal, formatTooltipAxisLabel = _self$params.formatTooltipAxisLabel, formatTooltipDataset = _self$params.formatTooltipDataset;
    if (currentIndex === null)
      return "";
    var total = 0;
    var currentValues = datasets.filter(function(dataset, index2) {
      return !hiddenDatasets.includes(index2);
    }).map(function(dataset) {
      return {
        color: dataset.color,
        label: dataset.label,
        value: dataset.values[currentIndex]
      };
    });
    currentValues.forEach(function(dataset) {
      total += dataset.value;
    });
    if (formatTooltip2) {
      return formatTooltip2({
        index: currentIndex,
        total,
        datasets: currentValues
      });
    }
    var labelText = formatTooltipAxisLabel ? formatTooltipAxisLabel.call(self, axisLabels[currentIndex]) : this.formatAxisLabel(axisLabels[currentIndex]);
    if (!labelText)
      labelText = "";
    var totalText = formatTooltipTotal ? formatTooltipTotal.call(self, total) : total;
    var datasetsText = currentValues.length > 0 ? '\n      <ul class="area-chart-tooltip-list">\n        ' + currentValues.map(function(_ref4) {
      var label = _ref4.label, color = _ref4.color, value = _ref4.value;
      var valueText = formatTooltipDataset ? formatTooltipDataset.call(self, label, value, color) : (label ? label + ": " : "") + value;
      return '\n              <li><span style="background-color: ' + color + ';"></span>' + valueText + "</li>\n            ";
    }).join("") + "\n      </ul>" : "";
    return '\n      <div class="area-chart-tooltip-label">' + labelText + '</div>\n      <div class="area-chart-tooltip-total">' + totalText + "</div>\n      " + datasetsText + "\n    ";
  };
  _proto.setTooltip = function setTooltip() {
    var self = this;
    var app = self.app, el = self.el, svgEl = self.svgEl, hiddenDatasets = self.hiddenDatasets, currentIndex = self.currentIndex, _self$params2 = self.params, tooltip = _self$params2.tooltip, datasets = _self$params2.datasets;
    if (!tooltip)
      return;
    var hasVisibleDataSets = datasets.filter(function(dataset, index2) {
      return !hiddenDatasets.includes(index2);
    }).length > 0;
    if (!hasVisibleDataSets) {
      if (self.f7Tooltip && self.f7Tooltip.hide)
        self.f7Tooltip.hide();
      return;
    }
    if (currentIndex !== null && !self.f7Tooltip) {
      self.f7Tooltip = app.tooltip.create({
        trigger: "manual",
        containerEl: el,
        targetEl: svgEl.querySelector('line[data-index="' + currentIndex + '"]'),
        text: self.formatTooltip(),
        cssClass: "area-chart-tooltip"
      });
      if (self.f7Tooltip && self.f7Tooltip.show) {
        self.f7Tooltip.show();
      }
      return;
    }
    if (!self.f7Tooltip || !self.f7Tooltip.hide || !self.f7Tooltip.show) {
      return;
    }
    if (currentIndex !== null) {
      self.f7Tooltip.setText(self.formatTooltip());
      self.f7Tooltip.setTargetEl(svgEl.querySelector('line[data-index="' + currentIndex + '"]'));
      self.f7Tooltip.show();
    } else {
      self.f7Tooltip.hide();
    }
  };
  _proto.setCurrentIndex = function setCurrentIndex(index2) {
    if (index2 === this.currentIndex)
      return;
    this.currentIndex = index2;
    this.$el.trigger("areachart:select", {
      index: index2
    });
    this.emit("local::select areaChartSelect", this, index2);
    this.$svgEl.find("line").removeClass("area-chart-current-line");
    this.$svgEl.find('line[data-index="' + index2 + '"]').addClass("area-chart-current-line");
    this.setTooltip();
  };
  _proto.onLegendClick = function onLegendClick(e) {
    var index2 = parseInt(dom7_default(e.target).closest(".area-chart-legend-item").attr("data-index"), 10);
    this.toggleDataset(index2);
  };
  _proto.onMouseEnter = function onMouseEnter2() {
    this.calcLinesOffsets();
  };
  _proto.onMouseMove = function onMouseMove(e) {
    var self = this;
    if (!self.linesOffsets) {
      self.calcLinesOffsets();
    }
    var currentLeft = e.pageX;
    if (typeof currentLeft === "undefined")
      currentLeft = 0;
    var distances = self.linesOffsets.map(function(left) {
      return Math.abs(currentLeft - left);
    });
    var minDistance = Math.min.apply(Math, distances);
    var closestIndex = distances.indexOf(minDistance);
    self.setCurrentIndex(closestIndex);
  };
  _proto.onMouseLeave = function onMouseLeave2() {
    this.setCurrentIndex(null);
  };
  _proto.attachEvents = function attachEvents2() {
    var svgEl = this.svgEl, $el = this.$el;
    if (!svgEl)
      return;
    svgEl.addEventListener("mouseenter", this.onMouseEnter);
    svgEl.addEventListener("mousemove", this.onMouseMove);
    svgEl.addEventListener("mouseleave", this.onMouseLeave);
    $el.on("click", ".area-chart-legend-item", this.onLegendClick);
  };
  _proto.detachEvents = function detachEvents2() {
    var svgEl = this.svgEl, $el = this.$el;
    if (!svgEl)
      return;
    svgEl.removeEventListener("mouseenter", this.onMouseEnter);
    svgEl.removeEventListener("mousemove", this.onMouseMove);
    svgEl.removeEventListener("mouseleave", this.onMouseLeave);
    $el.off("click", ".area-chart-legend-item", this.onLegendClick);
  };
  _proto.render = function render15() {
    var self = this;
    var _self$params3 = self.params, lineChart = _self$params3.lineChart, toggleDatasets = _self$params3.toggleDatasets, width = _self$params3.width, height = _self$params3.height, axis = _self$params3.axis, axisLabels = _self$params3.axisLabels, legend = _self$params3.legend, datasets = _self$params3.datasets;
    var chartData = self.getChartData();
    var verticalLines = self.getVerticalLines();
    var visibleLegends = self.getVisibleLabels();
    var LegendItemTag = toggleDatasets ? "button" : "span";
    return jsx_default2("div", null, jsx_default2("svg", {
      xmlns: "http://www.w3.org/2000/svg",
      width,
      height,
      viewBox: "0 0 " + width + " " + height,
      preserveAspectRatio: "none"
    }, chartData.map(function(data) {
      return lineChart ? jsx_default2("path", {
        stroke: data.color,
        "fill-rule": "evenodd",
        d: data.points
      }) : jsx_default2("polygon", {
        fill: data.color,
        "fill-rule": "evenodd",
        points: data.points
      });
    }), verticalLines.map(function(line, index2) {
      return jsx_default2("line", {
        "data-index": index2,
        fill: "#000",
        x1: line,
        y1: 0,
        x2: line,
        y2: height
      });
    })), axis && jsx_default2("div", {
      class: "area-chart-axis"
    }, axisLabels.map(function(label) {
      return jsx_default2("span", null, visibleLegends.includes(label) && jsx_default2("span", null, self.formatAxisLabel(label)));
    })), legend && jsx_default2("div", {
      class: "area-chart-legend"
    }, datasets.map(function(dataset, index2) {
      return jsx_default2(LegendItemTag, {
        "data-index": index2,
        class: "area-chart-legend-item " + (toggleDatasets ? "area-chart-legend-button" : ""),
        _type: toggleDatasets ? "button" : void 0
      }, jsx_default2("span", {
        style: "background-color: " + dataset.color
      }), self.formatLegendLabel(dataset.label));
    })));
  };
  _proto.update = function update22(newParams, onlySvg) {
    if (newParams === void 0) {
      newParams = {};
    }
    if (onlySvg === void 0) {
      onlySvg = false;
    }
    var self = this;
    var params = self.params;
    Object.keys(newParams).forEach(function(param) {
      if (typeof newParams[param] !== "undefined") {
        params[param] = newParams[param];
      }
    });
    if (self.$svgEl.length === 0)
      return self;
    self.detachEvents();
    self.$svgEl.remove();
    if (!onlySvg) {
      self.$axisEl.remove();
      self.$legendEl.remove();
    }
    var $rendered = dom7_default(self.render());
    var $svgEl = $rendered.find("svg");
    extend(self, {
      svgEl: $svgEl && $svgEl[0],
      $svgEl
    });
    if (!onlySvg) {
      var $axisEl = $rendered.find(".area-chart-axis");
      var $legendEl = $rendered.find(".area-chart-legend");
      extend(self, {
        $axisEl,
        $legendEl
      });
      self.$el.append($axisEl);
      self.$el.append($legendEl);
    }
    self.$el.prepend($svgEl);
    self.attachEvents();
    return self;
  };
  _proto.init = function init46() {
    var self = this;
    var $rendered = dom7_default(self.render());
    var $svgEl = $rendered.find("svg");
    var $axisEl = $rendered.find(".area-chart-axis");
    var $legendEl = $rendered.find(".area-chart-legend");
    extend(self, {
      svgEl: $svgEl && $svgEl[0],
      $svgEl,
      $axisEl,
      $legendEl
    });
    self.$el.append($svgEl);
    self.$el.append($axisEl);
    self.$el.append($legendEl);
    self.attachEvents();
    return self;
  };
  _proto.destroy = function destroy50() {
    var self = this;
    if (!self.$el || self.destroyed)
      return;
    self.$el.trigger("piechart:beforedestroy");
    self.emit("local::beforeDestroy areaChartBeforeDestroy", self);
    self.detachEvents();
    self.$svgEl.remove();
    self.$axisEl.remove();
    self.$legendEl.remove();
    if (self.f7Tooltip && self.f7Tooltip.destroy) {
      self.f7Tooltip.destroy();
    }
    delete self.$el[0].f7AreaChart;
    deleteProps(self);
    self.destroyed = true;
  };
  return AreaChart2;
}(class_default);
var area_chart_class_default = AreaChart;

// node_modules/framework7/esm/components/area-chart/area-chart.js
var area_chart_default = {
  name: "areaChart",
  params: {
    areaChart: {
      el: null,
      lineChart: false,
      datasets: [],
      axis: false,
      axisLabels: [],
      tooltip: false,
      legend: false,
      toggleDatasets: false,
      width: 640,
      height: 320,
      maxAxisLabels: 8,
      formatAxisLabel: null,
      formatLegendLabel: null,
      formatTooltip: null,
      formatTooltipAxisLabel: null,
      formatTooltipTotal: null,
      formatTooltipDataset: null
    }
  },
  create: function create69() {
    var app = this;
    app.areaChart = ConstructorMethods({
      defaultSelector: ".area-chart",
      constructor: area_chart_class_default,
      app,
      domProp: "f7AreaChart"
    });
    app.areaChart.update = function update22(el, newParams) {
      var $el = dom7_default(el);
      if ($el.length === 0)
        return void 0;
      var areaChart = app.areaChart.get(el);
      if (!areaChart)
        return void 0;
      areaChart.update(newParams);
      return areaChart;
    };
  }
};

// node_modules/framework7/esm/components/elevation/elevation.js
var elevation_default = {
  name: "elevation"
};

// node_modules/framework7/esm/components/typography/typography.js
var typography_default = {
  name: "typography"
};

// node_modules/framework7/esm/framework7-bundle.js
router_class_default.use([component_loader_default]);
app_class_default.use([device_default, support_default, utils_default, resize_default, request_default2, touch_default, clicks_default, router_default, history_default, component_default, service_worker_default, store_default, statusbar_default, view_default, navbar_default, toolbar_default, subnavbar_default, touch_ripple_default, modal_default, appbar_default, dialog_default, popup_default, login_screen_default, popover_default, actions_default, sheet_default, toast_default, preloader_default, progressbar_default, sortable_default, swipeout_default, accordion_default, contacts_list_default, virtual_list_default, list_index_default, timeline_default, tabs_default, panel_default, card_default, chip_default, form_default, input_default, checkbox_default, radio_default, toggle_default, range_default, stepper_default, smart_select_default, grid_default, calendar_default, picker_default, infinite_scroll_default, pull_to_refresh_default, lazy_default, data_table_default, fab_default, searchbar_default, messages_default, messagebar_default, swiper_default, photo_browser_default, notification_default, autocomplete_default, tooltip_default, gauge_default, skeleton_default, menu_default, color_picker_default, treeview_default, text_editor_default, pie_chart_default, area_chart_default, elevation_default, typography_default]);
var framework7_bundle_default = app_class_default;

// dep:framework7_bundle
var framework7_bundle_default2 = framework7_bundle_default;
export {
  jsx_default as $jsx,
  component_class_default as Component,
  dom7_default as Dom7,
  create_store_default as createStore,
  framework7_bundle_default2 as default,
  getDevice,
  getSupport,
  request_default as request,
  utils_exports as utils
};
//# sourceMappingURL=framework7_bundle.js.map
